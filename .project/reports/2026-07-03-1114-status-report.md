# Status Report — 2026-07-03

**Requested**: standard status + a fresh look at what is now on `main`.

## Headline: the ground shifted under the current branch

`main` moved two commits past this branch's base while the branch sat still, and the newer of the two — **#100** — changes the compute backend in a way that undercuts what this branch is for.

- **#99** `b34e4567` — pre-warm JAX at lifespan startup to kill the first-user 502.
- **#100** `63f8cd12` — serve **1costingfe v0.1.1 (numpy-only)** from a GitHub tag tarball; **drop jax/jaxlib from the deploy.**

The current branch, `feat/compute-concurrency-semaphore`, exists to cap **concurrent JAX allocation** (a `threading.Semaphore` bounding `forward()` to 2 in flight). If the deployed serving path is now numpy-only, the JAX memory-explosion the semaphore guards against may no longer be the production memory profile. **This needs a decision before the branch is worth landing** — see "What needs a decision."

## Branch / working-tree state

| Fact | Detail |
|------|--------|
| Current branch | `feat/compute-concurrency-semaphore` |
| Commits ahead of `main` | **0** — the branch is exactly at base `bd4c403d` |
| Commits behind `origin/main` | **2** (#99, #100) |
| Semaphore code | **uncommitted** in the working tree (`M server.py`, +20/−7) — not a commit |
| Local `main` | behind `origin/main` by 2 (not pulled) |

The Layer-3 semaphore described in `CURRENT_WORK.md` as "implemented, ready to PR" is **sitting as an uncommitted working-tree edit**, not a commit on the branch. `_MAX_CONCURRENT_FORWARD`, `_FORWARD_SEMAPHORE`, and the acquire/finally wrap are present in `server.py:161–164, 1091` but nothing is committed.

The edit was written against `bd4c403d`, which does **not** contain #99's JAX warm-up. `origin/main`'s `server.py` now has warm-up code around line 1013. So a rebase/merge will put the semaphore edit next to the new warm-up path — they interact (both concern JAX + `forward()`), and the line numbers in the plan doc have moved.

## Stale entries in CURRENT_WORK.md (last updated 2026-06-28)

- **"Compute OOM — debounce + cache quantization (implemented, ready to PR)"** — this **already merged** as **#98** (`eb3ff0cc`, on `origin/main`). Layer 1+2 is done, not pending.
- Nothing in `CURRENT_WORK.md` mentions #99 or #100, both of which landed after the file's last update and both of which touch the same compute surface as the active OOM work.
- Two new remote branches are in flight and untracked in the doc:
  - `origin/feat/explorer-jax-warmup` — has an **unmerged** extra commit `10e8da8a` (parallel warm-up, JAX persistent cache, retry-on-502 spinner) on top of the merged #99.
  - `origin/chore/1costingfe-0.1.1-numpy-backend` — the source of #100 (`ff6ce19f`), now merged.

## What needs a decision

1. **Is the JAX-allocation semaphore still needed after the numpy-only migration (#100)?**
   - If serving is genuinely numpy-only, the semaphore guards a memory profile that no longer exists in production → the branch may be **obsolete**, or its rationale needs to change to "bound peak numpy allocation" (different sizing math than `2×per-forward JAX`).
   - Note the tension: `origin/main`'s `server.py` still runs **JAX warm-up** (#99) even though #100 dropped jax/jaxlib from the deploy manifest. Worth confirming what actually loads at runtime before deciding — the two commits may not be fully reconciled.

2. **If it's still needed**, the working-tree edit must be (a) committed, (b) rebased onto current `origin/main`, and (c) re-verified against the new warm-up code — the "verified against base `bd4c403d`" note in `CURRENT_WORK.md` is now against a stale base.

## Backlog / loose ends (unchanged, still open)

From the v0.1.0 migration, all small and non-blocking:
- Concept **27** (polywell) serves stale data — concept-side routing-config bug.
- FR-SO1 regression test's `>5%` override-impact assertion is stale (premised on the old ~18%; post-recalibration it's ~1.26%).
- Item 2-FU: re-extract **37 & 39** after concept-side `model_setup.py` fixes (both still carry 0 override records).

Phase-1 of Explorer UX v3 is done and verified on `main`. The next epic item by leverage is **D1** (per-account override decomposition).

## Recommended next actions

1. **Decide the semaphore's fate first** (item 1 above) — don't rebase/commit dead work. This is the one thing blocking a clean branch state.
2. If keeping it: commit the working-tree edit, `git pull` main, rebase, re-verify against the #99 warm-up path, then PR.
3. **Refresh `CURRENT_WORK.md`**: mark debounce+quantize (#98) as merged, record #99/#100, note the two new branches. It's ~5 days stale on a fast-moving surface.
4. The migration loose ends (27, FR-SO1 assertion, 37/39) are P2/trivial — bundle them, don't spend a session each.
