# Align — Goal Harness Item 2 (Native Research Acquisition and Registration Seam)

**Date**: 2026-08-25 · **Owner**: Reid W · **Orchestrator**: Fable (`/_my_orchestrate`)

Owner rulings at launch (`[OWNER 2026-08-25]`):

1. **Branch**: dedicated worktree `../fusion-tea-goal-research-seam`, branch `feat/goal-research-seam` based on `fbfe430a` (pre-Item-1 launch), so the branch carries only Item 2 work. Item 1 runs in parallel on `feat/run-study-first-consumer` and stays out of research-seam files.
2. **Non-Zotero manifest identity** (concept open Q10): delegated to design — "make sure you use your judgement."
3. **DI gate**: separate operations and an explicit approval gate confirmed. Acquisition may register sources but must not automatically mint DIs. This does not require a separate session or goal round: an approved DI may be created later in the same broader workflow through the native research-approval operation.
4. **Reserved gates**: none beyond standing defaults (merge/push, item close stay owner-held). "Just get it done. You are accountable for final quality."

Orchestrator readings recorded at Align (`[AGENT]`, unchallenged):
- Live network acquisition proof belongs to epic Item 5; Item 2 tests use fixtures/local files where possible.
- Changes inside pinned `agentic-mbse` are upstream filings, never in-repo edits; the seam is a fusion-tea-side wrapper/extension.
- Item 2 does not touch CLAUDE.md, the run-study runbook, DISCOVERY_LOG, GOAL_RUNBOOK, or ADR files; epic text is cited rather than Item 1's in-flight artifacts. ADR-home adoption is coordinated when this item files decisions (per Item 1's align §3).
- `spec_review` and `design_review` run as fresh stage sessions.
