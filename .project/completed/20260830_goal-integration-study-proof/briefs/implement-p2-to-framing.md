# Resume 5 — Phase 1 committed; run Phase 2 up to the owner framing gate, then stop

Your Phase-1 close is committed (trail + plan + the P7 note about `assert_read_set_covered`).
Phase 2 now, per plan § Phase 2 — but ONLY through the framing argument. The owner's ruling
(runbook step 4) is a hard gate; the orchestrator carries the framing to the owner and brings
the ruling back. You do not run points in this leg.

## This leg

- Write the study's `### T-00N scope` and `### T-00N start` in the trail (task chosen from
  evidence per D3 — the study task).
- `/run-study` execute-mode preparation through runbook steps 1–4:
  - step 1–2: mint the study id, declare candidate axes as qualified entry-key groups —
    choose axes from the goal's § Question (where does the `recirc_ok` fence move across the
    swept space) and the comparand record `20260821-power-cycle-ab` (its axes were R and a;
    the fence geography it published is R ≤ 8.0/6.5/5.5 at a = 0.8 against threshold 0.5).
    Respect goal invariants: `p_pump` is NOT an axis (owner-ruled held input).
  - step 3: run indicators for every proposed axis, including any you decline.
  - step 4: write the framing argument — but do NOT self-rule it.
- STOP and report: study id, declared groups, indicator results verbatim, the framing
  argument, your recommended framing, and any protocol rulings the runbook wants from the
  user at this point. The orchestrator takes it to the owner.

## Hard rules unchanged

No commits. No points run. Prior committed records untouched. Cite-don't-mirror in the trail.
The store goes beside the record directory; nothing under the record dir until execution is
authorized. End with the stop report.
