# Resume 7 — option 1 ruled; the mask precedent already exists; finish the run

Rulings (orchestrator, execution-detail tier, both grounded in the comparand's committed record):

1. **Option 1 — disclose and exclude.** Pre-screen `p_net > 0` with the package-owned oracle
   over the proposed grid; exclude the unevaluable points; re-run step 9 over the evaluable
   set; report the staircase boundary as a result. The window stays the owner-adopted one —
   the exclusion is an evaluability bound, not a window change.
2. **This is not a new precedent, so it needed no owner ruling.** The comparand's own § 11
   already derived its density floor from oracle scan + probes and disclosed it as "the
   package's evaluability limit, not a design screen," with points below not proposed
   (`20260823-magnet-technology-ab/record.md:224-226`). Yours is the same pattern with a 2-D
   staircase instead of a 1-D floor. Cite that section as the pattern's authority in the
   record's disclosure.
3. **Correction to your finding plan — do not mint.** The mechanism you describe is already
   on the log as `20260823-magnet-technology-ab#1` (record.md:258): net_positive can never
   read violated; the CAS10 land term's sqrt fails first; routed "modeling item — not
   minted". What is new at this pin is narrower and real: **at 195 MW the unobservable
   region intrudes 42 points into the comparand's standard window** (it was entirely outside
   the evaluated space at 1.0 MW). Record that as this study's sighting row citing
   `20260823-magnet-technology-ab#1` per the log's join conventions — executor's pen, but
   anchored to the existing id's mechanism, not duplicating it.
4. **Administrator boundary — agreed as you proposed**: seam-run history, re-pin commits, and
   the refusal-first sequencing stay out of the record; the record discloses the bound, how
   it is derived (oracle pre-screen, § 11-pattern), and its boundary, on its merits.

## Work

- Oracle pre-screen; exclusion list as a results/ artifact (labelled, with the boundary
  staircase); re-run step 9 over the evaluable set (reuse the completed store cases if the
  tooling permits — your call, mechanics are yours; state what you did).
- Steps 10–14: oracle sample verification; rec_frac/p_net operands artifact; near-fence
  re-evaluation rule; record.md written complete (protocol rulings section carries the owner's
  three framing rulings AND this option-1 ruling with its precedent citation); findings per
  ruling 3 plus anything genuinely new; discovery-log sighting rows (executor's pen).
- Validation: `uv run python -m pytest tests/study/test_records.py` green incl. the join test;
  snapshot names the T-003 pin + fingerprints exactly.
- Trail: T-004 execution events + return per your judgment of outcome class (a completed
  record with a disclosed adverse-leaning bound is what it is — classify honestly).
- Hard rules unchanged: no commits; no administering/synthesis (Phase 3 is fresh); exclude
  __pycache__ from the would-be commit list. End with: point counts (evaluated/excluded/
  masked), verification outcome, findings registered (ids), T-004 return class, file list
  grouped for the record commit.
