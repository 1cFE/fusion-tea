The design_review is complete: verdict Revise. Read `.project/active/goal-integration-seam/design_review.md` in full (committed). The shape (prove-don't-perform, D1-D12 skeleton) survives; fix all eight must-fix findings and take the advisory items unless you record a reason not to. Verify each review claim against the cited code before acting — the reviewer is fresh too.

Also: the B2 spike returned while you were away — read `spike_snapshot_stability.md` (committed). B2 is CONFIRMED stronger than bet: recapture is byte-identical (v6 envelope, no `captured_at` at all; the WI-029 measurement was of the dead pre-v6 format). Gate 4 compares whole-file bytes, no exclusion. The spike already wrote back-references into design.md — keep them coherent with your revision.

Orchestrator steering, recorded `[AGENT]`:

- **M1 (gate 5 repo-scoped)**: In production the seam runs against the real tracked package, where the spine suite judging the repo IS correct. State gates 1a and 5 as repo-scoped producers explicitly, and derive what that means for D10's workspace: the workspace materializes the committed state, so repo-scoped gates remain meaningful exactly when workspace == committed state — make that an asserted precondition, not an accident. For the R-G4 refusal fixture, do not edit tracked files (R-G3) and do not edit the suite (R-B2): pick refusal fixtures from gates that accept caller-supplied inputs (gate 4's `--census-file` is the natural stale-census refusal home; gate 6/7 via doctored workspace manifest). If gate 5's refusal genuinely cannot be exercised hermetically, record that as a stated test-coverage boundary with the reason, per R-G4's spirit rather than its letter — and say so loudly.
- **M2**: the seam's gate-0 precondition sweep should own ALL environment preconditions (SYSIDE_LICENSE_KEY, STOP_PARSER_*_WHEEL*, STOP_PARSER_TEAX_ROOT) and classify absence as could-not-run before any producer is invoked — one place, one rule.
- **M3 (restore)**: prefer one restore mechanism that works in both git-visible and gitignored contexts. The rejected copy-the-tree option earns its place now that git cannot restore in the harness: a package-tree backup into `--out-dir/_backup` before the first mutating gate (~150 files, cheap per the spikes' runtimes), restore-by-copy on byte-movement refusal. Re-derive D7 honestly rather than patching its text.
- **M7 (refused vs PREREQUISITE)**: do NOT import the goal layer's task-return vocabulary into the native seam — that's an ownership boundary. Instead make the blocker detail sufficient for the caller to classify (e.g. a stable `condition` the operator guide and Item 6 can map: package-not-integrated is the modeling item's unfinished work, i.e. the goal layer's PREREQUISITE), and state the mapping in the operator guide. The seam stays two-class; the goal layer reads the detail.
- M4 (stderr discriminator), M5 (restore the read-set assertion or record why not), M6 (give the lineage comparison a schema home — if that means a ninth explicit gate row, take it and fix the eight-vs-nine advisory with it), M8 (define the subprocess env: inherited, plus the explicit vars, documented in the guide): your call on mechanism, record each.
- The two R-F5 filings the lens demands (M1: spine suite takes no package argument; M4: verify.py writes no summary on failure) go into Integration Strategy's filing list.

Amend design.md in place; append a Resolutions block to design_review.md mapping each finding to what changed. Do not commit. End with `ARTIFACT: .project/active/goal-integration-seam/design.md`.
