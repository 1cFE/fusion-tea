# Product-Lens Ledger — goal-research-model-proof (GSTH Item 5)

Append-only. One block per run; a changed gate is a new dated block. Resolution is by citation of a finding id, never by recency.

## close — 2026-08-28 — rev `.project/completed/20260828_goal-research-model-proof/` (archived at close; audited at `eef3865c`, findings fixed at `c389afc1`)

Epic: GSTH (`.project/backlog/epic_goal_strategy_task_harness.md` § Item 5)

**Instruction-provenance note.** `~/.claude/scripts/product-lens.md` was not readable in this session (Read denied, non-interactive so no grant was possible) — the same limitation the Item 1 audit-hop, Item 3, and Item 4 blocks record. This block follows the §3 format as evidenced by `.project/completed/20260827_goal-cold-pickup-proof/product-lens.md` and `.project/completed/20260827_goal-integration-seam/product-lens.md`. The §4 smell list was not readable; the smells checked are named explicitly below.

**Ledger hop, not a build hop.** The lens is run once here, at close, rather than at spec and again at implementation. `spec-review.md:30` (finding L1-5) flagged the dangling `product-lens.md` reference at spec time and it was never created; `audit.md` § Product Judgment records the gate as **NOT RUN** and states that the audit does not clear it. This block is what clears it, and it says so rather than backdating a spec-hop block that never happened.

**Point (re-derived from SOURCES, independently of the spec's and audit's framing):** The owner's requirement for this layer is that criticism sits *before* work compounds on a misread — `[OWNER-VERBATIM]` "Study > Analysis > Dispositions Plan / Critic looks at the analysis and plan and can push back on either/both / Those docs get updated and re-reviewed, looping as necessary" (`.project/concepts/goal-driven-model-development-harness.md:33`), formalized as SC 5: the critic is never the author's session, the analysis and plan loop until a pass or a declared cap, and the run directory records each critique and what changed (`:47`). Beside it sits SC 4: research and extraction are one stage, every source registered and MR-4-citable, "**No hand-written index entries remain in the path**" (`:46`). Item 1 wrote both into `GOAL_RUNBOOK.md`; Item 2 built the seam; neither had ever run live. Item 5 is the run that decides whether the pre-execution checkpoint actually binds on a real question and whether re-authorized modeling actually flows from a native research return. [source: `.project/concepts/goal-driven-model-development-harness.md` § Owner's Words (critic placement, patterns) and SC 2, 4, 5, 6; `.project/backlog/epic_goal_strategy_task_harness.md` § Item 5 (Objective, Scope 2–4, Success Criteria) and § Hardening rule; `.project/adr/003-lean-first-persistence.md`, `004-finding-disposition.md`, `005-review-topology.md`; `work/orchestration/GOAL_RUNBOOK.md`; grade: owner / owner-ratified agent decomposition]

**Falsifier:** Item 5 closes reading as though the epic's Item 5 objective — a `model → research → model` sequence under one unchanged strategy — were discharged, when the research half never ran; or a criterion is satisfied by a substitute; or a shortfall is softened somewhere a reader will actually look; or the contract was repaired mid-run so the route scores better than it is.

**Does it fire?** Not on the item's own records, and the strongest evidence is what the round declined to do. `T-001` found the repository could answer the question and returned `COMPLETE` instead of manufacturing a research need to light up criteria 3, 4 and 7. The covering branch that outcome was graded against was committed at `e02ce403`, before the round opened, and the audit re-read the blob rather than the record's summary of it. `git diff e44498d4..HEAD -- '*GOAL_RUNBOOK.md'` is 0 lines: the contract was left alone rather than amended to make a criterion pass. `verification_record.md` says in every relevant row that the seam never ran, and `audit.md` states the limit in its own words ("the epic's Item 5 heading should not be read as 'the seam is proven'").

It fires in two places *outside* the item directory, where a reader who never opens the archive would be told otherwise. Both are recorded below and both are fixed in this same close hop.

### Findings

- **close-F1 [DO] — the epic tells Item 6 that Item 5 delivered research it did not deliver.** `epic_goal_strategy_task_harness.md:439`, in Item 6's Current State, reads: "✅ Item 5 supplies a grounded goal, audited model work, disposition history, and accepted research/model learning." Three of those four are real; "accepted research/model learning" is not — the Item 2 seam was never invoked (criterion 3, `knowledge/` diff vs base empty), no work item was minted (criterion 4, `work/` untouched), and no model change landed (`p_pump` is still `1.0` at `models/designs/stellarator_09/stellarator_plant.sysml:502`). The audit's honest scope note sits at `:405`, inside Item 5's own Success Criteria; nothing carried it forward to the line the *next* item on the critical path reads as its green light. A cold agent starting Item 6 from its Current State block would plan against a research return that does not exist. — `.project/backlog/epic_goal_strategy_task_harness.md:439` versus `:405` (**the audit's own scope note**), `verification_record.md` rows 3/4, `audit.md` § Spec conformance rows 3–4 — **disposition: FIXED this hop** (the `:439` line now names what Item 5 actually supplied and points at what it did not; Item 6's dependency line carries the same correction).

- **close-F2 [DO] — the defect Item 5 measured would live only inside an archive.** `GOAL_RUNBOOK.md:256`/`:264` still routes a round's research disposition to the WI-031 hand pattern — stale since Item 2 shipped the seam — and `work/orchestration/goals/p-pump-basis/goal.md:130` carries the same instruction into the goal file. That is precisely the shape SC 4 forbids ("no hand-written index entries remain in the path"): the next operator to hit a research disposition would compensate by hand for a producer boundary that already exists. Item 5 measured this and recorded it (`verification_record.md` § Failures 1), and correctly did not repair it — R-G3 makes the flip rest on a live seam run, and no run happened, so criterion 7 was branch-cancelled. But once this item is archived, the only live artifact carrying the warning is a verification record under `.project/completed/`, and the runbook itself carries no marker at all. The measurement outliving the archive is the whole point of recording it. — `work/orchestration/GOAL_RUNBOOK.md:256,264`; `work/orchestration/goals/p-pump-basis/goal.md:130`; `.project/concepts/goal-driven-model-development-harness.md:46` (SC 4, **owner**); `verification_record.md` § Failures 1 — **disposition: DISPOSE-and-proceed, done this hop** (the carried gap is written into `.project/CURRENT_WORK.md` § Recently Completed and into the epic's Item 6 Current State, where the next item's operator reads it; repairing the runbook row stays owed to whichever item next runs the seam live, as the record says. Not raised to BLOCK: the omission is not Item 5's work to fix, and the fix available at close — putting the warning where a live reader will find it — is fully within this hop).

### Reverse check

**CLEAR.** Every artifact in the archive traces to a spec criterion, a design invariant, or an owner-requested deliverable: `covering-branches.md` to R-B3/R-H4 (declare the covering branches before the run), `freshness-record.md` and `operator-notes.md` to the Item 4 pattern the spec adopts, `sessions/` to the freshness evidence the proof rests on, `verification_record.md` to the epic's Deliverables list. No orphan work. Nothing from the hardening path entered the build — the diff vs `e44498d4` is 58 files and contains no function, no test, and no tool.

### Smells (product-lens spec §4, enumerated from the Item 4 block since the spec file was unreadable)

- *A test passes only because it selects one favorable route* — **checked hardest, does not fire.** The available substitution was letting the round invent a source need after `T-001` came back `COMPLETE`, and grading criteria 3/4/7 as met. It was refused, and the refusal is ancestry-checked: `covering-branches.md@e02ce403` and its `08af1532` amendment are both ancestors of `C-T001` `71d2abe8`.
- *A special category exempts a case whose meaning is unchanged* — **fires weakly on criterion 1's retirement, and is disclosed rather than hidden.** The category ("unreachable by construction on a deliberately chosen need") was named after the miss, not before it. Three things keep it honest: the ruling is the owner's, at a real commit (`briefs/implement_resume_gate_a.md@c8362239` Ruling 1, verified by the audit as an ancestor of the session-02 brief); the box stays `[ ]` in both the spec and the epic rather than being marked met; and the measurement that produced it is kept as the item's own finding — "a recorded gap is a readable gap; a need selected because it is documented cannot yield blind discovery" (`verification_record.md` § Failures 3). Recorded as a note, not a finding, because nothing here reads as a pass.
- *Consumer compensating for a producer or platform guarantee* — **fires on close-F2**, folded into that finding rather than left in the rubric.
- *A baseline preserves behavior contradicting the product's reason for existing* — fires weakly on the same stale runbook row; same fold.
- *Two representations manually kept in sync* — does not fire. The trail cites native state by `<path>@<sha>` and restates none of it; the discovery-log join is machine-checked (`tests/study/test_records.py`, 7 passed) and removed-line count is 0.
- *Correctness depends on downstream knowledge of an internal representation* — does not fire. The fresh reviewer closed the round from the goal directory and its citations alone (session `cbc65841`, which authored nothing prior).
- *Ownership change without saying so* — does not fire. The goal never wrote modeling-PM state; gates 2/3/4 were referred to the owner with a recommendation, and the follow-up landed as WI-033 in the modeling PM's own registry, not as a mirror in the goal directory.

### Notes (no finding)

- **The item's best product outcome is a negative one, and it is the reason the round was worth running.** The bet under criterion 1 was that a bounded model task would discover a prerequisite blind. It measured false, and the cause generalizes: any need selected *because* it is documented carries its own answer in the evidence pointers the grounding step is required to walk. That is a fact about how to design the next proof, and it is on the record in the owner's own characterization rather than smoothed away.
- **What Item 5 proves is the model → critic → disposition → review spine.** `C-001.r1` refused with three required changes, the author revised, `C-001.r2` passed, and the dispositions landed only after the pass (`2b9ee81e` after `b209766c`) — the owner's checkpoint bound and released on a live round for the first time. That is SC 5 exercised end to end, by sessions the audit confirmed distinct at the session id.
- **The seam half is unproven and now has a named home.** After an item titled "research-to-model," criteria 1, 3, 4 and 7 carry no live evidence. Whether Items 1–5 together discharge the epic is an epic-scope question the item audit explicitly declined; it belongs to Item 6's epic-evidence deliverable.

### Promise scan

**Nothing filed, and nothing hand-minted.** `.project/product/` does not exist in this repository and `.project/scripts/product.sh` is absent, so there is no ledger to write to and no id to allocate. This matches what the Item 2, Item 3, and Item 4 closes did with the same gap — note it, do not work around it. On the substance: Item 5 ships no code, no public surface, and no cross-cutting contract; the density bar would not have been met even with a ledger present. The gap is a control gap in this repository, not an omission by this item, and it is recorded here so a later ledger initialization can backfill from the archive if the owner wants it.

### Gate

**CLEAR.** Two `[DO]` findings, both dispositioned in this same hop by citation (close-F1 FIXED, close-F2 DISPOSE-and-proceed with the carry written into the live artifacts). No unresolved `BLOCK` in this ledger.

**Epic ledger state:** `.project/backlog/epic_goal_strategy_task_harness.md` § Product-Lens reads **CLEAR** (second block, 2026-08-25, owner-dispositioned decomposition; `epic-plan-F1` and `epic-plan-F2` both FIXED). This close hop does not change that gate. Note that `epic-plan-F2`'s stated basis — "Item 5 proves the author revises through that checkpoint before execution" — is the one epic-level promise Item 5 did discharge in full.
