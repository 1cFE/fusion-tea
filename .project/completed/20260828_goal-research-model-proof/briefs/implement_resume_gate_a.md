# Resume — owner rulings at gate (a) and on the premise surprise (2026-08-28)

The owner has ruled. Resume execution at Phase 2. All rulings below are
`[OWNER 2026-08-28]` unless marked otherwise; the owner's sentences are verbatim where
quoted. Stop again at the next `WAIT` boundary or on any `BLOCKED` condition, as before.

## Ruling 1 — the premise surprise: option B

Criterion 1 (the bounded task returns a "real" PREREQUISITE, discovered untipped) is
**retired as unreachable-by-construction** on any deliberately chosen need. The
measurement — recorded gap = readable gap, so a selectable need cannot yield blind
discovery — is kept as the item's finding about the goal layer. Record it with the
owner's characterization: the check was "a stupid test to begin with and was never
going to work" — i.e. it tested the wrong thing (see Ruling 2). Grade the retirement
`[OWNER 2026-08-28]`; cover it under the covering-branches row for criterion-1
non-exercise. T-001 still runs as a real bounded task and is graded on the work it
does, not on blindness.

## Ruling 2 — framing correction (propagate everywhere; capture-fidelity Rule 3: amend, don't accrete)

The owner corrected the item's framing: the workflow intent is **not** "the agent
notices a specific source is missing." It is: **the agent recognizes the repository's
current data cannot answer the question and returns "research is needed"; a bounded,
open-ended research round then runs through the seam** — search, evaluate, register
what is admissible, or return a bounded negative. Rephrase accordingly, replacing (not
annotating) the old framing wherever the run's artifacts state it: T-001's expected
return reads as "research needed to establish a defensible value," the retired-check
write-up says it tested the wrong thing and was unreachable, and no artifact frames
T-002 as fetching a known document. Moscato is one candidate the search may surface,
nothing more. The seam request stays a question with `where_to_look` and limits, as
designed.

## Ruling 3 — gate (a): the charter, approved verbatim

- § Question: **"Is `p_pump` = 1.0 MW defensible for a helium-primary loop at this
  plant scale, and if not, what sourced value should the model carry?"**
- § Answered when: **a written, sourced answer either way — a better number, or "keep
  1.0 MW, here's why" — with any model change coming back to the owner first** (a model
  change happens only through a work item at gate (b); trail-only is a complete answer).

## Operator answers under delegated authority (`[AGENT]`, consistent with the approved charter)

- The DI-008 band discrepancy (60–190 vs 30–190 MW): the goal works against DI-008 as
  written; the discrepancy is logged for the owner, not reconciled inside this goal.
- The answer's shape (scalar MW vs fraction of p_th): not fixed by the contract; the
  follow-up work item proposes it, and retiring `p_pump` as a settable input is
  explicitly gate material.
- § Close rule: the fresh round review hands the owner a recommendation; the owner's
  ruling closes the goal (the cryo-volume-basis pattern).

Session 02's brief carries Ruling 3 and the operator answers into `goal.md` (Rulings 1
and 2 are item-level, not goal-file content, except where the goal file's own phrasing
needs Ruling 2's framing). Fence discipline unchanged.
