Your design was reviewed by a fresh session: verdict Revise, artifact at
.project/active/goal-research-model-proof/design-review.md. Read it, then revise
design.md in place. Do NOT run git commits. Finish with `ARTIFACT: <path>`.

Orchestrator dispositions — apply all of these:

- M1 (checkpoint/route coherence) — ACCEPTED. Add the rule: if the executed route
  differs from the route the critic approved (D5's ruling changes it), the revised
  dispositions go back as C-001.r2 before T-002 executes. The cap still holds.
- M2 (Invariant 2 vs brief delivery) — ACCEPTED. State Item 4's mechanism explicitly:
  briefs delivered on stdin, never read from the item dir; define the read-allowlist
  (or denial rationale) for each session 03–08 so the fence sweep has a defined target.
- M3 (D8's home) — ACCEPTED. Move the class→outcome mapping out of goal.md
  § Invariants into covering-branches.md (committed ahead of the round, same
  ancestry guarantee). goal.md § Invariants stays comparison-meaning only per R-A3.
- M4 (BLOCKER retry) — ACCEPTED. A fixed request changes the request key, so per
  GOAL_RUNBOOK.md:132 it is a NEW task, not a retry. Rewrite the BLOCKER handling
  accordingly (new T-00N with its own scope, still inside the round).
- M5 (hidden bet) — ACCEPTED. Add it as B6: a registered source plausibly moves the
  premise (R-E3), not just the value — the band is ~60–190 MW vs 1.0 MW held on arms
  at rec_frac 0.94/0.79/0.68. Rebalance the likely-path narrative, D3's framing, and
  the budget/risks so advance (R-E1) and STRATEGY_BLOCKER (R-E3) are peer outcomes;
  the judgment stays the round's, under the critic and reviewer (spec R-A6).
- C1 (R-A7 accretion) — ACCEPTED. The owner ruled a REMOVAL: the design's
  CURRENT_WORK.md edit deletes the p_pump re-source from the Phase 4 close list
  (amend the sentence), with one pointer to the goal directory. Not an appended note
  beside a still-standing list member (capture-fidelity Rule 3).
- C2 (R-C2 surfacing) — ACCEPTED. Carry the spec's obligation: the runbook :140
  trigger-phrase tension is surfaced in the run record (verification_record.md
  § Failures or its own noted spot), not silently absorbed.
- Advisory Invariant 9 — TAKE IT: reword so it is checkable, e.g. every predicate
  command pasted into verification_record.md must show its date anchor; absence is
  the audit finding. Other advisories: apply any that are cheap and clearly right;
  list the ones you decline with one line each.

The review's "came back clean" list is settled — do not rework those areas.
