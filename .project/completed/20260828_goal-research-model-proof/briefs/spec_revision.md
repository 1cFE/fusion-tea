# Brief to /_my_spec — REVISION pass, GSTH Item 5

You are revising an existing spec against a completed review. Read both:
- `.project/active/goal-research-model-proof/spec.md` (the Draft you are amending)
- `.project/active/goal-research-model-proof/spec-review.md` (verdict: Revise)
Also read `.project/active/goal-research-model-proof/align.md` (owner rulings, settled)
and `.project/backlog/epic_goal_strategy_task_harness.md` § Item 5 (requirements source).

Edit spec.md in place. Do NOT run git commits. Finish with `ARTIFACT: <path>`.

## Apply ALL must-fix findings, with these orchestrator rulings

- **L3-3 (R-C2)** — ruled by the orchestrator, record it this way: the broad reading
  STANDS, derived from the epic's own Item 5 scope step 2 ("Write the reading and
  proposed research/model dispositions, then send them to a fresh non-author critic
  before either semantic follow-up executes"), which the owner ratified with the
  decomposition (epic Priority/Effort headers record the ratification 2026-08-25).
  Restate R-C2 as a requirement whose basis is that owner-ratified sentence — grade
  `[INHERITED: epic § Item 5 scope 2, decomposition ratified by owner 2026-08-25]` —
  not as your own inference. Keep one surfacing sentence: if the owner ever reads the
  runbook trigger narrowly, the runbook sentence is what gets amended. Orchestrator
  decision tier: execution detail, recorded loudly; also surfaced to the owner in the
  run log.
- **L3-1** — criterion 3 must admit all four native returns routed honestly:
  `REGISTERED` / `OPERATOR_QUEUE` (owner-visible handoff with reason) /
  `BOUNDED_NEGATIVE` / `BLOCKER`. Note `OPERATOR_QUEUE` is the *likeliest* honest
  return given DI-008's primary is an un-ingested open PDF, and say what the item does
  then (park at the owner gate; the round can close on it honestly).
- **L2-1** — add the staged-discovery discipline: the goal question settled at gate (a)
  is about the MODEL value (is `p_pump` = 1.0 MW defensible; what sourced value should
  the model carry), never phrased as "acquire the Moscato PDF". The bounded task's
  scope names a modeling objective only; the research prerequisite must EMERGE as its
  return. If the grounding exchange itself would pre-write the prerequisite into the
  scope, that is recorded as a spec-level hazard the design must guard.
- **L1-1** — correct R-A6: the INPUT shift is equal across arms; the EFFECT is not
  (`rec_frac` 0.94 / 0.79 / 0.68 by arm; feasibility fences at R ≤ 8.0 / 6.5 / 5.5 —
  take exact numbers from the study record/review, cite what you verify). The invariant
  must not pre-answer the R-E1 vs R-E3 (advance vs STRATEGY_BLOCKER) question — state
  that that judgment belongs to the round with the critic and reviewer over it.
- **L3-2** — add the owner-gate park to the honest-outcomes list (R-A4/R-E2 make it
  near-certain the round parks at least once).
- **L3-5** — widen R-G scope: the flip also amends the sentence "Two seams are not
  repaired yet…" (`GOAL_RUNBOOK.md:262` area) to say one seam (integrate) remains
  pending; still not touching the integrate row itself.
- **L1-2** — regrade R-A7 `[INFERRED]` (recording obligation is agent inference, the
  owner ruled only the removal itself).
- **L1-3** — regrade R-C3's cap NUMBER as inherited default (`GOAL_RUNBOOK.md:232`)
  that the goal may override at grounding; the *existence* of a cap and cap-stops-work
  stay owner-graded.
- **L1-4** — split criterion 8 so its first clause (record every prose failure) is
  graded to its true source (Item 4 pattern / epic hardening rule), not the epic
  criterion it miscited.
- **L1-5** — fix the `product-lens.md` reference: mark "(to be created at close)" or
  drop it; do not leave a dead citation.

## Two advisories promoted by the orchestrator

- **L3-7** — promote the pre-declared covering branch to a requirement (section H):
  before the run, declare which honest non-exercise outcomes count for which criteria
  (R-B3, R-D6, R-E3, the queue park), committed ahead of the round, ancestry-checkable —
  Item 4's pattern. Grade `[INFERRED]`, rationale: the audit relied on it in Item 4.
- **L3-6** — reword the two non-disk-checkable criteria as the review indicates so each
  maps to a path/commit/return file.

## Leave for design (do not expand spec scope)

L2-2 (state in one sentence what the item is worth if the second half never runs — allowed),
L2-3 (how far the follow-up WI is carried), L3-4 (registered source suffices for the round;
DI mutation stays gated per R-D5 — one clarifying sentence max), L4-1 (density — optional).

Do not renumber requirements the review cited unless necessary; if you renumber, leave a
mapping note. Keep all Align rulings exactly as graded.
