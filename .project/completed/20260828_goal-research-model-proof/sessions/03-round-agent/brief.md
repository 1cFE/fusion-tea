# Operator brief — round agent, round 1

You are a fresh session operating the repository's goal layer as a round agent. You did
not build the layer and need nothing beyond what is on disk. Do not run git commits —
write files only; the operator commits.

## What you may read

The goal directory `work/orchestration/goals/p-pump-basis/`;
`work/orchestration/GOAL_RUNBOOK.md`; `work/orchestration/goal-templates/`;
`.claude/skills/run-goal/`; `.project/adr/`; and the native repository — `models/`,
`knowledge/`, `work/`, `exploration/`, `modeling_project/`.

You may **not** read `.project/active/goal-research-model-proof/`, any `.orchestrate-logs/`
directory, or `.project/backlog/epic_goal_strategy_task_harness.md`. These are
orchestration surfaces, not goal material. Anything not on the "may read" list is out of
scope for this exchange.

## The ask

The goal `p-pump-basis` is grounded. Open round 1 and run it per `GOAL_RUNBOOK.md`:

1. Write the `### Strategy revision` in `trail.md` from the evidence in hand.
2. Run the round's first bounded task. Its objective, in modeling terms: **re-base
   `p_pump` from repository-native sources under the goal's invariants.** Write
   `### T-001 scope` before starting, then `### T-001 start`, then do the work.
3. Return per § Running one task: outcome, evidence refs, the goal-level reading of
   them, and the five decision fields for every goal-level decision (finding or
   trigger; decision and reason; tier; who decided; what changed).
4. After the return, write the reading and the proposed dispositions for the discovery
   row(s) the evidence touches, then a `### Stop — <today's date>` of kind `handoff`:
   the disposition checkpoint is a fresh session's job, never yours (§ What "fresh"
   means). Stop there.

Scope discipline: the scope names the modeling objective only — no prerequisite list,
no forward task list (a scope that lists its own prerequisites is a plan). If the work
cannot be completed from what the repository holds, say so in the return in the
runbook's vocabulary, on the evidence you gathered.
