The checkpoint entry is written at `work/orchestration/goals/p-pump-basis/trail.md:134`, appended, nothing edited in place. Uncommitted, as instructed.

**Verdict: does not pass — revise and resubmit as C-001.r2.** Submission 1 of 3; no semantic follow-up may execute until a passing entry exists.

The reading is careful and its central conclusion holds. I re-derived every number rather than checking the round's working, and it all reproduces: Cismondi's 150/2389 = 6.279 % at 80 bar; ARIES-ACT is LiPb-cooled with helium only in the divertor, so it genuinely isn't evidence about a helium primary loop; `p_th` = 3238.1 MW at the baseline `p_fus`; 203.3 MW held and 209.9 MW self-consistent; `rec_frac` 0.1514 → 0.3294 and 0.116 → 0.2421, `p_net` −18.5 % / −11.6 %, no verdict flip at baseline. My independent reconstruction of the power balance reproduced the study's own oracle values exactly, so the round's reconstruction is sound. `points.csv` is as described (3,792 rows, `p_fus` everywhere, the five power-balance columns empty everywhere). Nine cost accounts take `p_th` directly — exactly nine. Every citation sha matches HEAD, and the ancestry argument for the record-directory pin checks out. Nothing was written outside the goal directory and every reserved gate held.

Three required changes:

1. **`#5` cannot return as already-disposed.** Its newest log row reads `unrouted`, the round agrees its evidence touched it, and ADR-004 is unconditional that no touched row returns unrouted. The rule the round invoked ("a finding the round discovers itself is not a log row") bars minting an id, not appending under an existing one. And the round holds exactly the missing next reference — I verified `p_cryo` is geometry-independent, so `rec_frac` really is reconstructible from the committed `p_fus` column plus bound inputs at every grid point.

2. **"A single point, one source" overstates it.** Moscato's helium-primary HCPB figures (≈131 MW / 6.2 %, and 83–94 MW / ~4 %) are quoted with their inputs in the approved research file the return itself cites. Admitting them gives ~4–6.3 %, i.e. ~130–205 MW — a range sitting *inside* DI-008's band rather than above it. Excluding them on re-derivability grounds is defensible, but the rule has to be stated. Either way D-2 should claim what the evidence carries: the *low* (ARIES-ACT) end doesn't apply.

3. **Precision.** "~150MW" became 6.279 % and 203/210 MW. At the source's own precision the value is ~194 MW — *at* the top of DI-008's band, not above it, so the sharpest comparative claim is a precision artifact. The same paragraph also says design work is underway to reduce the figure (9 km → 3 km of piping), which the reading doesn't report. The indefensibility of 1.0 MW survives all of this and the entry should say so plainly rather than rest on a sharp number.

Two non-blocking notes (the denominator check folds in 50 MW of plasma heating that isn't blanket-deposited; and the source itself offers a stronger warrant than the pressure match — HCPB PHTS "representative for the HCLL concept", and Stellaris is HCLL). One item flagged for the round review, not settled here: `goal.md` § Invariants routes LCOE through `p_net`, and the round correctly found `p_th` also reaches nine cost accounts directly.