Revision written as three new entries — `### T-001 return — revised r2`, `### T-001 dispositions — proposed, revised r2`, and a handoff `### Stop`. Nothing amended, nothing outside the goal directory touched, no commits.

## The three required changes

**RC1 — `#5` gets a disposition row. Accepted; I was wrong.** ADR-004's bar on a self-discovered finding becoming a log row bars *minting an id*. `#5`'s id exists, its subject is exactly the gap my reconstruction ran over, and its Home reads `unrouted`. Proposed as `declared seam`, with the concrete next reference: `rec_frac` and `p_net` are recoverable at all 3,792 rows from the committed record alone. I checked the constancy claim myself rather than taking it — `p_cryo` computes to 0.864352 MW from bound inputs only and moves with neither R nor a, and every other term of the sum is a bound constant. I flagged `upstream filing` as the defensible alternative and said why I didn't pick it: this round cannot file anything upstream.

**RC2 — accepted, and it moves the answer.** Moscato's figures *are* in the repository, in the approved research artifact I was already citing. I admit them at second-order grade — transcribed from a PDF that was never ingested, so unlike Cismondi they can't be re-derived at a line — and say what that grade means. The result is a range, not a point: **~4–6 % of thermal power, ~130–195 MW**. D-2 is restated to the claim the evidence carries: **the low end of DI-008's 60–190 MW band does not apply**, because its 2 % leg is a machine with no helium-primary blanket. "One point, one source, above the band" is withdrawn.

**RC3 — accepted.** "~150 MW" is one approximate figure; 6.279 % was false precision, and the "just above the band" claim was its artifact. At ~6 % the value is ~195 MW held, ~200 MW computed — *at* the band top. The source's own qualification is now carried with the value: the pressure-drop work is ongoing and the figure is expected to fall (9 km of pipe → ~3 km), which is consistent with Moscato's near-term ~4 %. So the low end is the direction of travel.

## The two observations

Both taken. **Obs 1** — I report both forms of the denominator check (1.178 vs 1.160 × `p_fus` against DEMO's 1.173) as a sensitivity rather than switching basis; the 1.6 % spread is inside the source's precision, and the paragraph doesn't say whether DEMO's 2389 MW excludes the equivalent term. **Obs 2** — the warrant is replaced with the source's own sentence: the HCPB PHTS is representative for HCLL, and Stellaris's blanket is HCLL. Pressure match is now corroboration, not the argument.

## One dispute

The checkpoint's passing correction — "ACT1 is DCLL, ACT2 is SCLL" — is inverted. At `:290` the *advanced* characterization carries the SCLL blanket and the *conservative* one the DCLL blanket, and the same page names ACT2 as conservative. So ACT1 is SCLL, ACT2 is DCLL. My own phrasing was separately wrong: "self-cooled LiPb (DCLL)" conflated the two concepts — DCLL is dual-coolant. Both are recorded with the quotes. Neither error changes anything: neither configuration has a helium-primary blanket, which is the entire reason ARIES-ACT doesn't answer this question.

## Unchanged

Outcome stays `COMPLETE`. The central conclusion — 1.0 MW is not defensible for a helium-primary loop at this scale — is unchanged and now rests on more evidence, and the return says plainly that it survives any precision and either end of the range. D-3 and D-4 carry as written. Two items flagged for the round review: the LCOE channel, and that `goal.md`'s own "Moscato is the one not in the repository" is imprecise in the same way my return was — surfaced, not resolved, since `goal.md` is the operator's to amend.

C-001.r2 is submission 2 of 3. The fresh critic reviews next.