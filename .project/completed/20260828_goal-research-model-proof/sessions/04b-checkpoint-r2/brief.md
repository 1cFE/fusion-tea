# Operator brief — disposition checkpoint, C-001.r2 (second submission)

You are a fresh session acting as the pre-execution disposition checkpoint reviewer for
one goal round. You authored no part of the round. Do not run git commits — write files
only; the operator commits.

## What you may read

The goal directory `work/orchestration/goals/p-pump-basis/`;
`work/orchestration/GOAL_RUNBOOK.md`; `work/orchestration/goal-templates/`;
`.claude/skills/run-goal/`; `.project/adr/`; and the native repository — `models/`,
`knowledge/`, `work/`, `exploration/`, `modeling_project/`.

You may **not** read `.project/active/goal-research-model-proof/`, any `.orchestrate-logs/`
directory, or `.project/backlog/epic_goal_strategy_task_harness.md`. These are
orchestration surfaces, not goal material. Anything not on the "may read" list is out of
scope.

## The ask

Run the checkpoint per `GOAL_RUNBOOK.md` § The pre-execution disposition checkpoint, on
round 1 of goal `p-pump-basis`:

- The material: `trail.md` `### T-001 return` and `### T-001 dispositions — proposed`,
  against `goal.md` and the evidence refs the return cites. Read the cited evidence at
  its cited lines yourself — the review is of the reading, so re-derive, don't trust.
- Write your verdict as a new `### Checkpoint C-001.r2 — <today's date>` entry in
  `trail.md`, per the runbook's entry shape: the reviewer (your session), the reading
  reviewed, the dispositions reviewed, the verdict, and what you require changed if
  anything. Never amend an existing entry.
- Stop after writing the entry. Follow-up execution is not yours; neither is the round
  result.

## Second-submission notes

This is submission 2 of 3 (cap: 2 revisions). The material now includes, in trail.md:
the original `### T-001 return` and dispositions, `### Checkpoint C-001.r1`, and the
revised `### T-001 return — revised r2` and `### T-001 dispositions — proposed, revised
r2`. Review the REVISED entries against the r1 verdict's required changes, re-deriving
against the cited evidence yourself. The revision disputes one statement in C-001.r1's
own confirmations — adjudicate that dispute at the cited lines and record your finding
either way. Write your verdict as `### Checkpoint C-001.r2 — <today's date>`.
