# Product-Lens Ledger — goal-harness-contract (GSTH Item 1)

Append-only. One block per run; a changed gate is a new dated block. Resolution is by citation of a finding id, never by recency.

## spec — 2026-08-25 — rev drafted Item 1 contract (`.project/active/goal-harness-contract/spec.md`)
Epic: epic_goal_strategy_task_harness

Point (re-derived): Item 1 puts the goal layer on disk so someone who did not build it can operate it. That means the seven approved decisions filed in a repository-native ADR home with their recorded grades intact and the guidance they contradict amended (CLAUDE.md's live "do not cross-reference" rule); the three lean files with conventions sufficient to derive current goal state without mirroring native stage state; one lightweight fresh non-author checkpoint over a study reading and its proposed dispositions before any semantic follow-up executes, distinct in timing and responsibility from the post-round fresh `RoundReview`; the three writer-ownership homes agreed on joined `<study-id>#<n>` disposition rows with no touched row left `unrouted`; and one shared `GOAL_RUNBOOK.md` that a human and a goal agent operate against the same artifacts, gates, returns, and reviews. All of it under the owner's lean-first rule: no hardening mechanism without a recorded observed failure of the prose-and-native-facts route. [source: `.project/concepts/goal-driven-model-development-harness.md` § Owner's Words + Success Criteria 5, 8; `.project/concepts/goal-strategy-task-harness-design.md` § First-Build Persistence, § Findings and Learning, § Review Pattern, § Recorded Rulings; `.project/concepts/goal-strategy-task-harness-design-review.md` § Resolutions C1/P2-M4/Goal evidence seam; `.project/concepts/study-driven-model-development.md` § Success Criteria 4–5; `.project/backlog/epic_goal_strategy_task_harness.md` Item 1, grade: owner]

Falsifier: Item 1 closes while the pre-execution checkpoint can loop forever or die quietly because no cap and no owner-visible stop were ever contracted; while an owner-stated citation term has been dropped without an owner ruling; or while the runbook satisfies every stated criterion and still cannot be operated by anyone but the builder.

Findings:
- `spec-F1` [DO] The pre-execution disposition checkpoint has no cap and no owner-visible stop. The spec's `[NEED]` (`spec.md:57`) requires the checkpoint to read the reading and proposed dispositions before follow-up executes and requires the author to revise through it — and stops there. The owner's criterion is two-sided: "the author revises until the checkpoint passes **or its declared cap produces an owner-visible stop**" (epic Success Criteria, `[OWNER 2026-08-25]`), which itself inherits the concept's "loop with their critic until it passes or a declared cap is hit" (concept SC 5, owner). Without the cap half, a failing checkpoint has no defined end and no defined escalation. § Open Questions defers "what a fail-and-revise loop looks like" to design, but the cap-and-owner-visible-stop is an owner-stated obligation, not a design mechanism, and Item 5 is specced to prove it (`epic ... .md` Item 5 SC 2) against this contract. — `.project/backlog/epic_goal_strategy_task_harness.md` § Success Criteria bullet 2 `[OWNER 2026-08-25]`; `.project/concepts/goal-driven-model-development-harness.md` SC 5 (owner) — disposition: BLOCK (expected: the `[NEED]` at `spec.md:57` gains the cap and the owner-visible stop; the numeric default may stay in § Open Questions with the retry cap)
- `spec-F2` [DO] The digest term of the Goal Evidence Seam ruling is dropped without a ruling. The owner's words are "goal input references may cite `.project/` artifacts **by path and digest**" (review 1 § Resolutions, Goal evidence seam, `[OWNER]` 2026-08-23), carried into the approved design as "cite them by path or native id and, for mutable evidence, a digest" (`goal-strategy-task-harness-design.md:102`). The spec carries the rule twice — the `[NEED]` CLAUDE.md line (`spec.md:41`) and the "Cite, don't restate" `[INHERITED]` line (`spec.md:59`) — and both say path/native id only. Separately, § Non-Goals bars "content digests" as a hardening-path mechanism (`spec.md:84`, `[OWNER]` P2/M4). Those two owner rulings may genuinely collide, but the hardening table's entry is "immutable task envelope and **authority** digests" (`design.md:186`), which is not the evidence digest. Either way the spec resolves the tension silently in one direction. — `.project/concepts/goal-strategy-task-harness-design-review.md` § Resolutions, Goal evidence seam (owner); `.project/concepts/goal-strategy-task-harness-design.md:102` — disposition: BLOCK (expected: either restore "and, for mutable evidence, a digest" to `spec.md:59` and `:41`, or record the collision as an owner-visible surfaced item saying the lean-first ruling supersedes the digest term — not a silent drop)
- `spec-F3` [DO] The owner's quality bar for this item's own deliverable is quoted as motivation, never as a requirement. The two `[OWNER-VERBATIM]` lines — "I just want really good documentation and clean patterns so that it can be easily operated and managed by a human" and the operator "shouldn't have to be me (who built this and therefore is mostly familiar)" — sit in § Problem (`spec.md:22`) with no matching entry in § Known Requirements § Operating surface and no success criterion. The stated bar for `GOAL_RUNBOOK.md` is instead "describes the same artifacts, gates, returns, and reviews for human and goal-agent operation" (SC 5), which a document written for the builder can satisfy in full. The non-builder test exists only downstream in Item 4, which reads this item as its input, so a miss is found late and re-opens Item 1. — `.project/concepts/goal-driven-model-development-harness.md` § Owner's Words (owner-verbatim); § Success Criterion 8 (owner) — disposition: NOTE (expected: one `[NEED]` line in § Operating surface carrying the verbatim bar as the acceptance standard the runbook is written against)
- `spec-F4` [NOTE] The concept's post-execution audit critic has no named home in the contract. Owner grade, twice: "after dispositions execute, an audit critic checks that each landed and that the finding moved" (concept SC 5) and "an audit critic after execution" (§ Next-Stage Handoff, `[OWNER]`). Review 1's P4 resolution collapsed the critic set to the fresh round review as an explicitly `[AGENT]` inference ("owner may override"), and `RoundReview` does check touched-finding dispositions and carry-forward (`spec.md:56`) — so the function is plausibly discharged. But SC 3 makes the checkpoint/`RoundReview` division of responsibility a pass criterion, and the spec never says `RoundReview` absorbs the audit role. Not re-litigating the collapse; asking that the contract state where the obligation landed. — `.project/concepts/goal-driven-model-development-harness.md` SC 5 and § Next-Stage Handoff (owner); `.project/concepts/goal-strategy-task-harness-design-review.md` § Resolutions M1/P3+P4 (`[AGENT]`) — disposition: NOTE
- `epic-plan-F1` (owner) — resolved on the epic 2026-08-25 (FIXED, authority: owner); basis landed on Item 4, not here. Reference only.
- `epic-plan-F2` (owner) — resolved on the epic 2026-08-25 (FIXED, authority: owner); its basis is this item's obligation to define the one lightweight pre-execution checkpoint. `spec-F1` is the unfinished half of that basis, not a reopening of the finding. Reference only.

Reverse check: CLEAR — every entry in § Known Requirements traces to the concept-design, an owner resolution, the epic's Item 1 scope, or a marked `[INFERRED]` orchestrator reading that says so and stays challengeable. No orphan scope found.

Notes (no finding):
- Provenance handling is clean where it is hardest. `spec.md:40` preserves each filed decision's own recorded grade rather than flattening the seven to `[INHERITED]`, and the two `[INFERRED]` lines (`:77`, `:78`) name themselves as the orchestrator's reading and remain challengeable. The Align's `[OWNER 2026-08-25]` rulings are dated and quoted at `:68`, `:75`, `:76`.
- The dependency the epic states as "Item 6 Phase 4 must close **or explicitly coordinate**" is discharged through the coordinate branch, on the owner's 2026-08-25 word, with the three protected findings named (`spec.md:68`).

Smells:
- *Line-number drift on a load-bearing citation.* The spec cites `CLAUDE.md:73` twice (`spec.md:19`, `:42`), and one of those makes the line the surface the Goal Evidence Seam ADR must name. The quoted sentence is at `CLAUDE.md:72`. Cheap to fix, and worth fixing before it is copied into an ADR.
- *"Approved" dropped from the CLAUDE.md amendment.* The epic deliverable reads "the **approved** CLAUDE.md evidence-seam amendment"; the spec's `[NEED]` (`:41`) says only that CLAUDE.md must say this. Align ruling 2 reserves no gate beyond the standing defaults, so this is probably intended — but CLAUDE.md is live project guidance for every session, and the epic's word is the only place approval is stated.
- *One hardening mechanism missing from the Non-Goals list.* § Non-Goals (`:84`) enumerates five of the design's six hardening-table rows; "denser per-stage trail events" (`design.md:188`) is absent. SC 6's generic "no hardening-path mechanism" covers it, and `spec.md:51` states the positive form, so nothing is lost — but the list reads as exhaustive and is not.

Gate: BLOCKED (`spec-F1`, `spec-F2`)

Epic ledger state: `.project/backlog/epic_goal_strategy_task_harness.md` § Product-Lens currently reads **CLEAR** (second block, 2026-08-25, owner-dispositioned decomposition; `epic-plan-F1` and `epic-plan-F2` both FIXED). This spec-hop block does not change that gate.

---

## spec — 2026-08-25 — rev spec revised against the lens block above
Epic: epic_goal_strategy_task_harness

Resolves:
- `spec-F1`: FIXED — authority: owner — basis: the epic's `[OWNER 2026-08-25]` criterion (`epic_goal_strategy_task_harness.md:49`) was verified verbatim and its second half is now a `[NEED]` in § Lean artifact contract: the author revises through the checkpoint until it passes or a declared cap is hit, and hitting the cap produces a recorded owner-visible stop rather than silently permitting execution. Only the cap's numeric value stays in § Open Questions, and that deferral now says so explicitly.
- `spec-F2`: FIXED — authority: owner — basis: the owner's term is restored. The CLAUDE.md `[NEED]` now reads "by path and digest"; a following `[NEED]` records that the barred hardening row is *authority* digests (`design.md:186`), not the evidence-citation digest, and instructs design to surface the collision to the owner if one is genuinely found rather than resolving it silently. § Non-Goals carries the same distinction and now names the barred row precisely.
- `spec-F3`: FIXED — authority: owner — basis: a `[NEED]` in § Operating surface carries both `[OWNER-VERBATIM]` lines as the acceptance standard the runbook and templates are written against, with `work/orchestration/handshake-lcoe-construction.md` marked `[REFERENT]` — the bar to match, not an illustration.
- `spec-F4`: FIXED — authority: agent (concept-design's `[AGENT]` review-topology collapse, unchanged) — basis: an `[INHERITED]` line now states where owner criterion 5's post-execution audit obligation landed — inside `RoundReview`, which accounts for every touched discovery row and what changed — rather than leaving it homeless while the critic set stays collapsed. The collapse itself is not re-litigated and remains owner-overridable.

Smells dispositioned:
- *Line-number drift* — NOT A DEFECT. Re-checked: `grep -n` puts "**CRITICAL: Do not cross-reference between them.**" at `CLAUDE.md:73`. The spec's two citations are correct as written; the lens's `:72` is off by one. No change made.
- *"Approved" dropped* — FIXED. The `[NEED]` now reads "the approved CLAUDE.md amendment", matching the epic deliverable's word.
- *Missing hardening row* — FIXED. "Denser per-stage trail events" added to § Non-Goals, so the list is now the design's full hardening table.

Gate: CLEAR

---

## correction — 2026-08-25 — to this ledger's `spec-F3` resolution
Epic: epic_goal_strategy_task_harness

The `spec-F3` resolution above records the `[REFERENT]` marking of `work/orchestration/handshake-lcoe-construction.md` as "authority: owner". The independent spec review (`spec-review.md` L1-2) checked and found no owner statement marking it binding — the phrase "proven prose referent" is the epic's Required Reading text (`epic_goal_strategy_task_harness.md:120`), which is owner-ratified but agent-authored. The reviewer is right.

Disposition (orchestrator, 2026-08-25): the `[REFERENT]` **force stands** — it is the bar `GOAL_RUNBOOK.md` must match, not an illustration. Only the provenance changes: `spec.md` now grades it `[INHERITED: epic Item 1 Required Reading — "proven prose referent"]`. Recorded here because this ledger is append-only and a later reader would otherwise inherit the overstated grade.

No finding reopened; no gate change.

Gate: CLEAR

---

## design — 2026-08-25 — rev drafted Item 1 design (`.project/active/goal-harness-contract/design.md`)
Epic: epic_goal_strategy_task_harness

**Method note:** run inline by the design-review session, not by an independent subagent (session-level instruction bars spawning agents unasked). Point re-derived from sources, not inherited from the design's own § The Point.

Point (re-derived): Item 1 puts the goal layer on disk so a non-builder can operate it — seven approved decisions filed in a repository-native ADR home with their recorded grades intact and the guidance one contradicts amended (`CLAUDE.md:73`); three lean files whose conventions let a fresh reader derive current goal state without mirroring native stage state; one lightweight fresh non-author checkpoint over a study reading and its proposed dispositions before any semantic follow-up executes, distinct in timing and responsibility from the post-round `RoundReview`; five textual homes agreed on joined `<study-id>#<n>` disposition rows with no touched row left `unrouted`; and one shared `GOAL_RUNBOOK.md` a human and an agent operate against identically. Under the lean-first rule: no hardening mechanism without a recorded observed failure of the prose-and-native-facts route. [source: `.project/concepts/goal-driven-model-development-harness.md` § Owner's Words + SC 5, 8; `.project/concepts/goal-strategy-task-harness-design.md` § First-Build Persistence, § Findings and Learning, § Review Pattern, § Recorded Rulings; `.project/concepts/goal-strategy-task-harness-design-review.md` § Resolutions C1/P2-M4/Goal evidence seam; `.project/backlog/epic_goal_strategy_task_harness.md` Item 1, grade: owner]

Falsifier: Item 1 closes while an inherited contract rule silently has no home on disk; while a control-plane mechanism enters under a different word; or while the runbook satisfies every stated criterion and still cannot be operated by anyone but the builder.

Findings:
- `design-F1` [DO] The inherited rule "external mutation voids task authority" has no home in the design, and its natural mechanism is the barred one. `spec.md:74` `[INHERITED]`: "If a referenced native work item changes outside an active goal task, the task loses authority; re-ground or close the round before more work." Absent from I1–I12, from the trail entry kinds, from § Component Overview's runbook sections, and from § Non-Goals. Detecting that a cited artifact moved is a stale-authority check, which I6 and § Non-Goals bar. The spec attached an explicit surfacing duty to exactly this seam (`spec.md:42`); it went unexercised in both directions — not designed, not surfaced. — `.project/active/goal-harness-contract/spec.md:74` (`[INHERITED]` from `goal-strategy-task-harness-design.md` § Required Invariants); `spec.md:42` (surfacing duty) — disposition: BLOCK (expected: a prose home — a `RoundReview` check plus a runbook re-grounding paragraph plus an invariant — or a surfaced collision with the dependent choice parked)
- `design-F2` [DO] Round-closure conditions are not designed. `spec.md:64` `[INHERITED]` carries the one-pin/one-study bound and six close triggers; the design states them only inside a table cell justifying the absence of a task cap (`design.md:176`). SC 2's bar is that current goal state is derivable from the files, and "is this round open?" is the first such question. — `.project/active/goal-harness-contract/spec.md:64`; epic SC 2 (owner-ratified) — disposition: BLOCK (expected: an invariant and a runbook section at the grain the design gives the trail entry headings)
- `design-F3` [NOTE] The hardening boundary is claimed testable and is not tested. § Validation's SC 6 mapping names I6 "the testable form of the hardening boundary"; none of the four `test_goal_contract.py` tests asserts it. — `design.md:249` vs `:241-245` — disposition: NOTE (expected: add a phrase-level assertion or drop the testability claim)
- `design-F4` [NOTE] `<path>@<commit-sha>` does not pin untracked evidence — gitignored study stores (`run-study-first-consumer/plan.md:244`) and R2-synced research binaries are the mutable-evidence class the owner's digest term most plausibly targets, and a commit sha pins nothing for them. Naming the gap discharges it; inventing a scheme would cross the boundary. — `design.md:83`; `CLAUDE.md` § Research Artifact Sync — disposition: NOTE

Reverse check: CLEAR — every design element traces to a spec requirement, a settled Open Question, or a recorded and re-derived judgment call (D4's `adr.sh`, which names its own deviation and grounds it in `epic:111`'s "filing mechanism"). No orphan scope found.

Notes (no finding):
- The lean-first ruling is held. No envelope, ledger, idempotency, reconciliation, concurrency, or dispatch mechanism enters. `adr.sh` was checked directly against the boundary: it mints an id, copies a template, appends an index row, and touches no goal-loop authority question.
- D8's digest re-derivation is sound for tracked artifacts. The function split — read by a person versus compared by a procedure — is real, and I6 states it as a rule rather than leaving it implicit. Correctly handled as `[INFERRED]`, challenged by re-derivation rather than by asking the owner.
- The `[REFERENT]` bar survives at its stated force: § Research Findings analyzes the referent's actual prose shape rather than gesturing at it, and Implementation Notes binds the runbook to it.

Smells:
- *Two false claims about the repo's own conventions, one of them load-bearing on a decision.* D7 rejects a slash command because "every file in `.claude/commands/` is an agentic-mbse symlink" — `.claude/commands/manage-concept.md` is a real fusion-tea file. `design.md:42` says "every other entry in `.claude/skills/` is a symlink" — three are local directories. The conclusions survive; the recorded reasons are what future agents re-derive against.
- *The joined-row guarantee is a docstring.* The spec asked that the set-comparison accident become a stated guarantee; the design's assertions do not fail if `in_log` is rewritten from a set to a list, which is the exact edit that kills append-as-update.

Neither design-level structural smell fired: the writer-ownership change does move an invariant, but it says so across six edits, an ADR, and a test rather than silently; and the goal round writing into the study producer's log is the owner's own ruling with the producer's contract amended, not a consumer compensating around a producer guarantee.

Gate: BLOCKED (`design-F1`, `design-F2`)

Epic ledger state: `.project/backlog/epic_goal_strategy_task_harness.md` § Product-Lens reads CLEAR (2026-08-25, owner-dispositioned decomposition). This design-hop block does not change that gate.

---

## design — 2026-08-25 — rev design revised against the lens block above (`.project/active/goal-harness-contract/design.md` rev 2)
Epic: epic_goal_strategy_task_harness

Resolves:
- `design-F1`: FIXED — authority: agent (re-derivation against the recorded `[INFERRED]` reconciliation, not an owner ruling) — basis: "external mutation voids authority" now has four homes — invariant I13, a `### Stop — kind: external mutation` trail entry, a `RoundReview` check on cited-ref liveness, and a `GOAL_RUNBOOK.md` section "when a cited artifact moves". The collision with I6 was re-derived and does **not** hold: I6 bars a *procedure* comparing a stored digest to a computed one to decide authority; I13 asks a *reader* — the resumer and the fresh reviewer — to check cited refs against the native artifacts' own state and history. Same person-versus-procedure split the design already applies to the evidence digest (D8), now stated explicitly at D11. No surfacing owed, because no collision was found; the surfacing duty at `spec.md:42` is discharged by exercising the re-derivation and recording it rather than by silence. The hidden bet the lens named is now stated as B5 with its failure mode, and § Non-Goals names the stale-authority guard as barred by name, with B5's failure pre-declared as the observed failure that would promote it.
- `design-F2`: FIXED — authority: inherited (`spec.md:64`, `[INHERITED]` from the concept-design § Round Semantics) — basis: the one-pin/one-study bound and all six close triggers are now invariant I14; round open/closed state is invariant I15 and decision D12, derivable from `trail.md`'s headings alone with nothing maintained; § Architecture carries a "Round boundaries" paragraph; § Component Overview gives `GOAL_RUNBOOK.md` an "opening and closing a round" section at the grain of the trail entry headings; and the `Round N result` entry writes its stop reason as a derivation ("last outcome `<X>` + `<limit or none>` → round closes") rather than as a second enum.
- `design-F3`: FIXED — authority: agent — basis: `test_goal_contract.py` gains a fifth test asserting I6's and I13's sentences are present in the runbook and templates and that no document instructs a digest comparison or recomputation. The SC6 mapping now states plainly what that test does and does not cover: it checks the documents, not runtime behaviour, and the runtime half is manual verification.
- `design-F4`: FIXED — authority: agent — basis: D8 now names the evidence classes. Tracked artifacts cite `<path>@<commit-sha>`; gitignored study stores cite the committed record directory whose `snapshot.json` already hashes the store; R2-synced research binaries cite the tracked extracted markdown whose frontmatter carries `content_hash_sha256:`. Anything with no native hash — including `knowledge/MANIFEST.jsonl` entries, verified to carry none — is cited by path with "unpinned; no native digest" stated in the citation, so a reader can see the citation pins nothing. No new digest machinery; § Non-Goals records the refusal.

Smells dispositioned:
- *Two false claims about the repo's own conventions* — FIXED. Both verified against the live tree and corrected. `.claude/commands/` holds fifteen agentic-mbse symlinks and one real fusion-tea file (`manage-concept.md`); `.claude/skills/` holds four real fusion-tea directories (`browser-inspect`, `concept-research-navigation`, `html-explainer`, `run-study`) and eleven symlinks. D7's conclusion stands on two replacement reasons that do check out: the repo's own division of form puts documented multi-stage procedures in skills and interactive sessions in commands, and a skill carries a trigger description the model matches, which matters because the goal agent is invoked by task description rather than by a human typing a slash command.
- *The joined-row guarantee is a docstring* — FIXED. The join logic is extracted to two module-level helpers that the existing test calls unchanged, and an added fixture test writes a synthetic record and log where one id carries both a sighting row and a joined disposition row, asserting the helpers still join. Rewriting `_ids_in_log` from a set comprehension to a list turns that test red.

Also carried from the design review's minor findings: the four dropped `[INHERITED]` clauses restored (no future task list; `PREREQUISITE` never predicted in scope; stop reason derived, not a second enum; `RoundReview` never resumes the closed round); test 1's absent-clause check specified as a negative lookahead rather than a bare substring that its own replacement contains; D4's premise restated as a claim checkable at `~/.claude/commands/_my_design.md` and outside this repo, with the repo-side half named separately; checkpoint revisions specified as new `### Checkpoint C-00N.rK` entries rather than in-place amendments, per I4; the log's scannability cost given a runbook sentence (scan for the id, newest row is current state); and the five-versus-six count settled everywhere as "five homes, six edits".

Gate: CLEAR

---

## audit — 2026-08-25 — rev implementation of Item 1 goal-harness-contract
Epic: epic_goal_strategy_task_harness

**Method note:** `~/.claude/scripts/product-lens.md` was unreadable in the lens session (the sandbox denies reads outside the repo, for Read and Bash alike). The lens was run against the block format and discipline evidenced by the six prior blocks in this ledger, not against its own instruction text. Point re-derived from the sources; no framing inherited from `spec.md` or `design.md`. Tests executed: `tests/orchestration` + `tests/study` → 256 passed, 43 skipped. The auditing session verified `audit-F1` and `audit-F2` independently against their cited sources before accepting them.

Point (re-derived): Item 1 puts the goal layer on disk so a non-builder can operate it. Seven approved decisions filed in a repository-native ADR home with their recorded grades intact and the one live guidance they contradict amended (`CLAUDE.md:73`); three lean files whose fixed shapes let a fresh reader derive current goal state without mirroring native stage state; one lightweight fresh non-author checkpoint over a study reading and its proposed dispositions before any semantic follow-up executes, capped so the cap stops work rather than releasing it, and distinct in timing and responsibility from the post-round `RoundReview`; five textual homes agreed on joined `<study-id>#<n>` disposition rows with no touched row left `unrouted`; and one `GOAL_RUNBOOK.md` that a human and an agent operate identically, written to the owner's stated bar — "really good documentation and clean patterns so that it can be easily operated and managed by a human", by an operator who "shouldn't have to be me (who built this)". All under the lean-first rule: no hardening mechanism without a recorded observed failure of the prose-and-native-facts route. [source: `.project/concepts/goal-driven-model-development-harness.md` § Owner's Words + SC 5, 8; `.project/concepts/goal-strategy-task-harness-design.md` § First-Build Persistence, § Findings and Learning, § Review Pattern, § Recorded Rulings; `.project/concepts/goal-strategy-task-harness-design-review.md` § Resolutions C1/P2-M4/Goal evidence seam; review 2 § Resolutions mi1; `.project/backlog/epic_goal_strategy_task_harness.md` Item 1 + § Success Criteria, grade: owner]

Falsifier: Item 1 closes while an owner-originated decision is filed at a grade that makes it challengeable by re-derivation instead of by asking the owner; while a gate the contract requires has no way to be performed by anyone but the builder; or while a home the joined disposition row breaks still contradicts the others.

Findings:
- `audit-F1` [DO] ADR-005 files an owner-originated decision under an agent grade. Its frontmatter reads `grade: "[AGENT] inference; owner may override"` (`005-review-topology.md:5`) and `INDEX.md:11` renders the same. But the record's title and § Decision now carry two decisions, and the second is explicitly `[OWNER 2026-08-25]` in the body (`:20`) — the pre-execution checkpoint placement, which is the owner's disposition of lens finding `epic-plan-F2`. The register's own rule is "A record may carry a split grade … Write the split; do not round it to the stronger half" (`README.md:45`), and ADR-007 § Rationale names this exact failure: filing it as one agent-grade row would make an owner ruling challengeable by re-derivation. ADR-004 and ADR-007 both carry split grades; ADR-005 does not. A future agent reading the frontmatter or the index — the two surfaces built for scanning — is told to re-derive an owner ruling. The design specified this grade (`design.md:152`) by copying the concept-design's row verbatim, which was correct for the topology half and predates the 2026-08-25 owner disposition; the copy was not re-checked when the owner half was added. — `.project/adr/README.md:45`; `.project/adr/007-supersession.md` § Rationale; `.project/backlog/epic_goal_strategy_task_harness.md:49` `[OWNER 2026-08-25]` — disposition: BLOCK (expected: `grade: "[AGENT] review topology, owner may override; [OWNER] 2026-08-25 pre-execution checkpoint"` in the frontmatter and the matching `INDEX.md` row; the body already says it)
- `audit-F2` [DO] "Fresh" is the load-bearing word of two gates and is never defined, and the skill claims the runbook defines it. `SKILL.md:28-30` tells the reader "The runbook says what 'fresh' means at each place it matters." It does not: `GOAL_RUNBOOK.md` says only "a fresh agent who did not do the work" (`:11`, `:113`, `:129`) and never says what makes an agent fresh or how an operator on either path obtains one. The owner's criterion is stronger than the runbook's paraphrase — "The critic is never the author's session" (`goal-driven-model-development-harness.md:47`, `[OWNER]`, SC 5) names a session boundary where the runbook names a work boundary. On the human path this is survivable. On the agent path it is not: the runbook's instruction is "**hand** the reading and its proposed dispositions to a fresh agent" (`:113`), the `run-goal` skill declares `allowed-tools: Bash, Read, Write, Edit, Glob, Grep` (`SKILL.md:12`) so the round agent cannot delegate, and no document says the alternative — stop and hand back to the operator. A goal agent reaching the owner's checkpoint has no defined next move, and the checkpoint is the gate the owner added by name. Neither the spec nor the design defers this; the spec's `[NEED]` at `:67` states the checkpoint and stops. The builder knows to open a second session. That is precisely the operator the owner said this must not require. — `.project/concepts/goal-driven-model-development-harness.md:47` § Success Criteria 5 and § Owner's Words (operator "shouldn't have to be me"); epic § Success Criteria bullets 2 and 3 — disposition: BLOCK (expected: prose only, no dispatch. One runbook sentence defining fresh against the owner's session wording, and one sentence per gate saying who obtains the reviewer on each path, including the agent-path move when it cannot: a recorded stop that hands back to the operator. Then either back `SKILL.md:28-30`'s claim or delete it. Building a way to *start* a fresh session is dispatch and stays barred by ADR-003)
- `audit-F3` [NOTE] The new register is invisible from the two files that route readers to homes. `CLAUDE.md` § Project Structure lists `.project/` as active / backlog / completed / EPIC_GUIDE / epic_template (`:33-38`) — no `adr/`, and the only ADR mention anywhere in `CLAUDE.md` is ADR-006 cited inside the two-PM paragraph (`:75`). `modeling_project/ARCHITECTURE.md` never mentions `.project/adr/` at all, so the split the register asserts — `ADR-NNN` here, `AD-XXX` there (`README.md:9-11`) — is stated in only one direction, in the file a modeling agent does not read. Item 1's own problem statement is that "there is no place in the repository where an architecture decision belongs" (`spec.md:14`), and SC1 asks that the decisions be "cited by the runbook and affected project guidance". The runbook half is done well (seven records cited, none restated). Fairness: the `CLAUDE.md` tree already omitted `.project/concepts/` and `.project/research/` before this item, so the map was partial already. — epic Item 1 § Success Criteria bullet 1; `.project/adr/README.md:9-11` — disposition: NOTE (expected: one line in the `CLAUDE.md` tree and one sentence in `modeling_project/ARCHITECTURE.md` naming where non-model decisions go)

Reverse check: CLEAR — every artifact shipped traces to an Item 1 scope line or a recorded design decision. `.project/scripts/adr.sh` traces to D4; the run-goal skill to D7; `README.md`/`INDEX.md`/`template.md` to Scope 1; the six writer-ownership edits to Scope 3 and review 2's `mi1`; the two test files to Item 1 SC6. No orphan scope found.

Notes (no finding):
- The writer-ownership amendment lands completely and is guarded honestly. All five homes agree — runbook step 14 (`:221-224`), the administrator paragraph (`:273-275`), the runbook's `DISCOVERY_LOG.md` prose (`:295-306`), and the log header (`DISCOVERY_LOG.md:3`) — and `test_goal_contract.py` checks the retired clause is gone using a negative lookahead rather than the bare substring that its own replacement contains.
- The joined-row guarantee is a real test now, not a docstring. `test_a_joined_disposition_row_is_legal` builds a synthetic sighting-plus-disposition pair and goes red if `_ids_in_log` is rewritten from a set to a list — the exact edit that would kill append-as-update.
- ADR-004's factual claim about the live log checks out: 22 rows, 6 `unrouted`, counted directly.
- The hardening boundary holds. No envelope, ledger, idempotency, reconciliation, or digest-comparing procedure appears anywhere in the runbook, templates, skill, or tests; `test_the_hardening_boundary_is_stated_and_not_crossed` scans for the barred instruction and the runbook states the person-versus-procedure rule where an operator writes a citation.
- The `CLAUDE.md` amendment is live in both directions — the prohibition narrowed to *state* rather than deleted, the permission stated with the digest term the owner used, and the record and the guidance cite each other (`CLAUDE.md:73-75`, `006-goal-evidence-seam.md:42`).
- The runbook is written to the referent's bar, not at it. It reads like `handshake-lcoe-construction.md`: obligations stated, judgment left to the operator, "Do / Write / Where / Who checks it" at every stage.

Smells:
- *Two dead cross-references inside the register's own Affected seams.* ADR-002 (`:32`) and ADR-005 (`:40`) both cite `GOAL_RUNBOOK.md` "§ The fresh `RoundReview`". The section is called "The fresh review". A reader following the pointer finds no such heading, and Affected seams is the list someone edits against.
- *`adr.sh new` defaults to the weaker grade.* The minted index row hardcodes `[AGENT]` (`adr.sh:28`) and `template.md:5` does the same. `README.md:57` says fill the grade in by hand, and the register's very first owner-graded record already came out under-graded (`audit-F1`) — the default sits on the path of least resistance in the direction that loses owner authority.
- *`ADR-00X` already means something else in committed modeling artifacts.* `work/completed/20260303_WI-008_hif-concept-instantiation/design.md:49-107` uses "ADR-002" for the modeling calc-placement decision (`AD-002`). The README's prior-art paragraph handles the `exploration/phase_1a/ADR-001` stray but not this looser usage, so "ADR-002" now resolves to two different decisions depending on which directory the reader came from.
- *The owner's "pre-defined checkpoints" stop trigger has no home.* `goal.md` carries Reserved gates and Limits; the trail's stop kinds are `interruption | limit | cap | owner gate | external mutation`. The owner's stop list (§ Owner's Words, SC 7) includes a checkpoint declared in the goal file, distinct from a reserved gate. The approved concept-design already narrowed the goal file to this heading set (`goal-strategy-task-harness-design.md:110`) and was owner-accepted, so this is inherited narrowing, not introduced here. Recorded so a later reader sees where it went.

Neither design-level structural smell fired: the writer-ownership change moves an invariant, but it says so across six edits, an ADR, and a test rather than silently; and the goal round writing into the study producer's log is the owner's own ruling with the producer's contract amended, not a consumer compensating around a producer guarantee.

Gate: BLOCKED (`audit-F1`, `audit-F2`)

Epic ledger state: `.project/backlog/epic_goal_strategy_task_harness.md` § Product-Lens reads CLEAR (2026-08-25, owner-dispositioned decomposition). This audit-hop block does not change that gate.

---

## Resolution — audit-fix hop, 2026-08-25

Applied by the implement stage under the orchestrator's audit-fix brief (`a317e103`), which dispositioned every audit finding APPLY. Authority basis is given per finding; nothing here re-decides an approved decision.

**`audit-F1` — RESOLVED.** ADR-005's frontmatter grade and its `INDEX.md` row now write the split: `[AGENT] inference (topology; owner may override) + [OWNER 2026-08-25] (pre-execution checkpoint placement)`, with the index cell matching. *Authority:* the register's own rule, `.project/adr/README.md:45` — "Write the split; do not round it to the stronger half" — which this had rounded to the *weaker* half; and capture-fidelity law 1, preserve the grade across every hop. The record's body already carried the owner half with its own grade and source, so this corrects the two scanning surfaces and invents nothing. The implement stage's original reasoning (`plan.md` § Phase 1) was sound for the topology half and was simply not re-checked after the owner half entered the body.

**`audit-F2` — RESOLVED, prose only.** `GOAL_RUNBOOK.md` gains § What "fresh" means, which (a) quotes the owner's rule at verbatim strength — "The critic is never the author's session", `[OWNER]`, `.project/concepts/goal-driven-model-development-harness.md:47`, SC 5 — and says plainly it is a session boundary rather than a work boundary; (b) states who obtains the reviewer on each path; and (c) defines the agent's move when it cannot start a session: a recorded `### Stop` of kind `handoff` that hands the material back to the operator, with the entry's shape given literally. Both gates now point at that section and both say what an agent does instead of proceeding. `handoff` joins the trail template's stop-kind vocabulary. *Authority:* owner SC 5 for the rule; ADR-003 for why the alternative is barred — building a way to start a session is unattended dispatch, which stays on the hardening path. **The prose handoff is the lean answer, not a placeholder for dispatch.** Guarded by `test_fresh_is_defined_at_owner_strength_with_an_agent_move`. `SKILL.md`'s overclaim at `:28-30` is corrected to point at the new section (staged at `staging/skill-edit-2.md`, edit 1).

**`audit-F3` — RESOLVED.** `CLAUDE.md` § Project Structure gains an `adr/` row under `.project/`; `modeling_project/ARCHITECTURE.md` gains a § Scope sentence naming the `AD-XXX` versus `ADR-NNN` split and pointing at `.project/adr/`. *Authority:* audit finding, dispositioned APPLY. The split was previously asserted only in the file a modeling agent does not read.

**Smaller confirmed findings — all applied.** Three uncited runbook pointers (the `run-study` runbook by path at the seams table; a one-line gloss of *pin* with a pointer to where the study layer owns the term; and the integration pattern rewritten to the honest form — no written pattern exists, so it is a `PREREQUISITE` return until epic Item 3 lands). Two dead § Affected seams cross-references now cite the live heading "The fresh review". `adr.sh:28` and `template.md:5` default to `[GRADE — copy from source]`, a placeholder that cannot be left as-is. The prior-art paragraph in `INDEX.md` gains a sentence on the loose `ADR-00X` usage in `work/completed/20260303_WI-008.../design.md`. `SKILL.md:42` and `:46` become pointers (staged, edits 2–3), and the "only such sentence" over-claim is corrected by a dated amendment note in `plan.md`.

**Left as recorded, not resolved.** The fourth smell — the owner's "pre-defined checkpoints" stop trigger having no home in `goal.md` — stands as the lens recorded it: inherited narrowing from the owner-accepted concept-design (`goal-strategy-task-harness-design.md:110`), not introduced by this item. Changing it would re-decide an approved decision, which is outside an audit-fix hop.

**Gate: CLEAR.**

Two things a later reader should carry, neither a block: the runtime half of every claim here is unverified — these are document checks, and Items 4–6 are the proofs; and the runbook is still written by the agent that built the layer, so any finding Item 4's cold-grounding proof raises against it is an **Item 1 defect**.
