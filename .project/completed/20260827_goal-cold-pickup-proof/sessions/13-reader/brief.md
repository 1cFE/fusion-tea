# Brief — read one goal directory and answer four questions

You are a fresh session. Read `work/orchestration/goals/cryo-volume-basis/` and whatever
repository artifacts its files cite — nothing else is needed.

## What you may read
The goal directory `work/orchestration/goals/cryo-volume-basis/`;
`work/orchestration/GOAL_RUNBOOK.md`; `work/orchestration/goal-templates/`;
`.claude/skills/run-goal/`; `.project/adr/`; and the native repository — `models/`,
`knowledge/`, `work/`, `exploration/`, `modeling_project/`, `docs/`, `scripts/`.
You may **not** read `.project/active/goal-cold-pickup-proof/`, any `.orchestrate-logs/`
directory anywhere, or anything under `~/goal-proof-logs/`.

## What to answer, by name

From the goal directory alone (following its citations where they point):
1. What is the active strategy of the most recent round?
2. What was the one task most recently in flight or completed, and what did it return?
3. What gate or limit state is currently open or binding?
4. What native evidence does the round rest on? Name paths.

Write the four answers plainly. Do not write any files. Do not run `git commit`.
