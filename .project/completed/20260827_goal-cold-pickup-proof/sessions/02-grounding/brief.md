# Operator brief — grounding, turn 2 (answers to your six questions)

Same read fence as turn 1. All answers below are operator judgment, `[AGENT]` grade.

1. **The 34% gap.** Choosing among (a)/(b)/(c) is the goal's own work — do not settle it in
   `goal.md`. Write § Answered when so a reasoned "no, keep it held" ends the goal on the
   same terms as a "yes". Your derivation belongs exactly where you put it, in the evidence.
2. **Consumer.** Your proposed reading is confirmed: the MFE cost-modeling epic. The answer
   decides whether a modeling item opens under it and how row `#2` is dispositioned.
3. **Answered when.** As you proposed: a recorded decision with reasoning; if "yes", a landed
   modeling item reproducing the Stellaris anchor within a stated tolerance and row `#2` no
   longer `unrouted`; a "no" ends the goal with the same recorded reasoning and a row `#2`
   disposition. The tolerance number is the modeling item's to state, not the goal's — the
   goal requires only that one is stated.
4. **Limits.** Keep the runbook defaults exactly (2 / 2 / 6 / none). Do not tighten the round
   limit: this goal ends on its gates and its answer, not on a countdown, and a tightened
   limit would manufacture a close the evidence didn't earn.
5. **Reserved gates.** Keep your five, and put the general rule above them: merge, push, item
   close, and archive are owner-held per the runbook, and **any model or knowledge mutation
   beyond the goal directory needs owner sign-off**. Your five (promoting a pin, retiring
   `vol_cold_cryo` as a settable input, re-basing `f_carnot_cryo`, minting or amending a DI,
   widening into row `#3`) stand as named instances of that rule. On the `integrate` seam:
   confirmed — run under that ceiling. Repairing seams is not this goal's work; a round that
   lands a model change and stops at `PREREQUISITE` is an honest stop.
6. **Close rule.** The owner closes this goal, on the fresh round review's recommendation,
   once § Answered when is met in either direction. The operator may recommend close; the
   close itself is owner-held.

Finish `goal.md` on these answers, keep every operator-derived line marked `[AGENT]`, and if
you judge the grounding evidence sufficient, set `Status: grounded`. If anything still
genuinely blocks grounding, say so with the same numbered precision. Do not run `git commit`;
do not start any task.
