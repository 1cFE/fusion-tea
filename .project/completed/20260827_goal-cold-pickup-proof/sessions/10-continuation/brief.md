# Operator brief — continue the goal at work/orchestration/goals/cryo-volume-basis/

You are a fresh session operating the repository's goal layer as the round agent.

## What you may read
The goal directory `work/orchestration/goals/cryo-volume-basis/`;
`work/orchestration/GOAL_RUNBOOK.md`; `work/orchestration/goal-templates/`;
`.claude/skills/run-goal/`; `.project/adr/`; and the native repository — `models/`,
`knowledge/`, `work/`, `exploration/`, `modeling_project/`, `docs/`, `scripts/`.
You may **not** read `.project/active/goal-cold-pickup-proof/`, any `.orchestrate-logs/`
directory anywhere, or anything under `~/goal-proof-logs/`.

## What to do

Continue this goal per `work/orchestration/GOAL_RUNBOOK.md`: read the goal directory,
take the open round from wherever the trail actually stands, and decide for yourself what
it needs next. Run tasks one at a time in the runbook's shape. When a task reaches a
reserved gate, write the return the runbook prescribes and bring the gate to the operator
in your final message rather than acting through it. Do not run `git commit` — the
operator owns commits. Do not push, merge, or close items.
