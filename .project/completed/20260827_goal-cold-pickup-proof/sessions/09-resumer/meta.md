# Run 09 — resumer
- Session: `edbfb4dc-8993-401b-8fbc-63485663bcab`; cost $1.68; 130 events; kept.
- Command: `claude -p --model opus --output-format stream-json --verbose --permission-mode bypassPermissions --max-budget-usd 10 < brief.md`, log dir `~/goal-proof-logs/09-resumer/`. Brief gave the goal directory and repository only — no ownership hint, no mention of an interruption.
- What it did: found the unreturned start on its own; read native state as truth (mint landed, disposition row did not); wrote `### Stop — interruption` with a full cited-ref walk (all digests unmoved; BACKLOG's move attributed to T-001's own mint); judged the remaining half of T-001's recorded scope resumable and appended the one joined disposition row (`model fix`, "routed, not yet answered", next ref WI-032), sighting untouched; ran `tests/study/test_records.py` (7 passed) to prove the join; wrote `### T-001 return — COMPLETE` with five-field decisions; ended with `### Stop — handoff`.
- Non-repetition, three ways: 0 `add-item` strings in any tool input; `grep WI-032 work/BACKLOG.md | sha256sum` identical pre/post (`79b7ab7f…`); WI-032 rows still exactly the frontmatter block + rendered table row.
- D6 measured: § Resuming an interruption's "write the missing return if the native evidence shows the task reached an outcome" was read as authorizing completion of the *already-scoped* remaining half, not just transcription. Contract observation surfaced by the session: ADR-004 has no "routed, undecided" category; it recorded `model fix` for the routing and said in words the item may still conclude "keep it held", declining to invent a category.
