# Operator brief — open a round on cryo-volume-basis

You are a fresh session operating the repository's goal layer as the round agent.

## What you may read
The goal directory `work/orchestration/goals/cryo-volume-basis/`;
`work/orchestration/GOAL_RUNBOOK.md`; `work/orchestration/goal-templates/`;
`.claude/skills/run-goal/`; `.project/adr/`; and the native repository — `models/`,
`knowledge/`, `work/`, `exploration/`, `modeling_project/`, `docs/`, `scripts/`.
You may **not** read `.project/active/goal-cold-pickup-proof/`, any `.orchestrate-logs/`
directory anywhere, or anything under `~/goal-proof-logs/`.

## The operator's framing

The round's interest is the held cryo inputs in this package, starting with
`vol_cold_cryo`. Open Round 1 per `work/orchestration/GOAL_RUNBOOK.md` § Opening and
closing a round: write the strategy revision in `trail.md`, then run tasks one at a time.

Your first task, from the operator: **route discovery row `20260823-magnet-technology-ab#2`
to the modeling item its Home column names** — "modeling item under the MFE cost modeling
epic" (`exploration/stellarator_e2e/studies/DISCOVERY_LOG.md`, epic registered in
`work/BACKLOG.md`). Routing means the native work item exists in the modeling PM and the
row's disposition is updated per the runbook's § The discovery log.

Follow the runbook's task shape exactly: scope first, then the start line — written before
the first native side effect, naming the native target and the artifact you expect (here,
the minted work item) — then the work through the native workflow's own operations, then
the return. Do not run `git commit` — the operator owns commits. Do not push, merge, or
close items.
