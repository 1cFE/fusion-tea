# Brief — close stage — GSTH Item 4 (goal-cold-pickup-proof)

Close `.project/active/goal-cold-pickup-proof/` (GSTH epic Item 4). State: run complete, audited Certify 2026-08-26 (`audit.md`; Criterion 8 certified not-exercised-as-designed); audit findings F1–F4 fixed and the external log-dir check closed post-audit (`41b2fcc6`); epic Item 4 criteria all closed 2026-08-27 by owner ruling (`4a8de283`). The proof's live goal `work/orchestration/goals/cryo-volume-basis/` is itself closed by owner ruling R3 (`e891b23a`) with WI-032 archived — those artifacts are kept product, not part of this item's archive.

The owner has authorized this close (2026-08-27). Do what `/_my_close` prescribes: verify criteria state, archive the item directory per convention (the `sessions/` transcripts go with it — they are the evidence), update trackers. Do not push or merge. End with `ARTIFACT: <path>`.
