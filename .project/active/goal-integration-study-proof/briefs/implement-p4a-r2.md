# Resume 9 — checkpoint C-001.r1 REFUSED; revise for r2

The fresh critic's verdict is appended to the trail and committed (309519e3 -> see
`### Checkpoint C-001.r1`). Reading class ANSWERED upheld. Three blocking findings, four
non-blocking corrections. Revise and resubmit; this is revision 1 of the cap of 2.

## Required by the refusal

1. **Finding 1 — land the artifact before the row.** Prepare the record ADDENDUM as a new
   file in the record directory (the committed record's existing files stay byte-identical;
   state in the addendum that snapshot.json is NOT edited and why its quoted sha256 stands —
   critic correction 6). The addendum carries: the 130/150 MW near-fence table with its
   derivation, rec_frac on the excluded set, the teax revision, the snapshot identity nits,
   and the two-package-paths explanation (state, no repair). Confirm the studies/2026*
   byte-unchanged invariant explicitly against this path: the invariant protects PRIOR
   records; this round's own record gains an addendum — argue that in the addendum header,
   citing the plan's constraint wording. The orchestrator commits the addendum BEFORE the
   dispositions land; your revised #3-row sentence may then cite it.
2. **Finding 2 — disposition `20260821-power-cycle-ab#1` and `#2`** (or argue non-touch in
   writing; the critic showed record.md:182-183 carries both ids — dispositioning looks
   right). Likely class: model fix, standing, re-confirmed at the new pin, no new work
   routed this round beyond the standing gaps.
3. **Finding 3 — fix the #3 disposition** to state what 0534c77b already did (fourth
   instance re-pinned by the orchestrator this round) and dispose of what remains (drop vs
   keep the literal is a recommendation about future practice; say it that way).
4. **Corrections 4–7**: reconcile the instance count with an explicit enumeration; reframe
   the minting conflict as resolved by GOAL_RUNBOOK § The discovery log's own rule; fix the
   item-14 citation to the direct measurement the critic named (empty git log
   ffa5c54c..8099217b~1 over generated/, and 8099217b's own diff); name the follow-up actor
   and gate for the minted CAS10-guard item per ADR-004 (WI-032 precedent).

Write the revision as appended trail entries (`### Checkpoint C-001.r2 submission` or per
runbook convention — never edit r1 or prior text). Prepare the addendum file in the record
dir. NO commits; no log appends yet; end with: the addendum file path, the revised
disposition table, and what changed vs r1.
