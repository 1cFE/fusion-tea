# Resume 12 — Phase 8: verification record and wrap

epic_evidence.md and the plan notes are committed. Final leg, plan § Phase 8:

1. `.project/active/goal-integration-study-proof/verification_record.md`, mirroring
   WI-033's shape:
   - Commit table (label → SHA → content) in branch order from the spec commit 45b61e1e
     through the epic-evidence commit — verify ancestry linearity with
     `git merge-base --is-ancestor` over each consecutive pair and say so.
   - Each spec ruling and each epic § Item 6 success criterion (plus criterion 8): the
     check run and the result, with resolving paths — no row reads "see above".
   - Deviations, each with its authorizing ruling: plan D1-D3; the owner rulings (oracle
     carry, framing set, option-1 evaluability bound was orchestrator-tier with comparand
     precedent — state the tier honestly; goal close set); the orchestrator-performed
     commits; the timeout interruption + three-session resume arc; the harness kill of the
     first hand-route launch; the lean-shape deviations (no design.md; briefs as the
     stage-instruction record).
   - Owner-held remainder, stated as such: push/PR/merge (one PR ships WI-033 + Item 6);
     coding-PM item close for goal-integration-study-proof; epic close-out; WI-034 is
     backlog, not this item's.
2. Update `.project/CURRENT_WORK.md`: the GSTH section brought current through Item 6
   implemented (cite the epic, this item dir, the record, the goal close — cite, don't
   restate). Preserve unrelated content.
3. Plan: tick Phase 8 boxes (except the fresh-audit line — the audit runs after your leg),
   fill `### Phase 8 Completion`, move the header Status to
   "Complete pending audit (2026-08-29)".

Hard rules unchanged: no commits; end with the verification record's criterion-table
summary (one line per row: met/deviation) and the file list.
