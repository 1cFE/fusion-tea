# Product-Lens Ledger — goal-cold-pickup-proof (GSTH Item 4)

Append-only. One block per run; a changed gate is a new dated block. Resolution is by citation of a finding id, never by recency.

## spec — 2026-08-26 — rev `.project/active/goal-cold-pickup-proof/spec.md`

Epic: GSTH

Instruction-provenance note: `~/.claude/scripts/product-lens.md` was unreadable in this session (it resolves to `/home/reid/agentic-project-init/claude-pack/scripts/product-lens.md`, outside the allowed working directory; Read and Bash both denied, non-interactive so no grant was possible). This is the same limitation the Item 1 audit-hop and Item 3 blocks record. The block follows the §3 format as evidenced by `.project/active/goal-harness-contract/product-lens.md` and `.project/active/goal-integration-seam/product-lens.md`. The §4 smell list was not readable; the smells checked are named explicitly below.

Point (re-derived): Item 1's goal layer was written entirely by the person who designed it and has never been used. Item 4 is the run that decides whether it works. Three bets get tested cold, each by a session that did not author what it is handed: a stranger grounds a real operator-chosen goal and an ungrounded draft cannot start a task; a second stranger resolves a write-ahead start that has no return, reading native artifacts as truth and neither repeating completed native work nor losing an open gate; a third stranger catches a seeded scope or comparison-meaning drift, accounts for every touched discovery row, and accepts or corrects the learning delta before it lands. The round closes bounded — no pin, no study — on one of the six triggers, with a mandatory round result and **five-field decisions** so the owner can replay the judgment. And the owner's rule governs the whole thing in both directions: a recorded run failure is the only thing that promotes a hardening mechanism, and its absence is the only thing that justifies leaving the prose route alone. [source: `.project/concepts/goal-driven-model-development-harness.md` § Owner's Words (goal doc, resumes, critic placement, who reads critiques) and Success Criteria 1, 3, 6–8 (owner); `.project/backlog/epic_goal_strategy_task_harness.md` § Executive Summary, § Critical Success Factor, § Success Criteria, § Hardening rule `[OWNER]`, § Item 4 (Scope 1–4, Out of Scope, Success Criteria, Deliverables); `.project/concepts/goal-strategy-task-harness-design-review.md` § Resolutions C1 and P2/M4 (owner); `work/orchestration/GOAL_RUNBOOK.md`; `.project/adr/001-007`; grade: owner / owner-ratified agent decomposition]

Falsifier: Item 4 closes while the proof ran without ever exercising the surface the owner asked for by name — the five-field replay record; while a stated success criterion could not be met against Item 1 as shipped and nobody said so before the run; while a cold resumer proved only "did not duplicate" and never touched the open-gate and moved-citation half of the same criterion; or while the goal directory that must stay a real goal is constrained by a rule the spec invented and then contradicts three sections later.

Findings:

- spec-F1 [DO] **The five decision fields — the owner's replay record — are absent from the spec entirely.** Concept Success Criterion 6 is `[OWNER]` and named by the epic in Item 4's Required Reading: "one entry per decision, every entry with all five fields filled: the finding, the decision and its reason, the tier, who decided, and what changed. Every change an entry names resolves to a path, an id, or a commit" (`goal-driven-model-development-harness.md:48`). The owner's reason is on the record too — he wants to "'replay' the evolutions and make sure the judgements for resolution were good" (§ Owner's Words, who reads critiques). Item 1 built the surface: the task return "carries ... five decision fields for every goal-level decision made" (`GOAL_RUNBOOK.md:128`), the trail template gives the block literally (`goal-templates/trail.md:43-45`), and the runbook says outright "Those five fields are what make `trail.md` the replay record without a second ledger." The epic's own Item 4 success criterion carries it: "A no-pin/no-study round closes with a mandatory `RoundResult`, **five-field decisions**, and no silent stop." The spec's Bounded closure criterion (`spec.md:37`) enumerates seven round-result fields and stops; the words *five*, *decision fields*, and *tier* appear nowhere in the document. This is the one surface that makes the lean route's central claim — no second ledger — checkable, and Item 4 is the only run that will exercise it before Items 5 and 6 build on it. — `.project/concepts/goal-driven-model-development-harness.md:48` (SC 6, **owner**); `.project/backlog/epic_goal_strategy_task_harness.md` § Item 4 § Success Criteria (**owner-ratified agent decomposition**); `work/orchestration/GOAL_RUNBOOK.md:128`; `work/orchestration/goal-templates/trail.md:43-45` — disposition: BLOCK (expected: the Bounded closure criterion names the five decision fields and requires each "what changed" value to resolve to a path, id, commit, or `none`; one `[NEED]` under § Closure, drift, and review carrying SC 6 with its owner grade)

- spec-F2 [DO] **"The grounding gate holds" cannot be met against Item 1 as shipped, and the spec does not say so.** The criterion at `spec.md:34` requires that a draft missing *any* of repository evidence, the answer contract, invariants, limits, or reserved gates be refused, and that "the refusal names which fields are missing." The gate that exists on disk keys on one field: "A goal whose § Grounding evidence is empty stays `Status: draft`" (`GOAL_RUNBOOK.md:72`), and the template says the same ("Empty means `draft`"; `goal-templates/goal.md` § Status, § Grounding evidence). Nothing in the runbook, the templates, or the skill refuses a draft for missing invariants, limits, or reserved gates, and nothing tells a refusing session where to *write* a refusal — a draft goal has no open round, so there is no trail entry kind for it, yet the criterion demands evidence that "the refusal came before the first `### T-001 scope` entry." Two things follow, and the spec surfaces neither. A cold session handed a draft missing reserved gates will either invent the rule (and the proof records a builder-shaped success) or fail to refuse (and a stated success criterion goes unmet). Item 1's own audit ledger already pre-assigned this class: "any finding Item 4's cold-grounding proof raises against it is an **Item 1 defect**" (`goal-harness-contract/product-lens.md:175`). The spec's job here is not to fix Item 1; it is to say which half of its own criterion is a test of the artifact and which half is a predicted prose failure, so the run's outcome is legible either way. — `work/orchestration/GOAL_RUNBOOK.md:72`; `work/orchestration/goal-templates/goal.md` § Status (**[INHERITED: Item 1]**); `.project/backlog/epic_goal_strategy_task_harness.md` § Item 4 Scope 1 `[OWNER 2026-08-25]`; capture-fidelity Law 4 — disposition: BLOCK (expected: the criterion states which refusals Item 1 supports today (grounding evidence) and which are predicted prose failures to be recorded rather than pre-fixed, and names where a refusal is written; the mismatch recorded as an Item 1 defect candidate under § The hardening bar's recording discipline)

- spec-F3 [DO] **A `[HARD]` invented by the spec contradicts the spec's own open question and would foreclose an epic deliverable.** `spec.md:50` reads: the goal directory holds "**exactly** `goal.md`, `trail.md`, and `learnings.md`", cited to `GOAL_RUNBOOK.md` § The five surfaces. That section says no such thing — it names three goal files, says "Copy them; the headings are the contract", and does not bar anything else from the directory. Meanwhile `spec.md:97` leaves open "where the cold-session evidence physically lives — inside the item directory, or **under the goal directory**", and the epic's Deliverables list "Cold non-builder and resumer evidence referenced from the verification record" without fixing its home. As written, the `[HARD]` decides an open question in one direction, at the strongest force the spec has, on a citation that does not support it. A `[HARD]` is the one grade design may not re-derive against. — `work/orchestration/GOAL_RUNBOOK.md:23-35` (§ The five surfaces, the cited source) versus `.project/active/goal-cold-pickup-proof/spec.md:50` (**agent overstatement of an `[INHERITED]` source**); `spec.md:97`; capture-fidelity Law 2 — disposition: DISPOSE-and-proceed (expected: the `[HARD]` narrowed to what the runbook actually binds — the three files exist, copied from the templates, headings unchanged — with the exclusivity claim either dropped or restated as `[INFERRED]` so the open question at `:97` stays genuinely open)

- spec-F4 [DO] **The resume criterion proves half of the owner's resume criterion.** Owner SC 3 is three-part: a fresh session continues an interrupted run "without re-running completed stages, duplicating side effects, **or losing open gates**" (`goal-driven-model-development-harness.md:45`). The spec's Interrupted resume criterion (`spec.md:36`) is entirely about non-duplication — the byte-identical artifact check is good and is stronger than the epic asked for — and says nothing about gate or limit state surviving the session boundary. The runbook makes the same omission expensive in a second place: its resume procedure has four ordered steps and step 3 is "walk the round's cited refs for external mutation" (`GOAL_RUNBOOK.md:216`), a check whose own section says "the resumer and the fresh reviewer. **Nothing else can catch it, and that is deliberate**" (`:202`). Item 4 is the only place either half gets a live test before Items 5 and 6 depend on it. This is not asking for more scope: the resume session is already running the procedure, so the cost is naming what its evidence must show. — `.project/concepts/goal-driven-model-development-harness.md:45` (SC 3, **owner**); `work/orchestration/GOAL_RUNBOOK.md:202,212-219`; `.project/backlog/epic_goal_strategy_task_harness.md` § Item 4 SC 2 (goal directory alone identifies "open gate/limit state") — disposition: DISPOSE-and-proceed (expected: the Interrupted resume criterion adds that the resumer's record shows the open gate/limit state carried across the session boundary intact, and that step 3's cited-ref walk was performed and its result recorded — a stop of kind `external mutation` or an explicit "none moved")

- spec-F5 [NOTE] **Owner SC 1's substance is reduced to a citation-format check.** The owner's criterion says what grounding evidence *is*: "which package, which entry keys and constraints can respond, what the discovery log and knowledge base already say" (`goal-driven-model-development-harness.md:43`, owner), and the runbook gives the reason — "Grounding is what stops a run from spending a week on a question the repository already answered" (`:72`). The spec's Cold grounding criterion (`spec.md:33`) requires grounding evidence "cited as `<path>@<sha>` (or explicitly unpinned)" — a form check that a goal.md listing any three repository paths passes. The narrowing is inherited, not introduced: Item 1's runbook already reduced it to "repository paths for what is already known", and the template to a citation-format paragraph. Recording it here because Item 4 is the proof that decides whether that narrowing was safe, and because the chosen question — `vol_cold_cryo` against the ampere-turns the model carries — has exactly the package/entry-key/knowledge-base chain the owner named, so the substantive check is available at no extra cost. — `.project/concepts/goal-driven-model-development-harness.md:43` (SC 1, **owner**); `work/orchestration/GOAL_RUNBOOK.md:68,72` (**[INHERITED: Item 1]**) — disposition: NOTE (expected: one clause in the Cold grounding criterion asking that the grounding evidence name the package, the entry keys and constraints that can respond, and what the discovery log and knowledge base already say — or an explicit line recording that Item 1 narrowed this and that the proof tests the narrowed form)

Reverse check: CLEAR — every entry in § Known Requirements traces to an owner ruling in `align.md`, an epic Item 4 scope line, an Item 1 artifact cited by path, or a marked `[INFERRED]` reading that says so and stays challengeable. No orphan scope. The three Non-Goals that could have been scope creep are correctly routed: route-equivalence to Item 6 (epic SC 10), the native seams to Items 5–6 (epic § Parallel work, which states the reason — the lean contract is tested *before* the repairs land), and unattended dispatch to ADR-003. Nothing from the hardening path enters: § The hardening bar states the owner's rule in both directions, and the `[INFERRED]` line separating "recording a failure" from "fixing it" is the right boundary for a proof item.

Notes (no finding):

- **The Problem section is the strongest part of the spec, and it earns the item.** It states the three bets plainly, then adds the owner's reason as a distinct second cause and draws the consequence correctly: without the run there is nothing to promote a mechanism on *and* nothing that justifies leaving the prose route alone. That symmetry is the P2/M4 ruling read properly rather than as a licence to do nothing.
- **The freshness discipline is captured at the owner's strength.** `spec.md:56` carries the session-boundary rule with the `[OWNER]` quote and its path, and `:58` bars handing any cold session the spec, the item directory, or the seeded-drift record. `:57` — a session whose inputs are not recorded does not count as proof — is the right auditability rule and is not in any upstream source; it is a good `[NEED]`.
- **The seeded-drift discipline is right.** Recording the seed's identity and expected detection *before* the review session runs, and requiring a plausible drift rather than a flagrant marker, is what makes the review a test rather than a demonstration.
- **Owner SC 1's other half is not proven by this item, and the spec does not say where it goes.** The owner's stranger is the *operator* ("an operator who did not build the repo runs the goal dialogue"); here the orchestrator — who built the layer — plays operator, on the owner's own 2026-08-26 ruling, and the freshness is on the agent side only. The epic relocated it first (Item 4 Scope 1, `[OWNER 2026-08-25]`), so this is twice-ratified, not a silent change. It is substantially mitigated by `spec.md:59`, which makes the orchestrator's notes on where the exchange stalled and what the operator had to supply a deliverable — that is the honest instrument for a builder-operator. Recorded so the residual gap has a name at close rather than being discovered by the first real operator.
- **`20260823-magnet-technology-ab#2` is verified `unrouted` in the live log**, so the discovery-row criterion has a real target and is not a hypothetical.

Smells:

- *Deliverable naming drift at the boundary the epic checks.* The epic's Deliverables name `.project/active/goal-cold-pickup-proof/verification_record.md`; the spec says "an honest report" (`:27`), "the proof report" (`:60`, `:79`), and never uses the epic's filename. § Related Artifacts lists `design.md` as to-be-created and omits the verification record entirely. Cheap to align, and it is the artifact the epic's last success criterion is scored against.
- *The operator-notes artifact was made a deliverable at Align and became prose in the spec.* `align.md:36-38` records the owner wanting the orchestrator's notes on how the exchange works and says "add an operator-notes artifact to the deliverables." The spec carries the obligation as a `[NEED]` at `:59` but names no artifact and adds no entry to § Related Artifacts. The obligation survives; its home does not.
- *Seven open questions for a one-day item, and three of them are load-bearing on whether the proof is valid at all* — how a cold session is obtained and captured, who authors the ungrounded draft and whether the refusal comes from the same session, and whether the interruption and closure ride the same task. That is design's work and correctly deferred, but the item is estimated at 1 day with 1.5h of design, and the freshness of every session downstream depends on the first of those three.

Smells checked, not fired: *ownership change without saying so* — does not fire; the spec states the study executor keeps first-sighting rows (`:74`), restates ADR-004's append-only join, and bars runbook/template/ADR changes outright (§ Non-Goals). *Baseline preserves behavior contradicting the product's reason for existing* — does not fire. *Consumer compensating for a producer or platform guarantee* — fires weakly on spec-F2 (the cold session would be compensating for a gate Item 1 did not build); folded into that finding rather than left in the rubric. *A test passes only because it selects one favorable route* — fires weakly on spec-F5 (the cold-grounding criterion is a heading-and-format checklist a builder-operator satisfies without exercising the owner's substantive bar); folded into that finding.

Gate: BLOCKED (spec-F1, spec-F2)

Epic ledger state: `.project/backlog/epic_goal_strategy_task_harness.md` § Product-Lens reads CLEAR (second block, 2026-08-25, owner-dispositioned decomposition). This spec-hop block does not change that gate.

### Disposition — 2026-08-26 — spec author, same hop

Gate: CLEAR. All five findings dispositioned in `spec.md` at this revision; no finding deferred.

- **spec-F1 — FIXED.** New success criterion "The judgment replays from the trail alone": all five decision fields named, the tier enum spelled out, the no-second-ledger claim stated, and a four-field decision declared a recorded prose failure rather than a formatting nit. Graded `[OWNER]` to concept SC 6.
- **spec-F2 — FIXED.** New § "A predicted prose failure" states the conflict plainly: the epic asks for five field classes, `GOAL_RUNBOOK.md:72` defends one, and no runbook heading exists to write a refusal into for a goal with no open round. The grounding-gate criterion now reads as one test and four predictions, met either by refusal or by a per-class record of why it did not happen. Repairing the runbook is named as Item 1's, not this item's.
- **spec-F3 — FIXED.** The invented `[HARD]` "exactly three files" is gone; the requirement now states the three files the templates supply and points at the open question on where cold-session evidence lives.
- **spec-F4 — FIXED.** The resume criterion now carries all three parts of owner SC 3 — no re-running, no duplicate side effects, no lost open gates — and requires the cited-ref external-mutation walk from `GOAL_RUNBOOK.md` § When a cited artifact moves.
- **spec-F5 — FIXED.** New `[INHERITED]` requirement: grounding evidence is checked for substance (package, responsive entry keys and constraints, discovery log, knowledge base), and well-formed citations carrying none of it do not pass.
- **Smells.** Deliverable naming reconciled — the proof report *is* the epic's `verification_record.md`, stated as one artifact under one name. The operator-notes artifact is restored to a named artifact in the item directory rather than prose with no home. The open-question list keeps seven entries but now marks the three that are load-bearing on proof validity and requires design to settle them before anything runs.

### Note — 2026-08-26 — spec-review revision, gate unchanged

Gate stays CLEAR. `spec-review.md` (verdict Revise) landed after the disposition block above; ten findings incorporated. Two touch this ledger's reasoning and neither reopens it:

- **spec-F1's fix survives and sharpened.** The five-decision-field criterion is unchanged; the spec-review's L3-2 added the git-ancestry ordering predicate that makes its sibling criteria auditable.
- **The reverse check's "§ hardening bar states the owner's rule in both directions" was right but the list was wrong.** Review L1-3 found the `[OWNER]` hardening list carried seven items where the owner's ruling names five (`goal-strategy-task-harness-design-review.md:209`); "concurrent goal run" and "unattended dispatcher" are ADR-003 premises, barred outright, not proof-gated. Corrected in both the Problem section and § The hardening bar. This strengthens the reverse check rather than contradicting it — as previously written, a recorded failure could have appeared to promote unattended dispatch, against the spec's own Non-Goal.

spec-F5's substance requirement and spec-F4's three-part resume criterion are untouched by the revision.
