# Operator brief — review the closed round at work/orchestration/goals/cryo-volume-basis/

You are a fresh session running the round review for this goal. Your session did none of
the round's work.

## What you may read
The goal directory `work/orchestration/goals/cryo-volume-basis/`;
`work/orchestration/GOAL_RUNBOOK.md`; `work/orchestration/goal-templates/`;
`.claude/skills/run-goal/`; `.project/adr/`; and the native repository — `models/`,
`knowledge/`, `work/`, `exploration/`, `modeling_project/`, `docs/`, `scripts/`.
You may **not** read `.project/active/goal-cold-pickup-proof/`, any `.orchestrate-logs/`
directory anywhere, or anything under `~/goal-proof-logs/`.

## What to do

Round 1 of this goal is closed with a written result. Run the fresh review per
`work/orchestration/GOAL_RUNBOOK.md` § The fresh review: read the round end to end,
perform every check that section lists, and write the `### Round 1 review` entry in
`trail.md` with your verdict. Settle the proposed learning delta — accepted, corrected,
or rejected — and append only what you accept to `learnings.md`, at review time, as the
runbook directs. Do not run `git commit` — the operator owns commits.
