# Verification record — nine criteria against disk

*(written in Phase 10. One row per spec criterion: criterion, producing run, paths checked,
the check, verdict. Plus: the three ancestry checks with pasted output, the eight Required
Invariant checks, the failures section, and the explicit hardening verdict.)*
