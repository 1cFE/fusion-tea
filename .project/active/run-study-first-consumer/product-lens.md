# Product-Lens Ledger — run-study-first-consumer

Append-only. One block per run; a changed gate is a new dated block. Resolution is by citation of a finding id, never by recency.

## spec — 2026-08-21 — rev 5247e081 | `.project/active/run-study-first-consumer/spec.md`
Epic: RUN-STUDY
Point (re-derived): Item 6 is the capability's first complete consumer. A goal-and-scope-only `run-study` invocation runs the magnet A/B comparison (shape chosen by design-stage research; new modeling routed as its own item), with indicators and the owner's ruling on every `no_constraint_response` axis before a point runs; every point through stock teax, one store per compatibility tuple; oracle verification for this demo only; an immutable record carrying LCOE and every executing constraint's qualified identity and status per arm; findings indexed in the discovery log; a fresh administrator synthesizing from the record alone; the policy moved to `modeling_project/STUDY_POLICY.md` only after the owner approves the final draft. All of it inside the demo's standing rules: ARIES-CS stays sealed, and no value enters the model without an owner-accepted source.   [source: `.project/concepts/run-study-skill.md` SC 1–5 + Settled; `.project/backlog/epic_run_study_capability.md` Success Criteria + Item 6; `.project/active/run-study-first-consumer/align.md` §1–3; `.project/concepts/stellarator-mbse-demo.md` Settled (quarantine); `modeling_project/OVERVIEW.md` Traceability Requirements, grade: owner]
Falsifier: Item 6 closes while any of these holds — the invocation carries process, verification, or reporting instructions; a point runs on a `no_constraint_response` axis with no recorded owner ruling; the policy is committed without the owner's review; the fresh administrator needs executor memory to recover framing, LCOE, or constraint outcomes; a second-arm value with no owner-accepted source enters the model; an ARIES-CS figure enters the comparison while `knowledge/holdout/aries-cs/PROTOCOL.md` reads `status: sealed`.
Findings:
- spec-F1 [DO] The design's first job is research for a second magnet arm (align §2: no LTS cryo, heating, or markup values exist in-repo), but the spec carries no requirement that this research stays clean-room of ARIES-CS — the one public LTS stellarator design with costing. `PROTOCOL.md` is `status: sealed`, `revealed: null`, and binds "all stellarator-demo model-development and research sessions" until the hold-out comparison stage, which the on-hold demo has not reached. — `.project/concepts/stellarator-mbse-demo.md` Settled: quarantine strictness + clean-room admissibility; `knowledge/holdout/aries-cs/PROTOCOL.md` §1–3 (owner) — disposition: BLOCK (expected: one `[HARD]` line in Known Requirements binding the design research and any new modeling to the hold-out protocol; CLAUDE.md enforces it per session, but the spec is what the design is audited against)
- spec-F2 [DO] The spec defers "whether one arm's values need sourcing" to design but never states the rule the design must obey: every value for a second arm comes from a source the owner accepts, never an agent default, and carries its citation. — `.project/active/run-study-first-consumer/align.md` §2 (no-fallbacks rule); `modeling_project/OVERVIEW.md` Traceability Requirements ("MUST NOT be silently invented") (owner) — disposition: BLOCK (expected: one `[NEED]` line; SC 2's owner ruling on the comparison then covers the sources, not only the arms)
- spec-F3 [DO] The epic's [OWNER] criterion 4 lands with Item 6 (epic Next Action: "Criteria 1-4 … land with Item 6"), but the spec carries only its first half (discovery log, SC 8). The second half — at least one proof-of-life process lesson inherited from exactly one home, with the record citing that home instead of re-deriving it (e.g. stratified verification sampling from `scripts/study/verify.py`, record §13) — has no success criterion, and neither `runbook.md` nor `record-template.md` names it. — `.project/backlog/epic_run_study_capability.md` Success Criteria bullet 4 (owner); `.project/concepts/run-study-skill.md` SC 5 — disposition: BLOCK (expected: one success-criterion line naming the inherited lesson and where the record cites its home)
- epic-F1 (owner) — resolved on the epic 2026-08-19 (FIXED, authority: owner); the spec carries its basis as SC 7 (fresh administrator from the new record alone). Reference only.
- epic-F2 (owner) — resolved on the epic 2026-08-19 (FIXED, authority: owner); the spec carries its basis as SC 5 (LCOE plus qualified constraint identity and status per arm). Reference only.
Notes (no finding): the `[NEED]` pause rule quotes an `[OWNER]` 2026-08-21 line ("Item 6 should PAUSE …") that `align.md` does not carry (align says "the owner sequences it"); the spec is that quote's only durable home — acceptable as a first capture, but `align.md` must not be cited as its source. Declined-axis indicators (concept SC 2) and named review outcomes (epic Item 6 SC 4) are not named in the spec but are carried transitively by "every template section filled" (record-template §8, §14; runbook step 3).
Smells: none fired (spec stage; §4 smells are design/audit tripwires).
Gate: BLOCKED (spec-F1, spec-F2, spec-F3)

## spec — 2026-08-21 — dispositions (same session, after the block)
- spec-F1: FIXED — authority: owner (`PROTOCOL.md`) — basis: `[HARD]` line added to Known Requirements binding the design research and any new modeling to the sealed hold-out.
- spec-F2: FIXED — authority: owner (no-fallbacks; OVERVIEW Traceability) — basis: `[NEED]` line added; the owner's ruling on the comparison now explicitly covers its sources.
- spec-F3: FIXED — authority: owner (epic criterion 4) — basis: success criterion added for the inherited process lesson cited from one home.
- Note on the pause quote: `align.md` § 2 now carries the `[OWNER]` 2026-08-21 ruling verbatim, so it is the quote's source.
Gate: CLEAR

## spec — 2026-08-21 — amendment (two studies; owner rulings)
Owner rulings added after the block was cleared: two studies (REBCO vs Nb3Sn magnets; Rankine vs sCO2 conversion), two records; REBCO ceiling bound at 24.9 T; a modeling-PM research round before execution. Success criteria 2–4 and 10 amended; `[NEED]` lines added. Each new owner item is quoted or dated in `align.md` § 2a. No finding reopened: the amendments widen scope on the owner's word and add no agent-grade requirement.
Gate: CLEAR
