# Grading — Depth Rubric v1, Row 1 re-grade (post WI-037)

**Date:** 2026-09-01
**rubric_version:** `.project/active/demo-depth-rubric/rubric.md@dc0f0b6d` — confirmed by this grader: `git log -1 --format=%h -- .project/active/demo-depth-rubric/rubric.md` returns `dc0f0b6d`.
**model_version:** `fbf700931421327c5f24ec9f824ad2ffd31dc2e7` (`git rev-parse HEAD`, branch `feat/demo-maturation`; all `models/` files read at this commit).
**Package identity (for executed-behavior claims):** `exploration/stellarator_e2e/studies/20260901-sustainment-fence/results/baseline_result.json` — `executed_under.identity_digest 41e06ecb430cf894309ea5d00174d26c079ebe3d63c892b4dbaad8f0dcb44244`, `case_id stellarator-baseline-point-v1:c0000`; semantic fingerprint `5b9abdfce2e086b6...` (CANDIDATE integration return `work/orchestration/goals/operating-point-closure/evidence/T-004_integration_return.json`, ten gates; census `tests/models/data/mfe_census.json` derived against the same semantic fingerprint). Baseline headline `lcoe_calc__lcoe` = 307.08712042841586; eight verdicts, `sustainment_ok` **violated** (disclosed and expected — see findings).
**Grader:** fresh grader session, 2026-09-01 (non-author: did not author the rubric, WI-037, the goal round, or any review of it; inherited no conversation context; received the spawn prompt and the pointer-only evidence map `evidence-map-r1-regrade.md`, no proposed scores; every citation below was opened and read by this session).

**Provenance caution respected:** the Row-1 gate note in the frozen rubric ("Rung C ... proposes reopening it to the owner") is stale text; the authority is `.project/concepts/stellarator-demo-maturation.md` § Corrections — 2026-09-01 (read by this grader: confinement was never owner-ruled out of scope; no agent may cite a Rung C gate as authority). The row's anchor table is unaffected and is graded as written.

Scoring applied exactly as written: highest level whose full evidence test is satisfied, integers only, no partial credit; the Row-1 anchor table is the operative test; a bound value checked against a bound value is not P3; calcs on the handwritten codegen seam cite both the declaration and the executable impl; correctness defects are evidence-integrity findings attached to cells, never levels. Prior cells (`grading.md` R1.P/R1.S) were read for the delta comparison only, never as anchors.

---

## Row 1 — Plasma geometry, fusion performance, operating-point closure

**cell_id:** R1.P
- **rubric_version:** rubric.md@dc0f0b6d · **model_version:** fbf70093 + package 41e06ecb.../stellarator-baseline-point-v1:c0000
- **score:** 3
- **anchor_satisfied:** "An achievable operating point: a confinement/transport relation links field and heating to density and temperature, and a beta, density, or power limit pushes back on the choice" (the P2 anchor remains satisfied underneath: "Fusion power, beta, wall load, peak field forward-computed from geometry, profiles, and field, verified in execution" — verification at this pin passed 12 channels at worst relative deviation 5.33e-16, `20260901-sustainment-fence/record.md` §13 / `results/verification_summary.json`).
- **anchor_reading (recorded because the spawn prompt asks for it):** the operative verbs in the anchor are *links* and *pushes back*, and both are satisfied by the forward-sustainment architecture. The confinement/transport relation is ISS04 in closed form inside 'Plasma Sustainment': τ_E = f(a, B, ι_2/3, n̄, R, W_th) with the P = W/τ_E substitution, and the balance p_aux_required = p_rad + W_th/τ_E − p_alpha_heat ties field (B, itself computed from coil current), heating (required vs installed), density (n_e0 lever, line-averaged inside), and temperature (T_i0 lever) into one executable relation — changing any of the four moves the sustainment verdict. The power limit ('Sustainment Limit': computed p_aux_required ≤ held installed coupled power) demonstrably pushes back on the lever choice, and the beta limit (whose operand is now computed from the sustainment chain's ash/fuel/T_e0) pushes back as well. The anchor does not say "solves for density and temperature"; it says "links". The lead phrase "An achievable operating point" is satisfied in the stronger of its two defensible readings short of a solver: the model decides achievability for any chosen point (it declares its own printed point unachievable at 50 MW), and achievable points exist and were located in the executed search (nine feasible points, best 293.468 $/MWh at 110 MW installed). Only a reading that requires density and temperature to be *outputs of a solve* fails — see the contestable-anchor note under findings.
- **model_evidence:**
  - Confinement/sustainment chain declared: `models/library/analyses/mfe_plasma_sustainment.sysml:4-162` ('Plasma Sustainment': line-averaged density, ISS04 closed form C = 0.134·f_ren·a^2.28·B^0.84·ι^0.41·n̄^0.54·R^0.64 with τ_E = (C·W_th^−0.61)^(1/0.39), damped A.5/A.6 ash fixed point with quasi-neutral fuel, composed brems + W-line + Albajar radiation, p_aux_required output; discretization/convergence contract in the doc; fail-loudly on non-convergence).
  - Codegen-seam executable (both cited per the scoring rule): `exploration/stellarator_e2e/generated/handwritten/mfe_plasma_sustainment/plasma_sustainment_impl.py:1-219` (`AUTO_IMPLEMENTED = False`, normative; full chain implemented — read in full by this grader: `_sigv_dt` Bosch-Hale, trapezoid N = 200,000, damped half-step ash iteration tol 1e12 m^-3 cap 200, `SustainmentError` raise paths, Albajar port, p_aux_required assembly at `:212`).
  - Power-limit constraint declared: `models/library/analyses/mfe_viability.sysml:148-174` ('Sustainment Limit': `p_aux_required_in <= p_aux_installed_in`, computed operand vs installed coupled power).
  - Wiring: `models/designs/generic_mfe/mfe_plant.sysml:224-244` (`calc sustain` reads `B_in = magnet.B`); `mfe_plant.sysml:54-56` (`magnet.B = field_calc.B_axis` — the field entering confinement is the WI-035 derived field from coil current, not a settable constant); `mfe_plant.sysml:247-260` (beta_calc rebound to `sustain.T_e0/n_D0/n_T0/n_He0`); `mfe_plant.sysml:285-295` (fusion rebound to `sustain.n_D0/n_T0`); retirements at `mfe_plant.sysml:178-181,190-192` (n_D0, n_T0, T_e0, n_He0 retired as plant attributes).
  - Instance: `models/designs/stellarator_09/stellarator_plant.sysml:1047-1050` (`assert constraint sustainment_ok`, operand `sustain.p_aux_required` vs `p_input`), with the expected-violation disclosure doc at `:1038-1046`; the two WI-037 retirement notes at `:516-520` and `:541-547`; the eight image-verified held sustainment facts at `:552-578` (iota_23 0.92, f_ren 1.0, f_alpha_fast 0.95, tau_ratio_ash 8.0, f_suppr_ash 0.5, Z_eff_core 1.20, f_W_core 7.76e-6, Ti_over_Te 0.95); the corrected plasma-coupled `p_input` doc at `:582-583`.
  - Census: `tests/models/data/mfe_census.json` — checked by this grader: `n_D0`, `n_T0`, `T_e0`, `n_He0` absent as entry points; `n_e0`, `T_i0`, and the eight held facts present.
- **runtime_evidence:** baseline (`baseline_result.json`, identity 41e06ecb...): `sustainment_ok` **violated**, other seven verdicts satisfied; `field_calc__B_axis` = 8.999999999999998, `fusion__p_fus` = 2725.363, `beta_calc__beta` = 0.0268396, `wall_load_calc__wall_load` = 3.10538, `peak_field_calc__B_peak` = 24.9. Sustainment operands at the baseline case (`results/oracle_operands.csv`, row `:c0299`): p_aux_required = 90.605 MW vs 50 installed, τ_E = 1.4499 s, p_rad = 228.61 MW, n_He0 = 5.781e19, n_D0 = 1.9519e20, W_th = 551.44 MJ — matching the instance's ~90-vs-50 disclosure. Verification: `sustainment_ok` re-derived per sampled row with both operands resolved, zero mismatches (record §13). Caveat on the six operand *values*: oracle-derived on both sides (finding EI-1 below).
- **study_evidence:** `20260901-sustainment-fence` (record §§3-8, Addendum; synthesis — the administrator's independent recount, all eight violation counts reproduced exactly). The limit genuinely pushes back on the choice, on every operating-point lever: `sustainment_ok` violated 219/334; at p = 50 MW the feasible set is empty everywhere (0/154 — every below-ceiling point fails sustainment, every sustainment-satisfying field sits over the conductor ceiling/stress fences); at p = 110 MW a feasible band exists, bounded **below by sustainment** (I = 14 fails, 15 passes) and above by the ceiling; the density band [0.9×, 1.1×] is closed by recirc below and wall load above; no T on the transect is feasible at 50 MW; the p_input flip sits between 90 and 100 MW. Field is rewarded through confinement and the constrained optimum leaves the beta floor (beta 0.0311 vs 0.05) — the committed inversion of the `20260823-magnet-technology-ab#4` pathology the row names. Record §8: every swept operating-point lever now reaches constraints (I_coil 7, n_e0 5, T_i0 5, p_input 3); no `no_constraint_response` on any swept axis. Beta pushback with computed operands: `beta_ok` violated 69/334 (binds at I ≤ 11 MA and T ≥ 28 keV). Round-1 physics validation: goal `learnings.md` L-001 (chain reproduces printed point A to ≤4% with nothing fitted; reviewer-reproduced) and `evidence/T-002_prototype/NOTES.md` cross-checks cited there.
- **why_not_next:** P4 fails on every conjunct: the operating point is not closed jointly with magnets (the conductor grade B_max = 24.9 is a held bound and was never an axis — the 50 MW sustainment/ceiling deadlock is exhibited, not resolved; record §15 finding #1), density and temperature remain hand-swept levers rather than being closed in a design search, the six sustainment quantities have no independent check (oracle-derived on both sides, record §13), and no sensitivity over a justified range exists for the held sustainment facts (synthesis: "No sustainment-fact sensitivity" — f_ren ± would move the ~91 MW threshold materially and no such arm was run).
- **grader:** fresh grader session, 2026-09-01

**cell_id:** R1.S
- **score:** not_applicable — confirmed again; no reason found to revisit. WI-037 added calcs, constraints, and held facts but no plasma hardware part and no plasma CAS account; the heating capital the new fence prices lives in Row 4's account (`heating_cost__cost` = $264.145M at baseline, driven by the p_ecrh tie), and the plasma's structure remains the calc spine (`mfe_plant.sysml:110-300`).
- **grader:** fresh grader session, 2026-09-01

---

## Evidence-integrity findings (protocol §6 — attached to cells, never levels)

- **EI-1 (R1.P, runtime evidence):** the six per-point sustainment quantities (p_aux_required, τ_E, p_rad, n_He0, n_D0, W_th) cannot reach the evidence store (multi-field module limitation) and exist only in `results/oracle_operands.csv`, oracle-derived on both sides — not independently verified numbers. What **is** independently verified is the `sustainment_ok` verdict they feed (re-derived per sampled row, both operands resolved, zero mismatches; record §13). The score above leans on the verified verdicts and fence structure, not on the unverified operand digits. Also: `verification_summary.json`'s own `not_independently_verified` field is empty; the disclosure lives only in record prose (synthesis).
- **EI-2 (R1.P, disclosed fidelity gap, not a defect):** the baseline verdict is an *expected violation* — the model's closure sits ≈17% of alpha power short of self-sustaining at the printed point (p_aux_required 90.6 vs 50 installed), dominated by the analytic W-form delta (+9.2% vs printed 504.65 MJ, entering conducted loss as W^2.56), disclosed at the assert site (`stellarator_plant.sysml:1038-1046`) and never tuned (L-001/L-002). The model contradicting its source's printed point is a recorded, explained divergence; graded as pushback working, flagged here so the reveal cannot read the violated baseline as an unexplained error.
- **EI-3 (R1.P, minor stale comments):** the instance retirement notes carry round-1 prototype-vintage computed values (n_D0 "1.945e20, −0.8%" at `stellarator_plant.sysml:518-519`; n_He0 "5.80e19, +3.6%" at `:544-546`) that differ in the last digits from the executed oracle values at the baseline case (1.9519e20; 5.781e19). Protocol §3 applies: stale comments never override current executed evidence. No conclusion changes; the comments should ride the next regeneration-for-cause.
- **EI-4 (R1.P, study-record defects, already filed at source):** the record's §4 "15–21 MA band at p=110", §6 "≈72–139 MW" range, and "≈72.6 near T = 18" minimum do not reproduce from the committed artifacts; all five administrator-found discrepancies are corrected in the record's Addendum and none overturns a §3/§6 conclusion (synthesis, "Administrator-found discrepancies"). The teax revision that executed the run is unrecorded anywhere in the study directory. Inherited context for this cell's study evidence, filed at its home.
- **EI-5 (bookkeeping):** `modeling_project/VALIDATION_MATRIX.md:67-69` — SV-041/042/043 describe exactly the graded behavior (amended 2026-09-01) but all three still read status `pending` despite the executed evidence existing at this pin; the matrix status column lags the evidence.
- **Contestable-anchor note (routed to the owner, not around them):** the Row-1 *preamble* says "field and heating **determine** density and temperature through a confinement relation"; the *anchor table* L3 says "**links** field and heating to density and temperature". The forward-sustainment architecture satisfies "links" (graded accordingly — the anchor table is the operative test per the protocol and the spawn contract) but not "determine" in the solver sense: n_e0 and T_i0 are levers, and the machine computes required heating rather than solving (n, T) from (B, P). If the owner intended the preamble's stronger reading, that is a rubric revision (new version per §7 / Versioning, with both model states re-scored), not a grading override. L-002 (reviewer-accepted) records why a solved-T form is the wrong architecture at this machine state: no stable feasible burn exists inside the limits at the baseline machine, and the printed point sits on the unstable branch.

---

## § Delta vs prior grading (protocol §7)

Same rubric sha (`dc0f0b6d`) + new model sha (`fbf70093`, prior `dc0f0b6d`; prior package f97f0848.../c0000, new package 41e06ecb.../c0000) = a valid progress comparison. The prior grading is not edited.

- **R1.P: 2 → 3.** Every element of the prior cell's `why_not_next` ("no confinement/transport relation links field and heating to density and temperature — densities, temperatures, and profile exponents are held source referents ..., confinement is explicitly out of scope ('Rung C' ...), and ... field is never rewarded so the optimum drives to the lowest B the beta limit allows") is discharged at the new model state: the ISS04 sustainment chain exists and executes (declaration + normative handwritten impl); n_D0/n_T0/T_e0/n_He0 are retired as entry points (census-confirmed) and computed from the levers; the Rung C framing is corrected as never-owner-ruled (concept § Corrections — 2026-09-01); and the committed study shows field rewarded through confinement with the constrained optimum off the beta floor — the named pathology inverted. Profile exponents and the eight sustainment quality facts remain held source referents, which the L3 anchor permits.
- **R1.S: not_applicable → not_applicable** (unchanged, reconfirmed).
- **Not a value comparison:** the prior grading's baseline (lcoe 333.067, six verdicts, all satisfied) and this one's (lcoe 307.087, eight verdicts, `sustainment_ok` violated) sit on opposite sides of the WI-037 semantic boundary; cross-record comparisons are barred (record §12) and none is made — the delta claimed is depth, not LCOE.
- **Knock-on noted for the goal round, no score assigned here:** Row 4's target note says "P3 ... rides on row 1's confinement closure, which is owner-gated — if row 1 reopens, revisit this target." Row 1 has now reached its target (P3 = target 3) via the un-gated confinement closure, and installed heating now has physics consequence (record §8; study finding #4: heating is pure cost with no system structure). Row 4's target deserves the promised revisit; that is target-setting (rubric versioning, owner-gated), not grading, so it is recorded here and not acted on.
