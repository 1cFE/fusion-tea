# Grading — Depth Rubric v1, Row 4 re-grade (post WI-039)

**Date:** 2026-09-04
**rubric_version:** `.project/active/demo-depth-rubric/rubric.md@dc0f0b6d` — confirmed by this grader: `git log -1 --format=%h -- .project/active/demo-depth-rubric/rubric.md` returns `dc0f0b6d`.
**model_version:** `103c41a51a529846ffbe6b8d9a8148c2e92409e5` (`git rev-parse HEAD`, branch `feat/demo-maturation`). `git status` is clean under `models/`, `exploration/stellarator_e2e/models/`, `exploration/stellarator_e2e/generated/`, `exploration/stellarator_e2e/verify_stellaris.py` and this rubric directory; the canonical `models/library/analyses/mfe_heating_chain.sysml` and `mfe_power_balance.sysml` are byte-identical to the materialized copies the package was generated from (diffed by this grader).
**Package identity (for executed-behavior claims):** `work/orchestration/goals/wall-and-heating/evidence/T-003_integration_return.json` — class `CANDIDATE`, ten gates passed on the first run; indicator-input pin `2649e0ea2a987f3e56e9f659c4b4c48c9b670e6a0f8e47831c5fc253608455ad`, semantic fingerprint `48731d1570bb20cdeae45faa20545aae46aeda5982a4fbc2129d7784a66595ba`, executable fingerprint `b0c5f3eda9014fa1c84573c300c63f7fc04ecc1f8ea47047caa7775c010c6c78` (`T-003_package_identity.json`: kind `sealed`, digest = that executable fingerprint), teax `744745f895677f3344b9884627369a6a47ed987f`. Baseline `exploration/stellarator_e2e/studies/20260903-wall-and-heating/results/baseline_result.json` — `executed_under.identity_digest b0c5f3ed…`, `case_id stellarator-baseline-point-v1:c0000`; headline `lcoe_calc__lcoe` = 307.08712042841586; `heating_cost__cost` = 264,145,000.0; nine verdicts, `sustainment_ok` **violated** (disclosed and expected), the other eight satisfied. Checked by this grader: all 58 store channels are equal to the pre-WI-039 baseline (`20260903-priced-levers/results/baseline_result.json`, identity `cc64dc5a…`) — the increment moved no baseline number. Census `tests/models/data/mfe_census.json` derived against semantic `48731d15…`: 199 entry points; `p_wallplug_heat`, `eta_source_heat`, `eta_couple_heat`, `p_delivered_direct_heat`, `p_coupled_direct_heat` present; `p_input`, `p_ecrh`, `eta_pin` absent.
**Battery (run by this grader at HEAD, not a rubric conjunct — the goal's § Answered when (a) names it):** `tests/study` 359 passed / 1 skipped / 0 failed under the seam's own environment (`uv run --env-file ~/1cfe/agentic-mbse/.env --env-file .venv/integration.env python -m pytest tests/study`). The count is three higher than the 356 the trail recorded at the T-002 pin (`trail.md` § T-002 return), so tests were added after that commit; nothing failed. A first run with `PYTHONPATH` pointed at teax-simkit failed exactly one test (`test_simkit_probe_refuses_without_the_teax_root`), which asserts that `simkit` is *not* importable without `STOP_PARSER_TEAX_ROOT` — an artifact of that invocation, not of the package; it passes under the clean environment. `tests/models` was not re-run by this grader (its 48 / 13 at the pin is the trail's claim, and the seam's `model-family-spine` gate passed at T-003).
**Grader:** fresh grader session, 2026-09-04 (non-author: did not author the rubric, WI-039, the goal round, its study, or any review of it; inherited no conversation context; received the spawn prompt and the pointer-only evidence map `evidence-map-r4-regrade.md@9c0dc933`, no proposed scores; every citation below was opened and read by this session, and the perturbation claims were re-executed by this session rather than taken from the record). Nothing under `knowledge/holdout/` was read.

**Provenance caution respected:** the Row-4 target note's phrase "rides on row 1's confinement closure, which is owner-gated" is stale — Row 1 is at target (`grading-r1-regrade.md@6bc81157`, R1.P = 3) and the "owner-gated" wording descends from the Rung C scope note corrected at `.project/concepts/stellarator-demo-maturation.md` § Corrections — 2026-09-01 (read by this grader). Row 4's written target stays P2 / S2 by owner ruling 4 of 2026-09-02 (`work/orchestration/goals/operating-point-closure/trail.md` § Goal close, read by this grader). The anchor table is unaffected and is graded as written; nothing below is scored against a P3 bar.

Scoring applied exactly as written: highest level whose full evidence test is satisfied, integers only, no partial credit; the Row-4 anchor table is the operative test; a bound value checked against a bound value is not a computed chain; a calc on the handwritten codegen seam cites both the declaration and the executable impl; correctness defects are evidence-integrity findings attached to cells, never levels. Prior cells (`grading.md@fc80e5b2` R4.P with grader note G1, and R4.S) were read for the delta comparison only, never as anchors. The prior grader's `why_not_next` test — "a heating-chain calc whose output re-derives when efficiencies or delivered power change independently" — was used as one instrument; the anchor text governs.

---

## Row 4 — Heating, current drive, fueling, plasma control

**cell_id:** R4.P
- **rubric_version:** rubric.md@dc0f0b6d · **model_version:** 103c41a5 + package b0c5f3ed…/stellarator-baseline-point-v1:c0000
- **score:** 2
- **anchor_satisfied:** "Wall-plug → coupled-power chain computed with a stated deposition assumption, verified" (the P1 anchor's held quantities are gone: installed coupled power and the lumped efficiency are no longer cited constants — census-confirmed above).
- **anchor_reading (recorded because the spawn prompt asks for it):** the anchor is a conjunction of three tests and each was judged on its own.
  - *Computed, not a relabelled held ratio.* The chain runs forward from one settable power. Wall-plug electrical power is the only heating power a concept binds; source-output power, plasma-coupled power, the lumped efficiency and the total wall-plug draw are outputs of executed arithmetic, and the three quantities that govern downstream physics and cost — coupled power into `p_th` and into `sustainment_ok`, delivered power into the heating account, wall-plug total into the recirculating sum — are all derived. The former inline division `p_input_in / eta_pin_in` (the exact object of G1's objection) no longer exists anywhere in the model. The chain is two stages (source conversion and coupling) because the pinned source publishes two and no third figure exists; the anchor names a chain and its direction, not a stage count, and I do not read a stage count into it. Thin, yes; a relabel, no: changing any one of the three inputs alone re-derives the dependent outputs in execution, which no relabel does (evidence below).
  - *A stated deposition assumption, not a silent default.* `eta_couple_heat = 1.00` is carried as a named model attribute, separate from the source efficiency, entering the chain at its own stage, with the assumption written in the model text: what it asserts (all gyrotron output reaches the plasma — no transmission, window, shine-through or first-pass loss), its status (the pinned 1costingFE per-concept value; an idealisation, not a measurement), its direction (optimistic — a real chain would raise wall-plug and recirculating power for the same coupled power), and where a sourced figure would enter. The anchor says "stated"; the same row's cost anchor says "with source basis" when it wants sourcing, so the rubric distinguishes the two deliberately and the letter is clear. A lossless coupling assumption written down with its consequence is a stated deposition assumption. That it sits at the optimistic end with no sourced loss figure behind it is an evidence-integrity finding attached to this cell (EI-1), not a failure of the conjunct — the project rule that a missing input is surfaced and never defaulted forbids the alternative of inventing a loss.
  - *Verified, not parity between two copies of one expression.* The check is the study layer's model-vs-oracle verification, the same standard every P2 and P3 cell in this rubric has been graded on. The oracle's chain was written from the design's stated equations, not transcribed from the generated module, and its old `p_input / eta_pin` term is gone. What that parity proves is precisely what P2's "verified" asks: that the declared equations, the codegen seam, the pipeline wiring and every downstream consumer execute as declared, at the baseline and under perturbation. It does not prove the equations are physically right — there is no external referent for coupled power beyond the printed 50 MW, which the chain reproduces by construction — and the rubric reserves "independently checked" for P4. The perturbation evidence is two-sided (model and oracle) at multiple efficiency settings, and I re-ran it myself on both sides rather than reading it off the record.
- **model_evidence:**
  - Declaration: `models/library/analyses/mfe_heating_chain.sysml:4-73` — `calc def 'Heating Power Chain'`: the four equations in the doc (`:9-13`); two stages because the pinned source publishes two and no third figure exists, a missing input surfaced rather than defaulted (`:15-21`); which output drives cost, physics, recirculation (`:23-28`); source citation to the pinned 1costingFE (`:37-45`); inputs `p_wallplug` (`:49`), `eta_source` (`:51`), `eta_couple` "(the deposition assumption)" (`:53`), the two dormant direct terms (`:55`, `:57`); outputs `eta_pin_eff = eta_source * eta_couple` (`:60`), `p_delivered = p_wallplug * eta_source + p_delivered_direct` (`:63-64`), `p_coupled = p_wallplug * eta_source * eta_couple + p_coupled_direct` (`:67-68`), `p_wallplug_total = p_wallplug + p_coupled_direct / eta_pin_eff` (`:71-72`).
  - Executable implementation (codegen seam, both cited per the scoring rule): `exploration/stellarator_e2e/generated/handwritten/mfe_heating_chain/heating_power_chain_impl.py:66-181` — `run_heating_power_chain` returns `(p_wallplug_total, p_delivered, p_coupled, eta_pin_eff)` with the four expressions at `:175-180`, matching the declaration term for term; `AUTO_IMPLEMENTED = True` (`:61`) — the seam file is itself generated from the SysML expressions, so declaration and impl are two renderings of one text and the independent check is the oracle, not this file. Generated module `generated/modules/mfe_heating_chain/heating_power_chain.py:249-254` imports and calls it. Pipeline wiring `generated/pipelines/pipeline.yaml:103-115` (module `stellarator_09__stellaris__heat` fed from the five entry points, publishing the four `heat__*` outputs).
  - Consumers, declared: `models/designs/generic_mfe/mfe_plant.sysml:402-410` (the five plant attributes), `:412-418` (`calc heat` wiring), `:422` (`pb.p_input_in = heat.p_coupled`), `:426` (`pb.p_wallplug_in = heat.p_wallplug_total`), `:548` (`heating_cost.p_ecrh_in = heat.p_delivered`). `models/library/analyses/mfe_power_balance.sysml:61-65` (`p_wallplug_in` "arrives computed from the heating chain … rather than being derived here"), `:121-122` (`p_th` consumes `p_input_in`), `:135-141` (`recirculating` consumes `p_wallplug_in`; the former `p_input_in / eta_pin_in` division is gone). Instance: `models/designs/stellarator_09/stellarator_plant.sysml:1186-1189` (`assert constraint sustainment_ok` with `p_aux_installed_in = heat.p_coupled`; the constraint def at `models/library/analyses/mfe_viability.sysml:197-222`, predicate `p_aux_required_in <= p_aux_installed_in`).
  - Consumers, executable: `pipeline.yaml:125` (heating cost reads `heat__p_delivered`), `:339` (`pb.p_wallplug_in` ← `heat__p_wallplug_total`), `:345` (`pb.p_input_in` ← `heat__p_coupled`), `:966` (`sustainment_ok.p_aux_installed_in` ← `heat__p_coupled`).
  - The stated deposition assumption: `stellarator_plant.sysml:670-698` (`eta_couple_heat = 1.00`: what it asserts `:675-677`, its status `:679-681`, its direction of optimism `:683-688`, where a sourced figure enters `:690-691`, Source/Ref/Basis `:693-696`); the library-level naming at `mfe_heating_chain.sysml:52` and plant-level at `mfe_plant.sysml:405`. The entry point: `stellarator_plant.sysml:640-666` (`p_wallplug_heat = 100.0`, the printed *coupled* 50 MW divided by the chain's stated efficiencies — an exact restatement under stated assumptions, with the consequence that an efficiency sweep moves coupled power off the printed value recorded at `:655-658`). `eta_source_heat = 0.50` sourced (`:667-669`). Direct terms zeroed with the double-counting reason (`:699-704`). `p_ecrh` retirement note (`:908-913`).
  - Oracle (independence of the check): `exploration/stellarator_e2e/verify_stellaris.py:188-194` (inputs, with the assumption restated), `:387-399` (the four equations, "written from the WI-039 design's stated equations rather than from the generated module"), `:400-401` (`p_th` takes the oracle's own coupled power), `:411-412` (the recirculating sum takes `heat_wallplug_total`), heating capital from `heat_delivered` (diff hunk of `git show b45b4b01 -- exploration/stellarator_e2e/verify_stellaris.py`: `- p["p_input"] / p["eta_pin"]` removed, `+ heat_wallplug_total` added; `- p["heating_ecrh_per_mw"] * p["p_ecrh"]` → `heat_delivered`), outputs exported at `:600-602`.
- **runtime_evidence:**
  - Baseline identity (SV-048, `modeling_project/VALIDATION_MATRIX.md:74`, `passing`): at `p_wallplug_heat` 100.0, `eta_source_heat` 0.50, `eta_couple_heat` 1.00 the chain reads `p_delivered` = `p_coupled` = 50.0 MW, `p_wallplug_total` = 100.0 MW, `heating_cost__cost` = 264,145,000; LCOE 307.08712042841586 and nine verdicts unchanged. Confirmed by this grader: the study baseline's 58 store channels equal the pre-WI-039 baseline's channel for channel; `results/oracle_operands.csv` row `c0113` (the baseline point) reads `p_delivered_MW` 50.0, `p_coupled_installed_MW` 50.0, `p_wallplug_total_MW` 100.0, `eta_pin_eff` 0.5, `q_eng` 3.0784304345474864.
  - The perturbation test (SV-049, `VALIDATION_MATRIX.md:75`, `passing`), **re-executed by this grader on both sides.** Independent oracle (`verify_stellaris.compute()` with `IN` overridden): `eta_source_heat` 0.50 → 0.45 alone moves `heat_delivered` 50 → 45, `heat_coupled` 50 → 45, `heat_wallplug_total` 100 → 100 (unchanged, as the chain says), heating capital 264,145,000 → 237,730,500, `p_th` −5.0 MW, `q_eng` 3.0784304345474864 → 3.074207573127353, `rec_frac` 0.32484 → 0.32529, `p_net` 743.910 → 742.295, LCOE 307.08712042841586 → 306.7987430839461, with `p_fus`, wall load and `p_aux_required` unchanged. Generated executable path (`Heating_Power_ChainInput` → `run_heating_power_chain`, then `Heating_CostInput` → `run_heating_cost`): the same inputs give `p_wallplug_total` 100.0, `p_delivered` 45.0, `p_coupled` 45.0, `eta_pin_eff` 0.45, `heating_cost` 237,730,500.0 — bit-identical to the oracle. Two further instruments this grader ran: `eta_couple_heat` 1.00 → 0.90 alone moves coupled 50 → 45 and the sustainment margin −40.605 → −45.605 MW while delivered power, heating capital (264,145,000) and wall-plug draw stay put (LCOE 307.628 in the oracle) — the deposition stage re-derives on its own; `p_wallplug_heat` 100 → 120 alone moves delivered/coupled 50 → 60, wall-plug total 100 → 120, heating capital 316,974,000, `q_eng` 2.9236, LCOE 315.670 — both sides agree at every setting. Each efficiency and the power re-derive the outputs independently, which is the prior grader's own test.
  - Study-layer verification at the pin (`20260903-wall-and-heating/results/verification_summary.json`, read by this grader): 639 of 639 cases; 20 sampled rows stratified over 11 verdict combinations; 13 channels (including `lcoe_calc__lcoe`, `lcoe_1cfe_calc__lcoe`, `total_capital`, `cas72_calc__cost`) compared model-vs-oracle at tolerance 1e-9, worst 5.55e-16 (`c0268`, the LCOE channel); 9 verdicts re-derived, 0 mismatches. The sampled rows span `eta_source_heat` 0.40, 0.45, 0.50, 0.55, 0.60 at both 100 and 220 MW wall-plug — i.e. the model and the independent oracle agree on LCOE and verdicts at points where the chain is perturbed, not only at the baseline. The integration return's own verification (`T-003_verification_summary.json`) covers the single baseline case at worst deviation 0.0.
  - Store-side (model) chain behaviour: `results/points.csv` column `heating_capital` is the store channel `stellarator_09__stellaris__heating_cost__cost` (`study.py:369`); on the seven `arm-transect-eta` rows (fixed 220 MW wall-plug, `eta_source_heat` 0.35 → 0.65) it equals 5,282,900 × 220 × η to the dollar on every row (406,783,300 → 755,454,700), while LCOE rises 269.823 → 273.675. On the eight `arm-couple-132` rows (`oracle_operands.csv`) `p_wallplug_total_MW` × η = 132.0 on every row (377.14 → 176.0 MW over η 0.35 → 0.75).
  - Reach (`indicators.json` beside `pre_wi039_indicators.json`, read by this grader): the `eta_source_heat` group reaches `sustainment_ok` through `heat__p_coupled`, class `computed`, `operand_classes: ['computed']`; the pre-WI-039 `eta_pin` group did not reach `sustainment_ok`, whose installed operand was then `stellarator_09__stellaris__p_input`, class `bound`. Caveat the tool itself records: reach is a possible path, never a statement that the axis responds — the response is the executed evidence above.
- **study_evidence:** `exploration/stellarator_e2e/studies/20260903-wall-and-heating/record.md@577a6fa5` § 3, § 6 `eta_source_heat` (the chain re-deriving at scale: at fixed 220 MW wall-plug over seven efficiencies heating capital, coupled power, sustainment margin and LCOE move with fusion performance bit-identical; at fixed 132 MW coupled over eight efficiencies wall-plug draw, `rec_frac` and LCOE move with heating capital constant; the sustainment crossing η* = 0.5238 on the transect), § 8 (the one new reach is efficiency → `sustainment_ok`), § 12 (36 operating points identical to every digit across the WI-039 boundary — the increment is neutral where old and new grids coincide), § 13 (verification outcome and what it does not cover), § 15 #2 and #4; `synthesis.md@31284e05` § 3 (the fresh administrator's recount agrees with every headline, transect end, the 96-cell identity across η, and the baseline row; five prose slips corrected in the record's Addendum, none touching a value used here), § 7 (both efficiency signs follow from the model's structure). Discovery-log history read: `20260901-sustainment-fence#4` (heating is pure cost → minted WI-039 → the heating half of this goal → discharged at the model level, grade pending) and `20260903-wall-and-heating#2`, `#4`.
- **why_not_next:** P3 ("Required heating solved from the plasma's response; a port, coupling, or geometry limit pushes back") fails on its second conjunct: no port, coupling, or geometry limit exists in the model — `eta_couple_heat` is an unconstrained attribute at 1.00 with nothing bounding it, there is no port or launcher count, area, or power-per-port anywhere, and the only thing that pushes back on the heating choice is the plasma's power requirement (`sustainment_ok`, Row 1's computed `p_aux_required` against the chain's computed `p_coupled`), which is a power limit, not a port, coupling, or geometry limit as the anchor names them. See the contestable-anchor note under findings for the first conjunct, which is arguably already met through Row 1.
- **grader:** fresh grader session, 2026-09-04

**cell_id:** R4.S
- **rubric_version:** rubric.md@dc0f0b6d · **model_version:** 103c41a5 + package b0c5f3ed…/stellarator-baseline-point-v1:c0000
- **score:** 2 (re-scored against the anchor with evidence at this pin, not carried forward)
- **anchor_satisfied:** "Cost follows installed heating power with source basis"
- **anchor_reading:** the driver is installed source-output (delivered) heating power — the gyrotron output the per-MW procurement rate is calibrated to — and it is now a computed quantity (`p_wallplug × eta_source`) rather than a held constant kept equal to the coupled power by a study-layer tie. It is not wall-plug power (binding wall-plug to a rate calibrated on delivered power would double the account) and not coupled power (which would make the cost depend on the deposition assumption). The anchor's "installed heating power" is satisfied on the natural reading, and the source basis is unchanged.
- **model_evidence:** `models/library/analyses/mfe_account_costs.sysml:196-224` — `calc def 'Heating Cost'`, CAS22.1.4, `cost = nbi_per_mw*p_nbi + icrf_per_mw*p_icrf + ecrh_per_mw*p_ecrh + lhcd_per_mw*p_lhcd` (`:221-223`), Source `cas22.py:454-459` (c220104 steady-state), per-MW rates ITER-procurement-calibrated (`:204-209`). Executable: `generated/handwritten/mfe_account_costs/heating_cost_impl.py:75` (`AUTO_IMPLEMENTED = True`, the same expression). Wired at `mfe_plant.sysml:542-551` with `p_ecrh_in = heat.p_delivered` (`:548`); `pipeline.yaml:118-130` (`p_ecrh_in` ← `heat__p_delivered`). Instance: `heating_ecrh_per_mw = 5282900.0` (`stellarator_plant.sysml:905-906`, Source `defaults.py:98-100`, 5.2829 M$/MW × 1e6); ECRH-only mix, `heating_lhcd_per_mw = 0.0` with its source line (`:900-901`) and the nbi/icrf zeros above it; the `p_ecrh` retirement note (`:908-913`: Table 2 line 237's 50 MW "is now reproduced, not asserted"). The CAS72 replaceable set remains blanket + divertor only (`mfe_plant.sysml:883-887`, `defaults.py:299`).
- **runtime_evidence:** `heating_cost__cost` = 264,145,000.0 at the baseline (store channel; = 5,282,900 × 50 exactly, unchanged to the dollar from the pre-WI-039 baseline — checked channel for channel). Under perturbation the account follows the computed driver: 237,730,500 at `eta_source_heat` 0.45 (model executable path and oracle, re-run by this grader); 316,974,000 at `p_wallplug_heat` 120; unchanged at `eta_couple_heat` 0.90 (delivered power does not move). Store-side across the study: `heating_capital` = 5,282,900 × wall-plug × η on all seven transect rows; 697,342,800 constant on all eight constant-coupled rows (`points.csv`, `synthesis.md` § 3 recount). `total_capital` and `lcoe`, which carry the account, verified model-vs-oracle at 5.55e-16 worst over 20 sampled rows spanning five efficiency settings.
- **study_evidence:** `record.md` § 6 `eta_source_heat` (heating capital 406.78 → 755.45 M$ exactly linear in η at fixed wall-plug; constant at 697.34 M$ at fixed coupled power); `synthesis.md` § 3 ("heating capital / eta = 1162.238 M$ exactly at all 7", "697,342,800 $ on all 8"), § 7 (5.2829 M$/MW on every row). The goal's own exclusion was honoured: `trail.md` § T-002 scope exclusion 2 ("S2 must not be broken reaching for S3") and § T-002 return ("R4.S was not risked").
- **why_not_next:** S3 ("Sources, transmission, launchers costed separately; replaceable components carry replacement logic") — the account is still one linear per-MW line per method with no source, transmission, or launcher subaccounts (the item's minted title names S3 content that the spec explicitly did not take as its bar, `spec.md:14`), and heating is not in the CAS72 replaceable set (`mfe_plant.sysml:883-887`), so no heating component carries replacement logic.
- **grader:** fresh grader session, 2026-09-04

---

## Evidence-integrity findings (protocol § 6 — attached to cells, never levels)

- **EI-1 (R4.P, the deposition assumption's value):** `eta_couple_heat = 1.00` is the optimistic end of the assumption, sourced only as the pinned 1costingFE per-concept default (`steady_state_stellarator.yaml:15`); no admissible source in the repository supplies a transmission, window, or single-pass absorption loss, and the model says so at the binding (`stellarator_plant.sysml:683-688`). Consequences the reader must carry: every feasibility and threshold number in the committed study is made at the most generous possible coupling (record § 8, § 17: the 0.871 efficiency threshold at 100 MW, the η* = 0.5238 crossing, every "feasible" count at 220 MW), and a sourced coupling figure below 1.0 would raise wall-plug draw and recirculating power for the same coupled power and move every one of them. The anchor is satisfied on its letter ("stated"); the physics gap is real and is the one input the chain still lacks. If the owner intended "stated deposition assumption" to mean a physically reasoned or sourced deposition fraction rather than a written idealisation, that is a rubric-wording question for v2 (see the contestable-anchor note), not something this grading can supply.
- **EI-2 (R4.P, runtime evidence coverage):** the chain's four output channels (`heat__p_delivered`, `heat__p_coupled`, `heat__eta_pin_eff`, `heat__p_wallplug_total`) do not reach the evidence store — 'Heating Power Chain' is a multi-field module and the store records single-field channels only; declaring them produced silent blank columns on the study's first execution (`20260903-wall-and-heating#4`, fourth committed sighting of the class). Their per-point values exist oracle-side only (`results/oracle_operands.csv`) and are oracle-derived on both sides. What *is* store-verified against the oracle is everything downstream of the chain (LCOE, total capital, CAS72, the nine verdicts including `sustainment_ok` re-derived through the published bindings). This grader closed the gap for the module level by executing the generated chain and cost modules directly at four input settings and matching the oracle bit for bit; that is a grader-side check, not a committed artifact. `verification_summary.json`'s `not_independently_verified` list is empty although coverage is incomplete — the known tooling gap, filed beside `20260903-priced-levers#4`; an empty list must not be read as full coverage. Tooling, not a model defect.
- **EI-3 (bookkeeping, the item record):** the evidence map points to `work/active/WI-039_heating-system-structure/plan.md` § Phase 4 for "the numbers as recorded"; at HEAD that section is an unchecked checklist (every checkbox in all five phases is `[ ]`, frontmatter `Status: draft`) with no implementation notes, and the perturbation numbers appear nowhere in `plan.md`. They are recorded in `VALIDATION_MATRIX.md` SV-049 and in `trail.md` § T-002 return, and this grader reproduced them independently, so no conclusion depends on the plan file — but the item record lags the work it describes, and a reader following the map's pointer finds nothing. The MR-WI039-9 restatement in the same file also records its own ordering deviation (written after the regeneration commit rather than before).
- **EI-4 (R4.P/R4.S, inherited and disclosed at source):** `handshake_1costingfe.py` was broken before WI-039 (`FileNotFoundError` on a file the package stopped generating at the model migration) and stays broken; the heating keys it would need are written into `plan.md` § MR-WI039-9 for whoever revives it. Not this item's break; noted so the retirement of `eta_pin`/`p_ecrh` is not later blamed for it.
- **EI-5 (library dormant path, minor, does not affect this instance):** with the chain unbound and only the direct terms bound, `eta_pin_eff` defaults to 1.0 and `p_wallplug_total = p_coupled_direct / 1.0` — the recirculating draw equals the coupled power with no conversion loss (executed by this grader: direct terms 50/50 give wall-plug total 50.0). The design's "reproduces today's semantics exactly" for a dormant concept therefore holds only when that concept also binds `eta_source` to its former lumped efficiency, as the restatement table in `plan.md` § MR-WI039-9 says (`eta_source = eta_pin_effective`). The library doc (`mfe_heating_chain.sysml:30-35`) says the mode is selected by the powers, which is true, but does not say the efficiency must travel with them. A one-line doc clarification for the next dormant-chain concept; nothing at the Stellaris instance is affected (its chain is live).
- **EI-6 (study-record prose defects, already filed at source):** five statement-level slips in `record.md` § 4, § 6 and § 11 were found by the administrator's recount and corrected in the record's Addendum (2026-09-04); none touches a value used in this grading. A stale docstring in `study.py` and a stale group note in `axes.json` are left as digested and named in § 17 and the second Addendum. The teax revision is unrecorded by the verifier and carried from the integration return. Inherited context for this cell's study evidence.
- **Contestable-anchor note (routed to the owner, not around them; no score assigned against it):** the row's P3 anchor is a conjunction, "Required heating solved from the plasma's response; a port, coupling, or geometry limit pushes back." Its first conjunct is arguably already evidenced at this pin — required heating *is* solved from the plasma's response (Row 1's ISS04 sustainment chain, `sustain.p_aux_required`), and since WI-039 the fence compares it computed-vs-computed against Row 4's chain output (`indicators.json`: both `sustainment_ok` operands class `computed`; the study shows efficiency structuring feasibility through it). The second conjunct is not met on a plain reading: what pushes back is a power limit, and the model has no port, coupling, or geometry limit on heating at all. The rubric's own scoring rule about plant-level constraints reading unrelated downstream numbers does not bar the sustainment fence (it reads Row 4's own computed output), so the question is purely whether the owner meant "coupling limit" to include the plasma's coupled-power requirement or only a limit on what can be coupled (launcher, port, absorption). I read it as the latter and score P2. If the owner reads it as the former, R4.P would be arguable at 3 under the written anchor — but the written target is P2 by owner ruling and the P3 raise rides the next deliberate rubric version, so this belongs in that revision's deliberation, not in this grading. Separately, the target note's promised revisit ("if row 1 reopens, revisit this target") has fired and is on record as such (owner ruling 4, 2026-09-02).

---

## § Delta vs prior grading (protocol § 7)

Same rubric sha (`dc0f0b6d`) + new model sha (`103c41a5`, prior `dc0f0b6d`; prior package `f97f0848…`/c0000, new package `b0c5f3ed…`/c0000) = a valid progress comparison. The prior grading (`grading.md@fc80e5b2`) is not edited.

- **R4.P: 1 → 2.** Every element of the prior cell's `why_not_next` ("P2 needs a computed wall-plug → coupled-power chain with a stated deposition assumption verified in execution; no such calc def exists (the seam would need at minimum a heating-chain calc whose output re-derives when efficiencies or delivered power change independently)") is discharged at the new model state: the calc def exists with both its declaration and its executable impl; its outputs re-derive when `eta_source_heat`, `eta_couple_heat`, or `p_wallplug_heat` change independently (re-executed by this grader on the model's executable path and in the independent oracle, agreeing bit for bit, and exercised over 639 committed points); the deposition assumption exists as a model object with its own binding and written assumption rather than as prose inside an `eta_pin` doc. Grader note G1's objection and its author-ratified resolution ("a held ratio dividing one held constant inside another subsystem's sum derives nothing, and no chain element exists as a model object") are discharged specifically: the division is gone from `mfe_power_balance.sysml:139-141`, and the chain elements — wall-plug, source stage, coupling stage, delivered, coupled, lumped efficiency, wall-plug total — are seven named quantities in the model. The prior cell's model pointers (`p_input` at instance `:487`, `p_ecrh` at `:677`, `eta_pin` at `:499`) no longer resolve to anything: those three entry points are retired (census-confirmed).
- **R4.S: 2 → 2**, re-scored rather than carried. The anchor is satisfied on the same account and the same source basis; what changed underneath it is the driver, from a held `p_ecrh = 50` kept equal to `p_input` by a manifest tie to the chain's computed `heat.p_delivered`, with the tie removed from `studies/manifest.json` because the invariant is now structural. The baseline cost is unchanged to the dollar. The prior cell's wiring pointer `mfe_plant.sysml:379` now resolves to the cryoplant block; the heating cost wiring is at `:542-551`.
- **Not a value comparison:** unlike the r1 re-grade, the two model states sit on the same physics point — all 58 store channels are equal across the WI-039 boundary and the record's § 12 shows 36 shared operating points identical to every digit. The delta claimed is depth (held → computed, with the objected-to division removed), not any movement in LCOE or verdicts.
- **Knock-on noted for the goal round, no score assigned here:** `sustainment_ok`'s installed operand changed class from `bound` (`p_input`) to `computed` (`heat__p_coupled`) — the fence is computed-vs-computed since WI-039. That is evidence at Row 1's P3 (the r1 re-grade scored it with a held installed side and said so) and the substance of the Row-4 P3 question above; both are for the next deliberate rubric version.

## § 5 resolution of the contestable-anchor note — owner, 2026-09-04

- **author_reading:** none offered (the round agent did not pre-read the anchor; the note was routed to the owner as the grader asked).
- **grader_score:** R4.P = 2; the sustainment fence is a power limit, not the anchor's "port, coupling, or geometry limit".
- **resolution `[OWNER 2026-09-04, verbatim]`:** *"not if it is just a direct constraint. I would expect some curve or calculation for the push-back to be modeled."* A direct constraint does not satisfy "pushes back"; a modeled response (a curve or calculation through which the limit changes the design's outcome) does. The grader's reading and score stand. The wording clarification is carried to rubric v2 (concept open question 6). Recorded in `work/orchestration/goals/wall-and-heating/trail.md` § Owner ruling — 2026-09-04.
