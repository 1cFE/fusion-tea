# Evidence map — Row 4 re-grade (pointers only)

**Author:** the WI-039 author's round (goal `wall-and-heating` round 1, the resuming round-1 session), 2026-09-04. Per the grading protocol § 2 this map carries pointers and no proposed scores; the grader cites what it actually read. The v1 map (`evidence-map.md`) still covers the other rows; this file adds the post-WI-039 Row-4 surfaces. `evidence-map-r1-regrade.md` and `evidence-map-r3-regrade.md` are the two precedents for this shape.

## The yardstick

- `rubric.md@dc0f0b6d` § Row 4 (the P ladder; **the row's P2 anchor is the operative test**: "Wall-plug → coupled-power chain computed with a stated deposition assumption, verified") and § Grading protocol. **The written target is P2 / S2.** Provenance caution: the row's target note says P3 "rides on row 1's confinement closure, which is owner-gated" — Row 1 is at its target since the r1 re-grade (`grading-r1-regrade.md`, R1.P = 3), and the "owner-gated" phrase descends from the stale Rung C text corrected at `.project/concepts/stellarator-demo-maturation.md` § Corrections — 2026-09-01. Row 4's written target stays P2 by owner ruling 2026-09-02 (`work/orchestration/goals/operating-point-closure/trail.md` § Goal close: the raise rides the next deliberate rubric version). The anchors themselves are unaffected; grade against them as written, and nothing is graded against an unwritten P3 bar.
- Prior cells for the delta: `grading.md@fc80e5b2` § Row 4 — `R4.P` = 1 (its `model_evidence`, its `why_not_next`, and grader note G1, which states the test the grader would apply: "a heating-chain calc whose output re-derives when efficiencies or delivered power change independently"), and `R4.S` = 2, **already at its S2 target** — the increment was required not to break it (`work/orchestration/goals/wall-and-heating/trail.md` § T-002 scope, exclusion 2).

## Declared structure (canonical models/)

- `models/library/analyses/mfe_heating_chain.sysml` — `calc def 'Heating Power Chain'`, the whole file (74 lines): the four equations in the doc (`:9-13`); the two-stage rationale and why there is no third stage (`:15-21`, a missing input surfaced, never defaulted); which output drives cost, which drives physics, which drives recirculation (`:23-28`); the seven named quantities — five inputs `p_wallplug` `:49`, `eta_source` `:51`, `eta_couple` `:53` (commented "the deposition assumption"), `p_delivered_direct` `:55`, `p_coupled_direct` `:57`, and four outputs `eta_pin_eff` `:60`, `p_delivered` `:63-64`, `p_coupled` `:67-68`, `p_wallplug_total` `:71-72`; the source citation to the pinned 1costingFE (`:37-45`).
- `models/library/analyses/mfe_power_balance.sysml` — `p_wallplug_in` (`:61-65`, "arrives computed from the heating chain … rather than being derived here"); the recirculating sum (`:135-141`) — the former `p_input_in / eta_pin_in` division is gone and the term is consumed; the thermal sum still consumes `p_input_in` (`:115-122`), which is now the chain's coupled power.
- `models/designs/generic_mfe/mfe_plant.sysml` — the five plant-level chain attributes (`:402-410`); `calc heat : 'Heating Power Chain'` wiring (`:412-417`); `p_input_in = heat.p_coupled` (`:422`); `p_wallplug_in = heat.p_wallplug_total` (`:426`); the heating account's `p_ecrh_in = heat.p_delivered` (`:548`); (the sustainment fence's installed operand is bound at the concept instance, below).
- `models/designs/stellarator_09/stellarator_plant.sysml` — `p_wallplug_heat = 100.0` (`:640-666`: the printed *coupled* 50 MW divided by the chain's stated efficiencies, an exact restatement under stated assumptions, not a new number); `eta_source_heat = 0.50` (`:667-669`, sourced to the pinned 1costingFE); **`eta_couple_heat = 1.00` (`:670-698`) — the stated deposition assumption: what it asserts, its status as an idealisation, its direction of optimism, and where a sourced figure would enter**; the two dormant direct terms zeroed with the double-counting reason (`:699-704`); the `p_ecrh` retirement note (`:908`); **`assert constraint sustainment_ok` with `p_aux_installed_in = heat.p_coupled` (`:1186-1188`)** — the fence's installed side is the chain's computed coupled power (computed-vs-computed since WI-039; it was bound to the held `p_input` before).
- Codegen-seam rule reminder: a calc on the handwritten seam needs both the declaration and the executable impl cited — `exploration/stellarator_e2e/generated/handwritten/mfe_heating_chain/heating_power_chain_impl.py`.

## Runtime and executed behavior

- Package identity: `work/orchestration/goals/wall-and-heating/evidence/T-003_integration_return.json` (`CANDIDATE`, ten gates first run) — indicator-input pin `2649e0ea…`, semantic `48731d15…`, executable `b0c5f3ed…`; `T-003_package_identity.json`; regeneration rewrote no byte outside `handwritten/`; 199 entry points re-derive to the census.
- Baseline: `exploration/stellarator_e2e/studies/20260903-wall-and-heating/results/baseline_result.json` — LCOE 307.08712042841586, `heating_cost__cost` 264,145,000, nine verdicts with `sustainment_ok` expected-violated; **unchanged from the pre-WI-039 baseline** (`exploration/stellarator_e2e/studies/20260903-priced-levers/results/baseline_result.json`, same numbers). The chain's own channels are fields of a multi-field module and do not reach the evidence store (record § 13, § 15 #4); their per-point values are oracle-side in `results/oracle_operands.csv` (columns `p_delivered_MW`, `p_coupled_installed_MW`, `p_wallplug_total_MW`, `eta_pin_eff`).
- Entry-point census: `tests/models/data/mfe_census.json` (`p_wallplug_heat`, `eta_source_heat`, `eta_couple_heat`, both direct terms present; `p_input`, `p_ecrh`, `eta_pin` absent; 199 entry points, 197 before).
- **The perturbation test — the prior grader's own `why_not_next` test:** `modeling_project/VALIDATION_MATRIX.md` SV-048 (baseline identity: `p_delivered` and `p_coupled` both 50.0 MW, heating capital 264,145,000, `p_wallplug_total` 100.0 MW, LCOE and nine verdicts unchanged) and SV-049 (`eta_source_heat` 0.50 → 0.45 alone moves heating capital 264,145,000 → 237,730,500, LCOE 307.087120428 → 306.798743084, and in the independently written oracle `q_eng` 3.078430435 → 3.074207573 with `rec_frac` and `p_net`; model and oracle agree at both settings, rel dev below 1e-9); the numbers as recorded at `work/active/WI-039_heating-system-structure/plan.md` § Phase 4. Reproducible through the oracle seam `exploration/stellarator_e2e/studies/oracle_entry.py` `evaluate(point)` (needs `PYTHONPATH=$HOME/1cfe/teax/packages/teax-simkit:$PWD/exploration/stellarator_e2e/pkg` and `uv run --env-file ~/1cfe/agentic-mbse/.env --env-file .venv/integration.env python`).
- **The independent oracle computes the chain itself:** `exploration/stellarator_e2e/verify_stellaris.py` `:389-399` (the four equations from the design, not from the generated module), `:412` (the recirculating sum consumes `heat_wallplug_total`), `:190-194` (the inputs), `:601-602` (the outputs). Its former `p_input / eta_pin` term is gone — `git show b45b4b01 -- exploration/stellarator_e2e/verify_stellaris.py`.

## Load-bearing study evidence

- `exploration/stellarator_e2e/studies/20260903-wall-and-heating/record.md` — § 3, § 6 `eta_source_heat` (the chain re-deriving at scale: at fixed 220 MW wall-plug, 7 efficiencies, and at fixed 132 MW coupled, 8 efficiencies — heating capital, coupled power, wall-plug draw, recirculating fraction and LCOE moving under efficiency alone, with fusion performance bit-identical), § 8 (the structural comparison against `pre_wi039_indicators.json`: the one new reach is efficiency → `sustainment_ok`), § 12 (36 operating points identical across the WI-039 boundary — the increment is neutral where the old and new grids coincide), § 13, § 15 #2 and #4; `synthesis.md` beside it, if present when you read (the fresh administrator's independent recount); `results/points.csv`, `results/oracle_operands.csv` (arms `arm-transect-eta` and `arm-couple-132`).
- `indicators.json` beside `pre_wi039_indicators.json` in the same directory — `eta_source_heat` reaches `sustainment_ok`; the retired `eta_pin` did not.
- `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` — the `20260901-sustainment-fence#4` row history (heating is pure cost → minted WI-039 → the heating half of this goal) and the `20260903-wall-and-heating#2`, `#4` sightings.

## Item record and validation

- `work/active/WI-039_heating-system-structure/` — `spec.md` (MR-WI039-1..11; MR-WI039-2 no defaulted number, MR-WI039-3 the stated assumption), `design.md` (D1 wall-plug as the entry point and why the cheaper backward solve was rejected; D2 the two-stage chain with the coupling stage as a stated assumption; D3, D4; § Prototype and validation report; § Approval), `plan.md` (five phases with implementation notes; § Phase 4; § MR-WI039-9 restatement — the retirement, the two committed studies restated, the pre-existing 1costingFE handshake break disclosed as not this item's).
- `modeling_project/VALIDATION_MATRIX.md` — SV-048, SV-049, both `passing`.
- Battery at the pin: `tests/models` 48 passed / 13 skipped; `tests/study` 356 passed / 1 skipped (`work/orchestration/goals/wall-and-heating/trail.md` § T-002 return).
- Goal context: `work/orchestration/goals/wall-and-heating/goal.md` § Answered when (a) — the condition, carried verbatim from `priced-levers` — and § Invariants; `trail.md` § T-002 scope (the three exclusions, including "S2 must not be broken reaching for S3") and § T-002 return (the round's own reading — the grader weighs the anchor text itself, not the trail's argument).

## Candidate evidence-integrity items (protocol § 6 — attach to cells, never levels)

- `eta_couple_heat = 1.00` is the optimistic end of the deposition assumption and is sourced only as the pinned 1costingFE per-concept value; no admissible source in the repository supplies a transmission or coupling loss (instance doc `:670-698`; record § 8, § 17).
- The chain's four output channels do not reach the evidence store (multi-field module); values are oracle-side (`20260903-wall-and-heating#4`). A tooling gap, not a model defect.
- The `handshake_1costingfe.py` export was broken before WI-039 and stays broken (`plan.md` § MR-WI039-9 restatement, "a pre-existing break, not one this item caused").
