# Grading — Row 3 re-grade (post-WI-035)

**Date:** 2026-08-30
**rubric_version:** `.project/active/demo-depth-rubric/rubric.md@dc0f0b6d` (confirmed: last commit touching the file is `dc0f0b6d`)
**model_version:** `ffa38c053faa42d2ae7abf7614458a9ddeb5abd3` (git rev-parse HEAD; all `models/` files read at this commit)
**Package identity (for executed-behavior claims):** `exploration/stellarator_e2e/studies/20260830-stress-fence/results/baseline_result.json` — `executed_under.identity_digest 75f90a24dc419fd9f29a2c8d9ee26beae1f2d4fb126eca498c651538941551be`, `store_id stellarator-baseline-point-v1.db`, `case_id stellarator-baseline-point-v1:c0000`; semantic fingerprint `819a5a05220e5caff7d317c02c32ef4fed37f7f091e776fba8dff7f168235fd4` per `work/orchestration/goals/magnet-closure/evidence/T-004_integration_return.json` (class CANDIDATE, all gates pass). Baseline headline `lcoe_calc__lcoe` = 304.48161969642837; **seven** verdicts, all `satisfied` (`beta_ok`, `net_positive`, `peak_field_ok`, `recirc_ok`, `tbr_ok`, `wall_load_ok`, `wp_stress_ok`).
**Grader:** fresh re-grade session (non-author), 2026-08-30 — did not author the rubric, WI-035, or any review of it; received the pointer-only evidence map (`evidence-map-r3-regrade.md`), opened and read every citation below; prior cells read for context only, not as anchors.

Scoring applied exactly as written: highest level whose full evidence test is satisfied, integers only, no partial credit; the Row-3 anchor table is the operative test; a held sourced limit against a computed operand is the ladder working (prior note G6); a bound value against a bound value is not; correctness defects are evidence-integrity findings, never levels.

## Row 3 — Magnets, structures, power supplies, cryogenics

**cell_id:** R3.P
- **rubric_version:** rubric.md@dc0f0b6d · **model_version:** ffa38c05 + package 75f90a24…/c0000
- **score:** 3
- **anchor_satisfied:** "A stress or current-density limit pushes back on coil sizing and field choice." Lower rungs verified: the P2 conjunction now holds in full — "peak field computed from geometry and coil current" (B_axis = μ0·k_link·n_coils·I_coil/(2π·R0), then B_peak = B_axis × peak_ratio) and "cryo load computed from winding-pack heat" (the WI-024 chain, unchanged, per prior note G5).
- **model_evidence:** field derivation: `models/library/analyses/mfe_magnet_field.sysml:45` ('Coil Set Axis Field', B_axis from n_coils, I_coil, k_link, R0; k_link a held sourced coil-set transfer fact, doc at `:11-29`); wired at `models/designs/generic_mfe/mfe_plant.sysml:220-225` and EXPOSEd into the magnet part at `:55` (`:>> B = field_calc.B_axis`); the instance's held `:>> B = 9.0` is retired (`models/designs/stellarator_09/stellarator_plant.sysml:129-136` retirement note; `magnet__B` absent from `tests/models/data/mfe_census.json`, only `magnet__B_max` remains, and the twelve WI-035 levers are present). Stress operand: `mfe_magnet_field.sysml:81` (sigma_wp = k_sigma·I_coil·B_peak_in/wp_side), wired at `mfe_plant.sysml:233-238` from the **computed** B_peak (`:227-230`). Limit: `models/library/analyses/mfe_viability.sysml:123-146` ('Winding Pack Stress Limit', sigma_in ≤ sigma_allow_in at `:145`), asserted `wp_stress_ok` at `mfe_plant.sysml:891-894` against the held sourced allowable 800 MPa (instance `:205`, raw.pdf §2.10 cryogenic AISI 316LN). Cryo conjunct: `models/library/analyses/mfe_cryo_plant.sysml` chain wired at `mfe_plant.sysml:294-303`. Instance levers with image-cited docs: I_coil = 15.4 MA (`stellarator_plant.sysml:174`), n_coils = 48 (`:170`), k_link (`:182`), wp_side = 0.36 m (`:195`), k_sigma (`:201`). Validation: `modeling_project/VALIDATION_MATRIX.md:64-65` (SV-038, SV-039, both passing).
- **runtime_evidence:** baseline_result.json channels — `field_calc__B_axis` = 8.999999999999998 (design D2's 1-ulp-low bind of the printed 9.0 T), `peak_field_calc__B_peak` = 24.899999999999995, `wp_stress__sigma_wp` = 649999999.9999999 Pa vs sigma_allow 8.0e8 (margin 150 MPa, 23% — nonzero, not by construction); verdict `wp_stress_ok` `satisfied`; study verification at this pin passed at worst rel. deviation 4.37e-16 with all seven constraints re-derived from published operands, zero mismatches (`20260830-stress-fence/record.md:160`, recounted `synthesis.md:60`).
- **study_evidence:** the limit demonstrably binds on both conjuncts of the anchor in the committed study. Field choice: `wp_stress_ok` violated 660/1617 grid points and is **the binding ceiling on I_coil for R ≥ 16.5 m** (sole ceiling there; shared with `peak_field_ok` at 15.5–16.0 m) — `record.md:41` (§4 table), `:66` (§6), ceiling-identity handover recounted exactly from `results/points.csv` at `synthesis.md:24`. Coil sizing: the wp_side transect flips `wp_stress_ok` between 0.28 m (835.7 MPa, violated) and 0.30 m (780.0 MPa, satisfied), 5/16 points infeasible — `record.md:82`, recounted `synthesis.md:26`; `results/points.csv` opened and columns confirmed (sigma_wp, wp_stress_ok, feasible per point). The subsystem's choices feed plant feasibility (general-ladder P3 clause): the stress fence bounds the feasible I band that the constrained optimum lives inside (`record.md:25-27`).
- **why_not_next:** P4 needs the coil set closed with confinement and geometry in a design search, and that closure does not exist: no confinement relation couples the derived field to the plasma operating point (the owner-gated Rung C ruling, re-sighted as the optimum sitting on the beta floor at every feasible R, `record.md:66,200`), and the coil-sizing lever does not feed geometry or cost (winding length held at c_coil = 25 m, wp_side reaches zero objectives — DISCOVERY_LOG `20260830-stress-fence#1`/`#2`, log lines 48-49), so no search can trade coil-set choices against the rest of the design.
- **grader:** fresh re-grade session (non-author), 2026-08-30

**cell_id:** R3.S
- **rubric_version:** rubric.md@dc0f0b6d · **model_version:** ffa38c05 + package 75f90a24…/c0000
- **score:** 3
- **anchor_satisfied:** "Winding pack, structure, power supplies, cryoplant costed as separately sized sub-accounts" — each of the four exists as its own account following its own engineered driver with its own source basis, and none inherits another's lump.
- **model_evidence:** winding pack: `models/library/analyses/mfe_magnet_cost.sysml:56-101` ('Winding Pack Cost': kAm over the real winding length n_coils·I_coil·f_set·c_coil, × $/kA·m × content-mapped f_wp_fab; the old single 5.87 markup retired as sole carrier), wired `mfe_plant.sysml:350-357`. Structure: `mfe_magnet_cost.sysml:103-135` ('Magnet Structure Cost': casing steel mass basis n_coils·m_casing·steel_price·f_steel_fab, casings only — CAS22.1.5 boundary documented against double counting), wired `mfe_plant.sysml:361-367`. Rollup: `mfe_magnet_cost.sysml:137-158` ('Magnet Capital' = winding + structure), wired `:369-372`, bound into the magnet part's capital_cost at `:51`; the retained 'Magnet Coil Cost' is now an explicitly documented comparison channel (`mfe_magnet_cost.sysml:32-36`), exposed as `magnet_capital_1cfe` (`mfe_plant.sysml:909`). Power supplies: `models/library/analyses/mfe_account_costs.sysml:140` ('Power Supplies Cost', power-law in gross electric, CAS22.1.7), wired `mfe_plant.sysml:405`, part at `:82-83`. Cryoplant: `mfe_account_costs.sysml:559-589` ('Aux Cooling Cost' D7 split — `cryo_cost` = cryo_base·(p_cryo/30)^0.7 its own output at `:588`, driven by the **computed** p_cryo from the cryo chain, `mfe_plant.sysml:566`), exposed as its own channel `cryoplant_capital` at `:910`. Instance sizing parameters, each with image/pin-cited docs: f_set `stellarator_plant.sysml:186`, c_coil `:191`, f_wp_fab `:212`, m_casing `:219`, steel_price `:223`, f_steel_fab `:227`. Validation: `VALIDATION_MATRIX.md:66` (SV-040, passing).
- **runtime_evidence:** baseline_result.json — `winding_pack_cost__cost` = 5.3466e9, `magnet_structure_cost__cost` = 5.4432e7, `magnet_capital_rollup__capital_cost` = 5.401032e9 (rollup = exact sum), `magnet_cost__capital_cost` = 6.3235e9 (live comparison channel; the −14.6% delta decomposed, not fitted — design D6, `work/active/WI-035_magnet-closure/design.md:72`), cryoplant `cryo_elec__p_elec` = 0.8644 MW feeding `cryo_cost`.
- **study_evidence:** the rollup identity `magnet_capital == winding_pack + magnet_structure` held at all 1,634 executed points (round-1 review recount, `work/orchestration/goals/magnet-closure/trail.md:252`); the winding-pack account responds to the I_coil lever across the grid while the structure account is sized by its own drivers (`results/points.csv` columns winding_pack / magnet_structure, opened and confirmed).
- **on the cryoplant conjunct (weighed directly, not inherited):** the anchor's test is "costed as separately sized sub-accounts" — it does not require a CAS22.1.3 home, and it cannot: power supplies, which the same anchor names, have always lived in their own account (CAS22.1.7). The cryoplant capital is separately sized in exactly the sense the anchor's other members are — its own cost expression, its own engineered driver (the computed p_cryo, a stronger driver than several sibling accounts'), its own source basis, its own exposed channel (`cryoplant_capital`). That the account pre-existed and D7 exposed rather than newly built it is irrelevant to the anchor, which tests the model state, not the diff. Conjunction met as written.
- **on the known holds (weighed directly):** `c_coil`/`f_set` held (magnet capital R-flat at fixed I, DISCOVERY_LOG `#1`) and `wp_side` costless (`#2`) do not defeat "separately sized" — the winding pack is sized by a real engineered quantity (conductor kA·m over the sourced winding length) that is independent of the other accounts' drivers, which is the S3 test; what the holds defeat is a sizing chain tied to the engineered design, which is the S4 bar, so they live in why_not_next, not the score.
- **why_not_next:** S4 needs a design-based estimate with fabrication/installation boundaries and stated uncertainty — the winding length is a held 25 m circumference that does not follow coil geometry and wp_side carries no cost consequence (DISCOVERY_LOG `20260830-stress-fence#1`/`#2`), fabrication and installation enter only as content-mapped markups (f_wp_fab = 6.65, f_steel_fab = 3.0), and no estimate maturity or uncertainty is stated beyond the disclosed m_casing band.
- **grader:** fresh re-grade session (non-author), 2026-08-30

## Evidence-integrity findings (attached, never levels — protocol §6)

- **EI-1 (both cells):** the study record's §14 disposition names the held-c_coil MAJOR as "finding `20260830-stress-fence#3`" while the §15 register and DISCOVERY_LOG mint it as `#1` — a stale id inside the record, caught by the record's own Addendum (`record.md:206`) and independently by the administrator (`synthesis.md:75`). The finding content this grading relies on is unambiguous.
- **EI-2 (R3.P):** the record's §13 conductor-fence margin digit "5.3e-15 T" does not reproduce (float64 arithmetic gives 3.55e-15 T; `synthesis.md:58`, corrected in the Addendum, `record.md:207`); the on-fence-by-construction claim stands, and this grading does not rest on `peak_field_ok`'s margin.
- **EI-3 (R3.P, disclosed design fact):** the baseline sits on the conductor fence with ulp-scale margin **by construction** (k_link bound 1 ulp low so `peak_field_ok` cannot flip on rounding, design D2; `record.md:164`) — a disclosed convention, not a robustness claim; the graded stress fence carries a real 23% baseline margin.
- **EI-4 (R3.S, disclosed assumption):** `m_casing` = 63 t is the printed **floor** of the 63–200 t cast-part range, bound as a knowing lower bound with the band [$54.4M, $172.8M] disclosed (instance `:219` doc; design D5) — the structure account's size is bounded below by an assumption parameter whose seam is named.
- **EI-5 (R3.P, disclosed extrapolation):** `k_sigma` is anchored at wp_side = 0.36 m and applied in 1/side form across the 0.20–0.52 m transect (`record.md:146`) — the flip-location claim carries this held-fact extrapolation, disclosed at the claim site.

## Delta vs prior grading

Against `grading.md` § Row 3 at the pre-WI-035 model (dc0f0b6d + package f97f0848…): **R3.P 1 → 3, R3.S 2 → 3** — same rubric sha, new model sha, a valid progress comparison per protocol §7; both cells now sit at the row's declared target (P3 / S3), gap 0.
