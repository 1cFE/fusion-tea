# P0 Audit — Downstream references to old dimension column names

**Date:** 2026-05-20
**Branch:** `prep/v3-rewrite-prereqs` @ main (`7b5e8a9`)
**Purpose:** Identify downstream consumers of `economic_potential`,
`technical_feasibility`, `manufacturability_scale_out` (or `ep_evidence`,
`tf_evidence`, `mso_evidence`) that would break when P2's CSV-column refactor
lands. Surface anything outside the scoring framework + tests that needs
coordinated migration.

## Headline

**Zero downstream consumers will break.** All 15 files matching the column-name
patterns are either (a) the scoring framework itself or its tests (in scope
for P2's refactor) or (b) historical/planning documents (no active code path).

## Categorized findings

### A. In scope for P2 refactor (7 files)

The framework code + tests that will be refactored by Slice 1 (P2):

| File | Role | P2 action |
|---|---|---|
| `exploration/scoring_v2/score.py` | The framework | Replace `DIMENSIONS` → `AXES`, restructure CSV output |
| `exploration/scoring_v2/scores/table.csv` | Output artifact | Regenerated by `score.py` after P2 |
| `exploration/scoring_v2/weights/default.yaml` | Weights config | Restructure to axis-keyed shape |
| `exploration/scoring_v2/weights/slice1.yaml` | Reference config | Restructure or retire |
| `tests/scoring_v2/test_score_framework.py` | Framework tests | Replace dimension-column assertions with axis-column assertions |
| `tests/scoring_v2/test_component_modularity.py` | Modularity slice-2 tests | Retired by Slice 1b (replaced with v5-anchored tests) |
| `tests/scoring_v2/test_embeddings.py` | Modularity slice-1 tests | Retired by Slice 1b |

### B. Current work-item docs (3 files — describe what we're replacing)

```
.project/active/scoring-v3-rewrite/spec.md
.project/active/scoring-v3-rewrite/design.md
.project/active/scoring-v3-rewrite/plan.md
```

These reference the old names as part of describing the rewrite itself.
No action — they're meta-documentation.

### C. Legacy slice planning docs (5 files — historical artifacts)

```
.project/active/scoring-v2-modularity-slice/{design,plan}.md
.project/active/scoring-v2-component-modularity-slice/{spec,design,plan,implementation_notes}.md
```

These are the completed slice planning docs from Reid's slice-1 and slice-2
modularity work. They describe the original 3-dimension framework. **No active
code path consumes them.** Post-P2 they should move to `.project/completed/`
(housekeeping, not blocking).

### D. Narrative / English-language references (informational only)

A broader grep for "manufacturability" or "M&SO" finds additional uses in:
- `exploration/concept_analysis/analyses/*/synthesis.md` (~3 files) —
  LLM-generated concept narrative; uses "manufacturability" as the English
  word, not as a column name.
- `exploration/concept_analysis/handwritten/26-laser-icf-indirect-drive.md` —
  same.
- `archive/concept_research_legacy/` — gitignored research artifacts; the
  text references aren't consumed by code.
- `exploration/context/initial-fusion-concepts.csv` — old context CSV, not a
  framework input.

None of these are code or schema consumers of the dimension column names.
No action.

## Conclusion

P2's refactor is **structurally contained**: every file that uses the
dimension column names as identifiers is either part of the framework being
refactored or part of the planning artifacts for the refactor itself. No
external scripts, no `.project/` consumers outside the legacy planning docs,
no `tools/` (the `tools/score_explorer/` will be built in P6 against the new
schema), no analysis-pipeline references.

**Recommendation:** proceed with P2 without coordinated downstream migration.

## Optional housekeeping (post-P2)

After P2 lands, move the 5 legacy slice planning docs from
`.project/active/scoring-v2-{modularity,component-modularity}-slice/` to
`.project/completed/` so the active workspace reflects only ongoing work.
This is a `git mv` chore, not a code change.
