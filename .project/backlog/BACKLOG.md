# Product Backlog

Prioritized list of epics and features.

**Last Updated**: 2026-09-04

---

## Priority Legend

- **P0**: Critical - Blocking, do immediately
- **P1**: High - Important, do soon
- **P2**: Medium - Valuable, do when possible
- **P3**: Low - Nice to have, do eventually

---

## Flagged — don't lose

| Item | Priority | Description |
|------|----------|-------------|
| Goal status and closure timestamp contract | P2 | `[OWNER 2026-09-04]` Define a machine-readable goal-level status contract without conflating it with round results, review recommendations, or accepted learnings. Today closure is human-readable from the de facto `## Goal close — YYYY-MM-DD` trail heading, while the initial `goal.md` status remains `grounded` under append-only evolution and the artifact records no time; narratives can only recover a rough time from the introducing commit and must label it a Git proxy. Specify the authoritative open/closed vocabulary, preserve owner-only close and `closed by redirect`, require a timezone-aware `Closed at` value plus disposition in the close record, update the trail template and contract tests, and define how legacy goals remain readable without rewriting their history. Do not add a narrative dependency to `/run-goal`. |
| ~~Test cleanup: one-plant assumptions and migration-era asserts~~ | P1 | **Discharged 2026-08-21** by stellarator-model-migration Phase 4: `tests/models/test_self_binding_replacement.py` replaced by `tests/models/test_model_family_spines.py` over the family registry `tests/model_families.py` -- per-family generation from a materialized canonical subset, IFE 23/18 with the 11 renamed / 7 unrenamed sets merged into one neutral census, MFE census captured from the sealed 2.0.0 contract (`tests/models/data/mfe_census.json`), both IFE mutations unchanged, two MFE mutations added; `tests/test_codegen_teax_acceptance.py` and `tests/test_occurrence_mutation_teax.py` generate the IFE subset instead of all of `models/`. |
| Revert the six scalar-function rewrites and the ASCII comment normalization in the stellarator model once the codegen pin catches up | P2 | `[OWNER 2026-08-21]` chose "rewrite now, file upstream, revert later" for the six `sqrt`/`max`/`min`/`floor` sites, and accepted the ASCII normalization of comment text as Class B. Both are ledgered in `models/stellarator_migration_ledger.md`: the six invocation sites are hand rows 5-10 (bodies verbatim in Appendix A/B; now `'DT Fusion Power'` at `models/library/analyses/mfe_plasma_scaling.sysml:218` and `'Levelized Replacement Cost'` at `mfe_account_costs.sysml:879`, both opaque manual interfaces), the 134 ASCII rows carry trigger "mis-located unit scrape" (`—`→`--`, `Σ`→`Sigma` in 8 files). Upstream: sysml-codegen `[SCALAR-FUNCTION-VOCABULARY]` (motivating case attached) and `[UNIT-SCRAPE-BYTE-OFFSET]` (defect). When fusion-tea's codegen pin moves past both: restore the bodies from Appendix A/B, restore the punctuation, regenerate, re-pin `studies/manifest.json`, re-derive the known-answer and census fixtures (they are bound to the semantic fingerprint and say so when it moves). Until then the model text carries the workaround and codegen's own refusal is the guard -- a re-introduced `—` in a comment fails generation loudly, never silently. |
| `uv.lock` on main is not regenerable — fix upstream and re-pin | P1 | `uv lock` cannot resolve `pyproject.toml` from scratch (uv 0.8.22 / 0.9.5 / 0.10.0, online or offline): at the pinned commit, sysml-codegen's `pyproject.toml` declares `agentic-mbse = { path = "../agentic-mbse" }` under `[tool.uv.sources]`, and uv resolves a relative path inside a *git* source as a subdirectory of that checkout, which does not exist. The stop-parser pin commits (`510d8208`…`8cb0b838`) edited the lock by text substitution and only ever ran `uv lock --check`, which verifies consistency, not resolvability. Consequence: no dependency can be added to fusion-tea without hand-editing the lock (done once, verified by `uv lock --check`, for `jsonschema` on 2026-08-21 — see `.project/completed/20260821_stellarator-demo-landing/plan.md` Phase 1 notes). Fix: sysml-codegen drops or workspace-scopes the relative source; fusion-tea re-pins through the provenance chain (`tests/test_dependency_provenance.py`). Surfaced 2026-08-21. |
| Review codegen-generated artifacts: what belongs in git vs ignored | P1 | `[OWNER 2026-08-21]` "there is a LOT of data generated during the codegen process" — decide, per artifact class, tracked vs ignored vs regenerable-on-demand, then apply it in one cleanup commit + `.gitignore`. Census on `main` at `d0e4398d` (tracked files / bytes): `exploration/stellarator_e2e/generated/` 140 / 737 KB (the sealed package — hash-verified by teax, so it is either tracked or regenerated from the snapshot with a license); `stellarator.snapshot.json` 1 / 637 KB (license-free regeneration input); `exploration/stellarator_e2e/outputs/` **300 / 68 KB tracked despite the ignore rule added 2026-08-21** (predate it); `exploration/stellarator_e2e/study/` 8 / 654 KB (proof-of-life evidence, keep); `exploration/ife_e2e/generated/` 46 / 136 KB; `exploration/ife_e2e/outputs/` 72 / 19 KB (3 named fixture dirs + residue); `exploration/pipeline_spike/` 274 / 409 KB; `data/ife_sweep/` 3 / 1.35 MB. No `__pycache__`/`.pyc` tracked. Classes to rule on: sealed packages, snapshots, handwritten impls (must stay), generated `tests/`, per-run output dirs (keep only named fixtures), study stores (`study/_work/` already ignored), sweep data. Output: a short policy section in `exploration/README.md` or CLAUDE.md, the `.gitignore` rules, and the cleanup commit. Coordinate with the migration PR, which regenerates the stellarator package. **Note 2026-08-21:** the migration regenerated `exploration/stellarator_e2e/generated/` in place (147 sealed artifacts at runtime contract 2.0.0, tracked); the after-migration grid/sweep CSVs were byte-identical to the tracked proof-of-life CSVs, so nothing new was added under `study/`; study stores stay under the gitignored `study/_work/`. |
| Rewrite `exploration/stellarator_e2e/handshake_1costingfe.py` onto the stock route, or archive it | P3 | `[OWNER 2026-08-21]` leave it for now. The Items 1-4 handshake harness feeds 1costingFE's solved values into the package and compares account by account (`HANDSHAKE_REPORT.md`). It carries its own glue-1 (`patch_bop_wiring`, an in-place edit of the pipeline file) and an injection map keyed to the retired 1.0.0 package (`mfe_stellarator.yaml`, `system_design.json`, the per-usage CAS27/CAS28/`n_mod` keys), so it cannot run on the sealed 2.0.0 package and would tamper with it if it could. If the Stellarator Demo epic resumes: feed 1cfe's values as a proposal through `studies/study_route.py` (no in-place edits), re-key to the plant-level entry points, and redo the per-account comparison; otherwise archive it beside the report. Listed as historical in `tests/study/test_no_retired_identifiers.py`. |
| `VALIDATION_MATRIX.md` rows SV-034/SV-035 are invisible to the PM parser; `pm add-validation` reused SV-034 | P2 | Surfaced 2026-08-21 while registering SV-036 (WI-030). Both rows carry `\|rel dev\|` inside a cell; agentic-mbse's table parser splits on every `|` without honoring the escape (`src/agentic_mbse/pm/parser.py:109`), so the columns shift, the Type check fails ("Invalid Type 'rel dev'" on every `status` run), the rows are dropped, and `_next_id` derives the next SV from the surviving rows (`pm/operations.py:58-70,515`) -- it minted a second `SV-034`, hand-renamed to SV-036. Fusion-tea half: rewrite the two cells without the pipe character (e.g. "abs rel dev") so the rows parse; re-run `uv run agentic-mbse status` to confirm the two warnings are gone. Upstream half filed in agentic-mbse `.project/backlog/BACKLOG.md` `[PM-MATRIX-ESCAPED-PIPE]` (parser honors `\|`; `_next_id` scans raw ids so a dropped row can never cause reuse). Until the upstream lands, check the tail of the matrix after every `add-validation`. |
| Run-study tools cannot read a constraint predicate with arithmetic in an operand | P2 | Surfaced 2026-08-21 by the WI-030 design spike. The pinned sysml-codegen (`8a758e92`) compiles `B_axis * peak_ratio <= B_max` into a working predicate with a correct margin (`predicate_compiler.py:160-176, 260-281`), but `scripts/study/indicators.py:450-473` (`predicate_operands`) and `scripts/study/verify.py:174-200` (`derive_verdict`) admit only `feature_ref` and `literal` operands and raise on `operator`, so `preflight.py gates` and `verify.py` fail on any such catalog. WI-030 worked around it with a helper calc (`'Conductor Peak Field'`) and a plain comparison — the `wall_load_ok` shape — which is also the better shape for reachability. Fix when a one-line inequality is wanted without a helper calc: evaluate arithmetic operand subtrees in both tools (the leaf references resolve through the same `operand_bindings`), and classify the constraint's operand classes from the leaves. Owner: Run-Study Capability epic. Evidence: `knowledge/research/approved/20260821-152108_wi030-computed-beta-peak-field.md` § 1. |
| Census derivation has no importable home outside a test module | P2 | Surfaced 2026-08-26 by the integration seam (GSTH Item 3). The entry-point classification lives in `_by_entry_type`, a private helper inside `tests/models/test_model_family_spines.py:169`. It is the only implementation, and R-B1 forbids a second one, so `scripts/integrate.py` gate 4 imports a private test helper to re-derive the census against a sealed package. That is a production tool reaching into a test module. Fix: give the derivation an importable home the suite and the seam both consume. Home: `tests/models/test_model_family_spines.py`. Consequence today: the seam works, with a smell it names in a comment rather than absorbs. |
| fusion-tea has no automated teax revision pin | P2 | Surfaced 2026-08-26 by the integration seam (GSTH Item 3), and it is the fusion-tea half of a two-sided gap. teax is a working checkout reached through `STOP_PARSER_TEAX_ROOT`, not a pinned dependency; nothing in `tests/` or `scripts/study/` asserts its git revision, and the only pin check that has ever run is hand-typed (`.project/completed/20260821_stellarator-model-migration/plan.md:47`). The seam does the `git rev-parse HEAD` comparison itself against a caller-supplied `--expected-teax-revision`, and fails closed when it is absent. Fix: an automated pin beside the other three. Home: `tests/test_dependency_provenance.py`. Upstream half: stock teax exposes no `__version__` for a tool to read — same root cause as the open `verify.py` `teax.revision: "unrecorded"` row below. |
| The model-family spine suite is not parameterizable by package | P2 | Surfaced 2026-08-26 by the integration seam (GSTH Item 3). `pytest tests/models/test_model_family_spines.py` takes no package argument: `_subset` (`:216`) materializes from the repository's canonical `models/` tree and the MFE census test compares against the tracked `DATA / "mfe_census.json"` (`:353`). So the seam's gate 5 is **repo-scoped** and says so in its return, and its refusal path is **untestable** — driving a real refusal would need an edit to the tracked census (violates test hermeticity), to the canonical `models/` tree (a model change), or to the suite itself (frozen by R-B2). Recorded as a stated coverage boundary in `.project/completed/20260827_goal-integration-seam/plan.md`, not papered over with a mock. Fix: let the suite accept a package and a census. Home: `tests/models/test_model_family_spines.py`. |
| `verify.py` collapses both failure modes into one exit code and writes no summary when it refuses | P2 | Surfaced 2026-08-26 by the integration seam (GSTH Item 3). `main` catches `ToolError` and `ManifestError` and returns 1 (`scripts/study/verify.py:527-529`) *before* reaching `common.write_document`, and `VerifyError` is a `ToolError` subclass. So a parity refusal, an empty store, an unreadable manifest and a failed `import simkit` all look identical from outside: exit 1, stderr text, no document. Consequence: the seam cannot separate refused from could-not-run at gate 8 from verify's own output. It pre-empts the one environmental cause at gate 0 by probing `import simkit` under the same environment the subprocess gets, and past that reads any non-zero exit as `refused` — so a teax import failure the probe did not predict arrives mislabelled. Fix: write a summary on refusal, or separate the exit codes. Home: `scripts/study/verify.py`, same file as the `teax.revision` row below. |
| `assert_read_set_covered` has no caller outside the indicator reader | P2 | Surfaced 2026-08-26 by the integration seam (GSTH Item 3). `manifest.assert_read_set_covered` (`scripts/study/manifest.py:475`) needs `resolved_paths` — the paths the indicator reader opened from the pipeline's own `EntryPoint` refs — which exist only inside `scripts/study/indicators.py:808`, its sole caller. The seam's gate 6 runs the other three of R-B1.6's four manifest assertions and **cannot** run this one without reimplementing the reader. Nothing else covers it either: preflight's six gates do not, and `verify.build_summary` does not. So the assertion that a regenerated pipeline cannot point at a file the pin never globbed is, today, covered by nothing. The seam's gate-6 result says so in words rather than staying silent. Fix: a caller, or a form that derives the read set from published artifacts. Home: `scripts/study/manifest.py` / `scripts/study/indicators.py`. |
| `verify.py` records `teax.revision: "unrecorded"` | P2 | Open row carried from `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` `20260821-power-cycle-ab#8`, restated here 2026-08-26 because the integration seam works around it. `verify.py:441` writes `getattr(simkit, "__version__", "unrecorded")` and stock teax exposes no `__version__`, so every verification summary records an unknown toolchain revision. The seam records the teax checkout's git `HEAD` in its own return's `toolchain` block — that is **not** a discharge of this row: the summary `verify.py` writes is still unrecorded, and a study reading that summary alone still cannot say what it ran under. Home: `scripts/study/verify.py`; upstream half is teax exposing a version. |
| `tests/scoring_v2` writes a tracked file and 44 of its cases fail | P2 | Surfaced 2026-08-26 by the integration seam's Phase 10 whole-suite sweep, **not caused by it** — that item touches nothing under `exploration/scoring_v2/` or `tools/`. Two separate things, both pre-existing. (1) **Hermeticity:** running `uv run python -m pytest tests/ -q` leaves `tools/score_explorer/data/concepts.json` modified (107 lines rewritten), so `git status --porcelain` is dirty after a full test run and any gate that checks tree cleanliness after the suite fails for a reason unrelated to what it was gating. Home: `tests/scoring_v2/test_score_explorer_build.py` and whatever it drives. (2) **Failures:** 44 failed / 940 passed / 58 skipped, every failure in `tests/scoring_v2/test_spec_conformance.py::TestSpecPredictedScoresLand` (the `upper_cf` axis). The last recorded baseline was 34 in the same class (`.project/active/goal-research-seam/plan.md:589`), so the count has grown and nobody has looked. Fix: make the explorer-build test write to `tmp_path`, then reconcile the predicted-score expectations against the rulebook. |
| A declared study store channel that is one field of a multi-output calc comes back as a silent blank column | P1 | **Suspected code bug, not a modeling item** `[OWNER 2026-09-04]`. **Symptom:** a study definition declares a package channel (e.g. `stellarator_09__stellaris__heat__p_coupled`) in its exported column set; the store accepts the study, every case completes, and the column is blank in all rows — no gate, no warning. **Four committed sightings:** `20260821-power-cycle-ab#5` (`pb__*`), `20260901-sustainment-fence#3` (`sustain__*`, six blank columns on the first export), `20260903-priced-levers#4` (`aux_cooling__cryo_cost`), `20260903-wall-and-heating#4` (all four `heat__*` outputs blank in 639 rows on the first execution); three of them after `exploration/stellarator_e2e/studies/ANNEX.md` § Oracle documented the trap. **Mechanism, both halves:** (a) *teax* — `~/1cfe/teax/packages/teax-simkit/simkit/evaluation/projection.py:25-27,77-81` (`_is_scalar_output` / `project`) keeps only outputs that duck-type `RootModel[float]`; a multi-output calc is emitted by sysml-codegen as one module whose result is a multi-field model, so none of its fields is a scalar output and none reaches `ModelEvidence.outputs` — the store never sees them and cannot warn; (b) *fusion-tea* — the per-study exporters read `case.outputs.get(channel)` (`exploration/stellarator_e2e/studies/20260903-wall-and-heating/study.py:410`, same line in the priced-levers and sustainment-fence definitions), which yields `None` silently, whereas the route's shared exporter fails closed on exactly this (`study_route.py:263-268`, "case is missing required result channels") — the study-local exporters regressed the check. **Fix, fusion-tea side (this repo):** make `export()` in the study definitions raise on any declared channel absent from `case.outputs` (reuse `study_route.csv_rows`'s check), and add a declaration-time gate — in `scripts/study/preflight.py` or `indicators.py`, which already resolve every declared key against the package — that refuses a declared output channel unless the pipeline publishes it as a scalar; the `ANNEX` prose then stops being the only guard. **Fix, upstream (file in both):** sysml-codegen — flatten a multi-output calc's fields to scalar channels at emission, or mark them; teax — record multi-field module outputs in the evidence projection instead of dropping them. Attach the four sightings. Values today are exported oracle-side (`results/oracle_operands.csv`) and are oracle-derived on both sides, so they are never independently verified (record § 13 of each sighting). Owner: Run-Study Capability epic (fusion-tea half); upstream filings owner-held. |
| `scripts/source_registry.py` has no `unregister`, and `register` accepted a 1.4 KB bot-check page as a source | P2 | `[OWNER 2026-09-04]` mint the tooling item. **What happened:** during goal `wall-and-heating` T-001 (`work/orchestration/goals/wall-and-heating/evidence/T-001_research_return.md` § 7; run `knowledge/research/requests/runs/REQ-WALL-01/20260904T035638551421/`), the seam fetched an IOPscience URL, received a Radware bot-check page (1415 bytes), extracted it cleanly, and registered it: `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and/` (no `_2`), a `knowledge/MANIFEST.jsonl` row, and a `knowledge/SOURCE_INDEX.md` block (`:382-392`). No hold-out term, no capture failure, so nothing in the seam objected. The real paper is registered at the `_2` slug from the publisher PDF and its index `Caveat` names the junk slug. **Why it was left in place:** the registry's contract is that the source directory, the manifest row and the index block move together or not at all (`register` and `verify` are the only operations; `source_registry.py:198,740`); hand-editing three files to undo one registration is the surgery the contract forbids, and a goal round may not absorb a seam repair (`GOAL_RUNBOOK.md` § The native seams). **Fix:** (1) `unregister <source_id>` that removes the three pieces atomically and writes a receipt, refusing if anything else cites the id; (2) a guard in `register` that refuses an extraction below a minimum size or whose text matches a bot-wall / consent-page signature (Radware, "verify you are human", Cloudflare challenge), returning a `capture_failed` receipt the seam already knows how to route; (3) run `unregister` on the junk entry once (1) lands and re-run `verify`. Owner: research seam (GSTH Item 2's tool, archived epic) — a small standalone coding item. |
| ~~Rework design-point table: per-row verification gate never signed~~ | — | **Accepted as-is `[OWNER 2026-08-21]`.** `exploration/concept_analysis/tables/design_point.csv` rows were consumed by the June regeneration without the planned hand check (`verified_by` blank on 33/33); the owner chose to leave it. Record: `.project/completed/20260821_concept-rework-tables/spec.md`. |

---

## In Progress

| Epic | Priority | Status | Started | Notes |
|------|----------|--------|---------|-------|
| Run-Study Capability | P1 | In Progress | 2026-08-19 | Items 1–5 complete and audited (2026-08-20); item dirs archived 2026-08-21. Item 6 (first A/B consumer + policy cutover) runs **after the stellarator model migration** `[OWNER 2026-08-21]`, on the stock teax route. File: `epic_run_study_capability.md`. |
| Stellarator MBSE Full Demo | P0 | On Hold | 2026-07-18 | On hold by owner 2026-08-19. Items 1–4 complete (handshake to 1costingFE: LCOE 275.264220, 5/5 verdicts); item dirs archived 2026-08-21. The Run-Study Capability epic owns the A/B proof; this epic consumes that evidence if resumed. File: `epic_stellarator_mbse_demo.md`. |
| Explorer UX v3 — Provenance & Coherence | P1 | Active (dormant since 2026-06-29) | 2026-06-06 | Phase 1 done: Items 1, 2, FU1 (PR #52), Themes A (#58), B1 (#59), F1 (#64), 1costingfe v0.1.0 migration (2026-06-28); item dirs archived 2026-08-21. Remaining: Phase 2 (B2, B3, C1, C2, D1–D3, E1–E3 — not decomposed; D1 top candidate), FU2 (extract-all audit), Item 2-FU (re-extract 27/37/39). File: `epic_explorer_ux_v3.md`. Research: `.project/research/20260605-150329_concept-explorer-ux-user-journeys.md`. |

---

## P1 - High Priority

### Run-Study Capability

**Priority**: P1
**Effort**: ~6–9 days (6 items)
**Status**: In Progress — Items 1–5 complete and audited 2026-08-20; Item 6 after the stellarator model migration `[OWNER 2026-08-21]`

Turn the verified proof-of-life study discipline into a durable `run-study` skill, runbook, policy, package-agnostic tools, and evidence-complete records. This epic owns the first A/B proof while the Stellarator MBSE Demo epic is on hold.

**Items**:
- [x] Item 1: Indicator Reachability Spike — complete 2026-08-19
- [x] Item 2: Skill, Runbook, and Record Contract — complete 2026-08-20
- [x] Item 3: Indicator Tool and Package Manifest — complete 2026-08-20
- [x] Item 4: Quality Tools and Era Adapter Promotion — complete 2026-08-20
- [x] Item 5: Cold-Pickup Administrator Exercise — complete 2026-08-20 (owner-approved)
- [ ] Item 6: First A/B Consumer and Policy Cutover — after migration (stock route, no era adapter)

**File**: `epic_run_study_capability.md`

---

### ~~Goal Strategy and Task Harness~~ ✅

**Priority**: P1
**Effort**: ~9.5 days (6 items)
**Status**: Complete (2026-08-30) — all six items closed and archived 2026-08-27 → 2026-08-30
**Archived to**: `.project/completed/20260830_epic_goal_strategy_task_harness.md`

Build a lean, disk-resumable goal layer above the native research, modeling, integration, and study workflows. Repair the research-registration and verified-integration seams, then prove cold grounding, interruption recovery, independent disposition criticism, finding closure, and human/goal-agent route equivalence on a real goal.

**Items**:
- [x] Item 1: Lean Goal Contract and Operator Runbook
- [x] Item 2: Native Research Acquisition and Registration Seam
- [x] Item 3: Verified Package Integration Seam
- [x] Item 4: Goal Grounding, Cold-Pickup Resume, and Round-Review Proof
- [x] Item 5: Research-to-Model Round Proof
- [x] Item 6: Integration-to-Study Closure and Route Equivalence

**File**: `.project/completed/20260830_epic_goal_strategy_task_harness.md`

---

## Active Work Items

| Item | Priority | Status | Location |
|------|----------|--------|----------|
| run_analysis.py CLI Step Semantics | P1 | Paused — spec drafted 2026-06-05, never started, filed 2026-08-20. `analyze` and `model-setup` present as peers but overlap; `regenerate-concept` chains them so the weaker model-setup path overwrites the loop's output. Still present on `main` (`run_analysis.py:1449`, `:1312`). | `.project/active/run-analysis-cli-step-semantics/` |
| Traceability System | P1 | Spec + design + plan drafted 2026-03-02, never started. `scripts/trace_audit.py` does not exist. Still cited by CLAUDE.md and MR-4 as the citation-format spec. | `.project/active/traceability-system/` |
| Loop Dry-Run Symmetry | P2 | Spec only (2026-04-10). The gap is still live: under `--dry-run`, model-setup prints and returns without writing `model_setup_prompt.md` (`exploration/concept_analysis/scripts/lib/loop.py:620`). | `.project/active/loop-dry-run-symmetry/` |
| ~~Study parameterization policy (rulebook, not a work item)~~ | — | **Ratified and moved 2026-08-21** (RUN-STUDY Item 6 Phase 1, owner-reviewed): now `modeling_project/STUDY_POLICY.md`; the skill and runbook cite that path. |

### Explorer-rework-unblock follow-ups (2026-06-05, re-verified 2026-08-21)

Concept-side data issues surfaced by the first end-to-end run after `explorer-rework-unblock` landed (spec: `.project/completed/20260821_explorer-rework-unblock/spec.md`).

| Item | Priority | Description |
|------|----------|-------------|
| Fix the remaining concept-side model_setup.py issues + re-extract | P1 | State on `main` 2026-08-21. **04**: fixed (costingfe-asterisked, served). **17b**: served, but the live `analyses/17b-.../model_setup.py` is the old shape ("no result_1gw is needed") while `iter-1/model_setup.py` is three-forward; the extractor reads the live path (`extract_explorer_data.py:1246`), so the served data may be stale. **27**: still `freeform-deferred` frontmatter on a `from costingfe` module; stale JSON, 0 override records. **38**: `data/38.json` still missing (freeform model emits `inf` LCOE). **39**: re-routed to costingfe-asterisked but `model_setup.py:16` still says "should be updated with real values"; 0 override records. After fixes: `uv run python exploration/concept_explorer/extract_explorer_data.py --skip-narrative`, confirm all 40 concepts write a valid `data/{id}.json`, and `uv run python exploration/concept_explorer/server.py` starts cleanly. |

---

## Completed

| Epic | Completed | Duration | Notes |
|------|-----------|----------|-------|
| Visualization POC Sprint | 2026-01-19 | 2 days | Full Cytoscape.js pipeline, 23+ tests |
| Cost Modeling Patterns De-Risking | 2026-03-06 | ~2 months | Learnings handed off to sysml-codegen, all changes implemented |
| End-to-End Pipeline De-Risking | 2026-03-06 | ~5 weeks | Solar+battery pipeline proven, codegen enhancements in open PR |
| Full Workflow Demo | 2026-03-06 | 5 days | Interactive HTML explainer + IFE modeling demo |
| Knowledge Database Integration | 2026-02-27 (archived 2026-08-21) | ~3 weeks | Zotero → extract → SOURCE_INDEX pipeline; Item 4 abandoned (superseded by IFE source ingestion). `completed/20260821_epic-knowledge-database-integration.md` |
| Concept Analysis v2 | 2026-04-11 (archived 2026-08-21) | ~3 weeks | Iterative analysis loop, memory, manage-concept; Item 2 (visuals) superseded by the explorer. `completed/20260821_epic_concept_analysis_v2.md` |
| Explorer UX v2 | 2026-04-06 (archived 2026-08-21) | ~1 week | Selection tray, compare shell, categorical/capex views, integration. `completed/20260821_epic_explorer_ux_v2.md` |
| Pipeline Hardening (ad-hoc) | 2026-04-11 | ~1 week | 6 items archived 2026-04-11; see CHANGELOG |
| Source Extraction Fix & Re-extraction | Abandoned 2026-04-11 (archived 2026-08-21) | — | Never decomposed. Cleanup goals delivered by `source-replacement` + `orig-md-research`; the table-quality complaint itself was not addressed. `completed/20260821_epic_source_extraction_fix.md` |
| Ontology v3 Migration | 2026-05-21 (archived 2026-08-21) | ~1 week | Items 1–4 merged (PRs #15, #16, #19, #20); Item 5 superseded by the rework's regeneration; Item 6 discharged by the 2026-08-21 cleanup. `completed/20260821_epic_ontology_v3_migration.md` |
| Concept-Analysis Pipeline Rework | 2026-06 (archived 2026-08-21) | ~3 weeks | Items 1, 4–11 shipped (PR #44, regen PRs #46/#48); Items 2–3 superseded by the prototype findings; Item 12 aspirational. Open: the unsigned design-point gate (Flagged row above). `completed/20260821_epic_concept_analysis_rework.md` |

---

## Ideas / Future Considerations

- Traceability audit automation (blocked on traceability-system implementation)
- Rework Item 12: native-scale supplement beside the 1 GWe replication floor (aspirational; `completed/20260821_epic_concept_analysis_rework.md`)
