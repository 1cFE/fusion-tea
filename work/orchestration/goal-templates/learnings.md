# Learnings: <goal name>

What this run now knows. Copy to `work/orchestration/goals/<goal-slug>/learnings.md`.

Append-only, newest last, ISO dates, never edited in place. An entry is appended **only after** a round review has accepted or corrected the delta the round result proposed (`GOAL_RUNBOOK.md` § The fresh review). Mechanical failures produce no learning.

Each entry is one claim.

## L-001 — <the claim, in one line>

- **Evidence:** native refs that carry it, cited by path and digest.
- **Scope:** where the claim holds, and where it is not known to hold.
- **Implication:** what a later strategy should do differently because of it.
- **Supersedes:** `L-00M`, or `none`.
- **Accepted by:** round N review, YYYY-MM-DD.
