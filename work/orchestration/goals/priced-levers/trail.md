# Trail: priced-levers

What happened, and what was decided. Append-only, newest entry last, ISO dates; no entry is ever edited in place — corrections are `### Amendment` entries. This file logs judgment, not routine stage motion; native workflows keep their own stage records and are cited, never restated. Procedure: `work/orchestration/GOAL_RUNBOOK.md`.

Goal grounded 2026-09-02 (`goal.md`, owner-present session, straight off the close of `operating-point-closure`). No round open.

## Round 1 — priced-field-lever

### Strategy revision — 2026-09-02

- **Approach:** make the *field* lever real, because that is where the deadlock actually binds. The goal-grounding recount (goal.md § Amendment 2026-09-02) shows every sustainment-satisfying point at p = 50 is blocked by the conductor ceiling **and** the winding-pack stress limit together, so pricing either one alone opens nothing. Three moves: (1) **winding-pack sizing chain (WI-036)** — `wp_side` becomes a design variable that feeds winding-pack volume, conductor and coil cost, and the WI-024 cryo cold-mass chain, so relieving stress costs conductor, cold mass and radial build instead of being free; (2) **conductor-grade consequence chain (WI-038)** — `B_max` carries an admissible sourced basis and a cost consequence, so raising the ceiling is priced rather than granted; (3) a committed study sweeping the field lever at the printed p = 50 — does a feasible region open once both fences can be bought past, and at what LCOE. Research runs first and in parallel, because the conductor basis is the one input the pinned 1costingFE cannot supply.
- **Assumptions:** the model's stress form σ = k_sigma·I_coil·B_peak/wp_side (k_sigma held, calibrated at the printed worst-coil pair) is usable as the relief channel, so a wider pack lowers stress at fixed current and field; the cost and cryo chains can consume a computed `wp_side` (WI-036 is exactly that item); an admissible, clean-room-legal basis exists in the open literature for REBCO winding performance near 29–30 T at 20 K and for how conductor cost scales with it; the 800 MPa 316LN allowable is the model's own sourced structural limit and is challenged only with a source, never relaxed to fit.
- **Abandonment conditions:** no admissible field-ceiling basis **and** no structural relief that opens feasibility at p = 50 — that is a bounded negative on the field escape, and routes the goal to its heating half rather than continuing here; the stress relief channel proves not to be `wp_side` (the held k_sigma form cannot bear a sizing sweep with an admissible basis); a load-bearing basis fails image verification with no admissible substitute (surfaced with options, never defaulted); or the committed-study restatement cannot be honestly written.
- **Intended model increment:** WI-036 and WI-038 minted into spec/design/plan through the modelling PM — computed winding-pack volume and cost from `wp_side` and coil geometry, cold mass into the cryo chain, and `B_max` with a sourced grade basis plus its cost consequence; instance rebinds in `stellarator_09`; entry-point retirement with the MR-WI037-7 restatement duty recorded before regeneration.
- **Intended study question:** with the field lever priced on both fences, does a feasible operating point exist at the printed 50 MW installed heating, what does it cost in LCOE, and which fence binds at the constrained optimum?

No future task list. One pin, one committed study, close on the first valid reading.

### T-001 scope

- **Objective:** establish whether an admissible, clean-room-legal sourced basis exists for (a) a REBCO winding-pack peak-field capability at or above ~29 T at 20 K, (b) how conductor cost scales with that capability, and (c) whether the 800 MPa structural allowable has a defensible higher-strength alternative — or return a bounded negative naming what is missing.
- **Why now:** the strategy's move (2) cannot be specified without a basis, and the pinned 1costingFE cannot supply one — its `MAGNET_TABLE` tops out at `rebco_hts b_max = 23.0` (`defaults.py:609-617`), below the model's own 24.9 T. Research has latency and is the input to the model increment, so it runs first and in parallel with nothing else executing.
- **Scope:** authorized — the native research seam (`scripts/research_seam.py` request/open/log/close, `scripts/source_registry.py register`, `/research-acquire`), read-only reads of admissible sources and of the pinned 1costingFE, and the run directory under `knowledge/research/requests/`; excluded — any edit under `models/` or `exploration/`, any modelling-PM stage, discovery-log writes, and every artifact barred by `knowledge/holdout/aries-cs/PROTOCOL.md` §3 (the four sealed PDFs, the ARIES-CS-informed extractions, and the two documented-exception costing sources without an owner exception).
- **Inputs:** `goal.md` including § Amendment 2026-09-02; `knowledge/holdout/aries-cs/PROTOCOL.md` §3 (narrower constraint: clean room binds every search and every registration, and the seam's own hold-out check is a backstop, not a substitute for not looking).
- **Done when:** the seam returns for each request — `REGISTERED` with citable paths, `BOUNDED_NEGATIVE` with the durable negative written, or `OPERATOR_QUEUE` naming what a person must fetch — and the goal-level reading says which of the three model inputs are now sourced and which are not.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-001 start — 2026-09-02

Task T-001 under § T-001 scope: run the research seam for the conductor field-ceiling basis, the conductor cost-vs-field basis, and the structural-allowable alternative. Native target: `knowledge/research/requests/` (requests, run records, receipts, returns) and, on a keeper, `knowledge/sources/` + `knowledge/MANIFEST.jsonl` + `knowledge/SOURCE_INDEX.md` through `source_registry.py`. Expected artifacts: three request files, three run directories with `return.json`, and either registered source directories or durable bounded negatives.

### Strategy evidence — 2026-09-02

Deposited at `evidence/R1_deadlock_recount.md`: the recount behind `goal.md` § Amendment 2026-09-02 and behind this round's strategy, from `20260901-sustainment-fence/results/points.csv` (record dir `@62a1fa7b`). Goal-layer analysis of committed artifacts; no native side effect. Three results carry the round:

1. **No point at p = 50 is blocked by the conductor ceiling alone** — all 29 sustainment-satisfying points violate `peak_field_ok` and `wp_stress_ok` together.
2. **The model's stress form is a real relief channel** — σ = k_sigma·I_coil·B_peak/wp_side reproduces the committed `sigma_wp` column to the printed digit at every I_coil, and the relief `wp_side` needed for 800 MPa is tabulated (0.400 m at 18 MA, rising to 0.710 m at 24 MA). Nothing charges for it today.
3. **Eleven points fail only those two fences** — the candidate feasible region at the printed 50 MW. The unlock condition is concrete and conjunctive: **B_max ≥ 29.1 T and wp_side ≥ 0.400 m**.

**Pre-registered expectation, recorded before any model change:** the cheapest field-escape point reads 332.95 $/MWh with the wider pack and better grade **not** charged, against the committed heating-escape alternative at 293.468 $/MWh (p = 110). The field escape is 13.5% behind before it is priced, and pricing can only move it up — so this round is expected to return a *negative* on the field escape. It is run anyway because the grid is coarse, the 11 points were never optimized against a live sizing lever, and the heating escape's own number is not yet honestly priced either. The pre-registration exists so a confirming result is not mistaken for a discovery; it pre-settles nothing, and the dispositions still go to the fresh checkpoint reviewer.

### T-001 return — 2026-09-02

- **Outcome:** `COMPLETE`. Both halves of § T-001 scope's done-when hold: all three seam runs returned, and the goal-level reading below says which model inputs are now sourced and which are not.
- **Evidence:** `evidence/T-001_research_return.md` (the reading of record; this entry summarizes). Native artifacts: three requests and three closed runs under `knowledge/research/requests/`, all `REGISTERED`; four sources registered through `source_registry.py` into `knowledge/sources/`, `knowledge/MANIFEST.jsonl` and `knowledge/SOURCE_INDEX.md` — Molodyk et al. 2021 (`development_and_large_volume_production_of_extremely_high`), Titus & Kessel PPPL-5297 (`in_plane_and_out_of_plane_tf_coil_support_for_the_us_fnsf`), Bottura & Bordini 2025 (`hts_potential_and_needs_for_future_accelerator_magnets`), Zhang et al. 2017 (`general_approach_for_the_determination_of_the_magneto`). Clean room honored; one screened candidate (pyFECONs costing framework) deliberately **not** registered.
- **Reading.** Four results, in the order they change the round.
  1. **The binding fence is structural, and it has no headroom.** PPPL-5297 states verbatim — verified in the registered extraction at `output.md:95-99,137`, not taken on the searcher's word — that the ITER-basis primary stress allowable is 666 MPa (2/3 × 1000 MPa yield) and that **800 MPa is the "optimistic" allowable for improved 316 metallurgy**. The model's `sigma_allow = 800e6` is inherited from Stellaris' own design limit and is not wrong; but the goal's grounding assumed room above it, and there is none. Material substitution buys ~5% (JJ1 vs 316LN). The routes the report itself names are **reallocation of metal cross-section** and a change of design rule (limit analysis, FoS 2.0 against burst).
  2. **Field capability is not the limit.** Molodyk et al. give J_E > 1000 A/mm² at 20 K and 20 T in the worst orientation and state Jc ∝ B^(−0.6) at 20 K. Bracketed against the same wire's 8 T measurements, the exponent is 0.46–0.68. The 24.9 → 30 T step the deadlock needs costs **×1.12 in tape** (×1.32 on the steepest exponent) against a grading lever worth 4.8×. It costs **×1.45 in Lorentz stress**. That asymmetry — cheap in conductor, expensive in structure — is the round's central physical finding, and it points the increment at WI-036 rather than WI-038.
  3. **The model is missing a sizing relation its own primary source states.** The image-verified Stellaris Table 8 closes to <1% across all six coils under `I_coil = j_WP · wp_side²`, and Σ(side²)·25·8 reproduces the held `vol_cold_cryo = 136.56` exactly. The committed study swept `I_coil` 8→24 MA with `wp_side` frozen, implying a winding-pack current density rising from 119 to 154 A/mm² against a design range of 112–124. Sizing the pack consistently at the 18 MA point gives σ = 797 MPa at j_WP = 112 (**feasible**) and 822–839 MPa at 119–124 (over) — a knife-edge that makes `j_WP` the design lever the model does not currently have.
  4. **Bounded negative, with a route.** No open source gives $/kA·m as a function of field. The honest path is a composition — quantity from J_E(B), priced at a flat per-kA·m rate — labelled as constructed and carrying its three citations. For disclosure: the pinned 1costingFE holds 50 $/kA·m (its own comment: "aggressive NOAK target") against Bottura's present-day 150–200 and Zhai's 10 $/kA·m parity target.
- **Decision:** trigger — result 1 is a premise conflict: the goal's § Amendment 2026-09-02 named the structural fence as the binding lever on the assumption it could be bought past, and the literature says the allowable is already the optimistic end. Decision and reason — **surface it to the owner in session and continue**, rather than resolve it silently in either direction (capture-fidelity law 4). It does not abandon the strategy: the escape the sources name (cross-section reallocation) is the same lever WI-036 exists to price, and result 3 shows it reaches feasibility at the low end of the source's own design range. What it forbids is any later move that quietly relaxes `sigma_allow` above 800 to open a region. Tier — premise surprise. Who decided — the round agent, 2026-09-02, under the owner's standing delegation. What changed — `evidence/T-001_research_return.md` § 5; this entry; surfaced to the owner in session.
- **Decision:** trigger — the Wimbush/Strickland/Pantoja critical-current dataset (figshare, CC BY 4.0, openly downloadable) is the measurement basis Stellaris fitted, but its artifacts are `.xlsx` workbooks the source registry cannot extract. Decision and reason — do **not** force a registration the seam would mangle, and do not silently drop it: record the gap and its direct file URLs in the run log and in the evidence file, and use exactly one number from it (the 20 K / 8 T / 0° anchor, 1023 A/cm) as the low end of a bracket whose high end is the registered Molodyk paper. Tier — execution detail. Who decided — the round agent, 2026-09-02. What changed — `evidence/T-001_research_return.md` § 8; the REQ-038-01 run log.
- **Decision:** trigger — Stellaris' own j_crit model at 24.6 T rests on a parameterization fitted at 77 K and ≤0.4 T and a dataset stopping at 8 T. Decision and reason — this is a disclosure about the source, not a defect to correct and not grounds to reject the basis; the model will carry any computed field ceiling as an explicitly labelled extrapolation, licensed to the SuperOx wire family and to no other vendor. Tier — execution detail. Who decided — the round agent, 2026-09-02. What changed — `evidence/T-001_research_return.md` § 8.

**Next task:** not chosen yet — the owner has the premise conflict in front of them. The strategy's move (1) is unaffected and is the natural next task (mint WI-036 and write its spec through the modelling PM); move (2) narrows from "find a better conductor grade" to "compute the field ceiling from tape quantity, and label the extrapolation."

### T-002 scope

- **Objective:** establish what the winding-pack stress fence should actually be a limit **on** — which physical criterion governs, what quantity it is applied to, and what allowable that criterion sets — or return a bounded negative naming what is missing. Explicitly **not** "find a more permissive rule."
- **Why now:** T-001 returned a premise conflict (the 800 MPa allowable is the optimistic end of the steel rule, with no headroom above it) and the owner ruled in session, 2026-09-03, that a research cycle should run on the criterion. The model presently collapses at least two distinct limits into one number, and which one binds decides whether the fence means anything.
- **Scope:** authorized — the native research seam (`research_seam.py` open/log/close, `source_registry.py register`, research subagents under clean-room instruction), read-only reads of admissible sources and the pinned 1costingFE, and the run directories under `knowledge/research/requests/`; excluded — any edit under `models/` or `exploration/`, any modelling-PM stage, discovery-log writes, any change to `sigma_allow` or any other bound value, and every artifact barred by `PROTOCOL.md` §3.
- **Inputs:** `goal.md` including § Amendment 2026-09-02; `evidence/T-001_research_return.md` § 5. Narrower constraint, recorded at the owner's own framing: **the search may return a tighter limit, and a tighter limit is a first-class result.** No source is selected for being permissive, and `sigma_allow` is not relaxed in this task under any finding.
- **Done when:** the seam returns for each request, and the reading says (a) which criterion governs the winding pack, (b) what quantity it is applied to, (c) what allowable it sets, and (d) whether the conductor's own degradation limit binds before the steel's — each either sourced or named as an open gap.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-002 start — 2026-09-03

Task T-002 under § T-002 scope: run the research seam on the governing structural criterion for an HTS winding pack at 20 K and on the REBCO stress/strain degradation limit. Native target: `knowledge/research/requests/` and, on keepers, `knowledge/sources/` + `MANIFEST.jsonl` + `SOURCE_INDEX.md` through `source_registry.py`. Expected artifacts: two request files, two run directories with `return.json`, and either registered sources or durable bounded negatives.

### T-002 return — 2026-09-03

- **Outcome:** `COMPLETE`. All four done-when questions answered — which criterion governs, what it is applied to, what allowable it sets, and whether the conductor binds first — each either sourced or named as a gap.
- **Evidence:** `evidence/T-002_criterion_return.md` (the reading of record; this entry summarizes). Native: two requests and two closed runs under `knowledge/research/requests/`, both `REGISTERED`; three sources registered — Titus IAEA 2018 (`coil_concepts_for_demo_and_next_step_reactors_5th_iaea_demo`), Barth, Mondonico & Senatore 2015 (`electro_mechanical_properties_of_rebco_coated_conductors`), Zhai et al. FNSF (`conceptual_design_of_hts_magnets_for_fusion_nuclear_science`). Two durable queued items: the ITER MSDC itself (IDM, not public) and Pierro et al. 2019 (paywalled, the only source measuring irreversible strain through 20 K).
- **Reading.** The fence is the wrong *shape* of check, and no rule change opens the deadlock.
  1. **The criteria have categories and 800 MPa is not one of them.** Verified in the registered extraction: Sm = 666 MPa primary membrane, yield-only; peak limited to 2.0 Sm, reduced to 1.5 Sm "where local plasticity may affect insulation bonding"; and explicitly "Allowable TF Stress = 1GPa Peak, 666 PM". Our operand is anchored to a row titled "Peak stress on WP" in an analysis that resolves plates and casing as separate bodies, so on that reading it is a **peak** quantity and 800 MPa is conservative, not optimistic. **This corrects T-001's reading**, which compared PPPL-5297's *membrane* figure against our operand and concluded there was no headroom. The category identification carries the whole structural conclusion and is a reading, not a sourced statement.
  2. **The conductor re-tightens it at almost the same place.** Barth et al. measure irreversible strain by manufacturer at 4.2 K/19 T; **SuperOx — the tape Stellaris specifies — is the weakest of five at 0.45–0.47%**, and is one of only two that fully delaminate in test ("interfacial weakness"). The demonstrably safe zone is stated as below 0.4% strain and below 600 MPa. Stellaris' own design sits inside it (600 MPa peak, <0.2% strain).
  3. **Two limits the model cannot see.** Transverse tension (delamination) is **3.6 MPa** measured, 0.5–15 MPa across methods — two orders of magnitude below the axial limit, and impregnation does not help it. Transverse compression is ~200 MPa bare tape. A single scalar against 800 MPa would pass a design failing delamination by ~200×.
  4. **Comparable designs, Stellaris included, run two checks** — stress against the structure, strain against the conductor — with the structure sized so the conductor's strain criterion is met. The model inherited only the first.
- **Decision:** trigger — the owner's question ("go find a better rule") is answered, and the answer is that a looser steel rule exists but does not help. Decision and reason — **`sigma_allow` is not changed in this round**, in either direction. Read as a peak check it could defensibly be 1000 MPa, which would flip 9 of 29 sustainment-satisfying points past the stress fence; but the conductor's axial limit arrives at essentially the same place, and 800 MPa is best characterized as roughly right for the wrong reason. Relaxing it would open a feasible region on a category argument while the governing physical limit stayed where it is. Tier — execution detail under the standing delegation. Who decided — the round agent, 2026-09-03. What changed — `evidence/T-002_criterion_return.md` § 5; nothing under `models/`.
- **Decision:** trigger — the model makes one check where every surveyed design makes two. Decision and reason — record the second check (conductor strain at 0.2–0.4%) and the transverse limits as **findings for the increment's scope**, not as a change made here; T-002 was scoped to the criterion and explicitly excluded model edits. The transverse limits are recorded as a **named gap** with no value assigned, per the no-fallbacks rule. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — `evidence/T-002_criterion_return.md` §§ 3–5.
- **Decision:** trigger — the hold-out guard refused a candidate (`arXiv:2409.01925`, `term:aries-cs matched 4x`) **after** the research subagent had already fetched and reported its contents into this session. Decision and reason — do not register, do not cite, do not use any of its numbers; record the refusal durably (it is in the REQ-036-03 `return.json` `queued[]`) and **surface the disclosure to the owner** rather than absorb it. `PROTOCOL.md` §3 holds that bibliographic ARIES-CS mentions do not taint a source, so the matches may be citations — but that is the owner's call through the §6 exception path, not the round agent's. Every strain number this task relies on rests on Barth 2015 and Stellaris' own reported limit, both registered and independently verified. Tier — **reserved gate** (clean-room protocol is owner-held ground, whatever this goal's gate list says). Who decided — surfaced to the owner, 2026-09-03; the round agent took only the conservative half (refuse and disclose). What changed — `evidence/T-002_criterion_return.md` § 6; the REQ-036-03 return.

**Next task:** not chosen — the owner has the clean-room disclosure in front of them. The increment's shape is now clear enough to scope: WI-036 carries the winding-pack sizing chain plus a second, conductor-strain check, with the transverse limits declared as a named gap.

### T-003 scope

- **Objective:** produce the WI-036 spec — the winding-pack sizing chain, its cost and cryo consequences, and a second conductor-strain check — through the native modelling PM, at outcome level.
- **Why now:** T-001 and T-002 have settled every input the spec needs. The sizing relation is image-verified from the model's own primary source, the criterion question is answered, and the escape route named by both the literature and the evidence is cross-section, which is exactly this item.
- **Scope:** authorized — activating WI-036 in `work/BACKLOG.md`, `work/active/WI-036_*/spec.md`, VALIDATION_MATRIX entries through `pm add-validation`; excluded — design/plan/implement stages, any edit under `models/` or `exploration/`, source ingestion, discovery-log writes.
- **Inputs:** `goal.md`; `evidence/T-001_research_return.md`; `evidence/T-002_criterion_return.md`. Narrower constraint: `sigma_allow` is not changed by this item in either direction (T-002 ruling), and the transverse/delamination limits are carried as a named gap with no value assigned.
- **Done when:** the spec exists, requirements-complete at outcome level, with SV rows registered.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-003 start — 2026-09-03

Task T-003: activate WI-036 and write its spec through the modelling PM. Native target: `work/BACKLOG.md` row, `work/active/WI-036_winding-pack-sizing/spec.md`, `modeling_project/VALIDATION_MATRIX.md` SV rows.

### T-003 return — 2026-09-03

- **Outcome:** `COMPLETE`. The spec exists, requirements-complete at outcome level, with its SV rows registered.
- **Evidence:** `work/BACKLOG.md` — WI-036 flipped `backlog` → `active` with the goal named in its row. `work/active/WI-036_winding-pack-sizing/spec.md` — eleven outcome-level requirements (MR-WI036-1..11). `modeling_project/VALIDATION_MATRIX.md` — SV-044..047 added `pending` via `pm add-validation`. The parser's pre-existing 'Invalid Type' warning on an older row was not touched.
- **Reading:** the increment now has a native, auditable contract, and it is wider than the goal's grounding anticipated in one respect — it carries a **second constraint** (conductor strain), not just a sizing chain. That follows from T-002: every surveyed HTS design runs two checks and this model runs one. Nothing under `models/` moved.
- **Decision:** trigger — the radial build carries a separate held coil thickness (`coil_t = 0.30`) inconsistent with the 0.36 m winding pack, from a different source, with no relation between them. Decision and reason — **record it as a known defect and exclude it from this item**. Reconciling the two would move vessel, blanket and shield volumes at the same time as the magnet chain, making this item's comparison unreadable; the item that fixes it should do so alone. Tier — execution detail under the standing delegation. Who decided — the round agent, 2026-09-03. What changed — spec § Scope boundaries, § Current state.
- **Decision:** trigger — the conductor strain check may bind before the stress check at the design point, changing the baseline verdict set. Decision and reason — accept that as a **disclosed finding rather than a reason to loosen the limit**, recorded as risk 4 in the spec. The precedent is the disclosed `sustainment_ok` violation the predecessor goal carried: an explained verdict change is a finding, never something to tune away. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — spec § Assumptions & risks.

**Next task:** the WI-036 design (native next stage) — the sizing mechanism, the conductor-strain operand's honest meaning, and the tolerances.

### T-004 scope

- **Objective:** produce the WI-036 design — the sizing mechanism, the cold-volume and winding-length relations, the conductor-strain operand's honest meaning, and tolerances — under the spec's requirements.
- **Why now:** the spec is complete; design is the native next stage and the place the conductor operand's meaning gets fixed rather than assumed.
- **Scope:** authorized — `work/active/WI-036_winding-pack-sizing/design.md`, source and image verification reads; excluded — plan/implement stages, any edit under `models/` or `exploration/`, source ingestion, discovery-log writes.
- **Inputs:** the WI-036 spec; `goal.md`; `evidence/T-001_research_return.md`, `evidence/T-002_criterion_return.md`.
- **Done when:** design.md exists, requirements-complete against MR-WI036-1..11, every quantitative basis image-verified, and the conductor operand's meaning stated honestly enough that a reviewer can judge it.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-004 start — 2026-09-03

Task T-004: write the WI-036 design. Native target: `work/active/WI-036_winding-pack-sizing/design.md`.

### T-004 return — 2026-09-03

- **Outcome:** `COMPLETE`. The design exists and answers every requirement; all five derived constants reproduce their printed anchors exactly or within the stated tolerance.
- **Evidence:** `work/active/WI-036_winding-pack-sizing/design.md` — eight decisions D1–D8 with a tolerance table and a decision record of what the item deliberately does not do. Derived constants: `j_WP` = 118.8271604938 A/mm² (printed 119; reproduces the printed 15.4 MA / 360 mm pair exactly); `f_wp_vol` = 0.8780864197530865 (reproduces the printed 136.56 m³ exactly from the six printed sides); `k_coil` = 1.9685039370078741 (reproduces the printed 25 m at R = 12.7); `f_cond` = 0.6666666667; `E_wp` = 200 GPa; `eps_cond_allow` = 0.004.
- **Reading.** Three things worth carrying to the study.
  1. **Stress now grows as √I rather than linearly.** Substituting the sizing relation into the existing stress form gives σ = k_sigma·B_peak·√(I_coil·j_WP). That is the relief the sizing chain buys, and it falls out of two sourced relations rather than a new assumption.
  2. **The conductor check does not bind first, and the model now shows that rather than assuming it.** With load sharing calibrated from the source's own reported pair (~600 MPa pack von Mises against <0.2% stack strain), the operand reads 0.217% at the design point and 0.267% at the 800 MPa stress fence; it would bind at σ_wp = 1200 MPa. The structure governs — which is what a sound magnet design should show. The margin is not generous: the default sits at 0.4% against SuperOx's 0.45% irreversible limit, and a designer choosing the 0.2% that other projects enforce would find the conductor binding first. Hence settable.
  3. **A wider pack buys no conductor, and that is correct.** Ampere-metres are set by current and length, so the existing cross-section-blind winding-pack cost is right and is not changed. The cost consequence lands through the cryo chain, which was already live and waiting for an input.
- **Decision:** trigger — the model carries one winding pack while the machine has six unique coil sizes, and MR-WI036-2 needs a total cold volume. Decision and reason — use a held **set-distribution factor** (`f_wp_vol`, the printed-total to worst-coil-uniform ratio) rather than introduce per-coil geometry. Per-coil representation is a much larger change with no cost consequence of its own, and the factor is the same shape as the existing `f_set` coil-current distribution fact. It reproduces the held 136.56 m³ exactly and moves the six-cross-section arithmetic out of a doc comment into model content. Tier — execution detail under the standing delegation. Who decided — the round agent, 2026-09-03. What changed — design D2.
- **Decision:** trigger — MR-WI036-4 asks winding length to follow coil geometry, but per-coil circumferences are unprinted and no printed relation yields the 25 m typical value. Decision and reason — make length respond to machine **scale** through a held **shape** factor (`c_coil = k_coil · R`), which is exactly the trade the WI-036 mint record predicted ("one held constant traded for a smaller one") and the honest limit of what the source supports. The residual held quantity is named in the design rather than buried. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — design D3.
- **Decision:** trigger — the conductor strain operand cannot be derived rigorously without a finite-element result the source does not publish. Decision and reason — compose it as `f_cond · σ_wp / E_wp` with `f_cond` **calibrated from the source's own reported pair** rather than assumed, note that both anchors are printed bounds taken as values (so the operand over-estimates strain), and report the design-point value rather than tune it. This is the `k_sigma` convention applied to a second quantity. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — design D4, risk 1.

**Next task:** the WI-036 plan and implementation, through the native modelling PM with its validation levels.

### T-005 scope

- **Objective:** implement WI-036 through the native modelling PM — plan, model edits, regeneration, and the validation battery — with the MR-WI036-11 restatement recorded before regeneration.
- **Why now:** spec and design are complete; implement is the native next stage.
- **Scope:** authorized — `work/active/WI-036_winding-pack-sizing/plan.md`, edits under `models/library/` and `models/designs/`, the staged model twin at `exploration/stellarator_e2e/models/`, snapshot recapture and package regeneration, the oracle mirror and study-route constants, and **the tests/models and tests/study fixture surface** (named here in advance per the round-2 review's carried constraint 6: runner anchors, known-answer fixtures, the census, suite constants, the manifest, the ANNEX); excluded — the integrate seam, any study execution, discovery-log writes, any change to `sigma_allow`.
- **Inputs:** the WI-036 spec and design; `goal.md`.
- **Done when:** the battery is green and every changed expectation is re-derived from live evidence with its own explanation.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-005 start — 2026-09-03

Task T-005: implement WI-036. Native targets: `models/`, the staged twin, `exploration/stellarator_e2e/generated`, and the named fixture surface. Expected artifacts: plan with implementation record, regenerated package, green battery.

### T-005 return — 2026-09-03

- **Outcome:** `COMPLETE`. Battery green: `tests/models` **48 passed / 13 skipped** (the WI-037 baseline count), `tests/study` **353 passed / 1 skipped, zero failures**.
- **Evidence:** `work/active/WI-036_winding-pack-sizing/plan.md` § Implementation record and § MR-WI036-11 restatement; commits `4f3dd511`, `7f0e41e7`, and the battery-green commit.
- **Reading.** Four results.
  1. **The design point reproduces all nine anchors exactly** — LCOE 307.087120, total capital 14,542,872,713.455379, and the rest — and oracle bit-exactness holds at reldev 0.00e+00 on every channel. The sizing chain is neutral at the design point by construction (`wp_side` 0.360000 m, `c_coil` 25.0 m, cold volume 136.56 m³), so nothing moved that should not have.
  2. **`cond_strain_ok` evaluates satisfied and is not inert.** It reads 0.217% against its 0.4% limit at the design point, and it is **reachable from both field levers** (`I_coil`, `R+tie`) in the live indicator report — so the conductor is checked by the same sweeps that check the structure. That matters because this model already carries one constraint, `tbr_ok`, that is held-vs-held and reaches nothing from any axis.
  3. **A disclosed WI-035 limitation is closed.** The known-answer suite carried the comment "the decomposed rollup (magnet_capital) does NOT respond to R: the winding length is the held `c_coil` … a disclosed WI-035 limitation." With `c_coil = k_coil · R0`, `magnet_capital` moved into the tie-exclusive reachable set. The suite caught it; it is not an author's claim.
  4. **`vol_cold_cryo` survives as a settable entry point** in the regenerated census — the standing `[OWNER 2026-08-27]` WI-032 ruling holds. The plan's check was written to catch the opposite outcome and report it as a genuine conflict.
- **Decision:** trigger — the pinned codegen refused an expression in a calc input binding (`SI_EXPRESSION_SOURCE_UNSUPPORTED`). Decision and reason — move the additive cold-volume arithmetic **inside** the `Winding Pack Cold Volume` calc as a `vol_extra` input. Not a workaround: a calc is the right home for arithmetic, and the design reads better for it. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — `mfe_magnet_field.sysml`, `mfe_plant.sysml`; plan § Implementation record.
- **Decision:** trigger — the independent oracle (`verify_stellaris.py`) still took `wp_side` and `c_coil` as inputs and knew nothing of the conductor operand. Decision and reason — **update the mirror to compute the same chain independently**, rather than feed it the package's answers. Bit-exact parity then holds at reldev 0.00e+00 across all nine channels, which is evidence the two derivations agree rather than evidence they were wired together. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — `verify_stellaris.py`, `oracle_entry.py`.
- **Decision:** trigger — the fixture surface came in at roughly the size L-006 predicted. Decision and reason — every changed expectation is **re-derived from live evidence, never patched to match**: the census and the six known-answer expectation files by re-running the suites' own generation helpers against the regenerated package; the manifest re-pinned from `indicators.py --print-fingerprint`; counts and line numbers moved with their reason written at the site. Tier — execution detail. Who decided — the round agent, 2026-09-03. What changed — the surface named in § T-005 scope.

### T-006 scope

- **Objective:** obtain one study-ready `CANDIDATE` pin for the WI-036 package through the native integrate seam.
- **Why now:** the item is implemented and the battery is green; a committed study needs a verified pin.
- **Scope:** authorized — `scripts/integrate.py` and its out-dir, evidence deposit in the goal directory, and **the tests/models and tests/study fixture surface the seam's own gates reach** (named per carried constraint 6); excluded — any model edit, any study execution, discovery-log writes.
- **Inputs:** `goal.md`; the WI-036 item at its committed sha.
- **Done when:** the seam returns `CANDIDATE` with a pin, or a named `BLOCKER`.
- **Stop when:** a `BLOCKER` naming the seam, a strategy blocker, or a declared limit.

### T-006 start — 2026-09-03

Task T-006: run the integration seam against the WI-036 package. Native target: `scripts/integrate.py`. Expected artifact: an integration return carrying a `CANDIDATE` pin.

### T-006 return — 2026-09-03

- **Outcome:** `COMPLETE`. The seam returns **`CANDIDATE`** with all ten gates `pass`.
- **Evidence:** `evidence/T-006_integration_return.json`, `evidence/T-006_verification_summary.json`. Pin **`6262dbf42c709600dc03736d8c54058c9a08ec4369ccd0327a26474cd34be784`**; semantic fingerprint `3cb690aab05e…`, executable fingerprint `cc64dc5a4d15…`, teax revision `744745f8…`. Gate detail: regeneration rewrote no byte outside `handwritten/`; 70 handwritten files byte-identical; the snapshot recaptures byte-identically and **197 entry points re-derive to the census as bound**; preflight six of six; verification — oracle parity holds and every verdict re-derived.
- **Reading:** there is one study-ready candidate at a verified pin, and the census gate independently confirms the entry-point change this item intended (193 → 197, with `wp_side` and `c_coil` retired). The seam's own standing disclosure — `assert_read_set_covered` is out of reach at this gate and covered by nothing else — is carried forward unchanged; it is a known seam limitation, not a finding of this round.
- **Decision:** trigger — none at goal level; the seam ran its documented procedure and returned a candidate. Tier — execution detail. Who decided — n/a. What changed — `evidence/T-006_*`.

**Next task:** the committed study — with the field lever priced, does a feasible operating point exist at the printed 50 MW installed heating, and what does it cost.

### T-007 scope

**Recorded 2026-09-03 after execution, by the resuming session — not by the session that ran the task.** The round-1 agent executed and committed the study (`aa638133` critique, `76876b82` record) without writing this scope or a start line; the record names itself "task T-007" (record § 1). This entry reconstructs the scope from the T-006 return's named next task, the strategy's move (3), and the owner's in-session directive quoted verbatim at record § 2. It is written so the fresh reviewer has something to check the work against, and it is marked so the reviewer does not mistake it for a scope written before the work. **The reviewer should weigh T-007's scope adherence against record § 2's intake as much as against this entry.** No prior entry is edited.

- **Objective:** execute the round's one committed study against the T-006 candidate pin — with the winding-pack sizing lever priced (WI-036), does a feasible operating point exist at the printed 50 MW installed heating, what does it cost in LCOE, and which fence binds at the constrained optimum. Three arms per the owner's directive: p = 50, p = 110, and a `j_wp` transect.
- **Why now:** the pin exists and is proven (T-006 `CANDIDATE`); a valid study reading is the round's closing trigger; no abandonment condition was triggered; the owner directed the study in session ("run it", record § 2).
- **Scope:** authorized — the record directory `exploration/stellarator_e2e/studies/20260903-priced-levers/` per the run-study skill (execute mode), the pre-execution framing critique as a fresh session with its prompt deposited, first-sighting discovery-log rows (runbook step 14), the study commits, and the study-layer surface the runbook's steps may reach (manifest ties, `oracle_entry.py` map, ANNEX — named per carried constraint 6; **none was touched**, per the two commits' file lists). Excluded — any edit under `models/`, WI-038 work, a second pin or study, disposition rows for prior findings (the round result owes those).
- **Inputs:** `evidence/T-006_integration_return.json` (pin `6262dbf4…`); `goal.md` § Invariants (comparisons against this pin; SV-016 untouched; clean room; no defaults); `.claude/skills/run-study/runbook.md` + `modeling_project/STUDY_POLICY.md`.
- **Done when:** a committed study record with its § 15 findings register and first-sighting log rows — an adverse or disappointing reading still closes the round.
- **Stop when:** an axis traces `no_constraint_response` needing a ruling the delegation does not cover, a mechanical failure past the retry cap, or a strategy blocker.

### T-007 start — 2026-09-03

**Recorded after the fact (see § T-007 scope).** Task T-007: execute study `20260903-priced-levers` through the run-study runbook against pin `6262dbf4…`. Native targets: the record directory, DISCOVERY_LOG first-sighting rows, the study commits. Expected artifact: the committed record. The native side effects preceded this line: critique and pre-execution artifacts at `aa638133`, the committed record at `76876b82`.

### T-007 return — 2026-09-03

Written by the resuming session, not by the session that ran the task (see § T-007 scope).

- **Outcome:** `COMPLETE`. The round's one committed study exists and its record is closed: `exploration/stellarator_e2e/studies/20260903-priced-levers/` at `76876b82` (execution, record, five first-sighting log rows) and `30b651f0` (fresh administrator's `synthesis.md` + executor Addendum). `tests/study/test_records.py` 19 passed after the Addendum — 2 of 3 record-contract checks failed before it (decision 4).
- **Evidence:** the record directory — `record.md` §§ 3–17 and Addendum, `snapshot.json` (digest `59049340…`; was `838ec6a9…` at `76876b82`), `results/`; `synthesis.md` (the administrator's independent recount agrees with every violation count, the feasible counts, the 27-alone / 6-alone split, the optimum, the 14.63 keV slice and every transect delta; it caught five prose misstatements, each re-derived by this session before the Addendum was written); `evidence/T-007_precritique.md` + `T-007_precritique_prompt.md` (MAJOR — 3 major, 5 minor, all eight accepted before any point ran); `evidence/T-007_administrator_prompt.md`; `evidence/T-007_proposed_dispositions.md` (seven dispositions, awaiting C-001); `DISCOVERY_LOG.md` rows `20260903-priced-levers#1..#5` at `76876b82`.
- **Reading.** The strategy's intended question is answered, and the answer is a **bounded negative on the field escape for a reason the strategy did not predict.**
  1. **At the printed 50 MW there is no feasible point (0 / 240), and the fence that stops the machine once it can sustain itself is the wall, not the conductor.** 27 points are blocked by `wall_load_ok` alone against 6 by the ceiling alone; `wall_load_ok` is violated at 264 of 439 points, the dominant fence. The strategy's premise — every sustainment-satisfying point is blocked by the ceiling and the stress fence together (`goal.md` § Amendment 2026-09-02, `evidence/R1_deadlock_recount.md`) — was an artifact of the 14.63 keV temperature both the recount and the predecessor's grid arms held. With temperature swept, sustaining points exist under the ceiling (twelve of the 27 need no auxiliary heating at all) and the wall catches every one of them. Record § 15 #1, Addendum A.3; `synthesis.md` § 2.3.
  2. **The winding-pack lever WI-036 built is real physics and almost no economics.** Magnet capital is unchanged to the dollar over a 2.33× swing in pack cross-section; only the cryoplant moves; LCOE moves 0.100%. The goal's own defect — an unpriced lever — reappeared in the lever built to fix it. Design D8 disclosed the cause before the study; the study measured the consequence. Record § 15 #2.
  3. **The second check WI-036 added is live, reachable, and inert at its 0.4% limit** (study-wide maximum 0.286%, Addendum A.1); at the 0.2% other projects enforce it would bind at the pinned baseline itself. Record § 15 #3.
  4. **Temperature is worth 16.645 $/MWh at the feasible optimum**, and the 110 MW optimum (271.359) is window-edge in T and in `j_wp` and 0.007 MW/m² under the wall fence — window-bounded, not fence-bounded. Record § 15 #5, Addendum A.5.
  5. **The pre-registered headline held; the pre-registered mechanism was wrong.** The strategy evidence pre-registered a negative on the field escape and named the conductor ceiling as the last fence standing. The negative held; the fence is the wall. The fresh pre-execution critique caught the held-axis error before any point ran (MAJOR 2), which is exactly the case pre-registration and a fresh critic exist for: a confirming headline with a wrong mechanism.
  The strategy's move (2), WI-038, was not executed this round — the one-pin bound, and T-001 had already narrowed it to a labelled extrapolation — and the study declined `B_max` as a traced pure fence-relaxer (§ 8). The strategy's abandonment condition ("no admissible field-ceiling basis **and** no structural relief that opens feasibility at p = 50") is met on its second half: with `B_max` held, relieving stress opens nothing at 50 MW, because the ceiling still catches every point at 16 MA and above and the wall catches the sustaining points below it. The route the condition names — "routes the goal to its heating half" — is where the owner's same-day direction for a new goal points.
- **Decision:** trigger — the first study design held `T_i0` at 14.63 keV and pre-registered the conductor ceiling as the last fence; the fresh critique returned MAJOR 2. Decision and reason — `T_i0` became a declared search axis, the scan was re-run over all four axes, the windows re-fixed, the density window widened until `beta_ok` was testable, the p50 arm re-scoped from search to fence anatomy, arms tagged at construction, current refined to 0.2 MA, § 12 boundary treatment added — all eight critique dispositions accepted. Tier — execution detail. Who decided — the round-1 agent, 2026-09-03 (recorded here by the resuming session from record §§ 2/5/11/14 and `evidence/T-007_precritique.md`). What changed — `axes.json`, `scan.py`, `study.py`, `results/window_scan.json` between `aa638133` and `76876b82`.
- **Decision:** trigger — `B_max`, `sigma_allow` and `eps_cond_allow` each reach one constraint and zero objectives (record § 8). Decision and reason — declined as swept axes: each is a pure fence-relaxer as bound, and sweeping one would manufacture feasible points that cost nothing — the unpriced-lever defect this goal exists to remove; recorded as traced declined axes with MD-1..3 alongside. Tier — execution detail under the standing delegation. Who decided — the round-1 agent. What changed — record § 7/§ 8.
- **Decision:** trigger — the first execution's `aux_cooling__cryo_cost` store column came back blank across all 439 rows, silently (record § 13, § 15 #4). Decision and reason — the column moved to the oracle-side export and the study was re-executed before commit; no blank left, no value invented. **Classification, for the reviewer:** an in-task correction inside the study runbook's own steps, not a goal-level retry — the task never returned, and what changed was a harness declaration, not a model input or the study's meaning. The runbook's retry definition (identical task, inputs, scope and meaning after fixing the machinery) would also describe it; it is recorded here either way so it is not hidden. Tier — execution detail. Who decided — the round-1 agent. What changed — `study.py` export, `results/oracle_operands.csv`.
- **Decision:** trigger — at resume, the committed record failed two of three record-contract checks (a literal less-than sign in § 13; `arms[].effective_executable_fingerprint` absent from `snapshot.json`), and the fresh administrator's recount found five prose statements wrong (maximum strain, the stress-fence location, the 27 points' span, the relief cost, the p110 temperature edge). Decision and reason — fix under T-007 through the study runbook's own correction path: an executor Addendum for the prose, the identity field added to `snapshot.json` with the sealed fingerprint the snapshot already carries, § 16 updated naming the old digest. **The snapshot edit touches a file step 15 says is never edited.** It is disclosed in Addendum B.2 with the digest change (`838ec6a9…` → `59049340…`) and the exact diff (eighteen added lines, nothing removed), and put to the round review rather than resolved silently (capture-fidelity law 4). Reason for doing it at all: the record's own closure contract failed closed, the snapshot was incomplete rather than wrong, and leaving it red would have left every future study battery red on this record. Tier — execution detail. Who decided — the resuming session, 2026-09-03. What changed — `record.md` § 13, § 16, Addendum; `snapshot.json`; `synthesis.md` added; commit `30b651f0`.
- **Decision:** trigger — T-007 was executed and committed with no scope or start line in this trail. Decision and reason — reconstruct both after the fact, marked as such, rather than write a return with nothing to check it against; recorded as a process finding for the round review and proposed as a learning. Tier — execution detail. Who decided — the resuming session. What changed — § T-007 scope, § T-007 start above.
- **Decision:** trigger — the administrator named two readings the record does not carry (the p110 band's upper bound by `peak_field_ok` is an inference from B_peak's dependence on I_coil alone; 271.359 is window-bounded in T) and two contract gaps (`verification_summary.json` lists nothing under `not_independently_verified` while § 13 names three oracle-side-only classes; `teax.revision` unrecorded). Decision and reason — accepted into the Addendum as corrections and disclosures; no finding id minted, because a goal round may not; the verification-artifact gap is filed beside `20260903-priced-levers#4` as study-tooling contract debt. Tier — execution detail. Who decided — the resuming session. What changed — Addendum A.5, C.

**Next:** the pre-execution disposition checkpoint C-001 over `evidence/T-007_proposed_dispositions.md` (spawn prompt deposited at `evidence/round1_C-001_checkpoint_prompt.md`). No further task in round 1: the reading is valid and the round closes on it once the checkpoint passes and the dispositions land.

### Checkpoint C-001.r1 — 2026-09-03

- **Reviewer:** fresh non-author session, spawned from the deposited prompt (`evidence/round1_C-001_checkpoint_prompt.md`); not the executor, not the administrator, not the resuming session.
- **Reading reviewed:** `20260903-priced-levers/synthesis.md` (the administrator's reading of record) + record § 4/§ 6/§ 8/§ 12/§ 15/§ 17 and the Addendum.
- **Dispositions reviewed:** `evidence/T-007_proposed_dispositions.md` (r1: seven rows).
- **Verdict:** `REVISE`.
- **Revision:** 1 of 2. The reviewer independently recounted the crux numbers (264 / 144 / 27 / 6 / 12 sustainment-alone / 0 · 87 · 7 feasible; the 27's span and the 12 negative-heating points) and upheld every count; rows 2, 3 and 4 pass as written; the (c) question is settled — "unrouted" is the absence of class, status, actor and next reference, not the absence of a minted item, per the C-001.r2 precedent. Four bounded changes required: **row 1's statement that wall load does not depend on `R` is false at this pin** — since WI-037 the fuel densities come through ISS04 (`R^0.64`, `B ∝ I_coil/R`), so `R` reaches `p_fus`; the study's own data shows wall load 3.17 → 2.68 MW/m² across `I_coil` 15 → 18 MA at fixed T, n; the p-pump-fence constancy was measured at a pre-WI-037 pin — withdrawn, and "at the held geometry" added to the re-location claim; row 5 re-classed `declared seam` on the `20260829-p-pump-fence#2` precedent with runbook step 7 as a home; row 6's "bounded above by `peak_field_ok`" labelled an inference (0 of 192 violate at 110 MW); row 7's heating span replaced by the Addendum's (−47.8 to +49.3 MW, negative at 12 of 27) and "decides feasibility" tightened to what § 6 licenses. The reviewer also required the same two corrections in the draft `wall-and-heating/goal.md`, which had already absorbed the withdrawn `R` sentence as an invariant. Author revised as § Revision r2 of the same document and corrected the draft goal; resubmitted to the same session.

### Checkpoint C-001.r2 — 2026-09-03

- **Reviewer:** the same fresh checkpoint session (no execution context), re-reviewing the resubmission.
- **Reading reviewed:** `20260903-priced-levers/synthesis.md` + record § 15/Addendum (unchanged).
- **Dispositions reviewed:** `evidence/T-007_proposed_dispositions.md` including § Revision r2; the corrected draft `wall-and-heating/goal.md`; the § Checkpoint C-001.r1 entry above.
- **Verdict:** `PASS`.
- **Revision:** 1 of 2 used. What the author changed: row 1 withdrew the `R`-cancellation claim and the "geometry lever is `a`" inference, stated the ISS04 route by which `R` reaches `p_fus` since WI-037, cited the measured `I_coil` response, dated the p-pump-fence constancy to its pre-WI-037 pin, and qualified the re-location claim "at the held geometry"; row 5 re-classed `declared seam` with runbook step 7 as a home; row 6 labelled the `peak_field_ok` upper bound an inference; row 7 adopted the Addendum's heating span and the § 6-licensed wording. The reviewer re-read `mfe_plasma_sustainment.sysml:26-30` and confirmed the withdrawn phrase survives nowhere in the draft goal. One non-blocking note — the draft's p-pump-fence citation should say the `R`-constancy was at that pin — applied by the author before the rows were appended. All seven per-disposition rulings PASS; the rows may be appended and the round result written.

## Round 1 result — 2026-09-03

Written by the resuming session (the round-1 agent's session ended after committing the study; its handoff is deposited at `evidence/round1_handoff_20260903.md`).

- **Intent:** **unmet as declared, and the evidence says why.** The strategy set out to make the field lever real on both fences (WI-036 + WI-038) and find whether a feasible point exists at the printed 50 MW. The lever was priced on one fence (WI-036: sizing chain, cold volume, a second conductor check), pinned, and studied; the study shows **no feasible point at 50 MW**, that the binding fence there is the **neutron wall load and not the conductor**, and that the pack-sizing lever moves physics but almost no cost. That bounded negative — with the mechanism corrected by the fresh critique — is the result.
- **Task sequence:** T-001 (research: field-ceiling / cost / allowable basis) `COMPLETE` · T-002 (research: the governing structural criterion) `COMPLETE` · T-003 (WI-036 spec) `COMPLETE` · T-004 (WI-036 design) `COMPLETE` · T-005 (WI-036 implement, battery green) `COMPLETE` · T-006 (integrate → `CANDIDATE` pin `6262dbf4…`) `COMPLETE` · T-007 (committed study `20260903-priced-levers`; scope and start recorded after execution by the resuming session) `COMPLETE` · C-001.r1 `REVISE` → C-001.r2 `PASS`.
- **Last semantic outcome:** `COMPLETE` (T-007), with a valid study reading behind it (record + fresh administrator's synthesis + executor Addendum) and its dispositions checkpointed.
- **Stop reason:** last outcome `COMPLETE` + a valid study reading in hand + no declared limit reached → round closes on **trigger 1 (a valid study reading)**. Noted, not the stop reason: the strategy's own abandonment condition is met on its second half (structural relief opens nothing at 50 MW with `B_max` held), and the condition's named route — "routes the goal to its heating half" — coincides with the owner's same-day direction for a new goal.
- **Evidence refs:** `work/active/WI-036_winding-pack-sizing/` (spec, design D1–D8, plan with implementation record and MR-WI036-11 restatement) at `4f3dd511`; `evidence/T-001_research_return.md`, `evidence/T-002_criterion_return.md` (seven sources registered under `knowledge/sources/`, two queued); `evidence/T-006_integration_return.json` (pin `6262dbf42c70…`, ten gates); `exploration/stellarator_e2e/studies/20260903-priced-levers/` at `76876b82` (record, first sightings) and `30b651f0` (`synthesis.md`, Addendum); `evidence/T-007_precritique.md` + prompt, `evidence/T-007_administrator_prompt.md`, `evidence/T-007_proposed_dispositions.md`, `evidence/round1_C-001_checkpoint_prompt.md`; battery at result: `tests/models` 48 passed / 13 skipped; `tests/study` 356 passed / 1 skipped, zero failures (full run after the record Addendum; before it, `test_records.py` failed 2 of 3 checks on this record).
- **Learning delta (proposed):**
  - **L-001 — At the printed 50 MW the deadlock is sustainment against neutron wall load, not the conductor ceiling.** 27 of 240 p50 points are blocked by `wall_load_ok` alone against 6 by the ceiling alone; `wall_load_ok` 264 / 439. Scope: this machine at R 12.7 / a 1.3 (geometry not swept), T 14.63–19 keV, n 0.8–1.4×, `B_max` held. Implication: the escape at 50 MW is geometry (both `R` and `a` reach the wall fence through confinement as well as area since WI-037; `R`'s sign and size are unmeasured at this pin), lower power density, or a heating system with structure — not conductor grade; WI-038 is not the lead. Refines predecessor L-005.
  - **L-002 — A lever is "priced" only if it reaches a cost objective, and that is checkable before the study.** `j_wp` reached no magnet capital in the indicator run and the study framing called it priced anyway; magnet capital delta exactly zero over 2.33×; ~85% of the pack's mass has no cost home. Implication: an increment that claims to price a lever states, before its study, which objectives the lever reaches and which accounts exist for the mass it moves.
  - **L-003 — A held axis inherited from a prior study can decide a fence conclusion.** Holding T at 14.63 keV produced the "ceiling is the last fence" pre-registration; sweeping T overturned it; T is worth 16.645 $/MWh at the feasible optimum, which sits on the window edge. Implication: every fence study on this package sweeps T with a window wide enough that the optimum is interior or a fence catches it, and checks inherited holds for load-bearing before pre-registering. The fresh pre-execution critique is what caught it.
  - **L-004 — On the field axis the conductor is cheap and the structure is expensive, and the model's single stress check was the wrong shape.** 24.9 → 30 T costs ×1.12 in tape and ×1.45 in Lorentz stress (T-001); the steel allowable has categories and 800 MPa reads as a conservative *peak* value (T-002, correcting T-001); the conductor's own strain limit re-tightens at nearly the same place; transverse limits the model cannot see are far tighter. Implication: `sigma_allow` is never relaxed to open a region; the two-check form is now built (WI-036) and inert at 0.4%.
  - **L-005 — The hold-out guard cannot protect a session from what a research subagent already read.** The registry guard fires at registration; the subagent's fetch precedes it (T-002 § 6, `arXiv:2409.01925`). Implication: research subagent instructions carry the clean-room screen before any fetch; a refusal is recorded durably and surfaced to the owner, never absorbed.
  - **L-006 (process) — The study step is where goal-layer and study-layer obligations pile up, and both were skipped.** T-007 ran with no scope or start line, and the record's step-15 fail-closed check was not run before commit (two record-contract failures; five prose misstatements caught only by the fresh administrator's recount). Implication: append the scope and start before invoking `/run-study`; run `tests/study/test_records.py` before the study commit; the administrator's recount is load-bearing, not ceremony.
- **Finding dispositions:** all seven checkpointed dispositions (C-001.r2 PASS) landed as class-bearing rows appended to `DISCOVERY_LOG.md` 2026-09-03 — `20260903-priced-levers#1` `model fix` → the `wall-and-heating` goal (draft; grounding owner-held); `#2` `model fix`, not minted, the owner chooses between a WI-036 follow-on and the new goal; `#3` `research`, queued (Pierro 2019 operator fetch; no model change); `#4` `declared seam`, closed for this goal with a named hardening candidate; `#5` `declared seam`, carried as a study-design constraint (runbook step 7; new goal invariant); `20260901-sustainment-fence#1` `model fix`, premise re-measured at this pin, WI-038 stays minted and is not the lead; `20260901-sustainment-fence#4` `model fix`, WI-039 becomes the new goal's heating half with the redirect of `priced-levers` (a) surfaced to the owner. No touched row returns unrouted.
