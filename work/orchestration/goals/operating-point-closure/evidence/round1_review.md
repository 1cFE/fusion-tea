# Round 1 review — full text (fresh non-author session, 2026-09-01)

Deposited verbatim by the round agent from the reviewer session's final return. Spawn prompt: `evidence/round1_review_prompt.md`. The trail's § Round 1 review entry summarizes and cites this file; this file is the review of record.

---

## 1. Verdict

**Verdict: FINDINGS** — with corrections stated below; none of them undermines the round's substance. The `STRATEGY_BLOCKER` classification is genuine: I re-ran the deposited scripts and the round's own declared abandonment condition fires on the reproduced evidence (no stable feasible burn inside the model's own limits at the baseline machine; the printed point on the unstable branch; ~×3 error amplification into solved T). The physics chain validates as claimed — I verified every equation and held value against the page images and the iter-02 raw PDF, and the cross-check table matches the deposited outputs to the digit. The findings are: (a) discovery row `20260823-magnet-technology-ab#4` was touched and returned `unrouted`, which the written rule (`GOAL_RUNBOOK.md` § The discovery log, ADR-0004) does not permit; (b) NOTES.md finding 1's "≈140 MW sustained coupled heating" is unsupported by the deposited evidence (the correct figure is ≈90 MW); (c) the deposited `op_landscape_output.txt` is a crash traceback, not results. All three are correctable without reopening the round.

## 2. The checks

**Refs resolve and say what the trail claims.** Opened every cited artifact. `work/active/WI-037_operating-point-closure/spec.md` — exists, seven outcome-level requirements MR-WI037-1..7 as the T-001 return describes, § Open decisions carries the two delegation rulings. `work/BACKLOG.md:127,227` — WI-037 row, standard/P0, under the MFE epic, status `backlog`. `modeling_project/VALIDATION_MATRIX.md:67-69` — SV-041/042/043 present, all `pending`. `.project/active/demo-depth-rubric/rubric.md` Row 1 (`:73-83`) at sha `dc0f0b6d` (confirmed via `git log`) — P3 anchor text as the goal quotes it; the stale "Rung C" gate note is there and the concept correction at `81a4fee8` is the recorded authority, as goal.md says. `grading.md:37-45` — R1.P = 2 with `why_not_next` naming exactly this gap. Discovery log rows for `#4` at `:22,51,55,58` — newest row (2026-09-01, landed in pre-round commit `81a4fee8`) reads `unrouted`, awaiting a goal at rubric Row 1. One minor native inconsistency: the WI-037 spec frontmatter says `Status: active` while the BACKLOG row says `backlog` — a dashboard-detectable mismatch, not a trail falsehood (the trail calls it "backlog-stage work").

**Goal and strategy fidelity.** The round pursued the declared strategy: T-001 fixed the increment's contract at spec level; T-002 prototyped moves 1–2 before writing design, which is what surfaced the blocker. No drift.

**Task scopes.** Commit `2df2c548` (the round's only substantive commit) touched exactly: VALIDATION_MATRIX, BACKLOG, the WI-037 spec, and the goal directory. Nothing under `models/`, `exploration/`, or `knowledge/`. T-001 stayed inside its scope; T-002's prototypes live in the evidence directory (authorized "throwaway prototypes outside `models/`"), no source ingestion, no discovery-log writes. `design.md` was not produced — correct under a `STRATEGY_BLOCKER` return.

**Retries.** Confirmed none: each of T-001/T-002 has exactly one start and one return; no retry entries exist.

**Discovery row #4 — the handling is not correct.** The trail's round result says "No disposition row is appended... the row stays open; the next round owes the disposition when the increment lands." ADR-0004 and the runbook say the opposite: *every open row the round's evidence touched gets a disposition — its status, the responsible task or owner, and the concrete next reference — and no touched row returns as `unrouted`*. The round's evidence touched #4 in both tasks (goal.md names it a consumer; the spec's traceability routes it). A disposition is a routing record, not a fix record — the log's own prior rows for this id demonstrate exactly that pattern ("model fix — blocked on...", status rows). WI-037 exists and is the concrete next reference; the honest row (`model fix` — routed → WI-037 under goal `operating-point-closure`, increment not yet landed, next reference the round-2 strategy) was appendable at close and was owed. **Correction:** round 2 appends that disposition row as an early act. The round agent's reasoning ("the finding has not moved") conflates *disposition* with *resolution*; the rule exists precisely so a finding in flight is never re-read as orphaned.

**STRATEGY_BLOCKER classification (verify a).** The declared abandonment condition was "the solve cannot be made convergent and verifiable across the study domain." I re-ran `op_solve_final.py` — output reproduces `op_solve_final_output.txt` byte-for-substance: at B ≤ 9 T no density in the scanned range yields a stable feasible burn (the one burn point, n×1.15, violates the wall limit 7.6 > 4.05); point-A levers give "no burn" at 0 and 50 MW. I additionally computed the required-aux curve with the deposited `state` function: it never reaches zero at point-A density (minimum ≈72.6 MW near T=18), and required aux falls with T through 14.63 keV (99.5 → 84.8 MW across 14→15 keV), confirming dg/dT > 0 — the unstable branch. A solved-T model returns no value at the baseline and over large study voids; that is the condition firing, on the evidence, not by assertion. The classification (premise refuted → close on trigger 2) is right, and recording the successor architecture as next-strategy input rather than re-architecting mid-round is exactly what the runbook asks.

**Spec/SV-042 tension (verify b).** Confirmed not silently resolved anywhere: `spec.md` MR-WI037-2 and VALIDATION_MATRIX SV-042 still encode the refuted solved-T form, both `pending`; the trail says the spec "stands as written pending the next round's strategy"; learnings.md is empty. Carried as a constraint below — and note the tension is wider than the prompt states: MR-WI037-3's "through the solved temperature" and SV-043's "beta_ok via the solved temperature; field reaches fusion power... through the confinement channel" also encode solved-T and need the same amendment.

**Cross-check table (verify c).** Every row matches: τ_E 1.458 vs printed 1.46 (−0.1%), p_rad 228.7 = 98.4+116.0+14.3 vs 228.9 (−0.1%; the printed figure is Table 5's 0.70 MW/m² × 327 m²), n_He0 5.80e19 vs 5.6e19 (+3.6%), p_fus 2720 vs 2700 (+0.7%), W 551.3 vs 504.65 (+9.2%), vol-av τ_E 1.120 (−23%). Caveat: the W and vol-av rows come from `op_solve_proto4.py`, whose output file was never deposited — I re-ran it and reproduced both numbers (and the ≈15.8 keV ignition-window figure, which appears in no deposited output: it is proto4's brems+line variant, up-crossing 15.77 keV at 50 MW).

**Source verification.** Eq. A.7 image-verified (`page_031_eq_6.png` — the off-by-one link placement NOTES reports is real; `page_031_eq_5.png` is A.6), A.8 (`page_031_eq_7.png`, B^2.15 confirming the loss-scaling claim), A.2/A.3 (`page_031_eq_1/2.png`), A.5/A.6 (`page_031_eq_4/5.png`). Table 4 (f_ren 1.0, f_α 0.95, τ*/τ_E 8.0, f_suppr 0.5), Table 5 (all point-A values, including a=1.3/R=12.74/V=425 — the scripts' values), Table 2 ("Required plasma-coupled ECRH power 50" — NOTES's coupled-vs-wallplug citation nuance at `stellarator_plant.sysml:553` is real and correct). Raw PDF Table 3 confirms axis ι 0.86 / edge 0.98 — and shows the **iter-02 `output.md` extraction of Table 3 is itself garbled** (it prints axis 0.98 / edge 0.80, at "a = 1 m" vs the raw PDF's 1.3 m). NOTES read the raw PDF and is right; the extraction is wrong. Fig. 11(a) at raw PDF p.7: the ⟨β⟩_V ≈ 3% curve at s = 2/3 reads ≈0.92 — the ι_{2/3} = 0.92 ± 0.01 digitization is credible, and treating it as an image-verification act (WI-022 Fig-16 precedent), not a default, is the right classification. Model-side spot checks: B_max 24.9 / peak-ratio 2.767 (`stellarator_plant.sysml:157-165`), σ_allow 800 MPa (`:205`), baseline σ 650 MPa at wp_side 0.36 (`20260830-stress-fence/synthesis.md:26`) → 650×(10/9)² = 802 MPa, confirming NOTES finding 5's stress arithmetic.

**Evidence defects (the FINDINGS):** (i) `op_landscape_output.txt` contains only a `FileNotFoundError` traceback — the script was run from the wrong directory when the output was captured. Run in place it produces real results that *support* the round's claims (fd=1.0 requires 56.3 MW minimum aux with held ash; higher densities burn only into wall/beta violations), but the deposit as it stands evidences nothing; finding 3's citation of it is dead. (ii) NOTES finding 1: "the machine needs ≈140 MW sustained coupled heating vs 50 installed" — the deposited g = −89.6 MW is computed at p_aux = 0, so the required sustained heating at the printed point is ≈90 MW, not 140; ≈140 MW appears only as the transient access requirement near T=12. The trail's own reading ("~90 MW short of self-sustaining") and L-002 carry the correct number, so the error is confined to NOTES.

**Cited artifacts moving outside their task.** Walked `git log` over the cited paths: after `81a4fee8` (grounding-adjacent correction commit) only the round's own commits touch anything cited. Rubric, grading, concept, discovery log, `models/` — all unmoved during the round. Learnings.md correctly still empty pending this review.

## 3. Learning delta

- **L-001: ACCEPTED.** Every quantitative claim reproduced from deposited scripts and verified images: closure equations admissible and image-verified; ≤4% agreement on τ_E/radiation/ash/fusion power with nothing fitted; line-averaged n19 required (vol-av −23%); W-form +9.2% the dominant gap, entering conducted loss as W^2.56 (1 + 0.61/0.39, so +9.2% → ≈+25%).
- **L-002: ACCEPTED, one correction.** The evidenced core stands: no stable feasible burn at the baseline machine inside its own limits; printed point on the unstable branch ~90 MW short of self-sustaining (the correct number — see NOTES ≈140 MW correction above); ~×3 power-to-temperature error amplification. The forward-sustainment sentence is architectural argument rather than measured result, but it is sound on its face — `p_aux_required = p_rad + W/τ_E − f_α·p_α` is a closed-form forward computation with no convergence domain — and I accept it as the recorded rationale for the next strategy, which weighs it on the merits below. Correction to append with it: the ash chain it retires entry points through is itself a (benign, damped) fixed point, so "exact everywhere" applies to the sustainment balance, not to every sub-quantity.
- **L-003: ACCEPTED.** B^−2.15 read off printed A.8; the B=10 feasible burn, the conductor-ceiling breach (27.7 > 24.9), and the ~802 > 800 MPa stress figure all verify against deposited outputs and model/study values. The three-way trade (confinement reward vs conductor ceiling vs WI-035 stress fence) is the correct structural statement of what replaces the `#4` pathology.

## 4. Constraints carried forward

1. **Amend before implementing:** MR-WI037-2, MR-WI037-3's "solved temperature" language, SV-042, and SV-043's solved-T phrasing encode the refuted architecture. No implementation lands until the spec and SV rows are amended (native modeling PM, dated amendment — never silent edit). Spec § Open decisions 2 (held ash) is superseded by evidence (A.5/A.6 chain validated to +3.6%) and is re-ruled under the same delegation, visibly.
2. **The owed disposition row:** append `20260823-magnet-technology-ab#4` disposition — `model fix`, routed → WI-037 under this goal, in progress, next reference the Round 2 strategy — as an early Round 2 act (this review's correction; ADR-0004).
3. **Evidence repairs:** re-capture `op_landscape_output.txt` from an in-place run (or annotate it dead); correct NOTES finding 1's ≈140 MW to ≈90 MW by dated amendment note, not in-place edit.
4. **Restatement duty (MR-WI037-7, goal invariant):** retiring n_D0/n_T0/n_He0 as entry points changes what committed studies that held them mean; the replacement lever set and the affected-studies list are recorded before any regeneration.
5. **Standing rulings, unchanged:** one pin, one committed study per round; no `p_pump` fixed-fraction form; `vol_cold_cryo` stays settable; no rubric edits; SV-016 recorded against, never fitted; Anchor A closed at its pin; clean room in full.
6. **Fidelity discipline:** the model's own W (+9.2% vs printed) is used self-consistently and disclosed, never tuned to the printed 504.65; the expected consequence — the sustainment fence reads *violated* at the printed baseline point (≈90 required vs 50 installed) — is the one explained verdict change, a finding, not a failure.
7. **Extraction distrust extends to iter-02:** the iter-02 `output.md` Table 3 is garbled against its own raw PDF (axis/edge ι). Every load-bearing value verifies against the raw PDF or page images, including values the iter-02 markdown appears to print cleanly.
8. **Citation fix in passing:** `stellarator_plant.sysml:553` doc says "ECRH wallplug"; Table 2 prints *plasma-coupled* 50 MW. The sustainment fence compares coupled-to-coupled; the doc comment is corrected when that file is touched.

## 5. Strategy revision (opens Round 2)

The reviewer-authored Round 2 strategy revision is transcribed into `trail.md` § Round 2 (the operative copy, per ADR-0002); it is not duplicated here.
