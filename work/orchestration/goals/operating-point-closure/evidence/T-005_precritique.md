# Pre-execution critique — `20260901-sustainment-fence` (deposited verbatim by the round agent)

Fresh non-author session, spawned 2026-09-01, read-only oracle probes. Verdict and findings as returned; the record's § 14 row summarizes the dispositions.

---

**Verdict: MAJOR.** The axes are honest levers, the ties are right, and the sustainment fence enters exactly as the policy's R1 lever-plus-inequality pattern — this is a well-built plan. But I probed the package oracle read-only at candidate points (the same instrument step 7 will use), and the numbers show the grid arm's headline question is undeliverable as planned, one runbook obligation is unmet, and the pre-screen will silently amputate a curve §2 promises to commit. All three are fixable before any point runs.

**Grounds for the probe evidence** (oracle `oracle_entry.evaluate`, baseline levers unless noted): baseline p_aux_required = **90.6 MW** vs 50 installed (matches the manifest's expected-violated `sustainment_ok`), and baseline **B_peak = 24.90 T = B_max exactly**. At I = 18/20 MA, p_aux_required drops to 49.4/45.2 (fence would flip) — but B_peak is 29.1/32.3 T (limit 24.9) and sigma_wp 888/1096 MPa (limit 800). At n_e0 ×1.4, required is still 82 MW and wall load 5.41 > 4.05. Over T ∈ [6,30], required aux bottoms near 72–74 MW (T≈18) — never ≤ 50 — and T = 6 raises a complex-value exception. The p_input transect flips at ~91 MW as predicted, with pin ≥ 100 fully feasible.

## Findings

1. **MAJOR — The grid arm's feasible set is empty at the held p_input = 50, so "does the constrained optimum leave the beta floor" cannot be answered by this plan.** Every grid point either violates `sustainment_ok` (I ≲ 18 MA, any density in-window) or `peak_field_ok`/`wp_stress_ok` (I above baseline — the baseline sits *at* the conductor ceiling, so field cannot rise at all). §2/§5 promise a constrained optimum; the plan will deliver 0% grid feasibility, and whole-study feasible fraction lands near 3% — below H1's 5% bar for a search-framed study. Emptiness here is partly manufactured by holding installed power at a value the manifest itself pins as sustainment-violated. **Disposition:** either (a) add a second (I × n_e0) grid slice at a p_input above the ~91 MW flip (e.g. 110 MW, tie riding) so the field-vs-density trade has a live feasible region and the optimum question is answerable, or (b) strike the constrained-optimum promise from §5, pre-register the empty-set reading as the grid arm's claim, and pre-declare in the record that an under-5% feasible fraction will be reported as the H1 first-order finding per STUDY_POLICY §7 — not as boundary-mapping success. (a) is better; it answers the owner's actual question.

2. **MAJOR — Declined geometry axes are untraced.** The owner's intake quote asks the question "over (I_coil/B, n_e0, T_i0) **and geometry**"; §2 declines R/a with reasons (executor's addition), but `axes.json` and `indicators.json` carry only the four swept groups and §8 has no declined rows. Runbook step 3: "An axis proposed and then declined is still traced, and its indicator and the reason it was declined both reach the record." **Disposition:** declare `R` (+`magnet__R0` tie) and `a` as declined groups, rerun indicators for all groups (step 3 fails closed on a narrowed run), and add the declined rows with reasons to §8 before execution.

3. **MAJOR — The evaluability pre-screen amputates data §2 promises to commit, and its exclusion boundary coincides with the `net_positive` fence.** `screen()` drops any point with oracle p_net ≤ 0 or an oracle exception; the T = 6 probe already shows the low-T branch dies as an exception. So `points.csv` cannot carry "the committed form of round 1's required-aux curve" over the full T window, and every committed point satisfies `net_positive` by construction — the fence becomes an exclusion edge, not a verdict boundary. The pattern is disclosed (good), but the record as drafted overpromises against it. **Disposition:** (a) in `export_excluded`, also export the oracle-side sustainment operands (`p_aux_required`, `tau_E`, `p_rad`) for excluded points where the sustainment stage converges, recording unevaluable-with-reason where it does not; (b) commit in §6/§17 language that screened points count as infeasible-by-exclusion in every feasible-fraction and boundary claim — denominators include them, and "net_positive never violated among evaluated points" is barred phrasing.

4. **MINOR — §5's T-row search rationale overpromises a sustainment boundary that is not in the window.** Probes confirm round 1: required aux bottoms ~72–74 MW > 50 across T ∈ [6,30], so `sustainment_ok` never flips in T. The boundaries this arm can actually exhibit are wall-load (~T ≈ 16; T = 18 already reads 4.77 > 4.05) and beta (~T ≈ 28; 0.0554 at T = 30), plus the required-aux minimum. **Disposition:** reword the §5 T row: the expected result is *no* sustainment crossing in-window; the arm's search structure is the wall-load and beta fences and the location of the aux minimum.

5. **MINOR — `ANNEX.md § Baseline pin` contradicts the manifest.** Annex: headline 333.067, "all six viability constraints satisfied." Manifest (what preflight actually reads): 307.087, eight verdicts including `sustainment_ok` expected-violated and `wp_stress_ok`. The gate is right; the prose a cold reader is sent to is stale from before the WI-035/WI-037 regeneration. **Disposition:** update the annex section before execution.

6. **MINOR — held-fact disclosure gaps.** (a) The legacy entry key `stellarator_09__stellaris__n_e` (3.17e20) is a physical sibling of the swept `n_e0` — it feeds the fusion calc's 0D bypass and is inert only because `sigma_v = 0` (`pipeline.yaml:218`, `mfe_plasma_scaling.sysml:136-148`); the record should say so, since no tie is needed *only while the bypass is off*. (b) §2's held-facts list omits the profile exponents (alpha_n, alpha_n_e, alpha_T) and the synchrotron facts (kappa_sync, R_w_sync), all of which shape the fence position — add them. (c) `export_oracle_operands` hardcodes `recirc_threshold: 0.5` instead of reading the package's `recirc_ok__threshold` library default — read it from the package so the exported bound cannot drift from the executed one.

7. **MINOR (observation) — the baseline sits at exact equality on the conductor ceiling** (B_peak 24.90 vs B_max 24.9, `<=` satisfied). This pre-determines the pin = 50 answer to "is field rewarded" — there is zero field headroom without relaxing B_max (the declined magnet-technology axis) — and it is a numerically fragile verdict for the baseline gate. State it in the record so the result doesn't read as a discovery of the window edges.

**What passes cleanly:** all four swept axes are causal design levers, none computed (policy §2.1); the `p_ecrh` tie promotion and the riding `R0` tie are exactly the §2.2 discipline, checked against manifest `ties`; `sustainment_ok` is the R1 inequality (`p_aux_required <= p_input`), zero equality pressure; the I/n_e0/p_input windows all contain real fence crossings (beta at low I, peak-field/stress just above baseline, wall load in n and T, sustainment at ~91 MW on the p transect); intake provenance is honestly graded (delegated strategy text, not owner verbatim by pretense).
