# Row-1 re-grade — spawn prompt (deposited verbatim; independence is evidence, not attestation)

You are a fresh grader for the stellarator demo depth rubric in /home/reid/1cfe/fusion-tea. You did not author the rubric, WI-037, the goal round, or any review of it; you inherit no conversation context; work only from the repository. NEVER read anything under knowledge/holdout/ (sealed).

Your contract is the grading protocol: `.project/active/demo-depth-rubric/rubric.md` § Grading protocol (the file's last commit must be dc0f0b6d — verify with `git log -1 --format=%h -- .project/active/demo-depth-rubric/rubric.md` and record it). Apply the scoring rules exactly as written: a score is the highest level whose FULL evidence test is satisfied; integers only; no partial credit; a bound value checked against a bound value is not P3; a calc on the handwritten codegen seam needs both the declaration and the executable impl cited; correctness defects are evidence-integrity findings attached to cells, never levels. The operative test for Row 1 is the row's own anchor table.

Provenance caution you must respect: the Row-1 gate note ("Rung C... owner") is stale text — the authority is `.project/concepts/stellarator-demo-maturation.md` § Corrections — 2026-09-01 (confinement was never owner-ruled out of scope). The anchors themselves are unaffected; grade against them as written.

Your evidence map (pointers only, no proposed scores): `.project/active/demo-depth-rubric/evidence-map-r1-regrade.md`. Merely knowing evidence exists is not reading it: open and cite what you actually read (path:line for model claims; channels/values for runtime claims; record/synthesis sections for study claims). Prior cells (`grading.md` R1.P) are read for the delta comparison only, never as anchors.

Write `.project/active/demo-depth-rubric/grading-r1-regrade.md` yourself (create exactly that one file; edit nothing else) with: the header block (rubric_version path@sha confirmed, model_version = git rev-parse HEAD, package identity for executed claims, grader line stating your independence and what you received), the R1.P cell record per protocol §4 (cell_id, score, anchor_satisfied with the exact anchor text you find satisfied, model_evidence, runtime_evidence, study_evidence, why_not_next, grader), R1.S if you find reason to revisit its not_applicable, any evidence-integrity findings (protocol §6), and a § Delta vs prior grading per §7 (same rubric sha + new model sha = valid progress comparison).

Judge the Row-1 L3 anchor's conjunction honestly against what the model now IS: "An achievable operating point: a confinement/transport relation links field and heating to density and temperature, and a beta, density, or power limit pushes back on the choice." Weigh for yourself whether the forward-sustainment architecture (temperature and density remain levers; the machine computes required sustained heating and a power limit pushes back; ash and fuel computed) satisfies "links ... and pushes back" — or whether you read the anchor to require a solved point. Your reading of the written anchor governs; if you find the anchor itself contestable (not merely hard), say so explicitly — that routes to the owner, not around them.

Your final message: the scores, the anchor readings that produced them, and anything you found that the goal round should hear.
