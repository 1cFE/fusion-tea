# Trail: stored-energy-basis

What happened, and what was decided. Append-only, newest entry last, ISO dates; no entry is ever edited in place — corrections are `### Amendment` entries. This file logs judgment, not routine stage motion; native workflows keep their own stage records and are cited, never restated. Procedure: `work/orchestration/GOAL_RUNBOOK.md`.

Goal **drafted** 2026-09-04 (`goal.md`, status `draft`, owner not present; the rulings it needs are listed in `goal.md` § Reserved gates). No round open. **A draft goal authorizes no task.**

## Grounding — 2026-09-04

Worked on branch `goal/stored-energy-basis` in the sibling worktree `../fusion-tea-stored-energy-basis`, from `feat/demo-maturation` at `a5b0b96a`; that branch moved to `ea7304bc` during the session (`wall-and-heating` round 2 closed on trigger 1) and this branch was fast-forwarded onto it before its first commit. The goal directory was created from the templates; the previous session's handoff was deposited at `evidence/handoff_20260904-170919.md`.

**What was done.** The diagnostic the handoff scoped as T-001 was run as grounding evidence, not as a task: `evidence/w_counterfactual/` (Basis A on the oracle with a constant W scale, applied in memory to a copy of one function's source; Basis B on the operating-point prototype; the 6,311 committed points of `20260904-wall-and-heating` re-evaluated and every verdict flip counted against the record; the attribution arithmetic on the printed values; the printed values read from the raw PDF pages). Nothing under `models/`, `exploration/stellarator_e2e/generated/` or `studies/manifest.json` was written; no file on disk was edited; the pin is unchanged. Sixty committed points re-evaluated unmodified reproduce the record exactly.

**Reading** (`evidence/w_counterfactual/NOTES.md` §§ 3–8). Neither baseline violation survives the counterfactual: 90.6 → 37.5 MW required and `sustainment_ok` satisfied; the wall peak 4.088 → 3.861 and `wall_load_ok` satisfied, through the fusion power the re-closed ash takes away. At 100 MW the design geometry opens (six driven points, the baseline itself feasible at 332.6); the cheapest driven point moves 212.46 → 212.31; 347 of the record's driven points stop being driven, 305 by igniting. The paper's own printed table, through the paper's own balance, does not reach the ignition it prints (−61 MW), and the printed 504.65 MJ is not derivable from the paper's plotted profiles and printed peaks (readings of them give 527–575 MJ; the printed β implies 567). The +9.2 % is a fit residue against a source table that is not self-consistent under its own parametrization.

- **Decision 1** — trigger: the handoff asks for the goal to be grounded owner-present and the diagnostic run as T-001; the owner was not in session and a draft goal authorizes no task · decision and reason: draft `goal.md` with all five classes filled and status `draft`; run the diagnostic as grounding evidence because it is oracle-side, cheap, touches nothing the invariants protect, and its answer decides whether the goal needs a round at all · tier: `execution detail` · decided by: this session (agent) · what changed: `goal.md`, `trail.md`, `learnings.md`, `evidence/` in this directory; nothing else.
- **Decision 2** — trigger: the attribution arithmetic shows no reading of the paper's plotted profiles with its printed peaks reproduces the printed 504.65 MJ, and the printed β disagrees with it — evidence against the premise the question and L-001 rest on · decision and reason: surface it in `goal.md` § Question and § Answered when (c) and in `NOTES.md` § 7, with the dependent conclusions labelled conditional on the target; resolve nothing · tier: `premise surprise` · decided by: this session (agent), parked for the owner · what changed: `goal.md` § Question, § Answered when (c), § Reserved gates; `NOTES.md` § 7–8.

### Stop — 2026-09-04

- **Kind:** `owner gate` — grounding needs the owner (§ Reserved gates: ratification, the basis, the inherited invariants, the checkpoint timing).
- **What is true on disk:** this directory on branch `goal/stored-energy-basis` (worktree `../fusion-tea-stored-energy-basis`), `goal.md` at `draft`, no round open, evidence deposited under `evidence/`; `feat/demo-maturation` unchanged apart from a pointer in `.project/CURRENT_WORK.md` (uncommitted, the owner's file); pin `c1b0f0d1…` and the `20260904-wall-and-heating` record untouched; `wall-and-heating` round 2 closed on trigger 1 at `f51b2915` with its fresh review pending.
- **What the owner must see:** (1) whether to ratify `goal.md` as worded, close at grounding on the evidence, or open round 1 for § Answered when (b)/(c); (2) both pinned baseline verdicts flip under the printed W, and the 100 MW design-geometry reading of the round-2 record flips with them; (3) the premise conflict — the printed 504.65 MJ is not what the paper's own profiles and peaks integrate to, so "the W-form is the fidelity gap" is not supported by the source; (4) whether this evidence goes in front of the `wall-and-heating` round-2 fresh review and the close-packet rulings; (5) the fold-back of the branch.

### Amendment 2026-09-05 — amends the preamble drafting line and the branch state in § Stop — 2026-09-04

**The goal is `grounded` as of 2026-09-05**, ratified in chat by the owner: *"agreed with grounding the goal"* `[OWNER-VERBATIM 2026-09-05]`; recorded in `goal.md` § Amendment 2026-09-05. The owner was not in session when this entry was written. § Question and § Answered when stand as agent wording under the ratified grounding (`[AGENT] (grounding ratified by owner 2026-09-05; wording not separately reviewed)`), and the ratification-of-wording gate stays open in that narrower form. A grounded goal authorizes tasks. This amendment records the state change only; it decides nothing.

**Branch state.** `goal/stored-energy-basis` was rebased onto `feat/demo-maturation` at `1928925b` (the `wall-and-heating` round-2 review prompt; additive, no overlapping paths). The goal's first commit is now `3687d2f6`, formerly `89fc9f06`; nothing under this directory changed content in the rebase. Every citation to the goal directory "at its first commit" resolves at `3687d2f6`. The `.project/CURRENT_WORK.md` pointer in the primary checkout is still the owner's uncommitted working-tree edit.

## Round 1 — write-up-from-grounding

### Strategy revision — 2026-09-05

- **Approach:** the grounding evidence already answers § Answered when (a) and gives (b) a first pass. This round turns that evidence into a round result whose learning delta a fresh non-author review can accept, so the run carries citable knowledge and the owner can re-grade the two inherited invariants (`priced-levers/goal.md:52`, `wall-and-heating/goal.md:52`) and `operating-point-closure` L-001's never-tune line from an accepted record rather than from one agent's notes. No study, no pin, no model change. Two optional bounded research requests against the Stellaris paper's own definitions (REQ-W-01: what "Total plasma energy" and "Vol. av." integrate; REQ-W-02: β's reference field and any published point-A profile or 0.5-D description; drafted at `evidence/round1_research_option.md`, not opened) run first **if the owner takes them**, because a found definition could reconcile the printed numbers and would change the second proposed learning; the write-up task does not start until they return or the owner declines. The counterfactual reaches the `wall-and-heating` round-2 review only through that goal's own pen and the owner's reserved gate; this goal supplies the path (`evidence/round1_wall-and-heating_review_input.md`). `[AGENT] — the previous session's recommendation, relayed to the owner 2026-09-05 with the grounding; the owner ratified grounding only, and asked for the research option to be offered.`
- **Assumptions:** (1) the grounding evidence is sound enough to carry learnings without a re-run — the point construction reproduces the committed record exactly (60 points, zero verdict mismatches, `p_aux_required` to 1e-6 MW; `NOTES.md` § 3), and a fresh reviewer can re-run `w_counterfactual.py baseline` and `parity` in minutes with the primary checkout's interpreter; (2) a learning may rest on oracle-side evidence when its scope says so — `operating-point-closure` L-001 and L-005 rest on a prototype the same way; (3) the paper's definitions are not in the repository: the page reads (`NOTES.md` § 2) found no definition of "Total plasma energy" or of β's reference field, and the extracted text is unreliable for its tables and captions, so (b)'s residual is a research question, not a re-read; (4) the `wall-and-heating` round-2 review has not returned — the trail's last heading is `## Round 2 result — 2026-09-04` at `1928925b` and the deposited review prompt does not mention this goal — so the path that adds the counterfactual to the reviewer's inputs is still open.
- **Abandonment conditions:** the research returns a definition under which the printed 504.65 MJ follows from the paper's own profiles and peaks (a premise surprise: the attribution's second reading is wrong, the model's exponents or W form are back in question, the dependent learnings are parked, and § Answered when (c) becomes a model question the owner mints — surfaced, never resolved silently); the owner closes at the round boundary or redirects; a fresh reviewer cannot be obtained (the round waits; it does not review itself); the owner rewords § Question or § Answered when so that (b) requires something other than a written attribution or a bounded negative.
- **Intended model increment:** none. The helium-ash shape (`NOTES.md` § 7 item 2: the model gives helium the fuel's exponent; about 2 % of W and an off-axis charge imbalance) is a model candidate surfaced for the owner and deferred — it would disturb every committed study at the pin, and a change to the W form or the exponents is a reserved gate.
- **Intended study question:** none. The round runs no study and promotes no pin; its evidence is the grounding counterfactual, oracle-side, at pin `c1b0f0d1…`. The round's expected shape, for the reviewer to check and not a plan: the write-up task returns `COMPLETE` with the proposed delta (three learnings, agent wording at `evidence/handoff_20260905-063639.md` § Context, restated in the round result), the dispositions this goal owes on `20260904-wall-and-heating#1`, `#2`, `#4`, `#6` are written, and the round closes on trigger 4 (§ Answered when (c) is an owner gate) unless the owner rules at the boundary (trigger 6).

No future task list. The first task is chosen by the owner's answer to the research offer; after that, tasks are chosen from evidence as each returns.

### Stop — 2026-09-05

- **Kind:** `owner gate` — the round is open with no task started; the first task depends on the research offer, and two reserved gates are live.
- **What is true on disk:** `goal.md` `grounded` (§ Amendment 2026-09-05); round 1 open (§ Strategy revision above), no `T-001 scope` written; the grounding evidence unchanged at `3687d2f6`, plus two round-1 drafts (`evidence/round1_research_option.md` — REQ-W-01 and REQ-W-02 as JSON and a spawn prompt with the clean-room screen, not opened; `evidence/round1_wall-and-heating_review_input.md` — the path (a) and path (b) text); nothing written under `knowledge/research/requests/`; pin `c1b0f0d1…` and the `20260904-wall-and-heating` record untouched; `feat/demo-maturation` at `1928925b`, the `wall-and-heating` round-2 review not returned; `tests/orchestration/test_goal_contract.py` 28 passed / 1 failed after the rebase, the failure `test_narratives_are_separate_from_the_goal_contract` on `wall-and-heating/trail.md` at `f51b2915` (pre-existing on `feat/demo-maturation`; not this goal's).
- **What the owner must see:** (1) **the research offer** — REQ-W-01 (the paper's definition of "Total plasma energy" and "Vol. av." in Table 5: which profiles, which volume element, thermal only or with fast alphas) and REQ-W-02 (β's reference field; any published point-A profile data or 0.5-D code description): both, one, or neither. If either, it is T-001 and runs before the write-up; the expected return is a bounded negative, which is still the point — it makes the second learning unconditional. (2) **The wording** — does § Question / § Answered when stand as drafted; one line. (3) **The reserved gate on the `wall-and-heating` review** — the review has not returned, so path (a) at `evidence/round1_wall-and-heating_review_input.md` is available to that goal's operator now; once it returns, only path (b). (4) **The fold-back** of this branch, at the owner's convenience; the path to the counterfactual exists only on `goal/stored-energy-basis` until then.
- **Resume at:** `GOAL_RUNBOOK.md` § Running one task — write `### T-001 scope` for the research (if taken) or for the write-up (if declined).

### Amendment 2026-09-05 — amends § Stop — 2026-09-05, item (3), and § Strategy revision assumption (4)

**The `wall-and-heating` round-2 review returned during this session.** `work/orchestration/goals/wall-and-heating/evidence/round2_review.md` appeared in the primary checkout at 06:51 on 2026-09-05, untracked, after the Stop above was drafted: verdict `FINDINGS`, a recommendation that the owner close that goal on § Answered when, a close packet of five items, twelve constraints carried forward, and L-009..L-012 in the forms to append. It does not mention this goal, the counterfactual, or the printed 504.65 MJ; the reviewer worked from `1928925b`. That trail carries no `### Round 2 review` entry yet.

**What changes.** Path (a) at `evidence/round1_wall-and-heating_review_input.md` (adding the counterfactual to the reviewer's inputs) is closed; the review is written. Path (b) applies: the counterfactual reaches that goal only as a constraint carried forward, by the owner's ruling on the review's close-packet item 1 (the (b) ruling: W-sensitivity as a fifth disclosed condition beside the four the record states), by the round agent's landing of L-009 under close-packet item 5(b) with that condition written in, or as a thirteenth constraint carried forward; and if the owner grounds the geometry question as its own goal (close-packet item 5(c)), `NOTES.md` §§ 5, 8 is grounding evidence for it. Strategy assumption (4) is false in letter and nothing depended on it beyond the choice of path; the strategy stands. The Stop's item (3) now reads: the reserved gate is whether the counterfactual is carried into the `wall-and-heating` close packet or L-009's conditions, and by which of those three routes. This amendment records a state change and decides nothing.

### T-001 scope

- **Objective:** return the Stellaris paper's own definitions of its printed "Total plasma energy" (504.65 MJ) and "Vol. av." quantities (which profiles are integrated, over which volume element, thermal only or with fast alphas) and the reference field of its printed volume-averaged β — from the paper's pages, its supplementary material, a Proxima Fusion document, or a cited systems-code paper by the same authors — or a bounded negative naming what was searched. A negative is a full answer.
- **Why now:** § Answered when (b) closes when the owner accepts the first-pass attribution or a research-seam request returns the definitions or a bounded negative (`goal.md` § Answered when (b)). A found definition could reconcile the printed 504.65 MJ with the printed fusion power and β and would overturn the second proposed learning, so it runs before the write-up. The owner turned the offer into a judgment call on 2026-09-05 (*"use your best judgement to achieve the goal"* `[OWNER-VERBATIM 2026-09-05]`); the judgment is one bounded request, not two.
- **Scope:** authorized — the native research seam (`scripts/research_seam.py` open/log/close, `scripts/source_registry.py register --run`), read-only reads of the Stellaris PDF pages and of admissible sources, the run directory under `knowledge/research/requests/`, one fresh spawned research subagent with its prompt deposited first. Excluded — any edit under `models/` or `exploration/`, the discovery log, any fit or scaling toward 504.65 MJ, any inferred definition presented as the paper's, and every artifact barred by the clean room. REQ-W-02 is **not opened**: its β-reference clause is folded into REQ-W-01's question, and its point-A / 0.5-D clause is already in REQ-W-01's `where_to_look` (a change from the drafts at `evidence/round1_research_option.md`, recorded here).
- **Inputs:** `goal.md` (§ Invariants: clean room, never default, W never tuned; § Reserved gates). `evidence/round1_research_option.md` (the draft request and spawn prompt). Narrower constraint, as in the `wall-and-heating` T-001 precedent: the sealed hold-out is itself a stellarator study and the registry guard fires only at registration, so the screen goes in the subagent's prompt before any fetch, and the ARIES-CS extraction directories under `knowledge/concept_research/09-qi-stellarator-hts/iter-02/sources/` are not opened. Python from the worktree runs on the primary checkout's interpreter.
- **Done when:** the seam returns `REGISTERED` with a definition, `BOUNDED_NEGATIVE` with the durable negative written, or `OPERATOR_QUEUE` naming what a person must fetch — and the goal-level reading says whether the printed 504.65 MJ reconciles with the printed fusion power and β under a stated definition.
- **Stop when:** a strategy blocker (a reconciling definition is a premise surprise: surfaced, dependent learnings parked, never resolved silently), an owner gate, or a declared limit (6 searches, 3 captures).

### T-001 start — 2026-09-05

T-001 · the research seam on `knowledge/research/requests/REQ-W-01.json` (written and opened after this line; the run directory is named in the spawn prompt) · expected artifacts: the run's `return.json` and receipts, and the deposited spawn prompt at `evidence/T-001_REQ-W-01_prompt.md`.

### Checkpoint C-001.r1 — 2026-09-05

- **Reviewer:** a fresh non-author session spawned from the deposited prompt (`evidence/round1_C-001_checkpoint_prompt.md`, `dfc9eca7`), run in parallel with T-001 (the dispositions rest on the counterfactual, not on the research return; the checkpoint's `r2` exists for the case the return changes a row).
- **Reading reviewed:** `evidence/w_counterfactual/NOTES.md` §§ 1–8 at `3687d2f6` — every number in §§ 3, 5, 7 recounted from the data and reproduced; sixty points re-run unmodified reproduce the record; "the reading is right."
- **Dispositions reviewed:** `evidence/round1_proposed_dispositions.md` (`dfc9eca7`).
- **Verdict:** `REVISE`. Deposited verbatim at `evidence/round1_C-001_checkpoint.md` § r1. Grounds: the `wall-and-heating` goal was **closed by the owner on 2026-09-05** (`feat/demo-maturation` at `e0f66f7a`: review `FINDINGS` at `a11db053`, L-009..L-012 landed with four conditions, two F4 rows under `#6` and `#8`, two close rows under `#1` and `#6`, WI-041 archived), so every "current state" in the file is stale and two named actors are past events; `#1` mixes a three-arm numerator with a two-arm denominator (347 of 681, or 332 of 657) and claims robustness in a direction the counterfactual never tested (W was only lowered); `#2` uses "driven" against the record's definition; `#8` needs a row (61 points fall off the closure's validity edge). `declared seam` ruled the right class for `#1`, conditional on T-001 not returning a reconciling definition. One gate-adjacent fact surfaced by the reviewer: the owner's (b) close ruling on `wall-and-heating` was made on four conditions, without the fifth in front of them.
- **Revision:** 0 of 2 at this entry; the author's r2 follows T-001's return (change 2(f) depends on it) and is appended to the dispositions file as a section, r1's text kept.

### T-001 return — 2026-09-05

- **Outcome:** `COMPLETE` — the seam returned `REGISTERED` (two sources registered, two candidates queued, no negative written; `limit_reached: max_searches` because the sixth search was the last place on the request's own list; closed `--adequacy exhausted`).
- **Evidence:** `knowledge/research/requests/REQ-W-01.json` and `evidence/T-001_REQ-W-01_prompt.md` (`fbcc9166`); the run directory `knowledge/research/requests/runs/REQ-W-01/20260905T145916478870/` (`run.jsonl`, `process_log.md`, two receipts, `return.json`) at `d4059ef1`; the registrations at `d4059ef1` — `knowledge/sources/a_general_stellarator_version_of_the_systems_code_process/` (Lion, Warmer, Wang, Beidler, Muldrew, Wolf, Nucl. Fusion 61 (2021) 126021, CC-BY; the Stellaris paper's ref [12]) and `knowledge/sources/systems_code_models_for_stellarator_fusion_power_plants_and/` (Lion, PhD thesis, TU Berlin 2023, doi 10.14279/depositonce-18188, open access; ref [110]), with their `SOURCE_INDEX.md` blocks and `MANIFEST.jsonl` rows; the round agent's reproduction `evidence/w_counterfactual/attribution_sourced_definition.py`, `.json`, `_output.txt` (this commit). The subagent's report is not deposited verbatim; its substance is in the run's log and the two registrations' use-for, validation and caveat lines, and is restated here.
- **Reading.** (1) **The definition is sourced.** In the same author's code — the 2021 paper, Eqs. (8)–(11), and the thesis, Eqs. (2.10)–(2.12), whose Appendix A is the near-verbatim ancestor of the Stellaris paper's — W is the thermal energy from the imposed profiles, W = V · 3/2 ∫₀¹ dρ √g n(ρ) T(ρ) with the effective-radius element √g ∼ ρ (the same ρ dρ element the model uses), no fast-alpha term; ISS04's density is line-averaged; the "particle species averaged" n and T are a single product with no stated species rule (a mean over species would give about half of the sum; a total over species is the sum the model takes — the text does not say which). The paper itself states the profile set (Eqs. 2–3 with α_T 1.2, α_n 0.35 — the Fig. 16 set, "A-priori profiles are used according to Eq. (3)"), T_i/T_e = 0.95, the A.5 ash rule, V = 425 m³ and B0 = 9.0 T axis-averaged; it does not state the volume element behind "Vol. av.", whether "Total plasma energy" includes the fast-particle pressure it says it models, or the field its β is referenced to; it has no supplementary data ("Data will be made available on request"). (2) **Reproduced by the round agent** (`attribution_sourced_definition.py`; the subagent's own arithmetic agrees): the sourced definition applied with the paper's own rules on the printed peaks (species sum; T_i = 0.95 T_e; the fuel at 0.35 and T at 1.2; the ash shaped by A.5, n_He ∝ n_D n_T ⟨σv⟩(T_i), scaled to the printed peak, an effective exponent of 4.1; electrons by quasi-neutrality; the 2ρ dρ element over 425 m³) reproduces the printed fusion power (2711 vs 2700 MW), the printed peak electron density (5.04 vs 5.06) and the printed volume-averaged density (3.16 vs 3.17) — which corroborates the effective-radius element and the A.5 ash profile as the paper's averaging basis — and gives **W = 518.3 MJ, +2.7 % over the printed 504.65**, thermal β 2.52 % at 9.0 T (printed 2.76). The model's family on the same peaks gives 551.3; the model's exponents with the A.5 ash shape and quasi-neutral electrons give 524.5. **So the +9.2 % decomposes differently from the grounding evidence's first pass** (`NOTES.md` § 7: about two points ash, seven unreachable): about five points are the model's ash shape (the ash carries the fuel's exponent 0.33 where the paper's own rule peaks it at about 4) with the electrons re-closed, about one point the exponents and the 0.95 ratio, about 2.7 points remain between the paper's own rules and its printed value and are explained by no admissible source, and the printed β and printed W are mutually inconsistent as thermal quantities at 9.0 T (504.65 MJ ↔ 2.46 %; 2.76 % ↔ 567 MJ; they agree only at 8.49 T, a field no source names). A mean-over-species reading gives 259–274 MJ and is ruled out by the printed value. (3) **What this does to the premise.** The model's W-form *is* a fidelity gap against the paper's own stated rules — about 6 %, on the ash profile shape, sourced — so `operating-point-closure` L-001's "the analytic W-form is the dominant fidelity gap" is right in kind and overstated in size, and its target (the printed 504.65) is still not derivable: the last 2.7 % and the β row are the paper's, not the model's. The abandonment condition ("a definition under which 504.65 MJ follows from the paper's own profiles and peaks") is **not met**; the second and third proposed learnings change content. Queued for a person: the author query (J. Lion, the paper's "data on request") and the paywalled ISS04 paper (low priority). One Helios candidate refused on clean-room grounds, recorded in the log; nothing under `knowledge/holdout/` read.
- **Decision 1** — trigger: the owner's 2026-09-05 direction turned the research offer into a judgment call · decision and reason: one request (REQ-W-01, β's reference field folded in), not two — the second draft's other clause was already in the first's places to look, and one bounded return is as durable as two · tier: `execution detail` · decided by: this session · what changed: `REQ-W-01.json`; the run directory; the T-001 scope (REQ-W-02 not opened).
- **Decision 2** — trigger: the fresh checkpoint was spawned in parallel with T-001, before this return · decision and reason: the dispositions rest on the counterfactual, not on the research return; the checkpoint's `r2` exists for the case the return changes a row, and it did · tier: `execution detail` · decided by: this session · what changed: `evidence/round1_C-001_checkpoint_prompt.md`; C-001.r1 above.
- **Decision 3** — trigger: the sourced definition applied with the paper's own rules gives 518 MJ, not 504.65, and shows the model's ash shape is worth about five points of the +9.2 % — evidence against the grounding evidence's reading and the drafted second and third learnings · decision and reason: surface it, resolve nothing — not a strategy blocker (the round's job, a learning delta a review can accept, stands; the delta's content changes); the class of the `#1` disposition (`declared seam`, conditional) is put back to the checkpoint with an addendum rather than decided here; the counterfactual's one scale (0.915, W down) is labelled as one scale and one direction, and the baseline at the sourced 518 MJ is evaluated in the write-up task so the owner's (c) ruling has the number that matters; the ash-shape increment becomes a sourced, owner-gated model candidate, not built · tier: `premise surprise` · decided by: this session, parked for the checkpoint, the fresh review and the owner · what changed: `evidence/round1_proposed_dispositions.md` (r2 addendum, next commit); the round result's learnings; nothing under `models/`.

### T-002 scope

- **Objective:** write the round result: the three learnings rewritten on T-001's return, the joined discovery rows landed once C-001 passes (r2 with an addendum on the return), a second-scale evaluation of the counterfactual at the sourced 518.3 MJ (the baseline, and the committed window if it runs in the background while the write-up proceeds) so the owner's § Answered when (c) ruling has the number that matters, the evidence refs, and the close packet.
- **Why now:** T-001 returned `COMPLETE` with a sourced definition that changes the delta's content (about six points of the +9.2 % are the model's ash shape, sourced; 2.7 points and the β row are the paper's); C-001.r1 returned `REVISE` with its seven changes and a class condition that the return now informs; nothing else in the strategy remains.
- **Scope:** authorized — this goal's `trail.md` and `evidence/` (the existing counterfactual script gains an optional target argument and writes suffixed output files; the committed 504.65 outputs are not overwritten); the checkpoint resubmission to the same fresh session; a rebase of this branch onto `feat/demo-maturation` at or after `e0f66f7a` before any row lands; the joined rows in `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` under ids the checkpoint passed (the goal layer's one write outside its directory); `tests/study/test_records.py` and the goal-contract suite as checks. Excluded — `models/`, `exploration/stellarator_e2e/generated/`, `studies/manifest.json`; any write in another goal's directory; `learnings.md` (the review's act); any tuning; any change to the W form or exponents (a reserved gate — the ash-shape candidate is surfaced, not built).
- **Inputs:** `goal.md`; `NOTES.md` at `3687d2f6`; § T-001 return and `attribution_sourced_definition.*`; `evidence/round1_C-001_checkpoint.md` § r1; `DISCOVERY_LOG.md` at `e0f66f7a`. Narrower constraint: the second scale is one more constant scale (0.94, W down), labelled as such — the same § 6 caveat travels with it.
- **Done when:** `## Round 1 result` is written with every template field; the rows are landed and `test_records.py` passes; the goal-contract suite is unchanged (28 / 1).
- **Stop when:** the checkpoint cap (2 revisions) is hit without a pass — a `Stop` of kind `cap`, the rows not landed; an owner gate; a strategy blocker.

### T-002 start — 2026-09-05

T-002 · the second-scale evaluation (`w_counterfactual.py baseline 518.3`, then `window` at that scale in the background), the C-001.r2 resubmission with its addendum, the rows after a pass, the round result · expected: `baseline_counterfactual_518.3.json`, `window_summary_518.3.json` and its CSV, `### Checkpoint C-001.r2`, the rows at one commit, `### T-002 return`, `## Round 1 result`.

### Checkpoint C-001.r2 — 2026-09-05

- **Reviewer:** the C-001.r1 session, resumed with the resubmission (the same fresh non-author).
- **Reading reviewed:** `NOTES.md` §§ 1–8 at `3687d2f6` plus the second scale — `baseline_counterfactual_518.3.json`, `window_summary_518.3.json` / `window_counterfactual_518.3.csv` (`498a329e`) — and `attribution_sourced_definition.*` (`5cc30ac0`); every number recounted from the data and reproduced, the sourced arithmetic re-derived independently with a Bosch–Hale reactivity to the digit (518.3 / 2.52 % / 2,711 / 3.16 / 5.04; the ash's effective exponent 4.12; 5.3 + 1.2 + 2.7 = 9.2 points).
- **Dispositions reviewed:** `evidence/round1_proposed_dispositions.md` § Revision r2 and § r2 addendum (`498a329e`).
- **Verdict:** `REVISE` — one row. `#2`, `#4`, `#6`, `#7`, `#8` `PASS` as written in r2; `#1`'s class ruled **`model fix` — open, not minted, owner-gated** (a sourced rule to aim at — the paper's own ash rule — and a nameable increment; `declared seam` would leave the W-condition with no consumer; the reserved gates hold because the row mints nothing and routes the ruling to (c)); `#1`'s direction honest, its location not — "survives the paper's own rules" stated a boundary as fact where the data show a 0.03 MW/m² margin on `c0625` and 1.4 MW on `c2973`, the flip inside the 2.7 % between the sourced and printed values, not decidable at this pin from W's basis alone. Six exact changes required, all in the `#1` row (the margin sentence; the realized 516.2 MJ and the second-scale caveat; the provenance split between the sister code's definition of W and the Stellaris paper's own profile and ash rules; the candidate's target is the ash rule, not the printed number; the `wall-and-heating` close row's finality stated; shas in the reference column). Deposited verbatim at `evidence/round1_C-001_checkpoint.md` § r2.
- **Revision:** 1 of 2. The author applied the six changes exactly as `## Revision r3` in the dispositions file (r1, r2 and the addendum kept); resubmitted as submission 3 of 3.

### Checkpoint C-001.r3 — 2026-09-05

- **Reviewer:** the same fresh non-author session, resumed a second time.
- **Reading reviewed:** unchanged from r2.
- **Dispositions reviewed:** `evidence/round1_proposed_dispositions.md` § Revision r3 (`fd1e398e`): the six changes applied, 12 insertions and 0 deletions against r2, the other five rows byte-identical.
- **Verdict:** `PASS`. Each of the six changes confirmed applied; `#1` `model fix` — open, not minted, owner-gated; `#2`, `#4`, `#6`, `#7`, `#8` pass unchanged; no row under `#3`, `#5`; the ADR-0004 checklist met. Deposited verbatim at `evidence/round1_C-001_checkpoint.md` § r3. The rows may land once the branch carries `feat/demo-maturation` at or after `e0f66f7a` (it does, by the merge `ea6d5256`).
- **Revision:** 2 of 2 used; the cap was reached with a pass on the third submission.

### T-002 return — 2026-09-05

- **Outcome:** `COMPLETE`.
- **Evidence:** the six joined rows in `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` at `0964e9d3` (`tests/study/test_records.py` 25 passed after them; the goal-contract suite 28 / 1, unchanged); the second scale — `baseline_counterfactual_518.3.json` and `attribution_sourced_definition.*` (`5cc30ac0`), `window_summary_518.3.json`, `window_counterfactual_518.3.csv`, `window_output_518.3.txt` (`498a329e`), the committed 504.65 outputs byte-unchanged since `3687d2f6`; the checkpoint sequence `evidence/round1_C-001_checkpoint.md` r1 → r2 → r3 (`f8e2ee52`, `fd1e398e`, `0964e9d3`) and the dispositions file's r1, r2, addendum, r3 (`dfc9eca7` … `fd1e398e`); the merge `ea6d5256`.
- **Reading:** in § Round 1 result below.
- **Decision 1** — trigger: the T-002 scope said "a rebase of this branch onto `feat/demo-maturation` at or after `e0f66f7a` before any row lands"; a rebase rewrites every commit on this branch and would void the eight shas this trail already cites · decision and reason: merge `feat/demo-maturation` at `e0f66f7a` into the branch instead (`ea6d5256`) — the scope's purpose, that the six rows follow that goal's close rows in file order, is met, and every citation stays valid; the fold-back remains the owner's act · tier: `execution detail` · decided by: this session · what changed: the merge commit; nothing under this goal's directory.
- **Decision 2** — trigger: T-001 put a second, sourced W basis on the table, and the owner's § Answered when (c) ruling needs the baseline and the committed window at that basis, not only at the printed number · decision and reason: evaluate it with the committed script, which gained an optional target argument writing suffixed files (the 504.65 outputs untouched); one more constant scale, W down, under the same § 6 caveat; no third scale · tier: `execution detail` · decided by: this session · what changed: `evidence/w_counterfactual/w_counterfactual.py`; the `_518.3` outputs.
- **Decision 3** — trigger: the round has no study and § Answered when (c) is a reserved gate the owner has not ruled · decision and reason: close on trigger 4 with the packet below rather than hold the round open for a ruling the owner gives at the boundary; the derivation is in § Stop reason · tier: `execution detail` · decided by: this session · what changed: this trail.

## Round 1 result — 2026-09-05

Written by the round-1 agent (the session that grounded the goal on the owner's ratification, ran T-001 and T-002, and took C-001 through three submissions). Round 1 of 6.

- **Intent:** **met.** The strategy set out to turn the grounding evidence into a round result whose learning delta a fresh review can accept, with one optional bounded research check first and no study, pin or model change. The research check ran and came back with the definition the attribution needed; the checkpoint passed the reading and six disposition rows on the third submission; the three learnings below are proposed. What the round found was not what the strategy expected: the sourced definition does not reconcile the printed 504.65 MJ (it gives 518.3), but it shows that the model's own W-form carries a sourced gap of about 6 % against the paper's stated rules, five points of it the helium-ash profile shape — so the +9.2 % is neither a digitization slip nor a bad target alone; it is a real, sourced model discrepancy sitting on top of a printed number no source reproduces. § Answered when: (a) met at grounding and ratified; (b) met on the contract's second branch — a research-seam request against the source returned the definitions (the sister code's, registered at `d4059ef1`) and the attribution is written (5.3 points the ash shape, 1.2 the exponents and the 0.95 ratio, 2.7 the paper's own, and the β row inconsistent with the printed W) — subject to the review; (c) the owner's. Strategy assumptions (1)–(3) held; (4) failed in letter (the `wall-and-heating` review returned and the goal was closed mid-session) and nothing depended on it beyond the path to that goal; the abandonment condition ("a definition under which 504.65 MJ follows from the paper's own profiles and peaks") was not met.
- **Task sequence:** T-001 (REQ-W-01 through the research seam; a fresh subagent from a deposited prompt; two sources registered, two candidates queued, one clean-room refusal) `COMPLETE` · T-002 (the second-scale evaluation; C-001 r2 and r3; the six rows; this result) `COMPLETE`. No goal-level retry: the research subagent stopped once mid-registration with the extractor still running and was resumed by message to the same session, same run, same request — a continuation, not a re-run; the checkpoint's three submissions are the runbook's own revision loop, not retries.
- **Last semantic outcome:** `COMPLETE` (T-002).
- **Stop reason:** derived — last outcome `COMPLETE` + no study or pin under this strategy (none intended) + § Answered when (c) a reserved gate the owner has not ruled → the round closes on **trigger 4 (an owner gate that is not resolved)**. Trigger 6 (the goal answered) is the owner's at the boundary: a trail-only ruling that the model keeps its present family counts under (c), and so does a minted item.
- **Evidence refs:** `evidence/w_counterfactual/` at `3687d2f6` (the 504.65 counterfactual, §§ 1–9); `evidence/handoff_20260905-063639.md`, `round1_research_option.md`, `round1_wall-and-heating_review_input.md` with its Addendum (`88af6e46`, `93571027`); `evidence/T-001_REQ-W-01_prompt.md` and `knowledge/research/requests/REQ-W-01.json` (`fbcc9166`); the run `knowledge/research/requests/runs/REQ-W-01/20260905T145916478870/` and the two registrations (`d4059ef1`); `evidence/w_counterfactual/attribution_sourced_definition.py` / `.json` / `_output.txt`, `baseline_counterfactual_518.3.json`, `w_counterfactual.py` with its target argument (`5cc30ac0`); `window_summary_518.3.json`, `window_counterfactual_518.3.csv`, `window_output_518.3.txt` (`498a329e`); `evidence/round1_proposed_dispositions.md` r1 / r2 / addendum / r3 (`dfc9eca7`, `f8e2ee52`, `5cc30ac0`, `498a329e`, `fd1e398e`) and `round1_C-001_checkpoint_prompt.md` (`dfc9eca7`); `evidence/round1_C-001_checkpoint.md` r1 / r2 / r3 (`f8e2ee52`, `fd1e398e`, `0964e9d3`); the six rows in `DISCOVERY_LOG.md` (`0964e9d3`); the merge `ea6d5256`. Batteries: `tests/study/test_records.py` 25 passed after the rows; `tests/orchestration/test_goal_contract.py` 28 passed / 1 failed throughout, the failure on `wall-and-heating/trail.md` by the owner's 2026-09-05 ruling; `tests/models` and `tests/study` not re-run by this round (nothing under `models/`, the generated package or the manifest changed; `git diff --stat 1928925b..HEAD -- models exploration/stellarator_e2e/generated exploration/stellarator_e2e/studies/manifest.json` is empty).
- **Learning delta (proposed):**
  - **L-001 — The pinned baseline's sustainment verdict and every driven-set reading on this package are stored-energy-sensitive, and at the paper's own rules the baseline sits on a knife-edge.** Two constant scales, both W down, closure live, oracle-side: at the printed 504.65 MJ (scale 0.915) the design point's requirement moves 90.6 → 37.5 MW and both baseline verdicts flip (the wall's through fusion power, −5.6 % as the ash re-closes); over the committed 6,311-point window 1,128 sustainment verdicts flip violated → satisfied and 384 wall verdicts, the design geometry opens at 100 MW (six driven points), 347 of 681 driven points leave the driven set (305 by igniting) and 446 join it, and the cheapest driven point moves 212.46 → 212.31. At the sourced 518.3 MJ (scale 0.940, realized 516.2) the requirement is 51.4 MW against 50 coupled — violated by 1.4 MW — with the wall satisfied at 3.930; over the window the design geometry stays closed at 100 MW by 0.03 MW/m² on `c0625` and 1.4 MW on `c2973` (0 driven at R 12.7, a 1.3; 257 → 314 driven; cheapest `c1676` 208.72; 890 sustainment and 273 wall flips), five design-geometry points are driven at 220 MW, and the re-read arm goes 24 → 58. The flip of the a-1.3 verdict lies inside the 2.7 % between the paper's rules and its printed number, so which side the design column sits on is not decidable at this pin from W's basis alone. Evidence: `NOTES.md` §§ 3–5, `window_summary.json`, `baseline_counterfactual.json` (`3687d2f6`); `baseline_counterfactual_518.3.json` (`5cc30ac0`); `window_summary_518.3.json` (`498a329e`); the C-001 recounts. Scope: this package at pin `c1b0f0d1…`; constant, geometry-independent scales, W down only; sign claims robust, boundary locations not; a W above the model's 551 not evaluated. Implication: every sustainment-fence or driven-set reading on this package states the W basis it rests on and carries W-sensitivity as a condition beside the τ*/τ_E condition; "the baseline is violated" is a statement about the basis (90.6 MW at the model's, 51.4 at the paper's rules, 37.5 at the printed number), not about the machine. Supersedes: refines `operating-point-closure` L-005's precision bound (the ~91 MW threshold is basis-conditional).
  - **L-002 — The printed 504.65 MJ is not what the paper's own rules give under the sourced definition of W, and it is not a fit target.** The same author's systems code (Lion 2021 Eqs. 8–11; Lion 2023 Eqs. 2.10–2.12, whose Appendix A the paper's reproduces) defines W as thermal, from the imposed profiles, over the effective-radius element √g ∼ ρ, no fast-alpha term — the sister code's definition, not settled by any source to be the printed value; applied with the Stellaris paper's own rules (Eqs. 2–3 at 1.2 / 0.35, T_i/T_e 0.95, the ash from the fixed τ*/τ_E ratio applied pointwise, V 425, species sum) on the printed peaks it reproduces the printed fusion power (2711 vs 2700), n_e0 (5.04 vs 5.06) and ⟨n_e⟩ (3.16 vs 3.17) and gives 518.3 MJ, +2.7 % over the printed value; the printed β (2.76 %) is inconsistent with the printed W at the only field the paper names (9.0 T ↔ 567 MJ; 504.65 MJ ↔ 2.46 %; they agree only at 8.49 T); a mean-over-species reading gives 259–274 MJ and is ruled out by the printed value. No admissible source explains the 2.7 % or the β row; the author query is queued (the paper's "data on request"). Evidence: the two registrations and the run at `d4059ef1`; `attribution_sourced_definition.*` (`5cc30ac0`); `NOTES.md` § 7 (`3687d2f6`). Scope: Table 5 point A as printed; thermal; the A.5 shape assumes τ* uniform in ρ and the printed ash peak. Implication: no round tunes W or the exponents toward 504.65; the model's target for W is the paper's stated rules (518 on the printed peaks), not its printed number; the never-tune half of `operating-point-closure` L-001 stands and its target moves. Supersedes: refines `operating-point-closure` L-001's implication ("attributed first to the W-form, and W is never tuned to the printed value") — the re-grade is the owner's ruling, recorded in this trail.
  - **L-003 — The model's W-form carries a sourced gap of about 6 % against the paper's own rules, five points of it the helium-ash profile shape.** The model gives the ash the fuel's exponent (0.33; `stellarator_plant.sysml:578`), so ion charge exceeds n_e off-axis (7.5 % at ρ 0.5, 31 % at 0.8); the paper's rule — ash profiles from the fixed particle-to-energy confinement ratio, n_He ∝ n_D n_T ⟨σv⟩(T_i) — peaks the ash at an effective exponent of 4.1, and quasi-neutrality then re-closes the electrons: 551.3 → 524.5 MJ at the model's exponents (−4.9 %), → 518.3 with the paper's 1.2 / 0.35 and the 0.95 ratio (−1.2 % more); model W / sourced W = 1.064. The model's own exponents are a fair read of Fig. 16 (the WI-022 refit: electrons 0.617, fuel 0.33, T 1.19). Evidence: `attribution_sourced_definition.json` rows "SOURCED", "model exponents but A.5 ash and QN electrons", "MODEL family" (`5cc30ac0`); `attribution_arithmetic.json` quasi-neutrality rows (`3687d2f6`); `baseline_counterfactual_518.3.json`. Scope: the pressure integral at the printed peaks; the ash shape from A.5 in shape only (τ* uniform in ρ). Implication: a sourced model candidate exists — the helium-ash profile from the paper's own rule with electrons by quasi-neutrality — worth about 6 % of W and, at the baseline, 90.6 → 51.4 MW required; it is owner-gated (the W form and exponents are a reserved gate), a minted item through the modelling PM if taken, and every committed study at the pin is disturbed by it. Supersedes: refines `operating-point-closure` L-001's scope line ("the analytic W-form, +9.2 % vs printed, is the dominant fidelity gap"): the gap is real and sourced, about 6 % against the paper's rules, on the ash shape.
- **Finding dispositions:** six joined rows landed at `0964e9d3`, after C-001.r3 `PASS` — `20260904-wall-and-heating#1` `model fix`, open, not minted, owner-gated (the helium-ash profile shape; W-sensitivity disclosed at both scales; the design column's closure at the sourced scale a 0.03 MW/m² / 1.4 MW margin; the owner's (b) close ruling surfaced as made on four conditions, not re-ruled; responsible: the owner at (c), the geometry goal if grounded); `#2` `research`, open, unchanged, the knife-edge's location W-sensitive; `#4` `model fix`, open, unchanged, the ignited set the most W-sensitive set; `#6` `model fix`, discharged, unchanged, re-sighted 24 → 58; `#7` `model fix — none owed`, unchanged; `#8` `declared seam`, standing, unchanged, the edge moving inward with W (61 points at 0.915, 35 at 0.940). No row under `#3`, `#5` (the reading re-measures neither). No sighting row edited; no id minted; the `wall-and-heating` close rows stand as that goal's final state.
- **Surfaced for the owner, not resolved (the close packet):** (1) **the (c) ruling — the stored-energy basis the model carries:** (i) the present family, unchanged — a trail-only answer; the baseline stays at 90.6 MW required, now disclosed as a sourced ~6 % fidelity gap on the ash shape; (ii) a minted model item — the helium-ash profile from the paper's own rule with electrons by quasi-neutrality — sourced, about 6 % of W, the baseline at about 51 MW required (still violated, by 1.4 MW; the wall satisfied), every committed study at the pin re-run; (iii) a sourced alternative — none found; the printed 504.65 MJ is not a target under any reading. The round agent's recommendation, for the reviewer to check: (ii), because the rule is the paper's own, the change is sourced, and it turns "the machine needs nearly twice its heating" into "the machine is on the edge", a different demo statement — with the caveat that the edge is 1.4 MW wide and the flip of the a-1.3 verdict is not decidable from W's basis alone. (2) **The re-grade** of `priced-levers/goal.md:52`, `wall-and-heating/goal.md:52` and `operating-point-closure` L-001's implication line on L-002 / L-003: keep the never-tune line; replace "the model's own analytic W-form, +9.2 % at the printed point" with "a sourced ~6 % gap on the ash shape against the paper's own rules; the printed value is not the target". (3) **`wall-and-heating`, closed:** the `#1` row lands W-sensitivity as a fifth disclosed condition; the owner's (b) ruling was made on four — surfaced, not re-ruled; a geometry goal, if grounded, inherits it. (4) **The author query** — a person's act: J. Lion, the paper's "data on request"; the three questions are written into the run's log (`process_log.md`). (5) **Two imprecisions in this goal's own § Question**, flagged by the checkpoint and not repeated here: "347 of the record's 257 + 400 driven points" (the 347 spans three arms; 332 of 657 in the two search arms) and "every reading reproduces the printed fusion power" (one reading, ions at the electron exponent, misses it by 8.5 %) — a dated amendment to § Question, if the owner wants the draft wording corrected. (6) The pre-existing goal-contract failure on `wall-and-heating/trail.md`, owner-ruled to stand.

### Round 1 review — 2026-09-05

- **Reviewer:** a fresh non-author session spawned from the deposited prompt (`evidence/round1_review_prompt.md`, `4ee3ab73`); one freshness disclosure recorded in the review (the shared scratchpad held the research subagent's working files; the reviewer listed it, verified one sha against the registration, and read nothing else there).
- **Verdict:** `FINDINGS` — every task's evidence stands and no evidence error was found; six precision-level findings, none reopening a task: F1 the three learnings corrected before appending (the margin in W terms; the residual's reach and the sister-code provenance in L-002; "the 0.95 ratio" dropped, exact at point A; "W-form" → the profile family in the W integral in L-003); F2 `#3` touched by the edge optimum and owed a one-line unchanged row; F3 the changed script's default output adds one key (`W_target_MJ`) to `baseline_counterfactual.json`, every number identical, the committed file never overwritten; F4 (b) met on the written-attribution clause with the residual bounded, no formal seam negative (the caveat lines and the queued author query are the record); F5 the two § Question imprecisions, a dated amendment if the owner wants; F6 the second-scale evaluation preceded the checkpoint pass (the gated rows waited), a scoping constraint. Deposited verbatim at `evidence/round1_review.md`.
- **Checks:** every cited ref resolves; every number recounted from both window CSVs, both baseline JSONs and the attribution JSONs and reproduced, and 518.31 MJ re-derived independently with the reviewer's own reactivity and quadrature; the two registrations' equation images and the Stellaris pages 8–10 and 32 read and found to state what the T-001 return says; strategy pursued as declared, assumptions (1)–(3) held and (4) failed in letter as reported, the abandonment condition not met (with the residual's reach stated); T-001 and T-002 inside scope, the merge the same act as the scope's "rebase" for its purpose, the reproduction script inside T-001's return-writing; zero retries; the checkpoint cap respected with no submission edited in place (per-commit numstat all additions); the six rows moved or honestly said they did not, `#1`'s class right, the "surfaced, not re-ruled" sentence a surfacing; no voiding mutation, the pin's diff-stat empty; batteries `tests/study/test_records.py` 25, goal-contract 28 / 1 (owner-ruled), `tests/models` 48 / 13, `tests/study` 362 / 1 (7 min 31 s, sealed env); § Answered when (a) met and ratified, (b) met, (c) the owner's with trigger 4 the right derivation; no reserved gate crossed.
- **Learning delta:** L-001, L-002, L-003 accepted with corrections; appended to `learnings.md` in the reviewer's corrected forms at this commit.
- **Constraints carried forward:** eleven, in the review § Constraints carried forward — the W basis stated on every sustainment or driven-set reading; 504.65 never a target and the W definition always cited as Lion 2021 / 2023's; the ash-shape candidate owner-gated with its scope shape named; "whichever of merge or rebase keeps every cited sha valid"; a parallel checkpoint may spend a revision; a second evaluation scoped as evidence of the reading; default outputs byte-identical or disclosed; every touched id gets a row; the clean-room screen; the `wall-and-heating` constraints; the sealed env for `tests/study` from the worktree.
- **Next:** **close the goal after the owner rules § Answered when (c)** — the reviewer's recommendation is (ii), the minted ash-profile item, with two caveats (the verdict is not settled by it — violated by 1.4 MW at the paper's rules, inside the paper's own 2.7 % residual; and it costs a new pin and the restatement of every committed study), and (i) with the re-grade as the honest alternative if the pin is to stay stable. Round 1 of 6; no round 2 under this contract.
- **The round agent's acts on the review:** the learnings appended; the one-line unchanged row under `20260904-wall-and-heating#3` landed (F2; the `wall-and-heating` precedent of F4 rows after the review); F3 recorded, the script not changed (a change to a cited artifact after the round's close is not the round's to make; constraint 7 carries it); F5 left to the owner.

### Stop — 2026-09-05

- **Kind:** `owner gate` — § Answered when (c), the stored-energy basis the model carries.
- **What is true on disk:** branch `goal/stored-energy-basis` (worktree `../fusion-tea-stored-energy-basis`), containing `feat/demo-maturation` at `e0f66f7a` by merge; `goal.md` `grounded`; round 1 closed on trigger 4 (`1d0a988a`) and reviewed `FINDINGS` (this commit); `learnings.md` L-001..L-003; seven joined rows in `DISCOVERY_LOG.md` (six at `0964e9d3`, `#3` at this commit), `test_records.py` passing; two sources registered and the REQ-W-01 run committed (`d4059ef1`); nothing under `models/`, the generated package or the manifest changed; the pin `c1b0f0d1…` stands; `.project/CURRENT_WORK.md` in the primary checkout carries an uncommitted pointer (the owner's file).
- **What the owner must see:** (1) **the (c) ruling** — (i) keep the present profile family, disclosed as a sourced ~6 % over-count on the helium-ash shape (a trail-only answer; the pin and its studies stay); (ii) mint a model item through the modelling PM for the ash profile shape from the paper's own A.5 rule with electrons by quasi-neutrality (sourced; ~6 % of W; the baseline at about 51 MW against 50, still violated by 1.4 MW; a new pin; every committed study restated) — the reviewer's and the round agent's recommendation, with the caveats above; (iii) a sourced alternative — none exists; the printed 504.65 MJ is not a target under any reading. (2) **The re-grade** of `priced-levers/goal.md:52`, `wall-and-heating/goal.md:52` and `operating-point-closure` L-001's implication line on L-002 / L-003 — keep the never-tune line; the gap is a sourced ~6 % on the ash shape, not "+9.2 % against the printed point". (3) **The fifth condition on `wall-and-heating`'s closed (b)(ii)** — the owner's pen, by a dated amendment to L-009's Scope line or to that trail's § Goal close, citing the `#1` row at `0964e9d3`. (4) **The author query** to J. Lion (a person's act; the questions verbatim in the run's `process_log.md`) — send, or let the 2.7 % and the β row stand as the paper's. (5) Optional: a dated amendment to § Question for the two imprecisions (F5).
- **Resume at:** `GOAL_RUNBOOK.md` § Opening and closing a round — the owner's close per § Close rule after (c), or, if (ii) is taken, a model item through the modelling PM and a restating study under a new or re-grounded goal (the review's recommendation), not a round here.

### Owner ruling — 2026-09-05 — § Answered when (c)

Owner present in session. The round-1 review's close packet item 1 was put to the owner in plain language (fix the ash profile, or leave the model and footnote the delta), with the trust question underneath it answered (the paper holds up on everything except its stored-energy row; after the fix the last 2.7 % is inside what the paper prints, so the design point's sustainment is undetermined within the source's own precision).

- **The ruling** — `[OWNER-VERBATIM 2026-09-05]` *"ok I agree with next steps. we should fix the ash profile (and make sure this scales up for larger stellarators). and I don't want to add the footnote."*
- **Read as:** option (ii). The model changes: the helium-ash profile shape in the pressure integral follows the paper's own rule (the ash from the fixed particle-to-energy confinement ratio applied to the fusion-rate profile), with the electrons closed by quasi-neutrality — a minted work item through the modelling PM, per § Answered when (c). Option (i), keep the family and footnote the delta, is **rejected** `[OWNER]`.
- **New constraint** `[OWNER-VERBATIM 2026-09-05]` *"make sure this scales up for larger stellarators"*: the ash profile is computed from the rule at any geometry, never bound as an exponent digitized at point A, and the item's design and the restating study show the shape holds across the geometry window the committed studies sweep (R 11.2–17.2, a 1.3–2.2). Recorded in `goal.md` § Amendment 2026-09-05 (b).
- **The "next steps" the owner agreed to**, as the round agent put them: fix the ash profile; keep the Stellaris paper as the reference design point; record the design point's sustainment as undetermined within the source's own precision — on the boundary — rather than "the paper is wrong" or "the model is 90 MW off". `[AGENT] (ratified by owner 2026-09-05, "I agree with next steps")` — a ratified recommendation, challenged by re-deriving against the round-1 evidence, not an owner-originated ruling.
- **Not ruled this sitting, carried:** the re-grade of `priced-levers/goal.md:52`, `wall-and-heating/goal.md:52` and `operating-point-closure` L-001's line (packet item 2); the fifth condition on `wall-and-heating`'s closed (b)(ii) (item 3); the author query to J. Lion (item 4); the § Question amendment (F5).
- **What changed:** this entry; `goal.md` § Amendment 2026-09-05 (b). § Answered when (c) is ruled; the item is not yet minted — the next round's first act. The goal stays open: the owner asked for a handoff to the next `run-goal` agent, so the fresh review's close recommendation is superseded by the owner's ruling to continue with the fix here (round 2 of 6), unless the next agent and the owner prefer a new goal.

## Round 2 — ash-profile-from-the-rule

### Strategy revision — 2026-09-05

`[AGENT — the round-2 agent, from the handoff of 2026-09-05. Subject settled by the owner: "we should fix the ash profile (and make sure this scales up for larger stellarators). and I don't want to add the footnote." [OWNER-VERBATIM 2026-09-05, trail § Owner ruling]; the shape below is the round agent's. The round-1 reviewer preferred a new goal for the item and its restating study (evidence/round1_review.md § Recommendation); the owner instead asked for a run-goal handoff, § Answered when (c) names the minted item, and goal.md § Amendment (b) already places the item's design and restating study under this goal's invariants, so round 2 runs here — a new goal would need owner-present grounding for the same work. Recorded, not hidden; the owner can redirect at this boundary.]`

- **Approach:** land the ruling as one model item through the modelling PM, then one pin, then one restating study. The item: the helium-ash profile shape in the pressure integral follows the paper's own rule — n_He(ρ) ∝ n_D(ρ)·n_T(ρ)·⟨σv⟩(T_i(ρ)) with τ* = 8 τ_E uniform in ρ (Stellaris Appendix A, A.5–A.6; p. 9 "helium ash profiles are obtained using a fixed ratio of particle-to-energy confinement time") — with the electrons closed by quasi-neutrality, n_e = n_D + n_T + 2 n_He, and the ash amount (the peak fixed point from A.5 × A.6 the model already computes) unchanged. The pressure integral has two consumers in the plant — W_th in 'Plasma Sustainment' (`mfe_plasma_sustainment.sysml:25`) and ⟨p⟩ in 'Volume-Averaged Beta' (`mfe_plasma_scaling.sysml:411`) — and the electron profile has three more readers inside the sustainment calc: the ISS04 line-averaged density, the bremsstrahlung and line-radiation integrals, and the Albajar synchrotron profile parameter. The increment closes the plant on one electron profile and one pressure integral: every reader takes the profile the rule gives at that geometry, never an exponent digitized at point A. The W integral form (1.5 ⟨p⟩ V over the ρ dρ element), the fuel and temperature exponents and the peak values do not change (goal.md § Amendment (b)); nothing is fitted toward 504.65 MJ or any printed number (L-002; review constraint 2). Then `scripts/integrate.py` for the round's one pin. Then the restating study: the `20260904-wall-and-heating` window — its four arms, 6,311 points at `c1b0f0d1…` — re-executed at the new pin under the study runbook, its counts and its design-column verdict at 100 and 220 MW read beside the committed record and the two constant-scale counterfactuals (0.915 and 0.940), and the shape's effect on W read across the geometry window, not only at point A.
- **Assumptions:** (1) the rule applied pointwise with τ* uniform in ρ is the paper's own reading — corroborated at point A by Fig. 16(a) and by reproducing the printed n_e0 (5.04 vs 5.06) and ⟨n_e⟩ (3.16 vs 3.17) (L-002, L-003); a ρ-dependent τ* would be a new source, not a fit. (2) The peak fixed point stays the ash amount: with τ* uniform the rule makes the profile n_He0 × [the fusion-rate shape], so the peak and the shape are one equation, not two. (3) The change is a profile-family change inside the sustainment and beta calcs plus a manual-stage interface change; the heating, wall, geometry and magnet chains do not change in form, and every downstream move — τ_E, the converged ash and fuel peaks, p_fus, the wall load, β, CAS72, LCOE — is the closure re-closing, disclosed and never tuned. (4) The expected direction, from round-1 evidence and not a claim: W at the printed peaks 551.3 → about 524.5 MJ at the model's exponents (`attribution_sourced_definition.json` row "model exponents but A.5 ash and QN electrons"); with the closure live the baseline lands near the 0.940 counterfactual (51.4 MW required, violated by about 1.4 MW; the wall satisfied) but not on it, because the line-averaged density and the radiation move with the electron profile while the counterfactual scaled W alone (`NOTES.md` § 6) — the number is the design prototype's and then the pin's. (5) No multiplier re-reads a committed sustainment-dependent column (W_th, τ_E, p_aux_required, the ash and fuel peaks, p_fus, the wall load, β, the verdicts, the driven and ignited sets); the honest re-read is a re-execution, which is what the round's study is; the seven earlier records stand at their own pins as already-superseded records (the MR-WI041-11 shape). (6) The design can show the shape's scaling before any study: the effective ash exponent and W's move at the corners of the geometry window (R 11.2–17.2, a 1.3–2.2) across the T_i0 and n_e0 ranges are oracle-side arithmetic from the rule; the study then measures it on the package. (7) Both batteries green at the new package, with the census, the six known-answer fixtures and the manifest's expected verdicts re-derived from live runs and their derivations beside them.
- **Abandonment conditions:** the rule cannot be expressed without a defaulted number (surfaced with options; the owner rules); the honest form needs the W integral form, the fuel or temperature exponents, or the peak fixed point to change (a reserved gate: surface, park the dependent conclusions, owner); the pinned codegen cannot carry the form past the retry cap (a `PREREQUISITE` naming the seam, never a quiet repair); the design's window check shows the shape from the rule does not hold across the geometry window (a premise surprise against the owner's scaling line — surfaced, never resolved silently); the owner redirects at the boundary; a fresh gate session cannot be obtained (the round waits).
- **Intended model increment:** one work item under the MFE Cost Modeling epic, minted at the round's first act through `agentic-mbse pm add-item` and carried through spec → design → plan → implement as one task: in 'Plasma Sustainment' the ash profile from the rule, the electrons by quasi-neutrality, W_th over the unchanged 1.5 ⟨p⟩ V form, the line-averaged density and the radiation integrals over the derived electron profile, the ash fixed point unchanged in form (its ISS04 prefactor now inside the iteration, since the line average depends on the ash); 'Volume-Averaged Beta' consuming the same ⟨p⟩ — one pressure integral, two consumers; `alpha_n_e`'s fate decided explicitly (`stellarator_plant.sysml:602`, bound from the printed vol-av/peak pair; review constraint 3) — the design's first decision, with the expected outcome that it retires as an entry point and a computed effective exponent replaces it as the printed-pair cross-check and the Albajar parameter, because at point A the derived profile reproduces the pair it was bound from; the oracle mirroring the chain; SV rows for point-A identity (the rule reproduces the printed n_e0 and ⟨n_e⟩; W at the printed peaks at the model's exponents) and for the disclosed baseline moves; the restatement of the eight committed studies written before the regeneration commit; the design's geometry-window table. Nothing at the heating chain, the wall chain, the geometry, the magnet, the exponents or the peaks.
- **Intended study question:** at the round's pin, what does the `20260904-wall-and-heating` window say when the model integrates the paper's own ash profile — the feasible / ignited / driven counts and the design-column verdict at 100 and 220 MW at R 12.7, a 1.3, beside the committed record and the two counterfactual scales — and does the ash shape's effect on W hold across the geometry window, read as the effective ash exponent and W's move at every (R, a, T, n) the arms visit? The four arms as committed, at the new pin; the pre-execution critique, the fresh administrator and the C-001 checkpoint are the study runbook's own gates; every disposition this goal owes on `20260904-wall-and-heating#1` (the model fix discharged), `#2`, `#4`, `#6`, `#8` and any id the new record sights.

No future task list. The first task is the model item, because the ruling names it and the pin and the study both wait on it; after it returns, tasks are chosen from evidence.

### T-001 scope (round 2)

- **Objective:** land the owner's (c) ruling as model objects — one work item, minted under the MFE Cost Modeling epic and carried through spec → design → plan → implement: the helium-ash profile shape in the pressure integral from the paper's own rule (n_He(ρ) ∝ n_D n_T ⟨σv⟩(T_i(ρ)), τ* = 8 τ_E uniform in ρ), the electrons closed by quasi-neutrality, the ash amount (the peak fixed point) unchanged; the plant closed on one electron profile and one pressure integral (sustainment and beta); `alpha_n_e`'s fate decided explicitly; the oracle mirroring the chain; the restatement of the eight committed studies written before regeneration; SV rows for point-A identity and the disclosed baseline moves; and the design showing the shape computed from the rule across the geometry window (R 11.2–17.2, a 1.3–2.2) — never a point-A exponent.
- **Why now:** the owner ruled (c) (§ Owner ruling — 2026-09-05; `goal.md` § Amendment (b)); the item is the ruling's own vehicle ("a minted work item through the modelling PM"); the pin and the restating study both wait on it. Discovery row `20260904-wall-and-heating#1` stands `model fix` — open, not minted, owner-gated (its 2026-09-05 row); this item is the fix it names.
- **Scope:** authorized — `agentic-mbse pm add-item` run from this worktree on the primary checkout's interpreter (the documented exception), so the backlog write lands on this branch; the four modelling-PM stages with their validation levels; edits under `models/library/analyses/mfe_plasma_sustainment.sysml`, `models/library/analyses/mfe_plasma_scaling.sysml` ('Volume-Averaged Beta'), `models/designs/generic_mfe/mfe_plant.sysml`, `models/designs/stellarator_09/stellarator_plant.sysml` and their byte-identical twins under `exploration/stellarator_e2e/models/`; the handwritten sustainment impl (and beta's, if its interface changes) under `exploration/stellarator_e2e/generated/handwritten/`; regeneration through the pinned codegen and the snapshot, manifest, census and fixture re-derivation it forces; the oracle (`verify_stellaris.py`, `studies/oracle_entry.py`); SV and traceability rows; the item's section in `work/backlog/epic-mfe-cost-modeling.md`. Excluded — the W integral form, the fuel and temperature exponents, the peak values and the form of the peak fixed point (reserved: surface, park, owner); any fit toward 504.65 MJ or any printed number; the heating, wall, geometry and magnet chains; executing a study or promoting a pin (later tasks'); `/_my_quick_edit` under `models/`; the discovery log (the `#1` row lands with the study's reading, not here); every artifact barred by `knowledge/holdout/aries-cs/PROTOCOL.md` §3.
- **Inputs:** `goal.md` (§ Invariants with amendment (b)'s scaling line and amendment (c)'s Package reading; § Reserved gates). `learnings.md` L-002, L-003. `evidence/w_counterfactual/attribution_sourced_definition.py` / `.json` (`5cc30ac0`) — the reference calculation the design reuses; `attribution_arithmetic.json` (`3687d2f6`) — the quasi-neutrality rows. The registrations at `d4059ef1` (the W definition cited as Lion 2021 / 2023's, the Stellaris Appendix A ancestry the link — review constraint 2). Review constraints 3, 7, 8, 11. `work/completed/20260905_WI-041_source-anchored-wall-load-fence/plan.md` § MR-WI041-11 (the restatement's shape) and its two recorded traps (the manual-stage re-stencil; the pathspec commit). Narrower constraints, five: (i) the restatement is written and committed before any regenerated byte (commit A before commit B); (ii) no committed sustainment-dependent column re-reads by a multiplier — the restatement says so per study and names the round's re-execution as the honest re-read; (iii) the manifest's expected verdicts and the six fixtures are re-derived from live runs with their derivations beside them, never patched to match; (iv) the design's geometry-window table is written from the rule at the window's corners before any regeneration, so the owner's scaling line is checked on arithmetic before it is checked on the package; (v) the Stellaris pages (p. 9, Appendix A p. 32) are read from the rendered PDF or the page images, never the extraction.
- **Done when:** the item's model objects exist; the package regenerates and executes at the baseline with every non-sustainment quantity unchanged in form and the moves in W_th, τ_E, the ash and fuel peaks, p_fus, the wall load, β, CAS72 and LCOE declared and derived; the independent oracle reproduces them; `tests/models` and `tests/study` green with every delta derived; the restatement names all eight committed studies; the design's window table shows the shape computed from the rule at every corner — **or** a bounded negative names the quantity that cannot be expressed without a default.
- **Stop when:** a discovered prerequisite (the pinned codegen refuses the form past the retry cap; the handwritten stage cannot carry the profile), a strategy blocker (the honest form needs the W form, the exponents or the peak fixed point to change), an owner gate, or a declared limit.

### T-001 start — 2026-09-05 (round 2)

Task T-001 under § T-001 scope (round 2): mint the ash-profile work item and carry it through spec → design → plan → implement. Native target: `work/BACKLOG.md` (a new item under the MFE Cost Modeling epic) and `work/active/WI-0NN_<slug>/` (`spec.md`, `design.md`, `plan.md` with its restatement section); model edits under `models/library/analyses/mfe_plasma_sustainment.sysml`, `mfe_plasma_scaling.sysml`, `models/designs/generic_mfe/mfe_plant.sysml`, `models/designs/stellarator_09/stellarator_plant.sysml` and their twins; the handwritten sustainment impl; the oracle; the regenerated package with its pin files. Expected artifacts: the work-item directory with its stage records, the changed model files, the regenerated package, green batteries, SV rows.

### T-001 return — 2026-09-05 (round 2)

- **Outcome:** `COMPLETE`. WI-042 carried through spec → design → plan → implement: the helium-ash profile in the pressure integral follows the source's own rule (Eq. A.5 pointwise, τ* uniform in ρ — the fusion-rate shape scaled to the converged peak), the electrons close by quasi-neutrality pointwise, W and β read one volume-averaged pressure, every reader of the electron profile inside the chain takes the derived one, the point-A exponent `alpha_n_e` is retired as an entry point, and the shape is computed from the rule at every lever point — never bound. The model regenerates through the pinned codegen (twice, the manual-stage recovery) and executes at the baseline; the independent oracle reproduces every sustainment channel bit-exact; both batteries are green; every committed study is restated before regeneration.
- **Evidence:** `work/active/WI-042_sourced-helium-ash-profile/` — `spec.md` (MR-WI042-1..15; `17d64bef`), `design.md` (D1–D8, the prototype report, approval; `ae6c327e`), `plan.md` (six phases ticked, § MR-WI042-14 restatement written before regeneration, the phase records, § Spec success criteria, verified; `ae6c327e`, `9ac167b8`, this commit), `prototype/` (the oracle-side reference calculation, the window corners, the point-A identity). Commits: `17d64bef` (mint and spec), **`ae6c327e` (commit A: the model edits in both trees and the restatement, before any regenerated byte)**, **`9ac167b8` (commit B: the regenerated package, the impl, the oracle, the re-pin, the fixtures, the SV rows)**, `0c20e373` (one verify test restated). Model: `models/library/analyses/mfe_plasma_sustainment.sysml`, `mfe_plasma_scaling.sysml`, `models/designs/generic_mfe/mfe_plant.sysml`, `models/designs/stellarator_09/stellarator_plant.sysml` and their twins. The executed baseline at the new package: `evidence/round2_T-001_baseline_result.json` and `_package_identity.json` (executable `8ac14fdf3745…`; nine verdicts all satisfied); `evidence/round2_T-001_run_stellaris_single_output.txt` (verdict parity, bit-exact vs the oracle on every channel, the W–β identity, the guards; exit 0); `evidence/round2_T-001_perturbations.json` (T_i0 → 18 keV; τ*/τ_E → 4). SV-053..056 `passing`, SV-036/042/043 amended (`modeling_project/VALIDATION_MATRIX.md`). Batteries: `tests/models` 48 passed / 13 skipped (at `9ac167b8`); `tests/study` **363 passed / 1 skipped at `0c20e373`**, the run of record (the pre-commit run on the uncommitted tree: 13 failed / 16 errors, all the git-clean gate but two restatement sites — the single-point runner's fourth gate family and the operand-binding test's pinned headline, both restated in commit B; the run at `9ac167b8`: 362 / 1 / 1, the planted-mismatch verify test, restated at `0c20e373`). The reserved bindings (the fuel and temperature exponents, the peaks, the transport facts, the W form) are untouched between `f49da0f0` and `0c20e373`.
- **Reading.**
  - **The rule lands as the paper states it, computed everywhere.** The ash profile is n_He0 × S(ρ), S the fusion-rate shape (the same Bosch–Hale reactivity and 200,000-interval grid that give p_fus); the electrons are the ions' charge; n̄ (ISS04's density), the bremsstrahlung and line-radiation integrals and the synchrotron fit's profile parameter all take the derived profile; the ISS04 prefactor sits inside the fixed-point iteration because the line average now depends on the ash. At point A the derived profile reproduces the pair the retired exponent was bound from (`alpha_n_e_eff` 0.5953 vs 0.596; ⟨n_e⟩ 3.172e20 vs the printed 3.17), and the impl's own helpers at the printed peaks reproduce the round-1 reference rows to the last digit (524.523 MJ at the model's exponents, 518.308 at the paper's; SV-053).
  - **The baseline's two violated verdicts both flip to satisfied — disclosed, never tuned, and the design point stays on the boundary.** W 551.444 → **519.914 MJ**, τ_E 1.450 → 1.557 s, n̄ 38.34 → 37.78, more ash (6.04e19) and less fuel at the peak, p_fus 2725.4 → 2652.6 MW, p_rad 228.6 → 219.7, and the required sustained coupled heating **90.605 → 49.080 MW against 50 coupled: `sustainment_ok` satisfied by 0.92 MW**. That margin is about 0.2 % of W, inside the source's own 2.7 % residual on its printed stored energy (L-002), so what the model now says at the paper's design point is what round 1 said it would say once the ash shape was the paper's: **undetermined within the source's precision** — not "the machine needs 90 MW", not "feasible". The wall peak reads 3.979 against 4.05 (`wall_load_ok` satisfied) because the fusion power fell 2.7 %; LCOE rises 313.513 → 322.318 (net power −3.7 %, CAS72 −3.7 %); β from the one ⟨p⟩ is 2.53 %. The printed-point cross-checks trade as the design predicted: W +9.2 % → +3.0 %, p_fus +0.9 % → −1.8 %, ⟨n_e⟩ +0.1 %, τ_E −0.1 % → +6.6 % (the paper's own closed form at the lower W; the printed (W, τ_E) pair does not close the paper's own balance), n_He0 +3.6 % → +8 %. Nothing near either fence was bound or adjusted (SV-054; the plan's restatement (b)–(c)).
  - **The strategy's expected direction held; its number did not, and the difference is instructive.** Assumption (4) expected the baseline "near the 0.940 counterfactual (51.4 MW, violated by 1.4) but not on it, because the line-averaged density and the radiation move with the electron profile". They moved the other way from the counterfactual's guess: the derived line average is lower and the radiation lower, τ_E higher, and the 1.4 MW violation became a 0.9 MW satisfaction. The sign of the verdict at the design point was never decidable from W's basis alone (L-001); the closure decided it, by less than the source's precision. The demo statement the owner ratified — "on the boundary" — is what the package now says.
  - **Independence, four ways.** The oracle's `_sustainment` was rewritten from the design's equations and agrees bit-exact on every sustainment channel, the four new ones included; under T_i0 → 18 keV the ash shape's exponent moves 4.052 → 3.661 and W, τ_E, p_fus and the verdicts move in model and oracle together; under τ*/τ_E → 4 the ash amount halves and the shape does not move at all (4.052), which is what "the peak and the shape are one equation" predicts; the W–β identity holds to 0.0 from the package's own channels (SV-055).
  - **The shape scales — the owner's second clause.** The effective ash exponent is 4.73 at 10 keV, 4.05 at 14.63, 3.46 at 20 keV, a function of T_i0 through the reactivity and of nothing bound at point A; across the geometry window the correction to W (new / old) runs 0.99 at (R 17.2, a 1.3) to 0.77 at (R 11.2, a 2.2), following the ash fraction the rule gives at each geometry — the old family's over-count was largest exactly where the ash fraction is largest (SV-056; `prototype/window_corners.json`). The closure's fuel guard fires at the same window corner under both chains. The study measures both on the package at every executed point.
  - **The restatement.** No committed sustainment-dependent column re-reads by a multiplier — every one re-closes nonlinearly — so the honest re-read of `20260904-wall-and-heating` is the round's re-execution of its four arms at the new pin; the seven earlier records stand at their pins as already-superseded records; the closure's validity edge may move (row `#8`). The census is 207 → 205 exactly as predicted (`alpha_n_e`, `beta_calc__e_keV` retired; nothing minted); the six known-answer fixtures re-derive with the same modules fired and four more channels tainted on every sustainment-reaching axis; the manifest carries the new pin (indicator `ec984adc1572…`, semantic `c37fb58a2b19…`, executable `8ac14fdf3745…`) with both verdicts expected satisfied. One restatement site the plan's surface table missed was found while re-pinning — `studies/ANNEX.md` § Baseline pin, which still described the WI-039 state and which WI-041 had not restated either — and rewritten with the pin's history.
  - **What this does not yet establish:** the round's pin (the integration seam has not been run) and the `20260904-wall-and-heating` window at the new pin — the sustainment / wall / driven counts and the design-column verdict at 100 and 220 MW beside the two counterfactual scales are the study's, not this return's.
- **Decision.** Trigger: the bound electron exponent could be retired or rebound to a computed quantity. Decision and reason: **retired** (design D1) — a formal bound to something the calc computes itself would be the defect in another form; the printed vol-av/peak pair becomes the cross-check and a computed effective exponent replaces it as an output. Tier: `execution detail`. Decided by: the round agent. What changed: `mfe_plasma_sustainment.sysml`, `mfe_plant.sysml`, `stellarator_plant.sysml`, the census, `oracle_entry.py`.
- **Decision.** Trigger: the beta calc could keep its closed-form body with a bypass or read the sustainment calc's pressure. Decision and reason: **reads the one ⟨p⟩** (design D2) — the exact route admits no conditional, a second manual stage would be a second copy of the thing MR-WI042-4 forbids, and reading ⟨p⟩ makes W–β agreement true by construction; the cost is a non-manual calc's interface change and the beta test's formal list, restated. Tier: `execution detail`. Decided by: the round agent. What changed: `mfe_plasma_scaling.sysml`, `mfe_plant.sysml`, `tests/models/test_beta_peak_field.py`.
- **Decision.** Trigger: the first regeneration re-stencilled the sustainment impl to a stub and sealed the package with it (the recorded manual-stage trap). Decision and reason: **restore from the design, delete `handwritten/backup/`, regenerate a second time** — `Regenerated: 0`, the impl byte-identical, seal clean; no retry counted (the first regeneration exited 0 and did what it documents). Tier: `execution detail`. Decided by: the round agent. What changed: the impl, regenerated twice.
- **Decision.** Trigger: the verdict flips at the baseline could be read as "the design point is feasible" or as "on the boundary". Decision and reason: **on the boundary** — the 0.92 MW margin is inside the source's own 2.7 % residual (L-002) and L-001 already made the verdict a statement about the basis; the reading is written at every claim site (the instance's `sustainment_ok` comment, SV-054, the restatement, the annex) and is the demo statement the owner ratified on 2026-09-05. Tier: `execution detail` (the owner's ratified next steps; no reserved gate crossed). Decided by: the round agent. What changed: the doc comments and rows named.
- **Decision.** Trigger: three study tests and one operator document carried the previous baseline as literals or as a plant direction — the single-point runner's gate count, the pinned headline, the planted-mismatch plant, and the annex's baseline paragraph. Decision and reason: **each restated as a derived statement of the new baseline with the reason in its docstring or text, none patched to pass** (the WI-041 precedent for the plant's direction, reversed the other way); the annex site is recorded in the plan as one the surface table missed. Tier: `execution detail`. Decided by: the round agent. What changed: `tests/study/test_single_point_gate.py`, `test_operand_bindings.py`, `test_verify.py` (`9ac167b8`, `0c20e373`); `studies/ANNEX.md` (`9ac167b8`).
- **Decision.** Trigger: the manifest writer's first pass carried the fingerprint reader's file entries in the tool's own shape, which the manifest's validator refused. Decision and reason: **corrected to the schema's shape before anything left the machine and validated with `manifest.load`**; recorded as a re-pin trap in memory. Tier: `execution detail`. Decided by: the round agent. What changed: `studies/manifest.json` (`9ac167b8`).
- **Next task, chosen from this evidence:** the round's one promoted pin — `scripts/integrate.py` against `work/active/WI-042_sourced-helium-ash-profile@<the commit carrying this return>` with the three fingerprints the manifest now carries and the teax revision `744745f8…`.

### T-002 scope (round 2)

- **Objective:** obtain the round's one promoted pin — invoke the native integration seam against the landed WI-042 work item and read back exactly one `CANDIDATE` pin with its fingerprints, or a named `BLOCKER`.
- **Why now:** T-001 returned `COMPLETE` with the package regenerated, re-pinned and both batteries green at `0c20e373`; the restating study needs a promoted pin before any point runs, and a round has at most one (`GOAL_RUNBOOK.md` § Opening and closing a round). `goal.md` § Amendment (c) reads the Package invariant as binding this goal's own pen: the pin is the seam's, cited by the trail.
- **Scope:** authorized — `scripts/integrate.py` as documented (`docs/integration_seam_operator_guide.md`, the full invocation), run from this worktree on the primary checkout's interpreter (gate 1a reads the sealed wheels against the running interpreter's site-packages), read-only over the repository, writing only to a scratch `--out-dir` outside the package; the deposit of its return documents under `evidence/`. Excluded — any repair of the seam (a `PREREQUISITE` return names it), any edit under `models/`, `exploration/` or the work item, any study execution.
- **Inputs:** `goal.md` § Invariants (one pin per round; every comparison the study makes against the committed record is by case id at the record's own pin `c1b0f0d1…`, the new pin being the study's). The audited work `work/active/WI-042_sourced-helium-ash-profile@b75362c1` (the plan ticked and complete at that commit). The manifest's three fingerprints as re-pinned in `9ac167b8`: indicator `ec984adc1572792c0d64d26776621ebb154d5b62187a1647e386abd3ff9eb096`, semantic `c37fb58a2b19973d1698aeace5184890107efdf0136d20a502ffe88a0eb956a4`, executable `8ac14fdf3745be9682723902c11a2245f423f2fc6d5bc4726b7fed3b09b5c18e`; the teax revision the package ran under, `744745f895677f3344b9884627369a6a47ed987f` (unchanged since WI-041). `work/orchestration/goals/wall-and-heating/evidence/round2_T-003_integration_return.json` — the precedent invocation. Narrower constraint: the seam is a fixed-point proof and performs nothing — a refusal at the regeneration, census, snapshot or manifest gate is the model item's unfinished work (`PREREQUISITE`), not a retry.
- **Done when:** the seam returns `CANDIDATE` with the pin and fingerprints deposited as evidence, or a named `BLOCKER` recorded with its condition.
- **Stop when:** a discovered prerequisite (a seam refusal that names the model item), a strategy blocker, an owner gate, or a declared limit.

### T-002 start — 2026-09-05 (round 2)

Task T-002 under § T-002 scope (round 2): run `scripts/integrate.py` with `--audited-work work/active/WI-042_sourced-helium-ash-profile@b75362c1`, the tracked models root, package, manifest, known-answer groups and census, the three expected fingerprints and the expected teax revision, into a scratch out-dir. Native target: none mutated. Expected artifacts: the seam's return JSON and its sibling documents deposited as `evidence/round2_T-002_*.json`, and the T-002 return.

### T-002 return — 2026-09-05 (round 2)

- **Outcome:** `COMPLETE`. The seam returned exactly one `CANDIDATE`, all ten gates passing on the first run — the round's one promoted pin.
- **Evidence:** `evidence/round2_T-002_integration_return.json` (the return of record) and its siblings `round2_T-002_package_identity.json`, `round2_T-002_preflight_results.json` (six of six), `round2_T-002_verification_summary.json` (oracle parity and every verdict re-derived over the seam's own baseline store), `round2_T-002_baseline_result.json`, `round2_T-002_clean.json`, `round2_T-002_recaptured.snapshot.json`. Audited work `work/active/WI-042_sourced-helium-ash-profile@b75362c1`; run from this worktree on the primary checkout's interpreter.
- **The pin:** `stellarator_tea`, indicator-input **`ec984adc1572792c0d64d26776621ebb154d5b62187a1647e386abd3ff9eb096`**, semantic `c37fb58a2b19973d1698aeace5184890107efdf0136d20a502ffe88a0eb956a4`, executable `8ac14fdf3745be9682723902c11a2245f423f2fc6d5bc4726b7fed3b09b5c18e`, teax `744745f895677f3344b9884627369a6a47ed987f` (unchanged). Baseline at the pin: LCOE 322.31843948570247; W_th 519.914 MJ; required sustained coupled heating 49.080 MW against 50; wall peak 3.978845; **nine verdicts, all satisfied** — `sustainment_ok` and `wall_load_ok` flipped from the WI-041 pin's violated by the sourced ash profile, disclosed (T-001). Every comparison the study makes against the committed `20260904-wall-and-heating` record is by case id at that record's own pin `c1b0f0d1…` (`goal.md` § Invariants; § Amendment (c)); every number the study produces is at this one.
- **Reading.** The gates say what T-001 claimed: regeneration on the pin rewrote no byte outside `handwritten/` and the 74 handwritten files are byte-identical (the second-regeneration seal of the sustainment impl held); the snapshot recaptures byte-identically and the 205 entry points re-derive to the census as bound; the manifest's pin recomputes over the live package; preflight's six gates pass; oracle parity holds and every verdict re-derives, the two flipped ones included; the lineage is the pair the request named. The manifest gate's standing note (`assert_read_set_covered` not run, filed) is the seam's own, unchanged since round 1 of `wall-and-heating`.
- **Decision.** None at goal level; the seam performs nothing and decided nothing. Tier: n/a.
- **Next task, chosen from this evidence:** the restating study at this pin under the study runbook — the `20260904-wall-and-heating` window's four arms re-executed (`arm-fence-p100`, `arm-search-p220`, `arm-reread-p220`, `arm-transect-ash`; 6,376 proposals as committed, the pre-screen re-run at the new chain), the pre-execution critique spawned before any point runs, the effective ash exponent and W carried per point, the counts and the design-column verdict at 100 and 220 MW read beside the committed record and the two counterfactual scales, and the dispositions this goal owes on `20260904-wall-and-heating#1`, `#2`, `#4`, `#6`, `#8` and any id the new record sights.
