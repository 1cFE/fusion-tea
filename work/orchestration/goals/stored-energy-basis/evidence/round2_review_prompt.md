# Round 2 fresh review — spawn prompt (deposited 2026-09-06 before the review session ran; verbatim)

You are a fresh reviewer for goal round 2 (`ash-profile-from-the-rule`) of `work/orchestration/goals/stored-energy-basis/` in the worktree `/home/reid/1cfe/fusion-tea-stored-energy-basis` (branch `goal/stored-energy-basis`; the primary checkout at `/home/reid/1cfe/fusion-tea` is on another branch and is not the subject). You did not author any part of this goal, its work item WI-042, its pin, its study, its synthesis, its checkpoint, or its result; you inherit no context; work only from the repository. NEVER read anything under `knowledge/holdout/` (a quarantined clean room; `knowledge/holdout/aries-cs/PROTOCOL.md` §3 bars it). The worktree has no virtual environment of its own: for every Python invocation use the primary checkout's interpreter `/home/reid/1cfe/fusion-tea/.venv/bin/python` (never bare `python`, never `uv run` from the worktree). For the test batteries use exactly, from the worktree root: `set -a; source ~/1cfe/agentic-mbse/.env; source /home/reid/1cfe/fusion-tea/.venv/integration.env; set +a; unset PYTHONPATH; rm -rf .integration_workspace; /home/reid/1cfe/fusion-tea/.venv/bin/python -m pytest <target> -q` with the Bash tool's timeout set to 600000 ms (`tests/models` takes about a minute; `tests/study` takes about 8 minutes and must run WITHOUT any `PYTHONPATH` — one of its tests is a negative test on that; `tests/study/test_records.py` and `tests/orchestration/test_goal_contract.py` take seconds). For package or oracle runs outside the batteries set `PYTHONPATH=$HOME/1cfe/teax/packages/teax-simkit:$PWD/exploration/stellarator_e2e/pkg`. Do not edit any file. Do not run any git command that changes state (no add, commit, stash, checkout, reset, merge). The working tree is clean. `tests/orchestration/test_goal_contract.py` has one failing test (`test_narratives_are_separate_from_the_goal_contract`) on `work/orchestration/goals/wall-and-heating/trail.md`, a line the owner ruled to stand on 2026-09-05; that failure is known, disclosed in the round result, and not this round's.

The round's commits, in order, all on this branch: `f49da0f0` (the owner's (c) ruling; the model state before the round), `b7baaf41` (round 2 opened: the strategy revision), `17d64bef` (WI-042 minted and specced), `ae6c327e` (WI-042 commit A: the model edits in both trees and the § MR-WI042-14 restatement, before any regenerated byte), `9ac167b8` (WI-042 commit B: the regenerated package, the handwritten sustainment impl, the oracle, the re-pin, the fixtures, the SV rows), `0c20e373` (one verify test restated), `b75362c1` (T-001 return), `af75a749` (T-002: the pin through `scripts/integrate.py`), `5ab0452c` (the study's definition, intake, indicators, baseline, preflight and edge transects; the pre-execution critique and its prompt; the T-003 scope amendment), `dcc77967` (one path in `study.py` corrected before any point ran), `9a78b92c` (the pooled oracle pre-screen; the administrator prompt, recount script and snapshot builder deposited), `0b181b9c` (the committed record and its eight first-sighting rows), `700bfe6d` (the fresh administrator's synthesis, the record's Addendum, the proposed dispositions r1, the checkpoint prompt), `2d402aea` (the dispositions r2 with every change marked `[r2]`; the C-001.r1 entry), `53eb5671` (the C-001.r2 entry; ten joined disposition rows), `830497b8` (the T-003 return and the Round 2 result). Confirm the sequence yourself with `git log`.

Read, in order: (1) `work/orchestration/GOAL_RUNBOOK.md` § The fresh review (your contract), § What "fresh" means, § The discovery log, § When a cited artifact moves, § Opening and closing a round, § Running one task, § The two checks are distinct, § Limits; (2) `goal.md` — § Question, § Answered when (read (a), (b) and (c) word by word), § Invariants, § Limits, § Reserved gates, § Close rule, and the three amendments (§ Amendment 2026-09-05 (Status), (b) (the owner's (c) ruling and the scaling line), (c) (the Package invariant read as the goal's own pen — `[AGENT]`, awaiting ratification, acted on by this round)); (3) `trail.md` end to end — round 2 runs from `## Round 2 — ash-profile-from-the-rule` through `## Round 2 result — 2026-09-06`, with the T-001..T-003 scope / start / return entries, the `### Amendment 2026-09-05` to the T-003 scope, and the `### Checkpoint C-001.r1` and `C-001.r2` entries in between; read § Round 1 result, § Round 1 review, § Stop — 2026-09-05 and § Owner ruling — 2026-09-05 for what round 2 inherited, `evidence/round1_review.md` § Constraints carried forward (the eleven constraints) and `learnings.md` L-001..L-003; (4) the evidence directory `evidence/round2_*`: the T-001 baseline result, package identity, perturbations and the single-point runner's output; the seven T-002 seam documents; the T-003 pre-execution critique, its prompt and its probes, the administrator prompt, the recount script, the snapshot builder, the proposed dispositions (r2, every change marked `[r2]`; r1 survives at `700bfe6d`), the checkpoint prompt; (5) the WI-042 item record `work/active/WI-042_sourced-helium-ash-profile/` (spec MR-WI042-1..15, design D1–D8 with its validation report, plan with its § MR-WI042-14 restatement, phase records and § Spec success criteria, `prototype/`) and the model text it changed (`models/library/analyses/mfe_plasma_sustainment.sysml` — the rewritten calc; `models/library/analyses/mfe_plasma_scaling.sysml` 'Volume-Averaged Beta'; `models/designs/generic_mfe/mfe_plant.sysml`; `models/designs/stellarator_09/stellarator_plant.sysml` — the referent block and the `sustainment_ok` comment), the handwritten impl `exploration/stellarator_e2e/generated/handwritten/mfe_plasma_sustainment/plasma_sustainment_impl.py`, the oracle (`exploration/stellarator_e2e/verify_stellaris.py` `_sustainment`; `exploration/stellarator_e2e/studies/oracle_entry.py`), and `modeling_project/VALIDATION_MATRIX.md` SV-053..056 and the amended SV-036 / 042 / 043; (6) the committed study `exploration/stellarator_e2e/studies/20260905-stored-energy-basis/` (`record.md` with its Addendum, `synthesis.md`, `snapshot.json`, `axes.json`, `indicators.json`, `study.py`, `scan.py`, `edges.py`, `results/` — `points.csv`, `oracle_operands.csv`, `excluded_points.csv`, `window_edges.json`, `baseline_result.json`, `package_identity.json`, `preflight_results.json`, `verification_summary.json`) and, for the joins, the committed predecessor `exploration/stellarator_e2e/studies/20260904-wall-and-heating/` at its own pin and the two counterfactual files under `evidence/w_counterfactual/`; (7) the `DISCOVERY_LOG.md` rows — the eight sightings `20260905-stored-energy-basis#1..#8` dated 2026-09-05 (`0b181b9c`) and the ten joined rows dated 2026-09-06 (`git show 53eb5671`); (8) the machine's own source, admissible: `knowledge/concept_research/09-qi-stellarator-hts/iter-01/sources/stellaris-design-details.md` at p. 9 (the ash rule sentence) and Appendix A (Eqs. A.5–A.6), and the page images the design says it read instead of the extracted text; the two Lion registrations at `d4059ef1` under `knowledge/sources/` for the W definition's provenance.

Your checks (all of them):

- **Every cited ref resolves** and says what the trail claims it says. Recount the numbers you rely on from `results/points.csv` and `results/excluded_points.csv` yourself (the primary interpreter over the CSVs), never from the prose: the classes 6,283 `executed_in_both` / 1,429 `not_proposed_by_committed`; the committed driven points' fate 510 / 146 / 25; the driven counts 131 / 181 / 45 / 3 at the rule against the committed 257 / 400 / 24 / 0; the restored rows 175 / 191 driven, none ignited; the four verdict flips (sustainment 964, wall 192, beta 156, recirc 111 the other way); the cheapest driven `c2823` 202.192 and `c3584` 221.022, `c6466` 218.950, `c7227` 240.377, `c7625` 370.551; the design column's one driven point `c3694` with its three margins (49.08 against 50; 3.979 against 4.05; 24.9 against 24.9); `rule_vs_scales` 3,456 / 478 / 2,316 recomputed from the three requirement columns; the W ratio 0.788–1.018 (mean 0.919) and its monotonicity in He/n_e; the five `alpha_He_eff` values by T; the transect facts at the two anchors (`c1856` −50.8, `c5495` −31.4; the 8 → 12 move); the excluded set 71 / 65 / 28; and the baseline's channels at T-001 (W 551.444 → 519.914 MJ; 90.605 → 49.080 MW; peak 4.088 → 3.979; LCOE 313.513 → 322.318; both verdicts satisfied) against `evidence/round2_T-001_baseline_result.json` and the single-point runner's output.
- **Goal and strategy fidelity.** The strategy revision's approach was: land the owner's (c) ruling as one model item through the modelling PM (the helium-ash profile from the source's own A.5 rule, τ* uniform in ρ, the electrons by quasi-neutrality, the ash amount unchanged, one pressure integral with two consumers, the W integral form, the fuel and temperature exponents and the peaks untouched); then one pin; then one restating study of the `20260904-wall-and-heating` window at the new pin beside the committed record and the two constant-scale counterfactuals, with the shape's effect on W read across the geometry window. Did round 2 pursue it in that order, with one pin and one committed study? Rule on the strategy's seven assumptions as the result reports them (assumption (4) held in direction and not in number) and on its abandonment conditions (none met, per the result).
- **Every recorded task scope, T-001..T-003, and the T-003 scope amendment.** Did the work stay inside? Rule specifically on: (i) **T-001's form** — is the change what the owner ruled and nothing more: the ash profile computed from the rule pointwise at every lever point, never a point-A exponent; the electrons by quasi-neutrality; the ash amount (the peak fixed point) unchanged in form; the W integral form, the fuel and temperature exponents and the peak values untouched (check `git diff f49da0f0..0c20e373 -- models/` and the reserved bindings in `stellarator_plant.sysml`); the retirement of `alpha_n_e` as an entry point (design D1) and the beta calc reading the one ⟨p⟩ (D2) — inside the scope, or beyond the reserved gate? (ii) **the disclosed baseline flips** — both `sustainment_ok` and `wall_load_ok` went violated → satisfied at the new package; was that disclosed at the claim sites and left alone, never tuned (the T-001 return's decision; SV-054; the plan's restatement)? Verify the perturbations (`T_i0` → 18 keV moves the shape; τ*/τ_E → 4 halves the amount and leaves the shape at 4.052). (iii) **the MR-WI042-14 restatement** — written in commit A (`ae6c327e`) before the regeneration in commit B (`9ac167b8`), as the round-1 constraint 3 required, and does the study's re-execution confirm what it said (the seven earlier records superseded at their pins; the `20260904` window re-executed, not multiplied)? (iv) **the owner's scaling clause** (`goal.md` § Amendment (b): "make sure this scales up for larger stellarators") — is it met by construction (the shape computed from the rule at any geometry) and demonstrated (the design's window table before regeneration; the study's `alpha_He_eff` a function of T only at every geometry and every τ*/τ_E; the W ratio 0.79–1.02 following the ash fraction), and does the result state honestly what it rests on (α_n, α_T, Ti/Te, τ* uniform, f_suppr × τ*/τ_E, ι — point-A facts)? (v) **T-002's pin** — `CANDIDATE` on all ten gates at the first run; the fingerprints in the trail match `studies/manifest.json` and the seam documents. (vi) **the T-003 scope amendment** — two critique-driven changes (the T 13 keV rows restored on both geometry grids, about 1,500 proposals; the window's edges re-read at the rule) plus the per-point counterfactual join and the execute guards, authorized by a dated amendment before any point ran — legitimate extension under the round-1 carry-forward rule, or drift? Here the git order attests the sequence: the amendment and the critique at `5ab0452c`, the screen at `9a78b92c`, the record at `0b181b9c` — say so if you confirm it. (vii) the study's held axes and the comparison rule (`goal.md` § Invariants: by case coordinates, never by a re-run of the record; "feasible", "ignited", "driven" keep the record's definitions) — followed?
- **Retry classification.** The trail claims zero goal-level retries. Rule on each of: the two screen launches that failed at load on a path slip in `study.py` before any oracle evaluation (`dcc77967`; the record § 17); the codegen re-stencil of the handwritten sustainment impl inside T-001 and its hand restoration and second regeneration; the harness command that failed while writing the dispositions r2 and the C-001.r1 entry (nothing written; redone the next day as separate commands — the T-003 return's fourth decision); the 164 proposals the oracle pre-screen excluded. Retries under the runbook's definition, native-stage corrections, or neither?
- **The checkpoint sequence.** C-001.r1 `REVISE` (2026-09-05; four wording points, every number upheld) → the dispositions revised in place as r2 with every change marked `[r2]` (r1's text surviving at `700bfe6d`) → resubmitted to the same session, resumed by message the next day → r2 `PASS` (2026-09-06). Rule on whether the in-place form meets the runbook's "each submission is a new entry" reading (the `wall-and-heating` round-2 precedent and its review's F5: the entries in the trail are new; the file's revision is marked), whether the cap (2 revisions / 3 submissions) was respected (one revision spent), and whether the `[r2]` markers being stripped from the ten rows when they landed (the checkpoint's own landing note) was right.
- **Every discovery row the round's evidence touched** — the eight sightings at `0b181b9c` and the ten joined rows at `53eb5671`: did each disposition land as a class-bearing row (class from the ADR-0004 set, status, responsible actor, concrete next reference), and did the finding actually move? Five of this record's sightings (`#1`, `#2`, `#3`, `#4`, `#6`) carry their routing in the sighting row and got no joined row — rule on that. Rule on the `20260904-wall-and-heating#1` row (discharged by WI-042; the fifth condition a disclosure at the rule's basis; the item close owner-held), on `#2` (the knife-edge re-measured with the edges bracketed), on `#4` (now load-bearing), and on this record's `#5`, `#7`, `#8` (the Addendum's corrections carried as joined rows, the sightings standing). Rule on the untouched committed `#5`.
- **The learning delta L-004..L-008** proposed in the round result, in the forms the checkpoint accepted at r2 — accept, correct, or reject each; be strict: a learning is one claim a later strategy should act on, not a summary. Check the forms against the data: L-004's slope provenance (0.96–1.11 MW/MJ from the constant-scale pairs at the baseline) and its direction at the rule (the flip about 1 MJ above the model's own 519.9 MJ, above the sourced-rules 518.3); L-005's 306 / 175 / 131 and the one-transect caveat; L-006's aggregate directions and the `c1041` sign exception; L-007's −0.998 and "T_i0 alone among the swept levers"; L-008's 117 MW at 13 keV in the committed `window_edges.json` against 33 MW at the rule's anchor, and whether the step-7 runbook sentence is rightly homed as a repository process edit and not a goal learning.
- **External mutation** — `git log` over every cited path: the record was amended after commit by one Addendum (disclosed; rule on it as the study runbook's own mechanism or not); the WI-042 item record under `work/active/` after the T-002 pin; the model twins under `exploration/stellarator_e2e/models/` byte-identical to `models/` at the pin (`diff -r` the analyses and designs trees); `studies/manifest.json`, `tests/models/data/mfe_census.json`, the six `tests/study/data/*.expected.json` and `tests/study/test_known_answers.py` re-pinned at `9ac167b8` — inside T-001 or outside it?; `studies/ANNEX.md` § Baseline pin and § Oracle rewritten at `9ac167b8` (the T-001 return names it as a restatement site the plan's surface table missed).
- **The battery.** Re-run `tests/models` and `tests/study` yourself and report the counts (the round claims 48 passed / 13 skipped at `9ac167b8` and 363 passed / 1 skipped at `0c20e373`; `tests/study/test_records.py` 28 passed after the rows). Also run `tests/orchestration/test_goal_contract.py` and confirm the one failure is the owner-ruled one on `wall-and-heating/trail.md` and that nothing under this goal fails it.
- **The § Answered when assessment, word by word.** (a): met at grounding and ratified — unchanged? (b): "either a written attribution of the +9.2 % or a bounded negative" — met in round 1 on the written-attribution clause (the round-1 review's F4); does anything in round 2 disturb it (the attribution's decomposition — 5.3 points the ash shape, 1.2 the exponents, 2.7 the paper's — against what WI-042 measured: W at the printed peaks 524.523 MJ at the model's exponents, 518.308 at the paper's, SV-053)? (c): "The owner rules what stored-energy basis the model carries … and any model change returns to the owner as a minted work item through the modelling PM" — the owner ruled (ii) on 2026-09-05; WI-042 is the minted item, landed, pinned, and the window restated; the scaling line in § Amendment (b) — met? Say plainly whether (c) is met by the ruling plus its executed vehicle, or whether it waits on the owner's close of WI-042. Then the demo statement: the round-1 owner ruling ratified "record the design point's sustainment as undetermined within the source's own precision — on the boundary"; the round-2 result states it in L-004's form (satisfied on every fence, on the boundary; "needs 90 MW" retired; "is feasible" undecidable from the source's two-sided spread; the flip above the sourced-rules W). Is that what the package says, and is the L-004 form the honest statement of it? The goal's § Close rule is the owner's: "at grounding on the deposited evidence, on § Answered when, or by redirect at any round boundary."
- **Reserved gates.** Was any crossed? The W form, the exponents and the peaks (untouched, per T-001); the inherited invariants' re-grade (owner-held, carried); `goal.md` § Amendment (c) — an agent-grade reading the round acted on before ratification: surfaced honestly, or a gate crossed?

Then per § The fresh review: return `Verdict: PASS | FINDINGS | OWNER_GATE` with grounds; the checks with what you opened and the counts you recounted; the learning delta rulings in the corrected forms you would append to `learnings.md` (each with Evidence, Scope, Implication, Supersedes, Accepted-by lines, in the file's existing shape — read L-001..L-003 for it); the constraints carried forward; and **either** a recommendation that the owner close the goal on § Answered when with the close packet the owner rules on — the close ruling, WI-042's close through the modelling PM, the ratification of § Amendment (c), the four items carried from round 1 (the re-grade, the fifth condition on `wall-and-heating`, the author query, the § Question amendment), the three unminted follow-ons and the runbook step-7 sentence, and anything you add — **or** the round-3 strategy revision in the runbook's shape (approach, assumptions, abandonment conditions, intended model increment, intended study question — no future task list), if the goal is not answered or should be re-grounded. Give your own recommendation on the close in one paragraph, with the reason, so the owner can rule from it. Do not list merging or folding the branch back: the owner has ruled that it is not being merged and does not want it raised. The goal's round limit is 6 and this is round 2. Do not edit any file; return everything in your final message, in markdown, so the round agent can deposit it verbatim at `evidence/round2_review.md` and summarize it in the trail.
