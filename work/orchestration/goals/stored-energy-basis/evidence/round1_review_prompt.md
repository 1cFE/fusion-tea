# Round 1 fresh review — spawn prompt (deposited 2026-09-05 before the review session ran; verbatim)

You are a fresh reviewer for goal round 1 (`write-up-from-grounding`) of `work/orchestration/goals/stored-energy-basis/` in the worktree `/home/reid/1cfe/fusion-tea-stored-energy-basis` (branch `goal/stored-energy-basis`, HEAD `1d0a988a`, which contains `feat/demo-maturation` at `e0f66f7a` by merge). You did not author any part of this goal, its grounding evidence, its research run, its checkpoint, or its result; you inherit no context; work only from the repository. NEVER read anything under `knowledge/holdout/` (a quarantined clean room). Python is `/home/reid/1cfe/fusion-tea/.venv/bin/python` (the primary checkout's interpreter; never bare `python`, never `uv run` from this worktree). Do not edit any file. Do not run any git command that changes state. `tests/orchestration/test_goal_contract.py` has one failure (`test_narratives_are_separate_from_the_goal_contract`, on `wall-and-heating/trail.md`) that the owner ruled to stand on 2026-09-05; it is not this round's.

Read, in order: (1) `work/orchestration/GOAL_RUNBOOK.md` § The fresh review (your contract), § What "fresh" means, § The discovery log, § When a cited artifact moves, § Opening and closing a round, § The two checks are distinct; (2) `goal.md` end to end — § Question, § Answered when (a)/(b)/(c) word by word, § Invariants, § Limits, § Reserved gates, § Amendment 2026-09-05; (3) `trail.md` end to end — § Grounding, § Stop — 2026-09-04, the two Amendments of 2026-09-05, `## Round 1 — write-up-from-grounding` (the strategy revision, the Stop of 2026-09-05 and its amendment, T-001 scope / start / return, C-001.r1 and C-001.r2, T-002 scope / start / return, `## Round 1 result`); (4) the evidence: `evidence/handoff_20260905-063639.md` (the owner's ask as relayed; the three learnings as first drafted), `round1_research_option.md`, `round1_wall-and-heating_review_input.md` (with its Addendum), `T-001_REQ-W-01_prompt.md`, `round1_proposed_dispositions.md` (r1, r2, the r2 addendum), `round1_C-001_checkpoint_prompt.md`, `round1_C-001_checkpoint.md` (r1 and r2 verdicts), and `w_counterfactual/` — `NOTES.md` §§ 1–9, `w_counterfactual.py` (modes `baseline | parity N | window [workers]`, optional W target as the last argument), `baseline_counterfactual.json` and `baseline_counterfactual_518.3.json`, `window_summary.json` and `window_summary_518.3.json` with their CSVs, `attribution_arithmetic.py` / `.json` and `attribution_sourced_definition.py` / `.json` / `_output.txt`; (5) the research run `knowledge/research/requests/REQ-W-01.json`, `knowledge/research/requests/runs/REQ-W-01/20260905T145916478870/` (`run.jsonl`, `process_log.md`, `receipts/`, `return.json`) and the two registered sources `knowledge/sources/a_general_stellarator_version_of_the_systems_code_process/` and `knowledge/sources/systems_code_models_for_stellarator_fusion_power_plants_and/` with their `SOURCE_INDEX.md` blocks (read the equation images the registrations cite, not only `output.md`); (6) the six rows dated 2026-09-05 appended by this round at the end of `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` under `20260904-wall-and-heating#1`, `#2`, `#4`, `#6`, `#7`, `#8`, and the rows above them (the sightings at `9df54941`, the round-2 dispositions at `8906d4e7`, the F4 and close rows at `e0f66f7a`); (7) what this goal inherits from: `work/orchestration/goals/operating-point-closure/learnings.md` L-001 and L-005, `priced-levers/goal.md:52`, `wall-and-heating/goal.md:52`, `wall-and-heating/learnings.md` L-009, `wall-and-heating/trail.md` § Goal close — 2026-09-05; (8) the machine's own source, admissible: the Stellaris paper at `knowledge/concept_research/09-qi-stellarator-hts/iter-02/sources/publikationen-1000179851-172386752/tmpissrtbos/raw.pdf` (R2-synced, gitignored — if absent in the worktree, read it at the same relative path in `/home/reid/1cfe/fusion-tea`), pages 8–10 and 32, rendered with `fitz`; never its `output.md` for tables or captions.

Your checks (all of them):

- **Every cited ref resolves** and says what the trail claims. Recount from the data, never from the prose, with `/home/reid/1cfe/fusion-tea/.venv/bin/python`: from `window_counterfactual.csv` the flip counts 1,128 / 21 / 384 / 211 / 20, the 100 MW arm 458 / 787 / 257 → 776 / 1,282 / 332, the six design-geometry driven points, `c1676` 212.314 and `c1721` 226.507, the 332-of-657 and 15-of-24 decomposition (347 of 681), the re-read arm 24 → 58 and 371.70, the transect rows `c6308` / `c6309`, the 61 closure-edge rows and their record state by id; from `window_counterfactual_518.3.csv` the flip counts 890 / 18 / 273 / 145 / 14, the 100 MW arm 458 / 787 / 257 → 689 / 1,152 / 314 with 0 driven at the design geometry, `c1676` 208.72, the re-read arm 24 → 58, the 35 closure-edge rows; from the two baseline JSONs 90.6 → 37.5 / 51.4 MW, 4.088 → 3.861 / 3.930, the realized W 501.67 / 516.17 against the applied scales 0.9151 / 0.9399; re-run `w_counterfactual.py parity 60` (writes nothing) and `baseline` (overwrites `baseline_counterfactual.json` with identical content — check with `git diff`) and `baseline 518.3`; re-run `attribution_sourced_definition.py` and confirm 518.3 / 2.52 % / 2711 / 5.04 / 3.16, 551.3, 524.5, the ash's effective exponent 4.1, the β-implied 567 and 2.46 %, the reconciling field 8.49 T; recompute 518.3 independently from the printed peaks with the paper's rules (Eqs. 2–3 at 1.2 / 0.35, T_i = 0.95 T_e, n_He ∝ n_D n_T ⟨σv⟩ scaled to 0.56e20, electrons by quasi-neutrality, the 2ρ dρ element over 425 m³, species sum) and say whether you get it.
- **The research return.** Read the two registrations' equation images (Lion 2021 Eqs. 8–11; the thesis Eqs. 2.10–2.12) and say whether they state what the T-001 return says they state: W thermal, from the imposed species profiles, a single product of a species-averaged n and T, √g ∼ ρ, no fast-alpha term; ISS04's density line-averaged; no β reference field. Rule on the T-001 scope (one request, β folded in; REQ-W-02 not opened), on the clean-room record (one Helios refusal logged; nothing under `knowledge/holdout/` read), on the two queued items, and on whether the "species averaged" ambiguity (a mean over species would give 259–274 MJ) is resolved by the printed value as the return claims.
- **Goal and strategy fidelity.** The strategy: turn the grounding evidence into a round result whose learning delta a fresh review can accept; an optional bounded research check first; no study, no pin, no model change; the counterfactual supplied to `wall-and-heating` only through that goal's pen and the owner's gate. Did the round pursue it? Rule on the four assumptions as the result reports them — (4) failed in letter (the `wall-and-heating` review returned mid-session and the goal was closed) — and on the abandonment condition ("a definition under which 504.65 MJ follows from the paper's own profiles and peaks"): not met, the round says; is that right, given 518.3?
- **Every recorded task scope, T-001 and T-002.** Did the work stay inside? Rule specifically on: the second-scale evaluation (`baseline 518.3`, `window 10 518.3`) run inside T-002 on the committed script with a new target argument — an evidence write inside the goal directory, or drift?; the merge of `feat/demo-maturation` at `e0f66f7a` into this branch (the T-002 scope said "rebase"; the round agent merged to keep every sha cited in the trail valid — say whether that is the same act for the scope's purpose, namely that the six rows follow the close rows in file order); the reproduction script written during the T-001 return (the round agent verifying a subagent's arithmetic before recording it — inside T-001's reading, or T-002's work?).
- **Retry classification.** The trail claims zero retries. The research subagent stopped once mid-registration and was resumed by message to the same session; the checkpoint session was resubmitted (r2) as the runbook provides. Retries under the runbook's definition, or not?
- **The checkpoint sequence.** C-001.r1 `REVISE` → r2 (the r1 text kept, `## Revision r2` appended, then an `### r2 addendum` written after T-001 returned and before submission) → r2 `REVISE` (one row; the class of `#1` ruled `model fix`) → r3 `PASS`. Rule on whether "submissions never edited in place" and the cap (2 revisions / 3 submissions) were respected, and on whether running the checkpoint in parallel with T-001 was sound.
- **Every discovery row the round's evidence touched** — the six rows at `0964e9d3`: class from the ADR-0004 set, status, responsible actor, concrete next reference; did each finding actually move, or did the row honestly say it did not? Rule on `#1`'s class as landed (``model fix` — open, not minted, owner-gated`), on the "surfaced, not re-ruled" sentence about the owner's (b) close ruling, on the no-row reason for `#3` and `#5`, and on whether writing under ids whose `wall-and-heating` rows are marked "final for that goal" is the runbook's reading (a goal round may append under an id a committed record's § 15 carries; a sighting is never edited).
- **External mutation** — `git log` over every cited path: `NOTES.md` and the 504.65 outputs unchanged since `3687d2f6`? The `wall-and-heating` files this goal cites moved at `a11db053` / `e0f66f7a` (disclosed in the trail's amendment) — voiding anything? `models/`, `exploration/stellarator_e2e/generated/`, `studies/manifest.json` untouched by this branch (`git diff --stat 1928925b..HEAD -- models exploration/stellarator_e2e/generated exploration/stellarator_e2e/studies/manifest.json` should be empty)?
- **The battery.** Run `tests/study/test_records.py` (the round claims 25 passed after the rows) and `tests/orchestration/test_goal_contract.py` (28 / 1, the owner-ruled failure). `tests/models` and `tests/study` were not re-run by the round (nothing under `models/` or the package changed); run them if you judge it necessary and report the counts (the review prompt precedent: `rm -rf .integration_workspace; uv run --env-file ~/1cfe/agentic-mbse/.env --env-file .venv/integration.env python -m pytest <target> -q` from the PRIMARY checkout `/home/reid/1cfe/fusion-tea`, `tests/models` after `set -a; source ~/1cfe/agentic-mbse/.env; set +a`, `tests/study` without any `PYTHONPATH`; the worktree's own venv is not the sealed one).
- **The learning delta L-001..L-003** proposed in the round result — accept, correct, or reject each; be strict: one claim a later strategy should act on, with Evidence, Scope, Implication, Supersedes lines in the file's shape. Check every number in them against the data. Rule on whether L-002 and L-003 together say what the T-001 return supports and no more: that the model's W-form carries a sourced ~6 % gap on the ash shape, that the printed 504.65 is not what the paper's rules give, and that the printed β row is inconsistent with it.
- **§ Answered when, word by word.** (a) "the counterfactual, deposited" — met at grounding and ratified? (b) "the attribution, written or bounded — either a written attribution of the +9.2 % or a bounded negative naming what was ruled out; (b) closes when the owner accepts the first pass as the attribution, or a research-seam request against the source returns those definitions or a bounded negative" — did REQ-W-01 return the definitions in the contract's sense, and is the attribution now written (about five points ash shape, one point exponents and ratio, 2.7 points and the β row the paper's, the author query queued)? (c) "the basis, ruled" — the owner's; the result closes on trigger 4 (an owner gate not resolved). Say whether that derivation is right.
- **Reserved gates.** Did the round tune W, change the W form or exponents, re-rule the inherited invariants or L-001's line, write in another goal's directory, or decide the basis? The rows and the result surface the owner's (b) close ruling as made on four conditions — surfaced or re-ruled?

Then per § The fresh review: return `Verdict: PASS | FINDINGS | OWNER_GATE` with grounds; the checks with what you opened and the counts you recounted; the learning delta rulings in the corrected forms you would append to `learnings.md` (each with Evidence, Scope, Implication, Supersedes, Accepted-by lines); the constraints carried forward; and **either** a recommendation that the owner close the goal after ruling (c), with the close packet the owner rules on — the basis (the present family; a minted item for the ash profile from the paper's A.5 rule with quasi-neutral electrons; or a sourced alternative), the re-grade of the two inherited invariants and `operating-point-closure` L-001's line, the fifth condition on `wall-and-heating`'s closed (b)(ii), the author query, and anything you add — **or** a round-2 strategy revision in the runbook's shape (approach, assumptions, abandonment conditions, intended model increment, intended study question — no future task list) if the goal is not answered or should be re-grounded. Give your own recommendation on the (c) ruling in one paragraph, with the reason, so the owner can rule from it. Do not list merging or the fold-back of this branch: both are the owner's and the owner does not want them raised. The goal's round limit is 6 and this is round 1. Do not edit any file; return everything in your final message, in markdown, so the round agent can deposit it verbatim at `evidence/round1_review.md` and summarize it in the trail.
