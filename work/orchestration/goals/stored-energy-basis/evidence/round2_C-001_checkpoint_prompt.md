# C-001 checkpoint (round 2) — spawn prompt (deposited 2026-09-05 before the checkpoint session ran; verbatim)

You are the fresh pre-execution disposition checkpoint reviewer for goal `stored-energy-basis` round 2 in `/home/reid/1cfe/fusion-tea-stored-energy-basis` (a git worktree on branch `goal/stored-energy-basis`; the primary checkout `/home/reid/1cfe/fusion-tea` is a different branch — use the worktree's files). You did not produce the study, its synthesis, or the proposed dispositions; you inherit no context; work only from the repository. NEVER read anything under `knowledge/holdout/`. For any scripting use `/home/reid/1cfe/fusion-tea/.venv/bin/python` (never bare `python`, never `uv run` from this worktree). Do not edit any file and do not run any git command that changes state.

Your contract: `work/orchestration/GOAL_RUNBOOK.md` § The pre-execution disposition checkpoint — you review one study reading and its proposed dispositions and return a verdict. The shape here: the round closes on this reading (trigger 1); no further task follows it inside the round. What your verdict gates is: the disposition rows being appended to the discovery log, the T-003 return and the round-2 result that cite them, and the evidence base the fresh reviewer will write the next strategy (or the owner's close) on.

Read, in order:
1. `work/orchestration/GOAL_RUNBOOK.md` § The pre-execution disposition checkpoint + § The discovery log; `.project/adr/0004-finding-disposition.md` (the disposition rules: class, status, responsible actor, concrete next reference; no touched row returns `unrouted`; a sighting is never edited).
2. The reading: `exploration/stellarator_e2e/studies/20260905-stored-energy-basis/synthesis.md` (the fresh administrator's reading of record) and the committed record's §§ 2, 3, 4, 5, 6, 11, 12, 13, 14, 15, 17 (`exploration/stellarator_e2e/studies/20260905-stored-energy-basis/record.md`), with `results/points.csv` and `results/excluded_points.csv` beside them. The deposited recount the executor used is `work/orchestration/goals/stored-energy-basis/evidence/round2_T-003_recount.py` — you may run it, but recount the crux numbers yourself from the CSV columns first.
3. The proposed dispositions: `work/orchestration/goals/stored-energy-basis/evidence/round2_T-003_proposed_dispositions.md` (submission r1; sections A–D).
4. The current log rows they act on: `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` — every row for `20260904-wall-and-heating#1`, `#2`, `#3`, `#4`, `#5`, `#6`, `#7`, `#8` and the eight first-sighting rows `20260905-stored-energy-basis#1..#8` (scan the whole file for each id; ids are wrapped in backticks; the newest row is the current state).
5. Context as needed: `work/orchestration/goals/stored-energy-basis/goal.md` (§ Question, § Answered when, § Invariants with amendments (b) and (c), § Reserved gates) and `trail.md` § Round 2 (the strategy revision; the T-001, T-002 returns; the T-003 scope and its amendment); `learnings.md` L-001..L-003; the pre-execution critique `evidence/round2_T-003_precritique.md` (eleven findings the study was revised on); `work/active/WI-042_sourced-helium-ash-profile/design.md` (D1–D8, § Expected baseline behaviour) and `plan.md` § MR-WI042-14 restatement (why no multiplier re-reads a committed column); the counterfactual evidence `evidence/w_counterfactual/NOTES.md` §§ 5, 6, 8 (what the two constant scales claimed and did not claim).

Answer, for each proposed disposition: is the reading right (does the cited evidence say what the disposition claims), and does the disposition follow from it under the discovery-log rules (class named from the ADR-0004 set; status, responsible actor and concrete next reference present; no touched row returns `unrouted`; disposition is not resolution; a row that changes nothing says so)? Scrutinize hardest:
(a) **A.1 — `#1` "discharged by WI-042" with its reading moved.** Recount from `points.csv` over the `executed_in_both` class (`class_vs_committed`): the committed driven points' fate at the rule (`transition_vs_committed`: driven → ignited / driven / violated, per arm), the driven counts at the rule beside `committed_feasible_driven` per arm, and over all executed points the restored 13 keV rows' driven count (`class_vs_committed = not_proposed_by_committed`, `T_i0_keV` 13.0). Rule on whether "discharged" is right when the model fix landed and the finding's reading changed, and on whether the fifth condition (W-sensitivity) is honestly stated as *measured* now rather than disclosed.
(b) **A.4 and this record's `#1` — the ignited set.** Recount the ignited count over all executed points and per arm, the feasible-ignited count, and whether every committed headline driven point (`committed_case_id` ending `c1721`, `c4639`, `c0821`) is ignited at the rule with the requirement the record states. Rule on "load-bearing for every count" and on the class (`model fix`, open, not minted; the owner mints or carries).
(c) **This record's `#2` and `#3` and the design column.** Recount: the design column (R 12.7, a 1.3) at 100 MW — points, feasible-driven count, the baseline row (`is_baseline_point`) and its `p_aux_required_MW_oracle`, `wall_load_peak`, `committed_p_aux_required_MW`, `cf0915_*`, `cf0940_*`; whether any I 15 MA point on it is driven; the cheapest driven point at the rule over all points and over `executed_in_both`, with coordinates; the cheapest driven per `T_i0_keV` at 100 MW. Then rule on the words "on the boundary" (§ 3, § 15 #3) and "one step above a caught edge" (§ 11's transects at the rule: `results/window_edges.json`, the `driven-at-the-rule-100` anchor's T rows) — is the record's claim about the driven band's width supported, and does § 17 state the bracketed-not-executed 12.5 keV honestly?
(d) **This record's `#4` — the counterfactuals.** Recount `rule_vs_scales` over the `executed_in_both` class and per arm, and the `W_ratio_vs_committed` min / max / mean. Rule on whether "predictions the rule tested, never bounds" is carried at every claim site (§§ 2, 4, 12, 15 #4) and on the class `declared seam` (a method finding about constant-scale counterfactuals) and its home (L-001's scope line).
(e) **This record's `#5` — the correction across the window.** Recount `alpha_He_eff_oracle` by `T_i0_keV` (distinct values; any variation with anything else), `W_ratio_vs_committed` by (R, a) and its monotonicity against `He_over_ne_oracle`, the count above 1.0 and where, and `W_store_vs_oracle_reldev`'s worst value. Rule on "met by construction and demonstrated" and on whether what the claim rests on (α_n, α_T, Ti/Te, τ* uniform, f_suppr × τ*/τ_E, ι) is stated at the claim site.
(f) **A.2, A.3, A.5, A.6, A.7 — the re-sighted rows.** Does each proposed row actually move the finding or merely restate it — a row that changes nothing must say so plainly. For A.2 recount the transect arm at the rule (`arm-transect-ash`: `tau_ratio_ash`, `p_aux_required_MW_oracle`, `wall_load_peak`, `wall_load_ok`, `feasible_driven`, `ignited`) beside the `committed_*` columns; for A.5 the re-read arm's driven count by `eta_source_heat` beside the committed; for A.6 the excluded set by class and reason beside the committed 65 (`results/excluded_points.csv`, `class_vs_committed`).
(g) **The proposed learning delta (D, L-004..L-008):** is each learning supported by the cited findings, and is any one an over-claim in the way the round-1 review corrected L-001 and L-003 (a margin stated in the wrong unit; a scope wider than the evidence; a mechanism asserted, not measured)? In particular: does L-004's "neither is decidable within the source's precision" follow from a 0.92 MW margin against a 2.7 % residual on the printed W (about 14 MJ; the record's own slope estimate), and is L-008 a learning of this goal or a runbook matter?
(h) **The scope amendment and the critique-driven changes** (record § 14; trail § Amendment 2026-09-05 — amends § T-003 scope (round 2)): rule on whether the disclosure is honest and complete (the restored rows as a named class; the pooled screen; the execute-phase guards; the edges re-read; the counterfactual join); the round review, not you, rules on drift.
(i) **Where the record's numbers and the administrator's recount disagree, if anywhere:** which is right, and does any disposition rest on the wrong one?

Return (final message): `Verdict: PASS | REVISE` with grounds; per-disposition rulings; the recounts you did with the numbers you got; and, if REVISE, exactly what the author must change. The revision cap is 2 (three submissions); your verdict is submission r1's.
