# Spawn prompt — fresh administrator, study `20260905-stored-energy-basis` (the `study.read` seam)

Deposited 2026-09-05 before the administrator session ran, by the round-2 agent of goal `stored-energy-basis` (every gate session's spawn prompt is deposited as evidence). The administrator is a fresh non-author `general-purpose` session with no inherited context. It reads the record directory and nothing else, and writes `synthesis.md` there. It is not the disposition checkpoint and not the round review; those are separate fresh sessions.

## The prompt given

You are a fresh **administrator** for a committed parameter-study record in `/home/reid/1cfe/fusion-tea-stored-energy-basis` (a git worktree; use its files), per `.claude/skills/run-study/runbook.md` § Administer. You did not execute this study and you inherit no context about it. Your only output is `synthesis.md` in the record directory. For any scripting use `/home/reid/1cfe/fusion-tea/.venv/bin/python` (never bare `python`, never `uv run` from this worktree).

CLEAN ROOM: never open anything under `knowledge/holdout/`. You do not need it.

Read, in this order, and nothing else about the study:
1. `.claude/skills/run-study/runbook.md` § Administer and the `synthesis.md` contract that follows it (your rules), and `modeling_project/STUDY_POLICY.md` if you need the vocabulary.
2. The record directory only: `exploration/stellarator_e2e/studies/20260905-stored-energy-basis/` — `record.md`, `snapshot.json`, `indicators.json`, `axes.json`, and everything under `results/` (`points.csv`, `oracle_operands.csv`, `excluded_points.csv`, `window_edges.json`, `verification_summary.json`, `preflight_results.json`, `baseline_result.json`, `package_identity.json`). `study.py`, `scan.py` and `edges.py` are in the directory and are digested by the snapshot; you may read them for the study definition but cite them as definition, not as evidence.

Do NOT read the package, the manifest, the discovery log, the goal directory, the work items, the committed predecessor record, the counterfactual evidence, or the annex. The record joins the predecessor's and the counterfactuals' readings per point into its own columns (`committed_*`, `cf0915_*`, `cf0940_*`) — those columns are what you may read about them, and only as the record carries them. What the record does not carry, you report as missing rather than recover from elsewhere. Only paths inside the record directory may be cited in `synthesis.md`.

Your job:
- Recover the fresh-administrator facts: the framing per axis, the LCOE result, every named constraint outcome, and every § 15 finding — each traced to a committed artifact in the directory. Keep recorded facts, missing facts, and your own interpretations distinct and labeled.
- **Independently recount the crux numbers from `results/points.csv` and `results/excluded_points.csv`** rather than taking §§ 3, 4, 6, 11, 12 and 15 on their word. State exactly which columns you used and how you defined "driven" (`feasible_driven`: every verdict satisfied and `p_aux_required_MW_oracle` ≥ 0), "ignited" (`ignited`), and the point classes (`class_vs_committed`: `executed_in_both`, `committed_excluded_now_executed`, `not_proposed_by_committed`; in the excluded file also `committed_executed_now_excluded`, `excluded_in_both`). Recount at least: the evaluated, feasible, ignited and feasible-driven counts per arm at the rule over all executed points AND over the `executed_in_both` class only, beside the committed counts the record carries (`committed_feasible`, `committed_ignited`, `committed_feasible_driven`) over the same class; the transition table (`transition_vs_committed`) per arm; the per-constraint verdict flips between `committed_<verdict>` and `<verdict>` over the `executed_in_both` class; `rule_vs_scales` per arm; in `arm-fence-p100`, the cheapest `feasible_driven` point at the rule over all executed points and over the `executed_in_both` class, with its coordinates, `lcoe`, `wall_load_peak`, `p_aux_required_MW_oracle`, `W_th_MJ_oracle`, `alpha_He_eff_oracle`, `He_over_ne_oracle`, `class_vs_committed`, `committed_case_id` and `committed_lcoe`; the same in `arm-search-p220`; the cheapest driven point per `T_i0_keV` value in `arm-fence-p100` (the restored 13 keV row against the committed rows); the design column (R 12.7, a 1.3) at 100 and 220 MW — how many points, how many feasible driven, which (the baseline row by `is_baseline_point`, with its `lcoe`, `p_aux_required_MW_oracle`, `wall_load_peak`, `committed_p_aux_required_MW`, `cf0915_p_aux_required_MW`, `cf0940_p_aux_required_MW`), and whether any grid point (I 15 MA) on it is driven; the baseline row's `W_th_MJ_oracle` against `W_th_MJ_store` and the worst `W_store_vs_oracle_reldev` in the file; `W_ratio_vs_committed` over the `executed_in_both` class — min, max, mean, and by (R, a) — and `alpha_He_eff_oracle` by `T_i0_keV` (how many distinct values, and whether it varies with anything else); along `arm-transect-ash`, `alpha_He_eff_oracle`, `He_over_ne_oracle`, `W_th_MJ_oracle`, `p_aux_required_MW_oracle` and the wall verdict at each `tau_ratio_ash` for each anchor; in `arm-reread-p220`, the feasible-driven count and the cheapest driven point at the rule beside the committed columns; the excluded set by `class_vs_committed` and by reason class, beside the committed exclusions the record carries. Where your recount disagrees with the record, say so plainly with both numbers.
- Say what the evidence establishes about the study's own question (record § 2 carries it) and what it does not, in your own words, labeled as the administrator's reading. Be specific about (i) what the window says at the rule about a feasible-DRIVEN region at 100 MW and where it sits (the restored 13 keV row against the committed window), (ii) what the design column says and how its margins are stated, (iii) what the joined committed and counterfactual columns show about how the rule moved the committed points (the transitions; where the rule fell relative to the two scales), (iv) what the per-point W ratio and the ash exponent show about the correction across the window, and (v) what the record says about the basis of every number (the WI-042 family; the committed WI-037 family; the counterfactuals' constant-scale basis).
- Write `synthesis.md` with the header (administrator, date 2026-09-05, the `snapshot.json` sha256 you read, computed with `sha256sum`) and the sections the runbook names: what the study set out to do; what it found; the framing verdict per axis; the constraint structure; findings carried forward; and **What the record does not support** (mandatory; empty only if nothing was missing).

Do not edit any other file. Return, as your final message, a short summary: the path you wrote, any recount disagreements with the record, and the list of facts you could not recover.
