# Handoff: stored-energy (W_th) counterfactual diagnostic, run beside the open wall-and-heating goal

Written 2026-09-04 from a session on `feat/demo-maturation` in `/home/reid/1cfe/fusion-tea`. Status marks: **[verified]** = checked in files this session; **[carried]** = taken from records or a subagent report and not re-checked.

## Focus

Ground a small goal, working name `stored-energy-basis`, whose first task is an oracle-side diagnostic: re-evaluate the plasma sustainment balance at the Stellaris reference point, and the current pin's 100 MW wall-plug candidate set, with the model's stored thermal energy forced to the paper's printed 504.65 MJ. Report whether the baseline sustainment violation and the "no feasible driven point at the printed power" result survive. Never touch `models/`, the generated package, or the pinned manifest. Work in a sibling worktree. Any model change is a follow-on work item, owner-gated.

The owner asked for this because the gap has been carried as an invariant by two later goals and nobody owns it, and because every study reading since 2026-09-01 sits on the biased balance.

## Context

**The gap.** [verified] The model computes stored thermal energy W_th by integrating assumed power-law profiles (`models/library/analyses/mfe_plasma_sustainment.sysml:25`; exponents bound in `models/designs/stellarator_09/stellarator_plant.sysml` at `alpha_n` 0.33, `alpha_T` 1.19 digitized from the paper's Fig. 16, `alpha_n_e` 0.596 back-computed from the printed peak/average density ratio). The operating-point prototype found W = 551.3 MJ against the printed 504.65 MJ, +9.2%; every other balance term matched the paper (τ_E −0.1%, radiation 228.7 vs 228.9 MW, fusion 2720 vs 2700 MW). Because conducted loss goes as W^2.56 in the closed ISS04 form, +9.2% in W is ≈+25% in loss. Balance residual at point A: −89.6 MW; the pinned baseline needs 90.6 MW coupled against 50 installed and fails `sustainment_ok`. Source: `work/orchestration/goals/operating-point-closure/evidence/T-002_prototype/NOTES.md` lines 19–29; learnings L-001, L-002, L-005 in `work/orchestration/goals/operating-point-closure/learnings.md`.

**The paper's own claim.** [carried, subagent read the Table 5 page image] Stellaris point A is ignited: auxiliary power at the operating point 0 MW, gain ∞. The 50 MW in Table 2 is "required plasma-coupled ECRH power". The model reads that 50 MW as installed steady-state heating and compares it with a computed steady-state requirement.

**The rule that stops anyone fitting it.** [verified] L-001's implication line: any residual at the printed point is attributed first to the W-form, "and W is never tuned to the printed value." Written by the round-1 agent 2026-09-01, accepted by a fresh reviewer the same day. The owner's five close rulings of 2026-09-02 (`work/orchestration/goals/operating-point-closure/trail.md:280-288`) do not mention it. It is agent-grade. The owner said this session it is theirs to keep or overturn.

**How it has been inherited.** [verified] `work/orchestration/goals/priced-levers/goal.md:52` and `work/orchestration/goals/wall-and-heating/goal.md:52` both carry "the violated baseline sustainment_ok is not a defect and is not this goal's to fix." The operating-point round-2 review (`evidence/round2_review.md:51`) says "a finding, not a failure; do not fix it." No backlog item owns the gap: the open modeling items are WI-034, WI-038, WI-040, and WI-041 (`work/BACKLOG.md`). The counterfactual with W at the printed value was never run: NOTES.md contains no such evaluation.

**Current pin and goal state.** [verified via git log messages] Pin `c1b0f0d1…` indicator, `d468f3b6…` semantic, `d4be3951…` executable, teax `744745f8`, at WI-041 (source-anchored wall fence). Goal `wall-and-heating` round 2 is open. Its study `exploration/stellarator_e2e/studies/20260904-wall-and-heating/` has **executed and committed** (commits `9df54941`, `9b1a4ef1`): 6,376 proposed, 6,311 evaluated. Under the honest wall fence a feasible driven region exists at the printed 100 MW wall-plug only at a ≥ 1.5 m, cheapest 212.460 $/MWh at R 14.2 m, a 2.2 m, conditional on the held ash-transport ratio (a knife-edge at the source's 8), on nothing bounding or pricing a, and on the one-sided sustainment fence passing ignited points. Round-2 T-004 proposed dispositions (r1) and the C-001 checkpoint prompt are deposited. The checkpoint and round review are the next acts on that goal. Baseline LCOE at this pin 313.513 $/MWh with `sustainment_ok` and `wall_load_ok` both violated.

**Why timing matters.** [verified] `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` rows dated 2026-09-04 under `20260904-wall-and-heating#2` and `#4`: feasibility flips with τ*/τ_E ("the plasma ignites at 6"), and the sustainment fence passes ignited points (201 of 458 "feasible" at 100 MW). Both readings move if W moves. The diagnostic's answer should exist before those dispositions are accepted.

**Where this session's summary work lives.** [verified] `work/narratives/20260904-234255Z-goal-overview.md` (untracked): one-table overview of the seven goals with the owner's one-line result summaries. Governing frame: `.project/concepts/stellarator-demo-maturation.md` (Stellaris supplies physics and geometry, 1costingFE supplies cost, ARIES-CS hold-out is the exam; instance header `stellarator_plant.sysml:8-13`).

## What to do

1. **Worktree.** [verified convention] `git worktree add ../fusion-tea-stored-energy-basis -b goal/stored-energy-basis` from the repo root, sibling to the repo, never under `.claude/worktrees/`. The diagnostic is plain Python and does not need the sealed seam interpreter. Check the prototype's and `verify_stellaris.py`'s imports; if `uv run` in the worktree needs a sync, do it, or run the primary checkout's interpreter as `docs/integration_seam_operator_guide.md` § Running from a second checkout describes. Commit with explicit pathspecs only (`git commit -m ... -- <paths>`); the owner keeps files staged and a bare commit sweeps them.

2. **Ground the goal, owner present.** Invoke `/run-goal` in `ground` mode; procedure in `work/orchestration/GOAL_RUNBOOK.md` § Grounding a goal. `goal.md` sections used by the sibling goals: Status, Question, Consumer, Answered when, Invariants, Grounding evidence, Limits, Reserved gates, Close rule, Amendments (`work/orchestration/goals/priced-levers/goal.md` is a clean example). Draft for the owner to ratify or reword:
   - **Question:** Does the baseline sustainment violation, and the "no feasible driven point at the printed 100 MW wall-plug" result, survive when the model's stored thermal energy is set to the paper's 504.65 MJ? If not, what in the profile integral produces the +9.2%?
   - **Consumer:** the wall-and-heating round-2 checkpoint and review; the next-goal selection; the two inherited "not this goal's to fix" invariants.
   - **Answered when:** (a) a deposited oracle-side evaluation at the reference point and over the 20260904 study's candidate window with W scaled to 504.65 MJ, with every verdict flip counted against the committed record; and (b) either a written attribution of the 9.2% or a bounded negative saying what was ruled out. A trail-only answer counts. Any model change returns to the owner as a minted work item.
   - **Invariants:** nothing written under `models/`, `exploration/stellarator_e2e/generated/`, or `exploration/stellarator_e2e/studies/manifest.json`; the pin stays `c1b0f0d1…`; every number labeled oracle-side, not package evidence; W is not tuned in the model; the clean room holds (`knowledge/holdout/` never read; Stellaris sources admissible).
   - **Reserved gates (owner-held):** changing the W_th form or profile exponents in `models/`; re-ruling the inherited invariants in `priced-levers/goal.md:52` and `wall-and-heating/goal.md:52`; any rubric Row 1 consequence.

3. **T-001, the diagnostic.** Two bases, both cheap:
   - **Basis A, the model as implemented.** Copy `exploration/stellarator_e2e/verify_stellaris.py`; `_sustainment(p, V, B_axis)` at lines 97–158 computes W_th at :123 inside `state()`, which the ash fixed-point loop calls at :130, so the override must live inside `state()`, not after it. First evaluate the reference point unmodified and read `W_th` (the oracle also exports it as `sustain__W_th`, `sustain__tau_E`, `sustain__p_aux_required`; key map at `exploration/stellarator_e2e/studies/oracle_entry.py:200-207`). Then scale W_th by 504.65 / W_th_baseline and re-evaluate: report `p_aux_required` and the `sustainment_ok` verdict at baseline. Then re-run the round-2 window scan with the same scale: `exploration/stellarator_e2e/studies/20260904-wall-and-heating/scan.py` already drives the oracle over (R, a, I_coil, n_e0, T_i0, wall-plug) via `study_route` and `oracle_entry`; count feasible driven points at 100 MW and compare with the committed record's counts. State the assumption plainly: a constant scale factor assumes the profile-shape error is multiplicative and geometry-independent.
   - **Basis B, the paper's own closure.** `work/orchestration/goals/operating-point-closure/evidence/T-002_prototype/op_solve_final.py`, `state(T, n_e0, p_aux, B, iota)` at :39; apply the same override and confirm the point-A residual moves from −89.6 MW toward zero. This checks the model-side result against the independent prototype.
   - **Deposit** scripts, raw outputs, and a NOTES.md in the new goal's `evidence/T-001_w_counterfactual/`, same shape as `T-002_prototype/`. Cite the raw PDF or page images for every printed value; the text extractions of the Stellaris tables carry phantom rows.

4. **T-002, only if the owner wants it in this goal: attribute the 9.2%.** Candidates, none checked yet: the parabolic power-law profile family versus the paper's actual profiles (digitize Fig. 16 and integrate numerically); the by-eye `alpha_T` 1.19; whether the paper's 504.65 MJ is thermal-only or includes fast-alpha energy (unknown; the NOTES call the model's W "thermal"). Research seam per `GOAL_RUNBOOK.md`; hold-out restriction stated in the research instruction before any retrieval.

## Key discoveries

- [verified] The gap is un-owned by design drift, not by ruling: two goal contracts inherited an agent-grade "do not fix" and no owner ruling exists. Surface this at grounding so the owner can re-grade it deliberately.
- [verified] The counterfactual has never been run, and it is a one-session job because the prototype and the oracle expose W_th as a separate term.
- [verified] The round-2 study is committed, not queued. Its record and synthesis exist; its dispositions are at checkpoint r1. Read `record.md` § 15 findings and the two discovery rows before writing the goal's Consumer section.
- [verified] Two subagent reports from this session reproduced the reference-design pin map and the study-variation inventory; the useful residue: of 145 sweepable design attributes, 14 have ever been an axis; limits (B_max, sigma_allow, strain, wall, beta) were declined as axes on principle.
- [carried] Discrepancies the pin-map subagent flagged and nobody has acted on: the stress factor was fitted so stress equals 650 MPa where the paper prints "<650"; the paper prints a 90% availability target while the model binds 1costingFE's 0.85; the design-file header still reports a stale LCOE of $203.6/MWh; `f_carnot_cryo` 0.20 has no source. Not this goal's work; noted so they are not rediscovered.
- [verified] Owner working preferences that bind this task: never use the AskUserQuestion tool, ask in prose; no unsolicited fixes, diagnose then propose; plain language; one line per markdown paragraph; commit with explicit pathspecs; worktrees as siblings.

## Suggested skills

- `/run-goal` in `ground` mode: the only sanctioned way to open the goal and the trail.
- `/run-study` is not needed for T-001; the diagnostic is oracle-side, not a package study. Use it only if the owner later wants the corrected balance run as a package study, which would require a model change first.
- `/record-learning` after the round review accepts the result, so the counterfactual outcome lands in `learnings.md`.
- `/narrate-goal` at close, so the goal gets a snapshot beside the seven in `work/narratives/`.
- `/_my_wrap_up` at session end; update `.project/CURRENT_WORK.md` with the goal's state.

## Open questions

- **Grounding needs the owner in session.** The Question and Answered-when drafts above are agent wording awaiting ratification.
- **Does the owner want to re-rule the inherited "not this goal's to fix" invariants now**, or only after the diagnostic reports? The diagnostic does not need the ruling to run.
- **Should the diagnostic's result be put in front of the wall-and-heating round-2 checkpoint before its dispositions are accepted?** The owner leaned yes this session; it is not recorded anywhere.
- **Fold-back of the `goal/stored-energy-basis` branch into `feat/demo-maturation`** is additive (a new goal directory only) but is the owner's act.
- **Is the paper's 504.65 MJ thermal-only?** Unverified; it changes what "forcing W to 504.65" means.
