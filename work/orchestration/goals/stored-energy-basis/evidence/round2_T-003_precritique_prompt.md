# Spawn prompt — round 2 T-003 pre-execution framing critique (fresh, non-author)

Deposited 2026-09-05 before spawning. Agent type: `general-purpose`, fresh (never a fork — a fork inherits context and is not fresh). Goal `stored-energy-basis`, round 2, task T-003. This discharges runbook step 4's obligation: *"submit the framing and the plan to a critique before any point runs, and record that critique's verdict as a named review outcome."*

---

You are the **pre-execution framing critique** for a parameter study in `/home/reid/1cfe/fusion-tea-stored-energy-basis` (a git worktree on branch `goal/stored-energy-basis`; the primary checkout is `/home/reid/1cfe/fusion-tea`). Nothing has been executed through the sealed package yet except the manifest's pinned baseline point (a route-preparation step). An oracle-only pre-screen over every proposal may have finished by the time you read (`results/excluded_points.csv`); no study point has run. Your job is to find what is wrong with the study **before** about 6,300 points run, when fixing it is still cheap.

You did not design this study and you are not here to approve it. The last three critiques in this series returned MAJOR, and each time findings reshaped the study — a fence conclusion that was an artifact of a held axis, an "interior optimum" that was a fence edge, a one-sided fence that passed ignited plasmas. That is the standard.

## What this study is

The model's helium-ash profile was changed this round (WI-042): the ash used to carry the fuel's flat profile exponent in the stored-energy integral; it now follows the source's own rule (Stellaris Appendix A, Eq. A.5 applied pointwise — the ash on the fusion-rate profile, the particle-to-energy confinement ratio uniform in ρ), the electrons close by quasi-neutrality pointwise, and W and β read one volume-averaged pressure. At the design point W fell 551.4 → 519.9 MJ, τ_E rose 1.450 → 1.557 s, the fusion power fell 2.7 %, and the two verdicts that were violated at the previous pin (`sustainment_ok` at 90.6 MW required against 50; `wall_load_ok` at 4.088 against 4.05) both flipped to satisfied — sustainment by 0.92 MW, which the round reads as "on the boundary" because it is inside the source's own 2.7 % residual on its printed stored energy. Nothing was tuned.

This study is a **restatement by re-execution**: the committed study `20260904-wall-and-heating` (6,311 executed points, four arms) is re-executed point for point at the new pin, with the committed record's own readings joined to each point by coordinates, so the record can say what the window says at the paper's own ash rule — the feasible / ignited / driven counts, the design-column verdict at 100 and 220 MW, and how the stored-energy correction varies across the window (the owner asked that the fix "scales up for larger stellarators"; the effective ash exponent and W are carried per point). Two constant-scale counterfactuals of the same window exist as goal evidence (W scaled to 0.915 and 0.940 of itself, closure live); the record will read its counts beside them. Attack the framing of that comparison hardest: what may a re-execution at a changed profile family honestly claim beside a record at the old one, and beside two constant-scale counterfactuals?

## What to read

- `exploration/stellarator_e2e/studies/20260905-stored-energy-basis/study.py` — the study definition; its docstring says exactly what it inherits and what it changes; read `load_committed`, `_coord_key`, `export()` (the `committed_*` join and the WI-042 columns), `screen()` (a process pool) and `run()` (the screen/execute phases). **Start here.**
- `exploration/stellarator_e2e/studies/20260905-stored-energy-basis/record.md` §§ 1, 2, 7, 8, 9, 10, 11 as written so far; `axes.json` (inherited verbatim); `indicators.json`; `results/preflight_results.json`, `results/baseline_result.json`; `results/excluded_points.csv` if it exists yet.
- The committed record this study restates: `exploration/stellarator_e2e/studies/20260904-wall-and-heating/record.md` §§ 3, 4, 6, 11, 15, 17 and its two Addenda; `results/points.csv` and `results/oracle_operands.csv` (the columns the join reads); `results/excluded_points.csv` (the committed 65).
- What changed in the model and why: `work/active/WI-042_sourced-helium-ash-profile/design.md` (D1–D8, § Expected baseline behaviour, § Research findings "The shape scales") and `plan.md` § MR-WI042-14 restatement (why no multiplier re-reads a column) and its phase-5 record (the perturbations); `prototype/window_corners.json`.
- The goal: `work/orchestration/goals/stored-energy-basis/goal.md` (§ Question, § Answered when, § Invariants with amendments (b) and (c)); `trail.md` § Round 2 strategy revision, § T-001 return, § T-003 scope; `learnings.md` L-001..L-003; the counterfactuals `evidence/w_counterfactual/NOTES.md` §§ 5, 6, 8, `window_summary.json`, `window_summary_518.3.json`.
- `modeling_project/STUDY_POLICY.md` and `.claude/skills/run-study/runbook.md` (steps 2, 3, 4, 7, 11); `exploration/stellarator_e2e/studies/ANNEX.md` (§ Oracle, § Validity masks).

## What to attack

1. **The comparison's meaning.** The record will put this run's counts beside (a) the committed record's counts and (b) the two constant-scale counterfactuals. (a) is the same points at a different profile family: is "feasible / ignited / driven at the WI-042 family" comparable to the committed columns at all, and what must every comparative sentence carry (L-001: "the baseline is violated" is a statement about the basis)? (b) scaled W alone with the closure live; this run changes the profile family, which moves the line-averaged density, the radiation and the ash amount too — at the design point the counterfactual at 0.940 said 51.4 MW violated and the rule says 49.08 satisfied. Is a side-by-side with the counterfactuals informative or misleading, and what may the record say about them?
2. **The join.** `_coord_key` joins on nine rounded coordinates. Check it against both CSVs: does every executed committed point join exactly once, do the re-read arm's four efficiencies and the transect arm's τ values join to the right rows, and what happens at the 24 shared points the committed geometry grid carried instead of the re-read arm? Is `feasible_driven_changed` a sound column?
3. **The "restatement" framing itself.** Is re-executing the committed window the right read, or is the window now the wrong one — the closure's validity edge moves (row `#8`: 65 committed exclusions; the counterfactual predicted 35 more falling off at 0.940 of W; the design's prototype found the same corner failing under both chains), the ignited set was the most W-sensitive set (row `#4`: 787 → 1,282 ignited per level under the forced W), and the wall verdicts move through `p_fus`. If the edge moves inward, points the committed record executed will be excluded here: what must the record say about a comparison over a set that shrank? If the ignited set grows, is the one-sided fence's pass-through now the dominant fact and what does the record owe on it?
4. **"Scales up for larger stellarators."** The record will report the effective ash exponent and W's move (`W_ratio_vs_committed`) per point. The exponent depends on T_i0 and the profile exponents only (design D1; it does not move with the ash amount), so across the window it takes four values; the W ratio follows the ash fraction. Is "the shape scales" a claim this study can make, and what exactly? What would falsify it? Is there a held input whose constancy the claim rests on (`f_suppr_ash`, `tau_ratio_ash`, `alpha_n`, `alpha_T`, `iota_23`)?
5. **The design column.** At 100 MW the committed record found nothing feasible driven at a 1.3 at any (R, I, T, n); at the sourced 0.940 counterfactual the design geometry stayed closed by 0.03 MW/m² and 1.4 MW; at the printed 0.915 it opened (six driven points). At the rule the baseline itself is satisfied by 0.92 MW. What may the record claim about the design column, and how must it state the margin? Probe the oracle yourself along the design column (R 12.7, a 1.3, I 15 MA, T 14.63, n 1.0×) and its neighbours at 100 MW.
6. **The two mechanics.** The pooled screen and the screen/execute split: does either change what is evaluated or recorded? Does the cache under `results/_work/` create a way for the executed set to differ from the screened set? Is `p_net > 0` still the right evaluability criterion at the new chain?
7. **The held keys and the shadow columns.** Everything the committed study held is held here (the `wall_peak_*` anchor, `eta_couple_heat` 1.00, `j_wp`, availability 0.85). The committed study's shadow columns (the wall-anchor band; the magnet `a`-shadow) are inherited: are they still meaningful, or noise this record should drop or label?
8. **The W-basis disclosure and the counterfactual's constant-scale caveat.** Every reading must state its basis (review constraint 1). Check that the record's §§ 2, 11 and the study's docstring say the right thing, and say what the results sections must carry.
9. **Anything else:** arm tagging, the baseline as an explicit member, the per-case held-key assertions, the channel list (the store records only single-field floats — the four WI-042 channels are multi-field and oracle-side; is that disclosed and is the oracle the right source?), the no-shared-point assertion, the ordering (the screen before the critique), and the record's use of the committed record's definitions of "feasible", "ignited", "driven".

## Rules

- Python only through the primary checkout's interpreter: `/home/reid/1cfe/fusion-tea/.venv/bin/python` (never bare `python`, `python3`, `pip`; `uv run` from this worktree resolves the wrong venv). Before any oracle or package import: `set -a; source ~/1cfe/agentic-mbse/.env; source /home/reid/1cfe/fusion-tea/.venv/integration.env; set +a; export PYTHONPATH=$HOME/1cfe/teax/packages/teax-simkit:$PWD/exploration/stellarator_e2e/pkg` (from the worktree root). The oracle surface is `oracle_entry.evaluate(point)` (`exploration/stellarator_e2e/studies/oracle_entry.py`; a point is a dict of qualified entry keys — see `study.py` `point()` for the keys).
- **Never read anything under `knowledge/holdout/`.**
- Do not edit any file. Do not run the study. Do not commit. You may run read-only analysis and probe the oracle. Do not touch `results/_work/`.

## Return

A verdict of **MAJOR**, **MINOR**, or **CLEAN**, then a numbered list of findings. For each: what is wrong, why it matters, what it would take to fix, and how confident you are. Rank them — the author will act on the top ones first. If you believe the study is sound, say so plainly and say what you checked to conclude it; a critique that finds nothing but says what it looked at is a useful result, and an invented finding is not.
