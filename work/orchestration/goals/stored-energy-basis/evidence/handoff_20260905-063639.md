# Handoff: run-goal on `stored-energy-basis` — record the owner's grounding, open round 1, land the counterfactual and attribution as learnings; offer research runs; feed the result to the wall-and-heating round-2 review

Written 2026-09-05 from a session on `feat/demo-maturation` in `/home/reid/1cfe/fusion-tea`, working in the sibling worktree `/home/reid/1cfe/fusion-tea-stored-energy-basis` (branch `goal/stored-energy-basis`). Status marks: **[verified]** = checked in files or run this session; **[carried]** = taken from records or an earlier session and not re-checked here.

## Focus

You are the round agent for goal `stored-energy-basis` (`work/orchestration/goals/stored-energy-basis/`; `goal.md`, `trail.md`, `learnings.md`, `evidence/`). Invoke `/run-goal`; the procedure is `work/orchestration/GOAL_RUNBOOK.md`. Four things, in order:

1. **Record the grounding.** The owner ratified grounding in chat on 2026-09-05: *"agreed with grounding the goal"* `[OWNER-VERBATIM 2026-09-05]`. `goal.md` is at `draft`; write an `### Amendment 2026-09-05 — amends § Status` moving it to `grounded`, carrying the quote, and append the matching line to `trail.md` (precedent: `work/orchestration/goals/wall-and-heating/trail.md` § Amendment 2026-09-03). The owner did not separately adopt or reword § Question or § Answered when; if the owner is present, ask in one line whether they stand as worded. If not present, record them as `[AGENT] (grounding ratified by owner 2026-09-05; wording not separately reviewed)`. The slug already exists and the owner agreed to ground it under that name.
2. **Open round 1 with a write-up strategy.** The grounding evidence already answers § Answered when (a) and gives (b) a first pass. The round's job is to turn that evidence into a round result whose learning delta a fresh non-author review can accept, so the run has citable knowledge and the owner can re-grade the two inherited invariants. No study, no pin, no model change. Proposed learnings are below.
3. **Offer the owner the research option, and if taken, run it before the write-up task.** One or two bounded research-seam requests for the Stellaris paper's definitions. A found definition could reconcile the printed numbers and would change learning 2, so the research must return before the result is written. Expect a bounded negative; a durable negative is still the point.
4. **Get the counterfactual in front of the `wall-and-heating` round-2 review.** Its spawn prompt (`work/orchestration/goals/wall-and-heating/evidence/round2_review_prompt.md`, deposited at `1928925b`, 2026-09-05 06:30) does not mention this goal or the counterfactual `[verified]`. Details under Context.

Invariants that bind every step `[verified, goal.md § Invariants]`: nothing written under `models/`, `exploration/stellarator_e2e/generated/`, or `exploration/stellarator_e2e/studies/manifest.json`; the pin stays `c1b0f0d1…`; W is never tuned; every number is labelled oracle-side; the clean room holds (`knowledge/holdout/` never read; the Stellaris paper is admissible); a missing input is surfaced with options, never defaulted. Reserved gates (owner-held, none resolved): the W form or exponents in `models/`; re-ruling the inherited invariants and L-001's never-tune line; whether the result goes in front of the wall-and-heating review; any rubric Row 1 consequence.

## Context

**The goal, in one sentence** `[verified]`: does the pinned baseline's failing sustainment check rest on the model's stored-energy estimate being 9 % above the paper's printed 504.65 MJ, and if so what causes the 9 % and is the printed value the right target. Why it matters: two later goals inherited "the violated baseline is the W-form, not ours to fix" (`priced-levers/goal.md:52`, `wall-and-heating/goal.md:52`) from an agent-grade rule (`operating-point-closure/learnings.md` L-001) with no owner ruling and no backlog item.

**What the grounding evidence found** `[verified this session; evidence/w_counterfactual/NOTES.md at 89fc9f06]`:

- Basis A (the oracle, W scaled by 0.9151 in an in-memory copy of one function): at the baseline the requirement drops 90.6 → 37.5 MW and `sustainment_ok` flips to satisfied; the wall peak drops 4.088 → 3.861 and `wall_load_ok` flips too, entirely through fusion power falling 5.6 % as the ash fixed point re-closes (§ 3). Basis B (the operating-point prototype) agrees to 0.5 MW (§ 4).
- The committed `20260904-wall-and-heating` window, all 6,311 points: 1,128 sustainment flips violated → satisfied, 384 wall flips, 211 recirculation flips the other way; at 100 MW the design geometry opens (six driven points, the baseline among them at 332.6), the cheapest driven point moves 212.46 → 212.31, and 347 of the record's driven points stop being driven, 305 by igniting (§ 5). 61 points fail the closure's validity edge under the forced W; none was feasible in the record.
- Attribution (§ 7): no reading of the paper's plotted profiles with its printed peaks reaches 504.65 MJ. The caption's exponents on every species give 575; a figure-consistent family with the ash peaked gives 527–559; the model's 551 is a fair read of the figure (the WI-022 digitization, `work/completed/20260718_WI-022_predictive-confinement/prototype/fig16_curves.json`, refits at electrons 0.617, fuel 0.33, temperatures 1.19). Every reading reproduces the printed fusion power; none the printed energy.
- Two weaker points, stated as hints, not evidence: the printed β implies 567 MJ if referenced to the 9.0 T axis field (a different reference field reconciles it); the printed rows through the paper's own balance miss ignition by 61 MW (assumes the printed τ_E is the one in the balance).

**Assumptions behind the attribution, graded** `[verified]`: energy is 3/2 × pressure over volume, thermal species (strong; fast alphas are ~2 %); Fig. 16 is the profile set the table's energy came from (moderate — the printed average density is also more peaked than the figure, hinting the table used a different profile); volume grows with the square of normalized radius (moderate); β referenced to B_0 (weak); printed τ_E is the balance's (weak).

**What the owner was told and ruled** `[verified, chat 2026-09-05]`: the recommendation was do not tune toward 504.65; one short round to land three learnings; hand the counterfactual to the wall-and-heating review as a fifth condition; optional research request for the paper's definitions; defer a helium-shape model change (~2 % of W, would disturb every committed study). The owner ruled only *"agreed with grounding the goal"* and asked for this handoff "including offering the option to do research runs". Nothing else is ratified.

**Proposed learning delta for the round result** (agent wording; the fresh review accepts or corrects):

- L-001 — The baseline sustainment verdict and the study's driven set are stored-energy-sensitive: a 9 % change in W moves the design-point requirement by about 50 MW and flips 1,128 sustainment verdicts over the committed window; the wall verdict moves with it through ash-diluted fusion power. Evidence: `NOTES.md` §§ 3, 5. Scope: this package at pin `c1b0f0d1…`, constant-scale counterfactual, oracle-side. Implication: every sustainment-fence reading on this package carries W-sensitivity as a stated condition, beside the τ*/τ_E condition.
- L-002 — The printed 504.65 MJ is not derivable from the paper's stated profile family and printed peaks (527–575 MJ across readings) and is not a fit target. Evidence: `NOTES.md` § 7. Scope: conditional on the research option's return, if taken. Implication: no round tunes W or the exponents toward it; the model's balance stands as a fair read of the figure.
- L-003 — The model's profile exponents are a fair read of Fig. 16; the one shape the family gets wrong against the figure is the helium ash (it carries the fuel's exponent), worth about 2 % of W and a local charge imbalance off-axis. Evidence: `NOTES.md` § 7 item 2, `attribution_arithmetic.json`. Implication: a model increment is possible but small; owner-gated; deferred unless the owner wants it.
- Supersedes: refines `operating-point-closure` L-001's implication line. That goal is closed, so the re-grade is an owner ruling recorded in this goal's trail, with amendments to `priced-levers/goal.md:52` and `wall-and-heating/goal.md:52` as owner acts.

**The research option** (offer it; run it first if taken):

- REQ-W-01 — How does the Stellaris paper (Lion et al., Fusion Engineering and Design, in press; concept research 09, iter-02 `publikationen-1000179851-172386752/tmpissrtbos/raw.pdf`) define "Total plasma energy" and "Vol. av." in Table 5: which profiles (Fig. 16 or another set), which volume element, thermal only or including fast alphas? `gap_type`: `unsourced_value` or a definition gap; `consumer`: the model element `mfe_plasma_sustainment.W_th` / `stellarator_plant.alpha_n_e` (the guide accepts a model element as consumer).
- REQ-W-02 — The reference field in the paper's volume-averaged β, and any published point-A profile data or 0.5-D code description (supplementary material; Proxima technical notes; the Lion et al. 0.5-D systems papers the paper cites).
- `where_to_look`: the paper's supplementary material at the publisher; Proxima Fusion's publications page (`knowledge/concept_research/09-qi-stellarator-hts/proxima-fusion-technology-page.md`); Nuclear Fusion / FED stellarator systems-code papers by the same authors; the paper's ISS04 and 'Aurora' references. Limits as the precedent: `max_searches` 6, `max_captures` 3.
- Mechanics `[verified, docs/research_seam_operator_guide.md § Forming a request, § Running an invocation]`: write `knowledge/research/requests/REQ-W-0N.json`; `open` → `log --search` / `log --candidate … --triage keeper|rejected` / `log --failure … --reason …` → `source_registry.py register --run` → `close --adequacy exhausted`. `/research-acquire` drives the same. Everything under `knowledge/research/requests/` is committed evidence; commit the run directories with the round. Precedent request and spawn prompt: `knowledge/research/requests/REQ-WALL-01.json` and `wall-and-heating/evidence/T-001_REQ-WALL-01_prompt.md` — copy the clean-room screen from that prompt verbatim into any research subagent's instructions **before any fetch** (the hold-out is ARIES-CS, a stellarator; the registry guard fires only at registration). Deposit every spawn prompt as evidence before spawning. An author query is a person's act and returns as `OPERATOR_QUEUE`, not a negative.

**Feeding the wall-and-heating review** `[verified at 1928925b]`: round 2 closed on trigger 1 (`f51b2915`), C-001.r2 `PASS` with seventeen disposition rows landed (`8906d4e7`), the review prompt deposited (`1928925b`); no review entry in the committed trail yet. Three of the result's four stated conditions (L-009: the design geometry closed at 100 MW; the ash knife-edge; the one-sided fence) are the readings the counterfactual moves. Two paths: (a) if that review has not returned, the wall-and-heating operator adds `work/orchestration/goals/stored-energy-basis/evidence/w_counterfactual/NOTES.md` §§ 5, 8 to the reviewer's inputs, labelled oracle-side and conditional on the target, by a follow-up message or a redeposited prompt recorded in that trail; (b) if it has returned, the counterfactual lands as a constraint carried forward through the owner's close-packet ruling or an amendment to L-009's conditions. Either way it is wall-and-heating's own pen; this goal supplies the path. The path exists only on `goal/stored-energy-basis` until the branch is folded back.

**Branch topology** `[verified]`: `goal/stored-energy-basis` = `ea7304bc` + `89fc9f06` (the goal directory, additive). `feat/demo-maturation` = `ea7304bc` + `1928925b`. Diverged by one commit each, no overlapping paths. Rebasing the goal branch is safe (`git -C /home/reid/1cfe/fusion-tea-stored-energy-basis rebase feat/demo-maturation`); merging into `feat/demo-maturation` is the owner's act. `.project/CURRENT_WORK.md` in the primary checkout carries an uncommitted pointer block written this session (the owner's file; leave it to them). Commit with explicit pathspecs only (`git add -- <paths>; git commit -m … -- <paths>`); the owner keeps files staged and a bare commit sweeps them.

**Environment** `[verified]`: from the worktree, run Python with the primary checkout's interpreter, `/home/reid/1cfe/fusion-tea/.venv/bin/python` (the documented exception, `docs/integration_seam_operator_guide.md` § Running from a second checkout or worktree); `uv run` from the worktree would resolve a different venv. The counterfactual scripts (`w_counterfactual.py baseline | parity N | window [workers]`) run that way; the window pass takes about seven minutes on ten workers. The goal-contract suite `tests/orchestration/test_goal_contract.py` runs the same way.

**Test state** `[verified]`: that suite passes 28 of 29 on both branches. `test_narratives_are_separate_from_the_goal_contract` fails because `wall-and-heating/trail.md` at `f51b2915` contains the string `work/narratives/`, which the test forbids in goal files. Pre-existing on `feat/demo-maturation`; not this goal's; tell the owner.

## Key discoveries (verified this session unless marked)

- The extracted text's Fig. 16 caption is wrong ("Eq. (1), α_n 1.2, α_T 3.0"); the page reads Eq. (3), α_T 1.2, α_n 0.35, and the extracted Table 5 carries phantom rows. The sysml doc comment at `stellarator_plant.sysml:562-566` already records the caption error. Render pages with pymupdf (`uv run python`, `fitz`) and read them; never trust `output.md` for these tables.
- The WI-022 digitization dumped no helium points (`fig16_curves.json`); the helium exponent in the attribution is by eye (~2, bracketed 1–3). A digitized helium curve would tighten L-003.
- The forced-W state is not "closer to the paper" on every row: fusion power ends 4.7 % under the printed 2700 and ash 13 % over the printed value; the realized W ratio over the window is 0.908–0.913, not the applied 0.915, because the ash re-closes.
- Two readings of "printed W" disagree on the verdict: printed W with the closed-form τ_E rising (37.5 MW, passes); printed W with the printed τ_E held (56 MW, fails). The paper's own printed rows do not close its appendix balance at zero auxiliary power (−61 MW). Say which reading a claim uses.
- The point construction reproduces the committed record exactly (60 points, zero mismatches in verdicts, `p_aux_required` to 1e-6 MW, LCOE to 1e-9 relative).
- The primary branch moved three times during the previous session. Re-run `git log --oneline -3` before citing state and before committing.
- `[carried]` The owner's working preferences, from memory: never use the AskUserQuestion tool, ask in prose; no unsolicited fixes; plain language; one line per markdown paragraph; commit with explicit pathspecs; worktrees as siblings; never list merge as an open item.

## Suggested skills

- `/run-goal` — `ground` to record the ratification amendment, then `round` for round 1; the runbook's § Running one task and § Opening and closing a round govern the entries.
- `/research-acquire` — only if the owner takes the research option; it drives the seam's open/log/register/close.
- `/record-learning` — after the fresh review accepts the delta, so the three claims land in `learnings.md`.
- `/narrate-goal` — at close, for a snapshot beside the others in the narratives directory (a presentation surface, never goal state).
- `/_my_wrap_up` — at session end; update `.project/CURRENT_WORK.md` on the primary branch with the goal's state.

## Open questions

- **Wording.** Does § Question / § Answered when stand as drafted? Owner; one line.
- **Research.** Does the owner want REQ-W-01, REQ-W-02, both, or neither? If either, it runs before the write-up task.
- **The inherited invariants and L-001's never-tune line.** Owner re-grade after the review; recorded as rulings in this goal's trail and amendments in the two goal files.
- **Timing against the wall-and-heating review.** Whether that review has returned decides path (a) or (b) above; check its trail first.
- **Fold-back.** Owner's act; the round agent may rebase the goal branch but does not merge.
- **The failing narrative-separation test** on `feat/demo-maturation` — the owner should know; not this goal's to fix.
