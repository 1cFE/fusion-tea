# Round 2 fresh review — deposited verbatim 2026-09-06 (the reviewer's final message; a fresh session spawned from `round2_review_prompt.md` at `5a855307`)

`tests/study` finished: **366 passed, 1 skipped in 9 min 51 s**, with the working licence key. I have everything. Here is the review.

---

# Round 2 fresh review — goal `stored-energy-basis`, round `ash-profile-from-the-rule` — 2026-09-06

**Reviewer.** A fresh session spawned from `evidence/round2_review_prompt.md` (`5a855307`), in the worktree `/home/reid/1cfe/fusion-tea-stored-energy-basis` on `goal/stored-energy-basis` at `5a855307`, working tree clean. No part of this goal, WI-042, the pin, the study, the synthesis, the checkpoint or the result was authored by this session. Nothing under `knowledge/holdout/` was read. No file was edited; no state-changing git command was run.

**Freshness disclosure.** The scratchpad directory this session was given is the same one the round agent used; it holds the round's working files (`apply_r2.py`, `checkpoint_r1_result.txt`, `failed_r2_cmd.txt`, `integration-run-wi042/`, `probe1-4.txt`, `recount*.py`/`.txt`, `stellaris_pages/`, `wi042_proto/`). I listed the names and read none of them; my own scripts and battery outputs are under a separate `review/` subdirectory.

**Environment disclosure (bears on the battery check).** The SysIDE licence key in `~/1cfe/agentic-mbse/.env` reads `ImportError: License expired` on 2026-09-06. Under the prescribed command `tests/models` fails at collection and 22 `tests/study` tests fail at the integration seam's regeneration gate with the same error. The key in `/home/reid/1cfe/fusion-tea/.env` imports cleanly (`syside 0.8.4`). The batteries were re-run with that file sourced in addition — the only deviation from the prescribed command — and reproduce the round's counts. The seam's committed return of 2026-09-05 11:11 shows syside-dependent gates passing, so the lapse happened after the round's runs. Environmental, not the round's; carried as a constraint below.

**The Stellaris page images and `raw.pdf` are not present locally** (R2-synced, not pulled in this worktree). I read the extracted text at `stellaris-design-details.md` lines 708 and 2945–2961, which is legible for the sentence and equations at issue; the design's reading of the page renders is checked against that text, not against the images.

---

## Verdict: `FINDINGS`

**Grounds.** Every cited ref resolves and says what the trail claims; every number I rely on reproduces from `results/points.csv` and `results/excluded_points.csv`; the strategy was pursued as declared, in order, with one pin and one committed study; every task stayed inside its scope, the T-003 amendment included; zero goal-level retries; the checkpoint cap respected; every touched discovery row landed as a class-bearing row and the findings moved; no cited artifact moved outside its task; the batteries are green (with the licence caveat); § Answered when (a), (b) and (c) are met; no reserved gate was crossed. Eight findings, all precision-level, none reopening a task — F1 is the one that matters for the record's honesty, F4 changes a learning's form, F5 is an environment constraint the owner must act on.

---

## 1. Every cited ref resolves — the recounts

**Commit sequence** (`git log`): `f49da0f0` → `b7baaf41` → `17d64bef` → `ae6c327e` → `9ac167b8` → `0c20e373` → `b75362c1` → `af75a749` → `5ab0452c` → `dcc77967` → `9a78b92c` → `0b181b9c` → `700bfe6d` → `2d402aea` → `53eb5671` → `830497b8` → `5a855307`, exactly as the prompt lists. Timestamps: `5ab0452c` 11:46:40, `dcc77967` 11:47:44, `9a78b92c` 12:22:19, `0b181b9c` 15:14:59 (all 2026-09-05); `2d402aea` 08:15, `53eb5671` 08:19, `830497b8` 08:24 (2026-09-06).

**Recounted from the CSVs with my own script** (7,712 executed rows; 164 excluded rows):

| Claim | Recount | Match |
|---|---|---|
| Classes 6,283 `executed_in_both` / 1,429 `not_proposed_by_committed` | 6,283 / 1,429 | yes |
| Committed driven points' fate 510 / 146 / 25 | `transition_vs_committed`: driven→ignited 510, driven→driven 146, driven→violated 25 (681 total: 257 / 400 / 24 / 0 by arm) | yes — see F2 on the wording |
| Driven at the rule over the committed window 131 / 181 / 45 / 3 vs committed 257 / 400 / 24 / 0 | 131 / 181 / 45 / 3; committed 257 / 400 / 24 / 0 | yes |
| Restored 13 keV rows 175 / 191 driven, none ignited | fence 734 rows, 175 driven, 0 ignited; search 695 rows, 191 driven, 0 ignited; every restored row at T 13.0 | yes |
| Verdict flips: sustainment 964, wall 192, beta 156 violated→satisfied; recirc 111 satisfied→violated | 964 (0 the other way) / 192 (0) / 156 (0) / 111 (0 the other way) | yes |
| Cheapest driven `c2823` 202.192; `c3584` 221.022; `c6466` 218.950; `c7227` 240.377; `c7625` 370.551 | 202.192 (R 15.7, a 2.2, 13 MA, 13 keV, 1.0×, peak 3.961, 33.34 MW); 221.022 (17.2, 2.2, 13 MA, 17 keV, 0.6×); 218.950; 240.377; 370.551 (12.7, 1.3, 14.25 MA, 17 keV, 0.8×, η 0.60) | yes |
| Design column at 100 MW: one driven point `c3694`, 49.08 / 50, 3.979 / 4.05, 24.9 / 24.9, coupling 1.00 | 126 rows; exactly one driven, the baseline: 49.0796 MW, 3.9788, 24.9, `eta_couple_heat` 1.0, W 519.914, LCOE 322.318; I 15 MA at 1.0× reads 62.66 MW and 4.072 | yes |
| Design column at 220 MW 47 driven (committed 26) | 47 (45 re-read arm + 2 search arm); committed 26 | yes |
| `rule_vs_scales` 3,456 / 478 / 2,316 | recomputed from the three requirement columns over the 6,250 rows with both scale values: below 3,456 / between 478 / above 2,316 (33 rows have no scale value) | yes |
| W ratio 0.788–1.018, mean 0.919; monotone in He/n_e | min 0.7882, max 1.0180, mean 0.9191 over 6,283; Spearman −0.9978 against `He_over_ne_oracle`; 145 rows above 1.0, all with He/n_e ≤ 0.0619; per-(R, a) means 0.826 at (11.2, 2.2) to 0.995 at (17.2, 1.3) — the Addendum's corrected extremes | yes |
| `alpha_He_eff` 4.268 / 4.052 / 3.885 / 3.770 / 3.661 at 13 / 14.63 / 16 / 17 / 18 keV, a function of T only | one value per temperature over 1,429–1,574 rows each, min = max to four decimals across every R, a, I, n and τ*/τ_E | yes, and stronger than stated |
| Transect anchors `c1856` −50.8, `c5495` −31.4; the 8 → 12 move | `c1856` (R 14.2, a 1.8, 14 MA, 16 keV, 0.8×, ratio 8) −50.83 MW, committed `c1500` driven at +23.09; `c5495` −31.43, committed `c4459` driven at +59.47; the 100 MW anchor's transect: ignited at 2/4/6 (−294.7/−183.9/−107.1), driven at 12 (25.57 MW, peak 3.115; committed 141.86), blocked at 16 (74.55); the design column: ignited at 2–4, wall-blocked at 6 (23.12 MW, peak 4.439), sustainment-blocked at 12–16 (85.57 / 109.67; committed 158.43 / 205.84) | yes |
| Excluded 71 / 65 / 28 | `not_proposed_by_committed` 71, `excluded_in_both` 65, `committed_executed_now_excluded` 28; reasons 94 non-positive fuel, 70 non-real (complex) | yes |
| Baseline channels at T-001 | `evidence/round2_T-001_baseline_result.json`: LCOE 322.31843948570247, peak 3.9788448937763854, β 0.025305, nine verdicts satisfied; the single-runner output: W_th 519.914213988, p_aux_required 49.079600788, τ_E 1.557333622, n_He0 6.0436e19, `alpha_He_eff` 4.051846994, bit-exact vs the oracle on every listed channel, W–β identity 0.0; the committed record's `baseline_result.json`: LCOE 313.5134115016116, peak 4.088044684423419, `sustainment_ok` and `wall_load_ok` violated; the joined baseline row: committed 90.605 MW at 551.444 MJ | yes |
| Perturbations | T_i0 → 18: `alpha_He_eff` 4.0518 → 3.6614, W 642.5, p_aux 15.84; τ*/τ_E → 4: `alpha_He_eff` 4.0518 unchanged, n_He0 6.044e19 → 3.661e19, p_aux −11.08 | yes — see F3 on "halves" |
| Round 1's 220 MW grid: 45 driven (7/9/13/16), 8 ignited, 11 of 279 wall flips, LCOE +2.57 % (+1.24 to +4.26), `c6256` → 389.50 | 45 (0.45: 7, 0.50: 9, 0.55: 13, 0.60: 16), committed 24 (1/3/8/12); 8 ignited; 279 committed wall violations, 11 flip; mean +2.573 %, range +1.240 to +4.256; `c6256` → `c7657` 389.497, driven | yes |
| Driven by `a` 1/38/59/63/69/76; ignited by `a` 41 → 460; driven by T 175/63/30/22/16 | all reproduced | yes |
| `W_th_MJ_store` vs oracle worst 5.5e-16 | 5.519e-16 over 7,712 rows | yes |
| L-004's slope 0.96–1.11 MW/MJ | at the baseline row: (90.605−37.469)/(551.444−501.667) = 1.0675; (90.605−51.377)/(551.444−516.174) = 1.1122; between the two scale points 0.9587; the 0.92 MW margin is 0.83–0.96 MJ at those slopes and 0.177 % of W | yes |
| `c1041` sign exception (L-006) | committed `c1041` (R 12.7, a 2.0, 16 MA, 14.63, 1.0×) → this record's `c1287`: committed +130.37 violated, scales +139.13 / +135.62, rule −67.00 (a negative requirement; state `violated` because another fence fails) | yes — the "ignited" is the column sense, see F2 |
| `c1721` | → `c2132`: committed +7.44 driven, scales +5.94 / +4.71, rule −188.76 ignited | yes |
| L-008: 117 MW at 13 keV committed; 33 MW at the rule's anchor | committed `window_edges.json` T transect at (R 14.2, a 1.8): 117.07 at 13 keV; the rule's `window_edges.json` at (R 15.7, a 2.2, 13 MA, 1.0×): 91.88 at 12.5, 33.34 at 13, −23.21 at 13.5; on the committed anchor's own coordinates the rule reads 56.84 MW at 13 keV, caught | numbers yes — see F4 on the form |
| Manifest / seam / snapshot fingerprints | `manifest.json` indicator `ec984adc1572…`, semantic `c37fb58a2b19…`, executable `8ac14fdf3745…`; the seam return `CANDIDATE`, ten gates `pass`, the same three values and teax `744745f8…`; the study's `snapshot.json` the same three, `repo_commit 9a78b92c`, counts 7,876 / 7,712 / 164, feasible 1,839, ignited 3,654, driven 726 | yes |

**The source.** `stellaris-design-details.md:708`: "Helium ash profiles are obtained using a fixed ratio of particle-to-energy confinement time." Lines 2950–2961: A.5 `n_He / τ*_α = n_D n_T ⟨σv⟩_T`; "τ*_α = ρ* τ_E, where we use ρ* ∼ 8"; A.6 `n'_He = f_suppr n_He`, f_suppr = ½. The design's reading (τ* uniform in ρ, the profile the fusion-rate shape) is the only one the text supports. The two Lion registrations exist at `d4059ef1` under `knowledge/sources/` with their `SOURCE_INDEX.md` blocks stating Eq. (8)–(11) / (2.10)–(2.13) and the caveat that the definition is stellarator-PROCESS's, not the Stellaris paper's own.

**The model change** (`git diff f49da0f0..0c20e373 -- models/`: four files, +211/−138). The only binding line that changed in `stellarator_plant.sysml` is the retirement of `:>> alpha_n_e = 0.596`; `T_i0` 14.63, `alpha_n` 0.33, `alpha_T` 1.19, `n_e0` 5.06e20, `iota_23`, `f_ren`, `tau_ratio_ash` 8.0, `f_suppr_ash` 0.5 are byte-identical. The sustainment calc's doc carries `n_He(ρ) = n_He0 S(ρ)` with S the fusion-rate shape, `n_e = 2 n_D0 u^α_n + 2 n_He0 S`, `p_avg` as the one pressure integral, `W_th = 1.5e-6 p_avg V`; the beta calc reads `p_avg_in`. The impl and the oracle's `_sustainment` carry the same equations (S(u), four trapezoid integrals on the 200,000-interval grid, the exponent as a least-squares slope over 1,700 nodes); `alpha_n_e` survives only as the derived `alpha_n_e_eff`; `oracle_entry.py` retires the entry key and maps the four channels.

---

## 2. Goal and strategy fidelity

Pursued as declared and in order: WI-042 minted (`17d64bef`) → spec → design → plan → implement (`ae6c327e` A, `9ac167b8` B, `0c20e373`), one pin (`af75a749`, the only manifest change in the round is at `9ac167b8`), one committed study (`0b181b9c`, one record directory). The item is what the approach named: A.5 pointwise with τ* uniform, electrons by quasi-neutrality, the ash amount's fixed point unchanged in form, one ⟨p⟩ with two consumers, the W integral form and the reserved bindings untouched. **Assumptions:** (1) held (`alpha_n_e_eff` 0.5953 vs 0.596; ⟨n_e⟩ 3.172e20 vs 3.17); (2) held (τ*/τ_E → 4 leaves the shape at 4.0518); (3) held (the single runner's oracle gate lists every heating, magnet and geometry channel bit-identical); (4) **held in direction, not in number** as reported (51.4 violated by 1.4 expected; 49.08 satisfied by 0.92 landed; the line average 38.34 → 37.78 and the radiation 228.6 → 219.7 moved the other way from the counterfactual's guess); (5) held (no multiplier; the re-execution); (6) held (`prototype/window_corners.json`: 23 corners, W ratios 0.991 at (17.2, 1.3) to 0.768–0.786 at (11.2, 2.2), one fuel-guard failure); (7) held (batteries green, fixtures re-derived). **Abandonment conditions:** none met — no defaulted number, no reserved quantity changed, the codegen carried the form (the re-stencil was the documented trap, recovered), the shape held across the window.

---

## 3. Task scopes

**(i) T-001's form.** Inside. The change is the ruling and nothing more: the ash from the rule at every lever point (the impl computes S(u) per evaluation; no literal near 4 anywhere), the electrons by quasi-neutrality pointwise, the peak fixed point unchanged in contract (damped half-step, 1e12, cap 200, fail-loud), the W form `1.5 ⟨p⟩ V` unchanged, the exponents and peaks byte-identical. D1's retirement of `alpha_n_e` and D2's beta calc reading the one ⟨p⟩ are inside the scope — the scope authorized `alpha_n_e`'s fate "decided explicitly" (round-1 constraint 3) and edits to 'Volume-Averaged Beta'; neither touches a reserved quantity, and the retired exponent was itself a point-A binding the owner's scaling line forbids. Not beyond the reserved gate.

**(ii) The baseline flips.** Disclosed at every claim site (the instance's `sustainment_ok` comment, SV-054, the plan's restatement (b), the manifest's expected verdicts, the T-001 return's fourth decision) and never tuned: nothing near either fence was bound or adjusted (the binding-line diff is the one retirement). The perturbations verify: T_i0 → 18 moves the shape 4.052 → 3.661; τ*/τ_E → 4 leaves it at 4.052 and cuts the amount 6.04e19 → 3.66e19 (F3: a 39 % fall, not a halving).

**(iii) MR-WI042-14.** Written in commit A (`ae6c327e`: `plan.md` +294 lines including § MR-WI042-14 (a)–(g), naming all eight committed studies) before any regenerated byte (commit B `9ac167b8`, 27 minutes later). The study confirms what it said: the seven earlier records untouched (`git log` over `20260904-wall-and-heating/` since `f49da0f0` is empty; the earlier records are not touched either), the `20260904` window re-executed (6,283 of its 6,311 points, joined by nine coordinates, never multiplied), the closure edge measured (28 further in, as (f) said it might).

**(iv) The owner's scaling clause.** Met by construction (the shape is computed from the rule at every point; nothing bound at point A enters it except α_n and α_T, which the ruling reserved) and demonstrated three ways: the design's window table before regeneration; `alpha_He_eff` exactly one value per temperature across every geometry, current, density and τ*/τ_E in 7,712 executed points; the W ratio 0.79–1.02 following the ash fraction at −0.998. The result states what it rests on honestly (α_n, α_T, Ti/Te, τ* uniform in ρ, f_suppr × τ*/τ_E, ι — point-A facts; the committed `#2` research request). One precision: the *shape* rests on T_i0, α_n, α_T and the reactivity only; Ti/Te and the transport facts enter the *amount* and therefore the W correction. Both are stated in the record § 6; the learning should keep them apart (L-007 below).

**(v) T-002's pin.** `CANDIDATE` on all ten gates at the first run; the fingerprints in the trail, the manifest, the seam return, the study's snapshot and `package_identity.json` all agree; teax unchanged.

**(vi) The T-003 amendment.** A legitimate extension under round 1's carry-forward rule, not drift. The git order attests the sequence: the amendment, the critique and its probes at `5ab0452c` (11:46:40); the path fix at `dcc77967` (11:47:44); the screen at `9a78b92c` (12:22:19); the record at `0b181b9c` (15:14:59). The two critique-driven changes are what the amendment says (the 13 keV rows as a named class never entering a committed comparison; the edges re-read in `edges.py`, 138 rows), plus the per-point counterfactual join and the three F5 guards, which I read in `study.py` (cache stamped with the proposal count, the oracle's source digest and the package identity; an existing store refused; the executed key set asserted equal to the screened set). No new axis; the window is the committed grid before its own F6 cut, on the ground the cut rested on.

**(vii) The held axes and the comparison rule.** Followed. `axes.json` inherited; the comparison is by nine coordinates against the committed columns (`committed_*` read, never recomputed; the snapshot carries the digests of the three committed files and the two counterfactual CSVs); `feasible`, `ignited` (p_aux < 0) and `feasible_driven` (feasible and p_aux ≥ 0) keep the committed definitions — I checked the column identities (0 mismatches on both). The record adds its own three-state classification (`state_here`) on top, which is where F2 arises.

---

## 4. Retry classification

Zero goal-level retries, correctly. The two screen launches failed at load on a one-line path constant whose tail a comment had swallowed (`dcc77967`, +2/−1); no oracle evaluation, no store, no task-level outcome — native-stage corrections, not `MECHANICAL_FAILURE` returns. The codegen re-stencil is the documented manual-stage trap (`Regenerated: 2` → restore → `Regenerated: 0`); the first regeneration exited 0 and did what it documents — a native step, not a retry. The failed harness command wrote nothing and the work was redone as separate commands the next day — a tooling slip outside any native stage; neither a retry nor a native correction, and honestly disclosed (the r1 entry's date is its verdict's, its commit the next morning's). The 164 excluded proposals are the oracle pre-screen's evaluability exclusions, the committed study's own pattern — neither.

---

## 5. The checkpoint sequence

C-001.r1 `REVISE` (2026-09-05; four wording points, every number upheld) → r2 with every change marked `[r2]` (`2d402aea`: 11 insertions / 9 deletions in the dispositions file, r1 recoverable at `700bfe6d`) → resubmitted to the same session, resumed by message → r2 `PASS` (2026-09-06). One revision spent of two; the cap not reached. The in-place form meets the runbook's "each submission is a new entry": the trail carries two new `rK` entries, neither amended, and the `wall-and-heating` round-2 precedent (its review's F5) accepted the marked-revision form. The round-1 practice — r1/r2/r3 as sections in the file — is the stronger form because a reader recovers the disagreement without `git show`; a form point (F6), not a defect. Stripping the `[r2]` markers when the ten rows landed was right: the log carries dispositions, the trail carries the revision history.

---

## 6. Discovery rows

Eight sightings at `0b181b9c` (`20260905-stored-energy-basis#1..#8`), ten joined rows at `53eb5671`, each with a class from the ADR-0004 set, a status, a responsible actor and a concrete next reference; no sighting edited; no id minted; `test_records.py` 28 passed.

- **`20260904-wall-and-heating#1`** — `model fix`, **discharged by WI-042**: right. The fix landed (the item's plan complete at `b75362c1`, the pin at `af75a749`) and the finding's reading moved (the (b)(ii) region ignites; the column opens at the design point alone). "The fifth condition a disclosure at the rule's basis" is right and the checkpoint's correction of "measured" was necessary: the record measured the committed reading's response to the family change, not the new reading's own W-sensitivity (no second basis at the rule; the baseline sits off the constant-scale line — 519.9 MJ → 49.08 MW against the 0.940 scale's 516.2 → 51.38). The item close is owner-held and the row says so.
- **`#2`** — `research`, open, re-measured with the edges bracketed in (8, 12) and (12, 16): right; the anchors' own ratio-8 points ignite (Addendum item 8 carried).
- **`#3`** — `model fix`, open, re-sighted; ignition grows with `a` (41 → 460): right.
- **`#4`** — `model fix`, open, **now load-bearing**: right — 3,654 negative-requirement points, 510 of the committed 681 driven points in the ignited state; the cheapest "feasible" points are ignited unless the driven set is read separately.
- **`#6`** (discharged, re-sighted; 45 vs 24), **`#8`** (`declared seam`, 28 further in, fewer than the predicted 35), **`#7`** (`none owed`, unchanged, one line): right, each saying what moved.
- **This record's `#5`, `#7`, `#8`** — the Addendum's corrections as joined rows, the sightings standing: right (items 4, 3 and 8 respectively).
- **This record's `#1`, `#2`, `#3`, `#4`, `#6` routed in the sighting row, no joined row.** Acceptable under the runbook for `#1`, `#2`, `#4`, `#6`: the sighting is the executor's account and carries class, status, actor and next reference; nothing moved after it. **Not for `#3`** — its sighting carries the "inside the source's own 2.7 % residual … about 14 MJ, about 15 MW" reasoning that the checkpoint corrected in direction at L-004; like `#5`, `#7`, `#8`, it owes one joined row carrying the corrected form (F1). The reading (undetermined, on the boundary, never "feasible") stands; the stated reason changed.
- **The committed `#5`** untouched: right — the lifetime chain is upstream of nothing WI-042 changed and the record § 17 says its columns were inherited, not re-read.

---

## 7. External mutation

None. `git log f49da0f0..HEAD` over every cited path: `models/` and the twins only at `ae6c327e`; the generated package, `manifest.json`, `stellarator.snapshot.json`, the census, the six fixtures, `test_known_answers.py`, `test_single_point_gate.py`, `test_operand_bindings.py`, `verify_stellaris.py`, `oracle_entry.py`, the two runners, `VALIDATION_MATRIX.md`, the traceability CSV and `ANNEX.md` only at `9ac167b8`; `test_verify.py` only at `0c20e373`; `work/active/WI-042_…` last at `b75362c1`, the sha the pin cites, nothing after; the study record directory at `5ab0452c`, `dcc77967`, `9a78b92c`, `0b181b9c`, `700bfe6d` (the Addendum) and nothing after; `DISCOVERY_LOG.md` at `0b181b9c` and `53eb5671`; `20260904-wall-and-heating/`, `evidence/w_counterfactual/`, `learnings.md`, both runbooks, `knowledge/sources/` untouched. The twins agree on every shared file (`diff -r` reports only the IFE files under `models/`, outside the stellarator package and not in the round's diff). The re-pin at `9ac167b8` (manifest, census, fixtures, known-answers test) is inside T-001 — its scope authorized "regeneration through the pinned codegen and the snapshot, manifest, census and fixture re-derivation it forces". `ANNEX.md` § Baseline pin and § Oracle at `9ac167b8` — inside T-001, disclosed in the return as a restatement site the plan's surface table missed; the § Baseline pin text is the WI-042 state with the pin's history. The record's one Addendum (`700bfe6d`) is the study runbook's own mechanism (statement corrections after the administrator's recount; `results/`, `snapshot.json`, `indicators.json` untouched — the snapshot's digests confirm). The study's `__pycache__/` and `results/_work/` are ignored by rule (`git check-ignore`), not evidence.

---

## 8. The battery

Run from the worktree root in the sealed env with `PYTHONPATH` unset (verified empty), `.integration_workspace` removed before each suite:

| Suite | Prescribed key (agentic-mbse `.env`) | With the primary checkout's key added | Round's claim |
|---|---|---|---|
| `tests/models` | collection error: `syside` "License expired" | **48 passed / 13 skipped** (11.5 s) | 48 / 13 at `9ac167b8` |
| `tests/study/test_records.py` | 28 passed | **28 passed** | 28 |
| `tests/orchestration/test_goal_contract.py` | 28 passed / 1 failed | **28 passed / 1 failed** — `test_narratives_are_separate_from_the_goal_contract` on `wall-and-heating/trail.md`, the owner-ruled line; the test's glob covers only `goal.md`/`trail.md`/`learnings.md` per goal, and this goal's three carry no `work/narratives/` string (the two hits under this goal are `evidence/handoff_*.md`, outside the glob) | 28 / 1 |
| `tests/study` | 344 passed / 14 failed / 8 errors / 1 skipped — all 22 in `test_integrate_*`, every one the seam's regeneration gate failing on the licence | **366 passed / 1 skipped** (9 min 51 s) | 363 / 1 at `0c20e373` |

366 = 363 + the three parametrized cases `test_records.py` gained when the new record landed (25 → 28); consistent. The licence lapse is F5.

---

## 9. § Answered when, word by word

**(a)** Met at grounding and ratified; unchanged by round 2.

**(b)** "Either a written attribution of the +9.2 % or a bounded negative" — met in round 1 on the written-attribution clause (round-1 review F4). Round 2 does not disturb it; it corroborates it in the package: SV-053 reproduces the attribution's anchor rows with the impl's own helpers at the printed peaks — 524.523 MJ at the model's exponents, 518.308 at the paper's (the round-1 script's rows to every printed digit, two integrators). The executed baseline's 519.914 differs from 518.3 because the *converged* peaks (n_He0 6.04e19, not the printed 5.6e19) enter, not because the rules differ. The decomposition 5.3 + 1.2 + 2.7 stands as written.

**(c)** "The owner rules what stored-energy basis the model carries … and any model change returns to the owner as a minted work item through the modelling PM." The owner ruled (ii) on 2026-09-05; WI-042 is the minted item, landed, pinned, its restating study committed; the scaling line in § Amendment (b) is met by construction and demonstrated. **(c) is met by the ruling plus its executed vehicle.** The clause asks that a model change *return to the owner as a minted item* — it has, and it is in front of the owner for close. The item's close and archive are owner-held acts in the close packet, not conditions of (c); reading them as conditions would make (c) unmeetable by any agent, which is not what the clause says.

**The demo statement.** The ratified next step — "record the design point's sustainment as undetermined within the source's own precision — on the boundary" — is what the package says: nine verdicts satisfied, `sustainment_ok` by 0.92 MW at the held coupling 1.00 (49.0 MW coupled at 0.98 violates), the wall by 0.071 MW/m², the ceiling at equality. **L-004's form is the honest statement of it**, and it is more honest than the form the native artifacts carry: the model's W at the rule (519.9) lies *above* both the sourced-rules 518.3 and the printed 504.65, and the verdict is satisfied throughout that band, so the 2.7 % residual on the printed value cannot be what makes the point undecidable; what can is the source's two-sided internal spread (the β row's 567 MJ and the fast-alpha term upward; the printed W downward) against a flip roughly 1 MJ above the model's own W on a slope borrowed from the constant-scale pairs. "Needs 90 MW" is decided as a retired basis; "is feasible" is the undecidable one. The § Close rule is the owner's.

---

## 10. Reserved gates

None crossed. The W form, the exponents and the peaks: untouched (the binding-line diff). The inherited invariants' re-grade: owner-held, carried in the packet. `goal.md` § Amendment (c): an agent-grade reading, surfaced with its trigger and marked `[AGENT] … awaiting ratification` before the round acted on it (`b7baaf41`); it is the only reading consistent with the owner's (ii) ruling (a minted item through the modelling PM cannot land without writing under `models/` and re-pinning), the Package line's original purpose — that `c1b0f0d1…` stays the pin of record for every round-1 number and every comparison — is preserved, and round 1's second invariant already anticipated it. Surfaced honestly, not a gate crossed; ratification is in the packet.

---

## Findings

- **F1 — The reason for "undetermined" is stated two ways, and the native artifacts carry the superseded one.** MR-WI042-9 *requires* the reading "inside the source's own 2.7 % residual, so … undetermined within the source's precision"; the instance's `sustainment_ok` comment, SV-043, SV-054, ANNEX § Baseline pin, the design, the plan's restatement (b), the record § 15 `#3` and its sighting row all carry it. The checkpoint corrected the direction at L-004 (the flip lies above the sourced-rules W; the undecidability rests on the two-sided spread) and the T-003 return and round result adopt the corrected form. Every number agrees; the stated reason differs. Actions: (a) one joined row under `20260905-stored-energy-basis#3` in L-004's form (the next writer's, like `#5`/`#7`/`#8`); (b) the owner should know that WI-042's spec sentence and the model's doc comment carry the earlier reasoning — a doc-text item or a dated spec amendment at the owner's call; never an edit of the pinned model outside an item. Precision; nothing reopens.
- **F2 — "510 … read a negative requirement" counts the ignited *state*, not the column.** 523 committed driven points read a negative requirement at the rule; 510 of them also satisfy every other fence (the record's `state_here = ignited`); 13 read negative and fail `recirc_ok` (classed `violated`, inside the "25 become violated"). The two senses of "ignited" both appear in the round's prose (the column: 3,654 points; the state: 1,113), and `c1041`'s "−67.0 ignited" in L-006 is the column sense. The learnings below say which sense each number uses.
- **F3 — "The ash amount halves" under τ*/τ_E → 4** (T-001 return, SV-054, design validation plan 6): 6.04e19 → 3.66e19 is a 39 % fall (ratio 0.606); the fixed point is nonlinear. The shape's invariance (4.0518 exactly) is the claim that matters and is exact. Wording.
- **F4 — L-008 compares two anchors.** 117 MW at 13 keV is on the committed anchor's T transect (R 14.2, a 1.8, 14 MA, 0.8×) at the old family; 33.3 MW is on the rule's anchor (R 15.7, a 2.2, 13 MA, 1.0×). On the committed anchor's own coordinates the rule reads 56.8 MW at 13 keV — still caught. The honest claim is that the F6 cut removed a row that is driven at the rule at large `a` (175 points at 100 MW), not that the old anchor's 13 keV point opened. Corrected in L-008's form.
- **F5 — The SysIDE licence in `~/1cfe/agentic-mbse/.env` expired between 2026-09-05 11:11 and 2026-09-06.** Under the prescribed command `tests/models` cannot be collected and 22 `tests/study` tests fail at the seam's regeneration gate; with `/home/reid/1cfe/fusion-tea/.env` sourced both reproduce. Not the round's; an environment constraint the owner resolves (renew the key or retire it from that file) and a reproduction-recipe correction for every later review.
- **F6 — The dispositions file was revised in place**; r1 survives only at `700bfe6d`. Meets the runbook (the trail's `rK` entries are new; the markers were in the file at `2d402aea`); the round-1 sections form is the stronger one. Form only.
- **F7 — `b75362c1` edits `plan.md` (+8/−3) after commit B and after the `0c20e373` battery**: the return-writing act inside T-001 (the status line and battery counts); the pin cites the item at that sha, so nothing voids. Note only.
- **F8 — `round2_T-003_recount.py` (+62) and `_snapshot_builder.py` (+2/−2) changed at `0b181b9c` after their deposit at `9a78b92c`**: inside T-003, visible in the commit stat. Note only.

A note, not a finding: "independence, four ways" — the oracle's `_sustainment` is the same equations on the same grid as the impl by design (D5), so the bit-exact parity is a mirror check as the repository's standard has always been; the real independence at the rule is the SV-053 two-integrator identity at the printed peaks (a 200,000-interval trapezoid against numpy on 20,001 nodes) and the round-1 reviewer's Bosch–Hale re-derivation of 518.3. The record § 13 states what verification did not cover honestly.

---

## Learning delta rulings — the forms to append to `learnings.md`

All five accepted with corrections. Appended in the file's existing shape.

*(Round 2's delta, accepted with corrections by the round-2 fresh review on 2026-09-06 — `evidence/round2_review.md` § Learning delta rulings; appended in the reviewer's corrected forms.)*

## L-004 — At the paper's own ash rule the pinned design point is satisfied on every fence, on the boundary: "needs 90 MW" is decided as a statement about a retired basis, and "is feasible" is undecidable from the source's own two-sided spread on its stored energy, not from its 2.7 % residual

- **Evidence:** the WI-042 package at pin `ec984adc1572…`: `sustainment_ok` satisfied by 0.92 MW (49.080 required against 50 coupled at the held `eta_couple_heat` 1.00; 49.0 coupled at 0.98 violates), `wall_load_ok` by 0.071 MW/m² (3.979 against 4.05), `peak_field_ok` at equality (24.9 against 24.9, the design convention), the other six satisfied — `evidence/round2_T-001_baseline_result.json` and `_run_stellaris_single_output.txt` (`9ac167b8`, bit-exact vs the oracle), the seam's baseline (`af75a749`), the study's `c3694` (`0b181b9c`); no grid point on the design column is driven at 100 MW (I 15 MA reads 62.7 MW and 4.072; 16 MA breaks the ceiling). The margin in W terms: 0.92 MW is 0.83–0.96 MJ at the 0.96–1.11 MW/MJ slope the two constant-scale pairs give at the baseline ((90.605 − 37.469)/(551.444 − 501.667) = 1.07; (90.605 − 51.377)/(551.444 − 516.174) = 1.11; between the pairs 0.96) — a slope borrowed from a method the same study shows predicts no location elsewhere (L-006) — about 0.18 % of W. The W ordering: the model's 519.9 MJ at the rule sits above the sourced-rules 518.3 and the printed 504.65; the verdict is satisfied throughout that band. The two-sided spread: the printed β row implies 567 MJ at 9.0 T and the fast-alpha term about 12 MJ upward (`attribution_arithmetic.json` at `3687d2f6`; L-002); the printed W 2.7 % downward. Record § 15 #3 and the checkpoint C-001.r1/r2 recounts.
- **Scope:** the pinned baseline point at the WI-042 profile family, the held coupling 1.00, the transport facts τ*/τ_E 8 and f_suppr 0.5; the slope is the constant-scale pairs' at this one point; the new reading's own W-sensitivity has no second basis at the rule and is not measured. The form "inside the source's own 2.7 % residual" that MR-WI042-9, the instance's `sustainment_ok` comment, SV-043, SV-054 and the record's § 15 #3 carry is superseded in reasoning by this entry; their numbers stand.
- **Implication:** the demo statement is "satisfied on every fence, on the boundary". "The machine needs 90 MW" was a statement about the retired profile family and is decided; "the machine is feasible" is not decidable from the source, because the source's own stored energy is uncertain in both directions by more than the margin. Every reading of the design point states its W basis (constraint 1) and the coupling it holds.
- **Supersedes:** refines L-001's implication line (the verdict as a statement about the basis) and corrects its direction at the rule: the flip lies above the sourced-rules W, not inside the printed-to-sourced band.
- **Accepted by:** round 2 review, 2026-09-06 (corrected: the superseded form named with its sites; the slope's three values stated; the margin's W range stated).

## L-005 — The sourced ash profile ignites the committed window's driven set, so the one-sided sustainment fence is load-bearing for every count on this package; the driven region at 100 MW is 306 points, 175 of them on the restored 13 keV row

- **Evidence:** over the 6,283 points executed in both records, of the committed 681 driven points 523 read a negative requirement at the rule; 510 of those also satisfy every other fence (the record's ignited state: 235 of 257 at 100 MW, 275 of 400 at 220), 146 stay driven, 25 are violated (13 of them with a negative requirement and `recirc_ok` failing); sustainment flips 964 committed violations to satisfied and none back; every committed headline driven point reads negative (`c1721` −188.8 MW). Over all 7,712 points 3,654 read a negative requirement and pass the fence; 1,839 are "feasible"; 726 are driven. The driven region at 100 MW: 306 points — 175 on the restored 13 keV row (none of the 734 restored rows ignites), 131 at 14.63–18 keV (63 + 30 + 22 + 16; the cheapest in-both point `c3584` at 17 keV, 221.02), at a 1.5–2.2 plus the pinned baseline at a 1.3; the study's cheapest driven point `c2823` 202.19 on the 13 keV row at the window's `a`-edge (R 15.7, a 2.2, 13 MA, 1.0×, 33.3 MW). Through that anchor the T transect reads 91.9 MW at 12.5 (caught), 33.3 at 13 (driven), −23.2 at 13.5 (ignited) — one transect, not the region's width. Record §§ 3, 4, 6, 11, 15 #1, #2 (`0b181b9c`); the recounts of the checkpoint and this review.
- **Scope:** the `20260904-wall-and-heating` window plus the restored 13 keV rows, at the WI-042 pin; "ignited" as a count is the column (p_aux_required < 0); the 510 is the record's three-state classification (negative requirement with every other fence satisfied); no point below 13 keV was executed; the `a`-top is the window's edge, not fence-caught.
- **Implication:** every count on this package that does not say "driven" is a count the burn-control gap decides; a second sustainment inequality or a burn-control lever (the committed `#4`, now load-bearing) is the model item the owner mints or carries before any LCOE claim rests on the ignited set.
- **Supersedes:** none; sharpens the committed `20260904-wall-and-heating#4` row's bearing.
- **Accepted by:** round 2 review, 2026-09-06 (corrected: the 523 / 510 / 13 split and which sense of "ignited" each number uses).

## L-006 — A constant-scale counterfactual on this closure predicts the sign of a move at its anchor and the aggregate directions, and nothing about per-point locations or some per-point signs

- **Evidence:** the aggregate directions held (sustainment 964 flips at the rule against the scales' 1,128 / 890; the wall 192 against 384 / 273; more ignition); per point, over 6,250 committed points carrying both scales, the rule's requirement lies below both at 3,456, between at 478, above both at 2,316; the W correction is 0.788–1.018 (mean 0.919), not 0.915 or 0.940; at the baseline the rule (49.1) lies between the scales (37.5 / 51.4) with a W above both scaled values; `c1721` predicted +5.9 / +4.7, reads −188.8; the sign exception `c1041` (committed +130.4 violated; scales +139.1 / +135.6; the rule −67.0 — a negative requirement with another fence violated). Record § 15 #4; `NOTES.md` § 6 (`3687d2f6`).
- **Scope:** this closure (the damped A.5/A.6 fixed point with ISS04's line average inside it); W scaled by a constant with the closure live; the WI-037 → WI-042 family change as the tested move.
- **Implication:** a constant-scale counterfactual on this package is a sign-and-direction diagnostic at its anchor; it is never cited for a location, a count or a per-point verdict, and a reading that needs one re-executes.
- **Supersedes:** sharpens L-001's scope line ("sign claims robust, boundary locations not"): some per-point signs are not robust either.
- **Accepted by:** round 2 review, 2026-09-06 (corrected: `c1041`'s state at the rule stated exactly).

## L-007 — The stored-energy correction the rule makes is not a constant: 0.79–1.02 of the committed W, monotone in the ash fraction, above 1 where the ash is scarce; the ash shape's exponent is a function of `T_i0` alone among the swept levers, and the amount is what carries the point-A facts

- **Evidence:** `W_ratio_vs_committed` 0.788–1.018 (mean 0.919) over 6,283 points, Spearman −0.998 against He/n_e, per-(R, a) means 0.995 at (R 17.2, a 1.3) to 0.826 at (R 11.2, a 2.2), 145 points above 1.0 all with He/n_e ≤ 0.062; `alpha_He_eff` 4.268 / 4.052 / 3.885 / 3.770 / 3.661 at 13 / 14.63 / 16 / 17 / 18 keV — one value per temperature across every R, a, I, n and τ*/τ_E (1,429–1,574 points each, min = max to four decimals); under τ*/τ_E → 4 at the baseline the amount falls 39 % (6.04e19 → 3.66e19) and the exponent does not move; `W_th_MJ_store` equals the oracle's W to 5.5e-16 at every point. Record § 6, § 15 #5; `prototype/window_corners.json` (`ae6c327e`); SV-056; `evidence/round2_T-001_perturbations.json`.
- **Scope:** the geometry window R 11.2–17.2, a 1.3–2.2 at the WI-042 pin. The *shape* rests on `T_i0`, the reserved α_n 0.33 and α_T 1.19 and the Bosch–Hale reactivity, and on the reading τ* uniform in ρ (corroborated at point A only); the *amount*, and so the W correction, rests also on Ti/Te 0.95, f_suppr × τ*/τ_E = 4.0 and ι 0.92 — point-A facts (the committed `#2` research request). Nothing in the study could falsify the shape itself.
- **Implication:** the owner's scaling clause is met by construction and demonstrated; no reading of a larger machine may apply a point-A W correction as a factor, and any change to the transport facts moves the amount and the W correction, never the shape.
- **Supersedes:** refines L-003 (the ~6 % gap was a point-A number; across the window it is +2 % to −21 %).
- **Accepted by:** round 2 review, 2026-09-06 (corrected: the shape's and the amount's dependencies separated; "halves" replaced by the measured 39 %).

## L-008 — The committed window's 13 keV floor was fixed by the sustainment fence at the WI-037 family; at the rule the fence moves and the driven region at large minor radius sits on that row, so a restated window's edges must be re-read at the new chain before any point runs

- **Evidence:** the committed record's `window_edges.json` (`a5b0b96a`): 117.1 MW required at 13 keV on its 100 MW anchor's T transect (R 14.2, a 1.8, 14 MA, 0.8×), the reason its critique's F6 cut the row. At the rule, on that anchor's own coordinates, 13 keV reads 56.8 MW — still caught — while 14.63 and 16 keV ignite (−6.5, −50.8); on the rule's anchor (R 15.7, a 2.2, 13 MA, 1.0×) 13 keV reads 33.3 MW (driven), 12.5 keV 91.9 (caught), 13.5 keV −23.2 (ignited). The restored 13 keV rows carry 175 driven points at 100 MW and 191 at 220, none ignited, and the study's cheapest driven point. The pre-execution critique's F1 caught it before any point ran (`evidence/round2_T-003_precritique.md`, `5ab0452c`; record § 11, § 14, § 15 #2).
- **Scope:** an inherited, engineered window under a model change that moves a fence; the like-for-like at the old anchor is still caught at the rule — the floor's removal opens rows elsewhere in the window, not at the old anchor.
- **Implication:** the process consequence — before a restated window's points run, its edges are re-read at the new chain from an anchor driven at the new chain — is a clarifying sentence for the study runbook's step 7 (`.claude/skills/run-study/runbook.md`, repo-tracked): a repository process edit with the runbook as its home, for the owner or the next round; not a goal learning and not an upstream filing.
- **Supersedes:** none.
- **Accepted by:** round 2 review, 2026-09-06 (corrected: the two anchors named; the like-for-like 56.8 MW added; the step-7 sentence homed as a repository edit).

---

## Constraints carried forward

1. Every sustainment-fence or driven-set reading states its W basis (the WI-042 family; the WI-037 family; a constant scale) and the coupling it holds; boundary locations under a constant scale are never claims, and now neither are per-point signs (L-006).
2. The reason for "undetermined, on the boundary" is L-004's: the source's two-sided spread, never "inside the 2.7 % residual"; every new claim site uses that form, and the native sites carrying the older form (MR-WI042-9, the `sustainment_ok` comment, SV-043, SV-054, ANNEX § Baseline pin) are corrected only through an item or a dated amendment, never by editing the pinned model in place.
3. "Ignited" is two counts on this package — the column (p_aux < 0) and the record's state (negative requirement with every other fence satisfied); a claim says which.
4. The printed 504.65 MJ is not a target; the W definition is cited as Lion 2021 / 2023's (unchanged from round 1).
5. A restated window's edges are re-read at the new chain from an anchor driven at the new chain before any point runs (L-008); the runbook step-7 sentence is the owner's or the next round's edit.
6. A critique-driven change past a written scope gets a dated amendment before points run (held this round; the git order is the attestation).
7. The checkpoint waits for the administrator's synthesis (held); a revision keeps r1 recoverable — in the file as a section (the round-1 form) or at a cited sha (this round's form), and the trail says which.
8. Every touched id gets a row, the unchanged ones included; a sighting whose *reasoning* the checkpoint corrects owes a joined row as much as one whose number it corrects (F1).
9. Reproduction from the worktree: the primary checkout's interpreter; `~/1cfe/agentic-mbse/.env`, `/home/reid/1cfe/fusion-tea/.env` (the working SysIDE key as of 2026-09-06) and `.venv/integration.env` sourced; `PYTHONPATH` unset for `tests/study`; the seam's regeneration gate and `tests/models` need a live SysIDE licence.
10. The clean-room screen, the `wall-and-heating` constraints, and the goal-contract rule (no goal file gains a narratives path) stand unchanged.
11. Every value in the model cites a page image or a rendered page, never the extraction; a reviewer without the R2 binaries says so and reads the extraction only where it is legible (this review's disclosure).

---

## Recommendation: close the goal on § Answered when

**Close packet — for the owner to rule on:**

1. **The close ruling** under § Close rule: (a) met at grounding and ratified; (b) met in round 1 and corroborated in the package (SV-053); (c) ruled 2026-09-05 and executed — WI-042 minted, landed, pinned (`ec984adc1572…`), the `20260904-wall-and-heating` window restated by re-execution; the scaling line met by construction and demonstrated. The demo statement, in L-004's form: satisfied on every fence, on the boundary; "needs 90 MW" retired; "is feasible" undecidable from the source.
2. **WI-042 close and archive** through the modelling PM (`pm close-item WI-042`; the plan complete, SV-053..056 passing, batteries green); every row naming `work/active/WI-042_…` resolves at the archived path after the move, as WI-041's did.
3. **Ratification of `goal.md` § Amendment (c)** (the Package invariant read as the goal's own pen), `[AGENT]` since 2026-09-05 and acted on by this round.
4. **Carried from round 1, still unruled:** (i) the re-grade of `priced-levers/goal.md:52`, `wall-and-heating/goal.md:52` and `operating-point-closure` L-001's implication line — keep the never-tune line; the gap was a sourced ~6 % on the ash shape, now landed: 90.6 MW at the retired family, 49.08 at the rule; (ii) the fifth condition on `wall-and-heating`'s closed (b)(ii) — its content is now the `#1` row at `53eb5671` (at the rule the (b)(ii) region ignites and the column opens at the design point alone; a disclosure at the rule's basis); (iii) the author query to J. Lion (the three questions verbatim in the REQ-W-01 run's `process_log.md`); (iv) the § Question amendment for round 1's two imprecisions (F5 of that review).
5. **Three follow-ons this record surfaced, not minted:** a second sustainment inequality or a burn-control lever (`20260904-wall-and-heating#4`, load-bearing for every count on this package); a bound on `a` with a re-anchoring of the wall-peak calibration (`#3`, with ignition growing along `a`); the research request on the aspect-ratio scaling of τ*/τ_E, f_suppr and ι (`#2`, whose location this record shows W-basis-sensitive a second time).
6. **The study runbook's step-7 sentence** (L-008) — a repository process edit with `.claude/skills/run-study/runbook.md` as its home.
7. **Added by this review:** (a) one joined row under `20260905-stored-energy-basis#3` in L-004's form (F1; the next writer's, like the round-1 F2 row); (b) the reasoning-alignment note for MR-WI042-9, the instance's `sustainment_ok` comment, SV-043, SV-054 and ANNEX § Baseline pin (F1) — a doc-text item or a dated spec amendment at the owner's call; (c) the SysIDE licence in `~/1cfe/agentic-mbse/.env` — renew it or retire it from that file, and the reproduction recipe names `/home/reid/1cfe/fusion-tea/.env` (F5); (d) nothing on the goal-contract failure, owner-ruled to stand.

**My recommendation, in one paragraph.** Close the goal on § Answered when. The question this goal asked — whether the sustainment verdict rests on the model's stored-energy form and whether the printed 504.65 MJ is the right target — is answered and the answer is now in the package, not only in a trail: the printed value is not a target under any admissible reading (L-002), the gap was the helium-ash profile shape and it is gone (WI-042; W 551.4 → 519.9 MJ at the paper's own rule, nothing tuned), and what the model says at the design point is the honest thing a source with a 2.7 % internal inconsistency on its own stored energy allows it to say — satisfied on every fence by less than the source's spread, on the boundary. Every remaining act is the owner's (the item close, the ratification, the four carried items) or a new question (burn control, the `a`-bound, the transport facts' scaling), and those three are new goals with their own grounding, not a round 3 here: a third round under this contract would have no model increment inside the (c) ruling and no study question § Answered when still needs. Round 2 of 6; no round 3 under this contract.
