# Round 1 review — goal `stored-energy-basis` (fresh reviewer, 2026-09-05)

Deposited verbatim by the round agent from the reviewer's final message (spawned from `evidence/round1_review_prompt.md` at `4ee3ab73`). Two transport artifacts normalized: HTML-escaped angle brackets in one shell command.

---

**Reviewer:** a fresh session spawned from the deposited prompt (`evidence/round1_review_prompt.md`, `4ee3ab73`); no context inherited; not the round agent, not the research subagent, not the checkpoint session. Worked from the worktree `/home/reid/1cfe/fusion-tea-stored-energy-basis` at `4ee3ab73` (branch `goal/stored-energy-basis`; contains `feat/demo-maturation` at `e0f66f7a` by the merge `ea6d5256`), and from the primary checkout `/home/reid/1cfe/fusion-tea` (at `e0f66f7a`) for the R2-synced Stellaris PDF and the `tests/models` precedent. Nothing under `knowledge/holdout/` opened. No file edited; no state-changing git command. One disclosure on freshness: the harness scratchpad is shared per top-level session and still held the research subagent's working files (the Lion 2021 PDF, its page renders, a row-building script); I listed that directory, verified the PDF's sha256 against the registration (`db74f1c6…`, identical) and rendered its journal page 4 myself, and read nothing else there.

**What I read, in the prompt's order:** `GOAL_RUNBOOK.md` §§ The fresh review, What "fresh" means, The discovery log, When a cited artifact moves, Opening and closing a round, The two checks are distinct, Running one task, Limits; ADR-0004; `goal.md` end to end with its Amendment; `trail.md` end to end; every file under `evidence/` (both handoffs, the research option, the `wall-and-heating` review input with its Addendum, the T-001 prompt, the dispositions file r1 / r2 / addendum / r3, the checkpoint prompt, the checkpoint r1 / r2 / r3); all of `w_counterfactual/` (NOTES §§ 1–9, both scripts, the four attribution files, both baseline JSONs, both window summaries and CSVs, both window outputs, the op_solve files); `REQ-W-01.json`, the run directory (`run.json`, `run.jsonl`, `process_log.md`, both receipts, `return.json`); the two registrations' `SOURCE_INDEX.md` blocks, their `output.md` sentences and the thesis equation images; the six 2026-09-05 rows and every earlier `20260904-wall-and-heating#n` row in `DISCOVERY_LOG.md`; `operating-point-closure/learnings.md` L-001 / L-005; `priced-levers/goal.md:52`; `wall-and-heating/goal.md:52` and its § Amendment 2026-09-05; `wall-and-heating/learnings.md` L-009; `wall-and-heating/trail.md` § Round 2 review and § Goal close; `wall-and-heating/evidence/round2_review.md` § Close packet and § Recommendation; the Stellaris raw PDF pages 8, 9, 10 and 32 rendered with `fitz`; Lion 2021 journal page 4 rendered with `fitz`; `stellarator_plant.sysml:560-605`, `mfe_plasma_sustainment.sysml:20-50`, `verify_stellaris.py` `_sustainment`.

**What I ran** (all with `/home/reid/1cfe/fusion-tea/.venv/bin/python`): recount scripts over both window CSVs, `points.csv`, `oracle_operands.csv` and both baseline JSONs (scratchpad, not deposited); `w_counterfactual.py parity 60`, `baseline`, `baseline 518.3`; `attribution_sourced_definition.py`; an independent recompute of the sourced arithmetic with my own Bosch–Hale reactivity and quadrature; `git log` / `git diff --numstat` over every cited path; the four batteries (§ 9).

---

## Verdict: `FINDINGS`

The round stays closed and every task's evidence stands. Every cited ref resolves and every number I recounted from the data agrees with the trail, the rows and the round result — I found no evidence error. The strategy was pursued as declared; both task scopes held (two recorded deviations, both judged the same act for the scope's purpose); zero retries, correctly classified; the checkpoint cap respected with no submission edited in place; six joined rows landed, each saying what moved or that nothing did; no cited artifact moved outside its task in a way that voids anything; the batteries pass. The findings are precision-level: the three learnings need corrections before they are appended (one attribution term is worth exactly zero at point A; one learning names the wrong thing as "the W-form"; one claim needs the residual's reach stated); one touched row (`#3`) got no row; one reproducibility sentence is off by one line; and (b)'s closing condition is met by the written attribution rather than by a formal seam negative, which should be said plainly. None reopens a task.

**Grounds.**

1. Every cited ref resolves and says what the trail claims (§ 1): the flip counts at both scales, the arm counts, the six design-geometry points, `c1676` / `c1721`, the 347-of-681 decomposition, the transect rows, the 61 / 35 closure-edge rows joined to the record, the two baselines, the realized ratios, the sourced arithmetic to the digit, and my own independent 518.31 MJ.
2. The two registrations' equations state what the T-001 return says (§ 2), and the Stellaris pages state the profile forms, the 1.2 / 0.35 caption, the 0.95 ratio, the fixed-ratio ash rule and A.5–A.8 — and do not state the volume element, the fast-alpha inclusion, or β's reference field.
3. Strategy fidelity holds (§ 3); the scopes held (§ 4); zero retries (§ 5); the checkpoint sequence is clean and at cap (§ 6); the rows moved (§ 7); no voiding mutation (§ 8); batteries `25`, `28 / 1`, `48 / 13`, `362 / 1` (§ 9).
4. § Answered when: (a) met and ratified; (b) met on the written-attribution clause with the residual bounded; (c) the owner's — trigger 4 is the right derivation (§ 10).
5. No reserved gate crossed (§ 11).

---

## 1. Every cited ref resolves — recounts

**`window_counterfactual.csv` (scale 0.915142; 6,311 rows, 61 error rows, 6,250 evaluated).** Flips record → forced: `sustainment_ok` violated → satisfied **1,128**; satisfied → violated **21**; `wall_load_ok` violated → satisfied **384**; `recirc_ok` satisfied → violated **211**; `beta_ok` violated → satisfied **20**. Field, stress, strain: zero. Arms, feasible / ignited / driven, record → forced: 100 MW **458 / 787 / 257 → 776 / 1,282 / 332**; 220 search 598 / 787 / 400 → 793 / 1,282 / 390; re-read **24 / 0 / 24 → 58 / 51 / 58**; transect 0 / 8 / 0 → 0 / 9 / 0. Six driven points at R 12.7, a 1.3 at 100 MW, none feasible in the record: `c0625` 315.090 (31.41 MW; peak 4.012), `c0621` 319.821, `c2973` 332.585 (the pinned baseline; 37.47 MW; peak 3.861), `c0629` 340.114, `c0620` 386.307, `c0624` 389.877. Cheapest driven: `c1676` **212.314** (R 14.2, a 2.2, I 13 MA, T 14.63, n 0.9×; 40.02 MW; record LCOE 200.98, not driven in the record at 60.39 MW); `c1721` forced **226.507**, 5.94 MW, still driven; `c0821` 275.877, still driven. Driven lost **347 = 305 ignited + 40 `recirc_ok` + 2 `sustainment_ok`**, by arm 152 (147 + 5) / 180 (158 + 20 + 2) / 15 (all `recirc_ok`); the two search arms **332 of 657**; the record's evaluated driven total **681**; driven gained **446**. Re-read cheapest `c5944` **371.702** (record 351.04 not feasible; the record's cheapest feasible re-read 378.556 per L-009). Transect: `c6308` (τ*/τ_E 6) record +44.44 MW, peak 4.540, wall violated → forced **−12.20 MW**, peak 4.326, wall still violated, ignited; `c6309` (τ 12) **158.43 → 108.96 MW**, wall satisfied both, not feasible either. Closure edge: **61** error rows (28 at 100 MW, 33 at 220; 55 non-positive fuel, 6 `TypeError`), joined to `points.csv` by id: **0 feasible, 0 ignited, 0 driven** in the record; R 11.2 (48) / 12.7 (13); a 2.0–2.2 (55 of 61); I 16–18 MA (51 of 61). Record columns in the CSV against `points.csv`: **0 mismatches** over every verdict, flag, `p_aux` and LCOE. Realized W_forced / W_record across the window **0.9084–0.9128**.

**`window_counterfactual_518.3.csv` (scale 0.939896; 35 error rows, a subset of the 61).** Flips **890 / 18 / 273 / 145 / 14**. Arms: 100 MW **458 / 787 / 257 → 689 / 1,152 / 314**, **0 driven at R 12.7, a 1.3** (driven `a` 1.5–2.2 only), cheapest `c1676` **208.718** (44.02 MW; peak 3.809); 220 search 598 / 787 / 400 → 743 / 1,152 / 395, **5** design-geometry driven (cheapest `c3572` 365.51); re-read **24 / 0 / 24 → 58 / 12 / 58**, cheapest `c6112` 365.955; transect 0 / 8 / 0 → 0 / 8 / 0. `c1721` 222.044, driven; `c0821` 269.959, driven. The design column's margin: `c0625` fails only the wall, **peak 4.079 against 4.05** (0.029 MW/m²), 49.84 MW required; `c2973` fails only sustainment, **51.38 MW** (1.38 short), peak 3.930. `c6308` +2.69 MW (not ignited), `c6309` 121.90 MW. Driven lost 282 (132 / 138 / 12), gained 368. Realized ratio **0.9350–0.9383**.

**The two baseline JSONs.** 504.65: `p_aux_required` **90.605 → 37.469 MW**; peak **4.088 → 3.861**; `p_fus` 2,725.4 → 2,574.0 (**−5.55 %**); τ_E 1.4499 → 1.6811; applied W 551.444 × 0.915142 = **504.65**, realized **501.667** (ratio 0.9097); every verdict satisfied under the forced W. 518.3: **90.605 → 51.377 MW** (`sustainment_ok` violated by 1.38); peak **4.088 → 3.930** (satisfied); `p_fus` −3.87 %; τ_E → 1.6078; applied 518.3, realized **516.174** (0.936); LCOE 313.513 → 326.490.

**Re-runs.** `parity 60`: **0 mismatches**, writes nothing. `baseline`: prints the identical numbers, but the JSON it writes is **not byte-identical** to the committed `3687d2f6` file — the script at `5cc30ac0` adds a `"W_target_MJ": 504.65` line the original did not carry (one insertion, every number identical). I restored the committed bytes with `git show HEAD:<path> > <path>`; the worktree is clean. `baseline 518.3`: identical to the committed `_518.3.json`. `attribution_sourced_definition.py`: **518.3 / 2.52 % / 2711 / 5.04 / 3.16**, ash effective exponent **4.13**, model family 551.3, model exponents with the A.5 ash **524.5**, the ash at 0.35 573.4, **567.1 / 2.46 % / 8.49 T**, sourced / printed 1.0271, model / sourced **1.0637** — identical to the committed JSON.

**Independent recompute** (own Bosch–Hale reactivity, own 40,001-point quadrature, no oracle code): paper rules (1.2 / 0.35, T_i = 0.95 T_e, n_He ∝ n_D n_T ⟨σv⟩(T_i) scaled to 0.56e20, electrons by quasi-neutrality, 2ρ dρ over 425 m³, species sum) → **W 518.31 MJ (+2.71 %)**, β 2.52 %, p_fus 2,710, n_e0 5.04, ⟨n_e⟩ 3.16, ash exponent 4.13, W_e 273.5 / W_i 244.8; model family 551.33; model exponents with the A.5 ash 524.52; the mean-over-species readings **129.5 / 259.0 / 266.7 MJ** (four-species mean; mean over electrons and total ions; n_e with the mean temperature) against W_e alone 273.5 — the 259–274 bracket reproduces. **I get 518.3.** Decomposition in points of 504.65: 5.31 + 1.23 + 2.71 = 9.25. One precision point: at point A the printed T_i0 is exactly 0.95 × 15.40 = 14.63, so "the 0.95 ratio" contributes **nothing** to the 1.2 points — they are the exponents (1.19 / 0.33 against 1.2 / 0.35) alone; the JSON's second row shows this (identical to the first).

**Numbers I could not reproduce: none.**

## 2. The research return

**The equation images say what the return says.** Lion 2021, journal page 4 (rendered from the registered PDF, sha verified): Eq. (8) P_Loss^conf ≃ W/τ_E "where W is the total plasma energy"; Eq. (9) ISS04 with n̄_e "the line averaged electron density" and B_t; the sentence "The stored energy W in equation (8) is obtained from the imposed profiles for particle species averaged density n and temperature T"; Eq. (10) W = V · 3/2 ∫₀¹ dρ √g(ρ) n(ρ) T(ρ); Eq. (11) √g(ρ) ∼ ρ "the effective radius"; Eqs. (12)–(13) the (1−ρ²)^α forms with the ion profiles as user-defined multiples of the electron profiles; no fast-alpha term; no reference field for β anywhere on the page. Thesis images `…-0040-04/06/08/10.png`: Eq. (2.10) p_Loss^conf ≃ w/τ_E; Eq. (2.11) w = 3/2 ∫₀¹ dρ √g n(ρ) T(ρ); Eq. (2.12) √g ∼ ρ; Eq. (2.13) ISS04 with n̄_e; the extraction's sentence at line 726 carries "volume averaged total plasma energy density … particle species averaged density n and temperature T". The thesis Appendix A (extraction lines 2481, 2520) reads "p ≡ ⟨p⟩_V ≡ ∫_V p dV, where V is the plasma volume, usually taken of the confining plasma volume up to a certain 'core' radius r" and "P = W/τ_E, where W is the plasma energy" — the Stellaris page 32 reproduces both sentences almost verbatim, minus the core-radius clause. **The Stellaris pages** (8, 9, 10, 32, rendered): Eqs. (2)–(3); "α_T = 1.2 (a peaked profile shape) and α_n = 0.35 (a flat profile shape)"; "a fixed ion-to-electron temperature ratio of 0.95"; "Helium ash profiles are obtained using a fixed ratio of particle-to-energy confinement time"; "Fast particle pressures are modeled using the model of [142]"; Table 4 (f_ren 1.0, f_α 0.95, τ*/τ_E 8.0, f_suppr 0.5); Table 5 as `NOTES.md` § 2 reads it (Total plasma energy 504.65; Vol. av. beta 2.76; axis-averaged B₀ 9.0; V 425; the peaks); Fig. 16 caption "Eq. (3) for α_T = 1.2 and α_n = 0.35"; A.3, A.5–A.8 with "P = W/τ_E, where W is the stored plasma energy". Nothing on those pages states the volume element, whether "Total" includes the fast-particle pressure the paper says it models, or β's reference field. **The registrations' use-for and caveat lines are accurate on every point I checked.**

**T-001 scope — one request, β folded in, REQ-W-02 not opened: right.** The draft REQ-W-02's β clause is in REQ-W-01's question and its point-A / 0.5-D clause in `where_to_look`; the owner's direction was a judgment call; the change is recorded in the scope. The scope's Stop-when named the 6-search limit; the run used exactly six (Stellaris PDF, thesis, Lion 2021, Elsevier record, Proxima, ISS04) and closed `--adequacy exhausted` while the seam computed `limit_reached: max_searches` — both true, the sixth search being the last place on the list; the "Aurora" reference in the last `where_to_look` item was not searched, which is immaterial (a radiation code, not an energy definition).

**Clean room: held.** `run.jsonl` carries one candidate rejected "Helios material; clean-room barred (PROTOCOL.md section 3), not fetched" (`S0920379626002899`, surfaced by the ISS04 web search). No path under `knowledge/holdout/` appears in the run directory, the receipts or the registrations; the only "holdout" strings in the goal's evidence are the screen text in the deposited prompt. The subagent's own report is not deposited; the trail says so and the seam's native return is the evidence — acceptable under the runbook.

**The two queued items: right.** The author query is a person's act and was logged as a failure with `disposition: queued` carrying the three questions verbatim; the ISS04 paper is paywalled, low priority, and Lion 2021 Eq. (9) already settles what it would (the line-averaged density).

**The "species averaged" ambiguity: resolved as a corroborated reading, not as a sourced statement — and the return says so.** Neither source states the species rule. A mean reading gives 259–274 MJ; a sum gives 518.3 — and both printed numbers (504.65, and 567 implied by β at 9.0 T) sit in the 500–570 band, not the 260 band. The return's wording ("the text does not say which"; "ruled out by the printed value") is the right weight.

## 3. Goal and strategy fidelity

**Pursued as declared.** The optional bounded research check ran first (T-001), the write-up followed (T-002), no study, no pin, no model change (`git diff --stat 1928925b..HEAD -- models exploration/stellarator_e2e/generated exploration/stellarator_e2e/studies/manifest.json` is empty), and the counterfactual reached `wall-and-heating` only through the path this goal supplied and the owner's gate (nothing written in that directory by any non-merge commit on this branch; the only commits touching it are `a11db053` and `e0f66f7a`, that goal's own).

**The four assumptions as the result reports them: right.** (1) held — parity 60 / 0 and every number reproduces. (2) held — the learnings say "oracle-side" in their Scope lines, the `operating-point-closure` L-001 / L-005 precedent. (3) held — the page reads found no definition; the definitions came from the sister code through the seam, which is exactly what (3) predicted. (4) failed in letter, disclosed by amendment before T-001 started, and nothing depended on it beyond the choice of path (a) or (b).

**The abandonment condition — "not met" is right, and the reason should be stated precisely.** The sourced definition applied with the paper's own rules gives 518.3, not 504.65, while reproducing p_fus, n_e0 and ⟨n_e⟩; no admissible reading reaches 504.65. But 2.7 % is inside the reach of the one thing the paper does not state that could lower W (its volume element, or the thesis's core-radius clause; a stellarator's dV/dρ is not exactly ∝ ρ), while the other unstated term (fast-particle pressure) can only widen the gap. So the honest statement is "no admissible definition reproduces the printed number; the residual is small enough that an unstated volume element could close it; the printed β cannot be closed the same way (it needs 567 MJ at 9.0 T)". The round result's L-002 needs that sentence (§ Learning delta rulings).

## 4. Task scopes

**T-001: inside.** The seam's open / log / register / close, the read-only page reads, one fresh subagent from a deposited prompt (`fbcc9166`, before the run's first log at 15:04:59), nothing under `models/` or `exploration/`, no fit toward 504.65, the inferred-definition exclusion honoured by the provenance split (the sister code's definition vs the paper's own rules).

**The reproduction script written during the T-001 return (`attribution_sourced_definition.py`, `5cc30ac0`): inside T-001's return-writing.** The runbook's return carries "the goal-level reading" of the evidence; verifying a subagent's arithmetic before recording it as that reading is part of writing the return, and the script is the evidence of that reading. T-002's Inputs list it as an input, which is the right relation. It imports the oracle's reactivity (a read) and writes only in the goal directory. Not drift.

**T-002: inside, with one recorded deviation.** The second-scale evaluation (`baseline 518.3`, `window 10 518.3`) was authorized by name in the T-002 scope ("the existing counterfactual script gains an optional target argument and writes suffixed output files; the committed 504.65 outputs are not overwritten"): an evidence write inside the goal directory, not drift. The committed 504.65 outputs were not overwritten (each has exactly one commit, `3687d2f6`). The one imprecision is F3 below: the default run of the changed script no longer reproduces the committed JSON byte for byte.

**The merge instead of the rebase: the same act for the scope's purpose.** The scope's stated purpose was that the six rows follow that goal's close rows in file order; at `ea6d5256` the log carries the `e0f66f7a` rows and the six rows were appended after them at `0964e9d3` (`git diff --numstat e0f66f7a..HEAD -- DISCOVERY_LOG.md` = 6 / 0). The merge took `feat/demo-maturation`'s versions of every shared path exactly (`git diff e0f66f7a ea6d5256 -- work/orchestration/goals/wall-and-heating exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` is empty; `git diff --stat e0f66f7a ea6d5256` shows 0 deletions) and kept every sha the trail cites valid, which a rebase would have broken a second time. Decision 1 records it with its reason. Not drift.

**Sequencing note, not a finding.** T-002's start line (`5cc30ac0`) preceded the checkpoint's pass (`0964e9d3`); the part of T-002 that ran before the pass was the second-scale evaluation (oracle-side, no row, no model change), and the parts the checkpoint gates — the rows — waited for r3. That is consistent with the runbook's purpose (no work compounds on a misread). A cleaner shape next time is to scope a second evaluation as evidence of the reading under review rather than bundle it with the rows, so the checkpoint reads a single reading.

## 5. Retry classification

**Zero retries, correctly.** The research subagent's mid-registration pause and resume by message to the same session, same run, same request produced no return and no `MECHANICAL_FAILURE`; under the runbook a retry is a new `### T-00N start` after a mechanical-failure return, and none was written or owed — this is a continuation of one invocation, and the run directory shows one unbroken sequence (last log 15:12:25; receipts 15:13:18 and 15:18:41). The checkpoint's three submissions are the runbook's own revision loop, not retries.

## 6. The checkpoint sequence

**Submissions never edited in place: held.** Per-commit `git diff --numstat` on the dispositions file: `dfc9eca7→f8e2ee52` 72 / 0 (r2 appended), `f8e2ee52→5cc30ac0` 14 / 0 (addendum appended), `5cc30ac0→498a329e` 2 / 2 — the two changed lines are inside the addendum, which was completed with the window numbers **before** its submission (the r2 verdict at `fd1e398e` reviewed `498a329e`) — `498a329e→fd1e398e` 12 / 0 (r3 appended, exactly the six changes; the checkpoint's own count). The checkpoint file: 57 / 0 then 29 / 0. The trail, `goal.md` and `learnings.md` since `3687d2f6`: additions only (115 / 0, 8 / 0, 0 / 0).

**The cap: respected.** Three submissions (r1 `REVISE`, r2 `REVISE` on one row, r3 `PASS`), two revisions — the declared 2 revisions / 3 submissions, reached with a pass on the third. Each `rK` is a new entry in the trail and a new section in both files.

**Parallel with T-001: sound, and it cost one revision.** The dispositions rested on the counterfactual at `3687d2f6`, which was final; r1's grounds were staleness against the `wall-and-heating` close and precision, independent of the return. The return did change `#1`'s class, and the r2 → r3 cycle absorbed it — so the parallel run spent the round's last revision on a change the return caused; had r3 failed, the round would have stopped on cap. The round agent's Decision 2 named the risk ("the checkpoint's r2 exists for the case the return changes a row"). Sound; carried as a constraint.

## 7. Discovery rows

All six rows at `0964e9d3` carry a class from the ADR-0004 set, a status, a responsible actor and a concrete next reference; no touched row returns unrouted; no sighting edited; no id minted; the `wall-and-heating` close rows stand as that goal's final state.

- **`#1` `model fix` — open, not minted, owner-gated: right.** The class rests on the paper's own stated ash rule (p. 9, A.5) — a sourced rule to aim at and a nameable increment — and the row mints nothing, changes nothing under `models/`, and routes the ruling to (c). `declared seam` would have left the W-condition with no consumer. The row moved the finding: it re-opens it on new evidence, discloses W-sensitivity at two scales with the direction and the untested direction named, and states the design column's closure at the sourced scale as a margin (0.03 MW/m² / 1.4 MW), not a location. The **"surfaced, not re-ruled" sentence is a surfacing**: it records that the owner's (b) close ruling of 2026-09-05 was made on four conditions and that the fifth post-dates it; it amends nothing in `wall-and-heating`'s files and does not re-rule the close.
- **`#2` `research`, `#4` `model fix`, `#8` `declared seam`: unchanged in class and routing, each saying what moved** — the knife-edge's location (`c6308` +44.4 → −12.2), the ignited set (787 → 1,282; 305 crossings), the edge (61 rows, the same corner as the 65 exclusions, joinable by id). All verified.
- **`#6` discharged, unchanged; `#7` none owed, unchanged:** both honest (24 → 58, `c5944` 371.70, the 25 flip-backs; zero field-chain flips, the wall peak still rising with R). Verified.
- **`#3`, `#5` — no row.** `#5` (the lifetime chain's price): sound — the reading re-measures neither the charge nor the ratio. `#3` (`a` unbounded and unpriced): **not quite sound** — the reading does touch its first clause: the cheapest driven point sits at the window's edge a = 2.2 in every search arm at **both** scales (`c1676`, `c4660`), a re-sighting of "the optimum's `a` is the window's edge" under W. The row's own rule for `#7` ("row written so the touched set is complete") would have produced a one-line "unchanged" row. The checkpoint ruled the other way three times; the round review may disagree, and I do, at the lowest weight: substance and routing unchanged, no re-open of the round; a one-line row is the next writer's (F2).
- **Writing under ids whose `wall-and-heating` rows are marked "final for that goal": the runbook's reading.** "A goal round may append only under an id a committed record's § 15 already carries" — these ids are `20260904-wall-and-heating` § 15's; the positional rule makes the newest row the finding's current state; "final for this goal" is that goal's statement about its own pen, which the new rows honour in their first parenthetical. The alternative (a new id) is barred, and leaving the condition homeless is the loss ADR-0004 exists to stop.

## 8. External mutation

- `NOTES.md` and every 504.65 output (`baseline_counterfactual.json`, `window_summary.json`, `window_counterfactual.csv`, `window_output.txt`, `attribution_arithmetic.*`, `op_solve_*`): one commit each, `3687d2f6`. **Unchanged.** `w_counterfactual.py` changed once, at `5cc30ac0`, inside T-002's authorized scope; the r2 checkpoint read the diff; the default path's numbers are unchanged (parity 0 / 60; the baseline numbers identical) — see F3 for the one-key difference.
- The `wall-and-heating` files this goal cites moved at `a11db053` / `e0f66f7a`: disclosed in the trail's Amendment before T-001 started and in C-001.r1; every citation at `f51b2915`, `8906d4e7` and `a5b0b96a` still resolves at its sha; the dispositions were re-based on the moved state (r2). **Voids nothing** — neither task's authority rested on that goal's state beyond row order, which the merge handled.
- `models/`, `exploration/stellarator_e2e/generated/`, `studies/manifest.json`: **the diff-stat is empty.** The pin stands.

## 9. The battery

- `tests/study/test_records.py` (worktree, primary interpreter): **25 passed.**
- `tests/orchestration/test_goal_contract.py` (worktree): **28 passed, 1 failed** — `test_narratives_are_separate_from_the_goal_contract`, the assertion on the narratives-directory string, and the only goal file carrying that string is `wall-and-heating/trail.md` — the owner-ruled failure; not this round's.
- `tests/models` (primary checkout at `e0f66f7a`, sealed env, the precedent's invocation): **48 passed, 13 skipped.** The primary's `models/` is byte-identical to the worktree's (the branch contains `e0f66f7a` and the diff-stat is empty), so this tests the same model text.
- `tests/study` (worktree, primary interpreter, `~/1cfe/agentic-mbse/.env` and `.venv/integration.env` sourced, no `PYTHONPATH`; `.integration_workspace` removed before and after): **362 passed, 1 skipped in 7 min 31 s** — the `wall-and-heating` round-2 review's counts, now against the log with the six rows. (Without the sealed env: 279 passed / 84 skipped, every skip `STOP_PARSER_TEAX_ROOT is not set`; reported so the next reviewer knows the env is needed from the worktree too.)

The round's "not re-run" call on `tests/models` and `tests/study` was defensible (nothing under `models/` or the package changed); I ran them because the log is an input to `tests/study`, and they pass.

## 10. § Answered when, word by word

**(a) "The counterfactual, deposited" — met at grounding and ratified.** `NOTES.md` §§ 3–5 at `3687d2f6`: the reference point, the whole window by case id against the committed verdict columns, the constant-scale assumption stated (§ 6). The owner ratified grounding ("agreed with grounding the goal", `[OWNER-VERBATIM 2026-09-05]`); the wording gate stays open in its narrower form. Met.

**(b) "The attribution, written or bounded."** Both halves of the first sentence are now present: a **written attribution** of about 6.5 of the 9.2 points to the model (5.3 the helium-ash shape against the paper's own rule; 1.2 the exponents 1.19 / 0.33 against 1.2 / 0.35 — the 0.95 ratio is exact at point A and worth nothing), and a **bounded negative** on the rest naming what was ruled out (the mean-over-species reading, 259–274; fast alphas as a bridge, ~12 MJ the wrong way; the caption's exponents on every species, 575; the figure-consistent families, 527–559) and what remains (2.7 points and the β row, the paper's own; the only remaining act the queued author query, a person's). On the second sentence's mechanism: REQ-W-01 returned the definitions of W's integrand and volume element and of ISS04's density **from the sister code** (Lion 2021 Eqs. 8–11; thesis Eqs. 2.10–2.13), linked to the paper by its Appendix A's near-verbatim ancestry and its own "P = W/τ_E, where W is the stored plasma energy"; for β's reference field and the fast-alpha inclusion no admissible source states them and the seam wrote **no formal negative** (`REGISTERED` takes precedence in the seam's return), the durable record being the two caveat lines and the queued author query. So (b) closes on this review's acceptance of the written attribution with the residual bounded, not on a formal seam negative — **met**, with that said plainly (F4). The registrations' caveats are the honest limit: whether the printed 504.65 is the sister code's W is not settled by any source.

**(c) "The basis, ruled" — the owner's.** The derivation is right: last outcome `COMPLETE`, no study or pin under a strategy that intended none, (c) a reserved gate the owner has not ruled → **trigger 4**. Trigger 6 is the owner's at the boundary, and a trail-only "keep the present family" counts under (c) by the goal's own wording.

## 11. Reserved gates

None crossed. W is tuned nowhere (the scale lives in an in-memory copy of one function; the diff-stat on `models/` is empty); the W form and exponents are unchanged; the inherited invariants and `operating-point-closure` L-001's line are not re-ruled — L-002's Supersedes line says the re-grade is the owner's; nothing written in another goal's directory; the basis not decided — the round agent's preference for (ii) is labelled a recommendation for this review to check; the owner's (b) close ruling on `wall-and-heating` is surfaced as made on four conditions, and nothing in that goal's files is amended.

---

## Findings

- **F1 (learning delta, corrected before appending).** L-001 states a margin as a metaphor ("knife-edge") — state it in W terms (1.4 MW is worth about 1.3 MJ, 0.25 % of W); L-002 must carry the residual's reach (the unstated volume element) and the sister-code provenance in its Scope, and must drop "the 0.95 ratio" from the attribution (exact at point A, worth nothing); L-003 names "the W-form" where the integral form is the same as the sourced one and the gap is in the profile family (the ash shape), and should state that the gap does not depend on the sister-code caveats. Corrected forms below.
- **F2 (`#3` touched, no row).** The edge optimum (a = 2.2, cheapest driven in every search arm at both scales) re-sights `#3`'s first clause; by the round's own `#7` rule a one-line "unchanged" row completes the touched set. Substance and routing unchanged; concrete next reference `window_summary.json` / `window_summary_518.3.json` `driven_a_values_forced` and `cheapest_driven_forced`. For the owner's close or the next round; the closed round is not reopened.
- **F3 (reproducibility sentence off by one line).** The trail's evidence-refs and this review's own prompt say a re-run of `w_counterfactual.py baseline` overwrites `baseline_counterfactual.json` with identical content; with the committed script (`5cc30ac0`) it adds `"W_target_MJ": 504.65`. Every number is identical; the committed file was never overwritten in the round (one commit); I restored the committed bytes after my run. A precision point for the next reader; a script that gains an argument should keep its default output byte-identical or the trail should say it does not.
- **F4 ((b)'s closing mechanism).** (b) is met on the written-attribution clause with the residual bounded; the seam returned the sister code's definitions and no formal negative on β's reference field or the fast-alpha inclusion (the caveat lines and the queued author query are the record). Stated here so a fresh clone reads the return correctly; no work owed.
- **F5 (`goal.md` § Question, two imprecisions the round result itself flagged).** "347 of the record's 257 + 400 driven points" (the 347 spans three arms; 332 of 657 in the two search arms) and "every reading reproduces the printed fusion power" (ions at the electron exponent miss by 8.5 %). The wording gate is open in its narrower form; a dated amendment is the owner's, if wanted.
- **F6 (sequencing, noted, not a violation).** T-002's start and its second-scale evaluation preceded the checkpoint pass; the gated part (the rows) waited. Carried as a constraint on how a second evaluation is scoped.

---

## Learning delta rulings (the forms to append to `learnings.md`)

**L-001 — accepted, corrected** (the margin in W terms; the realized ratios; L-009's bearing stated as the owner's to carry).

## L-001 — The pinned baseline's sustainment verdict and every driven-set reading on this package are stored-energy-sensitive; at the paper's own profile rules the baseline is violated by 1.4 MW, a margin worth about 0.25 % of W

- **Evidence:** two constant scales, both W down, closure live, oracle-side, every verdict compared with the committed record by case id. At the printed 504.65 MJ (scale 0.915; realized 0.908–0.913 across the window): the design point's requirement 90.6 → 37.5 MW and both baseline verdicts flip (the wall's through fusion power, −5.6 % as the ash re-closes); over the 6,311-point `20260904-wall-and-heating` window 1,128 sustainment and 384 wall verdicts flip violated → satisfied, 211 `recirc_ok` the other way; the design geometry opens at 100 MW (six driven points at R 12.7, a 1.3, the baseline among them at 332.585); 347 of 681 driven points leave the driven set (305 by igniting) and 446 join it; the cheapest driven point moves 212.460 (`c1721`) → 212.314 (`c1676`); 61 points fall off the closure's validity edge. At the sourced 518.3 MJ (scale 0.940; realized 516.2 at the baseline, 0.935–0.938 across the window): 51.4 MW against 50 coupled — violated by 1.4 MW, about 1.3 MJ of W — with the wall satisfied at 3.930; over the window 890 sustainment and 273 wall flips, the 100 MW arm 458 / 787 / 257 → 689 / 1,152 / 314 with 0 driven at the design geometry (closed by 0.03 MW/m² on `c0625` and 1.4 MW on `c2973`), cheapest `c1676` 208.72, five design-geometry points driven at 220 MW, the re-read arm 24 → 58, 35 closure-edge rows. `NOTES.md` §§ 3–6, `window_summary.json`, `baseline_counterfactual.json` (`3687d2f6`); `baseline_counterfactual_518.3.json` (`5cc30ac0`); `window_summary_518.3.json` (`498a329e`); the C-001 recounts; the round-1 reviewer's recount, agreeing on every number.
- **Scope:** this package at pin `c1b0f0d1…`; constant, geometry-independent scales, W down only — sign claims robust, boundary locations not; a W above the model's 551 MJ not evaluated (the paper's β row would put it at 567); the flip of the a-1.3 verdict at 100 MW lies inside the 2.7 % between the paper's rules and its printed number and is not decidable at this pin from W's basis alone.
- **Implication:** every sustainment-fence or driven-set reading on this package states the W basis it rests on and carries W-sensitivity as a condition beside the τ*/τ_E condition; "the baseline is violated" is a statement about the basis — 90.6 MW at the model's family, 51.4 at the paper's rules under the sourced definition, 37.5 at the printed number — not about the machine. `wall-and-heating` L-009's four conditions have a fifth in this sense; recording it there is the owner's act, not this goal's.
- **Supersedes:** refines `operating-point-closure` L-005's precision bound (the ~91 MW threshold is basis-conditional).
- **Accepted by:** round 1 review, 2026-09-05 (corrected: the margin stated in W terms; the realized ratios; L-009's bearing stated as owner-held).

**L-002 — accepted, corrected** (the residual's reach; the sister-code provenance in Scope; the 0.95 ratio dropped).

## L-002 — No admissible source reproduces the printed 504.65 MJ: the sister code's definition of W applied with the Stellaris paper's own profile rules gives 518.3 MJ on the printed peaks (+2.7 %), and the printed β is inconsistent with the printed W at the only field the paper names; the printed value is not a fit target

- **Evidence:** the registrations at `d4059ef1` — Lion et al. 2021 Eqs. (8)–(11) (W thermal, from the imposed "particle species averaged" n and T, √g ∼ ρ, no fast-alpha term; ISS04's density line-averaged) and Lion 2023 Eqs. (2.10)–(2.13), whose Appendix A ("p ≡ ⟨p⟩_V", "P = W/τ_E, where W is the plasma energy") the Stellaris Appendix A reproduces almost verbatim; the Stellaris pages (Eqs. 2–3; α_T 1.2, α_n 0.35; T_i/T_e 0.95; "helium ash profiles are obtained using a fixed ratio of particle-to-energy confinement time"; V 425 m³; B₀ 9.0 T axis-averaged). `attribution_sourced_definition.*` (`5cc30ac0`): 518.3 MJ, β 2.52 %, p_fus 2,711 (printed 2,700), n_e0 5.04 (5.06), ⟨n_e⟩ 3.16 (3.17), the ash's effective exponent 4.1; printed W ↔ 2.46 % at 9.0 T, printed β ↔ 567 MJ, agreeing only at 8.49 T; a mean-over-species reading 259–274 MJ, ruled out by both printed numbers. `attribution_arithmetic.json` (`3687d2f6`): fast alphas ~12 MJ. Reproduced independently by the checkpoint and by the round-1 reviewer with a Bosch–Hale reactivity (518.31).
- **Scope:** Table 5 point A as printed. The definition is the sister code's (thermal, species sum, the effective-radius element over V = 425 m³); the Stellaris paper states its profile forms, exponents, temperature ratio and ash rule but not its volume element, whether "Total plasma energy" includes the fast-particle pressure it models, or β's reference field. A.5 is applied pointwise with τ* uniform in ρ, scaled to the printed ash peak, corroborated by Fig. 16(a) and the reproduced n_e0 / ⟨n_e⟩. The 2.7 % residual is inside the reach of the unstated volume element or the thesis's core-radius clause (a fast-alpha term would widen it), so the claim is "not reproduced by any admissible definition", not "wrong"; the β row is not closable that way. The author query (queued in the run) is the only remaining act on the residual and is a person's.
- **Implication:** no round tunes W or the exponents toward 504.65; the model's target for W is the paper's stated rules on the printed peaks (518 MJ), not its printed number; the never-tune half of `operating-point-closure` L-001 stands and its target moves.
- **Supersedes:** refines `operating-point-closure` L-001's implication line ("attributed first to the W-form, and W is never tuned to the printed value") — the re-grade of that line and of the two inherited invariants is the owner's ruling, recorded in this goal's trail.
- **Accepted by:** round 1 review, 2026-09-05 (corrected: the residual's reach stated; the sister-code provenance carried into the Scope line; "the 0.95 ratio" removed from the attribution — 0.95 × 15.40 = 14.63 exactly, it contributes nothing at point A).

**L-003 — accepted, corrected** ("W-form" → the profile family in the W integral; the gap's independence from the sister-code caveats stated; the 0.95 ratio dropped).

## L-003 — The model's profile family in the W integral carries a sourced gap of about 6 % against the paper's own profile rules, about five points of it the helium-ash shape; the integral form itself is the sourced one

- **Evidence:** `attribution_sourced_definition.json` (`5cc30ac0`) rows "MODEL family" 551.3, "model exponents but A.5 ash and QN electrons" 524.5, "SOURCED" 518.3; model W / sourced W = 1.064; `attribution_arithmetic.json` (`3687d2f6`) quasi-neutrality rows: the model's ion charge exceeds n_e off-axis by 7.5 % at ρ 0.5, 31 % at 0.8, 55 % at 0.9; `stellarator_plant.sysml:578` (the ash carries the fuel exponent 0.33), `mfe_plasma_sustainment.sysml:25-30` (the same 1.5 ⟨p⟩ V form over the ρ dρ element the sourced definition uses); `baseline_counterfactual_518.3.json` (90.6 → 51.4 MW required); the reviewer's split — model W_e 285.7 / W_i 265.7 against sourced 273.5 / 244.8, the over-count sitting in both the ion pressure and, through quasi-neutrality, the electron pressure.
- **Scope:** the pressure integral at the printed peaks; the paper's ash rule (A.5, "a fixed ratio of particle-to-energy confinement time") applied pointwise with τ* uniform in ρ — a reading corroborated by Fig. 16(a) and by reproducing ⟨n_e⟩ 3.16 and n_e0 5.04. Because the model and the sourced evaluation use the same integral form, the gap does not depend on the sister code's volume element or thermal-only clause and stands even if the paper's own volume element differs. Decomposition in points of 504.65: 5.3 the ash shape (551.3 → 524.5 at the model's exponents), 1.2 the exponents 1.19 / 0.33 against 1.2 / 0.35 (the 0.95 ratio is exact at point A), 2.7 the paper's (L-002). The WI-022 electron and fuel exponents (0.617 / 0.33 / 1.19) are a fair read of Fig. 16.
- **Implication:** a sourced, nameable model candidate exists — the helium-ash profile shape in the pressure integral from the paper's own rule, with electrons by quasi-neutrality, the peak fixed point unchanged — worth about 6 % of W and, at the baseline, 90.6 → about 51 MW required (still violated, by 1.4 MW; the wall satisfied). It is owner-gated (the W form and exponents are a reserved gate), a minted item through the modelling PM if taken, a profile-family change and not a tuning, and every committed study at the pin is disturbed by it.
- **Supersedes:** refines `operating-point-closure` L-001's scope line ("the analytic W-form, +9.2 % vs printed, is the dominant fidelity gap"): the gap is real and sourced, about 6 % against the paper's rules, on the ash shape, and it is not a gap against the printed number.
- **Accepted by:** round 1 review, 2026-09-05 (corrected: "W-form" → the profile family in the W integral, the form being common; the gap's independence from the sister-code caveats stated; the 0.95 ratio dropped).

**Together, L-002 and L-003 now say what the T-001 return supports and no more:** the model's profile family carries a sourced ~6 % gap on the ash shape; the printed 504.65 is not what the paper's rules give under the sourced definition (with the residual's reach stated); the printed β row is inconsistent with it at 9.0 T.

---

## Constraints carried forward

1. Every sustainment-fence or driven-set reading on this package states its W basis and carries W-sensitivity as a condition beside the τ*/τ_E condition (L-001); boundary locations under a constant scale are never claims; W above 551 MJ is untested.
2. The printed 504.65 MJ is not a target; nothing is fitted toward it; the model's W target is the paper's stated rules on the printed peaks (L-002). Any citation of the W definition names it as Lion 2021 / Lion 2023's, with the Stellaris Appendix A ancestry as the link, never as the Stellaris paper's own.
3. The ash-shape candidate is owner-gated (L-003). If minted, the item's scope names the profile shape in the pressure integral and the electron closure explicitly, leaves the peak fixed point as is, decides what becomes of `alpha_n_e` (`stellarator_plant.sysml:602`, now bound from the printed vol-av / peak pair), and restates every committed study at the pin rather than silently breaking it (the MR-WI037-7 shape).
4. A scope that says "rebase" after the trail cites shas on the branch says instead "whichever of merge or rebase keeps every cited sha valid"; this round's merge is the precedent for the file-order purpose.
5. A checkpoint run in parallel with a task whose return can change a row's class may spend a revision; the scope says so, or the checkpoint waits for the return.
6. A second evaluation of a reading under checkpoint is scoped as evidence of that reading, not bundled into the follow-up task that lands the rows.
7. A script that gains an argument keeps its default output byte-identical to the committed one, or the trail says it does not (F3).
8. Every touched id gets an appended row, the "unchanged" ones included, and a re-sighting of one clause of a finding (the edge optimum, `#3`) counts as a touch (F2).
9. The clean-room screen sits in every research prompt before any fetch (inherited; held this round: one Helios refusal logged, nothing under `knowledge/holdout/` read).
10. The `wall-and-heating` round-2 review's constraints stand unchanged, including: no goal file gains a path under the narratives directory (the owner-ruled failure on that goal's trail is the only one).
11. Reproduction from the worktree: the primary checkout's interpreter, with `~/1cfe/agentic-mbse/.env` and `.venv/integration.env` sourced for `tests/study` (84 tests skip without them).

---

## Close packet — for the owner to rule on

1. **The (c) ruling — the stored-energy basis the model carries.** (i) The present family, unchanged: a trail-only answer; the baseline stays at 90.6 MW required, now disclosed as a sourced ~6 % over-count on the ash shape, carried as a condition on every reading (constraint 1). (ii) A minted model item through the modelling PM: the helium-ash profile shape in the pressure integral from the paper's own A.5 rule with electrons by quasi-neutrality, the peak fixed point unchanged — sourced, about 6 % of W; the baseline at about 51 MW required (still violated, by 1.4 MW; the wall satisfied); a new pin and every committed study restated; the item's scope decides `alpha_n_e`'s fate (constraint 3). (iii) A sourced alternative: none found; the printed 504.65 is not a target under any reading. My recommendation is in § Recommendation.
2. **The re-grade** of `priced-levers/goal.md:52`, `wall-and-heating/goal.md:52` and `operating-point-closure` L-001's implication line, on L-002 / L-003 as accepted: keep the never-tune line; replace "the model's own analytic W-form, +9.2 % at the printed point" with "a sourced ~6 % gap on the helium-ash profile shape against the paper's own rules; the printed 504.65 MJ is not the target; the baseline's requirement is 90.6 MW at the model's family, about 51 at the paper's rules, 37.5 at the printed number". Those goals are closed; the amendments are dated and the owner's.
3. **The fifth condition on `wall-and-heating`'s closed (b)(ii).** Whether W-sensitivity is recorded beside L-009's four conditions, by a dated amendment to that learning's Scope line or to that trail's § Goal close — the owner's pen — citing the `#1` row at `0964e9d3`, `NOTES.md` §§ 5, 8 at `3687d2f6` and `window_summary_518.3.json` at `498a329e`. The honest content: at the sourced basis the (b)(ii) headline holds by 0.03 MW/m² / 1.4 MW; at the printed basis the design column opens; W-up untested. The (b) close ruling itself was made on four conditions and is not re-ruled by anything here.
4. **The author query** — a person's act: J. Lion, the paper's "data on request"; the three questions are verbatim in `process_log.md` (what 504.65 integrates — species, thermal or with fast-alpha pressure, volume element and volume; how "Vol. av." is formed; which field the 2.76 % β is referenced to). Send, or decline and let the 2.7 % and the β row stand as the paper's.
5. **Added by this review:** (a) a one-line "unchanged" row under `20260904-wall-and-heating#3` (F2), the next writer's; (b) the two `goal.md` § Question imprecisions (F5), a dated amendment if wanted; (c) the reproducibility note (F3) — if byte-parity of the committed `baseline_counterfactual.json` with the committed script matters to the owner, a re-deposit at close, not required; (d) nothing on the goal-contract failure — owner-ruled to stand.

---

## Recommendation

**Close the goal after ruling (c).** (a) is met and ratified; (b) is met — the +9.2 % is now written down as 5.3 points the ash shape, 1.2 the exponents, 2.7 the paper's, with the β row inconsistent with the printed W and the residual bounded by what was ruled out; what remains under (b) is a person's act. The learnings above carry the run's knowledge in a form a fresh clone can re-derive. Round 1 of 6; there is nothing a round 2 under this contract would learn that the owner's (c) ruling does not decide first, and if the ruling is (ii), the work is a model item and a restating study under a new goal or a re-grounded one, not a round here.

**On (c), my recommendation is (ii), the minted item, for one reason: the ash rule is the paper's own stated rule, and the model already cites A.5 for the ash peak while giving the ash profile the fuel's shape — so this is not a tuning toward a number but the model doing what its own source says, with the electrons closed consistently.** The change is sourced, small in code, and it removes a known 6 % over-count that every future reading would otherwise have to carry as a condition; and the ash-shape gap does not depend on the parts of the definition the sources leave open (the volume element, the fast-alpha term), because the model and the paper's rules share the integral form. Two caveats the owner should weigh. First, the change does not settle the verdict — at the paper's rules the baseline is still violated, by 1.4 MW, a margin worth 0.25 % of W, inside the paper's own 2.7 % residual — so the demo statement becomes "on the edge at the paper's own rules", not "feasible"; if the owner wants a settled verdict, no basis available gives one. Second, the cost is a new pin and the restatement of every committed study at `c1b0f0d1…`, which the one-pin / one-study bound makes a round of its own. If the owner prefers to keep the pin and its studies stable for the demo, (i) with the re-grade in packet item 2 is honest: the over-count is disclosed, sourced and bounded, and nothing is hidden.
