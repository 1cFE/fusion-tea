# Proposed dispositions — goal `stored-energy-basis`, round 2, task T-003 (the restating study `20260905-stored-energy-basis`)

**Submission r2** (r1 returned `REVISE` by checkpoint C-001.r1 on four wording points, every number upheld; every change below is marked `[r2]`), drafted 2026-09-05 by the round-2 agent on the committed record (`exploration/stellarator_e2e/studies/20260905-stored-energy-basis/` at `0b181b9c`) and its own § 15, **reconciled with the fresh administrator's `synthesis.md`** before the checkpoint reads it (review constraint 5: the checkpoint waits for the synthesis): the synthesis reproduced every count and found seven statement slips and one omission, all landed by the record's first Addendum; A.5 and A.6 below carry the corrected numbers, and § B adds joined correction rows for this record's `#5` and `#7`. Every proposed row is an appended disposition row under an id a committed record's § 15 already carries; no sighting row is edited; no id is minted. Every number is at the WI-042 profile family unless it says "committed" (the WI-037 family at pin `c1b0f0d1…`) or "the scales" (the two constant-scale counterfactuals, oracle-side predictions).

The rules: `work/orchestration/GOAL_RUNBOOK.md` § The discovery log; `.project/adr/0004-finding-disposition.md` (class from the set `model fix | research | declared seam | upstream filing`, a status, a responsible actor, a concrete next reference; no touched row returns `unrouted`; disposition is not resolution).

## A. The `20260904-wall-and-heating` rows this record's evidence touches

**A.1 — `20260904-wall-and-heating#1`** (its 2026-09-05 row: `model fix`, open, not minted, owner-gated — the helium-ash profile shape; W-sensitivity disclosed). **Proposed:** `model fix` — **discharged by WI-042** (`work/active/WI-042_sourced-helium-ash-profile/`; the item's plan complete at `b75362c1`, the pin `ec984adc1572…` at `af75a749`), and the (b)(ii) reading re-measured at the rule by this record: at the paper's own ash profile the committed window's driven region **ignites** (510 of the committed 681 driven points; 131 driven at 100 MW against the committed 257; every committed headline driven point ignited — `c1721` −188.8 MW), the driven region sits **one temperature step below the committed window** on the restored 13 keV row (175 driven at 100 MW; cheapest 202.19 at R 15.7, a 2.2, 13 MA, 13 keV, 1.0×; over the committed window alone 221.02), and **the design column opens at exactly the pinned baseline point** (49.08 MW against 50; peak 3.979; the ceiling at equality; coupling 1.00) and at no grid point. The five conditions of the committed conditional positive stand. `[r2]` **What this record measured is the committed reading's response to the profile-family change** (510 of the committed 681 driven points ignite); **the new reading's own W-sensitivity is not measured** — there is no second basis at the rule, and the rule's baseline does not sit on the constant-scale line (W 519.9 MJ → 49.08 MW against the 0.940 scale's 516.2 MJ → 51.38 MW: a lower requirement at a higher W), so the fifth condition stands as a **disclosure at the rule's basis**, most acutely for the design point's 0.92 MW margin. The answer to "does a feasible driven region exist at the printed 100 MW" at the rule is *yes, and not at the design's minor radius except at the design point itself, on the boundary*. Status: the model fix landed (WI-042's close and archive are the owner's act; the item is still under `work/active/`); the finding's reading moved; responsible: the round agent (this row); the owner at the round-2 close (the demo statement; the item close). Next reference: record §§ 3, 6, 15 #1–#3; `learnings.md` (the round-2 delta). Home: `goal.md` § Answered when (c); WI-042.

**A.2 — `20260904-wall-and-heating#2`** (`research`, open — the `a`-reversal's knife-edge in the held ash-transport ratio; W-sensitive location). **Proposed:** `research` — open, unchanged in class and routing; **re-measured at the rule: the executed driven ratio moved from 8 to 12** `[r2]` (through the committed scan's best 100 MW anchor: **ignited at 2, 4, 6 and at the anchor's own 8** — `c1856`, −50.8 MW, where the committed reading was driven at +23.1 — driven at 12 (25.6 MW, peak 3.115) where the committed record was blocked at 141.9, sustainment-blocked at 16; at the 220 MW anchor ignited at 2–8 (`c5495` −31.4 at 8) and driven at 12 and 16; on the design column ignited at 2–4, wall-blocked at 6, sustainment-blocked at 12–16 at 85.6 / 109.7 MW against the committed 158.4 / 205.8; record § 6 `tau_ratio_ash`, § 15 #8 and Addendum item 8). "About a factor 1.5" is the ratio of the executed driven values (8 → 12); five values per anchor locate neither edge — the ignition edge is bracketed in (8, 12) and the sustainment edge in (12, 16) — the committed `#2` row's own resolution caveat, carried. The shape's exponent does not move with the ratio (a function of T_i0 alone since WI-042); the amount does. The aspect-ratio scaling of the held transport facts is still the research request; responsible: the owner at the close (the request the committed round carried). Home: unchanged, plus record § 15 #8.

**A.3 — `20260904-wall-and-heating#3`** (`model fix`, open — nothing bounds `a` above; the magnet account does not price it; the optimum at the window's edge). **Proposed:** `model fix` — open, **unchanged in class and routing; re-sighted at the rule**: the cheapest driven point sits at a 2.2 at both levels (`c2823` / `c6466`), the driven count by `a` is monotone 1 / 38 / 59 / 63 / 69 / 76 at 100 MW, and 2.4 is driven at the rule's anchor (record § 5, § 6 `a`, § 11) — and **ignition grows with `a`** (41 → 460 ignited over 1.3 → 2.2), so the edge is now also where the plasma is least controllable as modeled. A bound on `a` and a re-anchoring of the calibration remain the candidates the owner mints or carries; responsible: the owner at the close. Home: unchanged, plus record § 15 #2.

**A.4 — `20260904-wall-and-heating#4`** (`model fix`, open — the sustainment fence is one-sided and passes ignited points; the most W-sensitive set). **Proposed:** `model fix` — open, unchanged in class, **now load-bearing for every count on this package**: 3,654 of 7,712 executed points are ignited at the rule (1,819 per geometry arm; 585 / 528 of the "feasible" points), 510 of the committed 681 driven points ignite, and the cheapest "feasible" points at both levels are ignited unless the driven set is read separately (record §§ 3, 4, 15 #1). A second sustainment inequality or a burn-control lever is the model item the next strategy or the owner mints; responsible: the owner at the round-2 close (mint or carry). Home: unchanged, plus record § 15 #1.

**A.5 — `20260904-wall-and-heating#6`** (`model fix`, discharged by WI-041 — round 1's 220 MW result re-executed point by point; re-sighted under the forced W). **Proposed:** `model fix` — discharged, **unchanged; re-sighted at the rule** (record § 6 `eta_source_heat`, § 15 #7): the re-read arm reads 45 driven at the rule against the committed 24 (7 / 9 / 13 / 16 by efficiency), 11 of 279 committed wall violations flip to satisfied, LCOE rises at every point through the fusion power (mean +2.57 %, range +1.24 to +4.26 %; Addendum item 3), the committed cheapest `c6256` (378.556) reads 389.50 and stays driven, and the rule's cheapest, `c7625`, 370.55 at `eta_source` 0.60, was violated in the committed record. Responsible: none new. Home: unchanged, plus record § 15 #7.

**A.6 — `20260904-wall-and-heating#8`** (`declared seam`, standing — the closure has no bound of its own at extreme confinement; the edge moves inward with W). **Proposed:** `declared seam` — unchanged, standing; **re-measured at the rule: the edge moved inward by 28 committed points**, fewer than the 0.940 scale's predicted 35, in the same corner (R 11.2–12.7, 18 MA and some 16; a 1.3 at n 0.6–0.7× or a 2.0–2.2 at n 0.6–0.9×; Addendum item 1), the committed 65 all still excluded, 71 restored 13 keV rows excluded there too; none of the 28 was feasible, ignited or driven in the committed record (record § 11, § 15 #6). The ANNEX § Validity masks candidate stands. Home: unchanged, plus record § 15 #6.

**A.7 — `20260904-wall-and-heating#7`** (`model fix — none owed`, unchanged — `R`'s effect on wall load at a WI-037-class pin). Touched by one clause: the design-column `R` transect at the rule reads the ceiling at 11.2 m and the wall from 14.2 m (record § 11), and the driven optimum's `R` moved from 14.2 to 15.7 (§ 6). **Proposed, one line** (review constraint 8: every touched id gets a row, the unchanged ones included): `model fix — none owed` — unchanged; re-sighted at the rule: the peak still rises with `R` at fixed current on the design column (the wall catches 14.2 and above, the ceiling 11.2 and below), the exponent not re-derived; the driven band 11.2–17.2 with its optimum at 15.7 at the rule (record § 6 `R+tie`, § 11).

**Rows not touched, stated:** `#5` (the lifetime chain prices the wall too weakly) — the lifetime chain is upstream of nothing WI-042 changed, and this record inherited its columns without re-reading them (§ 17); no row owed and none proposed.

## B. This record's own rows

The eight first-sighting rows of `20260905-stored-energy-basis#1..#8` landed with the record's commit (`0b181b9c`) and are the executor's account; they carry their classes, statuses, actors and next references. Two of them state a number the fresh administrator's recount corrected (the record's Addendum, items 3 and 4); a sighting row is never edited, so each gets **one joined correction row** from this round:

**B.1 — `20260905-stored-energy-basis#5`:** `model fix — none owed` — unchanged; **number corrected per the Addendum (item 4):** the per-(R, a) mean W ratio spans 0.826 (R 11.2, a 2.2) to 0.995 (R 17.2, a 1.3), not "0.975 at R 14.2, a 1.3 to 0.826"; the 145 points above 1.0 sit at a 1.3 (R 12.7–17.2), at (R 17.2, a 1.5) and at (R 14.2, a 1.8), with the derived electron exponent 0.41–0.45 (items 5, 6). The reading stands. Responsible: the round agent. Home: unchanged.

**B.2 — `20260905-stored-energy-basis#7`:** `model fix — none owed` — unchanged; **number corrected per the Addendum (item 3):** the re-read arm's LCOE rise is a mean of +2.57 % (range +1.24 to +4.26 %), not "about 2.8 % at every point". The reading stands. Responsible: the round agent. Home: unchanged.

**B.3 — `20260905-stored-energy-basis#8`** `[r2]`: `research` — unchanged in class and routing; **account corrected per the Addendum (item 8):** the anchors' own ratio-8 grid members ignite at the rule (`c1856` −50.8 MW at the 100 MW anchor, `c5495` −31.4 at 220, both driven in the committed record), so the 100 MW anchor reads ignited at 2–8, driven at 12, blocked at 16 — not "ignited at 2–6, driven at 12"; "a factor 1.5" is the ratio of executed driven values with the edges bracketed in (8, 12) and (12, 16). The reading stands. Responsible: the round agent. Home: unchanged.

The round result cites all eight.

## C. What the checkpoint gates

The appended rows under A (six dispositions and one unchanged row) and B (three correction rows) `[r2]`, the T-003 return and the round-2 result that cite them, and the learning delta the round result proposes (D below) — the evidence base the fresh reviewer writes the next strategy or the owner's close on.

## D. The learning delta the round result will propose (for the checkpoint's over-claim check)

- **L-004 (proposed)** `[r2]`: at the paper's own ash rule the pinned design point is **satisfied on every fence, on the boundary**: sustainment by 0.92 MW (about 0.9 MJ of W at the 0.96–1.11 MW/MJ slope the constant-scale pairs give — a slope borrowed from a method that predicts no location elsewhere — about 0.18 % of W), the wall by 0.071 MW/m², the ceiling at equality, all at the held coupling 1.00; no grid point on the design column is driven at 100 MW. "The machine needs 90 MW" was a statement about a retired basis and is decided; "the machine is feasible" is the undecidable one: the model's W at the rule (519.9 MJ) is above the sourced-rules 518.3 and the printed 504.65, and inside that printed-to-sourced band the verdict is satisfied throughout, so what makes the point undecidable is the source's **two-sided** internal spread on its own stored energy — the printed β row implies 567 MJ and the fast-alpha term about 12 MJ upward, the printed W 2.7 % downward — against a flip about 1 MJ above the model's own W on that slope (record § 15 #3; L-001's implication line refined and its direction corrected at the rule).
- **L-005 (proposed)** `[r2]`: the sourced ash profile ignites the committed window's driven set (510 of 681), so the one-sided sustainment fence is now load-bearing for every count on this package; the driven region at 100 MW has **306 points, 175 of them on the restored 13 keV row and 131 at 14.63–18 keV** (the cheapest in-both point at 17 keV), at a 1.5–2.2 plus the pinned baseline at a 1.3, with the study's cheapest driven point on the 13 keV row at the window's `a`-edge; through one anchor (R 15.7, a 2.2, 13 MA, 1.0×) the driven band in temperature is about one keV wide (12.5 caught, 13 driven, 13.5 ignited) — a transect, not the region's width (record § 15 #1, #2).
- **L-006 (proposed)** `[r2]`: a constant-scale counterfactual on this closure predicts the sign of a move at its anchor and the aggregate directions (sustainment 964 flips at the rule against the scales' 1,128 / 890; the wall 192 against 384 / 273; more ignition), and nothing about per-point locations or some per-point signs (`c1041`): the rule's requirement fell below both scales at 3,456 of 6,250 committed points and above both at 2,316 (record § 15 #4; L-001's scope line sharpened).
- **L-007 (proposed)** `[r2]`: the stored-energy correction the rule makes is not a constant: 0.79–1.02 of the committed W, monotone in the ash fraction (rank correlation −0.998), above 1 where the ash is scarce; the shape's exponent is a function of `T_i0` alone among the swept levers; **met by construction** and demonstrated on the package; it rests on what is still bound at point A — α_n, α_T, Ti/Te, τ* uniform in ρ, f_suppr × τ*/τ_E, ι (record § 15 #5; L-003 refined; the owner's scaling clause demonstrated).
- **L-008 (proposed)** `[r2]`: **the committed window's 13 keV floor was a WI-037 sustainment artifact** — 117 MW required there at the old family, 33 MW at the rule's anchor — and the pre-execution critique's F1 caught it before any point ran (record § 14, § 15 #2). The process consequence — a restated window's edges are re-read at the new chain before points run — is a clarifying sentence for the study runbook's step 7 (`.claude/skills/run-study/runbook.md`, repo-tracked: a repository process edit with the runbook as its home, for the owner or the next round, not a goal learning and not an upstream filing).
