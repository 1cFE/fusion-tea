# Trail: wall-and-heating

What happened, and what was decided. Append-only, newest entry last, ISO dates; no entry is ever edited in place — corrections are `### Amendment` entries. This file logs judgment, not routine stage motion; native workflows keep their own stage records and are cited, never restated. Procedure: `work/orchestration/GOAL_RUNBOOK.md`.

Goal **drafted** 2026-09-03 (`goal.md`, status `draft`, owner not present; the rulings it needs are listed in `goal.md` § Reserved gates). No round open. **A draft goal authorizes no task.**

### Amendment 2026-09-03 — amends the preamble grounding line

The preamble above was written at draft time and was not updated when the goal was grounded later the same day. **The goal is `grounded` as of 2026-09-03, owner present**, the seven rulings taken one at a time through `/_my_ask_me` and recorded in `goal.md` § Status and § Reserved gates (slug let stand; one goal not two; `priced-levers` closed by redirect with its heating half carried in verbatim and its conductor half recorded open; the wall half study-shaped with no Row-2 re-grade claimed; no goal-specific gates; `20260903-priced-levers#2` routed to WI-040; WI-038 annotated). A grounded goal authorizes tasks. This amendment records the state change only; it decides nothing.

## Round 1 — heating-chain-first

### Strategy revision — 2026-09-03

- **Approach:** close the heating half first, and start the wall half's research in the same round so the next one opens with sources in hand. The heating half is grade-shaped — Row 4's P2 anchor names exactly what is missing ("wall-plug → coupled-power chain computed with a stated deposition assumption, verified") and the basis for it is already admissible in the pinned 1costingFE (`defaults.py:96-108`, `eta_pin = eta_source x eta_couple`), so it needs no research seam and can deliver a measured maturation delta in one round. The wall half is study-shaped and its honest form is an open question against sources, so its research runs first inside this round — research has latency, and its return feeds the *next* round's model increment, not this one's. Order: the wall-load research; then WI-039 through the modelling PM; then one promoted pin; then one narrow committed study of what the heating chain does to LCOE and to the fence structure; then the fresh non-author Row-4 re-grade. `[AGENT] (ratified by owner 2026-09-03: "great, please write a /_my_handoff for the next goal agent to run this")` — a ratified recommendation, not an owner-originated ruling; it is challenged by re-deriving against the reasoning recorded here.
- **Assumptions:** (1) Row 4's P2 anchor can be met by structure the model can carry as real objects — a computed wall-plug → transmission/coupling → deposited → coupled-power chain with named quantities — without inventing an unsourced number, because the only figure the chain needs beyond the pinned source is the deposition/coupling assumption, and `eta_couple = 1.0` is available as a *stated assumption with its basis* rather than as a silent default. (2) Row 4's S2 cell is already at target ("cost follows installed heating power with source basis") and the increment can add physics structure without breaking it; S3 (sources, transmission, launchers costed separately) is not the bar. (3) The heating half's measured delta is robust to whatever the wall half later does: a wall fix changes where the machine may sit, not what the heating chain computes. (4) The `p_input`/`p_ecrh` tie declared in `manifest.json` can be restructured with the two committed studies that swept it **restated**, at the MR-WI037-7 shape and roughly the recorded cost (~30 fixture re-derivation sites).
- **Assumption held open, not settled:** the wall-load fence's honest form. This round does not change it. Every fence-structure claim this round makes at the printed 50 MW is caveated that `wall_load_ok` compares a flat-wall **average** operand to the source's printed **peak** limit (`stellarator_plant.sysml:1061-1081`), so "the wall blocks here" is a statement about the fence as bound, not about the machine.
- **Abandonment conditions:** the P2 anchor cannot be met without an unsourced number and no admissible basis exists for the deposition assumption (that is a bounded negative on the heating half's form, surfaced with options, never defaulted); the entry-point restructuring cannot be restated honestly against the two committed studies that swept `p_input`/`p_ecrh`; the wall-load research returns evidence that the fence as bound is not merely imprecise but **wrong in a way that invalidates the deadlock reading this goal was grounded on** (a premise surprise — surfaced, with the heating half's dependent conclusions parked, never resolved silently); or a fresh gate session refuses to be obtained.
- **Intended model increment:** WI-039 (Heating System Structure) through spec → design → plan → implement, building the heating chain as model objects a grader can point at — wall-plug power, transmission/coupling, deposition, and plasma-coupled power as named quantities in a calc def that executes at the baseline and is independently verified by the oracle computing the same chain itself. Which held quantities retire as settable entry points (`eta_pin`; the `p_input`/`p_ecrh` pair) is the design's call, with the restatement duty recorded before regeneration.
- **Intended study question:** at one promoted pin with the heating chain live, what does the chain do to LCOE and to the fence structure at the printed 50 MW and at 110 MW — and does making wall-plug power explicit move the recirculating-power penalty enough to change which fence binds at the constrained optimum? Temperature is swept past 18 keV (the `20260903-priced-levers#5` lesson: the prior optimum sat at the window edge). Geometry belongs to the wall half and is declined here, traced with indicators.

No future task list. Tasks are chosen from evidence as each returns.

### T-001 scope

- **Objective:** determine what the honest form of the wall-load check is — a computed **peak** operand (the flat-wall average times a sourced peaking factor) against the printed peak limit, or an **average** operand against a sourced average limit — and whether an admissible source exists for a stellarator first-wall peaking factor or a shaped-wall area. A bounded negative naming exactly what is missing is a full answer.
- **Why now:** research has latency and its return feeds round 2's model increment, so it runs first and in parallel with nothing else executing. The fence it concerns is the one the grounding evidence shows binds at the printed power (`wall_load_ok` violated 264/439 in `20260903-priced-levers`), and the fence as bound compares two different quantities (`stellarator_plant.sysml:1061-1081`; the model's own cross-check comment says so).
- **Scope:** authorized — the native research seam (`scripts/research_seam.py` request/open/log/close, `scripts/source_registry.py register`, `/research-acquire`), read-only reads of admissible sources and of the pinned 1costingFE, and the run directory under `knowledge/research/requests/`. Excluded — any edit under `models/` or `exploration/`, any modelling-PM stage, discovery-log writes, deciding the fence's final form (that is round 2's model task, on this task's evidence), and **every artifact barred by `knowledge/holdout/aries-cs/PROTOCOL.md` §3**.
- **Inputs:** `goal.md` (§ Invariants, § Limits § Named risk, § Reserved gates). Narrower constraint, stated because the hazard is specific and recorded: **the sealed hold-out is itself a stellarator study, so a peaking-factor search walks straight at it.** The registry guard fires at *registration* — after a research subagent has already read what it fetched (`priced-levers/evidence/T-002_criterion_return.md` § 6). Every research subagent carries the clean-room screen in its own instructions **before any fetch**, and every refusal is recorded durably. No number is defaulted under any circumstance.
- **Done when:** the seam returns for each request — `REGISTERED` with citable paths, `BOUNDED_NEGATIVE` with the durable negative written, or `OPERATOR_QUEUE` naming what a person must fetch — and the goal-level reading says which of the two candidate honest forms has an admissible basis and which does not.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit is reached.

### T-001 start — 2026-09-03

Task T-001 under § T-001 scope: run the research seam for the honest form of the wall-load check — request `REQ-WALL-01` (a stellarator first-wall peak-to-average neutron wall load ratio, i.e. a peaking factor) and request `REQ-WALL-02` (a shaped first-wall area or areal shape factor for a QI stellarator, which would let the average operand be computed over the real wall rather than a circular torus). Native target: `knowledge/research/requests/` (requests, run records, receipts, returns) and, on a keeper, `knowledge/sources/` + `knowledge/MANIFEST.jsonl` + `knowledge/SOURCE_INDEX.md` through `source_registry.py`. Expected artifacts: two request files, two run directories with `return.json`, and either registered source directories or durable bounded negatives. Every search subagent carries the clean-room screen before any fetch; refusals are recorded durably.

### T-001 return — 2026-09-03

- **Outcome:** `COMPLETE`. Both seam runs closed `REGISTERED`, `--adequacy exhausted`. The task asked whether an admissible sourced basis exists for the honest form of the wall-load check; it does, for **both** candidate forms.
- **Evidence:** `evidence/T-001_research_return.md` (the goal-level reading, with the cross-check between the two sessions' sources and the arithmetic shown). Runs: `knowledge/research/requests/runs/REQ-WALL-01/20260904T035638551421/return.json`, `knowledge/research/requests/runs/REQ-WALL-02/20260904T035641288429/return.json`. Requests: `knowledge/research/requests/REQ-WALL-01.json`, `REQ-WALL-02.json`. Three sources registered: `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and_2/` (Lion et al., Nucl. Fusion 62 (2022) 076040, CC BY 4.0 — Table 1: `R`, `a`, `S_FW`, `Q_max`, `Q_avg`, `p_f` for four stellarator configurations at a 30 cm equidistant wall), `knowledge/sources/neutronics_analyses_for_a_stellarator_power_reactor_based/` (Häußler et al., HELIAS-5B, Monte Carlo corroboration), `knowledge/sources/the_helias_reactor_beidler_et_al_iaea_cn_77_ftp1_16/` (Beidler et al. 2001, published first-wall areas for HSR5/22 and HSR4/18). Spawn prompts deposited before spawning at `evidence/T-001_REQ-WALL-01_prompt.md` and `evidence/T-001_REQ-WALL-02_prompt.md`.
- **Reading:** the fence can be made honest, and the round-2 job is larger and better defined than the goal was grounded on. Three results carry it. (1) **A peaking factor exists and is bounded** — 1.51 to 1.69 across four unoptimised stellarator configurations in one table, 1.84 and 2.03–2.11 from two other machines and methods; roughly 1.5–2.1 read together. It is a property of the **wall you choose**, not of the plasma: the same HELIAS-5 plasma gives 1.69, 1.23 and 1.12 for three wall shapes in the same table, so no number transfers without its wall definition. (2) **A shaped first-wall area is published too**, and the two sources appear to disagree by 24% (ratios 1.338 and 1.663 against `4 pi^2 R a`) until the radius is fixed: a ratio taken against the *plasma* minor radius silently absorbs the plasma-to-wall standoff, and Beidler never publishes his — so his factor cannot transfer to the model at all. Lion publishes his (30 cm), so on the model's own **wall-side** radius convention the shape factor is 1.146–1.303 across four configurations, a much tighter spread, which is itself evidence that the wall-side radius is the right comparand. (3) **The finding that outranks both candidate forms:** every published average — Lion's, Häußler's, Beidler's — is taken over a **shaped 3D wall**. None is taken over a circular torus. So the model's flat-wall area is wrong for *either* form, and neither "computed peak against the peak limit" nor "average against a sourced average limit" is honest without correcting the area. That is a sharper statement of the defect than § Question was grounded on, and it is a finding, not a decision. **The honest fence probably tightens:** Lion's HELIAS-5 shape and peaking factors taken together give a net 1.475 on a circular-torus average, which would put the pinned baseline at 4.58 against the 4.05 limit — violated. Recorded for scale only, not as a prediction (the model's standoff is 0.10 m against Lion's 0.30 m, and arguing that transfer is round 2's work), and recorded *because* `goal.md` § Invariants requires a tighter check with a violated baseline to be disclosed and explained rather than tuned away. The § Limits named risk — no admissible source outside the sealed material — **did not materialise**; the three fallback options it names are not needed.
- **Decision.** Trigger: the wall research returned sources for both candidate forms plus the area finding that constrains both. Decision and reason: **the fence's form is not chosen here.** The task's scope excluded it, the evidence makes the choice larger than a form selection (it is a form *and* an area basis *and* a standoff-transfer argument), and `goal.md` § Reserved gates gives that decision to the round agent that makes the model change, on sourced evidence, with the fresh checkpoint and round review checking it. Round 1's strategy stands unchanged: heating first, the wall half in round 2 with these sources in hand. Tier: `execution detail`. Decided by: the round agent. What changed: `evidence/T-001_research_return.md`, `knowledge/research/requests/**`, three source directories, `knowledge/MANIFEST.jsonl`, `knowledge/SOURCE_INDEX.md`.
- **Decision.** Trigger: the REQ-WALL-01 session's URL registration captured an IOPscience Radware bot-check page and committed it as a source — `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and/` (1415 bytes), indexed at `knowledge/SOURCE_INDEX.md:382-392`. The extractor extracted the bot wall successfully, so no capture failure and no hold-out term existed for the seam to catch. Decision and reason: **left in place and surfaced, not hand-removed.** `source_registry.py` has no `unregister`, and the registry's contract is that a source directory, a manifest row and an index block move together or not at all; hand-editing three files to undo one registration is the ad-hoc surgery that contract exists to prevent, and routing around a missing operation is not a round agent's call. The correctly registered paper sits at the `..._2` slug and its own index `Caveat` names the junk slug, so a reader landing on either is warned. Tier: `execution detail`, with a tooling gap surfaced to the owner (no `unregister`; nothing guards against an extractor cleanly extracting a bot wall). Decided by: the round agent. What changed: `none` — recorded only, here and in `evidence/T-001_research_return.md` § 7.
- **Decision.** Trigger: the REQ-WALL-02 session refused `sciencedirect.com/.../S0920379626003273` — a **Helios** planar-coil stellarator first-wall paper — under PROTOCOL.md §3, without fetching, and logged it. Decision and reason: the refusal is correct and is recorded durably as required. The durable hazard is named for every future session in this area: **Helios (planar-coil, barred) and HELIAS (the W7-X-line reactor, admissible) differ by one letter and surfaced in the same result set.** The screen held only because it was applied before the fetch; the registry guard fires at registration and would not have helped. This is the `priced-levers` L-005 mechanism confirmed a second time, in a search where the near-miss was a homograph rather than a topic. Tier: `execution detail`. Decided by: the REQ-WALL-02 session, upheld by the round agent. What changed: the run record at `knowledge/research/requests/runs/REQ-WALL-02/20260904T035641288429/`; candidate for the round's proposed learning delta.

### T-002 scope

- **Objective:** build the heating chain as real model structure — carry **WI-039** through the modelling PM (spec → design → plan → implement) so that wall-plug power, transmission and coupling, the deposition assumption, and plasma-coupled power exist as **named quantities in a calc def that executes at the baseline and is independently verified**, satisfying rubric Row 4's P2 physics anchor in full.
- **Why now:** T-001 returned and the heating half is the round's model increment. It is the half with a written grade gap (`grading.md@fc80e5b2` cell R4.P = 1 against a written target of 2, grader note G1: "a held ratio dividing one held constant inside another subsystem's sum derives nothing"), and its basis is already admissible in the pinned 1costingFE (`defaults.py:96-108`), so it needs nothing T-001 was chasing.
- **Scope:** authorized — the modelling PM stages with their validation levels (`/spec-model`, `/design-model`, `/plan-model`, `/implement-model`), edits under `models/library/` and `models/designs/stellarator_09/`, the matching independent computation in the oracle `exploration/stellarator_e2e/verify_stellaris.py`, `agentic-mbse pm` operations, and activating WI-039 in `work/BACKLOG.md`.
- **Scope — three exclusions, each for a stated reason.** (1) **The wall-load fence is not touched.** T-001's evidence is round 2's to act on; changing the fence here would move comparison meaning mid-round and put one round's study on two different questions. (2) **Row 4's bar is the written P2 and nothing more.** The item's minted title names S3 content ("Sources, Transmission, Launchers") and R4.S is already **at** its S2 target ("cost follows installed heating power with source basis"); S3-shaped cost splitting may be built where the engineering genuinely wants it but is not the bar, and **S2 must not be broken reaching for it**. (3) **`sustainment_ok`'s violated baseline is not fixed here** — 90.6 MW required against 50 installed is the disclosed W-form residual (`goal.md` § Invariants). This task changes what installed power costs and how it reaches the plasma; it does not tune the requirement.
- **Inputs:** `goal.md` § Invariants and § Answered when (a). Narrower constraints, three, each stated because it is a trap rather than a general rule: (i) **the deposition assumption must be stated, not defaulted** — `eta_pin = 0.5` is `eta_source_ecrh 0.50 x eta_couple 1.0` (`stellarator_plant.sysml:648`), and `eta_couple = 1.0` is a *default*, not a sourced coupling figure; the P2 anchor requires a **stated** deposition assumption, so it is either sourced or written down as a named judgment with its reasoning, never left silent; (ii) **parity with the oracle is only evidence if the oracle computes the chain itself** — `verify_stellaris.py:391-392` today mirrors the model's own `p_input / eta_pin` form, so re-using that form would make the check a tautology; (iii) **the restatement duty is not waivable** — the `p_input`/`p_ecrh` tie is declared in `manifest.json` and was swept by both `20260901-sustainment-fence` and `20260903-priced-levers`; if this task retires or restructures either key, or retires `eta_pin` as an entry point, those committed studies are **restated, never silently broken** (the MR-WI037-7 shape, ~30 fixture re-derivation sites at the last two retirements), and every changed expectation is re-derived from live evidence rather than patched to match.
- **Done when:** the heating chain exists as model objects a fresh grader can point at, the model regenerates and executes at the baseline, `tests/models` and `tests/study` are green, any changed committed-study expectation is restated with its derivation shown, and the goal-level reading says whether the P2 anchor's full text is now evidenced — **or** a bounded negative says with reasons which part of the anchor cannot be met from admissible sources.
- **Stop when:** a discovered prerequisite, a strategy blocker, an owner gate, or a declared limit.

### T-002 start — 2026-09-03

Task T-002 under § T-002 scope: activate WI-039 and carry it through the modelling PM. Native target: `work/BACKLOG.md` (status `backlog` → `active`) and `work/active/WI-039_heating-system-structure/` (`spec.md`, then `design.md`, `plan.md`, implementation), with model edits under `models/library/analyses/` and `models/designs/stellarator_09/` and the independent chain added to `exploration/stellarator_e2e/verify_stellaris.py`. Expected artifacts: the work-item directory with its four native stage records, the changed model files, the oracle's own computation of the chain, and green `tests/models` / `tests/study`.

### T-002 return — 2026-09-03

- **Outcome:** `COMPLETE`. WI-039 carried through spec → design → plan → implement; the heating chain exists as model objects, executes at the baseline, and is verified live.
- **Evidence:** `work/active/WI-039_heating-system-structure/` — `spec.md` (MR-WI039-1..11), `design.md` (decisions D1–D4, prototype PASS), `plan.md` (five phases plus the § MR-WI039-9 restatement). Model: `models/library/analyses/mfe_heating_chain.sysml` (new), `models/library/analyses/mfe_power_balance.sysml`, `models/designs/generic_mfe/mfe_plant.sysml`, `models/designs/stellarator_09/stellarator_plant.sysml`. Oracle: `exploration/stellarator_e2e/verify_stellaris.py`, `exploration/stellarator_e2e/studies/oracle_entry.py`. Commits `b45b4b01` (phases 1–4), `015f026e` (re-pin, census, snapshot, seam, fixtures), `6678968e` (batteries green, SV rows). SV-048 and SV-049 recorded `passing`.
- **Reading.** The Row-4 P2 anchor's full text is now pointable, and the round's own evidence says so rather than asserting it.
  - **"Wall-plug → coupled-power chain computed":** `calc def 'Heating Power Chain'` carries wall-plug power, a source stage, a coupling stage, source-output power, plasma-coupled power, the lumped efficiency and the total wall-plug draw as **seven named quantities**, executing as a module in the sealed package. `recirculating` no longer performs `p_input_in / eta_pin_in`; it consumes the chain's computed wall-plug total. That inline division was the grader's stated objection (`grading.md@fc80e5b2` G1: "a held ratio dividing one held constant inside another subsystem's sum derives nothing"), so it is the line that had to change, and it did.
  - **"with a stated deposition assumption":** `eta_couple_heat = 1.00` carries a written assumption in the model text — what it asserts (all gyrotron output reaches the plasma), that it is an idealisation rather than a measurement, its **direction of optimism**, and that a sourced coupling figure would enter at that one attribute. It is no longer a silent inherited default.
  - **"verified":** the oracle computes the chain from the design's stated equations rather than from the generated module, and its own `p_input / eta_pin` term is gone. **The perturbation test is the anchor's real evidence**, and it is the grader's own `why_not_next` test ("re-derives when efficiencies or delivered power change independently"): moving `eta_source_heat` alone, 0.50 → 0.45, moves heating capital 264,145,000 → 237,730,500, LCOE 307.087120428 → 306.798743084, and in the independently written oracle `q_eng` 3.078430435 → 3.074207573 with `rec_frac` and `p_net`. Model and oracle agree to twelve digits at both settings.
  - **The baseline did not move.** LCOE 307.08712042841586, nine anchors green, nine verdicts identical (`sustainment_ok` violated, `cond_strain_ok` satisfied), heating capital $264.145M exactly. Nothing was disclosed because nothing changed — the increment adds derivation without moving the physics point, the WI-037 shape.
  - **An unplanned improvement, found by a test rather than claimed:** `sustainment_ok` was **computed-vs-bound** and is now **computed-vs-computed**. Its installed-power operand was bound to the held design attribute `p_input`; it now reads the chain's coupled power. `tests/study/test_operand_bindings.py` caught the stale binding and `test_known_answers.py` caught the class change. Worth naming because `grading.md` G6 records held-vs-held as the failure mode this ladder is built to catch, and this fence moved the other way.
  - **R4.S was not risked.** `heating_cost__cost` is unchanged to the dollar and still follows installed heating power with its source basis. The cost driver is the chain's **source-output** power, not wall-plug — the per-MW rate is calibrated to gyrotron procurement, and binding wall-plug there would have silently doubled the account.
  - **Batteries:** `tests/models` 48 passed / 13 skipped; `tests/study` 356 passed / 1 skipped — the recorded baselines exactly. Validation levels measured against the pre-WI-039 tree in a scratch worktree rather than assumed: Level 2 unchanged at its 12 pre-existing warnings, Level 6 improved 233 → 230.
  - **What this does not yet establish:** the grade. § Answered when (a) is a **fresh non-author** re-grade from a pointer-only evidence map, and this task's job was to make the anchor pointable, not to score it.
- **Decision.** Trigger: the P2 anchor could be met by a cheap backward solve (hold the sourced coupled power, compute wall-plug from it) or by a forward chain from installed wall-plug power. Decision and reason: **wall-plug is the entry point** (design D1). The backward solve satisfies the anchor's letter but is the same held-ratio division with names attached — a fresh grader could read it as a relabel and score R4.P = 1 again, and the round would have spent its increment for nothing. With wall-plug forward, the quantities that *govern* — the coupled power entering `p_th`, and the coupled power `sustainment_ok` compares against the plasma's requirement — become derived. Cost accepted and paid: three entry points retired, two committed studies restated, the census re-derived. Tier: `execution detail` (the goal reserved no gates and re-ruled the delegation, ruling 5). Decided by: the round agent. What changed: the four model files, the two oracle files, `studies/manifest.json`, `tests/models/data/mfe_census.json`, six `tests/study/data/*.expected.json`, `tests/model_families.py`, two test modules, `stellarator.snapshot.json`; commits `b45b4b01`, `015f026e`, `6678968e`.
- **Decision.** Trigger: the `p_input`/`p_ecrh` tie declared in `studies/manifest.json` kept two model constants moving together because the model did not know they were one physical quantity. Decision and reason: **the tie is removed, not rewritten.** Both powers now descend from one wall-plug input, so the invariant holds structurally and the declaration has nothing left to maintain. A workaround that manually maintains an invariant the platform can declare is a defect to remove, not a mechanism to formalise. The `magnet__R0` / `R` tie stays: it is genuinely two attributes for one radius with no chain relating them. Tier: `execution detail`. Decided by: the round agent. What changed: `exploration/stellarator_e2e/studies/manifest.json`.
- **Decision.** Trigger: the plan named the 1costingFE handshake as the sharp edge of the entry-point retirement, with a `PREREQUISITE` return as the honest outcome if it could not be translated. Decision and reason: **it is not repaired, and it is not this item's break.** `handshake_1costingfe.py` raises `FileNotFoundError` on `generated/inputs/system_design.json`, a file the package stopped generating at the model migration — it fails there *before* reaching any heating key, and it fails identically on the pre-WI-039 tree at `860ce7d1`. `tests/study/test_no_retired_identifiers.py:36` already classifies it as a historical record rather than an executable path. Repairing only its heating keys would leave it broken while looking repaired; the keys it would need are written into the plan's restatement so whoever revives it need not re-derive them. `emit_1cfe_point.py` needs no change at all — `_effective_eta_pin` is 1costingFE's method on 1costingFE's own params. Tier: `execution detail`, with the pre-existing break surfaced. Decided by: the round agent. What changed: `none` in code; recorded in `work/active/WI-039_heating-system-structure/plan.md` § MR-WI039-9 restatement.
- **Decision.** Trigger: MR-WI039-9 and the WI-037 precedent both require the restatement to be written *before* the regeneration commit; it was written after. Decision and reason: **recorded as a deviation rather than quietly reordered.** The regeneration was reversible and was in fact re-run, and no committed study was touched in between, so nothing was lost — but the sequence was not followed and the record says so. Tier: `execution detail`. Decided by: the round agent. What changed: the deviation is the first paragraph of `plan.md` § MR-WI039-9 restatement.

### T-003 scope

- **Objective:** promote exactly one pin for this round — run the integration seam (`scripts/integrate.py`) over the WI-039 package and obtain a `CANDIDATE` naming the package, its manifest, its pin and both fingerprints, or a `BLOCKER` naming the one producer that stopped the sequence.
- **Why now:** T-002 returned `COMPLETE` and the round's study must run against a promoted pin — that is what makes two results comparable and what bounds the round (`GOAL_RUNBOOK.md` § Opening and closing a round: at most one promoted pin, at most one committed study).
- **Scope:** authorized — `scripts/integrate.py` with its ten gates, and any repair the seam's own refusal names as belonging to the modelling item. Excluded — relaxing, skipping, or arguing with a gate; the seam proves and does not perform, so a refusal at the regeneration gate means the modelling work is unfinished and goes back to WI-039, never around the seam.
- **Inputs:** `goal.md` § Invariants; `docs/integration_seam_operator_guide.md`. Narrower constraint: gate 5 judges the **repository**, not the package, so the working tree must be clean before the run — a dirty tree refuses for reasons that have nothing to do with the package.
- **Done when:** the seam returns `CANDIDATE` with the pin and both fingerprints recorded as evidence, or `BLOCKER` with the refusing gate and its producer's own output named.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-003 start — 2026-09-03

Task T-003 under § T-003 scope: run the integration seam over the WI-039 package. Native target: `scripts/integrate.py` against `--models-root exploration/stellarator_e2e/models`, `--package exploration/stellarator_e2e/pkg/stellarator_tea`, `--manifest exploration/stellarator_e2e/studies/manifest.json`, citing `--audited-work work/active/WI-039_heating-system-structure@<commit>`. Expected artifact: one integration return JSON deposited under `evidence/`, carrying either a `CANDIDATE` pin or a named refusing gate.

### T-003 return — 2026-09-03

- **Outcome:** `COMPLETE`. **`CANDIDATE`** — all ten gates passed on the first run, no retry.
- **Evidence:** `evidence/T-003_integration_return.json` (the return of record), with the seam's own outputs beside it: `T-003_baseline_result.json`, `T-003_verification_summary.json`, `T-003_preflight_results.json`, `T-003_package_identity.json`, `T-003_recaptured.snapshot.json`, `T-003_clean.json`. Audited work cited `work/active/WI-039_heating-system-structure@ba7098a2` (ADR-0006's citation form).
- **Reading — this round's promoted pin.** Package `stellarator_tea`; indicator-input pin **`2649e0ea2a987f3e56e9f659c4b4c48c9b670e6a0f8e47831c5fc253608455ad`**; semantic fingerprint **`48731d1570bb20cdeae45faa20545aae46aeda5982a4fbc2129d7784a66595ba`**; executable fingerprint **`b0c5f3eda9014fa1c84573c300c63f7fc04ecc1f8ea47047caa7775c010c6c78`**; teax at `744745f8`. That is the round's one pin, and the round's one committed study runs against it (`GOAL_RUNBOOK.md` § Opening and closing a round).
  - The gates worth naming: **regeneration rewrote no byte outside `handwritten/`** — the package really is the integrated form of the model, not a stale copy; **72 handwritten files byte-identical**; **the snapshot recaptures byte-identically and 199 entry points re-derive to the census as bound** — the retirement of three entry points and the minting of five is confirmed by the producer rather than by my arithmetic; **oracle parity holds and every verdict re-derives**.
  - `verification_summary.json`'s `not_independently_verified` list is **empty**, and the known tooling gap behind that field (a multi-field module output declared as an evidence-store channel yields a silent blank column, filed beside `20260903-priced-levers#4`) still stands — an empty list is not by itself proof of full coverage. Recorded so the next reader does not over-read it.
  - The seam reports one thing it did **not** run and that nothing else covers: `assert_read_set_covered` is out of reach at this gate and is filed. Carried forward, not resolved here.
- **Decision.** Trigger: the seam returned `CANDIDATE` on the first attempt. Decision and reason: the pin is promoted and the round proceeds to its study. No gate was relaxed, skipped, or argued with; the working tree was committed clean before the run precisely because gate 5 judges the repository rather than the package. Tier: `execution detail`. Decided by: the round agent, on the seam's own verdict. What changed: `evidence/T-003_*.json`.

### T-004 scope

- **Objective:** one committed study at the promoted pin answering: **with the heating chain live, what does the heating system do to LCOE and to the fence structure at the printed heating level and at the higher one — and does the chain's new lever, source efficiency, buy anything the old held `eta_pin` could not?**
- **Why now:** T-003 promoted the round's one pin, and the round closes on a valid study reading (`GOAL_RUNBOOK.md` § Opening and closing a round, trigger 1). The strategy named this study as narrow and heating-shaped; it is the last task before the round result.
- **Scope.** Authorized axes: `p_wallplug_heat` at the two levels the predecessor study used, restated (**100 and 220 MW wall-plug**, which is the printed 50 MW and the 110 MW coupled at `eta_pin_eff = 0.50`); `eta_source_heat`, **the axis this round created** — the old lumped `eta_pin` was a held constant and could not be swept meaningfully, and sweeping source efficiency now moves coupled power, heating capital and recirculating power together; `T_i0`, swept **past 18 keV** because the previous optimum sat at the window edge (`20260903-priced-levers#5`, `goal.md` § Invariants); `n_e0` and `I_coil` as the power-density and field levers already exercised at the predecessor pin.
- **Scope — declined, and traced with indicators rather than silently dropped.** Geometry (`R`, `a`): it belongs to the wall half and round 2. `R`'s effect on wall load at this pin is an **open measurement**, not a known (`goal.md` § Invariants, corrected at checkpoint C-001.r1 of `priced-levers`), and sweeping geometry here would confound the heating question with the wall question this round deliberately did not touch. `B_max`: still unpriced — WI-038 is its vehicle and WI-040 comes first, so sweeping it would manufacture feasible points that cost nothing.
- **Inputs:** `goal.md` § Invariants; the promoted pin from § T-003 return; `modeling_project/STUDY_POLICY.md` and the `run-study` runbook. **Two narrower constraints, both about honesty rather than mechanism.** (1) **Every fence-structure claim at the printed heating level is caveated that `wall_load_ok` compares a flat-wall average operand to the source's printed peak limit** — the round's own held-open assumption, now with T-001's evidence behind it: the model's circular-torus area is 15–30% smaller than a shaped stellarator first wall on the same wall-side radius, and the peaking factor is 1.5–2.1 against a *shaped-wall* average. So "the wall blocks here" is a statement about the fence as bound, not about the machine. (2) **`sustainment_ok`'s violated baseline is disclosed, never fitted** — and note it is now computed-vs-computed, so a heating sweep moves both sides.
- **Done when:** the record is committed with a valid reading — including an adverse or inconclusive one — its dispositions proposed for the checkpoint, and every discovery-log row the evidence touches routed.
- **Stop when:** a discovered prerequisite, a strategy blocker, an owner gate, or a declared limit. **Before committing the record:** `tests/study/test_records.py` must pass — it rejects any `<` character in `record.md` (write "below 1e-9") and requires `effective_executable_fingerprint.value` on every `snapshot.json` arm (`gotcha_study_record_contract`; the predecessor record failed both and needed an Addendum, which is a recorded defect and not a precedent).

### T-004 start — 2026-09-03

Task T-004 under § T-004 scope: run and commit one study at the promoted pin `2649e0ea2a98…`. Native target: `/run-study` against `exploration/stellarator_e2e/pkg/stellarator_tea`, writing a new record directory under `exploration/stellarator_e2e/studies/`. Expected artifacts: the record directory (`record.md`, `axes.json`, `indicators.json`, `snapshot.json`, `study.py`/`scan.py`, `results/`), a fresh-administrator synthesis, and proposed dispositions for checkpoint C-001.
