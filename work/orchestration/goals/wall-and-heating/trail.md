# Trail: wall-and-heating

What happened, and what was decided. Append-only, newest entry last, ISO dates; no entry is ever edited in place — corrections are `### Amendment` entries. This file logs judgment, not routine stage motion; native workflows keep their own stage records and are cited, never restated. Procedure: `work/orchestration/GOAL_RUNBOOK.md`.

Goal **drafted** 2026-09-03 (`goal.md`, status `draft`, owner not present; the rulings it needs are listed in `goal.md` § Reserved gates). No round open. **A draft goal authorizes no task.**

### Amendment 2026-09-03 — amends the preamble grounding line

The preamble above was written at draft time and was not updated when the goal was grounded later the same day. **The goal is `grounded` as of 2026-09-03, owner present**, the seven rulings taken one at a time through `/_my_ask_me` and recorded in `goal.md` § Status and § Reserved gates (slug let stand; one goal not two; `priced-levers` closed by redirect with its heating half carried in verbatim and its conductor half recorded open; the wall half study-shaped with no Row-2 re-grade claimed; no goal-specific gates; `20260903-priced-levers#2` routed to WI-040; WI-038 annotated). A grounded goal authorizes tasks. This amendment records the state change only; it decides nothing.

## Round 1 — heating-chain-first

### Strategy revision — 2026-09-03

- **Approach:** close the heating half first, and start the wall half's research in the same round so the next one opens with sources in hand. The heating half is grade-shaped — Row 4's P2 anchor names exactly what is missing ("wall-plug → coupled-power chain computed with a stated deposition assumption, verified") and the basis for it is already admissible in the pinned 1costingFE (`defaults.py:96-108`, `eta_pin = eta_source x eta_couple`), so it needs no research seam and can deliver a measured maturation delta in one round. The wall half is study-shaped and its honest form is an open question against sources, so its research runs first inside this round — research has latency, and its return feeds the *next* round's model increment, not this one's. Order: the wall-load research; then WI-039 through the modelling PM; then one promoted pin; then one narrow committed study of what the heating chain does to LCOE and to the fence structure; then the fresh non-author Row-4 re-grade. `[AGENT] (ratified by owner 2026-09-03: "great, please write a /_my_handoff for the next goal agent to run this")` — a ratified recommendation, not an owner-originated ruling; it is challenged by re-deriving against the reasoning recorded here.
- **Assumptions:** (1) Row 4's P2 anchor can be met by structure the model can carry as real objects — a computed wall-plug → transmission/coupling → deposited → coupled-power chain with named quantities — without inventing an unsourced number, because the only figure the chain needs beyond the pinned source is the deposition/coupling assumption, and `eta_couple = 1.0` is available as a *stated assumption with its basis* rather than as a silent default. (2) Row 4's S2 cell is already at target ("cost follows installed heating power with source basis") and the increment can add physics structure without breaking it; S3 (sources, transmission, launchers costed separately) is not the bar. (3) The heating half's measured delta is robust to whatever the wall half later does: a wall fix changes where the machine may sit, not what the heating chain computes. (4) The `p_input`/`p_ecrh` tie declared in `manifest.json` can be restructured with the two committed studies that swept it **restated**, at the MR-WI037-7 shape and roughly the recorded cost (~30 fixture re-derivation sites).
- **Assumption held open, not settled:** the wall-load fence's honest form. This round does not change it. Every fence-structure claim this round makes at the printed 50 MW is caveated that `wall_load_ok` compares a flat-wall **average** operand to the source's printed **peak** limit (`stellarator_plant.sysml:1061-1081`), so "the wall blocks here" is a statement about the fence as bound, not about the machine.
- **Abandonment conditions:** the P2 anchor cannot be met without an unsourced number and no admissible basis exists for the deposition assumption (that is a bounded negative on the heating half's form, surfaced with options, never defaulted); the entry-point restructuring cannot be restated honestly against the two committed studies that swept `p_input`/`p_ecrh`; the wall-load research returns evidence that the fence as bound is not merely imprecise but **wrong in a way that invalidates the deadlock reading this goal was grounded on** (a premise surprise — surfaced, with the heating half's dependent conclusions parked, never resolved silently); or a fresh gate session refuses to be obtained.
- **Intended model increment:** WI-039 (Heating System Structure) through spec → design → plan → implement, building the heating chain as model objects a grader can point at — wall-plug power, transmission/coupling, deposition, and plasma-coupled power as named quantities in a calc def that executes at the baseline and is independently verified by the oracle computing the same chain itself. Which held quantities retire as settable entry points (`eta_pin`; the `p_input`/`p_ecrh` pair) is the design's call, with the restatement duty recorded before regeneration.
- **Intended study question:** at one promoted pin with the heating chain live, what does the chain do to LCOE and to the fence structure at the printed 50 MW and at 110 MW — and does making wall-plug power explicit move the recirculating-power penalty enough to change which fence binds at the constrained optimum? Temperature is swept past 18 keV (the `20260903-priced-levers#5` lesson: the prior optimum sat at the window edge). Geometry belongs to the wall half and is declined here, traced with indicators.

No future task list. Tasks are chosen from evidence as each returns.

### T-001 scope

- **Objective:** determine what the honest form of the wall-load check is — a computed **peak** operand (the flat-wall average times a sourced peaking factor) against the printed peak limit, or an **average** operand against a sourced average limit — and whether an admissible source exists for a stellarator first-wall peaking factor or a shaped-wall area. A bounded negative naming exactly what is missing is a full answer.
- **Why now:** research has latency and its return feeds round 2's model increment, so it runs first and in parallel with nothing else executing. The fence it concerns is the one the grounding evidence shows binds at the printed power (`wall_load_ok` violated 264/439 in `20260903-priced-levers`), and the fence as bound compares two different quantities (`stellarator_plant.sysml:1061-1081`; the model's own cross-check comment says so).
- **Scope:** authorized — the native research seam (`scripts/research_seam.py` request/open/log/close, `scripts/source_registry.py register`, `/research-acquire`), read-only reads of admissible sources and of the pinned 1costingFE, and the run directory under `knowledge/research/requests/`. Excluded — any edit under `models/` or `exploration/`, any modelling-PM stage, discovery-log writes, deciding the fence's final form (that is round 2's model task, on this task's evidence), and **every artifact barred by `knowledge/holdout/aries-cs/PROTOCOL.md` §3**.
- **Inputs:** `goal.md` (§ Invariants, § Limits § Named risk, § Reserved gates). Narrower constraint, stated because the hazard is specific and recorded: **the sealed hold-out is itself a stellarator study, so a peaking-factor search walks straight at it.** The registry guard fires at *registration* — after a research subagent has already read what it fetched (`priced-levers/evidence/T-002_criterion_return.md` § 6). Every research subagent carries the clean-room screen in its own instructions **before any fetch**, and every refusal is recorded durably. No number is defaulted under any circumstance.
- **Done when:** the seam returns for each request — `REGISTERED` with citable paths, `BOUNDED_NEGATIVE` with the durable negative written, or `OPERATOR_QUEUE` naming what a person must fetch — and the goal-level reading says which of the two candidate honest forms has an admissible basis and which does not.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit is reached.

### T-001 start — 2026-09-03

Task T-001 under § T-001 scope: run the research seam for the honest form of the wall-load check — request `REQ-WALL-01` (a stellarator first-wall peak-to-average neutron wall load ratio, i.e. a peaking factor) and request `REQ-WALL-02` (a shaped first-wall area or areal shape factor for a QI stellarator, which would let the average operand be computed over the real wall rather than a circular torus). Native target: `knowledge/research/requests/` (requests, run records, receipts, returns) and, on a keeper, `knowledge/sources/` + `knowledge/MANIFEST.jsonl` + `knowledge/SOURCE_INDEX.md` through `source_registry.py`. Expected artifacts: two request files, two run directories with `return.json`, and either registered source directories or durable bounded negatives. Every search subagent carries the clean-room screen before any fetch; refusals are recorded durably.

### T-001 return — 2026-09-03

- **Outcome:** `COMPLETE`. Both seam runs closed `REGISTERED`, `--adequacy exhausted`. The task asked whether an admissible sourced basis exists for the honest form of the wall-load check; it does, for **both** candidate forms.
- **Evidence:** `evidence/T-001_research_return.md` (the goal-level reading, with the cross-check between the two sessions' sources and the arithmetic shown). Runs: `knowledge/research/requests/runs/REQ-WALL-01/20260904T035638551421/return.json`, `knowledge/research/requests/runs/REQ-WALL-02/20260904T035641288429/return.json`. Requests: `knowledge/research/requests/REQ-WALL-01.json`, `REQ-WALL-02.json`. Three sources registered: `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and_2/` (Lion et al., Nucl. Fusion 62 (2022) 076040, CC BY 4.0 — Table 1: `R`, `a`, `S_FW`, `Q_max`, `Q_avg`, `p_f` for four stellarator configurations at a 30 cm equidistant wall), `knowledge/sources/neutronics_analyses_for_a_stellarator_power_reactor_based/` (Häußler et al., HELIAS-5B, Monte Carlo corroboration), `knowledge/sources/the_helias_reactor_beidler_et_al_iaea_cn_77_ftp1_16/` (Beidler et al. 2001, published first-wall areas for HSR5/22 and HSR4/18). Spawn prompts deposited before spawning at `evidence/T-001_REQ-WALL-01_prompt.md` and `evidence/T-001_REQ-WALL-02_prompt.md`.
- **Reading:** the fence can be made honest, and the round-2 job is larger and better defined than the goal was grounded on. Three results carry it. (1) **A peaking factor exists and is bounded** — 1.51 to 1.69 across four unoptimised stellarator configurations in one table, 1.84 and 2.03–2.11 from two other machines and methods; roughly 1.5–2.1 read together. It is a property of the **wall you choose**, not of the plasma: the same HELIAS-5 plasma gives 1.69, 1.23 and 1.12 for three wall shapes in the same table, so no number transfers without its wall definition. (2) **A shaped first-wall area is published too**, and the two sources appear to disagree by 24% (ratios 1.338 and 1.663 against `4 pi^2 R a`) until the radius is fixed: a ratio taken against the *plasma* minor radius silently absorbs the plasma-to-wall standoff, and Beidler never publishes his — so his factor cannot transfer to the model at all. Lion publishes his (30 cm), so on the model's own **wall-side** radius convention the shape factor is 1.146–1.303 across four configurations, a much tighter spread, which is itself evidence that the wall-side radius is the right comparand. (3) **The finding that outranks both candidate forms:** every published average — Lion's, Häußler's, Beidler's — is taken over a **shaped 3D wall**. None is taken over a circular torus. So the model's flat-wall area is wrong for *either* form, and neither "computed peak against the peak limit" nor "average against a sourced average limit" is honest without correcting the area. That is a sharper statement of the defect than § Question was grounded on, and it is a finding, not a decision. **The honest fence probably tightens:** Lion's HELIAS-5 shape and peaking factors taken together give a net 1.475 on a circular-torus average, which would put the pinned baseline at 4.58 against the 4.05 limit — violated. Recorded for scale only, not as a prediction (the model's standoff is 0.10 m against Lion's 0.30 m, and arguing that transfer is round 2's work), and recorded *because* `goal.md` § Invariants requires a tighter check with a violated baseline to be disclosed and explained rather than tuned away. The § Limits named risk — no admissible source outside the sealed material — **did not materialise**; the three fallback options it names are not needed.
- **Decision.** Trigger: the wall research returned sources for both candidate forms plus the area finding that constrains both. Decision and reason: **the fence's form is not chosen here.** The task's scope excluded it, the evidence makes the choice larger than a form selection (it is a form *and* an area basis *and* a standoff-transfer argument), and `goal.md` § Reserved gates gives that decision to the round agent that makes the model change, on sourced evidence, with the fresh checkpoint and round review checking it. Round 1's strategy stands unchanged: heating first, the wall half in round 2 with these sources in hand. Tier: `execution detail`. Decided by: the round agent. What changed: `evidence/T-001_research_return.md`, `knowledge/research/requests/**`, three source directories, `knowledge/MANIFEST.jsonl`, `knowledge/SOURCE_INDEX.md`.
- **Decision.** Trigger: the REQ-WALL-01 session's URL registration captured an IOPscience Radware bot-check page and committed it as a source — `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and/` (1415 bytes), indexed at `knowledge/SOURCE_INDEX.md:382-392`. The extractor extracted the bot wall successfully, so no capture failure and no hold-out term existed for the seam to catch. Decision and reason: **left in place and surfaced, not hand-removed.** `source_registry.py` has no `unregister`, and the registry's contract is that a source directory, a manifest row and an index block move together or not at all; hand-editing three files to undo one registration is the ad-hoc surgery that contract exists to prevent, and routing around a missing operation is not a round agent's call. The correctly registered paper sits at the `..._2` slug and its own index `Caveat` names the junk slug, so a reader landing on either is warned. Tier: `execution detail`, with a tooling gap surfaced to the owner (no `unregister`; nothing guards against an extractor cleanly extracting a bot wall). Decided by: the round agent. What changed: `none` — recorded only, here and in `evidence/T-001_research_return.md` § 7.
- **Decision.** Trigger: the REQ-WALL-02 session refused `sciencedirect.com/.../S0920379626003273` — a **Helios** planar-coil stellarator first-wall paper — under PROTOCOL.md §3, without fetching, and logged it. Decision and reason: the refusal is correct and is recorded durably as required. The durable hazard is named for every future session in this area: **Helios (planar-coil, barred) and HELIAS (the W7-X-line reactor, admissible) differ by one letter and surfaced in the same result set.** The screen held only because it was applied before the fetch; the registry guard fires at registration and would not have helped. This is the `priced-levers` L-005 mechanism confirmed a second time, in a search where the near-miss was a homograph rather than a topic. Tier: `execution detail`. Decided by: the REQ-WALL-02 session, upheld by the round agent. What changed: the run record at `knowledge/research/requests/runs/REQ-WALL-02/20260904T035641288429/`; candidate for the round's proposed learning delta.

### T-002 scope

- **Objective:** build the heating chain as real model structure — carry **WI-039** through the modelling PM (spec → design → plan → implement) so that wall-plug power, transmission and coupling, the deposition assumption, and plasma-coupled power exist as **named quantities in a calc def that executes at the baseline and is independently verified**, satisfying rubric Row 4's P2 physics anchor in full.
- **Why now:** T-001 returned and the heating half is the round's model increment. It is the half with a written grade gap (`grading.md@fc80e5b2` cell R4.P = 1 against a written target of 2, grader note G1: "a held ratio dividing one held constant inside another subsystem's sum derives nothing"), and its basis is already admissible in the pinned 1costingFE (`defaults.py:96-108`), so it needs nothing T-001 was chasing.
- **Scope:** authorized — the modelling PM stages with their validation levels (`/spec-model`, `/design-model`, `/plan-model`, `/implement-model`), edits under `models/library/` and `models/designs/stellarator_09/`, the matching independent computation in the oracle `exploration/stellarator_e2e/verify_stellaris.py`, `agentic-mbse pm` operations, and activating WI-039 in `work/BACKLOG.md`.
- **Scope — three exclusions, each for a stated reason.** (1) **The wall-load fence is not touched.** T-001's evidence is round 2's to act on; changing the fence here would move comparison meaning mid-round and put one round's study on two different questions. (2) **Row 4's bar is the written P2 and nothing more.** The item's minted title names S3 content ("Sources, Transmission, Launchers") and R4.S is already **at** its S2 target ("cost follows installed heating power with source basis"); S3-shaped cost splitting may be built where the engineering genuinely wants it but is not the bar, and **S2 must not be broken reaching for it**. (3) **`sustainment_ok`'s violated baseline is not fixed here** — 90.6 MW required against 50 installed is the disclosed W-form residual (`goal.md` § Invariants). This task changes what installed power costs and how it reaches the plasma; it does not tune the requirement.
- **Inputs:** `goal.md` § Invariants and § Answered when (a). Narrower constraints, three, each stated because it is a trap rather than a general rule: (i) **the deposition assumption must be stated, not defaulted** — `eta_pin = 0.5` is `eta_source_ecrh 0.50 x eta_couple 1.0` (`stellarator_plant.sysml:648`), and `eta_couple = 1.0` is a *default*, not a sourced coupling figure; the P2 anchor requires a **stated** deposition assumption, so it is either sourced or written down as a named judgment with its reasoning, never left silent; (ii) **parity with the oracle is only evidence if the oracle computes the chain itself** — `verify_stellaris.py:391-392` today mirrors the model's own `p_input / eta_pin` form, so re-using that form would make the check a tautology; (iii) **the restatement duty is not waivable** — the `p_input`/`p_ecrh` tie is declared in `manifest.json` and was swept by both `20260901-sustainment-fence` and `20260903-priced-levers`; if this task retires or restructures either key, or retires `eta_pin` as an entry point, those committed studies are **restated, never silently broken** (the MR-WI037-7 shape, ~30 fixture re-derivation sites at the last two retirements), and every changed expectation is re-derived from live evidence rather than patched to match.
- **Done when:** the heating chain exists as model objects a fresh grader can point at, the model regenerates and executes at the baseline, `tests/models` and `tests/study` are green, any changed committed-study expectation is restated with its derivation shown, and the goal-level reading says whether the P2 anchor's full text is now evidenced — **or** a bounded negative says with reasons which part of the anchor cannot be met from admissible sources.
- **Stop when:** a discovered prerequisite, a strategy blocker, an owner gate, or a declared limit.

### T-002 start — 2026-09-03

Task T-002 under § T-002 scope: activate WI-039 and carry it through the modelling PM. Native target: `work/BACKLOG.md` (status `backlog` → `active`) and `work/active/WI-039_heating-system-structure/` (`spec.md`, then `design.md`, `plan.md`, implementation), with model edits under `models/library/analyses/` and `models/designs/stellarator_09/` and the independent chain added to `exploration/stellarator_e2e/verify_stellaris.py`. Expected artifacts: the work-item directory with its four native stage records, the changed model files, the oracle's own computation of the chain, and green `tests/models` / `tests/study`.

### T-002 return — 2026-09-03

- **Outcome:** `COMPLETE`. WI-039 carried through spec → design → plan → implement; the heating chain exists as model objects, executes at the baseline, and is verified live.
- **Evidence:** `work/active/WI-039_heating-system-structure/` — `spec.md` (MR-WI039-1..11), `design.md` (decisions D1–D4, prototype PASS), `plan.md` (five phases plus the § MR-WI039-9 restatement). Model: `models/library/analyses/mfe_heating_chain.sysml` (new), `models/library/analyses/mfe_power_balance.sysml`, `models/designs/generic_mfe/mfe_plant.sysml`, `models/designs/stellarator_09/stellarator_plant.sysml`. Oracle: `exploration/stellarator_e2e/verify_stellaris.py`, `exploration/stellarator_e2e/studies/oracle_entry.py`. Commits `b45b4b01` (phases 1–4), `015f026e` (re-pin, census, snapshot, seam, fixtures), `6678968e` (batteries green, SV rows). SV-048 and SV-049 recorded `passing`.
- **Reading.** The Row-4 P2 anchor's full text is now pointable, and the round's own evidence says so rather than asserting it.
  - **"Wall-plug → coupled-power chain computed":** `calc def 'Heating Power Chain'` carries wall-plug power, a source stage, a coupling stage, source-output power, plasma-coupled power, the lumped efficiency and the total wall-plug draw as **seven named quantities**, executing as a module in the sealed package. `recirculating` no longer performs `p_input_in / eta_pin_in`; it consumes the chain's computed wall-plug total. That inline division was the grader's stated objection (`grading.md@fc80e5b2` G1: "a held ratio dividing one held constant inside another subsystem's sum derives nothing"), so it is the line that had to change, and it did.
  - **"with a stated deposition assumption":** `eta_couple_heat = 1.00` carries a written assumption in the model text — what it asserts (all gyrotron output reaches the plasma), that it is an idealisation rather than a measurement, its **direction of optimism**, and that a sourced coupling figure would enter at that one attribute. It is no longer a silent inherited default.
  - **"verified":** the oracle computes the chain from the design's stated equations rather than from the generated module, and its own `p_input / eta_pin` term is gone. **The perturbation test is the anchor's real evidence**, and it is the grader's own `why_not_next` test ("re-derives when efficiencies or delivered power change independently"): moving `eta_source_heat` alone, 0.50 → 0.45, moves heating capital 264,145,000 → 237,730,500, LCOE 307.087120428 → 306.798743084, and in the independently written oracle `q_eng` 3.078430435 → 3.074207573 with `rec_frac` and `p_net`. Model and oracle agree to twelve digits at both settings.
  - **The baseline did not move.** LCOE 307.08712042841586, nine anchors green, nine verdicts identical (`sustainment_ok` violated, `cond_strain_ok` satisfied), heating capital $264.145M exactly. Nothing was disclosed because nothing changed — the increment adds derivation without moving the physics point, the WI-037 shape.
  - **An unplanned improvement, found by a test rather than claimed:** `sustainment_ok` was **computed-vs-bound** and is now **computed-vs-computed**. Its installed-power operand was bound to the held design attribute `p_input`; it now reads the chain's coupled power. `tests/study/test_operand_bindings.py` caught the stale binding and `test_known_answers.py` caught the class change. Worth naming because `grading.md` G6 records held-vs-held as the failure mode this ladder is built to catch, and this fence moved the other way.
  - **R4.S was not risked.** `heating_cost__cost` is unchanged to the dollar and still follows installed heating power with its source basis. The cost driver is the chain's **source-output** power, not wall-plug — the per-MW rate is calibrated to gyrotron procurement, and binding wall-plug there would have silently doubled the account.
  - **Batteries:** `tests/models` 48 passed / 13 skipped; `tests/study` 356 passed / 1 skipped — the recorded baselines exactly. Validation levels measured against the pre-WI-039 tree in a scratch worktree rather than assumed: Level 2 unchanged at its 12 pre-existing warnings, Level 6 improved 233 → 230.
  - **What this does not yet establish:** the grade. § Answered when (a) is a **fresh non-author** re-grade from a pointer-only evidence map, and this task's job was to make the anchor pointable, not to score it.
- **Decision.** Trigger: the P2 anchor could be met by a cheap backward solve (hold the sourced coupled power, compute wall-plug from it) or by a forward chain from installed wall-plug power. Decision and reason: **wall-plug is the entry point** (design D1). The backward solve satisfies the anchor's letter but is the same held-ratio division with names attached — a fresh grader could read it as a relabel and score R4.P = 1 again, and the round would have spent its increment for nothing. With wall-plug forward, the quantities that *govern* — the coupled power entering `p_th`, and the coupled power `sustainment_ok` compares against the plasma's requirement — become derived. Cost accepted and paid: three entry points retired, two committed studies restated, the census re-derived. Tier: `execution detail` (the goal reserved no gates and re-ruled the delegation, ruling 5). Decided by: the round agent. What changed: the four model files, the two oracle files, `studies/manifest.json`, `tests/models/data/mfe_census.json`, six `tests/study/data/*.expected.json`, `tests/model_families.py`, two test modules, `stellarator.snapshot.json`; commits `b45b4b01`, `015f026e`, `6678968e`.
- **Decision.** Trigger: the `p_input`/`p_ecrh` tie declared in `studies/manifest.json` kept two model constants moving together because the model did not know they were one physical quantity. Decision and reason: **the tie is removed, not rewritten.** Both powers now descend from one wall-plug input, so the invariant holds structurally and the declaration has nothing left to maintain. A workaround that manually maintains an invariant the platform can declare is a defect to remove, not a mechanism to formalise. The `magnet__R0` / `R` tie stays: it is genuinely two attributes for one radius with no chain relating them. Tier: `execution detail`. Decided by: the round agent. What changed: `exploration/stellarator_e2e/studies/manifest.json`.
- **Decision.** Trigger: the plan named the 1costingFE handshake as the sharp edge of the entry-point retirement, with a `PREREQUISITE` return as the honest outcome if it could not be translated. Decision and reason: **it is not repaired, and it is not this item's break.** `handshake_1costingfe.py` raises `FileNotFoundError` on `generated/inputs/system_design.json`, a file the package stopped generating at the model migration — it fails there *before* reaching any heating key, and it fails identically on the pre-WI-039 tree at `860ce7d1`. `tests/study/test_no_retired_identifiers.py:36` already classifies it as a historical record rather than an executable path. Repairing only its heating keys would leave it broken while looking repaired; the keys it would need are written into the plan's restatement so whoever revives it need not re-derive them. `emit_1cfe_point.py` needs no change at all — `_effective_eta_pin` is 1costingFE's method on 1costingFE's own params. Tier: `execution detail`, with the pre-existing break surfaced. Decided by: the round agent. What changed: `none` in code; recorded in `work/active/WI-039_heating-system-structure/plan.md` § MR-WI039-9 restatement.
- **Decision.** Trigger: MR-WI039-9 and the WI-037 precedent both require the restatement to be written *before* the regeneration commit; it was written after. Decision and reason: **recorded as a deviation rather than quietly reordered.** The regeneration was reversible and was in fact re-run, and no committed study was touched in between, so nothing was lost — but the sequence was not followed and the record says so. Tier: `execution detail`. Decided by: the round agent. What changed: the deviation is the first paragraph of `plan.md` § MR-WI039-9 restatement.

### T-003 scope

- **Objective:** promote exactly one pin for this round — run the integration seam (`scripts/integrate.py`) over the WI-039 package and obtain a `CANDIDATE` naming the package, its manifest, its pin and both fingerprints, or a `BLOCKER` naming the one producer that stopped the sequence.
- **Why now:** T-002 returned `COMPLETE` and the round's study must run against a promoted pin — that is what makes two results comparable and what bounds the round (`GOAL_RUNBOOK.md` § Opening and closing a round: at most one promoted pin, at most one committed study).
- **Scope:** authorized — `scripts/integrate.py` with its ten gates, and any repair the seam's own refusal names as belonging to the modelling item. Excluded — relaxing, skipping, or arguing with a gate; the seam proves and does not perform, so a refusal at the regeneration gate means the modelling work is unfinished and goes back to WI-039, never around the seam.
- **Inputs:** `goal.md` § Invariants; `docs/integration_seam_operator_guide.md`. Narrower constraint: gate 5 judges the **repository**, not the package, so the working tree must be clean before the run — a dirty tree refuses for reasons that have nothing to do with the package.
- **Done when:** the seam returns `CANDIDATE` with the pin and both fingerprints recorded as evidence, or `BLOCKER` with the refusing gate and its producer's own output named.
- **Stop when:** a discovered prerequisite, a strategy blocker, or a declared limit.

### T-003 start — 2026-09-03

Task T-003 under § T-003 scope: run the integration seam over the WI-039 package. Native target: `scripts/integrate.py` against `--models-root exploration/stellarator_e2e/models`, `--package exploration/stellarator_e2e/pkg/stellarator_tea`, `--manifest exploration/stellarator_e2e/studies/manifest.json`, citing `--audited-work work/active/WI-039_heating-system-structure@<commit>`. Expected artifact: one integration return JSON deposited under `evidence/`, carrying either a `CANDIDATE` pin or a named refusing gate.

### T-003 return — 2026-09-03

- **Outcome:** `COMPLETE`. **`CANDIDATE`** — all ten gates passed on the first run, no retry.
- **Evidence:** `evidence/T-003_integration_return.json` (the return of record), with the seam's own outputs beside it: `T-003_baseline_result.json`, `T-003_verification_summary.json`, `T-003_preflight_results.json`, `T-003_package_identity.json`, `T-003_recaptured.snapshot.json`, `T-003_clean.json`. Audited work cited `work/active/WI-039_heating-system-structure@ba7098a2` (ADR-0006's citation form).
- **Reading — this round's promoted pin.** Package `stellarator_tea`; indicator-input pin **`2649e0ea2a987f3e56e9f659c4b4c48c9b670e6a0f8e47831c5fc253608455ad`**; semantic fingerprint **`48731d1570bb20cdeae45faa20545aae46aeda5982a4fbc2129d7784a66595ba`**; executable fingerprint **`b0c5f3eda9014fa1c84573c300c63f7fc04ecc1f8ea47047caa7775c010c6c78`**; teax at `744745f8`. That is the round's one pin, and the round's one committed study runs against it (`GOAL_RUNBOOK.md` § Opening and closing a round).
  - The gates worth naming: **regeneration rewrote no byte outside `handwritten/`** — the package really is the integrated form of the model, not a stale copy; **72 handwritten files byte-identical**; **the snapshot recaptures byte-identically and 199 entry points re-derive to the census as bound** — the retirement of three entry points and the minting of five is confirmed by the producer rather than by my arithmetic; **oracle parity holds and every verdict re-derives**.
  - `verification_summary.json`'s `not_independently_verified` list is **empty**, and the known tooling gap behind that field (a multi-field module output declared as an evidence-store channel yields a silent blank column, filed beside `20260903-priced-levers#4`) still stands — an empty list is not by itself proof of full coverage. Recorded so the next reader does not over-read it.
  - The seam reports one thing it did **not** run and that nothing else covers: `assert_read_set_covered` is out of reach at this gate and is filed. Carried forward, not resolved here.
- **Decision.** Trigger: the seam returned `CANDIDATE` on the first attempt. Decision and reason: the pin is promoted and the round proceeds to its study. No gate was relaxed, skipped, or argued with; the working tree was committed clean before the run precisely because gate 5 judges the repository rather than the package. Tier: `execution detail`. Decided by: the round agent, on the seam's own verdict. What changed: `evidence/T-003_*.json`.

### T-004 scope

- **Objective:** one committed study at the promoted pin answering: **with the heating chain live, what does the heating system do to LCOE and to the fence structure at the printed heating level and at the higher one — and does the chain's new lever, source efficiency, buy anything the old held `eta_pin` could not?**
- **Why now:** T-003 promoted the round's one pin, and the round closes on a valid study reading (`GOAL_RUNBOOK.md` § Opening and closing a round, trigger 1). The strategy named this study as narrow and heating-shaped; it is the last task before the round result.
- **Scope.** Authorized axes: `p_wallplug_heat` at the two levels the predecessor study used, restated (**100 and 220 MW wall-plug**, which is the printed 50 MW and the 110 MW coupled at `eta_pin_eff = 0.50`); `eta_source_heat`, **the axis this round created** — the old lumped `eta_pin` was a held constant and could not be swept meaningfully, and sweeping source efficiency now moves coupled power, heating capital and recirculating power together; `T_i0`, swept **past 18 keV** because the previous optimum sat at the window edge (`20260903-priced-levers#5`, `goal.md` § Invariants); `n_e0` and `I_coil` as the power-density and field levers already exercised at the predecessor pin.
- **Scope — declined, and traced with indicators rather than silently dropped.** Geometry (`R`, `a`): it belongs to the wall half and round 2. `R`'s effect on wall load at this pin is an **open measurement**, not a known (`goal.md` § Invariants, corrected at checkpoint C-001.r1 of `priced-levers`), and sweeping geometry here would confound the heating question with the wall question this round deliberately did not touch. `B_max`: still unpriced — WI-038 is its vehicle and WI-040 comes first, so sweeping it would manufacture feasible points that cost nothing.
- **Inputs:** `goal.md` § Invariants; the promoted pin from § T-003 return; `modeling_project/STUDY_POLICY.md` and the `run-study` runbook. **Two narrower constraints, both about honesty rather than mechanism.** (1) **Every fence-structure claim at the printed heating level is caveated that `wall_load_ok` compares a flat-wall average operand to the source's printed peak limit** — the round's own held-open assumption, now with T-001's evidence behind it: the model's circular-torus area is 15–30% smaller than a shaped stellarator first wall on the same wall-side radius, and the peaking factor is 1.5–2.1 against a *shaped-wall* average. So "the wall blocks here" is a statement about the fence as bound, not about the machine. (2) **`sustainment_ok`'s violated baseline is disclosed, never fitted** — and note it is now computed-vs-computed, so a heating sweep moves both sides.
- **Done when:** the record is committed with a valid reading — including an adverse or inconclusive one — its dispositions proposed for the checkpoint, and every discovery-log row the evidence touches routed.
- **Stop when:** a discovered prerequisite, a strategy blocker, an owner gate, or a declared limit. **Before committing the record:** `tests/study/test_records.py` must pass — it rejects any `<` character in `record.md` (write "below 1e-9") and requires `effective_executable_fingerprint.value` on every `snapshot.json` arm (`gotcha_study_record_contract`; the predecessor record failed both and needed an Addendum, which is a recorded defect and not a precedent).

### T-004 start — 2026-09-03

Task T-004 under § T-004 scope: run and commit one study at the promoted pin `2649e0ea2a98…`. Native target: `/run-study` against `exploration/stellarator_e2e/pkg/stellarator_tea`, writing a new record directory under `exploration/stellarator_e2e/studies/`. Expected artifacts: the record directory (`record.md`, `axes.json`, `indicators.json`, `snapshot.json`, `study.py`/`scan.py`, `results/`), a fresh-administrator synthesis, and proposed dispositions for checkpoint C-001.

### Checkpoint C-001.r1 — 2026-09-04

- **Reviewer:** fresh non-author `general-purpose` session, spawned from the deposited prompt `evidence/round1_C-001_checkpoint_prompt.md`; no execution context; not the administrator's session.
- **Reading reviewed:** `exploration/stellarator_e2e/studies/20260903-wall-and-heating/synthesis.md` (the fresh administrator's read, spawn prompt `evidence/T-004_administrator_prompt.md`) with the committed record's §§ 3, 4, 6, 8, 11, 12, 13, 15, 17 and its Addendum of 2026-09-04 (record dir `@31284e05`).
- **Dispositions reviewed:** `evidence/T-004_proposed_dispositions.md`, fourteen rows — the seven this study sighted (`20260903-wall-and-heating#1..#7`) and seven predecessor rows the round's evidence bears on (`20260903-priced-levers#1`, `#3`, `#4`, `#5`; `20260901-sustainment-fence#1`, `#3`, `#4`).
- **Verdict:** `REVISE`.
- **Revision:** 1 of 2. The reviewer independently recounted every crux number from `results/points.csv` joined to `results/oracle_operands.csv` and upheld all of them: 36 sustainment-alone / 10 wall-alone / 9 ceiling-alone of 240 at 100 MW with the minimum required coupled power 87.061 MW (`c0036`) and the 0.871 threshold; both transects to the digit (269.823 → 273.675 rising, heating capital exactly linear in efficiency; 317.234 → 255.970 falling, heating capital 697,342,800 on all eight); the 96-cell identity of fusion performance across efficiency; the shadow survivors 51 / 0 and the cheapest survivor 326.201; the 36 operating points identical across the WI-039 boundary (worst relative deviation 0, string-identical, no verdict differs); the five Addendum corrections all right and no disposition resting on a slipped value. Eight rows pass as written; row 2's class `model fix` stands; finding #7 stands as its own id with WI-040 as home. **Six rows carry statements the evidence does not support as written, and a permanent row cannot carry them:** rows 1 and 11's "at any efficiency" contradicts the sighting's own 0.871 threshold (the model opens the 12 cells behind the 36 at or above it; no source in the repository bounds buildability); row 2's "not new" claim rested on an uncommitted scratch-worktree run — a committed basis exists (at `860ce7d1` the recirculating sum divides held `p_input` by held `eta_pin` while the heating account prices the tied `p_ecrh`, so a held-power efficiency sweep is structurally the constant-coupled arm) and must be the citation; rows 3, 8 and 14 present 1.15–1.83× without T-001's condition — an unoptimised equidistant wall at a 0.30 m standoff — under which Lion's optimised walls give a net of 0.86–1.07 (below the range) and the model's 0.10 m standoff pushes above it, so "cannot invalidate unnoticed" over-promises and row 14's "fails the wall under even the mildest sourced correction" is false under an optimised wall. Two disclosure completions owed at the T-004 return: the scope extension (`arm-couple-132`) must appear in the trail, which record § 2 already claims; and the stale `arm-couple-110` / `110/eta_source` note in the digested `axes.json` must be disclosed as stale prose. **Author revised as § Revision r2 of the same document** (the r1 text stands above it; the pre-WI-039 model lines were re-read at `860ce7d1` before being cited), **appended a second record Addendum disclosing the stale `axes.json` note** (record dir `@` the commit of this entry), and resubmitted to the same session. `DISCOVERY_LOG.md` and the record's evidence are byte-unchanged.

### Checkpoint C-001.r2 — 2026-09-04

- **Reviewer:** the same fresh checkpoint session (no execution context), re-reviewing the resubmission.
- **Reading reviewed:** `20260903-wall-and-heating/synthesis.md` + record §§ 3–17 with both Addenda of 2026-09-04 (record dir `@577a6fa5`).
- **Dispositions reviewed:** `evidence/T-004_proposed_dispositions.md` including § Revision r2; the § Checkpoint C-001.r1 entry above.
- **Verdict:** `PASS`.
- **Revision:** 1 of 2 used. The reviewer confirmed at `577a6fa5` that nothing under `results/`, `snapshot.json`, `axes.json`, `study.py` or `DISCOVERY_LOG.md` differs from the record commit `2d11ca1b`, and that the r1 text stands unedited above § Revision r2. All seven items taken: row 1's replacement as specified; row 2's citations re-read line by line at `860ce7d1` (`mfe_power_balance.sysml:134-136` and `:119`; `mfe_plant.sysml:399,403,526`; `stellarator_plant.sysml:842-845` binding the rate and `p_ecrh`, the product `ecrh_per_mw * p_ecrh_in` at `mfe_account_costs.sysml:221-223`; the manifest tie at that tree) and found to say what the row claims; rows 3 and 8 carrying the unoptimised-wall / 0.30 m standoff condition with the optimised-wall net 0.86–1.07 checked; rows 11 and 14 as specified (crossover 4.05 / 3.886 = 1.042 checked); the second Addendum accurate; the scope extension owed at the T-004 return. Two non-blocking notes (the cost-product line; row 14's crossover wording) applied by the author before the rows were appended (`evidence/T-004_proposed_dispositions.md` § Notes applied). All fourteen per-row rulings PASS; the rows may be appended and the T-004 return and round result written. The reviewer's closing observation, carried forward for the round review and round 2's strategy: the log rows themselves now say that the 1.15–1.83× range is conditional on an unoptimised wall at a 0.30 m standoff and that the truth can fall outside it in either direction.

### T-004 return — 2026-09-04

- **Outcome:** `COMPLETE`. One committed study at the promoted pin with a valid reading, its dispositions checkpointed (C-001.r2 `PASS`), and every discovery-log row the evidence touched routed.
- **Evidence:** `exploration/stellarator_e2e/studies/20260903-wall-and-heating/` — the record at `2d11ca1b` (points executed 2026-09-03 at `cda4fd5c`; `record.md`, `snapshot.json` digest `547958ab…`, the eight `results/` artifacts, `axes.json`, `indicators.json`, `pre_wi039_indicators.json`, `study.py`, `scan.py`), the fresh administrator's `synthesis.md` and the first Addendum at `31284e05`, the second Addendum at `577a6fa5`. Spawn prompts deposited before spawning: `evidence/T-004_precritique_prompt.md` (critique at `evidence/T-004_precritique.md`, verdict MAJOR, nine findings accepted), `evidence/T-004_administrator_prompt.md`, `evidence/round1_C-001_checkpoint_prompt.md`. Proposed dispositions with § Revision r2 and the applied notes: `evidence/T-004_proposed_dispositions.md`. Fourteen class-bearing rows appended to `exploration/stellarator_e2e/studies/DISCOVERY_LOG.md` dated 2026-09-04. Battery before the record commit: `tests/study` 359 passed / 1 skipped (the record's three contract tests added); `tests/models` untouched by this task (48 / 13 at T-002).
- **Reading.** The study answers the round's question in two halves, and the second half is the wall's.
  - **At the printed heating level nothing is feasible, and heating efficiency cannot open it.** 0 of 240 at 100 MW wall-plug. Among the 36 points blocked by sustainment alone the minimum required coupled power is 87.061 MW, so the level opens only at `eta_source_heat` ≥ 0.871 at the optimistic `eta_couple` 1.00 (the scan said 0.92 on its coarser grid; the executed grid governs); the other 204 fail the wall or the conductor ceiling, which efficiency does not reach. No buildability claim is made beyond the pinned source's 0.50 (record § 17).
  - **Whether efficiency pays depends entirely on which quantity is held.** At fixed 220 MW wall-plug LCOE rises monotonically with efficiency (269.823 → 273.675 over 0.35 → 0.65; heating capital rises linearly, the wall-plug draw is constant) and efficiency buys only feasibility, at the sustainment crossing 0.5238; at fixed 132 MW coupled it falls 317.234 → 255.970 over 0.35 → 0.75 (61.264 $/MWh, heating capital constant to the dollar). Fusion performance is bit-identical across efficiency in every cell — the chain reaches `sustainment_ok` through the installed side only, the one structurally new reach in the comparison (record § 8).
  - **Every economic result at 220 MW is set by the wall fence as bound**, and its exposure to the honest fence is carried as data: the optimum 267.159 sits at 98.9% of the limit and reads 4.604 under the low end of the sourced range; 51 of 91 feasible search-arm points survive that end, 0 the high end; the cheapest survivor is 326.201. The range (1.15–1.83×) is for an unoptimised wall at a 0.30 m standoff; the truth can fall outside it in either direction (C-001.r2).
  - **Three claims were cut back after checking, before publication** (record § 14 Honesty): the structural-reach claim (the one new reach is efficiency → `sustainment_ok`; the wall-plug key's reach is not new), the "interior efficiency optimum" (a fence-edge artifact), and the newness of the constant-coupled economics (the pre-change model held the same structure — grounded on `860ce7d1`'s text at C-001.r2). Each correction made the claim smaller and sound.
  - **The WI-039 increment is neutral where the old and new grids coincide:** 36 operating points shared with `20260903-priced-levers` reproduce to every digit across the boundary (record § 12), so the two studies' 100/220 MW and 50/110 MW levels are exact restatements.
  - **What this does not establish:** the grade (T-005), and anything about the wall fence's honest form, area basis or standoff transfer (round 2).
- **Decision.** Trigger: the pre-execution critique's F1 showed the study as scoped (wall-plug "at the two levels the predecessor used") could only produce a forced answer to its own question — at fixed wall-plug the sign of efficiency's effect on LCOE is structural. Decision and reason: **the scope was extended by one arm, `arm-couple-132`**, which sweeps `p_wallplug_heat` continuously as 132 / `eta_source_heat` over efficiencies 0.35–0.75 at the search arm's cheapest feasible operating point, a third level set outside the scope as written; both axes it moves are authorized and only their joint pattern is new. Disclosed in `study.py`, record § 2 and § 11, and here, so the round review rules on it as an extension rather than discovering it. Tier: `execution detail`. Decided by: the round agent, on the critique's finding. What changed: `study.py` (the arm), record § 2/§ 11, `results/` (8 points).
- **Decision.** Trigger: the oracle window scan is placed at runbook step 7, after the step-4 critique. Decision and reason: **the scan was run before the critique** so the critique could read real numbers; no point ran through the sealed package. The critique accepted the ordering and credited it with three of its findings. Tier: `execution detail`. Decided by: the round agent. What changed: `scan.py` header, record § 11.
- **Decision.** Trigger: three defects in the executor's own design surfaced in execution before any commit — the four `heat__*` channels blank in all rows (a multi-field module; fourth sighting of the class), two arms silently sharing a point, and the constant-coupled arm anchored below its own fence. Decision and reason: **each was corrected and the study re-executed, and each is registered as a finding (`#4`, `#5`, `#6`) rather than repaired silently**; none is recorded as a goal-level retry — no return had been written, the first execution was never committed, and the task, inputs, scope and meaning were unchanged. The round review rules on the classification. Tier: `execution detail`. Decided by: the round agent. What changed: `study.py` (`CHANNELS`, `proposals()`, `COUPLE_TARGET`), `ANNEX.md` § Oracle, the three rows.
- **Decision.** Trigger: § Answered when (a) requires a fresh non-author re-grade, and the strategy revision lists it as the round's last step, while the handoff that opened this session placed it after the round result. Decision and reason: **the re-grade runs as T-005 inside round 1, after this checkpoint and before the round result**, on the `operating-point-closure` round-2 precedent (T-006 there) and because the strategy the round declared ends with it; the round closes on trigger 1 (a valid study reading) with the goal-level measurement in hand rather than owed. The fresh reviewer rules on fidelity either way. Tier: `execution detail`. Decided by: the round agent. What changed: `none` yet; T-005 follows.
- **Decision.** Trigger: fourteen touched rows owed dispositions. Decision and reason: **no work item is minted and nothing under `models/` changes** — the round is at its one-pin/one-study bound; rows `#1`, `#3` and `20260903-priced-levers#1` route to round 2 of this goal, whose subject the owner has settled and whose shape the fresh reviewer writes; `#2` and `20260901-sustainment-fence#4` carry into the T-005 re-grade; `#4`–`#6` and the two predecessor seam rows close as declared seams with practices recorded and the hardening candidate now carrying three repeats; `#7`, `20260903-priced-levers#3`, `#4`, `#5` and `20260901-sustainment-fence#1` are accounted for with nothing changed and say so. Tier: `execution detail` (the goal reserved no gates). Decided by: the round agent, passed at C-001.r2. What changed: the fourteen rows in `DISCOVERY_LOG.md`.

### T-005 scope

- **Objective:** the § Answered when (a) measurement — a **fresh non-author re-grade of rubric Row 4** (`R4.P` and `R4.S`) against `rubric.md@dc0f0b6d` from a pointer-only evidence map, producing `.project/active/demo-depth-rubric/grading-r4-regrade.md`, and the goal-level reading of whether (a) is met: `R4.P` = 2 with the anchor's full text evidenced, `R4.S` still at its S2 target, the battery green, the affected studies re-run clean.
- **Why now:** T-002 made the anchor pointable, T-003 pinned the package, T-004's reading is checkpointed, and the strategy revision names the re-grade as the round's last step. It is the goal-level measurement the heating half exists to produce, and the round result should carry it rather than owe it.
- **Scope:** authorized — spawning the fresh grader from the deposited prompt (`evidence/T-005_grader_prompt.md`) with the evidence map (`.project/active/demo-depth-rubric/evidence-map-r4-regrade.md`, pointers and no proposed scores); the grader creates exactly one file. Excluded — any edit to `rubric.md`, `grading.md` or `models/`; any communication with the grader beyond the prompt; any argument with the score (a disagreement is recorded per protocol § 5 as `author_reading` / `grader_score` / `resolution`, never averaged); grading against the unwritten P3 bar.
- **Inputs:** `goal.md` § Answered when (a) and § Invariants; the evidence map; the grader prompt. Narrower constraint: the grader receives pointers only and is told the prior cells are for the delta comparison, never as anchors; the round agent does not read the grader's draft or steer it.
- **Done when:** `grading-r4-regrade.md` exists with both cell records, any evidence-integrity findings, and § Delta vs prior; and the goal-level reading says whether (a) is met — or names the anchor as contested, which routes to the owner.
- **Stop when:** a fresh grader cannot be obtained; the grader finds the anchor itself contestable (`OWNER_GATE`); a declared limit.

### T-005 start — 2026-09-04

Task T-005 under § T-005 scope: spawn the fresh Row-4 grader (`general-purpose`, never a fork) from `evidence/T-005_grader_prompt.md`. Native target: `.project/active/demo-depth-rubric/grading-r4-regrade.md` (the grader's one file). Expected artifact: that file, with `R4.P` and `R4.S` cell records at `rubric.md@dc0f0b6d`, evidence-integrity findings, and § Delta vs prior; then the T-005 return with the goal-level reading of § Answered when (a).

### T-005 return — 2026-09-04

- **Outcome:** `COMPLETE`. The fresh non-author grader scored **`R4.P` = 2** (was 1) and **`R4.S` = 2** (re-scored against its own anchor at this pin, not carried forward) at `rubric.md@dc0f0b6d` — Row 4 at its written P2 / S2 target.
- **Evidence:** `.project/active/demo-depth-rubric/grading-r4-regrade.md` (the grader's one file; header confirms the rubric sha, model `103c41a5`, package pin `2649e0ea…` / semantic `48731d15…` / executable `b0c5f3ed…`); the pointer-only evidence map `.project/active/demo-depth-rubric/evidence-map-r4-regrade.md@9c0dc933`; the spawn prompt `evidence/T-005_grader_prompt.md`, deposited before the session ran. The grader re-executed the perturbation on both sides itself rather than reading it off the record, re-ran `tests/study` at HEAD (359 passed / 1 skipped / 0 failed), and read nothing under `knowledge/holdout/`.
- **Reading — § Answered when (a) is met as the fresh grader scores it.** Each conjunct of the anchor's full text is evidenced in the grader's own words: *computed* — wall-plug is the only settable heating power; delivered, coupled, lumped efficiency and wall-plug total are executed outputs, the inline `p_input_in / eta_pin_in` division G1 objected to is gone, and moving `eta_source_heat`, `eta_couple_heat` or `p_wallplug_heat` alone re-derives the dependents in both the generated path and the oracle, bit-identical; *stated* — `eta_couple_heat = 1.00` is a named attribute with the assumption written at the binding (what it asserts, its status, its optimistic direction, where a sourced figure enters), and the rubric says "with source basis" when it wants sourcing; *verified* — the oracle chain was written from the design's equations with its old division removed, and study verification agrees at 20 rows spanning efficiencies 0.40–0.60 with worst 5.55e-16. **`R4.S` was not broken:** the account still follows installed (delivered) heating power at the sourced rate, now from the chain's computed `heat.p_delivered`, with the baseline cost unchanged to the dollar. The battery is green (`tests/study` re-run by the grader; `tests/models` 48 / 13 at the pin, to be re-run by the round review). "Affected studies re-run clean": the two committed studies that swept the retired keys are **restated** with fixtures re-derived from live execution (`plan.md` § MR-WI039-9), and this round's own study executed clean at the new pin; no committed study is re-executed at its old pin, because prior studies stand at their own pins (`goal.md` § Invariants) — the reviewer rules whether that satisfies the clause in the form written.
- **Six evidence-integrity findings attached to the cells, never to the levels** (protocol § 6), each carried forward: **EI-1** — `eta_couple_heat = 1.00` is the optimistic end with no admissible loss source, so every feasibility and threshold number in the study is at the most generous coupling (the physics gap the chain still lacks); **EI-2** — the four `heat__*` channels never reach the store (the tooling gap, `20260903-wall-and-heating#4`), closed at module level by the grader's own direct execution, not by a committed artifact; **EI-3** — the WI-039 item record lags the work: `plan.md`'s checklists are all unticked and its status reads `draft`, and the perturbation numbers the evidence map pointed to there live in `VALIDATION_MATRIX.md` SV-049 and this trail instead; **EI-4** — the 1costingFE handshake break is pre-existing; **EI-5** — a dormant-chain concept that binds only the direct terms gets wall-plug equal to coupled power unless it also binds `eta_source`; the library doc should say the efficiency travels with the powers; **EI-6** — the record's prose slips, already filed at source.
- **A contestable anchor, routed to the owner and not scored.** The grader finds the P3 anchor's first conjunct ("required heating solved from the plasma's response") already true through Row 1 and `sustainment_ok` now computed-vs-computed, and reads the sustainment fence as a *power* limit rather than the anchor's "port, coupling, or geometry limit"; if the owner reads it the other way, `R4.P` is arguable at 3 under the written anchor. Row 4's written target stays P2 by owner ruling 2026-09-02; this belongs to the deliberate rubric v2 (concept OQ6, batched with the stale Rung C text).
- **Decision.** Trigger: the grader's contestable-anchor note. Decision and reason: **recorded for the owner's close packet and not acted on** — a rubric revision is owner-gated, and nothing in this goal is graded against an unwritten bar. Tier: `reserved gate` (the standing rubric-revision gate, not a goal-specific one). Decided by: the grader identified it; the round agent routes it. What changed: `none`.
- **Decision.** Trigger: EI-3, the unticked `plan.md` and its `draft` status. Decision and reason: **not edited by this session.** The plan is a native artifact cited by the T-002 return; editing it after that task returned would move a cited artifact outside its task, which is exactly what § When a cited artifact moves exists to catch. The lag is surfaced here for the round review and the owner (WI-039's close is owner-held); bringing the item record current is WI-039's close act, done through the modelling PM. Tier: `execution detail`. Decided by: the round agent. What changed: `none`.
- **Decision.** Trigger: EI-5, the dormant-path doc line. Decision and reason: **not applied** — a model-text edit outside any task of this round; recorded as a candidate for WI-039's close or an owner-directed quick model edit. Tier: `execution detail`. Decided by: the round agent. What changed: `none`.

## Round 1 result — 2026-09-04

Written by the resuming round-1 session (the session that opened the round and ran T-001 through the study's execution ended after committing the executed points; its handoff opened this session). Round 1 of 6.

- **Intent:** **met.** The strategy set out to close the heating half with a measured maturation delta and to bring the wall half's research in during the same round so the next round opens with sources in hand. Both happened: a fresh non-author re-grade at the frozen rubric reads **`R4.P` = 2 (was 1), `R4.S` = 2 — Row 4 at its written target**, which is the goal's § Answered when (a); T-001 registered three admissible sources and reframed the wall defect (the circular-torus area basis outranks the average-vs-peak form); and the one committed study says what the printed heating level's escape is not (heating-source efficiency) and what the wall fence as bound does to every economic number above it, with the correction's exposure carried as data.
- **Task sequence:** T-001 (wall-load research: two seam requests, three sources registered, one refusal recorded) `COMPLETE` · T-002 (WI-039 through spec → design → plan → implement; baseline unchanged in every number) `COMPLETE` · T-003 (integrate → `CANDIDATE` pin `2649e0ea…`, ten gates first run) `COMPLETE` · T-004 (committed study `20260903-wall-and-heating`; pre-execution critique MAJOR, nine findings accepted; administrator synthesis; C-001.r1 `REVISE` → C-001.r2 `PASS`; fourteen dispositions landed) `COMPLETE` · T-005 (fresh Row-4 re-grade) `COMPLETE`. No goal-level retry.
- **Last semantic outcome:** `COMPLETE` (T-005), with a valid, checkpointed study reading behind it (T-004: record + fresh administrator's synthesis + two executor Addenda; C-001.r2 `PASS`).
- **Stop reason:** derived — last outcome `COMPLETE` + a valid study reading in hand + the round's one pin and one study spent + no declared limit reached → the round closes on **trigger 1 (a valid study reading)**. Trigger 6 (goal answered) does **not** fire: § Answered when (b), the wall half, is open by design. Noted: changing the wall fence inside this round would have moved comparison meaning under the study (trigger 3), which is why the wall half is round 2's.
- **Evidence refs:** `work/active/WI-039_heating-system-structure/` (spec MR-WI039-1..11, design D1–D4 with prototype PASS, plan with § MR-WI039-9 restatement) at `6678968e`; `evidence/T-001_research_return.md` with the two request prompts, runs `knowledge/research/requests/runs/REQ-WALL-01/20260904T035638551421/` and `REQ-WALL-02/20260904T035641288429/`, three source directories under `knowledge/sources/`; `evidence/T-003_integration_return.json` and its five sibling JSON files; `exploration/stellarator_e2e/studies/20260903-wall-and-heating/` at `2d11ca1b` (record, snapshot, results, first sightings), `31284e05` (synthesis, first Addendum), `577a6fa5` (second Addendum); `evidence/T-004_precritique.md` + prompt, `evidence/T-004_administrator_prompt.md`, `evidence/T-004_proposed_dispositions.md` (r1, § Revision r2, § Notes applied), `evidence/round1_C-001_checkpoint_prompt.md`; the fourteen `DISCOVERY_LOG.md` rows dated 2026-09-04 at `103c41a5`; `.project/active/demo-depth-rubric/evidence-map-r4-regrade.md@9c0dc933` and `grading-r4-regrade.md` (this commit), `evidence/T-005_grader_prompt.md`. Battery at result: `tests/study` 359 passed / 1 skipped (record commit, and re-run by the grader at HEAD); `tests/models` 48 passed / 13 skipped at the pin (T-002), not re-run since — the review re-runs both.
- **Owner direction on record for the next round** `[OWNER 2026-09-04, verbatim]`: *"do you think we are ok with closing out Round 1, and then defining the broken wall-load fence as Round 2?"* — round 2's **subject** is the wall-load fence; its **shape** is the fresh reviewer's to write (`GOAL_RUNBOOK.md` § The fresh review), not this session's.
- **Learning delta (proposed):**
  - **L-001 — At the printed heating level the escape is not heating-source efficiency.** 0 of 240 at 100 MW wall-plug; the 36 points blocked by sustainment alone need at least 87.061 MW coupled, so the level opens only at `eta_source_heat` ≥ 0.871 at lossless coupling, and the other 204 fail the wall or the ceiling, which efficiency does not reach (record § 6, § 15 #1). Scope: this machine at R 12.7 / a 1.3, the executed windows, and the scan's 3080 candidates; no buildability claim. Implication: no later round spends a study on source efficiency at the printed level; the printed level's fate is the wall fence's.
  - **L-002 — Whether an efficiency lever pays depends on which quantity is held, and a study must ask in both parameterizations before it concludes.** At fixed wall-plug LCOE rises with efficiency (capital rises, the draw is constant); at fixed coupled power it falls 61.264 $/MWh over 0.35–0.75. The first design asked only the forced one. Implication: a study of any conversion-efficiency lever declares which of the chain's outputs is held (the cost driver or the physics driver) and runs both when they are different outputs.
  - **L-003 — The wall fence's defect is the area basis, and the sourced correction is conditional.** Every published stellarator average is over a shaped wall; the model's circular torus is 15–30% too small on the same wall-side radius, so neither honest form is honest without correcting the area (T-001 § 5). The 1.15–1.83× range is for an unoptimised wall at a 0.30 m standoff; optimised walls fall below it and the model's 0.10 m standoff pushes above it (C-001.r2). Implication: round 2 decides the form, the area basis and the standoff transfer together, discloses any baseline verdict change, and restates round 1's 220 MW result against whatever it chooses.
  - **L-004 — Check the previous pin before claiming an increment is new.** Three claims were cut back this round after checking (the structural reach, the interior optimum, the constant-coupled newness), and each corrected claim was smaller and sound; the last was grounded on committed text at `860ce7d1` only when the checkpoint required it. Implication: an increment's "new" claims are checked against the previous pin's indicator run and its model text, and a claim in a permanent row cites committed text, never a scratch run.
  - **L-005 (process) — A multi-field module's output declared as a store channel yields a silent blank column, now four times.** The ANNEX prose has not prevented a repeat (`20260901-sustainment-fence#3`, `20260903-priced-levers#4`, `20260903-wall-and-heating#4`). Implication: the declaration-time channel-shape guard proposed at `20260903-priced-levers#4` has the recorded repeats ADR-0003 asks for and should be minted owner-present.
  - **L-006 (process) — A study design that looks complete can hide two defects that only execution finds: a transect anchored below its own fence, and two arms sharing a point.** Both were caught by counting rows per arm after the first execution, not by any gate. Implication: a study definition asserts no two arms share a point, and reads a transect's held level off its anchor's own operands, before its points run.
  - **L-007 (process) — The re-grade belongs inside the round as its last task.** Running it as T-005 after the checkpoint let this result carry the goal-level measurement instead of owing it, on the `operating-point-closure` round-2 precedent; the handoff that opened this session had it after the review. Implication: a goal whose § Answered when is a re-grade puts the re-grade in the strategy as the round's last task, after the study reading is checkpointed.
- **Finding dispositions:** all fourteen checkpointed dispositions (C-001.r2 `PASS`) landed as class-bearing rows in `DISCOVERY_LOG.md` dated 2026-09-04 (`103c41a5`) — `20260903-wall-and-heating#1` and `#3` `model fix` → round 2 of this goal (the wall half), not minted; `#2` `model fix` → the Row-4 re-grade evidence and this result, no model change; `#4` `declared seam`, closed, the hardening candidate now with three repeats; `#5`, `#6` `declared seam`, practices recorded; `#7` `model fix`, standing at WI-040; `20260903-priced-levers#1` `model fix` → round 2 with the defect sharpened to the area basis; `#4` `declared seam` unchanged; `#5` `declared seam` applied, no recurrence; `20260901-sustainment-fence#4` `model fix` discharged at the model level by WI-039, grade now in hand (T-005), close owner-held; `#3` `declared seam` unchanged; `20260903-priced-levers#3` `research` unchanged, Pierro fetch still queued; `20260901-sustainment-fence#1` `model fix` unchanged, WI-038 waits on the honest fence, the 262 candidate re-measured at this pin with its wall exposure stated. No touched row returns unrouted.
- **Surfaced for the owner, not resolved (the close packet the review will complete):** (1) WI-039 close — the work is done and graded; the item record lags (EI-3: unticked plan, `draft` status), to be brought current at close through the modelling PM; (2) the junk registry entry `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and/` (a bot-check page; `source_registry.py` has no `unregister`) — T-001 § 7; (3) the grader's contestable P3 anchor, for rubric v2 (OQ6); (4) the hardening candidate (L-005) for minting; (5) the EI-5 library doc line; (6) merge and push of `feat/demo-maturation` (all of this round's commits are local); (7) the Pierro et al. 2019 operator fetch, still queued.

### Round 1 review — 2026-09-04

- **Reviewer:** fresh non-author `general-purpose` session, spawned from the deposited prompt (`evidence/round1_review_prompt.md`); no context inherited; not the executing session, not the resuming session, not the administrator, not the checkpoint session, not the grader. Full review deposited verbatim at `evidence/round1_review.md` — the review of record; this entry summarizes.
- **Verdict:** `FINDINGS` — the round stays closed and every task's evidence stands: every cited ref resolves; strategy fidelity holds (T-001 → T-005 in the declared order, one pin, one study, the wall fence untouched); every scope inside or disclosed; zero goal-level retries, correctly — the three in-execution corrections were native-stage corrections before any return or commit, two of which changed the definition and could not have been retries; the checkpoint sequence clean; all fourteen dispositions landed and moved or said plainly they did not; the two record Addenda are the study runbook's own step-15 mechanism inside T-004's window, not external mutation; **battery re-run by the reviewer at `d9930dfc`: `tests/models` 48 passed / 13 skipped, `tests/study` 359 passed / 1 skipped**; **§ Answered when (a) met** — R4.P = 2 with each conjunct argued on the anchor's letter, R4.S re-scored at its S2 target, "affected studies re-run clean" satisfied on the MR-WI039-9 restatement reading (the `operating-point-closure` precedent, with stronger evidence here: the ×2 mapping is exact and 36 shared points reproduce to every digit), to be confirmed by the owner at close. The findings are corrections to the proposed learning delta and to two readings the next strategy must not inherit.
- **Rulings on the two questions put to it:** (i) `arm-couple-132` is a **legitimate extension, not drift** — critique-triggered, authorised axes only, no pin, question or comparison meaning changed, disclosed before the review; carry-forward: a critique-driven change past the written scope gets a dated scope amendment *before* points run. (ii) **The re-grade inside round 1 (T-005) is the right placement** — a task cannot run outside an open round and the review never resumes a closed one; the handoff that placed it after the review was a coding-PM status note, not a goal surface.
- **Findings:** **F1** — L-001 over-claims: with the wall verdict removed, the 100 MW level opens at `eta_source` ≈ 0.24 (minimum required 23.8 MW among points passing every other fence), so the 0.871 threshold is a statement about the wall fence as bound, not about heating; the learning carries that condition. **F2 (material, and it reshapes round 2)** — the machine's own source, `knowledge/concept_research/09-qi-stellarator-hts/iter-01/sources/stellaris-design-details.md`, prints a 3D-neutronics peak of 4.05 MW/m² on its own CAD first wall (Table 2 image; Fig. 33), an average of 2.87 (line 747), a plasma surface area of 940 m² (line 231) and a first-wall minimum standoff of **100 mm** (line 1295): 2700 × 0.8 / 2.87 = 753 m² is *below* the 940 m² LCFS, so the 2.87 is not a shaped-first-wall average and is numerically a circular torus at R 12.7 m, r 1.5 m (752.1 m²); the model's 0.10 m standoff is therefore the source's own, not a model error; the model's doc comment calling 2.87 "the source's shaped-wall average" (`stellarator_plant.sysml:1139-1140`, WI-022 era) is unsourced; and the source's own peak gives a net multiplier of 4.05 / (2160 / 701.926) = 1.316 on the model's average, inside round 1's shadow range — under it the baseline reads 4.088 against 4.05, violated by the model's ~1% fusion-power excess over the source's ~2700 MW, 26 of 91 feasible 220 MW points survive (cheapest 371.005, `c0584`), and 9 points at 100 MW become sustainment-alone (threshold 0.92). For scale, not claims; round 2 establishes the averaging basis first. L-003 is corrected accordingly. **F3** — the Helios/HELIAS homograph the round nominated (T-001 return, decision 3) was dropped from the delta; added as L-008. **F4** — the newest `20260901-sustainment-fence#4` row says the grade is pending; it landed the same day. **EI-3 upheld:** the round agent was right not to edit `plan.md` after T-002 returned; bringing the item record current is WI-039's close act.
- **Learning delta:** L-001 **accepted, corrected** (the condition "under the wall fence as bound"; implication: a source-efficiency study at the printed level is worth running only if round 2's honest fence loosens the wall verdict); L-002 **accepted**; L-003 **accepted, corrected** (the external factors are on shaped-wall averages, but the machine's own source's 2.87 is not a shaped-first-wall average — establish that basis before choosing the form; the 1.15–1.83 range is conditional; the source-anchored net is 1.316); L-004 **accepted**; L-005 **accepted**; L-006 **accepted**; L-007 **accepted, corrected** to the claim and its reason without the handoff narrative (a candidate runbook clarification for the owner); **L-008 added** (the homograph). Appended to `learnings.md` with this entry in the reviewer's corrected forms.
- **Constraints carried forward:** ten, listed in `evidence/round1_review.md` § Constraints carried forward — the heating chain untouched in round 2 with `eta_couple_heat` at its disclosed 1.00; any baseline verdict change disclosed, never tuned; the CAS72 lifetime operand decided explicitly and its baseline move declared as a comparison-meaning change, the restatement written before the regeneration commit; round 1's 220 MW result restated under the chosen fence; the sweep rules (T interior or fence-caught; `a` bracketing the committed a ≥ 1.70 violation; `R` with its tie; `I_coil` under the ceiling; `j_wp` held; `B_max` declined); multi-field outputs exported oracle-side; the no-shared-point and anchor-operand assertions, with a first execution's `points.csv` kept beside the record when a re-execution follows a design defect; scope amendments before points run; the record contract; fresh gates spawned non-fork with prompts deposited first.
- **Close packet for the owner** (nine items, `evidence/round1_review.md` § Close packet): WI-039 close through the modelling PM with the plan's phases ticked, the `draft`/`active` status reconciled, a trail amendment redirecting citations, and a close row under `20260901-sustainment-fence#4`; confirmation of the "affected studies re-run clean" restatement reading; the junk registry entry (no `unregister` operation); the grader's contestable P3 anchor for rubric v2 (OQ6); minting the channel-shape guard (L-005); the EI-5 library doc line; merge and push of `feat/demo-maturation`; the Pierro et al. 2019 fetch; the unsourced doc comment at `stellarator_plant.sysml:1139-1140`, which round 2's model task corrects.
- **Acts taken on this review by the round agent, same day:** the review deposited verbatim; the eight learnings appended in the reviewer's corrected forms; **a row appended under `20260901-sustainment-fence#4`** recording that the grade landed (F4 — the review is the post-execution audit and its finding is landed by append, never by editing the stale row); **F2 verified before landing it** — the round agent re-read the four source lines (231, 241, 747, 1295) and reproduced the arithmetic (752.6 m² implied, 752.1 m² circular torus at r 1.5, net 1.3161, baseline 4.087), and **noted one more printed figure for round 2 to reconcile, as the round agent's observation and not the reviewer's: the same source table that prints the 2.87 average also prints "Peak neutron wall power 4.95" (line 748) beside Table 2's "Peak net/on wall load 4.05" (line 241)** — two printed peaks, one basis question; round 2 opened below with the reviewer's strategy revision landed verbatim. Nothing edited outside the goal directory, `learnings.md` and the discovery log.

## Round 2 — source-anchored-wall-fence

### Strategy revision — 2026-09-04

`[AGENT — the round-1 reviewer, per GOAL_RUNBOOK.md § The fresh review; landed verbatim by the round-1 agent. Subject settled by the owner: "defining the broken wall-load fence as Round 2" [OWNER 2026-09-04, verbatim]; the shape below is the reviewer's, challenged by re-deriving against its recorded reasoning in evidence/round1_review.md.]`

- **Approach:** make the wall-load fence compare like with like by anchoring it to the machine's own source first, and use the three registered external sources as plausibility bounds rather than as the transfer. The Stellaris paper prints, for this design at ~2700 MW, a 3D-neutronics peak of 4.05 MW/m² on its own first wall at its own ≥100 mm standoff, an "average neutron wall power" of 2.87, and a plasma surface area of 940 m². The round's first act is to establish the 2.87's averaging basis from the source text (§ 2.8, Fig. 33, the method it cites) — the arithmetic says it is not a shaped-first-wall average and is numerically a circular torus at R 12.7 m, r 1.5 m. Then build the fence as a computed peak against the printed peak limit: the model's forward-computed average, put on the source's stated basis by a named reference-radius correction, times a named, sourced net peaking factor, with the factor's definition, reference radius and standoff written into the model text. If the basis cannot be established, the form is T-001's shaped-wall route (Lion's area factor on the wall-side radius and peaking on the shaped average, with the 0.30 → 0.10 m transfer argued from admissible text); if that transfer cannot be argued either, the round returns the bounded negative `goal.md` § Limits names, with its three options, and the owner rules. Whatever lands, the baseline verdict change is disclosed and explained, never tuned — expect the baseline to read ≈ 4.09 against 4.05, violated by the model's ~1% fusion-power excess over the source's ~2700 MW, a first-class result on the `sustainment_ok` precedent. Then one promoted pin, then the one committed study § Answered when (b)(ii) names.
- **Assumptions:** (1) the source's own peak is admissible and is the right limit for this machine — it is the design's 3D value on its own wall, and the model already carries it; (2) the model's circular-torus average can be put on the source's basis by a stated reference-radius correction alone, without a shaped-wall area, once that basis is established — if the basis turns out to be a shaped wall, this assumption fails and the T-001 route applies; (3) the fence change is a verdict-class change (operand form) plus, at most, the CAS72 lifetime operand, so committed studies are restated at the MR-WI037-7 shape (~30 fixture sites) and their `wall_load_ok` verdicts are re-readable through a stated multiplier; (4) the lifetime operand (peak or average) is decided explicitly with its convention stated; (5) `R` reaches wall load through the ISS04 coupling at this pin and its effect is measurable; (6) heating stays as WI-039 left it.
- **Abandonment conditions:** the source's averaging basis cannot be established and the standoff transfer cannot be argued from admissible text (bounded negative, owner rules); the honest form would change `p_fus`, the geometry chain, or any quantity outside the wall chain and the lifetime operand (comparison meaning moves beyond a restatement — surface, park the dependent conclusions, owner); the fence cannot be expressed without a defaulted number; a fresh gate session cannot be obtained.
- **Intended model increment:** one work item through the modelling PM, minted at the round's first selection act under the MFE Cost Modeling epic — a wall-fence calc that computes the peak operand from the model's average through named quantities (reference-area correction, peaking factor, their basis and standoff as documented attributes); `wall_load_ok` comparing that peak to the printed 4.05; the average retained and reported against the source's 2.87 as a comparand, not a fence; the CAS72 lifetime operand decided and stated; the oracle computing the same chain from the design's equations; the committed-study restatement written before the regeneration commit; SV rows for the baseline identity (reproduce the source's 4.05 at its ~2700 MW) and the disclosed verdict change; the doc comment at `stellarator_plant.sysml:1139-1140` corrected. Nothing at the heating chain changes.
- **Intended study question:** at the promoted pin with the honest fence, does a feasible region exist at the printed 100 MW wall-plug (50 MW coupled at 0.50, lossless coupling) over the wall's levers `R`, `a`, `n_e0`, `T_i0` with `I_coil` — and at 220 MW, where does round 1's 267.159 optimum go under the chosen fence, and what does the machine pay to get under the wall through wall load → in-vessel lifetime → CAS72? Windows fixed by an oracle scan with `a` bracketing the committed a ≥ 1.70 violation and `T` interior or fence-caught; `j_wp` held; `B_max` declined; source efficiency held at 0.50 (round 1 answered it); one shadow column at the alternative admissible form, if the round chose between two, so the choice's sensitivity is data.

No future task list. Tasks are chosen from evidence as each returns.

### Owner rulings on the round-1 close packet — 2026-09-04

Owner present in session; the review's close packet put to the owner one ruling at a time through /_my_ask_me, each `[OWNER 2026-09-04]`:

1. **The heating half is answered as written.** "Affected studies re-run clean" is satisfied on the restatement reading (fixtures re-derived from live execution; 36 shared operating points identical across the boundary), as at the `operating-point-closure` close. Owner: *"Yes."* § Answered when (a) is met; round 1's result stands.
2. **WI-039 close and the push are delegated to the round agent for this case**; merge stays owner-held. Owner: *"yes, close and push."* Done below.
3. **The junk registry entry routes to a coding-PM tooling item**: an `unregister` operation for `scripts/source_registry.py` plus a guard at registration against a tiny or bot-wall extraction. Owner: *"mint the tooling item."* Entered in `.project/backlog/BACKLOG.md` § Flagged.
4. **The silent blank-column defect is a suspected code bug, not a modeling item**, and is documented in full in `.project/backlog/BACKLOG.md`. Owner: *"this should actually be documented fully in the `.project/backlog/BACKLOG.md` -- this is more likely a code bug in `fusion-tea` or `sysml-codegen` than a modeling work item."* Entered there with both halves named (fusion-tea's exporters read a missing channel silently where `study_route.csv_rows` fails closed; teax's evidence projection keeps only scalar outputs, so a multi-field module's outputs never reach the store).

Dropped from the packet as decisions, with the owner's assent by silence on the list put to them: the grader's contestable P3 anchor (parks for rubric v2 whatever is ruled), the EI-5 library doc line and the unsourced wall comment at `stellarator_plant.sysml:1139-1140` (both fixed inside round 2's model increment, which re-pins the package anyway), and the Pierro 2019 fetch (owner-only, nothing waits on it).

### Amendment 2026-09-04 — WI-039 archived; citations redirect

WI-039 closed and archived to `work/completed/20260904_WI-039_heating-system-structure/` (`uv run agentic-mbse pm close-item WI-039`; `tests/models` re-run green after the move, 48 passed / 13 skipped). Before the move the plan's checklists were ticked with a dated implementation record pointing at the commits, the trail's T-002 return, SV-048/049, the integration return and the re-grade; one item is left unticked and annotated (the 1costingFE handshake, not done as written, per § T-002 return decision 3). Every citation of `work/active/WI-039_heating-system-structure/` in this trail, in `evidence/`, in the R4 evidence map and grade, in the discovery log, and in the two doc comments at `stellarator_plant.sysml:700,703` (and their twin) now resolves at the archived path; per the magnet-closure and operating-point-closure precedents this amendment redirects them and no entry is edited. The two model doc comments are corrected when round 2's model increment regenerates the package, since a doc-text change re-pins it.

### Owner ruling — 2026-09-04 — what "pushes back" means in the rubric

Put to the owner in session after the round-1 close packet: the fresh Row-4 grader's contestable-anchor note (`.project/active/demo-depth-rubric/grading-r4-regrade.md` § Evidence-integrity findings) asked whether the rubric's P3 phrase "a port, coupling, or geometry limit pushes back" could be read to include the sustainment fence, a bare inequality on power, in which case `R4.P` would be arguable at 3. The owner's reading `[OWNER 2026-09-04, verbatim]`: *"not if it is just a direct constraint. I would expect some curve or calculation for the push-back to be modeled."* So a direct constraint — an inequality comparing a computed quantity to a limit — is not "push-back" in the rubric's sense; push-back is a modeled response, a curve or calculation through which the limit changes what the design does. The grader's reading and score stand (`R4.P` = 2, the written target). Recorded here and as the § 5 resolution appended to the grading file; it governs how "pushes back" is read across the rubric's rows until rubric v2 writes it into the anchor text (concept open question 6). It also sharpens round 2's yardstick vocabulary for the wall fence: a wall-load inequality is a fence, and the consequence chain wall load → lifetime → replacement cost is the push-back.

### T-001 scope (round 2)

- **Objective:** establish from the source text what the Stellaris paper's printed 2.87 MW/m² "average neutron wall power" is an average over — which power over which surface — which of the two printed peaks (4.05, Table 2 "peak neutron wall load"; 4.95, Table 5 as extracted) is the 3D-neutronics peak on the first wall and what the other is, and what the source itself says sets in-vessel lifetime (a peak or an average), so that round 2's model task can put the model's forward-computed average on the source's stated basis with every named quantity sourced. A bounded negative naming exactly which text is missing is a full answer.
- **Why now:** it is the strategy's declared first act (§ Strategy revision — 2026-09-04: "the round's first act is to establish the 2.87's averaging basis from the source text"), and the reviewer's F2 rests on arithmetic, not text. Sharpened by this session's own arithmetic before any work: two readings reproduce 2.87 to four figures — neutron power 2160 MW over a circular torus at R 12.7 m, r 1.5 m (752.06 m²), and fusion power 2700 MW over the printed 940 m² plasma surface (0.8 × 940 = 752.0 m²) — so the number cannot decide the basis and only the text can. Strategy assumption (2) turns on which it is; the CAS72 lifetime operand (constraint carried forward) turns on the third question.
- **Scope:** authorized — read-only reads of the registered Stellaris extraction and its page images (`knowledge/concept_research/09-qi-stellarator-hts/iter-01/sources/stellaris-design-details.md`, `…/stellaris-design-details/images/`), the iter-02 re-extraction `stellaris-paper-details.md`, the neutron-wall-load method the paper cites as [240] (Lion, Warmer & Wang 2022, registered in round 1 at `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and_2/`), the other two round-1 registered sources, the pinned 1costingFE geometry, the model text and the pin's baseline result; arithmetic in the scratchpad; one evidence file, `evidence/round2_T-001_source_basis.md`. Excluded — any edit under `models/`, `exploration/` or `knowledge/`; any registration (a source outside the registered set is a `PREREQUISITE` return naming the research seam, not a quiet fetch); any modelling-PM stage; discovery-log writes; deciding the fence's final form (the model task's, on this evidence); and every artifact barred by `knowledge/holdout/aries-cs/PROTOCOL.md` §3, named here because the admissible `iter-02/sources/` directory holds three barred entries beside the admissible re-extraction.
- **Inputs:** `goal.md` (§ Invariants: a baseline verdict change is disclosed, never tuned; a missing input is surfaced with options, never defaulted; § Limits named risk; § Reserved gates: the fence's form is the round agent's on sourced evidence). Trail § Round 2 strategy revision; `evidence/round1_review.md` grounds 2. Narrower constraint, stated because the hazard is recorded: the extraction's Table 5 is known-lossy — the model's own doc comment (`stellarator_plant.sysml:1140`) records a row with no counterpart in the printed image, and the extracted Table 5 carries rows that contradict Table 2 — so **no printed figure is taken from the text alone; each is confirmed against its page image, and a figure no image confirms is reported as unconfirmed, never as fact**.
- **Done when:** for each of the three questions the reading states what the source establishes, with line and image cites — the 2.87's power and surface; which peak is the 3D value and what the other is; the lifetime driver — and says whether strategy assumption (2) survives (a reference-radius correction alone), which candidate basis the fence's named quantities must carry, or that the text does not decide and what the model text must then say.
- **Stop when:** a discovered prerequisite (a printed figure the extraction garbled that no local image covers), a strategy blocker, an owner gate, or a declared limit is reached.

### T-001 start — 2026-09-04 (round 2)

Task T-001 under § T-001 scope (round 2): read the Stellaris extraction's Table 2, Table 5 and Table 6 page images, § 2.8 with Fig. 33, § 2.7's first-wall method and the lifetime-estimation passage, and the registered Lion 2022 method paper for its definition of average and peak neutron wall load; reproduce every printed figure arithmetically; deposit the reading at `evidence/round2_T-001_source_basis.md`. Native target: none mutated — every read is read-only. Expected artifacts: the evidence file and the T-001 return.

### T-001 return — 2026-09-04 (round 2)

- **Outcome:** `COMPLETE`. The three questions are answered from the printed pages, each figure confirmed against its image; no new source was needed and no artifact outside the goal directory changed.
- **Evidence:** `evidence/round2_T-001_source_basis.md` (the reading of record, with the table of image-confirmed figures in § 1). Source pages: `knowledge/concept_research/09-qi-stellarator-hts/iter-01/sources/stellaris-design-details/images/page_001_table_0.png`, `page_002_table_0.png`, `page_009_table_0.png`, `page_020_table_0.png`, `…pdf-17-1.png` (Fig. 33), `…pdf-17-2.png` (Fig. 34), `…pdf-19-3.png` (Fig. 38); text lines 1295, 1304–1306, 1588–1607, 1722–1727, 1755–1766, 1811–1813; `stellaris-design-details/decisions.json`. Method paper: `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and_2/output.md` lines 88, 92, 298–302, 366. Model: `evidence/T-003_baseline_result.json`; `stellarator_plant.sysml:1126-1152`; `mfe_account_costs.sysml:796-803`.
- **Reading.**
  - **The 2.87's basis is unstated in the source, and it is not a wall average.** By the source's own cited definition (neutron power per first-wall area, [240] line 92) the average over the Fig. 33 wall is at most neutron power over the printed 940 m² plasma surface, 2.30 MW/m²; the source's own damage map (Fig. 38: peak 3.9, mean 2.2 DPA/GWf/yr) puts the wall's average near 2.28 by the same ratio. A 2.87 average needs a 753 m² surface, smaller than the plasma. The only expression of printed figures that reproduces it is fusion power over the plasma surface, 2700 / 940 = 2.872. The round-1 reviewer's "circular torus at R 12.7 m, r 1.5 m" reproduces the number too, but **1.5 m is printed nowhere** — the extraction rewrote Table 2's minor radius (image: 1.3 m), and no printed standoff (0.10 m minimum, line 1295; 16.6 cm averaged, Fig. 34) reaches 1.5 m; the coincidence is 0.8 × 940 = 752.0 against 4π²·12.7·1.5 = 752.06. So there is no reference radius to correct to. The 2.87 is a comparand with its basis marked unstated, never a fence, and 4.05 / 2.87 = 1.41 is not a peaking factor.
  - **One printed peak, 4.05.** Table 2's headline "Peak neutron wall load" (image-confirmed); Fig. 33's scale runs 0–4 MW/m² with the maxima at its top. The Table 5 image has **no peak row**; the extracted "4.95" (line 748) and Table 6's extracted "~1.5 W/m²" row are artifacts of pages the extraction rewrote (`decisions.json` pages 9 and 20, `claude_replace`), which the WI-022 doc comment had already suspected for the 4.95. The 4.05 is a 3D value on the design's own CAD first wall (the one wall used for divertor, neutronics and thermal work, lines 1304–1306), at 2700 MW, by [240]'s deterministic method (line 1606–1607) — not an OpenMC tally, correcting the review's attribution.
  - **Lifetime is set by the peak.** Table 6 derives the first-wall lifetime (~4–6 FPY) from the peak first-wall DPA (10.7 DPA/FPY); the text says about 4 FPY at 42.8 DPA (lines 1811–1813); [240] line 88 states the rule. The model's fluence chain at the source's peak reproduces the band (18 / 4.05 = 4.44 FPY); at the current average it reads 5.80, at the band's top. The declared baseline move under a peak operand of 4.087: core life 5.797 → 4.404 FPY, replacements 4 → 5, CAS72 95.898 → 131.464 M$ (+37%), reproduced from the calc's own equations at the pinned constants.
  - **Strategy assumption (2) fails in letter, and nothing depends on it.** The fence needs neither the 2.87 nor its basis: the multiplier that carries the model's own circular-torus average to the source's peak at the source's power and geometry is N = 4.05 × 701.926 / 2160 = 1.3161, every factor printed or already in the model; it is an identity at the design point (SV: reproduce 4.05 at 2700 MW) and extrapolates through the model's own `wall_area` under one stated constancy assumption. Baseline: 3.10527 × 1.3161 = 4.087 against 4.05, violated by the model's 0.94% fusion-power excess — the expected, disclosed, never-tuned result. The decomposition is a cross-check only: the source's damage peaking 1.77 implies a wall area of ~945 m² (the printed 940 m² plasma surface plus a standoff); the external 1.5–2.1 band implies 800–1120 m². Neither the shaped-wall fallback nor the bounded negative is triggered. Two sourced forms are left for the model task to choose between (`evidence/round2_T-001_source_basis.md` § 5): the decomposition carried explicitly, or N alone with the decomposition and band recorded as bounds.
  - **The model text.** The WI-022 cross-check comment ("the source's shaped-wall average", `stellarator_plant.sysml:1137-1140`) is wrong in its claim, not merely unsourced; the corrected wording is written in the reading § 5. The model's 0.10 m standoff is the source's *minimum*, not its average (16.6 cm); the doc text says which.
- **Decision.** Trigger: the strategy's first act assumed a basis exists to establish; the printed pages show none, and the number that suggested one (r = 1.5 m) was an extraction artifact. Decision and reason: **the increment anchors to the source's peak directly, by an identity at the source's design point, and reports the 2.87 as a comparand of unstated basis** — the only construction whose every factor is printed or already in the model; assumption (2) is recorded as failed in letter, with the approach (source-anchored, computed peak against the printed peak, external sources as bounds) intact. Tier: `execution detail` (§ Reserved gates ruling: the form is the round agent's on sourced evidence). Decided by: the round agent, re-deriving the reviewer's `[AGENT]` strategy against its recorded reasoning. What changed: `evidence/round2_T-001_source_basis.md`; the model task's scope will carry it; no model, study or knowledge artifact.
- **Decision.** Trigger: the constraint carried forward that the CAS72 lifetime operand be decided explicitly. Decision and reason: **the operand is the peak**, because the source and its cited method both set lifetime by the peak load and the model's fluence chain at the peak reproduces the source's own lifetime band. Tier: `execution detail`, but it moves comparison meaning (assumption (3)): the committed studies' `cas72` expectations are restated, with the derivation shown, **before** the regeneration commit — the round-1 T-002 deviation is not repeated. Decided by: the round agent. What changed: none yet; the model task carries it with the declared move (95.898 → 131.464 M$ at the baseline).
- **Decision.** Trigger: extraction artifacts found on four rewritten pages, two of which produced figures cited in round 1 (the 4.95, the r = 1.5 m). Decision and reason: **recorded in the reading § 6 and proposed for the round result's learning delta as a correction to L-003's evidence line; no seam repair and no log row** — the extraction's own `decisions.json` already marks the rewritten pages, the README already says images are ground truth, and no study sighted the defect. Tier: `execution detail`. Decided by: the round agent. What changed: none outside the evidence file.
- **Next task, chosen from this evidence:** the model increment — mint the wall-fence work item under the MFE Cost Modeling epic and carry it through the modelling PM, on the reading above.

### T-002 scope (round 2)

- **Objective:** make the wall-load fence compare like with like — carry one work item (minted here, under the MFE Cost Modeling epic) through the modelling PM so that `wall_load_ok` compares a **computed peak** operand against the printed 4.05: the model's forward-computed average times a named, fully sourced calibration that reproduces the source's own 3D peak at the source's design point (`evidence/round2_T-001_source_basis.md` § 5), with the average retained and reported against the 2.87 as a comparand of unstated basis, the CAS72 lifetime operand moved to the peak with the baseline move declared and derived, the oracle computing both chains itself, the committed studies restated before the regeneration commit, SV rows for the design-point identity and the disclosed verdict change, and the three stale doc comments corrected (`stellarator_plant.sysml:1137-1140`; the archived-path refs at `:700,703`; the EI-5 library line in `mfe_heating_chain.sysml`).
- **Why now:** T-001 returned `COMPLETE` with the strategy's one open question closed — there is no basis to correct the model's average to, the source's peak is its own calibration, and the source sets lifetime by the peak — so this is the strategy's intended model increment as written, minus assumption (2). The round's pin and its § Answered when (b)(ii) study both wait on it.
- **Scope:** authorized — `uv run agentic-mbse pm add-item`; the four modelling-PM stages with their validation levels; edits under `models/library/analyses/` and `models/designs/` with their byte-identical twins under `exploration/stellarator_e2e/models/`; the handwritten CAS72 impl under `exploration/stellarator_e2e/generated/handwritten/`; regeneration through the pinned codegen and the snapshot, manifest, census and fixture re-derivation it forces; the oracle (`verify_stellaris.py`, `studies/oracle_entry.py`); SV and traceability rows; a restatement note in each affected study's record dir. Excluded — the heating chain (every `heat.*` element; `eta_couple_heat` stays 1.00); `p_fus`, the geometry chain and the definition of `wall_area` (changing any is the strategy's abandonment condition: surface, park the dependent conclusions, owner); the value of `wall_load_limit`; any tuning of the baseline verdict; executing a study or promoting a pin (later tasks'); `/_my_quick_edit` under `models/`; every artifact barred by `knowledge/holdout/aries-cs/PROTOCOL.md` §3.
- **Inputs:** `goal.md` § Invariants (a verdict change disclosed, never tuned; entry-point and comparison-meaning changes restated, never silently broken; the clean room); `evidence/round2_T-001_source_basis.md` §§ 2–5; the ten constraints carried forward (`evidence/round1_review.md` § Constraints carried forward); strategy assumption (3). Narrower constraints, three, each stated because it is a recorded trap: (i) **the restatement is written before the regeneration commit** — round 1's T-002 did the reverse and recorded it as a deviation; (ii) **the calibration is computed in the model from its printed factors, never typed in as 1.316** — the source's peak (4.05), its fusion power (2700) with the neutron share, and the model's own circular-torus area at the source's R, a and minimum standoff — so the design-point identity SV checks a chain, not a transcribed constant; (iii) **the manifest's expected `wall_load_ok` verdict flips `satisfied` → `violated` with its derivation written beside it** — a fixture patched to match is evidence of nothing.
- **Done when:** the fence exists as model objects a fresh reviewer can point at; the model regenerates and executes at the baseline with LCOE, the other eight verdicts and every heating-chain number unchanged, and the two declared moves (the `wall_load_ok` verdict at 4.087 against 4.05; CAS72 95.898 → about 131.5 M$) reproduced by the independent oracle; `tests/models` and `tests/study` green with every delta derived; the restatement names each affected study, fixture and expectation with its derivation — **or** a bounded negative says which named quantity cannot be sourced.
- **Stop when:** a discovered prerequisite (the pinned codegen refuses the form past the retry cap; the handwritten stage cannot carry the new operand), a strategy blocker (the honest form needs a change outside the wall chain and the lifetime operand), an owner gate, or a declared limit.

### T-002 start — 2026-09-04 (round 2)

Task T-002 under § T-002 scope (round 2): mint the wall-fence work item and carry it through spec → design → plan → implement. Native target: `work/BACKLOG.md` (a new item under the MFE Cost Modeling epic) and `work/active/WI-0NN_source-anchored-wall-load-fence/` (`spec.md`, `design.md`, `plan.md` with its restatement section), model edits under `models/library/analyses/mfe_plasma_scaling.sysml`, `mfe_viability.sysml`, `mfe_account_costs.sysml`, `models/designs/generic_mfe/mfe_plant.sysml`, `models/designs/stellarator_09/stellarator_plant.sysml` and their twins, the handwritten CAS72 impl, the oracle, and the regenerated package with its pin files. Expected artifacts: the work-item directory with its stage records, the changed model files, the regenerated package, green batteries, SV rows.
