# Spawn prompt — fresh administrator, study `20260904-wall-and-heating` (the `study.read` seam)

Deposited 2026-09-04 before the administrator session ran, by the round-2 agent of goal `wall-and-heating` (every gate session's spawn prompt is deposited as evidence). The administrator is a fresh non-author `general-purpose` session with no inherited context. It reads the record directory and nothing else, and writes `synthesis.md` there. It is not the disposition checkpoint and not the round review; those are separate fresh sessions.

## The prompt given

You are a fresh **administrator** for a committed parameter-study record in /home/reid/1cfe/fusion-tea, per `.claude/skills/run-study/runbook.md` § Administer. You did not execute this study and you inherit no context about it. Your only output is `synthesis.md` in the record directory. Use `uv run python` for any scripting, never bare `python`.

CLEAN ROOM: never open anything under `knowledge/holdout/`. You do not need it.

Read, in this order, and nothing else about the study:
1. `.claude/skills/run-study/runbook.md` § Administer and the `synthesis.md` contract that follows it (your rules), and `modeling_project/STUDY_POLICY.md` if you need the vocabulary.
2. The record directory only: `exploration/stellarator_e2e/studies/20260904-wall-and-heating/` — `record.md`, `snapshot.json`, `indicators.json`, `axes.json`, and everything under `results/` (`points.csv`, `oracle_operands.csv`, `window_scan.json`, `window_edges.json`, `verification_summary.json`, `preflight_results.json`, `baseline_result.json`, `package_identity.json`, `excluded_points.csv`). `study.py`, `scan.py` and `edges.py` are in the directory and are digested by the snapshot; you may read them for the study definition but cite them as definition, not as evidence.

Do NOT read the package, the manifest, the discovery log, the goal directory, the work items, the predecessor studies, or the annex. What the record does not carry, you report as missing rather than recover from elsewhere. Only paths inside the record directory may be cited in `synthesis.md`.

Your job:
- Recover the fresh-administrator facts: the framing per axis, the LCOE result, every named constraint outcome, and every § 15 finding — each traced to a committed artifact in the directory. Keep recorded facts, missing facts, and your own interpretations distinct and labeled.
- **Independently recount the crux numbers from `results/points.csv` and `results/oracle_operands.csv`** (join them on `case_id`) rather than taking §§ 3, 4, 6, 11, 12 and 15 on their word. State exactly which columns you used and how you defined "alone" (one verdict column `violated`, the other eight `satisfied`) and "driven" (`feasible` true and `ignited` false). Recount at least: the per-constraint violation counts over all evaluated points and per arm; the evaluated, feasible, ignited and feasible-driven counts per arm; in `arm-fence-p100`, the cheapest `feasible_driven` point with its coordinates, peak, `core_life_fpy_from_peak`, `n_replacements_from_peak`, `cas72_per_MWh` and `lifetime_charge_above_limit_per_MWh`, and the cheapest `feasible` (ignited allowed) beside it; the same in `arm-search-p220`; in `arm-fence-p100`, the feasible-driven set's range in each of R, a, I_coil, T_i0, n_e0, and how many feasible-driven points sit at the top a value of the window (2.2); the a-dependence of `wall_load_peak` and `p_aux_required_MW_oracle` along the design column (R 12.7, I 15 MA, T 14.63, n 1.0×) at 100 MW, and the R-dependence along (a 1.3, I 15 MA, T 14.63, n 1.0×); along `arm-transect-ash`, `wall_load_peak`, `p_aux_required_MW_oracle`, `He_over_ne_oracle` and the wall verdict at each `tau_ratio_ash` for each anchor; the baseline row (`is_baseline_point`): its LCOE, peak, verdicts; in `arm-reread-p220`, how many points' `wall_load` (the average) match the values the record § 12 claims for round 1 (you cannot read round 1 — say so; recount only what this record carries), and the cheapest feasible-driven point there; how many feasible-driven points survive `feasible_shadow_hi`; the largest `lcoe_magnet_shadow` minus `lcoe` gap among feasible-driven points and where it sits; and the size-matched wall-price reading the record § 6 gives, re-derived from the columns it names. Where your recount disagrees with the record, say so plainly with both numbers.
- Say what the evidence establishes about the study's own question (record § 2 carries it) and what it does not, in your own words, labeled as the administrator's reading. Be specific about (i) whether a feasible-DRIVEN region exists at the printed 100 MW and what it costs, (ii) what the transect arm says about how conditional that region is on the held transport facts, (iii) what the magnet shadow does to the a-trend, and (iv) what the lifetime chain does and does not price.
- Write `synthesis.md` with the header (administrator, date 2026-09-04, the `snapshot.json` sha256 you read, computed with `sha256sum`) and the sections the runbook names: what the study set out to do; what it found; the framing verdict per axis; the constraint structure; findings carried forward; and **What the record does not support** (mandatory; empty only if nothing was missing).

Do not edit any other file. Return, as your final message, a short summary: the path you wrote, any recount disagreements with the record, and the list of facts you could not recover.
