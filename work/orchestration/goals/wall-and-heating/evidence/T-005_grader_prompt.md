# Row-4 re-grade — spawn prompt (deposited 2026-09-04 before the grader session ran; verbatim; independence is evidence, not attestation)

You are a fresh grader for the stellarator demo depth rubric in /home/reid/1cfe/fusion-tea. You did not author the rubric, WI-039, the goal round, its study, or any review of it; you inherit no conversation context; work only from the repository. NEVER read anything under knowledge/holdout/ (sealed). Use `uv run python` for any scripting, never bare `python`. Edit nothing except the one file you are asked to create; run no git command that changes state.

Your contract is the grading protocol: `.project/active/demo-depth-rubric/rubric.md` § Grading protocol (the file's last commit must be dc0f0b6d — verify with `git log -1 --format=%h -- .project/active/demo-depth-rubric/rubric.md` and record it). Apply the scoring rules exactly as written: a score is the highest level whose FULL evidence test is satisfied; integers only; no partial credit; a bound value checked against a bound value is not a computed chain; a calc on the handwritten codegen seam needs both the declaration and the executable impl cited; correctness defects are evidence-integrity findings attached to cells, never levels. The operative test for Row 4 is the row's own anchor table, and the written target is P2 / S2.

Provenance caution you must respect: the Row-4 target note's phrase "rides on row 1's confinement closure, which is owner-gated" is stale — Row 1 is at target since `grading-r1-regrade.md` (R1.P = 3), and the "owner-gated" text descends from a scope note corrected at `.project/concepts/stellarator-demo-maturation.md` § Corrections — 2026-09-01. Row 4's written target stays P2 by owner ruling 2026-09-02 (`work/orchestration/goals/operating-point-closure/trail.md` § Goal close). The anchors are unaffected; grade against them as written, and grade nothing against an unwritten P3 bar — if you find the P3 anchor met or contestable, say so as a finding for the owner, not as a score against the target.

Your evidence map (pointers only, no proposed scores): `.project/active/demo-depth-rubric/evidence-map-r4-regrade.md`. Merely knowing evidence exists is not reading it: open and cite what you actually read (path:line for model claims; channels/values for runtime claims; record/synthesis sections for study claims). Prior cells (`grading.md` R4.P with its grader note G1, and R4.S) are read for the delta comparison only, never as anchors. The prior grader's `why_not_next` for R4.P stated a concrete test — "a heating-chain calc whose output re-derives when efficiencies or delivered power change independently"; you decide for yourself whether the evidence meets the written anchor, and you may use that test as one instrument.

Write `.project/active/demo-depth-rubric/grading-r4-regrade.md` yourself (create exactly that one file; edit nothing else) with: the header block (rubric_version path@sha confirmed, model_version = git rev-parse HEAD, package identity for executed claims — the pin, semantic and executable fingerprints from the integration return the map names, grader line stating your independence and what you received); the R4.P cell record per protocol § 4 (cell_id, score, anchor_satisfied with the exact anchor text you find satisfied, model_evidence, runtime_evidence, study_evidence, why_not_next, grader); **the R4.S cell record too** — it was at its S2 target before WI-039 and the increment was required not to break it, so re-score it against its own anchor with evidence rather than carrying it forward; any evidence-integrity findings (protocol § 6); and a § Delta vs prior grading per § 7 (same rubric sha + new model sha = a valid progress comparison).

Judge the Row-4 P2 anchor's conjunction honestly against what the model now IS: "Wall-plug → coupled-power chain computed with a stated deposition assumption, verified." Three tests inside it — is the chain computed (not a relabelled held ratio), is the deposition assumption stated (not a silent default), is it verified (independently, not by parity between two copies of one expression). Weigh for yourself whether a two-stage chain whose coupling stage is a stated assumption at 1.00 with no sourced loss figure satisfies "a stated deposition assumption", and whether an oracle written from the design's equations rather than from the generated module satisfies "verified". Your reading of the written anchor governs; if you find the anchor itself contestable (not merely hard), say so explicitly — that routes to the owner, not around them.

Your final message: the scores, the anchor readings that produced them, and anything you found that the goal round should hear.
