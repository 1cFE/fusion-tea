# Spawn prompt — fresh administrator, study `20260903-wall-and-heating` (the `study.read` seam)

Deposited 2026-09-04 before the administrator session ran, by the resuming round-1 session of goal `wall-and-heating` (operating-point-closure round-2 review constraint 6: every gate session's spawn prompt is deposited as evidence). The administrator is a fresh non-author `general-purpose` session with no inherited context. It reads the record directory and nothing else, and writes `synthesis.md` there. It is not the disposition checkpoint and not the round review; those are separate fresh sessions.

## The prompt given

You are a fresh **administrator** for a committed parameter-study record in /home/reid/1cfe/fusion-tea, per `.claude/skills/run-study/runbook.md` § Administer. You did not execute this study and you inherit no context about it. Your only output is `synthesis.md` in the record directory. Use `uv run python` for any scripting, never bare `python`.

CLEAN ROOM: never open anything under `knowledge/holdout/`. You do not need it.

Read, in this order, and nothing else about the study:
1. `.claude/skills/run-study/runbook.md` § Administer and the `synthesis.md` contract that follows it (your rules), and `modeling_project/STUDY_POLICY.md` if you need the vocabulary.
2. The record directory only: `exploration/stellarator_e2e/studies/20260903-wall-and-heating/` — `record.md`, `snapshot.json`, `indicators.json`, `pre_wi039_indicators.json`, `axes.json`, and everything under `results/` (`points.csv`, `oracle_operands.csv`, `window_scan.json`, `verification_summary.json`, `preflight_results.json`, `baseline_result.json`, `package_identity.json`, `excluded_points.csv`). `study.py` and `scan.py` are in the directory and are digested by the snapshot; you may read them for the study definition but cite them as definition, not as evidence.

Do NOT read the package, the manifest, the discovery log, the goal directory, the work items, the predecessor studies, or the annex. What the record does not carry, you report as missing rather than recover from elsewhere. Only paths inside the record directory may be cited in `synthesis.md`.

Your job:
- Recover the fresh-administrator facts: the framing per axis, the LCOE result, every named constraint outcome, and every § 15 finding — each traced to a committed artifact in the directory. Keep recorded facts, missing facts, and your own interpretations distinct and labeled.
- **Independently recount the crux numbers from `results/points.csv` and `results/oracle_operands.csv`** (join them on `case_id`) rather than taking §§ 3, 4, 6, 12 and 15 on their word. State exactly which columns you used and how you defined "alone" (one verdict column `violated`, the other eight `satisfied`). Recount at least: the per-constraint violation counts over all 639 points; the evaluated and feasible counts per arm; in `arm-fence-p100`, the points blocked by `sustainment_ok` alone, by `wall_load_ok` alone and by `peak_field_ok` alone, and the minimum `p_aux_required_MW` among the sustainment-alone points with the source-efficiency threshold it implies at 100 MW wall-plug; the constrained optimum of `arm-search-p220` with its coordinates, its wall load against the 4.05 limit, its `wall_load_shadow_lo` value and verdict; how many feasible search-arm points survive `feasible_shadow_lo` and how many survive the high shadow bound, and the cheapest survivor; along `arm-transect-eta`, LCOE and heating capital at both ends, whether `p_wallplug_total_MW` moves, and where the sustainment crossing falls; along `arm-couple-132`, LCOE at both ends, whether heating capital moves, and `rec_frac` at both ends; whether `p_fus`, `wall_load`, `beta` and `p_aux_required_MW` are identical across efficiency at fixed (I_coil, T_i0, n_e0) within `arm-search-p220`; and the baseline row (`is_baseline_point`). Where your recount disagrees with the record, say so plainly with both numbers.
- Say what the evidence establishes about the study's own question (record § 2 carries it) and what it does not, in your own words, labeled as the administrator's reading. Be specific about the two opposite-signed efficiency results and about what the wall-fence caveat (record § 4, § 15 #3) does to every economic claim at 220 MW.
- Write `synthesis.md` with the header (administrator, date 2026-09-04, the `snapshot.json` sha256 you read, computed with `sha256sum`) and the sections the runbook names: what the study set out to do; what it found; the framing verdict per axis; the constraint structure; findings carried forward; and **What the record does not support** (mandatory; empty only if nothing was missing).

Do not edit any other file. Return, as your final message, a short summary: the path you wrote, any recount disagreements with the record, and the list of facts you could not recover.
