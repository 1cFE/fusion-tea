# Round 1 review — goal `wall-and-heating` (fresh reviewer, 2026-09-04)

Deposited verbatim by the round agent from the fresh reviewer's final message (spawn prompt `round1_review_prompt.md`; `general-purpose` session, no inherited context; edited nothing). This is the review of record; the trail's `### Round 1 review` entry summarizes it.

---

**Verdict: FINDINGS.** The round stays closed. Every cited ref resolves, every task stayed inside its scope or disclosed its one extension, no retry was misclassified, the checkpoint sequence is clean, all fourteen dispositions landed, no cited artifact moved outside its task, both batteries are green, and § Answered when (a) is met. The findings are corrections to the proposed learning delta and to two readings the next strategy must not inherit — not defects in any task's evidence. I have edited nothing; the `### Round 1 review` entry, the accepted learnings, and the round-2 strategy below are for the owner or a round agent to land.

## Grounds

1. **Two learnings over-claim.** L-001's implication ("no later round spends a study on source efficiency at the printed level") holds only under the wall fence *as bound*: with the wall verdict removed, the minimum required coupled power among the 100 MW points that pass every other fence is 23.8 MW, so the level opens at `eta_source` ≈ 0.24 — below the pinned 0.50. The printed level's fate is the wall's, exactly as the second clause says; the first clause must carry that condition. L-003 generalises T-001's finding ("every published average is over a shaped wall, so the area basis is the defect for either form") past the three registered sources to the model's own source, and the model's own source does not fit it (finding 2).
2. **The Stellaris paper already carries evidence T-001 did not read, and it changes the shape of round 2.** `knowledge/concept_research/09-qi-stellarator-hts/iter-01/sources/stellaris-design-details.md` prints, for this design at ~2700 MW: peak neutron wall load 4.05 MW/m² from a 3D OpenMC/DAGMC run on its own CAD first wall (Table 2 image, lines 1590–1607, Fig. 33), average 2.87 (line 747), plasma surface area **940 m²** (Table 2 image, confirmed by viewing `images/page_002_table_0.png`), and a first-wall minimum standoff of **100 mm** (line 1295). Arithmetic on those: 2700 × 0.8 / 2.87 = 753 m², *smaller* than the 940 m² LCFS, so the 2.87 cannot be an average over the shaped first wall; it equals a circular torus at R 12.7 m and r = 1.5 m (752.1 m²) to three digits. The model's doc comment calling 2.87 "the source's shaped-wall average" (`models/designs/stellarator_09/stellarator_plant.sysml:1139-1140`, WI-022 era, not this round's) is an unsourced assumption. Consequences: the model's 0.10 m standoff is the source's own, not a model error; and the source's own peak gives a net multiplier on the model's circular-torus average of 4.05 / (2160/701.926) = **1.316**, inside round 1's 1.15–1.83 shadow range. Under it the baseline reads 3.105 × 1.316 = **4.088 against 4.05 — violated by the model's ~1% fusion-power excess over the source's ~2700 MW**; 26 of 91 feasible 220 MW search-arm points survive, cheapest 371.005 (`c0584`: I 14.75 MA, T 17 keV, n 0.8×, eta 0.60); at 100 MW, 9 points become sustainment-alone with minimum required 92.1 MW (threshold 0.92). These are for scale, not claims — round 2 establishes the averaging basis from the source text before anything is built.
3. **One learning candidate the round itself nominated was dropped** (the Helios/HELIAS homograph, T-001 return decision 3). Added as L-008.
4. **One log row is already behind the trail.** The newest `20260901-sustainment-fence#4` row says "the grade is pending (T-005)"; the grade landed the same day (R4.P = 2). A close row is owed at WI-039 close.

## Checks

**Native evidence, by citation — what I opened and what I recounted.**

- Trail and dispositions: append-only verified — `git show` on every round commit (`860ce7d1` … `d9930dfc`) removes zero lines from `trail.md`; `DISCOVERY_LOG.md` gains 7 sighting rows at `2d11ca1b` and 14 disposition rows at `103c41a5`, zero removed. The r1 text stands unedited above § Revision r2 in `evidence/T-004_proposed_dispositions.md`.
- Study record `exploration/stellarator_e2e/studies/20260903-wall-and-heating/` (`2d11ca1b`, Addenda at `31284e05` and `577a6fa5`): recounted from `results/points.csv` joined to `results/oracle_operands.csv` on `case_id` — 639 rows; arms 240/384/7/8; feasible 0/91/4/8 = 103; `sustainment_ok` violated 381, `wall_load_ok` 318, `peak_field_ok` 96, `recirc_ok` 25, `beta_ok` 12; at 100 MW alone-counts sustainment 36 / wall 10 / ceiling 9, the other 204 all fail wall or ceiling; minimum required among the 36 = 87.061 MW at `c0036` (I 15.4, T 17, n 0.9×) → 0.8706, crossings 0.871–1.629, 12 (I,T,n) cells; at 220 MW wall-alone 77, sustainment-alone 124, recirc-alone 13; optimum 267.159 at `c0550` (eta 0.60, I 14.25, T 16, n 1.0×, wall 4.0037 = 98.9%, `lcoe_1cfe` 262.089, coupled 132.0 vs required 128.64); shadow multipliers exactly 1.15 / 1.83 on every row; 51 of 91 survive low, 0 high, cheapest survivor 326.201 `c0546`; transect 269.823 → 273.675 with heating capital = 5,282,900 × 220 × η on all seven rows; couple arm 317.234 → 255.970, heating capital 697,342,800 on all eight, wall-plug × η = 132.0 on all eight, `rec_frac` 0.4498 → 0.3095; fusion performance single-valued across η in 96 of 96 cells; baseline `c0113` at 307.08712042841586 with `sustainment_ok` violated; `c0149` at 262.10, ceiling-alone, B_peak 27.487, wall 3.886, shadow-lo 4.469; 14.0 MA all 64 sustainment-blocked, 28 also wall; the 1.2× column at 100 MW wall-violated 57 of 60 and the I-series value 279.127 — both Addendum corrections right. The 36 shared points with `20260903-priced-levers` (`arm-fence-p50`, `j_wp` 118.827, `p_input` 50): 36 matched, worst relative deviation 0 over six channels, 0 verdict differences. `verification_summary.json`: 13 channels, worst 5.55e-16, 9 verdicts re-derived, 0 mismatches, `not_independently_verified` empty (and, as the trail says, that emptiness is not coverage). `snapshot.json`: four arms each carrying `effective_executable_fingerprint.value` = `b0c5f3ed…`.
- T-003: `evidence/T-003_integration_return.json` class `CANDIDATE`, ten gates all `pass`, audited work `WI-039…@ba7098a2`, pin `2649e0ea…`, semantic `48731d15…`, executable `b0c5f3ed…`; `T-003_package_identity.json` kind `sealed`, digest = executable fingerprint.
- T-002 model state: `models/library/analyses/mfe_heating_chain.sysml` exists; `stellarator_plant.sysml:636-704` carries the entry point and the written coupling assumption (what, status, direction, where a sourced figure enters); `:1186-1189` binds `sustainment_ok.p_aux_installed_in = heat.p_coupled`; `mfe_power_balance.sysml:135-141` consumes `p_wallplug_in` with the old division gone; `verify_stellaris.py` has no `p_input / eta_pin` term; `studies/manifest.json` carries only the `magnet__R0`/`R` tie; SV-048/049 `passing` in `VALIDATION_MATRIX.md:74-75`; `plan.md` § MR-WI039-9 restatement present with its ordering deviation first; spec MR-WI039-1..11 present.
- T-001 sources: `SOURCE_INDEX.md` blocks at `:364-427`; Lion Table 1 (`…_2/output.md:316-326`) reads R 13.9/17.6/22.2/9.3, a 2.2/2.0/1.8/1.5, S_FW 1720/2020/2110/861, p_f 1.59/1.67/1.69/1.51, optimised 1.23/1.12 — T-001's arithmetic reproduces (wall-side ratios 1.254/1.264/1.146/1.303; plasma-basis 1.338; net 1.475; bounds 1.15/1.83; optimised net 0.86/1.07; crossover 1.042); Häußler `output.md:109,117,120` (1.936/0.953, 1.958/0.926, "total plasma facing area"); Beidler `output.md:27-28,151` (R 22/18, ⟨a⟩ 1.8/2.1, 2600/2500 m², peak 1.7, avg below 1). Both run returns `REGISTERED`; the Helios refusal is in `runs/REQ-WALL-02/…/run.jsonl:3`; the junk slug is 1415 bytes with a `MANIFEST.jsonl` row and index block; model `wall_area` 701.926 and baseline 3.105376639122585 confirmed in the priced-levers baseline.
- Spawn prompts (`9c0dc933`, 09:11) precede the administrator (`31284e05`, 09:22), the checkpoint and the grader; the checkpoint prompt is non-fork and cites the runbook; the T-001 prompts carry the clean-room screen and name Helios before any fetch (`T-001_REQ-WALL-0x_prompt.md:26-33`).
- Rubric Row 4 at `dc0f0b6d` (`rubric.md:109-119`) reads as the trail and grader quote it.

**Goal and strategy fidelity.** Declared order: wall research → WI-039 → one pin → one narrow heating study → fresh Row-4 re-grade. Executed: T-001 → T-002 → T-003 → T-004 → T-005, exactly one promoted pin and one committed study. The wall fence was not touched (`git log -- models/` ends at `b45b4b01`, WI-039 only; the wall chain at `:1130-1160` unchanged). Geometry and `B_max` declined and traced with indicators; `j_wp` checked rather than assumed; T interior at the 220 MW optimum (16 keV) with the scan to 24 keV showing nothing feasible above 18. Fidelity holds.

**Task scopes.** T-001 inside (research seam only; the form not decided). T-002 inside (three exclusions honoured; S2 not broken; the wall untouched; `sustainment_ok`'s requirement not tuned). T-003 inside (no gate argued). T-005 inside (one file created by the grader; no steering). T-004 — two rulings:

- (i) **`arm-couple-132` is a legitimate extension, not drift.** It was triggered by the fresh critique (F1: the scope as written could only produce a forced answer); it moves only authorised axes, jointly; it added 8 points, changed no pin, no question, and no comparison meaning; it was disclosed in `study.py`, record § 2 and § 11, and the T-004 return before this review; the checkpoint checked the disclosure; and the owner delegated execution decisions (ruling 5). Drift is an undisclosed or question-changing expansion; this was neither. Carry-forward: when a critique reshapes a study past its written scope, append a dated `### Amendment` to the scope *before* points run rather than disclosing at return.
- (ii) **The trail's placement of the re-grade inside round 1 is right.** A task cannot run outside an open round, the review never resumes a closed round, and the round result must carry the § Answered when measurement rather than owe it; `operating-point-closure` round 2 (T-006) is the precedent. The handoff that placed it after the review (`.project/CURRENT_WORK.md@41553eb7:15`) is a coding-PM status note, not a goal surface, and the strategy revision it summarised had the re-grade last. Nothing changes: the round closes on trigger 1 either way, and T-005 ran after the checkpoint, which is the conservative order.

**Retry classification.** Zero goal-level retries is correct. The three in-execution corrections (findings #4, #5, #6) happened inside one T-004 execution before any return and before any commit; no `MECHANICAL_FAILURE` was returned, so nothing was re-started under the runbook's retry rule. Two of them changed the study definition (anchor 110 → 132 MW; the shared 0.60 point excluded), so they could not have qualified as retries even if claimed. They are native-stage corrections, each registered as a finding. One weakness, flagged by the administrator (`synthesis.md` § 8): the first execution's rows are not in the record, so #5 and #6 rest on the executor's account. Carry-forward below.

**Checkpoint sequence.** C-001.r1 `REVISE` → C-001.r2 `PASS`, one of two revisions used; each submission a new entry; the second Addendum written at the reviewer's request; the two non-blocking notes applied before rows were appended. Correct.

**Discovery rows — all fourteen landed as class-bearing rows (class, status, responsible actor, next reference on each), and each finding moved or says plainly that it did not:** `#1`, `#3`, `priced-levers#1` routed to round 2 with the defect sharpened (moved); `#2` answered at the model level and carried into the re-grade, which then scored it (moved); `#4`, `#5`, `#6` closed in-study with the practice recorded (moved); `#7` accounted at WI-040 (unchanged, says so); `priced-levers#4`, `sustainment-fence#3` unchanged with the fourth member named (say so); `priced-levers#5` applied without recurrence (says so); `priced-levers#3` unchanged, Pierro still queued (says so); `sustainment-fence#1` unchanged, the 262 candidate re-measured with its exposure (says so); `sustainment-fence#4` discharged at the model level — now stale by one fact (grade landed), see close packet. No touched row returns `unrouted`.

**Cited artifacts moved outside their task?** No. The two record Addenda (`31284e05`: +12 lines; `577a6fa5`: +4 lines; nothing under `results/`, `snapshot.json`, `axes.json`, `study.py` differs from `2d11ca1b`) are the study runbook's own step-15 mechanism, additive and dated, and both fall inside T-004's window before its return. Not an external mutation. **EI-3:** the round agent was right not to edit `work/active/WI-039_heating-system-structure/plan.md` after T-002 returned; bringing the item record current is WI-039's close act through the modelling PM. At HEAD every checkbox in all five phases is `[ ]`, `plan.md` says `Status: draft` while `spec.md` says `Status: active` and `work/BACKLOG.md` says `active`.

**Battery, re-run by me at HEAD (`d9930dfc`):** `tests/models` **48 passed / 13 skipped**; `tests/study` **359 passed / 1 skipped** in 7:35. Both exit 0.

**§ Answered when (a) — met.** `grading-r4-regrade.md` scores R4.P = 2 with each conjunct argued on the anchor's letter (computed: the division is gone and each input re-derives the outputs, re-executed by the grader on both sides; stated: the assumption is written at the binding; verified: oracle chain from the design's equations, 20 sampled rows spanning five efficiencies at 5.55e-16). R4.S re-scored 2, at its S2 target, cost unchanged to the dollar. Battery green (above). "Affected studies re-run clean" is satisfied on the MR-WI039-9 restatement reading, on the owner-ruled `operating-point-closure` precedent (trail `:285`, "No replay directed"), and this round has stronger evidence for it than that precedent had: the ×2 mapping is exact in IEEE arithmetic and 36 shared operating points reproduce to every digit across the boundary. The owner should confirm the same reading at close, as at OPC.

**§ Answered when (b) — what the evidence now says the honest form must decide**, in order: (1) the averaging basis of the source's own 2.87 (finding 2: not a shaped-first-wall average; numerically a circular torus at r = 1.5 m); (2) the operand form — a computed peak against the printed peak, anchored to the design's own 3D peak at its own 100 mm wall, with the external sources as plausibility bounds (Stellaris's implied shaped-wall peaking ≈ 1.8–1.9 sits between Lion's equidistant 1.5–1.7 and Häußler's 2.0–2.1); T-001's shaped-wall route (area factor 1.146–1.303 on the wall-side radius plus peaking on the shaped average, standoff transfer 0.30 → 0.10 m argued) is the fallback if (1) cannot be established; (3) which quantity feeds the lifetime chain — `mfe_account_costs.sysml:796-803` computes `q_n = p_neutron / firstwall_area` with `firstwall_area = rb.wall_area` (`mfe_plant.sysml:902`), a circular-torus average, while Lion says peak loads set blanket lifetime and the source estimates ~5 FPY at ~35 DPA/FPY peak — a CAS72 baseline move (95.9 M$) if the operand changes, a comparison-meaning change to declare; (4) the geometry sweep, with `R`'s effect on wall load measured for the first time at this pin; (5) the shadow columns pre-registered tightening only (1.15–1.83); the source-anchored 1.316 sits inside it, an optimised-wall net (0.86–1.07) does not — round 1's 220 MW result is restated under whatever form lands.

## Learning delta rulings

- **L-001 — accept, corrected.** Add the condition: *under the wall fence as bound*. With the wall verdict removed the 100 MW level opens at `eta_source` ≈ 0.24 (min required 23.8 MW among points passing every other fence), so the threshold 0.871 is a statement about the wall, not about heating. Implication becomes: the printed level's fate is the wall fence's; a source-efficiency study at the printed level is worth running only if round 2's honest fence *loosens* the wall verdict.
- **L-002 — accept** as written.
- **L-003 — accept, corrected.** "The external sources' factors are all defined on shaped-wall averages, so transferring any of them needs the area basis; the machine's own source prints a 4.05 peak on its own first wall at its own 100 mm standoff and a 2.87 average whose basis is not a shaped first wall (753 m² implied, below the 940 m² LCFS). Establish that basis before choosing the form. The 1.15–1.83 range is conditional (unoptimised wall, 0.30 m standoff); the source-anchored net is 1.316."
- **L-004 — accept** as written.
- **L-005 — accept** as written (three recorded repeats; mint the guard owner-present).
- **L-006 — accept** as written.
- **L-007 — accept, corrected** to the claim and its reason, without the handoff narrative: "A goal whose § Answered when is a re-grade runs the re-grade as the round's last task, after the study reading is checkpointed — a task cannot run outside an open round and the review never resumes a closed one, so a result placed before the re-grade would owe the goal's measurement rather than carry it." Candidate runbook clarification for the owner.
- **L-008 — added (the round nominated it and the result dropped it).** Refines `priced-levers` L-005: the clean-room near-miss can be a homograph (Helios, barred; HELIAS, admissible) surfacing in one result set; the screen held only because the prompt named Helios before any fetch. Implication: every stellarator-area research prompt names the Helios/HELIAS distinction explicitly.

## Constraints carried forward

1. The wall fence is round 2's model increment; nothing at the heating chain changes; `eta_couple_heat` stays 1.00, disclosed optimistic; the printed level is 100 MW wall-plug.
2. Any baseline verdict change from the honest fence (expected: 4.088 vs 4.05) is disclosed and explained, never tuned; the limit is not moved to open a region.
3. The CAS72 lifetime operand is decided explicitly and its baseline move declared as a comparison-meaning change; the round says which committed studies are restated and writes the restatement *before* the regeneration commit.
4. Round 1's 220 MW result (267.159 at `c0550`; 26 survivors at 1.316; 51 at 1.15; 0 at 1.83) is restated under the chosen fence; no feasibility-count comparison across the boundary.
5. Temperature is a search axis with an interior or fence-caught optimum; `a` brackets the committed a ≥ 1.70 violation; `R` is swept with its `magnet__R0` tie; `I_coil` stays under the ceiling; `j_wp` held; `B_max` declined (WI-038 after WI-040).
6. Multi-field module outputs (`pb__*`, `sustain__*`, `heat__*`, and any new wall-chain calc with several outputs) are exported oracle-side, never declared as store channels.
7. A study definition asserts no two arms share a point; a transect's held level is read off its anchor's operands; when a re-execution follows a design defect, the first execution's `points.csv` is kept beside the record so the finding is evidenced, not narrated.
8. A critique-driven change past the written scope gets a dated scope amendment before points run.
9. Record contract: no `<` in `record.md`; `effective_executable_fingerprint.value` on every arm; `tests/study/test_records.py` before the record commit.
10. Every fresh-session gate is a spawned non-author `general-purpose` session with its prompt deposited first; the grader receives pointers only.

## Close packet — for the owner to rule on

1. **WI-039 close** through the modelling PM: tick the plan's phases with notes pointing at `b45b4b01` / `015f026e` / `6678968e`, SV-048/049 and trail § T-002 return; reconcile `plan.md` `draft` vs `spec.md` `active`; `pm close-item WI-039`; archive; a trail amendment redirecting citations (magnet-closure precedent); and append a close row under `20260901-sustainment-fence#4` recording R4.P = 2.
2. **The "affected studies re-run clean" reading** — confirm the restatement reading, as at the OPC close.
3. **The junk registry entry** `knowledge/sources/a_deterministic_method_for_the_fast_evaluation_and/` — an `unregister` operation is missing; whether to add it or remove the three files by hand is the owner's call.
4. **The grader's contestable P3 anchor** (a power limit vs "port, coupling, or geometry limit") — rubric v2 (OQ6), batched with the stale Rung C text.
5. **The hardening candidate** (L-005: the declaration-time channel-shape guard) — mint owner-present.
6. **EI-5** — the one-line library doc clarification in `mfe_heating_chain.sysml:30-35` (the efficiency travels with the powers in the dormant path).
7. **Merge and push** of `feat/demo-maturation` — all round-1 commits are local.
8. **The Pierro et al. 2019 fetch** — still queued.
9. **The unsourced doc comment** at `stellarator_plant.sysml:1139-1140` ("the source's shaped-wall average") — round 2's model task corrects it; flagged so it is not read as a sourced fact meanwhile.

Working-tree note: your uncommitted `GOAL_RUNBOOK.md` edit, the `SUMMARY.md` deletion, the three `narrative.md` files, and a `.project/concepts/goal-narrative-snapshots.md` that appeared during this session are not the round's and were not read as such.

## Round 2 — strategy revision (for `trail.md`, opens round 2 of 6)

`## Round 2 — source-anchored-wall-fence`
`### Strategy revision — 2026-09-04` `[AGENT — the round-1 reviewer; subject settled by the owner: "defining the broken wall-load fence as Round 2" [OWNER 2026-09-04, verbatim]]`

- **Approach:** make the wall-load fence compare like with like by anchoring it to the machine's own source first, and use the three registered external sources as plausibility bounds rather than as the transfer. The Stellaris paper prints, for this design at ~2700 MW, a 3D-neutronics peak of 4.05 MW/m² on its own first wall at its own ≥100 mm standoff, an "average neutron wall power" of 2.87, and a plasma surface area of 940 m². The round's first act is to establish the 2.87's averaging basis from the source text (§ 2.8, Fig. 33, the method it cites) — the arithmetic says it is not a shaped-first-wall average and is numerically a circular torus at R 12.7 m, r 1.5 m. Then build the fence as a computed peak against the printed peak limit: the model's forward-computed average, put on the source's stated basis by a named reference-radius correction, times a named, sourced net peaking factor, with the factor's definition, reference radius and standoff written into the model text. If the basis cannot be established, the form is T-001's shaped-wall route (Lion's area factor on the wall-side radius and peaking on the shaped average, with the 0.30 → 0.10 m transfer argued from admissible text); if that transfer cannot be argued either, the round returns the bounded negative `goal.md` § Limits names, with its three options, and the owner rules. Whatever lands, the baseline verdict change is disclosed and explained, never tuned — expect the baseline to read ≈ 4.09 against 4.05, violated by the model's ~1% fusion-power excess over the source's ~2700 MW, a first-class result on the `sustainment_ok` precedent. Then one promoted pin, then the one committed study § Answered when (b)(ii) names.
- **Assumptions:** (1) the source's own peak is admissible and is the right limit for this machine — it is the design's 3D value on its own wall, and the model already carries it; (2) the model's circular-torus average can be put on the source's basis by a stated reference-radius correction alone, without a shaped-wall area, once that basis is established — if the basis turns out to be a shaped wall, this assumption fails and the T-001 route applies; (3) the fence change is a verdict-class change (operand form) plus, at most, the CAS72 lifetime operand, so committed studies are restated at the MR-WI037-7 shape (~30 fixture sites) and their `wall_load_ok` verdicts are re-readable through a stated multiplier; (4) the lifetime operand (peak or average) is decided explicitly with its convention stated; (5) `R` reaches wall load through the ISS04 coupling at this pin and its effect is measurable; (6) heating stays as WI-039 left it.
- **Abandonment conditions:** the source's averaging basis cannot be established and the standoff transfer cannot be argued from admissible text (bounded negative, owner rules); the honest form would change `p_fus`, the geometry chain, or any quantity outside the wall chain and the lifetime operand (comparison meaning moves beyond a restatement — surface, park the dependent conclusions, owner); the fence cannot be expressed without a defaulted number; a fresh gate session cannot be obtained.
- **Intended model increment:** one work item through the modelling PM, minted at the round's first selection act under the MFE Cost Modeling epic — a wall-fence calc that computes the peak operand from the model's average through named quantities (reference-area correction, peaking factor, their basis and standoff as documented attributes); `wall_load_ok` comparing that peak to the printed 4.05; the average retained and reported against the source's 2.87 as a comparand, not a fence; the CAS72 lifetime operand decided and stated; the oracle computing the same chain from the design's equations; the committed-study restatement written before the regeneration commit; SV rows for the baseline identity (reproduce the source's 4.05 at its ~2700 MW) and the disclosed verdict change; the doc comment at `stellarator_plant.sysml:1139-1140` corrected. Nothing at the heating chain changes.
- **Intended study question:** at the promoted pin with the honest fence, does a feasible region exist at the printed 100 MW wall-plug (50 MW coupled at 0.50, lossless coupling) over the wall's levers `R`, `a`, `n_e0`, `T_i0` with `I_coil` — and at 220 MW, where does round 1's 267.159 optimum go under the chosen fence, and what does the machine pay to get under the wall through wall load → in-vessel lifetime → CAS72? Windows fixed by an oracle scan with `a` bracketing the committed a ≥ 1.70 violation and `T` interior or fence-caught; `j_wp` held; `B_max` declined; source efficiency held at 0.50 (round 1 answered it); one shadow column at the alternative admissible form, if the round chose between two, so the choice's sensitivity is data.

No future task list. Tasks are chosen from evidence as each returns.
