# Round 2 fresh review — spawn prompt (deposited 2026-09-05 before the review session ran; verbatim)

You are a fresh reviewer for goal round 2 (`source-anchored-wall-fence`) of `work/orchestration/goals/wall-and-heating/` in /home/reid/1cfe/fusion-tea. You did not author any part of this goal, its work item WI-041, its source reading, its pin, its study, its synthesis, its checkpoint, or its result; you inherit no context; work only from the repository. NEVER read anything under `knowledge/holdout/` (a quarantined clean room; `knowledge/holdout/aries-cs/PROTOCOL.md` §3 bars it). Use `uv run python` for scripting, never bare `python`. For the test batteries use exactly: `rm -rf .integration_workspace; uv run --env-file ~/1cfe/agentic-mbse/.env --env-file .venv/integration.env python -m pytest <target> -q` with the Bash tool's timeout set to 600000 ms (`tests/models` needs `set -a; source ~/1cfe/agentic-mbse/.env; set +a` first and takes about a minute; `tests/study` takes about 8 minutes and must run WITHOUT any `PYTHONPATH` export — one of its tests is a negative test on that). Do not edit any file. Do not run any git command that changes state (no add, commit, stash, checkout, reset). The working tree carries the owner's untracked work in progress, not part of this round — `.project/concepts/physical-innovation-narrative.md`, two files under `.project/research/`, five files under `work/narratives/` — and `tests/orchestration/test_goal_contract.py` has one test (`test_shipped_narratives_meet_the_snapshot_contract`) failing on one of those untracked narratives; that failure is known, disclosed in the round result, and not the round's. The branch is `feat/demo-maturation`; the round's commits, in order: `19b79f92` (T-001), `df6dc964` and `cb355321` (WI-041), `7bd87cb7` (T-002 return), `55c1f0ac` (T-003), `9df54941` (the study record), `9b1a4ef1` (synthesis, first Addendum, dispositions r1, checkpoint prompt), `a5b0b96a` (second Addendum, dispositions r2, C-001.r1 entry), `8906d4e7` (17 disposition rows, C-001.r2 entry), `f51b2915` (T-004 return, Round 2 result), `ea7304bc` (CURRENT_WORK.md, a coding-PM status file, not a goal surface). Two owner commits landed between them (`368c37e5`, `f690b0cd`) on narrative and status files.

Read, in order: (1) `work/orchestration/GOAL_RUNBOOK.md` § The fresh review (your contract), § What "fresh" means, § The discovery log, § When a cited artifact moves, § Opening and closing a round, § The two checks are distinct; (2) `goal.md` (question, § Answered when — read (b)(i) and (b)(ii) word by word, § Invariants, § Limits, § Reserved gates, § Close rule); (3) `trail.md` end to end — round 2 runs from `## Round 2 — source-anchored-wall-fence` (the strategy revision written by the round-1 reviewer, with the owner's settled subject) through `## Round 2 result — 2026-09-04`, with the T-001..T-004 scope/start/return entries, the `### Amendment 2026-09-04` to the T-004 scope, and the `### Checkpoint C-001.r1` and `C-001.r2` entries in between; read § Round 1 review and `evidence/round1_review.md` § Constraints carried forward (the ten constraints round 2 inherited) and `learnings.md` L-001..L-008; (4) the evidence directory `evidence/round2_*`: the T-001 source basis, the T-002 baseline result, the T-003 integration return and its sibling JSON files and the recaptured snapshot, the T-004 pre-execution critique and its prompt, the administrator prompt, the proposed dispositions (r2, every change marked `[r2]`), the checkpoint prompt; (5) the WI-041 item record `work/active/WI-041_source-anchored-wall-load-fence/` (spec MR-WI041-1..12, design D1–D5, plan with its § MR-WI041-11 restatement, phase records and § Spec success criteria) and the model text it changed (`models/library/analyses/mfe_plasma_scaling.sysml` — the two new calc defs; `mfe_account_costs.sysml` 'Levelized Replacement Cost'; `models/designs/generic_mfe/mfe_plant.sysml` — the wall chain; `models/designs/stellarator_09/stellarator_plant.sysml` — the six `wall_peak_*` bindings and the rewritten cross-check block); (6) the committed study `exploration/stellarator_e2e/studies/20260904-wall-and-heating/` (record.md with its two Addenda, synthesis.md, snapshot.json, axes.json, indicators.json, study.py, scan.py, edges.py, `results/` — points.csv, oracle_operands.csv, excluded_points.csv, window_scan.json, window_edges.json, baseline_result.json, package_identity.json, preflight_results.json, verification_summary.json); (7) the `DISCOVERY_LOG.md` rows dated 2026-09-04 — the eight sightings `20260904-wall-and-heating#1..#8` and the 17 joined disposition rows appended at `8906d4e7` (`git show 8906d4e7`); (8) the machine's own source, admissible: `knowledge/concept_research/09-qi-stellarator-hts/iter-01/sources/stellaris-design-details.md` at the lines the T-001 return cites (231, 241, 747–748, 1295 and the Table 2/5/6 regions), and the page images the T-001 return says it read instead of the extracted text (the return names the paths and the `decisions.json` `claude_replace` entries).

Your checks (all of them):

- **Every cited ref resolves** and says what the trail claims it says. Recount the numbers you rely on from `results/` yourself (`uv run python` over points.csv), never from the prose: the 458 / 201 / 257 split at 100 MW, `c1721` 212.460 and its operands, `c0821` 257.35 on the design column, the 0 feasible at a 1.3 at 100 MW, the transect's fifteen rows, the re-read arm's 384 / 152 / 91 → 26, `c0550` → 5.271 violated and `c0584` → 378.556, the shadow survivors 257 / 142, the size-matched wall prices 26.54 / 17.08 against the charges 14.07 / 15.07, the R exponent 0.72 on the executed design column, the ceiling structure in the second Addendum, and the baseline's five changed channels at T-002 (calibration 1.3164408571, peak 4.0880446844, CAS72 131,494,480, LCOE 313.513412, `wall_load_ok` violated).
- **Goal and strategy fidelity.** The strategy revision's approach was: establish the 2.87's basis from the source before choosing the form; then one model increment making the fence compare like with like with the CAS72 lifetime operand decided explicitly and round 1's 220 MW result restated before regeneration; then one pin; then the (b)(ii) study over `R` (with its tie), `a`, `n_e0`, `T_i0` with `I_coil` at the two heating levels. Did round 2 pursue it in that order, with one pin and one committed study? Rule on the strategy's six assumptions as the result reports them (assumption (2) failed in letter at T-001).
- **Every recorded task scope, T-001..T-004, and the T-004 scope amendment.** Did the work stay inside? Rule specifically on: (i) **T-001's source reading** — that the printed 2.87 has no stated basis, is not a wall average by the source's own cited definition, equals fusion power over the printed 940 m² plasma surface to three figures, that the only printed peak is 4.05 (the "4.95" row the round-1 review noted does not exist in the source), and that the source sets first-wall lifetime by the peak — verify each against the source lines and page images, and say whether the extraction-artifact finding (the "r = 1.5 m torus") is right; (ii) **T-002's form decision** — anchoring the computed peak directly on the source's printed peak at its own design point (calibration 1.316440857 from six page-image facts, `calibration_direct` dormant at 0.0), the CAS72 lifetime operand becoming the peak, the exact `ash_frac` retired — is it the honest form under § Answered when (b)(i) and § Invariants ("made honest is not made looser"; any baseline verdict change disclosed, never tuned), and was the baseline flip (`wall_load_ok` satisfied → violated at 4.088 against 4.05, the model's 0.94 % fusion-power excess) disclosed and left alone?; (iii) **the MR-WI041-11 restatement** of round 1's 220 MW result before the regeneration commit — written where the round-1 constraint required, and does T-004's re-read arm (all 384 points re-executed, 26 survivors) confirm it?; (iv) **the T-004 scope amendment** — three critique-driven changes (a τ*/τ_E sensitivity transect over a held assumption; the re-read arm at all four source efficiencies; two labelled shadow readouts) authorized by a dated amendment before any point ran, under the round-1 review's carry-forward rule — legitimate, or drift?; (v) the study's held axes (`eta_source_heat` 0.50, `eta_couple_heat` 1.00, `j_wp`, `B_max`, the six `wall_peak_*` reference facts held by definition) — traced with indicators as the scope says?
- **Retry classification.** The trail claims zero goal-level retries. Rule on each of: the execution launch killed by the harness before any point ran and re-launched detached; the codegen re-stencil of the handwritten CAS72 impl and its hand restoration inside T-002; commit B's sweep of the owner's staged files and its amendment (`9ebf9f08` → `cb355321`); the 65 proposals excluded by the oracle pre-screen. Retries under the runbook's definition, native-stage corrections, or neither?
- **The checkpoint sequence.** C-001.r1 `REVISE` → the author's second Addendum and an in-place revision of the dispositions file marked `[r2]` → resubmitted to the same session → r2 `PASS`. Round 1 recorded its revision as a separate `§ Revision r2` section; round 2 revised in place with marks. Rule on whether that meets the runbook's "submissions never edited in place" reading, and whether the cap (2 revisions / 3 submissions) was respected.
- **Every discovery row the round's evidence touched** — the eight sightings and the 17 joined rows at `8906d4e7`: did each disposition land as a class-bearing row (class from the ADR-0004 set, status, responsible actor, concrete next reference), and did the finding actually move? Two of this record's sightings (`#6`, `#8`) carry their routing in the sighting row and got no joined row — rule on that. Rule on the routed availability row (`20260821-power-cycle-ab#1`, from `unrouted` to the goal's named follow-on), the conductor-escape row (`20260901-sustainment-fence#1`, the 262 candidate re-read wall-closed at 5.116), and the owner-ruling row (`20260903-wall-and-heating#4`).
- **The learning delta L-009..L-013** proposed in the round result — accept, correct, or reject each; be strict: a learning is one claim a later strategy should act on, not a summary. The checkpoint already corrected L-009, L-010, L-011 once; check the corrected forms in the result against the data.
- **External mutation** — `git log` over every cited path: the record was amended after commit by two Addenda (disclosed; rule on them as the study runbook's own mechanism or not); the WI-041 item record under `work/active/` after the T-003 pin; the model twins under `exploration/stellarator_e2e/models/` byte-identical to `models/` at the pin (`diff -r`); `manifest.json`, the census and fixtures re-pinned at `cb355321` — moved inside T-002 or outside it?
- **The battery.** Re-run `tests/models` and `tests/study` yourself and report the counts (the round claims 48 passed / 13 skipped and 359 passed / 1 skipped at `cb355321`; `tests/study/test_records.py` 25 passed). Also run `tests/orchestration/test_goal_contract.py` and confirm the one failure is the owner's untracked narrative.
- **The § Answered when assessment, word by word.** (b)(i): "the wall-load fence compares like with like — a computed peak operand against the printed peak limit … with the choice, its basis, and any verdict change at the baseline stated and never tuned away" — met by WI-041? (b)(ii): "one committed study at a promoted pin sweeps the levers that reach wall load in this model (the geometry pair `R` and `a`, the power-density levers `n_e0` and `T_i0`, with `I_coil` and `p_input`) and reports whether a feasible region exists at the printed 50 MW installed, its LCOE, and what the machine pays to get under the wall through the consequence chain the model already carries" — the study ran at 100 MW wall-plug, which is 50 MW coupled at the held source efficiency 0.50 and the disclosed coupling 1.00; say whether that is the contract's "printed 50 MW installed" and whether `p_input` in the contract is the coupled power. Then rule: is (b)(ii) *answered* by a conditional positive — a feasible driven region exists at 212.46 $/MWh but only at a minor radius of 1.5–2.2 m, never at the machine's own 1.3 m, conditional on the held ash-transport ratio (a knife-edge at the source's 8), an unbounded and unpriced `a`, a one-sided sustainment fence that passes ignited points (every claim made on the driven set), and the calibration's constancy over the sweep (bounded by the shadow: the region survives the external band, its price does not)? The contract's report obligation is "whether a feasible region exists, its LCOE, and what the machine pays"; say plainly whether the record discharges each of the three, and whether the conditions are disclosed facts of the model (which the contract accepts) or gaps that leave the question open (which it does not). The goal's § Close rule is the owner's: "on the § Answered when condition, or by redirect at any round boundary."

Then per § The fresh review: return `Verdict: PASS | FINDINGS | OWNER_GATE` with grounds; the checks with what you opened and the counts you recounted; the learning delta rulings in the corrected forms you would append to `learnings.md` (each with Evidence, Scope, Implication, Supersedes, Accepted-by lines, in the file's existing shape); the constraints carried forward; and **either** a recommendation that the owner close the goal on § Answered when (both halves met) with the close packet the owner rules on — the (b) ruling, WI-041 close through the modelling PM, the three unminted model candidates and the one research request, and anything you add — **or** the round-3 strategy revision in the runbook's shape (approach, assumptions, abandonment conditions, intended model increment, intended study question — no future task list), if the goal is not answered or should be re-grounded. Give your own recommendation on the close in one paragraph, with the reason, so the owner can rule from it. Do not list merging: the owner has ruled that the branch is not being merged and does not want it raised. The goal's round limit is 6 and this is round 2. Do not edit any file; return everything in your final message, in markdown, so the round agent can deposit it verbatim at `evidence/round2_review.md` and summarize it in the trail.
