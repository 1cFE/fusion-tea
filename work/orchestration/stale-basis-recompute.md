# Alignment Brief — WI-025 stale-basis-recompute

**Created:** 2026-07-18 (immutable — no stage cursor, no status, no execution log)
**Input source:** handoff `/tmp/handoff-20260718-132312.md` (2026-07-18) + Align conversation this session
**Work item:** WI-025 `STALE-BASIS Pass-Through Recompute`, registered in `work/BACKLOG.md` (P2, epic "MFE Cost Modeling — Tokamak & Stellarator", intent recorded in the epic's Deferred Decisions "STALE-BASIS pass-through recompute" entry). Standard route, entry at `/spec-model`. Related registration this Align: WI-026 `Pytest Baseline Re-Record` (trivial, P3, standalone) — deliberately **outside** WI-025 scope.

## Objective

Retire the model's last three STALE BASIS accounts by rebinding them as **forward-computed functions of the computed powers**, mirroring the 1costingFE formulas already cited in their doc comments. The three accounts (verified by grep — the complete stale set, nothing else annotated):

1. **CAS21 buildings** = $613.65 M literal (`models/designs/stellarator_09/stellarator_plant.sysml:280`) — stale basis p_et = 896.8 / p_th = 2693.1 (pre-WI-019 powers). Formula: `costs.py:83-144` building loop over `costing_constants.yaml:175-197`.
2. **CAS10 preconstruction** = $33.896 M literal (`:594`) — stale basis p_net = 575.3. Formula: `costs.py:52-80` (land ∝ sqrt(p_net·ref) + five fixed adders).
3. **CAS70 annual O&M** = $41.641 M/yr literal (`:643`) — stale basis p_net = 575.3. Formula: `costs.py:319-347`, `om_cost_dt = 54.9 M$ × sqrt(p_net/1000)`.

Executed powers at pick-up (WI-024 close, commit `72f7d054`): p_fus 2748.1, p_th 3238.1, p_et 1078.3, **p_net 915.081088 MW** — ~59% above the 575.3 basis; the largest known honesty gap in the headline. Outcome: the three accounts track the computed powers, STALE BASIS annotations retired, headline capital/LCOE re-baselined honestly, full validate/regen/runner/handshake evidence, fresh independent audit, owner close. Baseline moved from: WI-024 executed record (p_cryo 0.8643516, rec_frac 0.151362, q_eng 6.606662, LCOE $201.472065/MWh, total $12,601,519,645.07, magnet $6,323,469,946.33 / 50.2%; records in `work/completed/20260718_WI-024_recirc-power-derivation/`).

## Decision-carrying inputs, graded

- **[OWNER-VERBATIM] 2026-07-18 (this Align):** "the ultimate goal is to have *engineering parameters* -> forward pass -> *outcome attributes*"; "I don't want to invest a lot of modeling to backwards-compute costs from the outputs when that is not natural"; "for the *plant*, you could reasonably argue all the equipment is sized based on the power. that to me is forward."
- **[AGENT] (ratified by owner 2026-07-18, this Align):** the three accounts are rebound **forward-computed** as functions of the computed powers (point-recompute rejected — literals would re-stale on the next power move, as already happened five times: 575.3 → 786.1 → 578.0 → 804.1 → 915.1 → 915.081). Reasoning of record: powers are intermediate nodes, not terminal outcomes; costs never feed back into the power balance, so no cycle and no inversion; 1costingFE itself computes all three from powers; WI-021's CAS27 rebind is the in-model precedent. This supersedes the epic's recorded point-vs-forward alternative (epic WI-024 entry) — the forward variant is chosen.
- **[AGENT] 2026-07-18 (this Align, verified against `costing_constants.yaml:175-197`):** every CAS21 building is linear in exactly one scaling basis (fixed, p_fus/2300, p_th/2500, p_et/ref, p_the/ref, staff = sqrt(p_et/ref)), so the 18-building loop collapses **exactly** into ~6 grouped terms — the old "dict loop out of the codegen envelope" objection dissolves. Grouped-vs-per-building representation is a **design-stage** decision. Fuel/NOAK/module dimensions freeze at the design point (DT, FOAK, n_mod = 1) as documented constants. Note `p_the = p_et` for a no-DEC plant (`costs.py:104`); cryogenics building applies (superconducting coils, `costs.py:137`).
- **[OWNER] 2026-07-18 (this Align):** hard stop for owner checkpoint after `/spec-model`; nothing past spec runs until scope approval. Per the standing "outcomes at spec, mechanism at design" feedback ([OWNER-VERBATIM], WI-024 spec checkpoint: "spec should capture the outcomes — how the model is built should be done with the expertise of SysML modeling"), the checkpoint carries outcome-level questions only.
- **[OWNER] 2026-07-18 (this Align, by accepting the presented routing):** pytest baseline re-record is **out of WI-025 scope** (registered separately as WI-026); WI-025's operative pytest bar is tally-unchanged vs **11 failed / 18 passed / 14 skipped / 0 errors** (twice-verified environmental drift, WI-024 implement + audit).
- **[OWNER] 2026-07-18 (WI-024 spec §Checkpoint Rulings, ruling 3 — the standing successor bar):** `handshake_1costingfe.py` may be edited **only within `set_1cfe_inputs`'s injection map** (no comparison-logic change), and `git diff exploration/stellarator_e2e/handshake_comparison.json` must be empty after the run.
- **[OWNER] standing:** one item at a time through the modeling PM pipeline; no-fallbacks rule (never invent or default a missing input — surface honest options, owner decides); SV-016 (Q_eng ~10–40 band) stays `pending` — record against it, never fit it; owner holds close/archive.
- **[INHERITED: PROTOCOL]** `knowledge/holdout/aries-cs/PROTOCOL.md` §3 barred paths absolute; physics from admissible Stellaris sources only; engineering/cost from 1costingFE (`/home/reid/1cfe/1costingfe`, pinned `0254385`); every demo work item lists the PROTOCOL as Required Reading. This item's sources are 1costingFE-only (cost formulas + constants) — all admissible.
- **[INHERITED: handoff — WI-024-verified procedures and bars]** Validation `uv run agentic-mbse validate models --complete`: L1=0 over **22** files; offender list exactly the 6 pre-existing — `mfe_plant.sysml:353/359/364` (shifted by WI-024's insertion, content-identical to the old :329/335/340), `ife_plant.sysml:33/41`, `hif_plant.sysml:205`; compare the offender *list*, not level-summary flags. Regen: `~/1cfe/sysml-codegen` (HEAD `6db3212`) snapshot **with NO `--design-path-filter`** (the filter produces 8 spurious V11 offenders; unfiltered bridge reports exactly 3 — contingency/indirect/lcoe rollup keys) + `bridge_v11_generate.py` (`preserve_handwritten=True` at :108; WI-022 impl sha256 `8d2357…794a9f` must survive — content-hash check). Execute `run_stellaris.py` via `/home/reid/1cfe/fusion-tea/exploration/pipeline_spike/.venv-exec/bin/python`, bit-exact rel 1e-9. IFE regression `run_anchors.py` (252.30/68.69/270.12, Meier 4.735). `SYSIDE_LICENSE_KEY` in `~/1cfe/fusion-tea/.env`. Mirroring: staged `mfe_plant.sysml` twin has 3 known DEMO NOTE divergences (WI-015); instance twin has the viability-assert block — "twins identical" holds per-edit-region for those two. Next free SV: **SV-032**. Table extractions untrustworthy — images/raw.pdf are the witnesses (not expected to matter here: this item's sources are 1costingFE code/YAML, read directly).

## Premise caveats (recorded, not resolved)

- **Numerator blast radius.** Unlike WI-023/024 (denominator-only), this item moves **total capital and the LCOE numerator**. SV-030/SV-031 executed-value records and the runner headline asserts will need re-baselining; the handshake injects 1cfe's own refs so Anchor A is *expected* structurally unaffected — but that is re-derived at design under the successor bar, not carried.
- The handoff's "computed at p_net = 575.3" is imprecise for CAS21 buildings: its stale basis is p_et = 896.8 / p_th = 2693.1 (pre-WI-019 powers), per its own doc comment. The recompute keys each account to *its* actual scaling inputs.
- The forward-computed CAS21 must reproduce 1costingFE's loop exactly (grouping is algebraically exact, not a fit); the design proves agreement against a 1cfe-side evaluation at the executed powers.
- q_eng does not move this item (costs don't feed power) — SV-016 remains untouched; any band revisit is a future owner decision.

## Parked pending checkpoint

- Everything downstream of spec (design, plan, implement) parks until the post-spec owner checkpoint.
- Checkpoint substance (outcome-level only): confirmation of the three-account scope; what the re-baselined headline records look like (which SVs re-record, successor-bar treatment); any honesty question the spec sweep surfaces (e.g. constants that turn out unsourced or fuel-key ambiguities). Grouped-vs-per-building CAS21 representation is design's, not the checkpoint's.
