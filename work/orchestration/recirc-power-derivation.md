# Alignment Brief — WI-024 recirc-power-derivation

**Created:** 2026-07-18 (immutable — no stage cursor, no status, no execution log)
**Input source:** handoff `/tmp/handoff-20260718-110307.md` (2026-07-18)
**Work item:** WI-024 `Recirculating-power derivation model (coil/cryo parasitic loads)`, registered in `work/BACKLOG.md` (P2, epic "MFE Cost Modeling — Tokamak & Stellarator", entry in `work/backlog/epic-mfe-cost-modeling.md`). Standard route, entry at `/spec-model`.

## Objective

Replace the WI-023 stopgap (`p_tf = 0.0`, bound with the Stellaris paper's explicit parasitic-electricity deferral documented at `models/designs/stellarator_09/stellarator_plant.sysml:434`) with a modeled derivation of the coil/cryo parasitic loads. The intended shape ([OWNER] intent, epic WI-024 entry): decompose the parasitic loads and model the derivation chain — e.g. winding-pack nuclear heating density (35.5 W/m³, Table 6 image) × cold-mass/winding volume → heat load at 20 K → cryo-plant electrical via COP, plus the other parasitic splits — with **every step either sourced from admissible material or an explicit, owner-visible assumption parameter**. Subsumes the WI-018-era Stage-3 note (cold-mass → cryo-electrical COP treatment). Baseline moved from: WI-023 executed record (V 425 m³, p_fus 2748.1 MW, p_th 3238.1 MW, gross 1078.3 MW, net 915.1 MW, rec_frac 0.151, q_eng 6.609, total $12.6015B, LCOE $201.46/MWh, magnet $6.3235B / 50.2%; records in `work/completed/20260718_WI-023_magnet-field-errata-B9/`).

## Decision-carrying inputs, graded

- **[OWNER] 2026-07-18 (WI-023 spec checkpoint, recorded in epic WI-024 entry):** intent — derive/decompose and model *how* the recirculating power values are derived, instead of binding constants.
- **[OWNER] 2026-07-18 (WI-023 close-out, epic entry):** sequencing — WI-024 runs **before** the STALE-BASIS pass-through recompute, so the recompute happens at a settled p_net. Rejected alternative recorded in the epic entry (one line), remains an option at the recompute item's design.
- **[OWNER] 2026-07-18 (WI-023):** `p_tf` rebound to 0 (option b, no-fallbacks); 1costingFE `p_coils = 3.0 MW` (`src/costingfe/data/defaults/steady_state_stellarator.yaml:19`) **rejected** for the binding — generic 5.5 m-reference default, unrepresentative of a 12.7 m / 111 GJ coil set.
- **[OWNER] 2026-07-18 (this Align):** hard stop for owner checkpoint after `/spec-model`; nothing past spec runs until scope approval. Reserved at that checkpoint: (a) derivation scope — cryo-electrical only filling p_tf's slot vs. full parasitic decomposition touching other slots; (b) which inputs may be explicit assumption parameters (e.g. 20 K COP, winding-pack volume basis) vs. must be sourced; (c) the double-counting ruling once the slot map is on the table.
- **[OWNER] 2026-07-18 (recorded in validation matrix):** SV-016 (Q_eng ~10–40 band) stays `pending` until WI-024 lands a derived parasitic power; do not adjust the band to fit the provisional q_eng 6.609.
- **[OWNER] standing:** one item at a time through the modeling PM pipeline; no-fallbacks rule (never invent or default a value for a missing input — surface honest options, owner decides); owner holds close/archive.
- **[AGENT] (WI-023 sweep, verified against the in-repo Stellaris PDF and page images — authority record: `work/completed/20260718_WI-023_magnet-field-errata-B9/spec.md` §Sweep Findings):** no admissible source prints a total coil/cryo parasitic power in MW; the Stellaris paper defers parasitic electricity explicitly (conclusion, PDF-confirmed). Its only coil-thermal figures: **35.5 W/m³ mean winding-pack cryogenic nuclear heating** (Table 6 image `page_020_table_0.png` + §2.8; magnets at 20 K; EU DEMO ~50 W/m³ as reference) and **~7.5 kW steady-state joint losses** (§2.9, "negligible"). Converting 35.5 W/m³ to electrical power needs a winding-pack volume (not printed) and a 20 K COP (not given).
- **[AGENT] (WI-023, verified):** Table 2 image (`page_002_table_0.png`) magnet facts: 48 TF coils, peak conductor field 24.9 T, peak coil current 15.4 MA, stored magnetic energy 111 GJ, axis-averaged B₀ 9.0 T. Table 2 prints V = 428 m³ where Table 5 prints 425 (bound value).
- **[AGENT] (WI-023, verified):** W7-X cryo figures (iter-02 sources: ~7 kW at 4.5 K equivalent, 425 t cold mass) are **inadmissible as values** (owner no-fallbacks; 4 K NbTi experiment vs 20 K HTS plant) — may inform the *shape* of a derivation only.
- **[AGENT] (WI-023, verified):** a p_tf change's blast radius is recirc sum → p_net → q_eng → LCOE denominator; capital was p_tf-invariant. The instance currently binds 1costingFE-sourced neighbors p_tfcool 15.0 MW and p_cryo 0.8 MW (plus p_pump, p_trit, p_house, f_sub from the same defaults file) — these carry some coil-adjacent load already; the derivation must map which slots it replaces vs coexists with (double-counting risk).
- **[INHERITED: handoff — WI-023-verified procedures and bars]** validation `uv run agentic-mbse validate models --complete`: L1=0 over 21 files, offender set exactly the 6 pre-existing (`mfe_plant.sysml:329/335/340`, `ife_plant.sysml:33/41`, `hif_plant.sysml:205`), zero new; compare the offender *list*, not level-summary flags (hif_plant offender prints under L2). Regen via `~/1cfe/sysml-codegen` snapshot + `bridge_v11_generate.py` with `preserve_handwritten=True` (line 108) — WI-022 handwritten reactivity impl must survive (content-hash check pattern in WI-023 plan); regen resets 3 bridge keys + 4 BOP wirings, runner glue re-applies — diff `generated/` after a run, not straight after regen. Execute `run_stellaris.py` via `/home/reid/1cfe/fusion-tea/exploration/pipeline_spike/.venv-exec/bin/python`, bit-exact rel 1e-9. Handshake `handshake_1costingfe.py` unedited, then `git diff exploration/stellarator_e2e/handshake_comparison.json` empty — but it injects `pb__p_tf` (line 216) and `magnet__B` (line 271) from 1cfe's own refs, and a *new calc structure* could change the injection surface: **re-derive handshake safety at design, don't carry it**. IFE regression `run_anchors.py` (252.30/68.69/270.12, Meier 4.735). Pytest baseline red (2 failed + 18 errors, pre-existing) — tally-unchanged is the bar. Next free SV: **SV-031**. `SYSIDE_LICENSE_KEY` in `~/1cfe/fusion-tea/.env`.
- **[INHERITED: PROTOCOL]** `knowledge/holdout/aries-cs/PROTOCOL.md` §3 barred paths absolute; physics from admissible Stellaris sources only; engineering/cost from 1costingFE (`/home/reid/1cfe/1costingfe`, pinned `0254385`); every demo work item lists the PROTOCOL as Required Reading.
- **[INHERITED: handoff]** the published Stellaris PDF is in-repo and admissible: `knowledge/concept_research/09-qi-stellarator-hts/iter-02/sources/publikationen-1000179851-172386752/tmpissrtbos/raw.pdf` (Lion et al., FED 2025), registered in `knowledge/SOURCE_INDEX.md`. **Table values must be verified against page images or this PDF — both text extractions (iter-01 and iter-02, same lineage) have corrupted/invented table rows.**

## Premise caveats (recorded, not resolved)

- The derivation chain has two links with no admissible printed value: winding-pack (or cold-mass) volume, and a 20 K cryo-plant COP (or specific power, W_elec/W_cold). The spec must surface where each link's number can honestly come from — sourced, computed from sourced geometry, or an explicit owner-approved assumption parameter — as options, not pick one silently.
- The handshake-safety argument from WI-023 (instance rebinds can't leak into Anchor A) does not automatically extend to a new calc structure; it is re-derived at design.
- Table extractions are untrustworthy; images/PDF are the witnesses.

## Parked pending checkpoint

- Everything downstream of spec (design, plan, implement) parks until the post-spec owner checkpoint.
- Derivation scope (cryo-only vs full decomposition), parameter-vs-source rulings per link, and the double-counting resolution are the checkpoint's substance — surfaced by the spec, decided by the owner.
