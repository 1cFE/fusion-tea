# Audit — WI-027 Demo Constraint Execution (STELLARATOR-DEMO Item 2)

**Verdict: POSITIVE** (with one honestly-surfaced, out-of-scope bar and minor traceability notes).
**Scope:** work-item audit, `work/active/WI-027_demo-constraint-execution/`, against its own spec (MR-WI027-1…8), design (D1–D7 + Item-10 supersession), and plan Implementation Record (commit `35c1caee`, stage-logged at `d41b2db5`).
**Posture:** reproduce, don't trust. Every bar below was re-run or re-derived from the executed artifacts, not read back from the record.
**Date:** 2026-07-20. **Auditor:** independent audit stage (`/audit-models`), non-interactive.
**Protocol:** `knowledge/holdout/aries-cs/PROTOCOL.md` §3 barred paths were not read, cited, or opened during this audit. No barred artifact was touched. Surfaces used: `models/`, `exploration/stellarator_e2e/`, `exploration/ife_e2e/`, the item record, and the three toolchain repos.

---

## Executive summary

WI-027's claim — the five already-modeled viability asserts execute through the generated demo pipeline and emit verdicts as report data, with no hand-coded viability rule and zero movement of any established number — **reproduces in full**. I re-ran the executed runner (`run_stellaris_single.py`) and the handshake and IFE anchors from a clean tree; the constraint-report fingerprint, all five verdicts and margins, every numeric channel (oracle reldev 0.00e+00), the six headline anchors to the cent, the empty handshake diff, the WI-022 hash, the pytest tally, and the MR-2 grep all match the Implementation Record and SV-033 exactly.

The single non-passing bar (IFE anchors, MR-5.3) is **honestly characterized**: I reproduced Runs A/B byte-exact and Run C crashing, and confirmed the crash is a teax-`07eb0ac`-validator-vs-committed-HIF-package skew (the `# NEW: pass channel_types` line), not a WI-027 effect — the IFE/HIF package is git-clean and was last touched by WI-019. The item correctly left SV-033 `pending` and routed this to the lifecycle epic rather than tuning it away.

Canonical `models/` was not edited by WI-027 (D7 was superseded by Item 10 / owner decision D-2 and never landed); the item's only functional source edit is the runner (`+29/−2`), confined to `exploration/stellarator_e2e/`. MR-8's amended bound holds.

**Reproduction environment note.** The plan's prescribed interpreter `exploration/pipeline_spike/.venv-exec/bin/python` does not exist in this worktree. I reproduced with the project `uv` environment plus teax-simkit on `sys.path`/`PYTHONPATH` (`/home/reid/1cfe/teax/packages/teax-simkit`). Every numeric result matched to the last digit, so the environment substitution did not affect outcomes — but the recipe as literally written is not runnable and should be corrected in the record before close.

---

## Toolchain state found (recorded per MR-WI027-6)

| Component | Recorded candidate | Found at audit | Reconciliation |
|---|---|---|---|
| sysml-codegen | `06d95f8` (Item 10 certified at `1c85042`) | HEAD `3370e60` on `constraint-exec-epic` | `06d95f8` is an ancestor of `3370e60`; `git diff 06d95f8 3370e60` is **docs-only** (one `design-review.md`, +246). `1c85042` is an ancestor of `06d95f8`. Candidate faithfully reproducible. |
| agentic-mbse | `4c18d61` | `4c18d61` | exact |
| teax | `07eb0ac` | `07eb0ac` | exact |

The stellarator package under audit was generated by Item 10 at `1c85042` and committed; the runner only *executes* it via teax, so run reproduction depends on teax (`07eb0ac`, matched) + the committed package + interpreter, all present. **Note:** the recorded pin `06d95f8` is no longer HEAD; the delta to HEAD is documentation only, so equivalence holds, but the record pins a commit that has been superseded by two Item-11 doc commits.

---

## Per-bar reproduction results

Run reproduced: `exploration/stellarator_e2e/run_stellaris_single.py`, clean tree at `d41b2db5`. The run mutated nothing tracked (`git status` clean afterward; the `special_materials_capital` injection is idempotent against the committed inputs — confirmed byte-identical, git clean).

### Bar 1 — Five verdicts as report data (MR-1/4) ✅ REPRODUCED
`constraint_report.json`: `headline=all_satisfied`, `assessed_count=5`, `catalog_fingerprint=c565283a88599da6a2186b4092dc3eed9574a9870458cc45a76eabaeac4cdf2f` — **exact match** to the record, identical across all report copies. Five `status=satisfied` rows, all off-boundary, margins reproduced to the digit:

| constraint | observed | bound | margin (reproduced) | record |
|---|---|---|---|---|
| beta_ok | beta 0.0276 | ≤ 0.05 | +0.02240 | +0.0224 ✓ |
| net_positive | net_electric 915.0810878595104 | > 0 | +915.081 | +915.08 ✓ |
| recirc_ok | rec_frac 0.1513623734691124 | ≤ 0.5 | +0.34864 | +0.3486 ✓ |
| tbr_ok | tbr 1.074 | ≥ 1.05 | +0.02400 | +0.024 ✓ |
| wall_load_ok | wall_load 3.131234717504045 | ≤ 4.05 | +0.91877 | +0.9188 ✓ |

Five per-constraint modules present (`generated/modules/stellarator_09/stellaris{betaok,netpositive,recircok,tbrok,wallloadok}constraintmodule.py`), plus `constraints/predicates.py`, `constraintreportaggregatormodule.py`, `schemas/constraint_types.py`. Satisfies MR-WI027-1, -3, -4.

### Bar 2 — Oracle bit-exact rel 1e-9 (MR-5.1) ✅ REPRODUCED
Every executed channel `reldev = 0.00e+00` vs `verify_stellaris.py`: total_capital, lcoe, p_net, q_eng, rec_frac, direct_capital. Exceeds the < 1e-9 bar.

### Bar 3 — Headline to the cent (MR-5.1) ✅ REPRODUCED
total $12,638,857,665.74; LCOE $203.647152/MWh; p_net 915.081088 MW; q_eng 6.606662; rec_frac 0.151362; magnet $6,323,469,946.33 (50.03%). All match the WI-025 baseline — no re-baseline.

### Bar 4 — Handshake empty diff vs `342cc799` (MR-5.2) ✅ REPRODUCED
Ran `handshake_1costingfe.py` (teax on `PYTHONPATH`); it rewrote `handshake_comparison.json` and `git diff` afterward is **empty** — byte-identical regeneration. `git diff 342cc799 d41b2db5 -- handshake_comparison.json` is also empty (WI-027's commit did not touch it; last modified by Item 10 `c2f10960`). No `handshake_1costingfe.py` edit by WI-027. The committed comparison carries Item-10's honestly-shown single-pass simplification (total −41.70%, lcoe −30.65% vs 1costingFE); the six anchors are unaffected. Original successor bar held.

### Bar 5 — IFE anchors (MR-5.3) ⚠ SURFACED — honestly characterized, REPRODUCED
Ran `exploration/ife_e2e/run_anchors.py`:
- **Run A (Hawker):** LCOE **252.29996307 OK**, f_recirc 0.04166667 OK — byte-exact.
- **Run B (realistic HIF):** LCOE **68.69020165 OK** — byte-exact.
- **Run C (Osiris HIF full pipeline; the 270.12117794 + Meier 4.735 anchor):** **crashes** with `PipelineValidationError: Module 'hif_plant_pkg__hif_plant__meier_reactor_cost_calc' input 'thermal_power_gw': Type 'HifPlantParams' has no field '…meier_reactor_cost_calc__thermal_power_gw'`.

The characterization checks out on every point:
- **Crash is teax-validator skew, not a WI-027 effect.** The traceback terminates at `pipeline_validator.py:73` on the line commented `# NEW: pass channel_types` — the teax `07eb0ac` change (lifecycle Item 9 era). The validator now demands a module-scoped channel name the pre-existing committed HIF package (generated at codegen `2286e5aa`) doesn't carry.
- **IFE/HIF package git-clean.** `git status exploration/ife_e2e/` clean; last commit touching it is `0ccd0eda` (WI-019), well before WI-027. This item regenerated only the stellarator package.
- **Regression intent met.** The bar's purpose — "WI-027 did not disturb the IFE package" — is satisfied (package untouched, Runs A/B byte-exact). The literal Run-C anchor is unmeasurable at this candidate because of the pre-existing HIF/teax skew, correctly routed to the lifecycle epic. Honest.

### Bar 6 — L1 offender list = 6 pre-existing (MR-5.4) ✅ REPRODUCED (parse) + consistent by inspection
`agentic-mbse validate --level 1 models` → 22 files, **0 errors, 0 warnings**. Canonical `models/` is git-clean; staged `stellarator_plant.sysml` is byte-identical to canonical and staged `mfe_plant.sysml` differs only by two Item-10 explanatory comment blocks (not viability, not WI-027). The recorded V11 offender list — `mfe_plant.sysml:389/395/400` (powercore/bop/direct_capital cross-part derived `Real =` expressions), `ife_plant.sysml:33/41`, `hif_plant.sysml:205` — is present and consistent by code inspection; the five constraint asserts read covered operands and add none.
*Limitation:* I did not independently re-derive the offender list via a fresh `sysml-codegen snapshot` (that would mutate the committed snapshot and requires pinning the codegen commit). Bar 6 is reproduced at the L1-parse + structural-inspection level plus the end-to-end run succeeding, which is strong but not a from-scratch offender enumeration.

### Bar 7 — pytest tally (MR-5.6) ✅ REPRODUCED exactly
`uv run pytest tests/models/ -q` → **11 failed / 18 passed / 14 skipped, 0 errors**. Matches. (Whole-repo `pytest` still shows the pre-existing `simkit`-import collection errors in generated-package tests that need the exec env; the operative model-test scope is `tests/models/`, as recorded.)

### Bar 8 — MR-2 grep zero (no hand-coded viability) ✅ REPRODUCED
Both design-named greps over `exploration/stellarator_e2e/ --include=*.py` return **zero** matches (exit 1):
- `(p_net|net_electric|rec_frac|beta|wall_load|tbr)\s*[<>]=?[^=]` → 0
- `\b(viable|is_viable|passes_viability)\b` → 0

The added runner code is `status == "satisfied"` string equality (verdict parity), not an operand-vs-bound comparison. `verify_stellaris.py` is a pure numeric mirror (its only relational operators are Simpson-rule index bounds and a `sigma_v > 0` guard). Satisfies MR-WI027-2.

### Supporting — WI-022 handwritten-impl hash ✅ REPRODUCED
`generated/handwritten/mfe_plasma_scaling/dt_fusion_power_impl.py` sha256 = `8d23574793c34314c67b68b8e0f4ec438d3e2d9f92060eb4094a8d46c0794a9f` = `8d2357…794a9f`, before and after the run. `preserve_handwritten` survived.

---

## SV-033 record vs executed artifacts

SV-033 (`modeling_project/VALIDATION_MATRIX.md`) is a **single entry** whose executed-record block matches the artifacts I reproduced on every field: fingerprint `c565283a…`, five satisfied off-boundary rows with the exact margins, oracle reldev 0.00e+00, headline to the cent, handshake empty diff vs `342cc799`, L1=0 over 22 files with the 6-offender list, WI-022 hash, pytest 11/18/14/0, MR-2 greps zero, and the IFE Run A/B byte-exact / Run C surfaced skew. Status correctly `pending` (one standing bar, SV-023/IFE, did not fully hold), consistent with the plan's Phase-5 "passing only if all bars held" rule. **No discrepancy.**

---

## MR-WI027-8 amended-bound check (canonical edits)

- **WI-027 commit `35c1caee` touches no `models/`.** Changed files: `run_stellaris_single.py` (+29/−2), `VALIDATION_MATRIX.md` (SV-033), `plan.md` (records), `uv.lock` (2 lines). D7 (canonical passthrough rewiring) was superseded by owner decision D-2 / lifecycle Item 10 and never landed — verified: canonical `stellarator_plant.sysml` carries the asserts reading design attributes directly, git-clean.
- **The canonical edits were Item 10's**, not WI-027's (the design amendment and the `0a8add96..342cc799` history record this).
- **Runner numstat is exactly `+29/−2`**, matching the commit-message claim of "the only source edit."
- **One note against "only source edit":** the commit also bumped `uv.lock` by 2 lines (an `agentic-mbse` package hash in the lockfile). This is a lockfile artifact, not a model/harness-logic edit, so it does not breach MR-8's semantic bound — but the "only source edit = runner" phrasing is literally true only if `uv.lock` is treated as generated metadata (which it is). Benign; flagged for completeness.

MR-8 amended bound (zero numeric movement, no viability-semantics change, canonical adaptation confined to Item 10 / staged surface) **holds**.

---

## MR-XXX verdicts

| Requirement | Verdict | Evidence |
|---|---|---|
| MR-WI027-1 (verdicts execute + reported) | **PASS** | Bar 1 — five verdicts as `ConstraintReport` data |
| MR-WI027-2 (no hand-coded viability) | **PASS** | Bar 8 — both greps zero; oracle numeric-only |
| MR-WI027-3 (staged strip retired) | **PASS** | Asserts live at canonical line numbers; staged stellarator byte-identical to canonical |
| MR-WI027-4 (all five satisfied, else surface) | **PASS** | Bar 1 — all satisfied, off-boundary; parity assertion enforces |
| MR-WI027-5 (standing bars) | **PASS on 5/6 sub-bars; 5.3 SURFACED** | 5.1 oracle ✓, 5.2 handshake ✓, 5.3 IFE ⚠ (out-of-scope skew, honest), 5.4 offender list ✓, 5.5 WI-022 hash ✓, 5.6 pytest ✓ |
| MR-WI027-6 (pin recorded/reconciled) | **PASS (with note)** | Candidate `06d95f8` recorded; found HEAD `3370e60` = `06d95f8` + docs-only. Pin superseded but equivalent. |
| MR-WI027-7 (defect-register check) | **PASS** | Design recorded 12/12 NOT-HIT; execution shows no inversion (all satisfied, correct margins, off-boundary). Lifecycle remediation closed the literal-actual gate. |
| MR-WI027-8 (canonical models untouched by this item) | **PASS** | Commit touches no `models/`; D7 never landed; canonical git-clean |

**Spec Success Criteria:** verdicts as data ✓, no hand-coded viability ✓, staged strip retired ✓, toolchain pin recorded + standing bars (5/6, IFE surfaced) ✓, defect-register check recorded ✓, canonical untouched ✓, item record complete ✓ (SV-033 `pending` awaiting owner close).

---

## Findings and recommendations

**No correctness defect found.** The one non-passing bar is out of WI-027's scope and honestly reported. Minor items for the owner/orchestrator before close:

1. **Pin drift (low).** SV-033 and the record pin sysml-codegen `06d95f8`, but the checked-out HEAD is `3370e60` (two Item-11 *doc* commits later). The delta is documentation only, so reproducibility is preserved — but the pin cite is stale. Either checkout `06d95f8` for the archival record or note the equivalence explicitly in SV-033.
2. **Execution recipe not literally runnable (low).** `exploration/pipeline_spike/.venv-exec/bin/python` does not exist; reproduction requires the project `uv` env + teax-simkit on `PYTHONPATH`. Results are identical, but the recorded recipe should be corrected so a future auditor can re-run it verbatim.
3. **`uv.lock` in the "only source edit" commit (informational).** The WI-027 commit bumped `uv.lock` (2 lines) alongside the runner. Benign lockfile metadata; noted only because the commit narrates the runner as the sole source change.
4. **Offender list not re-enumerated from scratch (disclosure).** Bar 6 is reproduced at L1-parse + code-inspection + successful end-to-end run, not via a fresh codegen snapshot recapture (which would mutate committed state). Confidence is high given canonical `models/` is git-clean and the run generated coherently, but this is short of an independent offender enumeration.

**MR-WI027-2 PR-XXX promotion:** the item recommends PROMOTE ("no hand-coded viability rule anywhere in a demo pipeline, grep-provable"). Concur — it is a durable, enforceable (grep-terms) methodology rule now satisfied by both the IFE and stellarator demos. Left for owner/PR-tracking to action, not self-registered.

**Close posture:** per Align ruling 2 the owner holds close. This audit is POSITIVE on the in-scope deliverable (MR-1/2/3/4/6/7/8 pass; MR-5 passes 5/6 with 5.3 surfaced out-of-scope). SV-033 is correctly `pending`; the sole open decision is the bar-5.3 disposition (accept out-of-scope / regression-intent-met, or resolve the HIF/teax skew before close) — an orchestrator/owner call, not a WI-027 defect.

---

## Audit metadata

- **Item:** `work/active/WI-027_demo-constraint-execution/` (spec/design/plan).
- **Repo HEAD:** `d41b2db5` (stage-log); implement commit `35c1caee`; Item-10 baseline `342cc799`.
- **Reproduced runs:** `run_stellaris_single.py` (verdicts + oracle + anchors), `handshake_1costingfe.py` (empty-diff), `run_anchors.py` (IFE A/B/C), `agentic-mbse validate --level 1 models`, `pytest tests/models/`, MR-2 greps, WI-022 sha256.
- **Toolchain:** sysml-codegen `06d95f8`↔HEAD `3370e60` (docs-only delta) / agentic-mbse `4c18d61` / teax `07eb0ac`.
- **Thresholds:** oracle rel < 1e-9 (PASS); constraint verdicts string-exact; anchors to recorded precision.
- **Protocol:** ARIES-CS §3 barred paths not touched. Tree clean after audit (only pre-existing `.orchestrate-logs/` untracked).

**Overall verdict: POSITIVE** — all eight bars reproduce; the one surfaced bar (IFE Run C) is out of scope and honestly characterized; canonical models untouched by WI-027; SV-033 accurate and correctly pending. Ready for owner close ruling on the bar-5.3 disposition.
