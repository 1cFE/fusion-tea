[project]
name = "fusion-tea"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "agentic-mbse[extract-full]",
    "sysml-codegen",
    "teax-simkit",
    "pytest>=8.0",
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "httpx>=0.28.1",
    "matplotlib>=3.10.8",
    "numpy>=2.4.0",
    "pyzotero>=1.10.0",
    "python-dotenv>=1.2.1",
    "graphviz>=0.21",
]

[tool.uv.sources]
teax-simkit = { path = "../teax/packages/teax-simkit", editable = true }
agentic-mbse = { path = "../agentic-mbse", editable = true }
sysml-codegen = { path = "../sysml-codegen", editable = true }
