VERDICT: FINDINGS

### F-1: Refactor eta_th=0.85 workaround to canonical pulsed-conversion wiring
- **Target:** `model_setup.py` lines 252-337 (the MIF power balance kwargs passed to `model.forward(...)`), with primary focus on the `eta_th=0.85` block at lines 266-286 (DEVIATION block + multi-source rationale); secondary touch-ups in the header docstring (lines 33-42, 49-66) and the print-output blocks at lines 441 and 470 that propagate the "eta_th proxy" framing into user-facing output.
- **Category:** model
- **Finding:** `eta_th=0.85` stuffs Helion's direct electromagnetic recovery efficiency into the framework's thermal-cycle parameter, with `f_dec` implicitly left at the framework default (0). This is the exact double-count pattern issue #30 fixed structurally for 37 other concepts; the in-file DEVIATION block at lines 266-273 explicitly flags it as debt pending the structural refactor. The file's header (lines 34-37) and the print blocks at lines 441, 470 still cite `eta_th=0.90` while the actual value is 0.85 — arithmetic drift that compounds the workaround's confusion. Helion's physics is pure inductive direct conversion (no steam cycle, no LiPb blanket); the framework already has a `PulsedConversion` mode whose forward function expresses that topology natively. Goal connection: standardization of energy-conversion parameters per scoring_framework.md §"Energy capture efficiencies", with carve-out for concepts whose physics demands a non-thermal forward path.
- **Recommendation:**
  1. Read these costingfe sources before editing (these are the only inputs needed to choose the mode and wire the parameters):
     - `costingfe/types.py:28-30` — `PulsedConversion` enum (only two modes: `THERMAL`, `INDUCTIVE_DEC`).
     - `costingfe/types.py:51-62` — `CONCEPT_DEFAULT_CONVERSION` map. Confirm what mode `PULSED_FRC` defaults to.
     - `costingfe/layers/physics.py:533-655` — `pulsed_thermal_forward` signature and body. Note the kwargs it accepts (`eta_th`, `eta_de`, `f_dec`, `mn`, ...).
     - `costingfe/layers/physics.py:754-880` — `pulsed_dec_forward` signature and body. Note that this function takes `eta_dec` and `f_pdv` (not `eta_de`/`f_dec`) and that the recovered fraction includes driver-circuit energy ("cap bank → coils → plasma → coils → cap bank").
     - `exploration/concept_analysis/analyses/23-laser-icf-nanostructured-target/model_setup.py:33-46, 195-235` — precedent that uses `pulsed_conversion=PulsedConversion.INDUCTIVE_DEC` and calls `pulsed_dec_forward` for the DEC variant.
  2. Decide the `pulsed_conversion` mode for concept 08 based on which forward function's topology matches Helion's pure-inductive recovery physics (no thermal cycle, ~5% neutron deposition into walls, ~95% charged-particle energy recovered via expanding-FRC inductive pickup into the cap bank). The choice MUST be supported either by costingfe's source above or by an explicit "neither mode fits — surfacing as costingfe gap" outcome (see spec FR-11). Do not guess.
  3. Apply the structural fix. Add `pulsed_conversion=PulsedConversion.<MODE>` to the `model.forward(...)` kwargs (import `PulsedConversion` from `costingfe`). Rewire the conversion-efficiency kwargs to match the forward function's signature:
     - `eta_th` MUST carry thermal-cycle efficiency (zero if no thermal cycle).
     - Depending on the mode chosen, also wire either (`eta_de`, `f_dec`) or (`eta_dec`, `f_pdv`) per the relevant function's signature.
     - Source the wired values from the existing rationale comments (currently lines 274-286) — do not re-derive from external research. The numerical sourcing already done for the 0.85 / 0.90 / 0.70 range is the basis; choose one consistent value and reflect it in both the kwarg and any surviving narrative comments.
  4. Add an inline comment immediately above `pulsed_conversion=...` justifying the mode choice in one or two sentences, naming the costingfe source line that backs the choice.
  5. Remove the DEVIATION block at lines 266-273 entirely (the workaround it documents no longer exists). Keep the multi-source value-rationale comments at lines 274-286 to the extent they remain accurate after rewiring; rewrite the prose that still describes "eta_th here is a hack." Preserve unrelated kwargs (`p_driver`, `mn`, `eta_p`, `eta_pin`, `f_sub`, `p_pump`, `p_trit`, `p_house`, `p_cryo`, `p_target`, `p_coils`, geometry block), all `cost_overrides`, the `eta_th_overrides` block, and all unrelated DEVIATION comments. Do not touch any file outside `analyses/08-frc-w-direct-conversion/`.
  6. Update the header docstring (lines 33-42, 49-66) so the "Modeling approach" prose and the "Power balance" bullet list reflect the new wiring — no surviving "eta_th = 0.90" or "eta_th=0.85" if those are no longer the values, no surviving "thermal efficiency mimics the round-trip circuit efficiency" if the wiring now expresses that physics directly. Update the print blocks at lines 441 and 470 to match.
- **Priority:** blocking

### F-2: Reconcile analysis.md narrative with the new conversion wiring
- **Target:** `analysis.md` Sections 2 (Challenges), 3 (Energy Capture / Power Conversion), 5 (Parameters). Specifically: any prose that frames `eta_th=0.85` (or 0.90) as "direct EM recovery via eta_th proxy", any prose that asserts the THERMAL forward path is being used as a workaround, and any parameter-row entry that cites `eta_th` for the EM recovery efficiency rather than the (now distinct) parameter that carries it.
- **Category:** analysis
- **Finding:** Issue #30's verifier sweep flagged 08 for narrative drift around the conversion wiring. The narrative currently agrees with the workaround framing — it cites `eta_th` as the EM-recovery parameter. After the F-1 refactor lands, the model wiring will name the recovery efficiency via whichever parameter the chosen `pulsed_conversion` mode's forward function uses. The narrative must agree with the new wiring or downstream readers (and the verifier) will continue to flag conversion-related drift on 08. Goal connection: analysis-vs-model consistency per analysis_goals.md and scoring_framework.md §"Energy capture efficiencies".
- **Recommendation:**
  1. After completing F-1, re-read analysis.md Sections 2, 3, 5. Identify every passage that references `eta_th`, "thermal cycle", "direct EM recovery", or the 0.85/0.90 numeric values in the context of conversion efficiency.
  2. Rewrite those passages to reflect the new wiring: the model now uses `pulsed_conversion=<chosen mode>`, the recovery efficiency is carried by `<chosen parameter>`, and `eta_th` carries thermal-cycle efficiency (zero, given Helion has no thermal cycle).
  3. Keep all other narrative untouched — sourcing prose, challenge descriptions, gap notes, the Section 5 table entries for non-conversion parameters. This finding is scoped to conversion-related narrative; the verifier flagged other unrelated drift on 08 that rides along with the P2 batch (out of scope here).
- **Priority:** important

### F-3: Update user-facing print output to match new wiring
- **Target:** `model_setup.py` lines 441 and 470 (and any neighboring print statements that propagate the "eta_th proxy" or "0.90" framing into the model_output.txt artifact consumed by synthesize).
- **Category:** model
- **Finding:** Two print statements explicitly hard-code "eta_th proxy" framing into the model output that synthesize and downstream consumers see. Line 441 prints `f"Direct EM conversion eff (eta_th proxy): {0.90:.0%}"` and line 470 prints `"eta_th=0.90 approximates direct EM recovery..."`. Both will continue describing the (now-removed) workaround in synthesize-visible output unless updated alongside F-1. Goal connection: traceability of model parameters to printed artifacts.
- **Recommendation:** As part of F-1's edit pass, rewrite these print statements (and any neighbors with the same framing) so the labels and values match the new wiring. If `eta_th` is now zero, the print line should name the parameter that actually carries EM recovery efficiency, not `eta_th`. Keep the cost-breakdown and CAS-overrides print blocks untouched.
- **Priority:** important
