The critic review has been written to `critic_review_20260604-184205.md`. 

Key findings:

1. **Promotion gap, not analysis gap** — `iter-1/model_setup.py` exists, passed all pipeline validators, and received PASS from the assessor. It just was never copied to the concept root, so all four deterministic checks errored. The fix is mechanical: promote the file and run it.

2. **C220104 (piston driver at 15% of generic)** is the dominant sensitivity lever — a qualitative fraction with no quantitative anchor that likely determines whether this concept looks cheap or expensive.

3. **CAS27 implicitly selects lithium over LLE** despite the analysis flagging the material choice as unresolved, creating a 4.6× range in that account.

4. **Three fraction-based overrides (15%, 10%, 40%)** collectively define the concept's cost differentiation but rest entirely on adjective-level reasoning with no published cost data behind any of them.