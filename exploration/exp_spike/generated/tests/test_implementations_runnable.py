# Generated by scripts/codegen/tests.py - DO NOT EDIT
"""Verification tests for exp_toy_tea physics implementation stencils.

Tests verify that handwritten implementation functions:
1. Can be imported without errors
2. Accept correct Input model types
3. Return correct output types (float or tuple)
4. Tolerate NotImplementedError (before agent implements)
5. Validate return types (after agent implements)

Generated from 3 calculation definitions.
"""

import importlib

import pytest
from pydantic import BaseModel


def create_dummy_input(input_class: type[BaseModel]) -> BaseModel:
    """Create dummy input with sample values based on field types.

    Type Mapping:
        float -> 1.0
        int -> 1
        str -> "sample"
        bool -> True
    """
    fields = input_class.model_fields
    dummy_data = {}

    for field_name, field_info in fields.items():
        field_type = field_info.annotation
        if field_type is float or "float" in str(field_type).lower():
            dummy_data[field_name] = 1.0
        elif field_type is int or "int" in str(field_type).lower():
            dummy_data[field_name] = 1
        elif field_type is str or "str" in str(field_type).lower():
            dummy_data[field_name] = "sample"
        elif field_type is bool or "bool" in str(field_type).lower():
            dummy_data[field_name] = True
        else:
            dummy_data[field_name] = 1.0

    return input_class(**dummy_data)


# Generated test classes for each module

class TestBoschHaleReactivityRunnable:
    """Verify boschhalereactivity implementation runs without error.

    SysML Source: models/exp_toy.sysml:39
    """

    def test_import_and_run(self):
        """Test that run_boschhalereactivity can be imported and called."""
        # Import implementation module (ADR-003: namespaced path)
        impl = importlib.import_module("exp_toy_tea.handwritten.exp_toy.boschhalereactivity_impl")
        func = getattr(impl, "run_boschhalereactivity")

        # Import module wrapper for Input schema (ADR-003: namespaced path)
        module = importlib.import_module("exp_toy_tea.modules.exp_toy.boschhalereactivity")

        # Find Input class
        input_class = None
        for attr_name in dir(module):
            if attr_name.endswith("Input") and not attr_name.startswith("_"):
                candidate = getattr(module, attr_name)
                if isinstance(candidate, type) and issubclass(candidate, BaseModel):
                    input_class = candidate
                    break

        assert input_class is not None, "No Input class found in module"

        # Create dummy input
        dummy_input = create_dummy_input(input_class)

        # Call function - expect NotImplementedError or valid return
        try:
            result = func(dummy_input)

            # If implemented, verify return type
            assert isinstance(result, (float, int)), f"Expected number, got {type(result)}"

        except NotImplementedError:
            # Expected for stencils - test passes
            pass

class TestGainDoublingsRunnable:
    """Verify gaindoublings implementation runs without error.

    SysML Source: models/exp_toy.sysml:53
    """

    def test_import_and_run(self):
        """Test that run_gaindoublings can be imported and called."""
        # Import implementation module (ADR-003: namespaced path)
        impl = importlib.import_module("exp_toy_tea.handwritten.exp_toy.gaindoublings_impl")
        func = getattr(impl, "run_gaindoublings")

        # Import module wrapper for Input schema (ADR-003: namespaced path)
        module = importlib.import_module("exp_toy_tea.modules.exp_toy.gaindoublings")

        # Find Input class
        input_class = None
        for attr_name in dir(module):
            if attr_name.endswith("Input") and not attr_name.startswith("_"):
                candidate = getattr(module, attr_name)
                if isinstance(candidate, type) and issubclass(candidate, BaseModel):
                    input_class = candidate
                    break

        assert input_class is not None, "No Input class found in module"

        # Create dummy input
        dummy_input = create_dummy_input(input_class)

        # Call function - expect NotImplementedError or valid return
        try:
            result = func(dummy_input)

            # If implemented, verify return type
            assert isinstance(result, (float, int)), f"Expected number, got {type(result)}"

        except NotImplementedError:
            # Expected for stencils - test passes
            pass

class TestExpControlRunnable:
    """Verify expcontrol implementation runs without error.

    SysML Source: models/exp_toy.sysml:62
    """

    def test_import_and_run(self):
        """Test that run_expcontrol can be imported and called."""
        # Import implementation module (ADR-003: namespaced path)
        impl = importlib.import_module("exp_toy_tea.handwritten.exp_toy.expcontrol_impl")
        func = getattr(impl, "run_expcontrol")

        # Import module wrapper for Input schema (ADR-003: namespaced path)
        module = importlib.import_module("exp_toy_tea.modules.exp_toy.expcontrol")

        # Find Input class
        input_class = None
        for attr_name in dir(module):
            if attr_name.endswith("Input") and not attr_name.startswith("_"):
                candidate = getattr(module, attr_name)
                if isinstance(candidate, type) and issubclass(candidate, BaseModel):
                    input_class = candidate
                    break

        assert input_class is not None, "No Input class found in module"

        # Create dummy input
        dummy_input = create_dummy_input(input_class)

        # Call function - expect NotImplementedError or valid return
        try:
            result = func(dummy_input)

            # If implemented, verify return type
            assert isinstance(result, (float, int)), f"Expected number, got {type(result)}"

        except NotImplementedError:
            # Expected for stencils - test passes
            pass
