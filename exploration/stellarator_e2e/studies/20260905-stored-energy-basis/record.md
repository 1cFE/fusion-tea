# Record — `20260905-stored-energy-basis`

## 1. Study header

- **Study id:** `20260905-stored-energy-basis`
- **Package:** `stellarator_tea` (`exploration/stellarator_e2e/pkg/stellarator_tea` → `exploration/stellarator_e2e/generated`), at the WI-042 pin — indicator-input `ec984adc1572792c0d64d26776621ebb154d5b62187a1647e386abd3ff9eb096`, semantic `c37fb58a2b19973d1698aeace5184890107efdf0136d20a502ffe88a0eb956a4`, executable `8ac14fdf3745be9682723902c11a2245f423f2fc6d5bc4726b7fed3b09b5c18e` (integration return `work/orchestration/goals/stored-energy-basis/evidence/round2_T-002_integration_return.json`)
- **Date executed:** 2026-09-05
- **Executor:** the round-2 agent of goal `stored-energy-basis` (a Claude Code session on branch `goal/stored-energy-basis`, worktree `../fusion-tea-stored-energy-basis`)
- **Mode:** execute
- **Arms:** `arm-fence-p100`, `arm-search-p220`, `arm-reread-p220`, `arm-transect-ash` — the committed `20260904-wall-and-heating` arms, re-executed

Arms are variants of the same question, run to be compared. This record's question is one — what the committed window says at the paper's own ash rule — and its four arms are the committed study's four, inherited so that every point joins by coordinates to its committed reading.

## 2. Intake

The owner's ruling that this study serves, verbatim:

> *"ok I agree with next steps. we should fix the ash profile (and make sure this scales up for larger stellarators). and I don't want to add the footnote."* `[OWNER-VERBATIM 2026-09-05]` (`work/orchestration/goals/stored-energy-basis/trail.md` § Owner ruling — 2026-09-05)

The rest of the intake is the executor's, from the goal's round-2 strategy revision and the T-003 scope (`trail.md` § Strategy revision — 2026-09-05, intended study question; § T-003 scope (round 2)), marked as the executor's own: **at the round's pin, what does the `20260904-wall-and-heating` window say when the model integrates the paper's own ash profile — the feasible / ignited / driven counts and the design-column verdict at 100 and 220 MW at R 12.7, a 1.3, beside the committed record and the two constant-scale counterfactuals (0.915 and 0.940 of W) — and does the ash shape's effect on W hold across the geometry window, read as the effective ash exponent and W's move at every (R, a, T, n) the arms visit?** The study is a **restatement by re-execution**: WI-042's plan (§ MR-WI042-14) states that no multiplier re-reads any sustainment-dependent column of the committed record, because every one re-closes through the ash fixed point, so the honest re-read is the same points at the new pin. Hence every arm, window, held key and export column is inherited from the committed study verbatim, and the only additions are the WI-042 channels per point (`W_th`, `tau_E`, the effective ash and electron exponents, `⟨n_e⟩`, all oracle-side like every other multi-field channel) and a join by coordinates to the committed record's own columns (`committed_*`). Every number in this record is at the WI-042 profile family (the ash on the fusion-rate profile, electrons by quasi-neutrality); every `committed_*` number beside it is at the WI-037 family (the ash at the fuel's exponent, electrons at a point-A exponent). The comparison is by coordinates against the committed columns, never against a re-run of the committed record; "feasible", "ignited" and "driven" keep the committed record's definitions (`feasible_driven` = every verdict satisfied and `p_aux_required` ≥ 0).

## 3. Objective and result

- **LCOE objective channel(s):** `stellarator_09__stellaris__lcoe_calc__lcoe` (with `stellarator_09__stellaris__lcoe_1cfe_calc__lcoe` as the 1cfe-form comparison channel, exported and not read here)
- **LCOE result:** **202.192 $/MWh — the cheapest feasible *driven* point at the printed 100 MW wall-plug at the paper's own ash rule** (`c2823`: R 15.7 m, a 2.2 m, I 13 MA, T 13 keV, n 1.0×; peak 3.961 against 4.05; 33.3 MW required against 50 coupled; W 1,501 MJ; He/n_e 0.169; a **restored 13 keV row** the committed study never proposed). Over the committed window alone (the `executed_in_both` class) the cheapest driven point is **221.022** (`c3584`: R 17.2, a 2.2, I 13 MA, T 17 keV, n 0.6×; 4.8 MW required; its committed reading 216.918, *violated* there). At 220 MW the cheapest driven point is **218.950** (`c6466`, the same coordinates as `c2823`) and, over the committed window, 240.377 (`c7227`). At the **machine as designed** (R 12.7, a 1.3) exactly one point is feasible and driven at 100 MW — the pinned baseline itself, **322.318** (`c3694`: I 15.4 MA, 49.08 MW required against 50, peak 3.979) — and at 220 MW the cheapest driven point is **370.551** (`c7625`, in the re-read arm at `eta_source` 0.60: I 14.25 MA, T 17 keV, n 0.8×; its committed reading 360.897, violated there).

Every number above is at the WI-042 profile family; every "committed" number beside it is at the WI-037 family at pin `c1b0f0d1…`. The two headline facts are (i) the committed window's driven set mostly **ignites** at the rule — 510 of the committed 681 driven points pass the one-sided sustainment fence with a negative requirement, 146 stay driven, 25 become violated (§ 4, § 6) — so "feasible" grew (1,839 over 7,712) while "feasible driven" over the committed points fell (131 at 100 MW against the committed 257; 181 at 220 against 400); and (ii) the driven region at the rule sits **one temperature step below the committed window**, on the restored 13 keV row (175 driven points at 100 MW, 191 at 220, none ignited), whose bottom is fence-caught at 12.5 keV by sustainment and whose top, at 13.5 keV, ignites (§ 11). The objective still falls with fusion power toward the wall fence and the window's `a`-edge exactly as the committed record found; what the rule changed is where the driven plasma is, not what stops it.

## 4. Constraint outcomes

Every executing constraint, by qualified identity, over the 7,712 evaluated points (`results/points.csv`; "alone" means that column `violated` with the other eight `satisfied`; per arm in the order `arm-fence-p100` / `arm-search-p220` / `arm-reread-p220` / `arm-transect-ash`). The flips beside them are over the 6,283 points executed in both records (each verdict's `committed_` column against its own column).

| `constraint_id` | `source_local_identity` | Status | Note |
|---|---|---|---|
| `stellarator_09__stellaris__wall_load_ok__ab2c790419af93bb` | `wall_load_ok` | mixed | violated **2503 / 7,712** (1113 / 1113 / 268 / 9); alone 1376. **192 committed violations flip to satisfied** and none the other way: the fusion power falls with the fuel peak as the ash rises (the baseline 4.088 → 3.979). Still the fence that stops the objective at large `a` and high `n`, `T` |
| `stellarator_09__stellaris__sustainment_ok__77add152ed8eafce` | `sustainment_ok` | mixed | violated 2007 (1176 / 787 / 41 / 3); alone 930. **964 committed violations flip to satisfied and none the other way** — the fence WI-042 relieved. One-sided as committed: 3,654 evaluated points have `p_aux_required` below zero and pass it (1,819 per geometry arm, 585 / 528 of them otherwise feasible); the baseline is satisfied by 0.92 MW |
| `stellarator_09__stellaris__peak_field_ok__49c6b8228a73cac5` | `peak_field_ok` | mixed | violated 1942 (994 / 948 / 0 / 0); alone 649. Upstream of `sustain`; the structure is the committed one (small `R` at every current; 18 MA below R 17; nowhere at R ≥ 15.7 or at 13 MA), the count larger only because the restored rows add points |
| `stellarator_09__stellaris__recirc_ok__afc3be66f0a3421b` | `recirc_ok` | mixed | violated 1544 (484 / 1027 / 33 / 0); alone 381. **111 committed satisfactions flip to violated** — net power falls with the fusion power at the low-density end of the 220 MW grid |
| `stellarator_09__stellaris__beta_ok__82b78aad420730d5` | `beta_ok` | mixed | violated 616 (308 / 308 / 0 / 0); alone 4. **156 committed violations flip to satisfied**: β reads the same lower ⟨p⟩ as W; live at large `R` and `n`, still never decisive |
| `stellarator_09__stellaris__wp_stress_ok__f38a102195da1dd0` | `wp_stress_ok` | mixed | violated 490 (256 / 234 / 0 / 0); alone 0. Upstream of `sustain`; no flip |
| `stellarator_09__stellaris__cond_strain_ok__251d4c803804ab60` | `cond_strain_ok` | satisfied | 0 / 7,712 — inert in this window, as committed |
| `stellarator_09__stellaris__net_positive__484521d56c02667a` | `net_positive` | satisfied | 0 / 7,712 (the pre-screen excluded the 164 proposals the oracle could not close, § 11) |
| `stellarator_09__stellaris__tbr_ok__2cd198f674d413e4` | `tbr_ok` | satisfied | held-vs-held; unreachable from every declared axis |

**Feasible / ignited / feasible driven per arm at the rule, all executed points:** `arm-fence-p100` 891 / 1,819 (585 of the feasible) / **306**; `arm-search-p220` 900 / 1,819 (528) / **372**; `arm-reread-p220` 45 / 8 / **45**; `arm-transect-ash` 3 / 8 / **3**. **Over the 6,283 points executed in both records, beside the committed reading (WI-037 family) at the same points:** `arm-fence-p100` 716 / 1,819 / **131** against the committed 458 / 787 / **257**; `arm-search-p220` 709 / 1,819 / **181** against 598 / 787 / **400**; `arm-reread-p220` 45 / 8 / **45** against 24 / 0 / **24**; `arm-transect-ash` 3 / 8 / **3** against 0 / 8 / **0**. **The restored 13 keV rows alone:** `arm-fence-p100` 734 executed / 175 feasible / 0 ignited / **175 driven**; `arm-search-p220` 695 / 191 / 0 / **191**. Transitions over the points executed in both: driven → ignited **510**, driven → driven 146, driven → violated 25, violated → driven 214, violated → ignited 207, ignited → ignited 396, ignited → violated 3, violated → violated 4,782 (`transition_vs_committed`). **Beside the two constant-scale counterfactuals** (oracle-side, the WI-037 family times 0.915 / 0.940 with the closure live, at the committed pin; predictions the rule tested, never bounds — § 12): over the comparable points the rule's requirement lies **below both scales at 3,456 points, between them at 478, above both at 2,316** (33 not comparable); the counterfactuals' driven counts at the committed points were 332 / 314 at 100 MW and 390 / 395 at 220 where the rule gives 131 and 181, because the rule ignites what the constant scales left driven.

## 5. Framing

**As proposed at intake** — the framing submitted to the pre-execution critique (step 4). Every framing is the committed study's, inherited with its arms, because this study's object is the committed window at the new pin; what each axis is *for* here is stated beside it.

| Axis | Framing proposed | Why |
|---|---|---|
| `R+tie` | search | As committed: a bounded feasible band in `R` expected at both levels (the ceiling below, the wall / sustainment / β above). Here the question is where the band and its optimum sit at the rule beside the committed ones (interior at 14.2 there). |
| `a` | search, with the committed edge disclosure | As committed: the bottom fence-caught, the top caught by nothing modeled (the window stops at 2.2 by choice). Here: whether the rule moves the bottom (the committed record found nothing driven at a 1.3 at 100 MW; the counterfactuals disagreed about it) and whether the top is still an edge. |
| `n_e0` | search | As committed: bottom caught by sustainment / `recirc_ok`, top by the wall. |
| `T_i0` | search | As committed, **with the 13 keV row restored** (critique F1(b); trail § Amendment 2026-09-05): at the WI-037 family the bottom was caught by sustainment at 13 keV; at the rule the critique's probes find the driven region at large `a` at 12.5–13.5 keV, below the committed window, with the committed window's own points ignited there — so this axis is where the restatement's counts are most at stake, and the restored row is executed rather than bracketed. The edge transects at the rule (§ 11) catch 12.5 keV below. |
| `I_coil` | search | As committed: bottom caught by the wall and sustainment, top by the ceiling (not at R ≥ 15.7, where the ceiling does not bind — as committed). |
| `p_wallplug_heat` | sensitivity | As committed: two levels, no boundary claim in installed power. |
| `tau_ratio_ash` | sensitivity | As committed: the transect through the three committed anchors. Since WI-042 it moves the ash **amount** only (the shape's exponent does not depend on it — design D1; the critique's F6), so the transect tests the amount's fragility, not the shape's. |
| `eta_source_heat` | (re-read only) | As committed: not a swept axis; the re-read arm carries round 1's four values. |

**As judged after the run.**

| Axis | Framing judged | Changed? | Why |
|---|---|---|---|
| `R+tie` | search | no | A bounded driven band, 11.2–17.2 m, **optimum interior at 15.7** (cheapest driven per `R` at 100 MW: 306.0 / 250.7 / 219.3 / **202.2** / 205.4 over 11.2 → 17.2; the committed optimum was 14.2); the ceiling catches every current at 9.7 and the wall, sustainment and β catch 17.2 together at the rule's anchor (§ 11). |
| `a` | search, **edge** | no — the same one-sided structure | The bottom is real (on the design column only the baseline is driven at 1.3; the cheapest driven per `a` is monotone 322.3 / 283.6 / 242.2 / 235.6 / 221.8 / **202.2** over 1.3 → 2.2), the top is the window's edge (2.2 driven at the anchor, 2.4 too) and nothing modeled catches it — as committed. **New at the rule: ignition grows with `a`** (ignited at 100 MW by `a`: 41 / 161 / 320 / 382 / 455 / 460 over 1.3 → 2.2), so the edge is now where the plasma is least controllable as modeled, not only where it is cheapest. |
| `n_e0` | search | no | Cheapest driven at 1.0× (202.2; 0.6–0.9×: 221.0 / 229.7 / 221.8 / 224.1); bottom caught by sustainment (0.7× at the anchor), top by the wall (1.1×). |
| `T_i0` | search, **edge one step above a caught edge** | **yes** — the structure moved | The cheapest driven point is on the restored 13 keV row (202.2 against 221.8 / 229.7 / 221.0 / 228.8 at 14.63 / 16 / 17 / 18); driven by `T` at 100 MW: **175** / 63 / 30 / 22 / 16, ignited: 0 / 383 / 461 / 484 / 491. At the rule's anchor 12.5 keV is caught by sustainment and 13.5 ignites (§ 11): the driven band at large `a` is about one keV wide, and the executed window's bottom (13) is one step above a caught edge, not on it — 12.5 is bracketed, not executed. The committed judgment ("interior at 16 keV") does not transfer. |
| `I_coil` | search | no | Cheapest at 13 MA at both levels (202.2 / 205.4 / 222.9 / 241.1 / 289.8 over 13 → 18 at 100 MW); the bottom is fence-caught at 12 MA at the anchor (the wall, sustainment and β) and the top by the ceiling below R 15.7 and by nothing at R ≥ 15.7 — as committed, disclosed (§ 17). |
| `p_wallplug_heat` | sensitivity | no | Two levels; no boundary claim. The driven optimum at 220 MW (218.95) is dearer than at 100 MW (202.19), as committed: the extra installed heating buys nothing the plasma needs at 13 keV and 33 MW required. |
| `tau_ratio_ash` | sensitivity | no | Fifteen points, no boundary claim; the knife-edge the committed record found moved (§ 6). |

## 6. Per-axis account

Every number here is at the WI-042 profile family; "committed" means the same coordinates at the WI-037 family (pin `c1b0f0d1…`), read from the joined `committed_*` columns; "the scales" means the two constant-scale counterfactuals' `cf0915_*` / `cf0940_*` columns (oracle-side, predictions). The recount script that derives every count is `work/orchestration/goals/stored-energy-basis/evidence/round2_T-003_recount.py`.

#### `R+tie` — feasible structure (search framing)

Driven at every `R` in the window at 100 MW (19 / 66 / 83 / 87 / 51 points over 11.2 → 17.2), cheapest per `R` 306.0 / 250.7 / 219.3 / **202.2** / 205.4: an interior optimum at 15.7 m, one step outward from the committed 14.2. The band's edges at the rule's anchor: the conductor ceiling at 9.7 m; the wall, sustainment and β together at 17.2 m (§ 11). The W correction is smallest at large `R` and small `a` (mean `W_ratio_vs_committed` 0.975 at R 14.2, a 1.3; the rows at R 15.7–17.2, a 1.3 are where W *rises* — 145 points above 1.0, He/n_e below 0.062, because with almost no ash the derived electron profile is flatter than the retired 0.596) and largest at small `R` and large `a` (0.826 at R 11.2, a 2.2).

#### `a` — feasible structure (search framing; the top an edge)

Driven by `a` at 100 MW: 1 / 38 / 59 / 63 / 69 / **76** over 1.3 → 2.2; cheapest per `a` monotone 322.3 → 202.2; on the design column (R 12.7) driven by `a`: 1 / 15 / 15 / 13 / 11 / 11 and ignited 13 / 49 / 88 / 96 / 100 / 92. The committed reversal (the wall load falls with `a` through the converged ash) holds at the rule and is steeper, because the ash amount is larger at every `a` (He/n_e 0.04–0.27 over the study against the committed 0.074–0.185 on the best column): past a ≈ 1.5 the plasma ignites at most (I, T, n). The optimum's `a` is the window's edge at both levels, as committed; the magnet account does not price it (`lcoe_magnet_shadow` inherited, not re-read).

#### `n_e0` — feasible structure (search framing)

Cheapest driven per density at 100 MW: 221.0 / 229.7 / 221.8 / 224.1 / **202.2** over 0.6 → 1.0×; driven counts 66 / 76 / 62 / 52 / 50. The bottom is caught by sustainment (0.7× at the anchor; recirculation at 0.5× at 220 MW), the top by the wall (1.1×) — as committed. 111 committed `recirc_ok` satisfactions flip to violated at the 220 MW grid's low-density end, through the lower net power.

#### `T_i0` — feasible structure (search framing; the window's bottom one step above a caught edge)

The axis the rule moved. Driven at 100 MW by `T`: **175** (13 keV, the restored row) / 63 / 30 / 22 / 16; ignited: 0 / 383 / 461 / 484 / 491. Cheapest driven per `T`: **202.2** / 221.8 / 229.7 / 221.0 / 228.8 at 100 MW; 219.0 / 243.6 / 250.1 / 240.4 / 252.4 at 220. Through the rule's anchor (R 15.7, a 2.2, 13 MA, 1.0×) the T transect reads sustainment at 12.5, driven at 13, the wall with ignition at 13.5 and above (§ 11): the driven band at large `a` is about one keV wide, and 12.5 is bracketed, not executed. The committed window's own points at these coordinates are ignited: the committed cheapest driven `c1721` (14.2, 2.2, 15 MA, 16 keV, 0.9×; 212.46, +7.4 MW) reads **−188.8 MW** at the rule (ignited; LCOE 220.4; the scales predicted +5.9 / +4.7); `c4639` (the committed cheapest driven at 220) reads −109.3 (the scales +40.0 / +44.0); `c0821` (the design column's a 1.7 driven point) −60.1 (the scales +29.5 / +33.4). The effective ash exponent is a function of `T_i0` and nothing else in this study: 4.268 / 4.052 / 3.885 / 3.770 / 3.661 at 13 / 14.63 / 16 / 17 / 18 keV, identical at every (R, a, I, n) and at every τ*/τ_E (one value per temperature over 1,429–1,574 points each).

#### `I_coil` — feasible structure (search framing)

Cheapest driven at 13 MA at both levels (202.2 / 205.4 / 222.9 / 241.1 / 289.8 over 13 → 18 MA at 100 MW; driven counts 66 / 62 / 71 / 63 / 43, plus the baseline's 15.4 MA); the bottom is fence-caught at 12 MA through the rule's anchor (the wall, sustainment and β), the top by the ceiling below R 15.7 and not at all at R ≥ 15.7 (18 MA driven there) — as committed. On the design column at 100 MW: 15 MA needs 62.7 MW and reads 4.072 on the wall (both violated), 16 MA clears both and breaks the ceiling; only the pinned 15.4 MA is driven (§ 15 #3).

#### `p_wallplug_heat` — observed response (sensitivity framing)

Two levels; no boundary claim. At the rule's cheapest coordinates the 220 MW point (218.95) is dearer than the 100 MW point (202.19) by the heating capital the plasma does not use; the design column at 220 MW has 47 driven points (45 in the re-read arm across all four source efficiencies — 7 / 11 / 13 / 16 at 0.45 / 0.50 / 0.55 / 0.60 — and 2 in the search arm) against the committed 26.

#### `tau_ratio_ash` — observed response (sensitivity framing)

Fifteen points, no boundary claim. **The committed knife-edge moved up by about a factor 1.5.** Through the scan's best 100 MW anchor (R 14.2, a 1.8, 14 MA, 16 keV, 0.8×) the committed record found the point driven only at the source's own 8, ignited at 6 and sustainment-blocked at 12 (141.9 MW); at the rule it is **ignited at 2, 4 and 6** (−294.7 / −183.9 / −107.1 MW), **driven at 12** (25.6 MW required, peak 3.115) and sustainment-blocked at 16 (74.6). At the 220 MW anchor it is driven at 12 and 16 (42.8 and 89.7 MW). On the design column (the baseline geometry) it ignites at 2 and 4, is wall-blocked at 6 (23.1 MW required but peak 4.439) and sustainment-blocked at 12 and 16 (85.6 / 109.7 MW; the committed 158.4 / 205.8). The shape does not move with the ratio (`alpha_He_eff` 4.052 at every τ on the design column, 3.885 at the 100 MW anchor — set by `T_i0` alone); the amount does (He/n_e 0.04 → 0.18 over 2 → 16 on the design column). The committed `#2` finding — the `a`-reversal's sign rests on the held product f_suppr × τ*/τ_E — stands; its location is W-basis-sensitive exactly as L-001 said.

#### `eta_source_heat` — the re-read arm (not a swept axis)

Round 1's 220 MW grid at the design geometry, all four efficiencies: **45 driven at the rule** (7 / 9 / 13 / 16 at 0.45 / 0.50 / 0.55 / 0.60) against the committed 24 (1 / 3 / 8 / 12) and the scales' 58 / 58; 8 ignited; 11 of the 279 committed wall violations flip to satisfied. LCOE rises at every re-read point (about +2.8 %, through the fusion power); the cheapest driven point is 370.55 (`c7625`, 0.60: I 14.25 MA, T 17 keV, n 0.8× — committed 360.90 and violated there) and the committed cheapest, `c6256` (378.556), reads 389.50 and is still driven. **The machine as designed pays 370.55 for its cheapest feasible driven 220 MW point at the rule.**

#### The W correction across the window (the owner's clause) — observed, not a search

`W_ratio_vs_committed` over the 6,283 shared points: **0.788–1.018, mean 0.919** — and it is a function of the ash fraction: by He/n_e bin the mean ratio falls monotonically from 1.016 (He/n_e 0.04) through 0.999 (0.06), 0.964 (0.10), 0.918 (0.15), 0.899 (0.17) to 0.79 at the study's maximum 0.27; by geometry from 0.975 (R 14.2, a 1.3) to 0.826 (R 11.2, a 2.2). The 145 points above 1.0 sit where the ash is scarce (He/n_e ≤ 0.062; R 14.2–17.2 at a 1.3): there the derived electron profile (effective exponent 0.44–0.5) is flatter than the retired 0.596, so the electron pressure rises slightly. "Scales up" is met by construction (the rule computes at every point; the shape's exponent depends on `T_i0` and the two held exponents only) and *demonstrated* here (the consequence follows the ash fraction, not a constant); it rests on what is still bound at point A — α_n 0.33 and α_T 1.19 (reserved), Ti/Te 0.95, τ* uniform in ρ (corroborated at point A only), f_suppr × τ*/τ_E = 4.0 and ι 0.92 (Table 4 at A = 9.8) — and nothing in this study could falsify the shape itself (critique F6). `W_th_MJ_store` (from the store's β, B_axis and V) agrees with the oracle's W at every point to 5.5e-16, so the W column is package evidence.

#### The design column (R 12.7, a 1.3) — the machine as designed

At 100 MW **exactly one point is feasible and driven: the pinned baseline** (`c3694`, I 15.4 MA, T 14.63, n 1.0×): 49.08 MW required against 50 coupled (0.92 MW, about 0.2 % of W), peak 3.979 against 4.05 (0.071 MW/m², 1.8 % of a fusion power that moved 2.7 % with the ash), B_peak 24.9 against 24.9 (the design convention), and all of it at the held coupling `eta_couple_heat` 1.00 (at 0.98 the coupled power is 49.0 and the fence is violated). No grid point on the column is driven (I 15 MA reads 62.7 MW and 4.072; 13 points on the column are ignited at a 1.3, all at 16 MA and above where the ceiling fails). The committed column had 0 driven points; the 0.915 scale predicted 6, the 0.940 scale 0; the rule gives 1, the baseline, and the two committed points the 0.940 scale named as its closest (`c0625` at 0.03 MW/m², `c2973` at 1.4 MW) moved in opposite directions (`c0625` to 63.9 MW and 4.128, worse on both fences; `c2973` — the baseline — to satisfied). **The reading is the round's: the design point's sustainment is undetermined within the source's own precision — on the boundary — not feasible.**

## 7. Axis groups

The declaration is the committed study's, inherited verbatim (`axes.json` byte-identical to `studies/20260904-wall-and-heating/axes.json` at `a5b0b96a`); its per-group notes are that study's words and describe the reasoning that fixed these arms and windows. Nothing in WI-042 added, retired or re-tied a swept key (`alpha_n_e`, the retired entry point, was never an axis).

| Axis | Entry key | Provenance | Note |
|---|---|---|---|
| `R+tie` | `stellarator_09__stellaris__R` | fan_out | Swept. |
| `R+tie` | `stellarator_09__stellaris__magnet__R0` | **tie** | Same physical major radius; declared in `manifest.json → ties`, reasons in ANNEX § Declared ties. |
| `a` | `stellarator_09__stellaris__a` | fan_out | Swept, the committed window (1.3–2.2). |
| `n_e0` | `stellarator_09__stellaris__n_e0` | fan_out | Swept. |
| `T_i0` | `stellarator_09__stellaris__T_i0` | fan_out | Swept; `T_e0` rides the held 0.95 ratio inside the sustainment calc; **since WI-042 the ash shape's effective exponent is a function of `T_i0` through the reactivity** (4.05 at 14.63 keV, 3.66 at 18), so this axis is also where the shape moves. |
| `I_coil` | `stellarator_09__stellaris__magnet__I_coil` | fan_out | Swept. |
| `p_wallplug_heat` | `stellarator_09__stellaris__p_wallplug_heat` | fan_out | Two levels, sensitivity. No tie since WI-039. |
| `tau_ratio_ash` | `stellarator_09__stellaris__tau_ratio_ash` | fan_out | Swept as the committed sensitivity transect (15 points); held at 8.0 everywhere else — § 8. Since WI-042 it moves the ash **amount** only; the shape does not depend on it. |
| `eta_source_heat` (declined) | `stellarator_09__stellaris__eta_source_heat` | fan_out | Held at 0.50 — § 8; the re-read arm carries round 1's four values. |
| `eta_couple_heat` (declined) | `stellarator_09__stellaris__eta_couple_heat` | fan_out | Held at 1.00 — § 8. |
| `f_suppr_ash` (declined) | `stellarator_09__stellaris__f_suppr_ash` | fan_out | Held at 0.50 — § 8. |
| `iota_23` (declined) | `stellarator_09__stellaris__iota_23` | fan_out | Held at 0.92 — § 8. |
| `j_wp` (declined) | `stellarator_09__stellaris__magnet__j_wp` | fan_out | Held at 118.827 — § 8. |
| `B_max` (declined) | `stellarator_09__stellaris__magnet__B_max` | fan_out | Not swept — § 8. |
| `wall_peak_q_ref` (declined) | `stellarator_09__stellaris__wall_peak_q_ref` | fan_out | The source anchor, held by definition — § 8. |

Fifteen declared keys across fourteen groups, all validated as package inputs at preflight (§ 9). Every held key is asserted per case in `study.py` `export()` as in the committed study, and the six `wall_peak_*` reference facts are checked per case through the store's calibration channel against the baseline's to 1e-9.

## 8. Indicators and rulings

Per proposed axis, including the seven proposed and declined. Source: `indicators.json`, run over all fourteen groups (`subset: false`) on the WI-042 package. The reach is the committed study's on every axis — the same constraints and objectives — with **four more channels tainted on every axis that reaches `sustain`** (the WI-042 outputs `p_avg`, `n_e_volav`, `alpha_n_e_eff`, `alpha_He_eff`); no axis reaches a plant-level electron exponent any more, because there is none.

| Axis | Indicator | Constraints reachable | Objectives reachable | Modules fired | Ruling |
|---|---|---|---|---|---|
| `R+tie` | `constraints_reachable` | 8 / 9 | 11 / 11 | 72 (103 tainted) | swept — the committed geometry lever, re-executed |
| `I_coil` | `constraints_reachable` | 8 / 9 | 10 / 11 | 69 (95 tainted) | swept |
| `a` | `constraints_reachable` | 5 / 9 | 10 / 11 | 60 (91 tainted) | swept, the committed window |
| `T_i0` | `constraints_reachable` | 5 / 9 | 8 / 11 | 56 (82 tainted) | swept; the axis the ash shape moves along |
| `n_e0` | `constraints_reachable` | 5 / 9 | 8 / 11 | 56 (82 tainted) | swept |
| `p_wallplug_heat` | `constraints_reachable` | 3 / 9 | 4 / 11 | 49 (61 tainted) | swept, two levels, sensitivity |
| `tau_ratio_ash` | `constraints_reachable` | 5 / 9 | 8 / 11 | 56 (82 tainted) | swept as the committed sensitivity transect (15 points), not a design lever |
| `f_suppr_ash` | `constraints_reachable` | 5 / 9 | 8 / 11 | 56 (82 tainted) | **declined, held at 0.50** — as committed |
| `iota_23` | `constraints_reachable` | 5 / 9 | 8 / 11 | 56 (82 tainted) | **declined, held at 0.92** — as committed |
| `eta_source_heat` | `constraints_reachable` | 3 / 9 | 4 / 11 | 49 (61 tainted) | **declined, held at 0.50** — as committed; the re-read arm carries the predecessor's four values as its grid |
| `eta_couple_heat` | `constraints_reachable` | 3 / 9 | 4 / 11 | 49 (61 tainted) | **declined, held at 1.00** — as committed |
| `j_wp` | `constraints_reachable` | 4 / 9 | 4 / 11 | 53 (62 tainted) | **declined, held** — as committed |
| `B_max` | `constraints_reachable` | 1 / 9 | 0 / 11 | 2 (2 tainted) | **declined** — as committed |
| `wall_peak_q_ref` | `constraints_reachable` | 1 / 9 | 3 / 11 | 8 (9 tainted) | **declined, held by definition** — the source anchor, as committed |

**No axis reported `no_constraint_response`, so no owner ruling was owed under runbook step 4's fail-closed condition.** The rulings are the committed study's, inherited: this record asks what the committed window says at the new pin, and a changed axis set would be a different study.

**Not derivable, disclosed in every record.** Monotonicity of any channel in any axis; identity of the same physical quantity across differing key names; intra-module operand dependency. `constraints_reachable` is a possible path and never a statement that a constraint responds. `unresisted` is the agent's recorded judgment, never a tool output. Every response reported in § 6 is an executed observation on the grid, not an indicator claim.

**Model-development findings.** No axis reported `no_constraint_response`; the obligation is discharged by a stated nil: none owed. The committed study's observations carried beside its rulings (the unbounded `a`, the one-sided sustainment fence, the optimistic coupling, the bare `B_max` inequality, the un-priced `j_wp`, replacements costing no availability, `tbr_ok` held-vs-held) stand as its § 8 records them and are re-sighted here only where this record's own data speaks to them (§ 15).

## 9. Preflight results

`results/preflight_results.json`. **All six gates ran; all six pass.** The identity gate read `results/package_identity.json` and the baseline gate read `results/baseline_result.json`, both deposited by `study_route.execute_baseline` at step 5, before any study point ran.

| Gate | Outcome | Detail |
|---|---|---|
| Declared-group key validation (`declared_keys`) | pass | 15 declared keys across 14 groups, all package inputs |
| Suffix-sibling scan (warnings only; `sibling_scan`) | pass | no suffix-sibling findings |
| Identity (`identity`), against `results/package_identity.json` | pass | kind `sealed`, digest `8ac14fdf3745be96…` recomputed from 0 allowed-modified files and 0 declared sources; every other sealed artifact matches |
| Baseline gate against the pinned headline (`baseline_headline`), against `results/baseline_result.json` | pass | `stellarator_09__stellaris__lcoe_calc__lcoe` reproduces at relative deviation 0.000e+00 (322.31843948570247); **9/9** pinned verdicts match — **all nine satisfied**, `sustainment_ok` and `wall_load_ok` expected-satisfied since WI-042 (both were expected-violated at the committed pin) |
| Manifest / package fingerprint match (`manifest_currency`) | pass | both recorded package fingerprints match the package on disk |
| Package cleanliness (`package_clean`) | pass | package tree byte-untouched (git clean) |

## 10. Execution route and why

- **Route:** study-local direct-API definition (`study.py` + `study_route.py`, `StudyRunner` + `PreparedListStrategy`) — the committed study's route, inherited with its definition.
- **Why this route:** the arms are coordinated axis-group blocks with every held key explicit per proposal, the declared `R` tie, one arm omitting a column another carries, the pinned baseline as an explicit member, and the derived geometric mask (`R > a + 2.25`) applied at construction; none of that is a Cartesian grid the `teax-study` CLI runs. The route was exercised at step 5 (the baseline executed and deposited) and gated at step 6 before this rationale was written. `study.py` differs from the committed definition only in the places its docstring names (the id, the four oracle channels, the per-point WI-042 columns, the join to the committed record) and in two mechanics disclosed in § 11: the oracle pre-screen runs in a process pool (one evaluation per proposal, the same evaluation the committed screen made serially) and the run is split into a screen phase and an execute phase so the critique could read the screen before any point ran — with the execute phase guarded (critique F5): the cache is bound to the proposal count, the oracle's source digest and the package identity; an existing store is refused; the executed key set is asserted equal to the screened set.

**Glue disclosure — glue ledger: none.** `results/package_identity.json` records `kind: sealed`, zero allowed-modified files and zero adapter sources. The harness supplies no value the model does not compute; the package is sealed on stock teax at revision `744745f895677f3344b9884627369a6a47ed987f`.

## 11. Study definition and window provenance

**Window provenance: `engineered`, inherited — and amended in one respect before any point ran.** Every window, grid, anchor and held key is the committed study's (`studies/20260904-wall-and-heating/study.py` at `a5b0b96a`, its `record.md` § 11: windows fixed after two oracle passes and the pre-execution critique's revisions F6/F7): `R` 11.2 / 12.7 / 14.2 / 15.7 / 17.2 with its tie; `a` 1.3 / 1.5 / 1.7 / 1.8 / 2.0 / 2.2; `I_coil` 13 / 14 / 15 / 16 / 18 MA; `T_i0` 14.63 / 16 / 17 / 18 keV; `n_e0` 0.6–1.0× of 5.06e20 at 100 and 220 MW wall-plug; the re-read arm's 384-point grid at four source efficiencies less the 24 shared points; the ash transect τ*/τ_E 2 / 4 / 6 / 12 / 16 through the committed three anchors; the pinned baseline as an explicit member of `arm-fence-p100`; the derived geometric mask `R > a + 2.25` (ANNEX § Validity masks) at construction. **The one amendment (critique F1(b); trail § Amendment 2026-09-05 — amends § T-003 scope (round 2)): the T 13 keV rows are restored on the two geometry grids** — about 1,500 proposals at both levels, the pre-F6 committed grid rows the committed critique dropped because sustainment blocked all of them at the WI-037 pin (`20260904-wall-and-heating` `window_edges.json`: 117 MW required at 13 keV). Sustainment is the fence WI-042 relieved; the critique's probes at the rule find the driven region at large minor radius at roughly 12.5–13.5 keV, below the committed window's bottom, where the committed window's own points ignite. Those rows are a named class (`class_vs_committed = not_proposed_by_committed`) and every comparison against the committed record is over the points executed in both. **No other window was chosen and the full scan was not re-run**, because this study's object is the committed window itself: it asks what those points say at the WI-042 pin, and a window fixed from the new package's own scan would answer a different question. That is the disclosure the runbook's step 7 asks for: the provenance is engineered, by the committed study, for the committed study's reasons, and inherited here as the restatement's object. Consequently the committed record's own caveats on its windows travel with this one: the `a`-top is not fence-caught (the window stops at 2.2 by choice); the 220 MW optima sat on the window's bottom current and temperature; R 9.7 m and T 13 keV are bracketed by the committed transects and not executed.

**The window's edges at the rule** (critique F1(a); `edges.py`, `results/window_edges.json`): the committed transects' anchors ignite at the rule, so the one-axis transects were re-read anchored on a point driven at the rule at both levels (R 15.7, a 2.2, I 13 MA, T 13 keV, n 1.0×) and on the design column (R 12.7, a 1.3, I 15.4 MA, T 14.63, n 1.0×), the T transect refined to 11–20 keV: 138 transect rows, no oracle errors (`results/window_edges.json`). **At the anchor driven at the rule (100 MW):** `T` — caught below at 12.5 keV by sustainment (117 → the rule's requirement exceeds 50 MW) and above at 13.5 keV by the wall with the plasma ignited, so the driven band through that anchor is about one grid step wide at 13 keV, and the restored 13 keV row is the executed window's bottom while 12.5 is bracketed; `a` — caught below (sustainment and the wall at 1.3–2.0, sustainment alone at 2.1) and **not caught above** (2.2 and 2.4 driven), so the `a`-top stays the committed disclosure: the window stops at 2.2 by choice, and the magnet account does not price it; `I_coil` — caught below at 12 MA (sustainment, the wall and β) and **not caught above at this anchor** (13–18 MA all driven at R 15.7, where the conductor ceiling does not bind, as the committed record found for R ≥ 15.7); `n_e0` — caught below at 0.7× (sustainment) and above at 1.1× (the wall); `R` — caught below at 9.7 m (the ceiling) and above at 17.2 m (sustainment, the wall and β together). **At 220 MW through the same anchor:** `T` driven at 12.5 and 13, the wall and ignition from 13.5; `a` driven from 2.1 (top not caught); `I` 13–18 driven (top not caught at this R); `n` 0.6–1.0 driven, recirculation at 0.5, the wall at 1.1; `R` 11.2–15.7 driven, the wall and β at 17.2. **Along the design column (R 12.7, a 1.3, I 15.4 MA, n 1.0×, 100 MW):** `T` — driven at 14.63 only (sustainment from 14 keV down; the wall from 16 up); `a` — driven at 1.3 and 1.4, then every verdict satisfied but the plasma ignited from 1.5 to 2.4; `I` — the pinned 15.4 MA driven, 15 MA and below sustainment and the wall, 16 MA and above the ceiling; `n` — 1.0× only (sustainment below, the wall above); `R` — 12.7 only (the ceiling at 11.2; sustainment and the wall from 14.2). So the executed window at the rule is fence-caught on `T` (below, with the restored row), `n` and `R` at both anchors, and on `I` below; it is not caught on `a` above (as committed) nor on `I` above at R ≥ 15.7 (where the ceiling does not bind, as committed): both disclosed, neither new..

**The oracle pass this record does make** is the evaluability pre-screen over every proposal (the committed study's pattern, exception-hardened): the oracle at the WI-042 chain, evaluated once per proposal and fed to both exports, decides which proposals the sealed package could close (`p_net` above zero, no exception). Its exclusions are `results/excluded_points.csv`, each with its reason and — new here — whether the same coordinates were excluded by the committed screen (`committed_excluded`) and the committed case id where the committed run executed them. The counts, once the screen ran: **7,876 proposed / 7,712 evaluable / 164 excluded.** Of the 164: **65 are the committed record's own 65 exclusions, all still excluded** (`excluded_in_both`; every committed exclusion stays out, none became evaluable at the rule); **28 are points the committed record executed and the rule cannot close** (`committed_executed_now_excluded`); **71 are restored 13 keV rows** never proposed by the committed study (`not_proposed_by_committed`). So 6,283 of the committed record's 6,311 executed points are executed here too (the `executed_in_both` class, the only class counts compare over), and 1,429 of the 1,500 restored rows. By arm: `arm-fence-p100` 56 excluded, `arm-search-p220` 108; the re-read and transect arms lose nothing. Reason classes (`reason`, the oracle's own exception text): 94 non-positive fuel density, 0 ash fixed point did not converge, 70 non-real value — by class: {'excluded_in_both': {'non-positive fuel density': 54, 'non-real value (complex; the CAS10 land term)': 11}, 'committed_executed_now_excluded': {'non-positive fuel density': 26, 'non-real value (complex; the CAS10 land term)': 2}, 'not_proposed_by_committed': {'non-real value (complex; the CAS10 land term)': 57, 'non-positive fuel density': 14}}..

**Two mechanics disclosed, neither changing what is evaluated.** (i) The pre-screen runs in a process pool (`study.py` `screen()`: `multiprocessing.Pool`, `imap` preserving order, one oracle evaluation per proposal, each worker with its own oracle memo) because the WI-042 chain costs about 1.1 s per oracle evaluation — five reactivity-weighted profile integrals per point where the WI-037 chain had one — and a serial screen of 6,376 proposals would take two hours where a pooled one takes minutes; the committed screen was serial. (ii) The run is split into a `screen` phase and an `execute` phase (`study.py` `run(phase=…)`): the screen deposits `results/excluded_points.csv` and caches the evaluable list under `results/_work/` (gitignored, not evidence), and the execute phase loads that cache and runs the points; the oracle is still evaluated once per proposal and joined to the executed cases by input key exactly as the committed `run()` did. The split exists so the pre-execution critique could read the screen's result before any point ran through the sealed package.

**The ordering, recorded rather than hidden.** The pre-screen (an oracle pass, no point through the sealed package) ran **before** the critique, as the committed study's scan did and disclosed, so that the critique could read real numbers — the new excluded set beside the committed 65. No study point ran through the sealed package before the critique's verdict was recorded (§ 14); the only execution before it was the manifest's pinned baseline point (step 5), a route-preparation act.

**What the screen shows about the closure's validity edge (row `20260904-wall-and-heating#8`).** **The edge moved inward, and by less than the constant-scale counterfactual predicted.** The committed 65 (R 11.2–12.7, a 2.0–2.2, I 16–18 MA; 54 non-positive fuel, 11 non-real) are all still off the edge at the rule. **28 committed-executed points join them** — every one at R 11.2 or 12.7, at 18 MA (and a few at 16), at a 1.3 (n 1.0×) or a 2.0–2.2 (n 0.6–0.9×): {(11.2, 2.0, 18.0): 8, (11.2, 2.2, 16.0): 8, (11.2, 2.2, 18.0): 2, (12.7, 2.2, 18.0): 8, (11.2, 1.3, 18.0): 1, (12.7, 1.3, 18.0): 1} — the same corner the committed record's `#8` names, one step further in: at the rule τ_E is higher, so the converged ash is larger, and the fuel goes non-positive (or the closure returns a non-real value) one grid step earlier in current and density. The 0.940 counterfactual (W scaled by a constant, closure live) predicted 35 further points falling off; the rule takes 28 of the committed points, in the same corner. The 71 restored 13 keV rows excluded sit in the same corner ({(11.2, 1.3): 14, (11.2, 1.5): 8, (11.2, 1.7): 6, (11.2, 1.8): 6, (11.2, 2.0): 7, (11.2, 2.2): 14, (12.7, 2.2): 3, (12.7, 1.3): 6, (12.7, 1.5): 2, (12.7, 1.7): 1, (12.7, 1.8): 1, (12.7, 2.0): 1, (14.2, 1.3): 2}), at 16–18 MA. No excluded point is feasible, ignited or driven in the committed record (the committed `#8` finding stands: nothing the study reads sits on the edge). None of this is a package failure: the pre-screen is the oracle's, and the sealed package would not have closed these points either (the oracle mirrors its fail-loud contract)..

**The per-point columns this record adds to the committed export** (`study.py` `export()`): `W_th_MJ_oracle`, `tau_E_s_oracle`, `alpha_He_eff_oracle`, `alpha_n_e_eff_oracle`, `n_e_volav_oracle` (the WI-042 derived-profile channels, oracle-side like every `sustain__*` field), and the join `committed_case_id`, `committed_excluded`, `committed_lcoe`, `committed_p_fus`, `committed_wall_load_peak`, `committed_beta`, `committed_p_aux_required_MW`, `committed_W_th_MJ`, `committed_tau_E_s`, `committed_n_He0`, `committed_sustainment_ok`, `committed_wall_load_ok`, `committed_beta_ok`, `committed_recirc_ok`, `committed_feasible`, `committed_ignited`, `committed_feasible_driven`, `W_ratio_vs_committed`, `feasible_driven_changed` — every one read from the committed record's own `results/points.csv` and `results/oracle_operands.csv` at `a5b0b96a` by the nine coordinates a proposal is defined by (`_coord_key`), never recomputed.

**Arms are tagged at construction**, and the pinned baseline is a member of `arm-fence-p100` by construction (`is_baseline_point` true once) — as committed.

## 12. Cross-fingerprint correlation and what it means

**One fingerprint set, four arms, one store.** Every executed point in this record ran against one package identity — indicator-input `ec984adc1572…`, semantic `c37fb58a2b19…`, executable `8ac14fdf3745…` (the WI-042 pin; § 1) — and every arm is in the one store `snapshot.json` names, so within this record there is no cross-arm fingerprint correlation to state: same package, same route, same held keys.

**Across records the correlation is deliberate and is not a fingerprint match.** Every `committed_*` column joins this record's points to the committed `20260904-wall-and-heating` record at its own pin (indicator `c1b0f0d1…`, semantic `d468f3b6…`, executable `d4be3951…`) by the nine coordinates a proposal is defined by, never by case id and never by fingerprint; every `cf0915_*` / `cf0940_*` column joins, through the committed case id, to two oracle-side counterfactuals evaluated at that same committed pin with W scaled by a constant. So a row of `points.csv` carries three bases side by side — the WI-042 family executed here; the WI-037 family executed there; the WI-037 family times a constant, oracle-side — and the fingerprints say which is which: nothing with this record's fingerprints was produced at the committed pin, and nothing in a `committed_*` or `cf*` column was produced at this one. **What it means:** a difference between a `committed_*` value and this record's value at the same coordinates is the profile family's effect through the closure and nothing else (the same levers, the same held keys, the same fence, the same calibration, the same route), because WI-042 changed the sustainment and beta calcs and nothing upstream of them (its plan's restatement § MR-WI042-14 (a) and its phase-5 record: every heating, magnet and geometry channel bit-identical at the baseline); a difference between a `cf*` value and this record's value is the profile family's effect minus what a constant scale of W predicted. The join: 6,283 executed points carry a committed case id (`executed_in_both`; every one matched on all nine coordinates exactly, none twice); the 28 committed-executed points absent here are the ones the rule's pre-screen excluded (§ 11; they appear in `results/excluded_points.csv` with their committed case id); no committed exclusion is executed here (all 65 remain excluded); the 1,429 restored 13 keV rows carry no committed case id and no committed exclusion (`not_proposed_by_committed`) and join to nothing..

## 13. Verification

`results/verification_summary.json` (`scripts/study/verify.py`, tool digest `f02358bb…`, tolerance 1e-9, sampling `stratified-by-verdict-combination/v1`): one store, **20 sampled rows stratified over the verdict combinations the whole study produced, 13 channels at relative deviation below 1e-9 (worst 4.25e-16), 9 verdicts re-derived from the oracle's own operands** through the published operand bindings, none disagreeing. The package tree was git-clean after the run (the runbook's step-9 gate).

**What verification did cover:** the store's scalar channels the manifest's objective catalog and the predicate-resolved operands name (LCOE, the 1cfe-form LCOE, CAS27, CAS72, the wall peak, β, B_peak, σ, ε, the magnet rollup, the total capital; `p_fus` is **not** among them — ANNEX § Oracle's known coverage delta), and every constraint's verdict re-derived. Beyond the sample, **the stored-energy column is package evidence at every one of the 7,712 points**: `W_th_MJ_store` (β · B_axis² · 1.5 V / (2 μ0) from three store channels, WI-042 design D2) agrees with the oracle's `W_th_MJ_oracle` to a worst relative deviation of 5.5e-16 (`study.py` `export()` raises above 1e-9).

**What verification did not cover, stated:** the oracle-derived columns are not independently verified — `p_aux_required_MW_oracle` and therefore `ignited` and `feasible_driven`, `tau_E_s_oracle`, `n_e_volav_oracle`, `alpha_n_e_eff_oracle`, `alpha_He_eff_oracle`, He/n_e, the lifetime re-derivations and the shadow columns (identical by construction to the committed study's definitions, inherited, not re-read); the four WI-042 sustainment channels never reach the store (multi-field, ANNEX § Oracle), so their per-point values are the oracle's, guarded only by the oracle's bit-exact agreement with the package at the baseline and under two perturbations (WI-042 plan, phase 5). The `committed_*` and `cf*` columns are **read** from the committed record's and the counterfactuals' own files by coordinates and case id, never recomputed and never verified here; their provenance is § 12 and `snapshot.json` → `committed_record_joined`.

## 14. Review outcomes

| Lens | Verdict | Disposition |
|---|---|---|
| **Pre-execution framing critique** (runbook step 4; a fresh non-author `general-purpose` session from the deposited prompt `work/orchestration/goals/stored-energy-basis/evidence/round2_T-003_precritique_prompt.md`; verbatim at `evidence/round2_T-003_precritique.md`; ran before any point ran, while the first oracle pre-screen was in progress) | **MAJOR** — eleven findings | **All accepted.** F1 (the committed window's T-bottom was fixed by the fence WI-042 relieved; at the rule the driven optimum lies below it): (a) the edges re-read at the rule (`edges.py`, § 11); (b) the T 13 keV rows restored on both geometry grids by a dated scope amendment before any point ran (trail § Amendment 2026-09-05). F2 (the constant-scale counterfactuals do not bracket the rule): joined per point by committed case id and read only as predictions the rule tested; counts at three bases with basis and denominator (§§ 3, 4, 12). F3 (the design column opens at the pinned baseline point only, on three fences at once, at coupling 1.00): stated at the claim site (§§ 3, 6). F4 (the ignited set dominates): `feasible_driven` first, the transitions split (`transition_vs_committed`), a row-`#4` disposition (§ 15). F5 (the execute phase could execute a set other than the screened set): the cache stamped and checked, an existing store refused, the executed set asserted equal to the screened set (`study.py` `run()`). F6 ("the shape scales" is true by construction): stated as met by construction and demonstrated, with what it rests on (§ 6). F7 (W as package evidence): `W_th_MJ_store` from three store channels, asserted against the oracle (§ 13). F8 (four point classes and denominators): `class_vs_committed` on every point and every exclusion (§§ 4, 11). F9 (the counterfactuals' third basis): § 2 and every comparative sentence carry it. F10 (shadow columns): inherited, not re-read, labelled so (§ 6). F11 (exception classes): the screen's reasons classified (§ 11). |
| Correctness (the executor's own re-derivations from `results/`) | executor's pass | Every count in §§ 3, 4, 6, 11, 12 and 15 is derived by one deposited script from `results/points.csv` and `results/excluded_points.csv` (`work/orchestration/goals/stored-energy-basis/evidence/round2_T-003_recount.py`); the fresh administrator recounts independently (a separate session; `synthesis.md`) and the disposition checkpoint re-reads both. |
| Honesty (what the record claims against what `results/` carries) | executor's pass, with the caveats stated | Every reading names its basis (the WI-042 family; the committed WI-037 family; the constant-scale counterfactuals as predictions); the restored rows are a named class and never enter a comparison against the committed record; the counterfactuals are never called bounds; the one-sided fence's ignited set is counted first; the design column's margins are stated on three fences at the held coupling; § 17 lists what is not claimed. |
| Readability (a cold reader's recovery of the question, the bases and the answer) | executor's pass | § 2 carries the question and the three bases; § 3 the answer in two facts; § 6 the per-axis account with the numbers a reader can recompute; the administrator's read is the test. |

## 15. Findings

Each finding gets an id used verbatim in `DISCOVERY_LOG.md` as `20260905-stored-energy-basis#n`. Every number is at the WI-042 profile family unless it says "committed" (the WI-037 family, pin `c1b0f0d1…`, read from the joined columns) or "the scales" (the two constant-scale counterfactuals, predictions).

| Id | Kind | Finding | Disposition | Home |
|---|---|---|---|---|
| `20260905-stored-energy-basis#1` | `model` | **At the paper's own ash rule the committed window's driven set ignites.** Of the committed 681 driven points, 510 have a negative requirement at the rule (235 of 257 at 100 MW, 275 of 400 at 220), 146 stay driven, 25 become violated; over the committed window the driven set is 131 at 100 MW and 181 at 220. Sustainment flips 964 committed violations to satisfied and none the other way; every committed headline driven point ignites (`c1721` −188.8 MW, `c4639` −109.3, `c0821` −60.1). "Feasible" (the one-sided fence) is 1,839 of 7,712 and 3,654 points are ignited, so every count in this record that does not say "driven" is a count the burn-control gap decides. | `model fix` — open, not minted: the one-sided sustainment fence (the committed `#4`) is now load-bearing for every count on this package; a second inequality or a burn-control lever is the model item the owner mints or carries at the round-2 close. Responsible: the round agent at the T-003 return (the disposition rows), the owner at the close. | `goal.md` § Answered when (the demo statement's "on the boundary" rests on the driven reading); the committed `20260904-wall-and-heating#4` row |
| `20260905-stored-energy-basis#2` | `model` | **The driven region at the rule sits one temperature step below the committed window, on the restored 13 keV row, and the study's cheapest driven point is there.** 175 driven points at 100 MW and 191 at 220 on the row, none ignited; cheapest 202.19 $/MWh (`c2823`: R 15.7, a 2.2, I 13 MA, T 13 keV, n 1.0×) and 218.95 at 220; over the committed window alone 221.02 / 240.38. Through the rule's anchor the T transect is sustainment-caught at 12.5 and ignited at 13.5: the driven band at large `a` is about one keV wide, and the executed bottom (13) is one step above a caught edge, not on it; 12.5 is bracketed, not executed. The optimum's `a` is the window's edge (2.2), as committed. | `model fix` — open, unchanged in routing: the committed `#3` (nothing bounds `a`; the magnet account does not price it) stands and now the temperature band is a knife-edge too; a re-anchoring of the calibration and a bound on `a` remain the candidates. **And a study-window finding:** a restated window's edges must be re-read at the new chain (the critique's F1 caught it; the runbook's step 7 obligation applies to an inherited window). Responsible: the round agent (this record's § 11 executed the re-read); the next strategy author for the bound. | the committed `20260904-wall-and-heating#3` row; `.claude/skills/run-study/runbook.md` step 7 (a candidate clarifying sentence, not edited here) |
| `20260905-stored-energy-basis#3` | `model` | **The design column at 100 MW opens at exactly the pinned baseline point and at no grid point, on three fences at once, at the optimistic end of a held coupling.** `c3694`: 49.08 MW against 50 (0.92 MW, 0.2 % of W, inside the source's own 2.7 % residual on its printed stored energy — about 14 MJ, about 15 MW at the slope the counterfactual pairs give), peak 3.979 against 4.05 (0.071), B_peak 24.9 against 24.9 (0.0, the design convention), at `eta_couple_heat` 1.00 (0.98 violates). I 15 MA reads 62.7 MW and 4.072; 16 MA breaks the ceiling. The committed column had 0 driven, the scales predicted 6 / 0, and the 0.940 scale's two nearest points moved in opposite directions at the rule. At 220 MW the column has 47 driven points (committed 26), cheapest 370.55 at `eta_source` 0.60. | `model fix — none owed`: a measurement — the reading is **undetermined within the source's precision, on the boundary**, the round's demo statement, never "feasible"; carried to the T-003 return and the round result with its margins at the claim site. Responsible: the round agent; the owner at the close (the demo statement). | `goal.md` § Answered when (c); `learnings.md` L-001 (its implication line, refined) |
| `20260905-stored-energy-basis#4` | `process` | **The constant-scale counterfactuals did not bracket the rule, per point or in direction.** Over 6,250 comparable committed points the rule's requirement lies below both scales at 3,456, between at 478, above both at 2,316; the W correction is 0.79–1.02 (mean 0.919), not 0.915 or 0.940; at the baseline the rule (49.1) lies between the scales (37.5 / 51.4) with a W ratio above both; at `c1721` the scales predicted +5.9 / +4.7 MW and the rule gives −188.8. The sign of the design-point move the counterfactual predicted held; the location did not, almost everywhere else — exactly the caveat `NOTES.md` § 6 stated. | `declared seam` — standing: a constant-scale counterfactual on this closure predicts signs at the point it is anchored on and nothing about locations elsewhere; the goal's L-001 scope line already says so and is sharpened by this record (the learning delta of the round-2 result). Responsible: the round agent (the learning delta); the fresh review. | `learnings.md` L-001 (scope line); `evidence/w_counterfactual/NOTES.md` § 6 |
| `20260905-stored-energy-basis#5` | `model` | **The stored-energy correction across the window follows the ash fraction, from +1.8 % to −21 %, and the ash shape's exponent is a function of temperature alone.** `W_ratio_vs_committed` 0.788–1.018, mean 0.919 over 6,283 points; by He/n_e bin monotone from 1.016 (0.04) to 0.79 (0.27); by geometry 0.975 (R 14.2, a 1.3) to 0.826 (R 11.2, a 2.2); 145 points above 1.0 where the ash is scarce (He/n_e ≤ 0.062) and the derived electron profile is flatter than the retired 0.596. `alpha_He_eff` 4.268 / 4.052 / 3.885 / 3.770 / 3.661 at 13 / 14.63 / 16 / 17 / 18 keV at every geometry and every τ*/τ_E. `W_th_MJ_store` = the oracle's W to 5.5e-16 everywhere. | `model fix — none owed`: a measurement — the owner's scaling clause is met by construction and demonstrated on the package (SV-056's executed complement); what it rests on (α_n, α_T, Ti/Te, τ* uniform, f_suppr × τ*/τ_E, ι — all point-A facts) is stated in § 6 and is the committed `#2`'s research request. Responsible: the round agent (the learning delta). | `learnings.md` L-003 (refined); `work/active/WI-042_sourced-helium-ash-profile/` SV-056 |
| `20260905-stored-energy-basis#6` | `model` | **The closure's validity edge moved inward by 28 committed points, fewer than the 0.940 scale's 35, in the same corner.** The committed 65 exclusions all stand; 28 committed-executed points (R 11.2–12.7, 18 MA and some 16, a 1.3 at n 1.0× or a 2.0–2.2 at n 0.6–0.9×) and 71 restored 13 keV rows fail the oracle's fuel guard (94 non-positive fuel) or return a non-real value (70) at the rule; none of the 28 was feasible, ignited or driven in the committed record. | `declared seam` — unchanged, standing (the committed `#8`): the closure has no bound of its own at extreme confinement and fails as an exception; the edge is now one step further in; the ANNEX § Validity masks candidate stands. Responsible: none new; re-sighted. | the committed `20260904-wall-and-heating#8` row; `exploration/stellarator_e2e/studies/ANNEX.md` § Validity masks (candidate) |
| `20260905-stored-energy-basis#7` | `model` | **Round 1's 220 MW grid at the rule: 45 driven (committed 24), LCOE up about 2.8 % at every point, the cheapest driven 370.55.** 7 / 9 / 13 / 16 driven at `eta_source` 0.45 / 0.50 / 0.55 / 0.60 (committed 1 / 3 / 8 / 12; the scales 58 / 58); 8 ignited; 11 of 279 committed wall violations flip to satisfied; the committed cheapest `c6256` (378.556) reads 389.50 and stays driven; the rule's cheapest `c7625` (I 14.25 MA, T 17 keV, n 0.8×, 0.60) was violated in the committed record at 360.90. The machine as designed pays 370.55 for its cheapest driven 220 MW point at the rule. | `model fix — none owed`: a measurement, to the committed `#6` row (the point-by-point crossing of the WI-041 boundary is now the point-by-point crossing of the WI-042 boundary too) and to the round result. Responsible: the round agent. | the committed `20260904-wall-and-heating#6` row |
| `20260905-stored-energy-basis#8` | `model` | **The ash-transport knife-edge moved up by about a factor 1.5 at the rule.** Through the scan's best 100 MW anchor the committed record was driven only at τ*/τ_E = 8, ignited at 6, sustainment-blocked at 12; at the rule it is ignited at 2–6, driven at 12 (25.6 MW required, peak 3.115) and blocked at 16 (74.6); at 220 MW driven at 12 and 16; on the design column ignited at 2–4, wall-blocked at 6, sustainment-blocked at 12–16 (85.6 / 109.7 MW; committed 158.4 / 205.8). The shape's exponent does not move with the ratio; the amount does (He/n_e 0.04–0.18 on the design column). | `research` — open, unchanged in class and routing (the committed `#2`: the aspect-ratio scaling of the held transport facts); its location is W-basis-sensitive as L-001 says and this record shows a second time. Responsible: the owner at the close (the research request the committed round carried). | the committed `20260904-wall-and-heating#2` row |

**Homes a finding may route to:** tool, runbook step, policy rule, skill, modeling item, research round, documented seam. `unrouted` is a stated state, not a blank; no row above is unrouted.

## 16. Snapshot

- **File:** `snapshot.json`
- **sha256:** `b174a8a6416fb4e0a99e0c29111be18cb7fb30f861bf65a9c7aaa706e5ace68e`
- **Schema version:** `1`

No snapshot content is restated here. It carries the three package fingerprints and the sealed executable fingerprint; the manifest digest with the tie, baseline (both verdicts expected satisfied), objective catalog and oracle content actually used, the oracle's own source digest included; the one store's complete compatibility tuple (7,712 cases, 7,712 completed); per-arm windows (inherited, engineered; the restored 13 keV row named) with their `effective_executable_fingerprint`, verification block and artifact digests; the digests of every file under `results/`, of `indicators.json`, `axes.json`, `study.py`, `scan.py`, `edges.py` and the three study tools; **the digests of the committed record's three results files and of the two counterfactual CSVs the `committed_*` and `cf*` columns were read from** (`committed_record_joined`); the preflight outcome; the counts per arm and per class; the teax revision. Built by `work/orchestration/goals/stored-energy-basis/evidence/round2_T-003_snapshot_builder.py` (deposited so every value can be re-derived).

## 17. What this record does not contain

- **No claim that the rule's driven optimum is buildable.** It sits at the window's `a`-edge (2.2), at an aspect ratio the peak calibration, the ash-transport facts and the QI shape were never anchored on, and one temperature step above a fence-caught edge in a band about one keV wide; the model has no bound on `a` and no burn-control lever.
- **No claim about ignited operation.** 3,654 points have a negative requirement and pass the fence as modeled; every LCOE claim here is on the driven set, and the ignited set is counted, never claimed feasible.
- **No boundary in `a` above, and no executed point below 13 keV.** 12.5 keV is bracketed by the transects at the rule's anchor, not executed; 2.4 m is driven at the anchor and not executed.
- **No claim that the counterfactuals bound anything.** The `cf*` columns are what two constant scales predicted at the committed points, joined for the record to test; § 4 and § 15 #4 say what the test found.
- **No re-derivation of the shadow columns or the lifetime chain.** `lcoe_magnet_shadow`, `wall_load_shadow_*`, `core_life_fpy_from_peak`, `lifetime_charge_above_limit_per_MWh` are the committed study's definitions computed on this run's channels — inherited, not re-read, not claimed.
- **No transect anchored on the rule's best point.** The ash transect's anchors are the committed three (the committed scan's best points and the design column), so its readings are about those coordinates; the rule's own cheapest point was not transected.
- **No re-run of the window scan.** The window is the committed one plus the restored row; the edges were re-read at the rule (§ 11); a window fixed from the new package's own scan would be a different study.
- **No independent verification of the oracle-derived columns** (§ 13), and the four WI-042 sustainment channels never reach the store.
- **The `committed_*` and `cf*` columns are read, not verified**, from files whose digests the snapshot carries; a reader who doubts them reopens those files at `a5b0b96a`, `3687d2f6` and `498a329e`.
- **The teax revision that executed this run** is `744745f895677f3344b9884627369a6a47ed987f`, from the teax checkout and the round's integration return.
- **`results/_work/`** (the screen cache and the baseline executor's working directory) and the study store under `studies/_work/` are gitignored and are not evidence; every value cited is in `results/`.
- **`study.py`'s docstring carries the committed definition's own text under its restatement preamble**, including sentences that describe the committed study's revisions ("REVISED after the pre-execution framing critique…") — the committed words, kept as digested and executed rather than edited, as the committed record kept its own stale paragraph (`20260821-power-cycle-ab#9` precedent); this record's own revisions are the ones in its docstring's first paragraph and in § 11 and § 14.
- **The execution ran once, detached** (`setsid nohup`), after the pooled screen; the first two screen launches failed at load on a path slip in `study.py` before any oracle evaluation and left nothing (recorded in the study definition's commit history: `5ab0452c`, `dcc77967`); no point ran twice and no store existed before the execute phase (the guard the critique's F5 asked for).
