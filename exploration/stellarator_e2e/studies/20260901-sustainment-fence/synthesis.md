# Synthesis — 20260901-sustainment-fence

- **Administrator:** fresh administrator session, 2026-09-01 (no execution context inherited)
- **Date:** 2026-09-01
- **snapshot.json sha256 read:** `5bf3cfba5880d29229dbce6e0049bc48c6f040a8fb975f0a1d1fa86956e7a0ae` (recomputed by this administrator; matches `record.md` § 16)

This synthesis is written from the record directory only: `record.md`, `snapshot.json`, `indicators.json`, `axes.json`, `study.py`, `scan.py`, and `results/*`. Facts are labeled recorded, recounted (recomputed here from committed artifacts), or the administrator's interpretation.

## What the study set out to do

The owner-delegated question, quoted verbatim in `record.md` § 2: with the sustainment fence active, is field rewarded — does the constrained optimum leave the beta floor — and where do the power, beta, wall-load, conductor-ceiling, and stress fences bind over (I_coil/B, n_e0, T_i0) and geometry. The quote's provenance is the round-2 strategy revision under the owner's full delegation; the trail document it cites is outside this directory and the provenance is taken as recorded, not verified.

The executor's marked additions (§ 2): geometry held at the baseline machine (R 12.7, a 1.3, wp_side 0.36) — the (R, a) structure belongs to the two prior fence studies; sustainment facts (iota_23, f_ren, profile exponents, synchrotron facts, and the rest) are package-held sourced values, not swept; the legacy `n_e` sibling key is disclosed as inert only while the 0D bypass is off; and the baseline's exact conductor-ceiling equality (B_peak 24.90 vs B_max 24.9, one ulp low) is stated as a design-point fact. Recounted: the baseline row's B_peak is 24.899999999999995 in `results/points.csv`.

Four arms in one store (`snapshot.json` arms[], `results/points.csv`): an (I_coil 8–24 MA × n_e0 0.6–1.4×) grid at the printed installed heating p_input = 50 MW, the same grid at 110 MW (added on the pre-execution critique's finding 1 so the optimum question is answerable), a T_i0 transect 6–30 keV, and a p_input transect 30–150 MW with the newly declared p_ecrh tie riding. All windows declared `engineered`, fixed after the oracle scan in `results/window_scan.json`. Note: `record.md` § 1 lists only three arms; the fourth (`arm-grid-I-ne-p110`) exists in the snapshot, the CSV, and § 2's narrative — a § 1 bookkeeping defect, not a substantive one.

## What it found

- **At 50 MW installed heating the feasible set is empty — everywhere.** Recounted: 154 evaluated p=50 grid rows (baseline included), 0 feasible. 29 points do satisfy `sustainment_ok` (I ≥ 18 MA), and every one of them violates `peak_field_ok`. The deadlock the record claims — sustainment relief and the conductor ceiling exclude each other's territory — reproduces exactly. This emptiness was pre-registered before execution (§ 5, § 14) and is reported as the study's first-order H1 finding, not as a boundary-mapping success.
- **Baseline:** LCOE 307.087 $/MWh, `sustainment_ok` violated, infeasible. Recounted: the single `is_baseline_point` row reads 307.08712042841586 with sustainment violated, matching the manifest pin copied into `snapshot.json` (the pinned baseline verdict is itself "violated" — the study was launched knowing the printed operating point fails its own sustainment check).
- **Best feasible point: 293.468 $/MWh at (I_coil 15.0 MA, n_e0 1.1× = 5.566e20, T_i0 14.63 keV, p_input 110 MW).** Recounted from `results/points.csv`: min feasible LCOE = 293.4681…, beta 0.0311, p_fus 3270 MW. It beats the (infeasible) baseline headline by 4.43% while buying 2.2× the installed heating.
- **The constrained optimum is off the beta floor.** Recounted: optimum beta 0.0311 against the 0.05 limit (`results/oracle_operands.csv` bounds columns); at p=110 the feasible I-band is bounded below by `sustainment_ok` (I = 14 fails, 15 passes) and above by `peak_field_ok` (I = 16 fails on the ceiling); `beta_ok` binds only at I ≤ 11 MA at baseline density. The record's headline reading — field is rewarded through confinement and the stopping fence is magnet technology, not plasma stability — is supported by the committed evidence. (Its framing as the inversion of `20260823-magnet-technology-ab#4` references a prior record not in this directory; unverifiable here.)
- **Nine feasible points total, spanning 293.5–415.3 $/MWh.** Recounted: exactly 9 of 334 evaluated rows are feasible; LCOE over all evaluated points spans 127.81–112,220 (the low end and the absurd high end are both deep-infeasible territory).
- **The sustainment relief is non-monotonic in field.** Recounted from `results/points.csv` at p=50, n_e0 1.0×: `sustainment_ok` is satisfied at I = 18–21 MA and violated again at 22–24 — the executed grid confirms the window scan's synchrotron-reclaim structure (`results/window_scan.json` I-edge flips 17→18 and 21→22).
- **Installed heating is pure cost once sustainment is met.** Recounted: on the p transect the feasible points' LCOE rises strictly 354.336 → 415.256 over p = 100 → 150, and the heating-capital column is exactly linear at 5,282,900 $/MW across the transect — the executed evidence behind finding #4.
- **The ~91 MW threshold.** Recounted: `sustainment_ok` flips violated → satisfied between p = 90 and 100 MW on the transect, and the oracle's `p_aux_required` is constant at 90.6 MW along it — consistent with the recorded ≈ 91 MW oracle threshold.
- 10 proposed points were excluded by the fail-loud pre-screen and counted infeasible-by-exclusion: recounted, 3 on the T transect (T = 6–8 keV) and 7 in the p=110 grid's low-density/high-I corner; 344 proposed = 334 evaluated + 10 excluded, matching `snapshot.json` counts.

## Framing verdict per axis

All four swept axes proposed `search`, judged `search`, unchanged (§ 5). The administrator concurs on the committed evidence:

- `I_coil` — search, upheld: the p=50 slice is empty exactly as pre-registered, and the p=110 slice has a bounded feasible band with a constrained optimum. Recounted as above.
- `n_e0` — search, upheld: at (I = 15.0 MA, p = 110) the feasible density band is [0.9×, 1.1×] — recounted: `recirc_ok` violated at ≤ 0.8×, `wall_load_ok` violated at ≥ 1.2× (wall load 4.306 vs limit 4.05), and the optimum sits at the band's top.
- `T_i0` — search, upheld: no feasible point on the transect (recounted, 0 of 22), `sustainment_ok` violated at every evaluated T, with `recirc_ok` additionally violated at T = 9–11, `wall_load_ok` from 17, `beta_ok` from 28 — the two fence crossings match the window scan flips (16→17, 27→28).
- `p_input+tie` — search, upheld: the flip was found between 90 and 100 MW and its price measured. Recounted as above.
- Declined axes `R+tie` and `a` were declared at full group completeness, traced through the indicator run, and held. Recounted from `indicators.json`: R+tie reaches 7 constraints, `a` reaches 5. No axis — swept or declined — reported `no_constraint_response` (recounted: I_coil 7, n_e0 5, T_i0 5, p_input+tie 3 reachable constraints), so no owner ruling and no model-gap finding were owed under runbook step 4.

## Constraint structure

All eight § 4 violation counts reproduce exactly from `results/points.csv` (334 evaluated rows):

| constraint | recorded | recounted |
|---|---|---|
| `sustainment_ok` | violated 219/334 | 219 — match |
| `peak_field_ok` | violated 155/334 | 155 — match |
| `wp_stress_ok` | violated 119/334 | 119 — match |
| `recirc_ok` | violated 101/334 | 101 — match |
| `wall_load_ok` | violated 96/334 | 96 — match |
| `beta_ok` | violated 69/334 | 69 — match |
| `net_positive` | violated 0/334 (by construction — pre-screen excludes p_net ≤ 0) | 0 — match; the 10 exclusions are the fence's true boundary, as § 4 states |
| `tbr_ok` | violated 0/334 (held-vs-held, inert) | 0 — match |

Constraint identities are qualified throughout (`constraint_id` + `source_local_identity` in § 4, `results/baseline_result.json`, and `results/verification_summary.json`), and the bounds a fence claim is judged against are committed per-row in `results/oracle_operands.csv` (beta 0.05, wall 4.05, B_max 24.9 T, sigma_allow 800 MPa, recirc 0.5 — read from the package's inputs, not hardcoded, per `study.py`).

Preflight (§ 9, `results/preflight_results.json`): all six gates ran, all pass — declared keys (8 across 6 groups), sibling scan, identity (sealed digest `41e06ecb…` recomputed, zero adapter sources), manifest currency, baseline headline (relative deviation 0.000e+00, 8/8 pinned verdicts including `sustainment_ok` violated), package git-clean. Recounted: the preflight file's input digests match this directory's `results/baseline_result.json` and `results/package_identity.json` hashes.

Verification (§ 13, `results/verification_summary.json`): outcome pass; 21 rows sampled covering all 21 observed verdict combinations (the stratification cannot miss a produced verdict); 12 channels vs the package-owned oracle at worst relative deviation 5.33e-16; all 8 constraints re-derived from published operand bindings with zero mismatches, `sustainment_ok` with both operands resolved. Recounted from the summary file: cases_total = cases_completed = 334, strata_observed = sampled_rows = 21, constraint list of 8 confirmed. The § 13 prose names what verification did not cover — the six sustainment quantities in `oracle_operands.csv` are oracle-derived on both sides (the store cannot record multi-field module outputs), `p_fus` sits outside generic coverage, and the 10 excluded points were never executed. Note: the summary JSON's own `not_independently_verified` field is empty; that disclosure lives only in the record prose.

Integrity recount: every digest in `snapshot.json` `results_artifacts` matches its file on disk (all 8), `indicators.json` matches its snapshot digest, and the snapshot's own sha256 matches § 16. Route per § 10: study-local direct-API, no adapter, glue ledger none — consistent with `results/package_identity.json` (empty allowed-modified and adapter lists). Single store, single executable fingerprint; § 12 bars in-record comparisons across the WI-037 semantic boundary and makes none.

**Administrator-found discrepancies** (none overturns a § 3/§ 6 conclusion):

- § 4's note that `sustainment_ok` is satisfied "in a 15–21 MA band at p=110" does not reproduce: at p=110 the constraint is satisfied from I = 15 MA up to 24 MA at most densities (to 22 at 1.4×; the 0.6× column's I ≥ 19 points were excluded pre-screen). The feasibility band [15.0, 15.4] MA (§ 6) is correct — its upper bound is the ceiling, not sustainment.
- § 6's parenthetical "(required ≈ 72–139 MW vs 50)" for the below-ceiling p=50 grid does not recompute under any reading tried: the below-ceiling required-aux range in `oracle_operands.csv` is 90.6–1566 MW (per-I minima 90.6–759.9). The substantive claim stands — every below-ceiling p=50 point requires more than 50 MW, with minimum 90.6 MW in the grid and 72.4 MW anywhere at p=50 (the T transect) — but the quoted range is not a committed number.
- The required-aux minimum on the T transect is 72.4 MW at T = 17 in `oracle_operands.csv`, not "≈ 72.6 near T = 18" (§ 6); § 17's "monotone-falling required-aux from 9 → 18 keV" is off by one grid point (falling 9 → 17, rising at 18). Ulp-scale of the claim's substance; the stated digits are wrong.
- Arm labeling artifact: `study.py`'s `arm_of` classifies any p = 110 point as the p110 grid arm, so the p transect's own p = 110 sample (I = 15.4 MA — not a grid I-value) is labeled `arm-grid-I-ne-p110` in `points.csv`. Consequences: the "four-point feasible region" at p=110 includes that off-grid transect point (the grid proper contributes three, all at I = 15.0), and the transect's "p ∈ [100, 150] fully feasible" claim relies on that mislabeled row for p = 110 (recounted: its LCOE 365.256 does fit the strict rise). The evidence is all committed; only the arm attribution is misleading.
- § 1 lists three arms; the record executed four (above).
- The low-T exclusion is described as "p_net complex" (§ 2, § 11), but `excluded_points.csv` records p_net as real-negative (`p_net_is_complex` False) for T = 6–8 — the screen caught p_net ≤ 0 before the CAS10 sqrt went complex — while `results/window_scan.json` shows the same points raising a complex-value TypeError on the scan's path. Both artifacts are committed; the record blurs the two failure surfaces of the same WI-034 defect.
- The package path is spelled two ways: `exploration/stellarator_e2e/pkg/stellarator_tea` (`snapshot.json` package block) vs `exploration/stellarator_e2e/generated` (`package_identity.json`, `preflight_results.json`, `verification_summary.json`). All fingerprints agree (`41e06ecb…` throughout), so the administrator's reading is that both names resolve to the same sealed tree — the same defect the `20260830-stress-fence` synthesis noted; the record still never says so.
- `results/verification_summary.json` records `teax.revision` as "unrecorded", and unlike the prior record this snapshot carries no teax revision either: the teax library revision that executed the run is not recorded anywhere in this directory.

## Findings carried forward

From `record.md` § 15, verbatim in substance. The findings register is complete (ids, kinds, dispositions, homes all present). The DISCOVERY_LOG rows § 15 asserts are joined to these ids live in `DISCOVERY_LOG.md`, outside this record directory: **all four rows are unverifiable-from-here** and are reported as asserted, not confirmed.

- `20260901-sustainment-fence#1` (`model`) — at the printed 50 MW installed heating the baseline machine has no feasible operating point anywhere in the swept space: below the ceiling every point fails sustainment, and every sustainment-satisfying field sits over the ceiling/stress fences. The deadlock is between the ISS04 relief (~B^-2.15) and the held conductor grade (B_max 24.9, sourced REBCO). Feasibility returns at ≥ ~91 MW installed or, presumably, at a different conductor grade — the unswept axis. Disposition: first-order H1 reading, pre-registered; carried to the goal round result and this synthesis. Home: goal `operating-point-closure` round result; candidate follow-on a conductor-grade (B_max) arm or a heating-system item. Recounted: fully supported by `points.csv` (0/154 feasible at p=50; all 29 sustainment-satisfied points over the ceiling).
- `20260901-sustainment-fence#2` (`model`) — the CAS10 land term (√p_net) re-sighted: 10 points fail to evaluate before any verdict is written, now on the temperature axis. Disposition: excluded-by-pre-screen with converged sustainment operands exported; counted infeasible-by-exclusion in every denominator. Home: modeling item WI-034 (standing). Recounted: `excluded_points.csv` carries exactly 10 rows with reasons, p_net values, and sustainment operands. That this is the *third* committed sighting, and WI-034's state, are outside this directory.
- `20260901-sustainment-fence#3` (`process`) — the sustainment quantities are fields of one multi-field module and cannot reach the evidence store (the pb__* limitation); the first export shipped six empty columns, corrected pre-commit by the oracle-side re-export. Disposition: `points.csv` carries no sustainment columns; `oracle_operands.csv` carries them oracle-labelled. Home: documented seam, `ANNEX.md § Oracle`. Recounted: the committed CSVs match the described end-state; the "first export" and its correction left no artifact here, and the ANNEX is outside this directory — both taken as recorded assertions.
- `20260901-sustainment-fence#4` (`model`) — installed heating is pure cost once sustainment is met: the heating account is per-MW capital with no system structure, so heating buys nothing but the sustainment verdict and recirculation burden; a heating-system model (rubric Row 4) would change the trade. Disposition: disclosed at the claim site (§ 6). Home: **unrouted** — a stated state, per the runbook. Recounted: heating capital is exactly linear at 5,282,900 $/MW and feasible-transect LCOE rises strictly; the executed evidence is committed.

Review outcomes (§ 14): the pre-execution framing critique (fresh non-author session) returned MAJOR — 3 major, 4 minor — with all seven findings dispositioned before any point ran; the added p=110 slice, the traced declines, the exclusion-denominator rule, and the pre-registered no-crossing T result all trace to it. The critique's full text is deposited outside this directory and is unverifiable here; its recorded dispositions are all visible in the executed design. The post-run correctness/honesty/readability lenses are the executor's own passes and claim no independence — this synthesis is the record's independent reading.

## What the record does not support

- **Nothing at 50 MW is feasible, but this study cannot say what fixes it cheapest.** The conductor grade (B_max) was never an axis; finding #1's "presumably, at a different conductor grade" is explicitly a presumption. The committed alternative — buy ≥ ~100 MW of heating, or re-optimize at 110 MW for 293.5 $/MWh — is the only evidenced route to feasibility.
- **No geometry claim.** R and a were declined; the p=110 optimum is an optimum of this machine at this installed power, not of the design space (§ 17, and the engineered-window caveat of § 11: structure inside the frame, not the frame's rightness).
- **No sustainment-fact sensitivity.** f_ren = 1.0 and the other held facts carry undisplayed uncertainty; the record itself says a ±f_ren arm would move the ~91 MW threshold materially, and none was run. The threshold's precision is one-grid-step (90 < flip ≤ 100 MW committed; "≈ 91" is oracle-derived).
- **No physics claim about T ≤ 8 keV.** That band died on the WI-034 evaluability defect, not on a verdict; and the T = 8.32 up-crossing from round 1's analysis is not re-exhibited — the committed transect starts at 9 keV, and the unstable-branch reading rests partly on round-1 material outside this directory.
- **No per-point sustainment values exist anywhere but the oracle's own export.** `oracle_operands.csv` is oracle-derived on both sides for the six sustainment quantities (§ 13); they are consistent with the verified verdicts but are not independently verified numbers.
- **Cross-record comparisons are barred and unverifiable.** The WI-037 semantic boundary (§ 12) means prior-record LCOEs are not comparable in-record; the quoted baseline shifts (p_fus −0.83%, LCOE +0.86%) and the `20260823-magnet-technology-ab#4` pathology this study inverts are outside this directory.
- **Unverifiable-from-here by construction:** the four DISCOVERY_LOG first-sighting rows; the intake quote's provenance trail; the pre-execution critique text; the ANNEX sections the record cites (Oracle, Baseline pin, Declared ties — though the two tie declarations themselves are copied into `snapshot.json` and were read there); WI-034/WI-035/WI-037 item state; the uncommitted per-point store (`_work/…/20260901-sustainment-fence.db` — `points.csv`, `oracle_operands.csv`, and `excluded_points.csv` were sufficient for every recount above).
- **Numbers this administrator could not reproduce from the committed artifacts:** the § 6 "≈ 72–139 MW" required-aux range, the § 4 "15–21 MA band at p=110", and the "≈ 72.6 MW near T = 18" minimum (committed values: 90.6–1566 MW below-ceiling, satisfied-to-24-MA, 72.4 MW at T = 17). Filed here as record defects per the contract; the conclusions they decorate survive on the recounted numbers.
- **The teax revision that executed the run is unrecorded** in this directory (summary says "unrecorded"; the snapshot carries none).
